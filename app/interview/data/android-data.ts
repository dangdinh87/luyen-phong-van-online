import type { QAItem } from "../interview-data";

export const ANDROID_DATA: QAItem[] = [
  // ─── KOTLIN CƠ BẢN ───────────────────────────────────────────────────────────
  {
    id: 3001,
    category: "Kotlin",
    subcategory: "Kotlin Cơ Bản",
    level: "beginner",
    q: "`val` và `var` trong Kotlin khác nhau như thế nào?",
    a: "`val` khai báo biến chỉ đọc, không thể gán lại sau khi khởi tạo (tương tự `final` trong Java), còn `var` khai báo biến có thể thay đổi giá trị. Dùng `val` làm mặc định giúp code an toàn hơn và phù hợp với nguyên tắc immutability trong kiến trúc MVVM. Kotlin compiler khuyến khích dùng `val` ở mọi nơi có thể.",
    q_en: "What is the difference between `val` and `var` in Kotlin?",
    a_en: "`val` declares a read-only variable that cannot be reassigned after initialization (like `final` in Java), while `var` declares a mutable variable. Using `val` by default improves code safety and aligns with immutability principles used in MVVM architectures. The Kotlin compiler encourages using `val` everywhere possible.",
  },
  {
    id: 3002,
    category: "Kotlin",
    subcategory: "Kotlin Cơ Bản",
    level: "beginner",
    q: "Giải thích null safety trong Kotlin và sự khác biệt giữa kiểu nullable và non-nullable.",
    a: "Trong Kotlin, biến mặc định không được phép null để loại bỏ NullPointerException. Muốn cho phép null, bạn khai báo kiểu nullable bằng `?` (ví dụ `String?`). Compiler bắt buộc bạn xử lý null ngay lúc biên dịch qua các toán tử như `?.` (safe call) hoặc `!!` (ép buộc). Điều này giúp các lỗi liên quan đến null không thể lọt qua bước compile.",
    q_en: "Explain Kotlin null safety and the difference between nullable and non-nullable types.",
    a_en: "In Kotlin, variables are non-nullable by default to eliminate NullPointerException. To allow null values, declare a nullable type with `?` (e.g., `String?`). The compiler forces you to handle null cases at compile-time using safe operators like `?.` (safe call) or `!!` (unsafe cast). This makes null-related bugs impossible to pass compilation.",
  },
  {
    id: 3003,
    category: "Kotlin",
    subcategory: "Kotlin Cơ Bản",
    level: "beginner",
    q: "Extension function trong Kotlin là gì và hoạt động như thế nào?",
    a: "Extension function cho phép bạn thêm method mới vào class có sẵn mà không cần kế thừa hay sửa code gốc. Ví dụ có thể thêm method thẳng vào class `String`: `fun String.isPhoneNumber(): Boolean { return this.length == 10 }`. Tên hàm được đặt trước bằng tên class cần mở rộng, và `this` trỏ đến instance đang gọi. Cực kỳ hữu ích trong Android khi cần tạo helper method cho các class của platform.",
    q_en: "What are extension functions in Kotlin and how do they work?",
    a_en: "Extension functions allow you to add new methods to existing classes without inheritance or modifying the original class. For example: `fun String.isPhoneNumber(): Boolean { return this.length == 10 }`. The function name is prefixed with the class you're extending, and `this` refers to the calling instance. They're extremely useful in Android for creating helper methods on platform classes.",
  },
  {
    id: 3004,
    category: "Kotlin",
    subcategory: "Kotlin Cơ Bản",
    level: "beginner",
    q: "Data class trong Kotlin là gì và nó tự động tạo ra những gì?",
    a: "Data class tự động sinh ra các method boilerplate như `equals()`, `hashCode()`, `toString()` và `copy()`. Data class cần ít nhất một tham số trong constructor và rất phù hợp để chứa dữ liệu (như response API hay model database). Chúng không thể là abstract, open, sealed, hay inner class, và tất cả tham số constructor phải được đánh dấu `val` hoặc `var`.",
    q_en: "What is a data class in Kotlin and what does it auto-generate?",
    a_en: "A data class automatically generates boilerplate methods like `equals()`, `hashCode()`, `toString()`, and `copy()`. Data classes require at least one constructor parameter and are perfect for holding data (like API responses or database models). They cannot be abstract, open, sealed, or inner classes, and all constructor parameters must be marked as `val` or `var`.",
  },
  {
    id: 3005,
    category: "Kotlin",
    subcategory: "Kotlin Cơ Bản",
    level: "intermediate",
    q: "Sealed class trong Kotlin là gì và có ứng dụng gì trong Android?",
    a: "Sealed class giới hạn một class hierarchy thành tập hợp subclass cố định, được định nghĩa trong cùng một file hoặc package. Rất phù hợp để biểu diễn các trạng thái giới hạn như response API (Success/Error/Loading). Khi dùng với `when`, compiler bắt buộc xử lý hết tất cả subclass, không cần nhánh `else`, giúp code an toàn hơn rất nhiều.",
    q_en: "Explain sealed classes and their use case in Android.",
    a_en: "Sealed classes restrict a class hierarchy to a known set of subclasses defined in the same file or package. They're perfect for representing restricted type hierarchies like API responses (Success/Error/Loading). When used with `when` expressions, the compiler forces you to handle all possible subclasses, eliminating the need for an `else` clause.",
  },
  {
    id: 3006,
    category: "Kotlin",
    subcategory: "Kotlin Cơ Bản",
    level: "intermediate",
    q: "Các scope function (`let`, `run`, `apply`, `also`, `with`) là gì và khi nào dùng từng loại?",
    a: "Scope function thực thi một block code trên một object trong một scope tạm thời. `let` chạy code chỉ khi object khác null (trả về kết quả lambda). `run` giống `let` nhưng dùng `this` thay vì tham số. `apply` cấu hình object và trả về chính object đó (dùng khi khởi tạo). `also` giống `apply` nhưng truyền object vào làm tham số. `with` giống `run` nhưng cú pháp khác. Chọn loại dựa vào nhu cầu null-safety, giá trị trả về, hay cách tham chiếu object.",
    q_en: "What are scope functions (let, run, apply, also, with) and when to use each?",
    a_en: "Scope functions execute code on an object within a temporary scope. `let` executes code only if non-null (returns lambda result). `run` is like `let` but uses `this`. `apply` configures and returns the object (good for initialization). `also` is like `apply` but passes object as parameter. `with` is like `run` but called differently. Choose based on null safety, return value, or object reference needs.",
  },
  {
    id: 3007,
    category: "Kotlin",
    subcategory: "Kotlin Cơ Bản",
    level: "intermediate",
    q: "Coroutine là gì và tại sao tốt hơn callback?",
    a: 'Coroutine là các hàm nhẹ có thể tạm dừng và tiếp tục, cho phép lập trình bất đồng bộ hiệu quả mà không block thread. Khác với callback dễ dẫn đến "callback hell", coroutine cho phép viết code bất đồng bộ trông như code tuần tự, dễ đọc hơn nhiều. Một thread có thể chạy hàng nghìn coroutine nhờ cơ chế suspension, tiết kiệm bộ nhớ và tăng hiệu suất đáng kể.',
    q_en: "What are coroutines and why are they better than callbacks?",
    a_en: 'Coroutines are lightweight functions that can be suspended and resumed, allowing efficient asynchronous programming without blocking threads. Unlike callbacks which lead to "callback hell," coroutines let you write asynchronous code that looks sequential. A single thread can run thousands of coroutines due to suspension, saving memory and improving performance.',
  },
  {
    id: 3008,
    category: "Kotlin",
    subcategory: "Kotlin Cơ Bản",
    level: "intermediate",
    q: "Sự khác nhau giữa `launch` và `async` trong Kotlin coroutines?",
    a: "`launch` là coroutine fire-and-forget, trả về `Job` và không trả về kết quả. `async` dùng khi cần thực hiện tác vụ và lấy kết quả về, trả về `Deferred`. Dùng `launch` cho tác vụ độc lập (như cập nhật UI), còn `async` khi cần kết quả trả về (như fetch data từ network). Phải gọi `await()` trên Deferred để lấy kết quả.",
    q_en: "Explain the difference between `launch` and `async` in Kotlin coroutines.",
    a_en: "`launch` is a fire-and-forget coroutine that returns a `Job` and no result. `async` is used when you need to perform a task and get its result back, returning a `Deferred` object. Use `launch` for independent tasks (like updating UI), and `async` when you need the result (like fetching network data). You must call `await()` on Deferred to retrieve the result.",
  },
  {
    id: 3009,
    category: "Kotlin",
    subcategory: "Kotlin Cơ Bản",
    level: "intermediate",
    q: "Suspend function trong Kotlin là gì?",
    a: "Suspend function là hàm được đánh dấu bằng từ khóa `suspend`, có thể bị tạm dừng và tiếp tục bởi coroutine. Chúng chỉ có thể được gọi từ suspend function khác hoặc trong một coroutine scope. Suspend function là nền tảng của Kotlin coroutines, cho phép viết code bất đồng bộ theo kiểu tuần tự. Ví dụ điển hình là bất kỳ hàm nào được đánh dấu `suspend` bên trong `viewModelScope.launch {}`.",
    q_en: "What are suspend functions in Kotlin?",
    a_en: "Suspend functions are functions marked with the `suspend` keyword that can be paused and resumed by coroutines. They can only be called from other suspend functions or within a coroutine scope. Suspend functions are the building blocks of Kotlin coroutines, allowing you to write asynchronous code sequentially.",
  },
  {
    id: 3010,
    category: "Kotlin",
    subcategory: "Kotlin Cơ Bản",
    level: "intermediate",
    q: "Kotlin xử lý exception trong coroutine như thế nào?",
    a: "Bạn có thể dùng try-catch thông thường bên trong coroutine để xử lý exception cục bộ. Với exception không được bắt, dùng `CoroutineExceptionHandler` để bắt ở cấp coroutine scope. Trong Android, `viewModelScope` tự động xử lý exception bằng cách log chúng. Structured concurrency đảm bảo exception trong coroutine con được truyền lên đúng cách cho scope cha.",
    q_en: "How does Kotlin handle exception handling in coroutines?",
    a_en: "You can use standard try-catch blocks within coroutines to handle exceptions locally. For uncaught exceptions, use `CoroutineExceptionHandler` to catch them at the coroutine scope level. In Android, `viewModelScope` automatically handles exceptions by logging them. Structured concurrency ensures exceptions in child coroutines are properly propagated to the parent scope.",
  },
  {
    id: 3011,
    category: "Kotlin",
    subcategory: "Kotlin Cơ Bản",
    level: "beginner",
    q: "Companion object trong Kotlin là gì?",
    a: "Companion object cho phép định nghĩa các thành viên kiểu static (hằng số, factory method) bên trong class, thuộc về class chứ không phải instance. Mỗi class chỉ có một companion object. Có thể gọi trực tiếp mà không cần tạo instance: `MyClass.myStaticMethod()`. Giống `static` trong Java nhưng linh hoạt hơn vì có thể implement interface.",
    q_en: "What is a companion object in Kotlin?",
    a_en: "A companion object allows you to define static-like members (constants, factory methods) within a class that belong to the class itself, not instances. Only one companion object is allowed per class. You can call companion object members without creating an instance: `MyClass.myStaticMethod()`. It's similar to `static` in Java but offers better encapsulation and allows implementing interfaces.",
  },
  {
    id: 3012,
    category: "Kotlin",
    subcategory: "Kotlin Cơ Bản",
    level: "intermediate",
    q: "Destructuring trong Kotlin là gì?",
    a: "Destructuring cho phép bạn tách một object thành các biến riêng lẻ trong một câu lệnh duy nhất. Ví dụ `val (name, age) = person` trích xuất thuộc tính từ data class. Cũng có thể dùng trong vòng lặp: `for ((key, value) in map)`. Hoạt động với data class, Pair, List, và rất hữu ích để code dễ đọc hơn khi làm việc với nhiều giá trị.",
    q_en: "Explain destructuring in Kotlin.",
    a_en: "Destructuring allows you to break down an object into individual variables in a single statement. For example, `val (name, age) = person` extracts properties from a data class. You can also destructure in loops: `for ((key, value) in map)`. This works with data classes, pairs, lists, and makes code more readable when working with multiple values.",
  },

  // ─── ANDROID CORE ────────────────────────────────────────────────────────────
  {
    id: 3013,
    category: "Android",
    subcategory: "Android Core",
    level: "beginner",
    q: "Giải thích Android Activity lifecycle và mục đích của từng method.",
    a: "Activity lifecycle có 6 method chính theo thứ tự: `onCreate()` (khởi tạo), `onStart()` (trở nên visible), `onResume()` (có focus), `onPause()` (mất focus), `onStop()` (không còn visible), `onDestroy()` (dọn dẹp). Hiểu lifecycle rất quan trọng vì bạn phải lưu state trong `onSaveInstanceState()` và khôi phục trong `onCreate()`. Memory leak thường xảy ra khi giữ strong reference đến Activity quá vòng đời của nó.",
    q_en: "Explain the Android Activity lifecycle and each method's purpose.",
    a_en: "Activity lifecycle has six main methods: `onCreate()` (initialize), `onStart()` (become visible), `onResume()` (gain focus), `onPause()` (lose focus), `onStop()` (not visible), `onDestroy()` (cleanup). Understanding this is critical because you must save state in `onSaveInstanceState()` and restore it in `onCreate()`. Memory leaks often occur if you hold strong references to activities beyond their lifecycle.",
  },
  {
    id: 3014,
    category: "Android",
    subcategory: "Android Core",
    level: "beginner",
    q: "Fragment và Activity khác nhau như thế nào?",
    a: "Activity là component độc lập chiếm toàn màn hình, quản lý vòng đời UI cho một màn hình. Fragment là component UI nhẹ hơn, có thể tái sử dụng, và luôn tồn tại trong một Activity — có thể swap vào/ra tùy ý. Fragment giúp xây dựng UI linh hoạt thích ứng với nhiều kích thước màn hình, còn Activity đại diện cho các màn hình riêng biệt trong app.",
    q_en: "What is the difference between Fragment and Activity?",
    a_en: "An Activity is a full-screen independent component that manages the lifecycle of UI for a screen. A Fragment is a reusable, lightweight UI component that always exists within an Activity and can be swapped in and out. Fragments allow you to build flexible UIs that adapt to different screen sizes, while activities represent distinct screens in your app.",
  },
  {
    id: 3015,
    category: "Android",
    subcategory: "Android Core",
    level: "intermediate",
    q: "Vòng đời Fragment là gì và khác với Activity như thế nào?",
    a: "Fragment lifecycle phức tạp hơn Activity: `onAttach()`, `onCreate()`, `onCreateView()`, `onViewCreated()`, `onStart()`, `onResume()`, `onPause()`, `onStop()`, `onDestroyView()`, `onDestroy()`, `onDetach()`. Điểm khác biệt quan trọng là View của Fragment có thể bị destroy (`onDestroyView`) trong khi Fragment vẫn còn trong bộ nhớ. Vì vậy phải dọn dẹp View references trong `onDestroyView()` để tránh memory leak.",
    q_en: "Explain Fragment lifecycle and how it differs from Activity lifecycle.",
    a_en: "Fragment lifecycle is more complex than Activity: `onAttach()`, `onCreate()`, `onCreateView()`, `onViewCreated()`, `onStart()`, `onResume()`, `onPause()`, `onStop()`, `onDestroyView()`, `onDestroy()`, `onDetach()`. The key difference is the Fragment's View can be destroyed (`onDestroyView`) while the Fragment still exists in memory. Always clean up View references in `onDestroyView()` to prevent memory leaks.",
  },
  {
    id: 3016,
    category: "Android",
    subcategory: "Android Core",
    level: "intermediate",
    q: "Sự khác biệt giữa `replace()` và `add()` trong Fragment transactions?",
    a: "`replace()` xóa Fragment hiện tại và thêm Fragment mới vào chỗ đó — khi back thì Fragment bị replace sẽ được tạo lại. `add()` giữ Fragment hiện có và chồng Fragment mới lên trên — khi back thì quay về Fragment cũ. Dùng `replace()` để điều hướng giữa các màn hình, còn `add()` cho overlay như dialog hay bottom sheet.",
    q_en: "What is the difference between `replace()` and `add()` in Fragment transactions?",
    a_en: "`replace()` removes the current Fragment and adds a new one in its place — on back press the replaced fragment is recreated. `add()` keeps the existing Fragment and adds a new one on top — on back press returns to the existing fragment. Use `replace()` for navigation between screens and `add()` for overlays like dialogs or bottom sheets.",
  },
  {
    id: 3017,
    category: "Android",
    subcategory: "Android Core",
    level: "beginner",
    q: "Intent trong Android là gì và có mấy loại?",
    a: "Intent là object nhắn tin để yêu cầu một hành động từ component khác trong app. Explicit intent chỉ định chính xác component cần khởi động (ví dụ mở một Activity cụ thể trong app). Implicit intent khai báo action mà không chỉ định component, để hệ thống tìm app phù hợp xử lý (ví dụ mở URL trong browser). Cả hai loại đều có thể truyền dữ liệu qua bundle.",
    q_en: "What are Intents in Android and what are the two types?",
    a_en: "Intents are messaging objects used to request an action from another app component. Explicit intents specify the exact component to launch (e.g., starting a specific Activity in your app). Implicit intents declare an action without specifying the component, letting the system find the appropriate app to handle it (e.g., opening a URL in a browser). Both types can pass data through bundles.",
  },
  {
    id: 3018,
    category: "Android",
    subcategory: "Android Core",
    level: "intermediate",
    q: "Service trong Android là gì và khi nào nên dùng?",
    a: "Service là component chạy nền không có tương tác người dùng, thích hợp cho các tác vụ chạy lâu như phát nhạc, tải file, hay theo dõi vị trí. Service không có UI nhưng chạy trên main thread mặc định, nên cần offload tác vụ nặng sang background thread hoặc coroutine. Foreground service hiển thị notification liên tục và dùng cho tác vụ nền ưu tiên cao.",
    q_en: "What is a Service in Android and when should you use it?",
    a_en: "A Service is a background component that runs without user interaction, perfect for long-running tasks like music playback, file downloads, or location tracking. Services don't have UI but run on the main thread by default, so long operations should be offloaded to background threads or coroutines. Foreground services show a persistent notification and are used for high-priority background tasks.",
  },
  {
    id: 3019,
    category: "Android",
    subcategory: "Android Core",
    level: "intermediate",
    q: "BroadcastReceiver là gì và hoạt động như thế nào?",
    a: "BroadcastReceiver là component lắng nghe các broadcast trong toàn hệ thống (như pin yếu, thay đổi network, hay broadcast tùy chỉnh). Bạn đăng ký nó trong manifest (static) hoặc trong code (dynamic), và nó kích hoạt `onReceive()` khi nhận được broadcast phù hợp. Tránh thực hiện tác vụ nặng trong `onReceive()` vì phải hoàn thành nhanh — dùng WorkManager cho tác vụ dài.",
    q_en: "What are BroadcastReceivers and how do they work?",
    a_en: "BroadcastReceiver is a component that listens for system-wide broadcasts (like battery low, network changes, or custom broadcasts). Register it statically in the manifest or dynamically in code, and it triggers `onReceive()` when matching broadcasts are sent. Avoid heavy operations in `onReceive()` since it must complete quickly — use WorkManager for long tasks.",
  },
  {
    id: 3020,
    category: "Android",
    subcategory: "Android Core",
    level: "intermediate",
    q: "ContentProvider là gì và dùng trong trường hợp nào?",
    a: "ContentProvider là lớp trừu tượng cung cấp quyền truy cập có kiểm soát vào dữ liệu của app cho các app hoặc process khác. Nó dùng URI để xác định resource và implement các thao tác CRUD qua `query()`, `insert()`, `update()`, `delete()`. Danh bạ, lịch, và ảnh đều dùng ContentProvider. Chúng đảm bảo tính nhất quán dữ liệu, bảo mật qua permission, và cho phép chia sẻ dữ liệu qua ranh giới app.",
    q_en: "What is ContentProvider and its use case?",
    a_en: "ContentProvider is an abstraction that provides controlled access to app data for other apps or processes. It uses URIs to identify resources and implements CRUD operations through `query()`, `insert()`, `update()`, `delete()`. Contacts, calendars, and photos use ContentProviders. They ensure data consistency, security through permissions, and allow data sharing across app boundaries.",
  },
  {
    id: 3021,
    category: "Android",
    subcategory: "Android Core",
    level: "beginner",
    q: "Các loại Android permission và cách xử lý runtime permissions?",
    a: "Normal permission (internet, vibrate) được cấp tự động khi cài đặt. Dangerous permission (camera, location, contacts) phải xin lúc runtime. Để xin runtime permission: tạo `ActivityResultContract`, dùng `registerForActivityResult()`, và kiểm tra `hasPermission()` trước khi dùng tính năng được bảo vệ. Từ Android 6 trở đi, bỏ qua runtime permission sẽ làm app crash.",
    q_en: "What are the different Android permission types and how do you handle runtime permissions?",
    a_en: "Normal permissions (internet, vibrate) are granted at install time. Dangerous permissions (camera, location, contacts) require runtime requests. To request runtime permissions: create an `ActivityResultContract`, request using `registerForActivityResult()`, and check `hasPermission()` before using protected features. Starting from Android 6, skipping runtime permission requests will crash the app.",
  },
  {
    id: 3022,
    category: "Android",
    subcategory: "Android Core",
    level: "intermediate",
    q: "`savedInstanceState` và `onSaveInstanceState()` khác nhau như thế nào?",
    a: "`onSaveInstanceState()` là callback để lưu trạng thái UI tạm thời (như vị trí scroll) trước khi activity bị destroy do configuration change. Bundle này được truyền vào `onCreate()` hoặc `onRestoreInstanceState()`. Khác với lưu trữ lâu dài như SharedPreferences hay database. Dùng nó cho trạng thái tạm thời dễ lưu hơn là phải tạo lại UI từ đầu.",
    q_en: "What is the difference between savedInstanceState and onSaveInstanceState()?",
    a_en: "`onSaveInstanceState()` is a callback that saves transient UI state (like scroll position) before the activity is destroyed due to configuration changes. The Bundle is passed to `onCreate()` or `onRestoreInstanceState()`. This is different from permanent storage like SharedPreferences or database. Use it for temporary state that's cheap to save but better than recreating UI from scratch.",
  },
  {
    id: 3023,
    category: "Android",
    subcategory: "Android Core",
    level: "beginner",
    q: "Intent filter và implicit intent là gì?",
    a: "Intent filter được khai báo trong manifest để chỉ định loại implicit intent mà Activity có thể xử lý. Chúng định nghĩa action, category, và kiểu dữ liệu. Khi bạn gửi implicit intent, hệ thống tìm tất cả app có Intent filter phù hợp. Ví dụ Intent với action `ACTION_VIEW` và data `http://` có thể mở browser. Phải thêm `android.intent.category.DEFAULT` để implicit intent hoạt động.",
    q_en: "Explain Intent filters and implicit intents.",
    a_en: "Intent filters are declared in the manifest to specify what implicit intents an Activity can handle. They define action, category, and data types. When you send an implicit intent, the system finds all apps with matching Intent filters. For example, an Intent with action `ACTION_VIEW` and data `http://` might open a browser. You must include `android.intent.category.DEFAULT` for implicit intents to work.",
  },
  {
    id: 3024,
    category: "Android",
    subcategory: "Android Core",
    level: "beginner",
    q: "Sự khác biệt giữa Process và Thread trong Android?",
    a: "Process là một instance riêng biệt của app với bộ nhớ và tài nguyên riêng, chạy độc lập với các process khác. Thread là đơn vị thực thi nhẹ hơn trong một process, chia sẻ bộ nhớ với các thread khác. Android app có main thread (UI thread) xử lý toàn bộ UI, còn background thread xử lý tác vụ nặng. Tuyệt đối không được block main thread.",
    q_en: "What are the differences between Process and Thread in Android?",
    a_en: "A Process is a separate instance of the app with its own memory space and resources, running independently. A Thread is a lightweight execution unit within a process that shares memory with other threads. Android apps have a main thread (UI thread) that handles all UI operations, and background threads handle heavy tasks. Never block the main thread.",
  },

  // ─── JETPACK COMPOSE ─────────────────────────────────────────────────────────
  {
    id: 3025,
    category: "Jetpack Compose",
    subcategory: "Jetpack Compose",
    level: "beginner",
    q: "Jetpack Compose là gì và tại sao nó đang thay thế XML layouts?",
    a: "Jetpack Compose là bộ công cụ UI declarative hiện đại cho phép xây dựng giao diện Android bằng Kotlin function thay vì XML. Nó đang thay thế XML vì code ngắn gọn hơn, type-safe, và dễ tái sử dụng hơn. Compose tự động cập nhật UI khi state thay đổi, loại bỏ boilerplate và giúp UI dễ test hơn. Google đã tuyên bố đây là cách tiếp cận được khuyến nghị cho UI Android.",
    q_en: "What is Jetpack Compose and why is it replacing XML layouts?",
    a_en: "Jetpack Compose is a modern declarative UI toolkit that lets you build Android UIs with Kotlin functions instead of XML. It's replacing XML because it's more concise, type-safe, and reusable. Compose automatically manages UI updates when state changes, eliminating boilerplate and making UIs easier to test and maintain. Google has declared it the recommended approach for Android UI development.",
  },
  {
    id: 3026,
    category: "Jetpack Compose",
    subcategory: "Jetpack Compose",
    level: "beginner",
    q: "Composable function là gì và điều kiện để một function là Composable?",
    a: "Composable function là Kotlin function thông thường được đánh dấu `@Composable`, trả về Unit và mô tả UI. Nó phải pure (không có side effect), idempotent (cùng input thì cùng output), và chạy nhanh. Annotation `@Composable` báo cho Compose runtime theo dõi recomposition và state change. Composable function được gọi bởi Compose runtime chứ không phải do bạn gọi thủ công.",
    q_en: "What is a Composable function and what makes something Composable?",
    a_en: "A Composable function is a regular Kotlin function marked with `@Composable` that returns Unit and describes UI. It should be pure (no side effects), idempotent (same inputs = same output), and fast. The `@Composable` annotation tells Compose to track recompositions and state changes. Composable functions are called by the Compose runtime, not manually.",
  },
  {
    id: 3027,
    category: "Jetpack Compose",
    subcategory: "Jetpack Compose",
    level: "intermediate",
    q: "Giải thích state management trong Jetpack Compose với `mutableStateOf` và `remember`.",
    a: "`mutableStateOf()` tạo một state object có thể quan sát, kích hoạt recomposition khi thay đổi. `remember` lưu cache giá trị state qua các lần recomposition để không bị tạo lại mỗi lần. Kết hợp lại: `val count = remember { mutableStateOf(0) }` hoặc ngắn gọn hơn là `var count by remember { mutableStateOf(0) }`. Khi `count` thay đổi, chỉ các composable đang đọc nó mới recompose, không phải cả màn hình.",
    q_en: "Explain state management in Jetpack Compose using `mutableStateOf` and `remember`.",
    a_en: "`mutableStateOf()` creates an observable state object that triggers recomposition when changed. `remember` caches the state value across recompositions so it doesn't get recreated each time. Together: `val count = remember { mutableStateOf(0) }` or the shorter `var count by remember { mutableStateOf(0) }`. When `count` changes, only composables reading it recompose, not the entire screen.",
  },
  {
    id: 3028,
    category: "Jetpack Compose",
    subcategory: "Jetpack Compose",
    level: "intermediate",
    q: "Sự khác biệt giữa `remember` và `rememberSaveable`?",
    a: "`remember` giữ state qua các lần recomposition trong scope của composable nhưng mất khi configuration change như xoay màn hình. `rememberSaveable` persist state qua configuration change bằng cách tự động lưu vào Bundle. Dùng `rememberSaveable` cho dữ liệu người dùng nhập (form fields) và `remember` cho state UI tạm thời (animation values). `rememberSaveable` có overhead cao hơn một chút.",
    q_en: "What is the difference between `remember` and `rememberSaveable`?",
    a_en: "`remember` keeps state across recompositions but loses it on configuration changes like screen rotation. `rememberSaveable` persists state across configuration changes by automatically saving it to a Bundle. Use `rememberSaveable` for important user input (form fields) and `remember` for temporary UI state (animation values). `rememberSaveable` has slightly more overhead.",
  },
  {
    id: 3029,
    category: "Jetpack Compose",
    subcategory: "Jetpack Compose",
    level: "intermediate",
    q: "State hoisting trong Jetpack Compose là gì và tại sao quan trọng?",
    a: "State hoisting nghĩa là chuyển state từ composable con lên composable cha, làm cho composable con trở nên stateless. Composable stateless nhận state qua tham số và callback để cập nhật state, giúp tái sử dụng trên nhiều màn hình. Ví dụ hoist state của TextField lên cha để nhiều composable có thể đọc. Điều này cải thiện khả năng test, tái sử dụng, và làm data flow một chiều rõ ràng hơn.",
    q_en: "Explain state hoisting in Jetpack Compose and why it's important.",
    a_en: "State hoisting means moving state from a child composable to its parent, making the child stateless. A stateless composable receives state as a parameter and callbacks for state updates, making it reusable across different screens. This improves testability, reusability, and makes data flow unidirectional and predictable.",
  },
  {
    id: 3030,
    category: "Jetpack Compose",
    subcategory: "Jetpack Compose",
    level: "intermediate",
    q: "`LaunchedEffect` là gì và khi nào nên dùng?",
    a: "`LaunchedEffect` là side-effect function khởi chạy một coroutine scope gắn với lifecycle của composable. Dùng cho các tác vụ một lần như load data, animation, hay analytics. Nó chạy khi key parameter thay đổi và hủy khi composable rời khỏi composition. \n\n**Ví dụ:** `LaunchedEffect(userId) { loadUserData(userId) }` load lại data mỗi khi userId thay đổi.",
    q_en: "What is `LaunchedEffect` and when should you use it?",
    a_en: "`LaunchedEffect` is a side-effect function that launches a coroutine scope tied to the composable's lifecycle. Use it for one-time operations like loading data, animations, or analytics events. It runs when the key parameter changes and cancels when the composable leaves composition. \n\n**Example:** `LaunchedEffect(userId) { loadUserData(userId) }` reloads data whenever userId changes.",
  },
  {
    id: 3031,
    category: "Jetpack Compose",
    subcategory: "Jetpack Compose",
    level: "advanced",
    q: "Sự khác nhau giữa `LaunchedEffect`, `DisposableEffect`, và `SideEffect`?",
    a: "`LaunchedEffect` khởi chạy coroutine cho tác vụ bất đồng bộ. `DisposableEffect` dùng để thiết lập và dọn dẹp resource với một cleanup block (như event listener). `SideEffect` chạy sau mỗi lần recomposition, không có tham số, hiếm khi dùng (chủ yếu cho logging). Dùng `LaunchedEffect` cho load data, `DisposableEffect` cho resource, và tránh `SideEffect` trong hầu hết trường hợp.",
    q_en: "Explain the difference between `LaunchedEffect`, `DisposableEffect`, and `SideEffect`.",
    a_en: "`LaunchedEffect` launches a coroutine scope for async operations. `DisposableEffect` is for setting up and cleaning up resources with a cleanup block (like event listeners). `SideEffect` runs after every recomposition with no parameters and is rarely used (mostly for logging). Use `LaunchedEffect` for data loading, `DisposableEffect` for resources, and avoid `SideEffect` in most cases.",
  },
  {
    id: 3032,
    category: "Jetpack Compose",
    subcategory: "Jetpack Compose",
    level: "beginner",
    q: "Modifier trong Jetpack Compose là gì? Cho ví dụ.",
    a: "Modifier là object có thể chain lại để thêm behavior, styling, hay layout cho composable. \n\n**Ví dụ:** `.size(100.dp)`, `.background(Color.Blue)`, `.padding(16.dp)`, `.clickable { }`, `.weight(1f)`. Thứ tự quan trọng vì modifier chain từ trái sang phải: `.padding().background()` khác với `.background().padding()`. Modifier là lập trình hàm ứng dụng tốt nhất, kết hợp các behavior nhỏ thành behavior phức tạp.",
    q_en: "What is a Modifier in Jetpack Compose and give examples.",
    a_en: "Modifiers are chainable objects that add behavior, styling, or layout to composables. Examples: `.size(100.dp)`, `.background(Color.Blue)`, `.padding(16.dp)`, `.clickable { }`, `.weight(1f)`. Order matters because modifiers chain left to right: `.padding().background()` is different from `.background().padding()`. Modifiers compose small behaviors into complex ones.",
  },
  {
    id: 3033,
    category: "Jetpack Compose",
    subcategory: "Jetpack Compose",
    level: "intermediate",
    q: "Làm thế nào để xử lý navigation trong Jetpack Compose?",
    a: "Dùng Compose Navigation library với `NavController` và `NavHost`. Định nghĩa `NavGraph` với các màn hình và route, rồi dùng `navController.navigate(route)` để điều hướng. State được giữ lại khi quay về màn hình trước. Với Compose Navigation 2.5+, dùng type-safe routes thay vì string-based routes. `BackHandler` cho phép tùy chỉnh hành vi nút back.",
    q_en: "How do you handle navigation in Jetpack Compose?",
    a_en: "Use the Compose Navigation library with `NavController` and `NavHost`. Define a `NavGraph` with screens and navigation routes, and use `navController.navigate(route)` to navigate. State is preserved when returning to previous destinations. For type-safe navigation, use Compose Navigation 2.5+ with type-safe routes instead of string-based routes. `BackHandler` allows custom back button behavior.",
  },
  {
    id: 3034,
    category: "Jetpack Compose",
    subcategory: "Jetpack Compose",
    level: "advanced",
    q: "`derivedStateOf` là gì và khi nào nên dùng?",
    a: "`derivedStateOf` tạo ra một state dẫn xuất được tính toán từ các state khác và chỉ recompose khi kết quả tính toán thay đổi. \n\n**Ví dụ:** `val isFormValid = derivedStateOf { name.isNotEmpty() && email.isNotEmpty() }`. Đây là cách tối ưu để ngăn recomposition không cần thiết khi input thay đổi nhưng không ảnh hưởng đến giá trị dẫn xuất. Hữu ích cho các tính toán phức tạp trên state.",
    q_en: "What is `derivedStateOf` and when should you use it?",
    a_en: "`derivedStateOf` creates a derived state computed from other states and only recomposes when the computed result changes. For example: `val isFormValid = derivedStateOf { name.isNotEmpty() && email.isNotEmpty() }`. This optimization prevents unnecessary recompositions when input changes don't affect the derived value. It's useful for complex computations on state.",
  },
  {
    id: 3035,
    category: "Jetpack Compose",
    subcategory: "Jetpack Compose",
    level: "intermediate",
    q: "Làm thế nào để implement theming trong Jetpack Compose?",
    a: "Dùng `MaterialTheme` hoặc `CompositionLocal` tùy chỉnh để tập trung hóa các thuộc tính theme. Định nghĩa màu sắc, typography, và shape trong theme object, dùng `CompositionLocalProvider` để cung cấp chúng cho cây composable. Wrap app trong `MaterialTheme(colors = colors, typography = typography) { App() }`. Hỗ trợ dark/light mode qua `isSystemInDarkTheme()`.",
    q_en: "How do you implement theming in Jetpack Compose?",
    a_en: "Use `MaterialTheme` or custom `CompositionLocal` values to centralize theme properties. Define colors, typography, and shapes in a theme object, and use `CompositionLocalProvider` to provide them to the composable tree. Wrap your app in `MaterialTheme(colors = colors, typography = typography) { App() }`. Supports light/dark mode through `isSystemInDarkTheme()`.",
  },
  {
    id: 3036,
    category: "Jetpack Compose",
    subcategory: "Jetpack Compose",
    level: "advanced",
    q: "`CompositionLocal` là gì và hoạt động như thế nào?",
    a: "`CompositionLocal` là cách truyền giá trị ngầm định xuống cây composable mà không cần truyền qua tham số ở mỗi cấp. Dùng `CompositionLocalProvider(LocalValue provides value) { Content() }` để cung cấp giá trị. Truy cập bằng `LocalValue.current`. Hữu ích cho theme color, navigation controller, hay các giá trị cần dùng xuyên suốt cây. Tuy nhiên lạm dụng sẽ làm code khó theo dõi.",
    q_en: "What are CompositionLocals and how do they work?",
    a_en: "`CompositionLocal` is a way to pass values down the composable tree implicitly without passing them as parameters. Use `CompositionLocalProvider(LocalValue provides value) { Content() }` to provide values and `LocalValue.current` to access them. They're useful for theme colors, navigation controllers, or other globally needed values. However, overuse makes code hard to follow.",
  },

  // ─── ARCHITECTURE ─────────────────────────────────────────────────────────────
  {
    id: 3037,
    category: "Android",
    subcategory: "Architecture",
    level: "intermediate",
    q: "MVVM architecture là gì và cấu trúc ra sao?",
    a: "MVVM (Model-View-ViewModel) tách UI logic khỏi business logic: Model chứa dữ liệu và business rule, View hiển thị UI (Activity/Fragment/Composable), ViewModel chứa UI state và logic, tồn tại qua configuration change. ViewModel expose LiveData/StateFlow để View observe. Sự tách biệt này cải thiện khả năng test (test ViewModel không cần UI), tái sử dụng, và bảo trì. Hầu hết app Android ngày nay đều dùng MVVM.",
    q_en: "What is MVVM architecture and how is it structured?",
    a_en: "MVVM (Model-View-ViewModel) separates UI logic from business logic: Model holds data and business rules, View displays UI (Activity/Fragment/Composable), and ViewModel holds UI state and logic, surviving configuration changes. ViewModel exposes LiveData/StateFlow that View observes. This separation improves testability, reusability, and maintainability.",
  },
  {
    id: 3038,
    category: "Android",
    subcategory: "Architecture",
    level: "advanced",
    q: "MVI architecture là gì và khác MVVM như thế nào?",
    a: "MVI (Model-View-Intent) dùng luồng dữ liệu một chiều như Flux hay Redux. View gửi intent (hành động người dùng) lên ViewModel, ViewModel xử lý và emit ViewState mới để cập nhật View. Khác với MVVM nơi ViewModel có thể expose nhiều StateFlow, MVI dùng một state object duy nhất được cập nhật theo một hướng. Có thể đoán trước hơn nhưng cần nhiều boilerplate hơn.",
    q_en: "What is MVI architecture and how does it differ from MVVM?",
    a_en: "MVI (Model-View-Intent) uses unidirectional data flow like Flux or Redux. The View sends intents (user actions) to the ViewModel, which processes them and emits new ViewStates updating the View. Unlike MVVM where ViewModels expose multiple StateFlows, MVI uses a single state object updated in one direction. It's more predictable but requires more boilerplate.",
  },
  {
    id: 3039,
    category: "Android",
    subcategory: "Architecture",
    level: "intermediate",
    q: "Repository pattern là gì và tại sao quan trọng?",
    a: "Repository pattern trừu tượng hóa các nguồn dữ liệu (local database, remote API, cache) sau một interface duy nhất, để ViewModel không cần biết dữ liệu đến từ đâu. Sự decoupling này giúp test dễ hơn (mock repository) và cho phép đổi nguồn dữ liệu mà không cần sửa ViewModel. Repository điều phối giữa dữ liệu local và remote, implement caching strategy, và là single source of truth.",
    q_en: "What is the Repository pattern and why is it important?",
    a_en: "The Repository pattern abstracts data sources (local database, remote API, cache) behind a single interface, so the ViewModel doesn't know where data comes from. This decoupling makes testing easier (mock the repository) and allows switching data sources without changing ViewModels. A repository coordinates between local and remote data and provides a single source of truth.",
  },
  {
    id: 3040,
    category: "Android",
    subcategory: "Architecture",
    level: "intermediate",
    q: "Sự khác biệt giữa LiveData và StateFlow?",
    a: "LiveData lifecycle-aware và tự động unsubscribe khi lifecycle của observer bị destroy (ngăn memory leak). StateFlow cần quản lý lifecycle thủ công hơn nhưng tích hợp tự nhiên với coroutine. LiveData không cần initial value; StateFlow thì có. App mới nên ưu tiên StateFlow vì thân thiện với coroutine hơn và hoạt động tốt hơn với Compose.",
    q_en: "Explain the difference between LiveData and StateFlow.",
    a_en: "LiveData is lifecycle-aware and automatically unsubscribes when the observer's lifecycle is destroyed (preventing memory leaks). StateFlow requires manual lifecycle management but integrates naturally with coroutines. LiveData doesn't require an initial value; StateFlow does. Modern apps prefer StateFlow for new code because it's coroutine-friendly and works better with Compose.",
  },
  {
    id: 3041,
    category: "Android",
    subcategory: "Architecture",
    level: "beginner",
    q: "ViewModel là gì và tại sao nên dùng?",
    a: "ViewModel lưu trữ dữ liệu UI theo cách lifecycle-aware, tồn tại qua configuration change như xoay màn hình. Nó không giữ reference đến View (ngăn memory leak) và là nơi lý tưởng để đặt business logic, gọi API, và quản lý state. Tạo ViewModel mỗi màn hình bằng `viewModel()` trong Compose hoặc `ViewModelProvider` trong Activity/Fragment. Tuyệt đối không truyền Activity hay View vào ViewModel.",
    q_en: "What is a ViewModel and why should you use it?",
    a_en: "ViewModel holds UI-related data in a lifecycle-aware manner, surviving configuration changes like screen rotation. It doesn't hold references to Views (preventing memory leaks) and is the perfect place for business logic, API calls, and state management. Create one per screen using `viewModel()` in Compose or `ViewModelProvider` in Activities/Fragments. Never pass Activities or Views to ViewModel.",
  },
  {
    id: 3042,
    category: "Android",
    subcategory: "Architecture",
    level: "intermediate",
    q: "Dependency injection là gì và tại sao quan trọng trong Android?",
    a: "Dependency injection nghĩa là cung cấp dependency của một object qua tham số thay vì object tự tạo bên trong. \n\n**Lợi ích:** test dễ hơn (truyền mock dependency), loose coupling, linh hoạt khi đổi implementation. \n\n**Ví dụ:** thay vì `val database = Database.getInstance()` bên trong class, hãy truyền vào: `class MyClass(val database: Database)`. Code modular và testable hơn nhiều.",
    q_en: "What is dependency injection and why is it important in Android?",
    a_en: "Dependency injection means providing an object's dependencies through parameters instead of the object creating them internally. \n\n**Benefits:** easier testing (pass mock dependencies), loose coupling, flexibility to change implementations. For example, instead of `val database = Database.getInstance()` inside the class, pass it: `class MyClass(val database: Database)`. This makes code modular and testable.",
  },
  {
    id: 3043,
    category: "Android",
    subcategory: "Architecture",
    level: "intermediate",
    q: "Hilt là gì và nó đơn giản hóa dependency injection như thế nào?",
    a: "Hilt là thư viện DI được xây trên Dagger, thiết kế riêng cho Android. Nó loại bỏ boilerplate bằng cách cung cấp module có sẵn cho Android component. Đánh dấu Application với `@HiltAndroidApp`, Activity/Fragment với `@AndroidEntryPoint`, và inject bằng `@Inject`. Hilt tự động quản lý scope (như `@Singleton`) và tích hợp liền mạch với ViewModel và composable.",
    q_en: "What is Hilt and how does it simplify dependency injection?",
    a_en: "Hilt is a dependency injection library built on Dagger, designed specifically for Android. It eliminates boilerplate by providing predefined modules for Android components. Mark your Application with `@HiltAndroidApp`, Activities/Fragments with `@AndroidEntryPoint`, and inject with `@Inject`. Hilt automatically manages scopes (like `@Singleton`) and integrates seamlessly with ViewModels and composables.",
  },
  {
    id: 3044,
    category: "Android",
    subcategory: "Architecture",
    level: "advanced",
    q: "Clean Architecture là gì và có những layer nào?",
    a: "Clean Architecture gồm 3 layer: Presentation (UI, ViewModel), Domain (business logic, use case), và Data (database, API, repository). Mỗi layer độc lập và có thể test riêng. Domain layer là Kotlin thuần không phụ thuộc Android, test được trên JVM. Data layer cung cấp repository mà domain layer sử dụng. Cấu trúc này ngăn tight coupling và cải thiện tổ chức code.",
    q_en: "Explain Clean Architecture and its layers.",
    a_en: "Clean Architecture has three layers: Presentation (UI, ViewModels), Domain (business logic, use cases), and Data (databases, APIs, repositories). Each layer is independent and testable in isolation. Domain layer is pure Kotlin with no Android dependencies, making it testable on the JVM. This structure prevents tight coupling and improves code organization.",
  },
  {
    id: 3045,
    category: "Android",
    subcategory: "Architecture",
    level: "advanced",
    q: "Use Case pattern là gì và khi nào nên tạo một Use Case?",
    a: "Use Case (hay Interactor) đóng gói một thao tác business logic duy nhất, nhận input và trả về output. \n\n**Ví dụ:** `LoginUseCase`, `FetchUserDataUseCase`. Use Case là phần của Domain layer trong Clean Architecture và độc lập với UI framework. Chúng giúp logic tái sử dụng được, test được, và dễ hiểu. Tạo Use Case khi một logic cần được dùng từ nhiều ViewModel hoặc cần test độc lập.",
    q_en: "What is the Use Case pattern and when should you create one?",
    a_en: "A Use Case (or Interactor) encapsulates a single business logic operation, taking input parameters and returning output. Examples: `LoginUseCase`, `FetchUserDataUseCase`. Use Cases are part of Clean Architecture's Domain layer and are independent of UI frameworks. Create one when a piece of logic needs to be used from multiple ViewModels or tested independently.",
  },
  {
    id: 3046,
    category: "Android",
    subcategory: "Architecture",
    level: "intermediate",
    q: "Các Hilt scope khác nhau có ý nghĩa gì?",
    a: "Hilt scope kiểm soát thời gian sống của dependency instance: `@Singleton` sống suốt vòng đời app, `@ActivityScoped` theo vòng đời Activity, `@ViewModelScoped` theo ViewModel, và `@FragmentScoped` theo Fragment. Scope mặc định là unscoped (tạo instance mới mỗi lần). Chọn scope dựa vào thời gian muốn tái sử dụng instance. `@Singleton` phổ biến nhất nhưng dùng loại khác để quản lý bộ nhớ tốt hơn.",
    q_en: "What are the different Hilt scopes and what do they mean?",
    a_en: "Hilt scopes control how long dependency instances live: `@Singleton` lives for the app's lifetime, `@ActivityScoped` for the activity, `@ViewModelScoped` for the ViewModel, and `@FragmentScoped` for the fragment. Default scope is unscoped (new instance each time). Choose scopes based on how long you want to reuse instances. `@Singleton` is most common but use others to manage memory.",
  },

  // ─── DATA & NETWORKING ────────────────────────────────────────────────────────
  {
    id: 3047,
    category: "Android",
    subcategory: "Data & Networking",
    level: "beginner",
    q: "Room database là gì và tại sao dùng thay vì SQLite trực tiếp?",
    a: "Room là lớp trừu tượng type-safe trên SQLite, tự động sinh code lúc compile, ngăn SQL injection và lỗi runtime. Bạn định nghĩa entity (data class), DAO (database access object với query), và Database class. Room hỗ trợ LiveData/Flow để quan sát thay đổi database. An toàn và dễ dùng hơn SQLite thô rất nhiều, và tích hợp tốt với coroutine và Compose.",
    q_en: "What is Room database and why use it instead of SQLite directly?",
    a_en: "Room is a type-safe abstraction over SQLite that auto-generates code at compile time, preventing SQL injection and runtime errors. You define entities (data classes), DAOs (database access objects with queries), and a Database class. Room provides LiveData/Flow support for observing database changes and is much safer and easier than raw SQLite queries.",
  },
  {
    id: 3048,
    category: "Android",
    subcategory: "Data & Networking",
    level: "beginner",
    q: "DAO (Data Access Object) trong Room là gì?",
    a: 'DAO là interface với các method cho thao tác CRUD trên database. Bạn annotate method với `@Query`, `@Insert`, `@Update`, `@Delete`. Room sinh implementation lúc compile. \n\n**Ví dụ:** `@Query("SELECT * FROM users WHERE id = :id") suspend fun getUser(id: Int): User`. DAO là nơi định nghĩa query, và dùng suspend function giúp query tự động chạy trên background thread.',
    q_en: "What is a DAO (Data Access Object) in Room?",
    a_en: 'A DAO is an interface with methods for database CRUD operations annotated with `@Query`, `@Insert`, `@Update`, `@Delete`. Room generates the implementation at compile time. For example: `@Query("SELECT * FROM users WHERE id = :id") suspend fun getUser(id: Int): User`. DAOs are where you define your database queries, and using suspend functions allows queries to run on background threads automatically.',
  },
  {
    id: 3049,
    category: "Android",
    subcategory: "Data & Networking",
    level: "beginner",
    q: "Retrofit là gì và hoạt động như thế nào?",
    a: "Retrofit là HTTP client type-safe chuyển REST API thành Kotlin interface. Bạn định nghĩa interface với method được annotate bằng HTTP verb như `@GET`, `@POST`, thêm kiểu request/response, và Retrofit tự sinh implementation. Nó tự động deserialize JSON response thành Kotlin object, xử lý header, và hỗ trợ cả callback lẫn coroutine. OkHttp là thư viện networking nền mà Retrofit dùng.",
    q_en: "What is Retrofit and how does it work?",
    a_en: "Retrofit is a type-safe HTTP client that converts REST APIs into Kotlin interfaces. You define an interface with methods annotated with HTTP verbs like `@GET`, `@POST`, add request/response types, and Retrofit generates the implementation. It automatically deserializes JSON responses into Kotlin objects, handles headers, and supports both callbacks and coroutines.",
  },
  {
    id: 3050,
    category: "Android",
    subcategory: "Data & Networking",
    level: "intermediate",
    q: "Interceptor trong Retrofit/OkHttp là gì và cách dùng?",
    a: 'Interceptor chặn HTTP request/response để chỉnh sửa chúng. Application interceptor chạy một lần cho mỗi request, còn network interceptor chạy cho mỗi lần gọi network thực tế. Dùng phổ biến: thêm auth header, log request/response, xử lý token refresh, cache. \n\n**Ví dụ:** `OkHttpClient().addInterceptor { chain -> chain.proceed(chain.request().newBuilder().addHeader("Authorization", token).build()) }`.',
    q_en: "What are Interceptors in Retrofit/OkHttp and how do you use them?",
    a_en: 'Interceptors intercept HTTP requests and responses to modify them. Application interceptors run once per request, while network interceptors run for each actual network call. Common uses: adding authentication headers, logging, handling token refresh, caching. \n\n**Example:** `OkHttpClient().addInterceptor { chain -> chain.proceed(chain.request().newBuilder().addHeader("Authorization", token).build()) }`.',
  },
  {
    id: 3051,
    category: "Android",
    subcategory: "Data & Networking",
    level: "intermediate",
    q: "Làm thế nào để implement authentication với Retrofit?",
    a: "Dùng Interceptor để thêm auth header vào mọi request. Khi token hết hạn, interface Authenticator xử lý token refresh bằng cách chặn response 401 và retry với token mới. Lưu token an toàn trong EncryptedSharedPreferences. Gửi token trong Authorization header: `Authorization: Bearer token`. Xử lý token hết hạn một cách graceful để người dùng không thấy lỗi.",
    q_en: "How do you implement authentication with Retrofit?",
    a_en: "Use an Interceptor to add authentication headers to every request. When the token expires, the Authenticator interface handles token refresh by intercepting 401 responses and retrying with a new token. Store tokens securely in EncryptedSharedPreferences. Send the token in the Authorization header: `Authorization: Bearer token`. Handle expired tokens gracefully to avoid showing errors to users.",
  },
  {
    id: 3052,
    category: "Android",
    subcategory: "Data & Networking",
    level: "intermediate",
    q: "DataStore là gì và khác SharedPreferences như thế nào?",
    a: "DataStore là sự thay thế hiện đại cho SharedPreferences, được xây dựng trên coroutine và Flow cho lưu trữ dữ liệu type-safe và transactional. Async-first (không blocking), hỗ trợ protocol buffer cho type safety, và an toàn hơn SharedPreferences vốn có vấn đề threading. Dùng `context.dataStore.data.map { it.setting }` để đọc và `context.dataStore.updateData { it.copy(setting = value) }` để ghi.",
    q_en: "What is DataStore and how does it differ from SharedPreferences?",
    a_en: "DataStore is a modern replacement for SharedPreferences, built on coroutines and Flow for type-safe, transactional data storage. It's async-first (no blocking), supports protocol buffers for type safety, and is safer than SharedPreferences which has threading issues. Use `context.dataStore.data.map { it.setting }` to read and `context.dataStore.updateData { ... }` to write.",
  },
  {
    id: 3053,
    category: "Android",
    subcategory: "Data & Networking",
    level: "intermediate",
    q: "WorkManager là gì và khi nào nên dùng?",
    a: "WorkManager lên lịch background work có thể defer được, tồn tại qua app restart và system reboot. Nó tự động xử lý Doze Mode và App Standby, chọn cơ chế scheduling tốt nhất (JobScheduler, BroadcastReceiver). Dùng cho tác vụ đáng tin cậy, không khẩn cấp như sync data, upload file, hay báo cáo định kỳ. Tạo `Worker` subclass, dùng `OneTimeWorkRequest` hoặc `PeriodicWorkRequest`, và enqueue với `WorkManager.enqueue()`.",
    q_en: "What is WorkManager and when should you use it?",
    a_en: "WorkManager schedules deferrable background work that survives app restarts and system reboots. It automatically handles Doze Mode and App Standby, choosing the best scheduling mechanism. Use for reliable, non-urgent tasks like syncing data, uploading files, or periodic reports. Create `Worker` subclasses, use `OneTimeWorkRequest` or `PeriodicWorkRequest`, and enqueue with `WorkManager.enqueue()`.",
  },
  {
    id: 3054,
    category: "Android",
    subcategory: "Data & Networking",
    level: "intermediate",
    q: "Paging 3 library là gì và tại sao nên dùng?",
    a: "Paging 3 là thư viện phân trang được khuyến nghị, load hiệu quả dataset lớn từ nguồn local/remote. Bạn tạo `PagingSource` (hoặc `RemoteMediator` cho network+database), dùng `Pager` để tạo paginated stream, và collect `PagingData` trong UI với `collectAsLazyPagingItems()`. Nó xử lý tự động loading, appending, retry, và deduplication, loại bỏ lỗi phân trang thủ công.",
    q_en: "What is the Paging 3 library and why use it?",
    a_en: "Paging 3 is the recommended pagination library that efficiently loads large datasets from local/remote sources. You create a `PagingSource` (or `RemoteMediator` for network+database), use `Pager` to create paginated streams, and collect `PagingData` in the UI with `collectAsLazyPagingItems()`. It handles loading, appending, retrying, and deduplication automatically.",
  },

  // ─── NÂNG CAO ─────────────────────────────────────────────────────────────────
  {
    id: 3055,
    category: "Android",
    subcategory: "Nâng Cao",
    level: "advanced",
    q: "ProGuard và R8 là gì, chúng tối ưu app như thế nào?",
    a: "R8 (người kế nhiệm hiện đại của ProGuard) shrink code không dùng (tree shaking), obfuscate tên class/method để bảo mật, và optimize code cho runtime performance. Chạy trong release build, giảm đáng kể kích thước app và thời gian khởi động. Bật bằng `android.enableR8 = true` trong gradle.properties. Hiểu cách cấu hình R8 rất quan trọng cho production release.",
    q_en: "What is ProGuard and R8, and how do they optimize apps?",
    a_en: "R8 (the modern successor to ProGuard) shrinks unused code (tree shaking), obfuscates class/method names for security, and optimizes code for runtime performance. It runs during the release build, significantly reducing app size and startup time. Enable with `android.enableR8 = true` in gradle.properties. Understanding R8 configuration is critical for production releases.",
  },
  {
    id: 3056,
    category: "Android",
    subcategory: "Nâng Cao",
    level: "advanced",
    q: "App Bundle là gì và chúng giảm kích thước app như thế nào?",
    a: "App Bundle là định dạng phân phối Android (upload lên Play Store) tự sinh APK được tối ưu cho từng cấu hình thiết bị. Thay vì một APK 100MB, người dùng chỉ nhận 20-30MB với tính năng phù hợp thiết bị của họ. Google Play tự động xử lý APK splitting theo density, ngôn ngữ, ABI, và dynamic feature. Luôn dùng App Bundle cho release.",
    q_en: "What are App Bundles and how do they reduce app size?",
    a_en: "App Bundles are an Android distribution format (uploaded to Play Store) that generates optimized APKs for each device configuration. Instead of one 100MB APK, a user gets only 20-30MB with features for their device. Google Play automatically handles APK splitting based on density, language, ABI, and dynamic features. Always use App Bundles for release.",
  },
  {
    id: 3057,
    category: "Android",
    subcategory: "Nâng Cao",
    level: "advanced",
    q: "Dynamic feature module là gì và khi nào nên dùng?",
    a: "Dynamic feature module là các tính năng tùy chọn/theo yêu cầu mà người dùng có thể download sau khi cài. Dùng cho tính năng lớn không phải ai cũng cần (như AR filter hay advanced editor). Chúng được download theo yêu cầu, giảm kích thước cài đặt ban đầu. Implement qua conditional delivery trong Google Play Console. Truy cập bằng `SplitCompat` và dynamic feature API.",
    q_en: "What are dynamic feature modules and when should you use them?",
    a_en: "Dynamic feature modules are optional/on-demand features users can download after install. Use them for large features not needed by all users (like AR filters or advanced editors). They're downloaded on-demand, reducing initial install size. Implement with conditional delivery in Google Play Console and access using `SplitCompat` and dynamic feature APIs.",
  },
  {
    id: 3058,
    category: "Android",
    subcategory: "Nâng Cao",
    level: "advanced",
    q: "Làm thế nào để tối ưu app performance và các metric quan trọng là gì?",
    a: "Đo lường bằng Android Profiler (CPU, memory, network). Các metric quan trọng: thời gian khởi động app (cold/warm/hot), frame rate (mục tiêu 60fps), memory footprint, và battery drain. Tối ưu bằng: giảm tác vụ trên main thread, lazy-loading, caching, layout hiệu quả (Compose nhanh hơn XML), load ảnh đúng cách, và xử lý tác vụ nền đúng chỗ. Profile trước để tìm bottleneck — tối ưu sớm là lãng phí.",
    q_en: "How do you optimize app performance and what are the key metrics?",
    a_en: "Measure with Android Profiler (CPU, memory, network). Key metrics: app startup time (cold/warm/hot), frame rate (60fps target), memory footprint, battery drain. Optimize by: reducing main thread work, lazy-loading, caching, efficient layouts (Compose is faster than XML), proper image loading, and background work. Profile first to find bottlenecks — premature optimization is wasteful.",
  },
  {
    id: 3059,
    category: "Android",
    subcategory: "Nâng Cao",
    level: "advanced",
    q: "Testing trong Jetpack Compose là gì và khác Espresso như thế nào?",
    a: 'Compose Testing dùng semantics-based matcher thay vì view hierarchy. Dùng `ComposeTestRule`, `createComposeRule()`, và matcher như `onNodeWithText()`, `onNodeWithContentDescription()`. Khác Espresso tìm view theo cấu trúc, Compose testing query cây composable theo ngữ nghĩa. \n\n**Ví dụ:** `composeTestRule.onNodeWithText("Button").performClick()`. Đáng tin cậy và nhanh hơn Espresso truyền thống.',
    q_en: "What is testing in Jetpack Compose and how does it differ from Espresso?",
    a_en: 'Compose Testing uses semantics-based matchers rather than view hierarchies. Use `ComposeTestRule`, `createComposeRule()`, and matchers like `onNodeWithText()`, `onNodeWithContentDescription()`. Unlike Espresso which finds views, Compose testing queries the composable tree semantically. \n\n**Example:** `composeTestRule.onNodeWithText("Button").performClick()`. It\'s more reliable and faster than traditional Espresso testing.',
  },
  {
    id: 3060,
    category: "Android",
    subcategory: "Nâng Cao",
    level: "advanced",
    q: "Kotlin Multiplatform (KMP) là gì và liên quan đến Android như thế nào?",
    a: "KMP cho phép chia sẻ code giữa Android, iOS, và web platform bằng Kotlin. Bạn viết business logic một lần trong common module, rồi code platform-specific cho từng nền tảng. Với Android app, KMP cho phép chia sẻ code với iOS hay backend service. Không bắt buộc nhưng mạnh mẽ cho tổ chức support nhiều platform. Setup với plugin `kotlin-multiplatform` và source set `commonMain`, `androidMain`.",
    q_en: "What is Kotlin Multiplatform (KMP) and how does it relate to Android?",
    a_en: "KMP allows sharing code between Android, iOS, and web platforms using Kotlin. You write business logic once in the common module, then platform-specific code for each platform. For Android apps, KMP enables code sharing with iOS development or backend services. Set up with the `kotlin-multiplatform` plugin and `commonMain`, `androidMain` source sets.",
  },
  {
    id: 3061,
    category: "Android",
    subcategory: "Nâng Cao",
    level: "advanced",
    q: "Instrumentation testing và unit testing khác nhau như thế nào và khi nào dùng loại nào?",
    a: "Unit test chạy trên JVM không cần Android dependency (nhanh, không cần thiết bị). Dùng cho ViewModel, Repository, và utility logic. Instrumentation test chạy trên thiết bị/emulator với đầy đủ Android framework (chậm, cần thiết bị). Dùng cho Activity, Fragment, và UI behavior. Theo testing pyramid: 70% unit test, 20% integration, 10% UI test. Tool: JUnit + Mockito cho unit, Espresso/Compose Testing cho UI.",
    q_en: "Explain instrumentation testing and unit testing, and when to use each.",
    a_en: "Unit tests run on the JVM without Android dependencies (fast, no device needed). Use for ViewModel, Repository, and utility logic. Instrumentation tests run on devices/emulators with full Android framework access (slow, need device). Use for Activities, Fragments, and UI behavior. Follow the testing pyramid: 70% unit tests, 20% integration, 10% UI tests.",
  },
  {
    id: 3062,
    category: "Android",
    subcategory: "Nâng Cao",
    level: "advanced",
    q: "Best practice về memory management trong Android là gì?",
    a: "Tránh memory leak bằng cách: không giữ strong reference đến Activity/View (dùng weak ref), cancel coroutine trong lifecycle method, unregister listener, implement singleton đúng cách với application context. Dùng thư viện `LeakCanary` để tự động phát hiện leak. Monitor bằng memory tab trong Android Profiler. Phần lớn leak đến từ Activity reference vô tình trong background thread hoặc singleton.",
    q_en: "What are the best practices for memory management in Android?",
    a_en: "Avoid memory leaks by: not holding strong references to Activities/Views (use weak refs), canceling coroutines in lifecycle methods, unregistering listeners, and properly implementing singleton patterns with application context. Use `LeakCanary` library to detect leaks automatically. Monitor with Android Profiler's memory tab. Most leaks come from accidental Activity references in background threads or singletons.",
  },
  {
    id: 3063,
    category: "Android",
    subcategory: "Nâng Cao",
    level: "advanced",
    q: "Làm thế nào để implement CI/CD cho Android app?",
    a: "Setup GitHub Actions/GitLab CI để chạy test, build APK/Bundle, và deploy tự động. Pipeline điển hình: test mỗi push, build signed release khi có tag, upload lên Firebase App Distribution hoặc Play Store. Dùng secret cho signing key. Implement pre-commit hook để bắt lỗi sớm ở local. Automated testing giảm human error và đảm bảo build nhất quán.",
    q_en: "How do you implement CI/CD for Android apps?",
    a_en: "Set up GitHub Actions/GitLab CI to run tests, build APK/Bundle, and deploy automatically. Typical pipeline: test on every push, build signed release on tags, upload to Firebase App Distribution or Play Store. Use secrets for signing keys. Implement pre-commit hooks to catch issues locally. Automated testing reduces human error and ensures consistent builds.",
  },
  {
    id: 3064,
    category: "Android",
    subcategory: "Nâng Cao",
    level: "advanced",
    q: "Certificate pinning là gì và cách implement?",
    a: 'Certificate pinning đảm bảo app chỉ giao tiếp với server có certificate SSL cụ thể, ngăn chặn tấn công man-in-the-middle. Dùng `CertificatePinner` của OkHttp để pin public key hash. \n\n**Ví dụ:** `CertificatePinner.Builder().add("example.com", "sha256/AAAAAAA...").build()`. Cần xử lý việc xoay certificate. Implement cho app nhạy cảm (banking, social network) hoặc dữ liệu có giá trị cao.',
    q_en: "What is certificate pinning and how do you implement it?",
    a_en: 'Certificate pinning ensures your app only communicates with servers having a specific SSL certificate, preventing man-in-the-middle attacks. Use OkHttp\'s `CertificatePinner` to pin public key hashes. \n\n**Example:** `CertificatePinner.Builder().add("example.com", "sha256/AAAAAAA...").build()`. This adds security but requires handling certificate rotation. Implement for sensitive apps (banking, social networks).',
  },

  // ─── ADDITIONAL ───────────────────────────────────────────────────────────────
  {
    id: 3065,
    category: "Android",
    subcategory: "Architecture",
    level: "intermediate",
    q: "Observer pattern là gì và được dùng như thế nào trong Android?",
    a: "Observer pattern cho phép các object (observer) đăng ký nhận thông báo về thay đổi trạng thái của object khác (subject). Trong Android, LiveData/StateFlow là các implementation của pattern này. Khi bạn gọi `observe()` hoặc `collect()`, bạn đang đăng ký làm observer. Khi dữ liệu thay đổi, tất cả observer được thông báo, decoupling nguồn dữ liệu khỏi UI.",
    q_en: "What is the Observer pattern and how is it used in Android?",
    a_en: "Observer pattern lets objects (observers) subscribe to state changes of another object (subject). In Android, LiveData/StateFlow are implementations. When you call `observe()` or `collect()`, you're registering as an observer. When data changes, all observers are notified. This pattern decouples the data source from UI, allowing multiple UIs to react to the same data.",
  },
  {
    id: 3066,
    category: "Android",
    subcategory: "Android Core",
    level: "intermediate",
    q: "Làm thế nào để xử lý configuration change như xoay màn hình?",
    a: "Configuration change destroy và recreate Activity/Fragment. Bảo toàn state bằng: SavedInstanceState (UI state tạm thời), ViewModel (logic và data), và SharedPreferences/DataStore (data lâu dài). Không nên dùng `android:configChanges` để tắt recreation trừ khi thực sự cần thiết. Trong Compose, state tự động tồn tại qua rotation nếu dùng `rememberSaveable`. State management đúng cách giúp rotation trong suốt với người dùng.",
    q_en: "How do you handle configuration changes (like screen rotation)?",
    a_en: "Configuration changes destroy and recreate Activities/Fragments. Preserve state using: SavedInstanceState (temporary UI state), ViewModel (logic and data), and SharedPreferences/DataStore (persistent data). Don't use `android:configChanges` to suppress recreation unless absolutely necessary. In Compose, state automatically survives rotation if using `rememberSaveable`.",
  },
  {
    id: 3067,
    category: "Kotlin",
    subcategory: "Kotlin Cơ Bản",
    level: "advanced",
    q: "Sự khác biệt giữa hot flow và cold flow?",
    a: "Cold flow (như `flow {}` hay `PagingSource`) chỉ phát giá trị khi có collector và bắt đầu lại từ đầu cho mỗi collector. Hot flow (như `StateFlow`, `SharedFlow`) phát giá trị bất kể có collector hay không và chia sẻ cùng một stream. `StateFlow` là hot flow có current value — dùng cho state. `Flow` thông thường là cold — dùng cho luồng event. Hot flow hiệu quả bộ nhớ hơn khi có nhiều collector.",
    q_en: "What is the difference between hot and cold flows?",
    a_en: "Cold flows (like `flow {}` or `PagingSource`) only produce values when collected and start from the beginning for each collector. Hot flows (like `StateFlow`, `SharedFlow`) emit values regardless of collectors and share the same stream. `StateFlow` is hot with a current value — use it for state. Regular `Flow` is cold — use for event streams. Hot flows are more memory-efficient for multiple collectors.",
  },
];
