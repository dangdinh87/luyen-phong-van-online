import type { QAItem } from "../interview-data";

export const CSHARP_DATA: QAItem[] = [
  // === CƠ BẢN (Fundamentals) ===
  {
    id: 2701,
    category: "C#",
    subcategory: "Cơ Bản",
    level: "beginner",
    q: "Value type và reference type khác nhau như thế nào trong C#?",
    a: "Value type (`int`, `float`, `bool`, `struct`) lưu dữ liệu trực tiếp trên stack và tạo bản sao khi truyền đi. Reference type (`class`, `string`, `array`) lưu tham chiếu trên stack trỏ tới dữ liệu trên heap, khi truyền đi thì chia sẻ cùng một đối tượng. Value type nhanh hơn nhưng bị giới hạn bộ nhớ stack; reference type linh hoạt hơn nhưng chịu overhead của garbage collection.",
    q_en: "What is the difference between value types and reference types in C#?",
    a_en: "Value types (`int`, `float`, `bool`, `struct`) store actual data on the stack and copy data when passed around. Reference types (`class`, `string`, `array`) store references on the stack pointing to heap data — passing them shares the same object. Value types are faster but stack-space limited; reference types offer flexibility at the cost of GC overhead.",
  },
  {
    id: 2702,
    category: "C#",
    subcategory: "Cơ Bản",
    level: "beginner",
    q: "Boxing và unboxing trong C# là gì?",
    a: "Boxing là quá trình đóng gói một value type thành reference type — ví dụ `int x = 5` được boxing thành object trên heap. Unboxing là quá trình ngược lại: trích xuất giá trị gốc ra, ví dụ `int y = (int)boxedValue`. Boxing phát sinh overhead đáng kể nên cần tránh trong các vòng lặp quan trọng về hiệu năng. Dùng generic collections như `List<int>` để loại bỏ boxing cho value type.",
    q_en: "What is boxing and unboxing in C#?",
    a_en: "Boxing wraps a value type into a reference type — e.g., `int x = 5` becomes a boxed object on the heap. Unboxing extracts the original value back: `int y = (int)boxedValue`. Boxing adds overhead and should be avoided in performance-critical loops. Modern generic collections like `List<int>` eliminate boxing for value types.",
  },
  {
    id: 2703,
    category: "C#",
    subcategory: "Cơ Bản",
    level: "beginner",
    q: "Nullable type là gì và khi nào nên dùng?",
    a: "Nullable type cho phép value type nhận giá trị null: `int? age = null;`. Bên dưới nó sử dụng `Nullable<T>` để bọc value type lại. Dùng nullable type khi một giá trị có thể hợp lệ là vắng mặt, chẳng hạn trường tùy chọn hay dữ liệu null từ database. Kiểm tra null bằng thuộc tính `HasValue` hoặc toán tử `??` để cung cấp giá trị mặc định.",
    q_en: "What is a nullable type and when should you use it?",
    a_en: "Nullable types allow value types to hold null values: `int? age = null;`. They wrap value types in a `Nullable<T>` container. Use them when a value may legitimately be absent — optional fields or database nulls. Check nullability with the `HasValue` property or the null-coalescing operator `??` to provide defaults.",
  },
  {
    id: 2704,
    category: "C#",
    subcategory: "Cơ Bản",
    level: "beginner",
    q: "Tại sao String lại immutable trong C#?",
    a: "String trong C# là immutable — khi tạo ra thì không bao giờ thay đổi. Các thao tác như `string.Replace()` tạo ra chuỗi mới chứ không sửa chuỗi cũ. Tính immutable đảm bảo thread-safety và cho phép string interning (tối ưu bộ nhớ bằng cách tái dùng chuỗi giống nhau). Tuy nhiên việc ghép chuỗi trong vòng lặp sẽ tốn nhiều bộ nhớ — hãy dùng `StringBuilder` thay thế.",
    q_en: "Why is String immutable in C#?",
    a_en: "Strings are immutable — once created, they never change. Operations like `string.Replace()` create new strings rather than modifying existing ones. Immutability ensures thread-safety and enables string interning (memory optimization). However, concatenating strings in loops is expensive — use `StringBuilder` instead.",
  },
  {
    id: 2705,
    category: "C#",
    subcategory: "Cơ Bản",
    level: "beginner",
    q: "CLR (Common Language Runtime) là gì?",
    a: "CLR là bộ máy thực thi quản lý các chương trình .NET sau khi biên dịch thành Intermediate Language (IL). Nó đảm nhiệm garbage collection, quản lý bộ nhớ, type safety, bảo mật và xử lý exception. Tất cả ngôn ngữ .NET đều biên dịch ra IL, sau đó CLR dùng JIT compiler để chuyển thành mã máy lúc chạy, giúp đạt được khả năng cross-platform.",
    q_en: "What is the Common Language Runtime (CLR)?",
    a_en: "The CLR is the execution engine managing .NET programs after compilation to Intermediate Language (IL). It handles garbage collection, memory management, type safety, security, and exception handling. All .NET languages compile to IL, then CLR JIT-compiles to native machine code at runtime, enabling cross-platform execution.",
  },
  {
    id: 2706,
    category: "C#",
    subcategory: "Cơ Bản",
    level: "beginner",
    q: "Garbage collection hoạt động như thế nào và khi nào thì chạy?",
    a: "Garbage collection tự động giải phóng bộ nhớ của các object không còn được tham chiếu. GC chạy khi áp lực bộ nhớ tăng cao, hệ thống sắp hết RAM, hoặc khi gọi tường minh `GC.Collect()`. Các object không còn accessible từ root references (stack, static fields) sẽ trở nên eligible để thu gom. GC là non-deterministic — dùng `IDisposable` cho tài nguyên cần cleanup ngay lập tức.",
    q_en: "What is garbage collection and when does it run?",
    a_en: "Garbage collection automatically frees memory from unreachable objects. The GC runs when memory pressure increases, system memory runs low, or `GC.Collect()` is called explicitly. Objects unreachable from root references become eligible for collection. GC is non-deterministic — use `IDisposable` for resources requiring immediate cleanup.",
  },
  {
    id: 2707,
    category: "C#",
    subcategory: "Cơ Bản",
    level: "beginner",
    q: "Managed code và unmanaged code khác nhau như thế nào?",
    a: "Managed code chạy trong CLR với garbage collection, type safety và quản lý bộ nhớ tự động. Unmanaged code (C/C++) chạy ngoài .NET framework mà không có những bảo vệ đó. Khi tích hợp unmanaged code vào .NET, dùng P/Invoke hoặc COM interop và tự quản lý bộ nhớ thông qua `Marshal` và `GCHandle`.",
    q_en: "What is the difference between managed and unmanaged code?",
    a_en: "Managed code runs within the CLR with automatic garbage collection, type safety, and memory management. Unmanaged code (C/C++) operates outside the .NET framework without these protections. When integrating unmanaged code, use P/Invoke or COM interop and manually manage memory using `Marshal` and `GCHandle`.",
  },
  {
    id: 2708,
    category: "C#",
    subcategory: "Cơ Bản",
    level: "beginner",
    q: "`const` và `readonly` khác nhau như thế nào trong C#?",
    a: "`const` khai báo hằng số tại compile-time, giá trị được nhúng trực tiếp vào bytecode và luôn là `static`. `readonly` cho phép gán một lần duy nhất lúc khai báo hoặc trong constructor, có thể là per-instance. Dùng `const` cho các giá trị thực sự là hằng số không bao giờ thay đổi; dùng `readonly` cho các giá trị được tính toán lúc runtime hoặc cần biến thể theo instance.",
    q_en: "What is the difference between `const` and `readonly` in C#?",
    a_en: "`const` declares compile-time constants with values embedded directly into bytecode and is implicitly static. `readonly` allows a single assignment at declaration or inside a constructor, and can be per-instance. Use `const` for true never-changing constants; use `readonly` for values computed at runtime or needing per-instance variation.",
  },
  {
    id: 2709,
    category: "C#",
    subcategory: "Cơ Bản",
    level: "beginner",
    q: "String và StringBuilder khác nhau như thế nào và khi nào nên dùng cái nào?",
    a: "`String` là immutable — mỗi lần ghép chuỗi tạo ra một object mới, tốn bộ nhớ và CPU. `StringBuilder` là mutable và sửa đổi in-place, hiệu quả hơn nhiều khi ghép nhiều đoạn. Trong vòng lặp ghép chuỗi, `StringBuilder` vượt trội hoàn toàn. Dùng `StringBuilder` khi xây dựng chuỗi động từ nhiều phần; dùng `String` cho các thao tác đơn giản.",
    q_en: "What is the difference between String and StringBuilder and when should you use each?",
    a_en: "`String` is immutable — concatenation creates new objects, consuming memory and CPU. `StringBuilder` is mutable and modifies in-place, dramatically outperforming string concatenation in loops. Use `StringBuilder` when building dynamic strings from multiple pieces; use `String` for simple, infrequent operations.",
  },
  {
    id: 2710,
    category: "C#",
    subcategory: "Cơ Bản",
    level: "beginner",
    q: "Reflection trong C# là gì?",
    a: "Reflection cho phép truy xuất metadata về types, methods, properties và assemblies tại runtime. Dùng `typeof()` hoặc `obj.GetType()` để kiểm tra kiểu dữ liệu một cách động. Reflection được dùng trong plugin systems, serialization, ORM tools và dependency injection. Tuy nhiên nó có overhead về hiệu năng — cache kết quả reflection để tránh tra cứu lặp lại.",
    q_en: "What is reflection in C#?",
    a_en: "Reflection extracts metadata about types, methods, properties, and assemblies at runtime. Use `typeof()` or `obj.GetType()` to inspect types dynamically. Reflection enables plugin systems, serialization, ORM tools, and dependency injection, but carries performance overhead. Cache reflection results to avoid repeated lookups.",
  },
  {
    id: 2711,
    category: "C#",
    subcategory: "Cơ Bản",
    level: "beginner",
    q: "Toán tử `==` và phương thức `Equals()` khác nhau như thế nào?",
    a: "Toán tử `==` so sánh theo tham chiếu đối với class (trừ khi bị override) nhưng so sánh theo giá trị với struct và kiểu nguyên thủy. `Equals()` mặc định so sánh theo giá trị. Với custom types, nên override cả hai để nhất quán. Để tuân thủ best practice, hãy implement cả `IEquatable<T>` cùng với việc override `GetHashCode()`.",
    q_en: "What is the difference between the == operator and Equals()?",
    a_en: "The `==` operator compares references for classes (unless overloaded) but compares values for structs and primitives. `Equals()` compares values by default. Override both for custom types to keep them consistent. Best practice: also implement `IEquatable<T>` and override `GetHashCode()`.",
  },
  {
    id: 2712,
    category: "C#",
    subcategory: "Cơ Bản",
    level: "beginner",
    q: "Array và ArrayList khác nhau như thế nào trong C#?",
    a: "Array có kích thước cố định và kiểu đồng nhất: `int[] arr = new int[5]`. ArrayList thay đổi kích thước động và chấp nhận mixed types, nhưng gây boxing overhead cho value types. Ngày nay generic `List<T>` đã thay thế hoàn toàn ArrayList, cung cấp type safety mà không boxing. Luôn ưu tiên dùng `List<T>` thay vì `ArrayList`.",
    q_en: "What is the difference between Array and ArrayList in C#?",
    a_en: "Arrays have fixed size and homogeneous types: `int[] arr = new int[5]`. ArrayList dynamically resizes and accepts mixed types but causes boxing overhead for value types. Modern generic `List<T>` replaces ArrayList entirely, providing type-safety without boxing. Always prefer `List<T>` over `ArrayList`.",
  },

  // === OOP ===
  {
    id: 2713,
    category: "C#",
    subcategory: "OOP",
    level: "beginner",
    q: "Class và struct khác nhau như thế nào trong C#?",
    a: "Class là reference type lưu trên heap; struct là value type lưu trên stack. Class hỗ trợ kế thừa; struct thì không (mặc định là sealed). Class dùng reference equality; struct dùng value equality. Class truyền theo tham chiếu; struct tạo bản sao khi gán. Dùng class cho object phức tạp cần kế thừa; dùng struct cho dữ liệu nhỏ gọn, đơn giản.",
    q_en: "What is the difference between class and struct in C#?",
    a_en: "Classes are reference types stored on the heap; structs are value types stored on the stack. Classes support inheritance; structs do not (sealed by default). Classes use reference equality; structs use value equality. Classes pass by reference; structs copy on assignment. Use classes for complex objects with inheritance; use structs for small, simple data.",
  },
  {
    id: 2714,
    category: "C#",
    subcategory: "OOP",
    level: "intermediate",
    q: "Abstract class là gì và khi nào nên dùng?",
    a: "Abstract class định nghĩa blueprint chưa hoàn chỉnh với abstract methods (chỉ có chữ ký) và concrete methods (có implementation). Không thể khởi tạo trực tiếp mà phải kế thừa và implement các abstract member. Dùng abstract class làm base class khi muốn chia sẻ logic chung và bắt buộc subclass implement những phần cụ thể. Khác interface, abstract class có constructor và trạng thái.",
    q_en: "What is an abstract class and when should you use it?",
    a_en: "Abstract classes define incomplete blueprints with abstract methods (signature only) and concrete methods (with implementation). You cannot instantiate them directly — you must derive and implement abstract members. Use abstract classes as base classes that share common behavior while forcing subclasses to implement specific methods. Unlike interfaces, they support constructors and state.",
  },
  {
    id: 2715,
    category: "C#",
    subcategory: "OOP",
    level: "intermediate",
    q: "Abstract class và interface khác nhau như thế nào?",
    a: "Abstract class có constructor, fields, và có thể mix abstract/concrete methods. Interface (trước C# 8) là pure contract chỉ chứa abstract members. Một class chỉ kế thừa được một abstract class nhưng có thể implement nhiều interface. Dùng abstract class khi cần chia sẻ behavior chung; dùng interface khi định nghĩa contract cho nhiều type không liên quan nhau. Từ C# 8+, interface có thể có default method implementation, nhưng abstract class vẫn giữ lợi thế với constructor và fields.",
    q_en: "What is the difference between an abstract class and an interface?",
    a_en: "Abstract classes have constructors, state fields, and mix abstract/concrete methods. Interfaces (before C# 8) are pure contracts with only abstract members. A class can inherit only one abstract class but implement multiple interfaces. Use abstract classes for shared behavior; use interfaces for contracts across unrelated types. Since C# 8+, interfaces can have default method implementations, but abstract classes still have the advantage of constructors and fields.",
  },
  {
    id: 2716,
    category: "C#",
    subcategory: "OOP",
    level: "intermediate",
    q: "Sealed class là gì và tại sao nên dùng?",
    a: "Sealed class ngăn không cho class nào khác kế thừa nó. Đánh dấu một implementation cuối cùng là `sealed` để tránh subclassing không mong muốn và giúp compiler tối ưu hóa tốt hơn. Sealed class an toàn hơn vì không thể bị mở rộng theo cách bất ngờ. Dùng `sealed` cho các immutable data class và để khóa cứng chuỗi kế thừa.",
    q_en: "What is a sealed class and why use it?",
    a_en: "Sealed classes prevent inheritance — no class can derive from them. Marking a final implementation as `sealed` prevents accidental subclassing and enables compiler optimizations. Sealed classes are safer by design since they cannot be extended unexpectedly. Use `sealed` for immutable data classes and to finalize inheritance chains.",
  },
  {
    id: 2717,
    category: "C#",
    subcategory: "OOP",
    level: "intermediate",
    q: "Record trong C# là gì và khác class như thế nào?",
    a: "Record (C# 9+) là immutable reference type với equality dựa trên giá trị field, không phải tham chiếu — hai record có cùng giá trị được coi là bằng nhau. Record có cú pháp ngắn gọn với positional parameters và tự động implement `GetHashCode()` cùng `Equals()`. Dùng record cho DTOs, value objects và domain model immutable. Class vẫn phù hợp hơn khi cần mutable state và behavior phức tạp.",
    q_en: "What are records in C# and how do they differ from classes?",
    a_en: "Records (C# 9+) are immutable reference types with built-in value-based equality — two records with identical field values are considered equal. They provide concise syntax with positional parameters and automatically implement `GetHashCode()` and `Equals()`. Use records for DTOs, value objects, and immutable domain models. Classes remain better suited for mutable state and complex behavior.",
  },
  {
    id: 2718,
    category: "C#",
    subcategory: "OOP",
    level: "intermediate",
    q: "Interface là gì và C# hỗ trợ multiple inheritance như thế nào?",
    a: "Interface là một contract chỉ định các method/property mà class phải implement. C# không hỗ trợ kế thừa nhiều class nhưng cho phép implement nhiều interface. Điều này giúp đạt được polymorphism trên các type không liên quan nhau. Implement nhiều interface để thỏa mãn nhiều contract đồng thời — đây là cách thay thế thực tế cho multiple class inheritance.",
    q_en: "What is an interface and how does C# support multiple inheritance?",
    a_en: "An interface is a contract specifying what methods/properties a class must implement. C# does not support multiple class inheritance but allows implementing multiple interfaces. This enables polymorphism across unrelated types. Implement multiple interfaces to satisfy multiple contracts simultaneously — this is the practical substitute for multiple class inheritance.",
  },
  {
    id: 2719,
    category: "C#",
    subcategory: "OOP",
    level: "intermediate",
    q: "Extension method là gì trong C#?",
    a: "Extension method cho phép thêm method mới vào class đã có mà không cần subclass hay sửa source code. Khai báo chúng là `static` trong static class: `public static void CustomMethod(this MyClass obj) {}`. Extension method hữu ích để thêm functionality cho third-party types hoặc framework types và hỗ trợ method chaining. Tránh lạm dụng vì có thể gây khó hiểu khi đọc code.",
    q_en: "What are extension methods in C#?",
    a_en: "Extension methods add new methods to existing classes without subclassing or modifying source code. Declare them as `static` in a static class: `public static void CustomMethod(this MyClass obj) {}`. They enable method chaining on third-party and framework types. Use sparingly — overuse creates confusion about where methods are defined.",
  },
  {
    id: 2720,
    category: "C#",
    subcategory: "OOP",
    level: "intermediate",
    q: "Generics trong C# là gì và tại sao quan trọng?",
    a: "Generics cung cấp code type-safe và có thể tái dùng thông qua cú pháp `<T>`: `List<int>`, `Dictionary<string, object>`. Chúng loại bỏ boxing/unboxing overhead, cho phép kiểm tra kiểu tại compile-time và cải thiện hiệu năng đáng kể. Dùng generics để tạo containers và methods linh hoạt hoạt động được với nhiều kiểu dữ liệu trong khi vẫn đảm bảo type safety.",
    q_en: "What are generics and why are they important in C#?",
    a_en: "Generics provide type-safe, reusable code via `<T>` syntax: `List<int>`, `Dictionary<string, object>`. They eliminate boxing/unboxing overhead, enable compile-time type checking, and significantly improve performance. Use generics to create flexible containers and methods that work across types while maintaining full type safety.",
  },
  {
    id: 2721,
    category: "C#",
    subcategory: "OOP",
    level: "intermediate",
    q: "Indexer trong C# là gì?",
    a: "Indexer cho phép truy cập object theo kiểu mảng bằng cặp dấu ngoặc vuông: `obj[5]`, ngay cả khi object không phải collection. Định nghĩa với cú pháp `[]` và các accessor `get/set`. Indexer đơn giản hóa việc truy cập vào collection nội bộ của object và làm cho API trực quan hơn. Dùng indexer khi muốn cung cấp pattern truy cập quen thuộc cho custom data structure.",
    q_en: "What is an indexer in C#?",
    a_en: "Indexers enable array-like access to objects using square brackets: `obj[5]`, even for non-collection types. Defined with `[]` syntax and `get/set` accessors, they simplify accessing inner collections. Use indexers to provide intuitive, familiar access patterns for custom data structures.",
  },
  {
    id: 2722,
    category: "C#",
    subcategory: "OOP",
    level: "intermediate",
    q: "Property khác field như thế nào trong C#?",
    a: "Property cung cấp quyền truy cập có kiểm soát vào private field thông qua `get/set` accessors, thực thi encapsulation. Field là biến member trực tiếp. Property cho phép validation, computed values và change notification. Luôn expose public state qua property chứ không phải field. Auto-property (`{ get; set; }`) đơn giản hóa pattern phổ biến nhất.",
    q_en: "What are properties and how do they differ from fields in C#?",
    a_en: "Properties provide controlled access to private fields via `get/set` accessors, implementing encapsulation. Fields are direct member variables. Properties enable validation, computed values, and change notifications. Always expose public state through properties, not fields. Auto-properties (`{ get; set; }`) simplify the most common patterns.",
  },

  // === LINQ ===
  {
    id: 2723,
    category: "C#",
    subcategory: "LINQ",
    level: "intermediate",
    q: "LINQ là gì và có những ưu điểm gì?",
    a: "LINQ (Language Integrated Query) thống nhất cách truy vấn các nguồn dữ liệu khác nhau (collections, database, XML) bằng cú pháp nhất quán. \n\n**Ưu điểm:** type safety tại compile-time, IntelliSense hỗ trợ, query dễ đọc như SQL, giảm boilerplate. Hỗ trợ cả method syntax và query syntax — compiler chuyển đổi cả hai thành biểu diễn nội tại giống nhau.",
    q_en: "What is LINQ and what are its main advantages?",
    a_en: "LINQ (Language Integrated Query) unifies querying diverse data sources (collections, databases, XML) with identical syntax. \n\n**Advantages:** compile-time type safety, IntelliSense support, readable SQL-like queries, reduced boilerplate loops. Supports both method and query syntax — compilers transform both to the same underlying representation.",
  },
  {
    id: 2724,
    category: "C#",
    subcategory: "LINQ",
    level: "intermediate",
    q: "`IEnumerable` và `IQueryable` khác nhau như thế nào?",
    a: "`IEnumerable<T>` hoạt động trên in-memory collections, tải toàn bộ dữ liệu rồi mới filter (LINQ to Objects). `IQueryable<T>` nhắm vào nguồn dữ liệu remote (database), xây dựng expression tree và thực thi filter tại nguồn để tiết kiệm băng thông. Dùng `IQueryable` cho dataset lớn; dùng `IEnumerable` cho collections đã có sẵn trong bộ nhớ.",
    q_en: "What is the difference between IEnumerable and IQueryable?",
    a_en: "`IEnumerable<T>` operates on in-memory collections, filtering after loading all data (LINQ to Objects). `IQueryable<T>` targets remote sources like databases, building expression trees and executing filtering at the source for efficiency. Use `IQueryable` for large datasets; use `IEnumerable` for in-memory collections.",
  },
  {
    id: 2725,
    category: "C#",
    subcategory: "LINQ",
    level: "intermediate",
    q: "Deferred execution và immediate execution trong LINQ là gì?",
    a: "Deferred execution trì hoãn việc thực thi query cho đến khi dữ liệu thực sự được truy cập — `var result = collection.Where(x => x > 5)` chưa chạy cho đến khi bạn iterate hoặc gọi `ToList()`. Immediate execution chạy ngay lập tức: `ToList()`, `ToArray()`, `Count()`, `First()`. Hầu hết LINQ operators dùng deferred execution. Hiểu rõ khi nào query thực sự chạy để tránh bug khó tìm.",
    q_en: "What is the difference between deferred and immediate execution in LINQ?",
    a_en: "Deferred execution delays query evaluation until data is accessed — `var result = collection.Where(x => x > 5)` does not execute until you iterate or call `ToList()`. Immediate execution runs right away: `ToList()`, `ToArray()`, `Count()`, `First()`. Most LINQ operators use deferred execution. Understanding when queries actually run is key to avoiding subtle bugs.",
  },
  {
    id: 2726,
    category: "C#",
    subcategory: "LINQ",
    level: "intermediate",
    q: "Query syntax và method syntax trong LINQ khác nhau như thế nào?",
    a: "Query syntax trông giống SQL: `var result = from x in list where x > 5 select x;`. Method syntax dùng lambda expressions: `var result = list.Where(x => x > 5).Select(x => x);`. Cả hai đều tương đương về mặt chức năng — compiler chuyển query syntax thành method syntax nội bộ. Query syntax phù hợp cho join phức tạp; method syntax linh hoạt hơn cho các phép biến đổi chuỗi.",
    q_en: "What is the difference between query syntax and method syntax in LINQ?",
    a_en: "Query syntax resembles SQL: `var result = from x in list where x > 5 select x;`. Method syntax uses lambda expressions: `var result = list.Where(x => x > 5);`. Both are functionally equivalent — compilers transform query syntax to method syntax internally. Query syntax suits complex joins; method syntax offers more flexibility for chained transformations.",
  },
  {
    id: 2727,
    category: "C#",
    subcategory: "LINQ",
    level: "intermediate",
    q: "N+1 query problem là gì và giải quyết như thế nào?",
    a: "N+1 xảy ra khi một query tải parent records, rồi với mỗi parent lại có thêm một query riêng để tải children (1 + N queries). Lazy loading trong Entity Framework hay gây ra vấn đề này. Giải quyết bằng eager loading: `.Include()` cho quan hệ trực tiếp, `.ThenInclude()` cho quan hệ lồng nhau. Hoặc dùng `.Select()` projection để chỉ tải đúng field cần thiết trong một query duy nhất.",
    q_en: "What is the N+1 query problem and how do you solve it?",
    a_en: "N+1 occurs when one query fetches parent records, then separate queries fetch children for each parent (1 + N queries). Lazy loading in Entity Framework is a common cause. Solve with eager loading: `.Include()` for direct relationships, `.ThenInclude()` for nested ones. Or use `.Select()` projections to load only needed fields in a single query.",
  },
  {
    id: 2728,
    category: "C#",
    subcategory: "LINQ",
    level: "intermediate",
    q: "`GroupBy` trong LINQ hoạt động như thế nào?",
    a: "`GroupBy` tổ chức các phần tử theo một key, trả về các group mà mỗi group chứa key và các element tương ứng: `students.GroupBy(s => s.Department)`. Hỗ trợ grouping theo nhiều key thông qua anonymous object: `students.GroupBy(s => new { s.Department, s.Year })`. Kết quả có thể được tổng hợp thêm bằng `Count()`, `Sum()` hoặc `Select()` trên từng group.",
    q_en: "What is GroupBy in LINQ and how does it work?",
    a_en: "`GroupBy` organizes elements by a key, returning groups where each group contains a key and its associated elements: `students.GroupBy(s => s.Department)`. Supports multiple-key grouping via anonymous objects: `students.GroupBy(s => new { s.Department, s.Year })`. Results can be further aggregated with `Count()`, `Sum()`, or `Select()` per group.",
  },
  {
    id: 2729,
    category: "C#",
    subcategory: "LINQ",
    level: "intermediate",
    q: "`Select` và `SelectMany` khác nhau như thế nào trong LINQ?",
    a: "`Select` chiếu mỗi phần tử sang dạng mới (quan hệ 1-1). `SelectMany` làm phẳng các nested collections thành một sequence duy nhất (1-nhiều): `students.SelectMany(s => s.Subjects)` gộp tất cả danh sách subject thành một. `Select` giữ nguyên cấu trúc phân cấp; `SelectMany` xóa bỏ nesting — hữu ích khi cần flatten collection lồng nhau.",
    q_en: "What is the difference between Select and SelectMany in LINQ?",
    a_en: "`Select` projects each element into a new form (1-to-1 mapping). `SelectMany` flattens nested collections into a single sequence (1-to-many): `students.SelectMany(s => s.Subjects)` combines all subject lists into one. `Select` maintains hierarchy; `SelectMany` removes nesting — useful for flattening nested collections.",
  },
  {
    id: 2730,
    category: "Entity Framework",
    subcategory: "Entity Framework",
    level: "intermediate",
    q: "`AsNoTracking()` là gì và khi nào nên dùng?",
    a: "`AsNoTracking()` tắt change tracking của Entity Framework cho các entity được truy vấn. Mặc định, EF theo dõi mọi entity để phát hiện thay đổi khi `SaveChanges()` được gọi — điều này tiêu tốn bộ nhớ và CPU. Với các query read-only, dùng `.AsNoTracking()` để cải thiện hiệu năng và giảm memory usage. Áp dụng bất cứ khi nào bạn đọc dữ liệu mà không có ý định sửa đổi.",
    q_en: "What is AsNoTracking() in EF Core and when should you use it?",
    a_en: "`AsNoTracking()` disables Entity Framework change tracking for retrieved entities. Default tracking detects modifications during `SaveChanges()` but consumes memory and processing overhead. For read-only queries, add `.AsNoTracking()` to improve performance and reduce memory usage. Use it whenever you read data without intending to modify it.",
  },

  // === ASYNC & THREADING ===
  {
    id: 2731,
    category: "C#",
    subcategory: "Async & Threading",
    level: "intermediate",
    q: "Async/await khác multithreading truyền thống như thế nào?",
    a: "Async/await xử lý I/O operations hiệu quả mà không block thread — thread được giải phóng trong lúc chờ và được trả lại thread pool. Multithreading tạo thread mới (tốn khoảng 1MB stack mỗi thread). Async phù hợp và scalable hơn cho I/O-bound work (web request, database calls). Dùng threading cho CPU-bound work cần song song hóa thực sự.",
    q_en: "What is the difference between async/await and traditional multithreading?",
    a_en: "Async/await handles I/O operations efficiently without blocking threads — threads are freed during waits and returned to the thread pool. Multithreading creates new threads (~1MB stack each). Async is cleaner and more scalable for I/O-bound workloads (web requests, database calls). Use multithreading for CPU-bound work requiring true parallelism.",
  },
  {
    id: 2732,
    category: "C#",
    subcategory: "Async & Threading",
    level: "intermediate",
    q: "Tại sao nên tránh blocking calls như `.Result` và `.Wait()`?",
    a: "Các blocking call như `.Result` dừng thread hiện tại lại để chờ task hoàn thành. Trong UI hoặc ASP.NET, nếu continuation của task cần dùng đúng thread đó thì cả hai cùng chờ nhau — deadlock kinh điển. Luôn dùng `await` thay thế vì nó giải phóng thread và resume sau. Không bao giờ block trong async context.",
    q_en: "Why should you avoid blocking calls like .Result and .Wait()?",
    a_en: "Blocking calls like `.Result` halt the current thread waiting for task completion. In UI or ASP.NET contexts, if the task's continuation needs that same thread, both end up waiting on each other — a classic deadlock. Always use `await` instead; it frees the thread and resumes later. Never block in async contexts.",
  },
  {
    id: 2733,
    category: "C#",
    subcategory: "Async & Threading",
    level: "intermediate",
    q: "`ConfigureAwait(false)` là gì và khi nào nên dùng?",
    a: "`ConfigureAwait(false)` báo cho async runtime không cần resume trên synchronization context đã được capture. Điều này quan trọng trong library code và services nơi không cần thread affinity. Nó ngăn deadlock khi `.Result` được dùng ở nơi khác và cải thiện hiệu năng bằng cách tránh context-switching overhead. Luôn dùng trong non-UI async code của library.",
    q_en: "What is ConfigureAwait(false) and when should you use it?",
    a_en: "`ConfigureAwait(false)` tells the async runtime not to resume on the captured synchronization context. It is essential in library and service code where thread affinity is not needed. It prevents deadlocks when `.Result` is used elsewhere and improves performance by avoiding context-switching overhead. Always use it in non-UI async library code.",
  },
  {
    id: 2734,
    category: "C#",
    subcategory: "Async & Threading",
    level: "intermediate",
    q: "Race condition là gì và làm thế nào để ngăn chặn?",
    a: "Race condition xảy ra khi nhiều thread truy cập shared data đồng thời và tạo ra kết quả không thể đoán trước. Ngăn chặn bằng các synchronization primitives: câu lệnh `lock`, `Interlocked` operations, hoặc immutable data structures. Dùng `volatile` cho single fields cần visibility. Ưu tiên thiết kế immutable thay vì synchronization phức tạp.",
    q_en: "What is a race condition and how do you prevent it?",
    a_en: "Race conditions occur when multiple threads access shared data concurrently, producing unpredictable results. Prevent them with synchronization primitives: the `lock` statement, `Interlocked` operations, or immutable data structures. Use `volatile` for single fields requiring visibility guarantees. Prefer immutable designs over complex synchronization when possible.",
  },
  {
    id: 2735,
    category: "C#",
    subcategory: "Async & Threading",
    level: "intermediate",
    q: "Task và Thread khác nhau như thế nào trong C#?",
    a: "Thread là OS construct cấp thấp tiêu tốn tài nguyên đáng kể (~1MB stack). Task là abstraction cấp cao đại diện cho công việc bất đồng bộ, được tối ưu cho I/O. Task dùng thread pool để hiệu quả hơn. `Task.Run()` tạo background task từ thread pool. Dùng Task cho async patterns; dùng Thread khi thực sự cần kiểm soát thread (background worker với state riêng).",
    q_en: "What is the difference between Task and Thread in C#?",
    a_en: "Thread is a low-level OS construct consuming significant resources (~1MB stack). Task is a high-level abstraction representing async work, optimized for I/O operations and backed by the thread pool. `Task.Run()` creates background tasks from the thread pool. Use Task for async patterns; use Thread only when you need explicit thread control.",
  },
  {
    id: 2736,
    category: "C#",
    subcategory: "Async & Threading",
    level: "advanced",
    q: "SynchronizationContext là gì và tại sao nó quan trọng?",
    a: "SynchronizationContext đảm bảo async continuations resume trên đúng thread. Trong UI frameworks (WPF, WinForms), UI operations phải chạy trên UI thread — SynchronizationContext xử lý điều này tự động. ASP.NET request contexts cũng sử dụng nó. `ConfigureAwait(false)` bỏ qua context này, hữu ích trong library code nơi thread cụ thể không quan trọng.",
    q_en: "What is SynchronizationContext and why does it matter?",
    a_en: "SynchronizationContext ensures async continuations resume on the correct thread. In UI frameworks (WPF, WinForms), UI operations require the specific UI thread — SynchronizationContext handles this automatically. ASP.NET request contexts use it too. `ConfigureAwait(false)` bypasses it, useful in library code where thread affinity is irrelevant.",
  },
  {
    id: 2737,
    category: "C#",
    subcategory: "Async & Threading",
    level: "intermediate",
    q: "Fire-and-forget task là gì và tại sao nguy hiểm?",
    a: 'Fire-and-forget task thực thi mà không được await: `_ = someAsync();`. Nguy hiểm vì exceptions biến mất trong im lặng, gây ra lỗi rất khó debug. Luôn capture task result hoặc await. Đăng ký global handler `TaskScheduler.UnobservedTaskException` để bắt các exception bị bỏ sót. Không bao giờ "bắn rồi quên" trong production code trừ khi được xử lý cẩn thận.',
    q_en: "What are fire-and-forget tasks and why are they risky?",
    a_en: "Fire-and-forget tasks execute without being awaited: `_ = someAsync();`. They are risky because exceptions silently disappear, causing hard-to-debug failures. Always capture task results or await them. Register `TaskScheduler.UnobservedTaskException` for a global safety net. Never fire-and-forget in production without proper exception handling.",
  },
  {
    id: 2738,
    category: "C#",
    subcategory: "Async & Threading",
    level: "advanced",
    q: "Khi nào nên dùng `ValueTask` thay vì `Task`?",
    a: "Dùng `ValueTask<T>` khi method thường xuyên hoàn thành synchronously — tránh được allocation overhead của heap-allocated Task object và giảm GC pressure. Ít hữu ích hơn cho I/O-bound work luôn async. Hãy benchmark để xác nhận cải thiện thực sự trước khi áp dụng. `ValueTask` cần xử lý cẩn thận — không được await nhiều lần và không được dùng trong các context phức tạp.",
    q_en: "When should you use ValueTask instead of Task?",
    a_en: "Use `ValueTask<T>` when methods frequently complete synchronously — it eliminates the allocation overhead of heap-allocated Task objects, reducing GC pressure. It is less beneficial for I/O-bound work that is always async. Benchmark to confirm real gains before adopting. `ValueTask` requires careful handling: never await it more than once or use it in complex shared contexts.",
  },
  {
    id: 2739,
    category: "C#",
    subcategory: "Async & Threading",
    level: "intermediate",
    q: "Làm thế nào để hủy đúng cách một async operation trong C#?",
    a: "Truyền `CancellationToken` vào các async methods và kiểm tra `token.IsCancellationRequested` định kỳ. Ném `OperationCanceledException` khi phát hiện cancellation. Gọi `cts.Cancel()` để báo hiệu hủy bỏ; dùng `cts.CancelAfter(TimeSpan)` cho timeout tự động. Xử lý cancellation đúng cách ngăn resource leak và orphaned tasks.",
    q_en: "How do you properly cancel an asynchronous operation in C#?",
    a_en: "Pass a `CancellationToken` to async methods and periodically check `token.IsCancellationRequested`. Throw `OperationCanceledException` when cancellation is detected. Call `cts.Cancel()` to signal cancellation; use `cts.CancelAfter(TimeSpan)` for automatic timeout. Properly handling cancellation prevents resource leaks and orphaned tasks.",
  },
  {
    id: 2740,
    category: "C#",
    subcategory: "Async & Threading",
    level: "advanced",
    q: "`lock` và `ReaderWriterLockSlim` khác nhau như thế nào?",
    a: "`lock` đối xử bình đẳng với cả reader và writer, serialize toàn bộ access. `ReaderWriterLockSlim` cho phép nhiều reader đồng thời trong khi vẫn đảm bảo exclusive write access. Dùng `lock` khi read và write cân bằng; dùng `ReaderWriterLockSlim` cho workload nặng về đọc. \n\n**Lưu ý:** contention vẫn quan trọng — đo lường trước khi tối ưu.",
    q_en: "What is the difference between lock and ReaderWriterLockSlim?",
    a_en: "`lock` serializes all access equally for readers and writers. `ReaderWriterLockSlim` allows multiple concurrent readers while enforcing exclusive write access. Use `lock` for balanced read/write workloads; use `ReaderWriterLockSlim` for read-heavy scenarios. \n\n**Note:** contention still matters — measure before optimizing.",
  },

  // === ENTITY FRAMEWORK ===
  {
    id: 2741,
    category: "Entity Framework",
    subcategory: "Entity Framework",
    level: "intermediate",
    q: "Lazy loading và eager loading trong Entity Framework khác nhau như thế nào?",
    a: "Lazy loading tự động tải related data khi được truy cập — `customer.Orders` kích hoạt một query khi truy cập. Cần bật qua `UseLazyLoadingProxies()`. Eager loading tải dữ liệu trước bằng `.Include(c => c.Orders)`, giảm số query. Explicit loading tải thủ công qua `.Load()`. Lazy loading tiện nhưng dễ gây N+1; eager loading có thể tải dữ liệu không cần thiết.",
    q_en: "What is the difference between lazy loading and eager loading in Entity Framework?",
    a_en: "Lazy loading automatically fetches related data when accessed — `customer.Orders` triggers a query on access, requiring `UseLazyLoadingProxies()`. Eager loading fetches upfront with `.Include(c => c.Orders)`, reducing query count. Explicit loading manually loads via `.Load()`. Lazy loading risks N+1 problems; eager loading may pull unneeded data.",
  },
  {
    id: 2742,
    category: "Entity Framework",
    subcategory: "Entity Framework",
    level: "intermediate",
    q: "Migrations trong Entity Framework là gì và hoạt động như thế nào?",
    a: "Migration theo dõi thay đổi schema database dưới dạng C# code. Tạo migration bằng `Add-Migration MigrationName` sau khi thay đổi model. Migration sinh ra method `Up()` (áp dụng thay đổi) và `Down()` (rollback). Apply bằng `Update-Database`. Migration cho phép version control schema, rollback dễ dàng, và áp dụng theo từng môi trường một cách có kiểm soát.",
    q_en: "What are Entity Framework migrations and how do they work?",
    a_en: "Migrations track database schema changes as C# code. Create migrations via `Add-Migration MigrationName` when models change. Migrations generate an `Up()` method (apply changes) and a `Down()` method (rollback). Apply via `Update-Database`. Migrations enable schema version control, rollback capability, and controlled environment-specific application.",
  },
  {
    id: 2744,
    category: "Entity Framework",
    subcategory: "Entity Framework",
    level: "intermediate",
    q: "DbContext là gì và nó quản lý những gì?",
    a: "`DbContext` đại diện cho một session với database, quản lý entity instances, change tracking và `SaveChanges()`. Nó theo dõi tất cả entities đã tải (added, modified, deleted). Implement `IDisposable` — luôn dùng `using` statement. Trong ASP.NET Core được scope theo mỗi request. Không bao giờ dùng static hay singleton `DbContext` — luôn dùng scoped lifetime.",
    q_en: "What is DbContext and what does it manage in EF Core?",
    a_en: "`DbContext` represents a session with the database, managing entity instances, change tracking, and `SaveChanges()` operations. It tracks all loaded entities (added, modified, deleted) and implements `IDisposable` — always use a `using` statement. In ASP.NET Core it is scoped per request. Never use a static or singleton `DbContext`.",
  },
  {
    id: 2745,
    category: "Entity Framework",
    subcategory: "Entity Framework",
    level: "advanced",
    q: "Làm thế nào để tối ưu EF Core queries cho performance?",
    a: "Dùng `.AsNoTracking()` cho read-only queries để tắt change tracking. Dùng `.Select()` projection thay vì load toàn bộ entity. Batch updates với `ExecuteUpdate()` và `ExecuteDelete()` thay vì load-modify-save từng entity. Monitor SQL thực tế qua logging để phát hiện query xấu. Dùng `.Include()` đúng chỗ để tránh N+1. Tạo index trên các cột được filter/sort thường xuyên.",
    q_en: "How do you optimize EF Core queries for performance?",
    a_en: "Use `.AsNoTracking()` for read-only queries to disable change tracking overhead. Use `.Select()` projections instead of loading entire entities. Batch updates with `ExecuteUpdate()` and `ExecuteDelete()` rather than load-modify-save per entity. Monitor generated SQL via logging. Use `.Include()` appropriately to avoid N+1. Add database indexes on frequently filtered or sorted columns.",
  },
  {
    id: 2746,
    category: "Entity Framework",
    subcategory: "Entity Framework",
    level: "intermediate",
    q: "Explicit loading trong EF Core là gì?",
    a: "Explicit loading tải related data thủ công sau khi đã tải parent: `context.Entry(order).Reference(o => o.Customer).Load()` cho single reference hoặc `context.Entry(order).Collection(o => o.Items).Load()` cho collection. Cung cấp kiểm soát chính xác — chỉ tải khi thực sự cần, tiết kiệm bandwidth. Tuy nhiên chậm hơn eager loading nếu cuối cùng cần tải tất cả.",
    q_en: "What is explicit loading in EF Core?",
    a_en: "Explicit loading manually loads related data after the parent is loaded: `context.Entry(order).Reference(o => o.Customer).Load()` for single references or `context.Entry(order).Collection(o => o.Items).Load()` for collections. It gives precise control, loading data only when needed and saving bandwidth. However, it is slower than eager loading if you end up loading everything anyway.",
  },
  {
    id: 2747,
    category: "Entity Framework",
    subcategory: "Entity Framework",
    level: "advanced",
    q: "Làm thế nào để xử lý concurrency conflicts trong EF Core?",
    a: "Dùng optimistic concurrency với `[Timestamp]` row version token trên entity. Khi xung đột xảy ra, EF ném `DbUpdateConcurrencyException`. Xử lý bằng cách đọc lại giá trị mới nhất và retry, hoặc trả về HTTP 409 Conflict cho client yêu cầu refresh. Pessimistic locking dùng SQL-level locks hiếm khi cần thiết trong ứng dụng hiện đại.",
    q_en: "How do you handle concurrency conflicts in EF Core?",
    a_en: "Use optimistic concurrency with `[Timestamp]` row version tokens on entities. When conflicts occur, EF throws `DbUpdateConcurrencyException`. Handle by re-reading updated values and retrying, or return HTTP 409 Conflict to clients requesting a refresh. Pessimistic locking via SQL-level locks is rarely needed in modern applications.",
  },
  {
    id: 2748,
    category: "Entity Framework",
    subcategory: "Entity Framework",
    level: "intermediate",
    q: "`Add`, `Update`, và `Attach` trong EF Core khác nhau như thế nào?",
    a: "`Add()` đánh dấu entity là mới (INSERT khi SaveChanges). `Update()` đánh dấu là đã sửa đổi (UPDATE). `Attach()` thêm vào context mà không đánh dấu modified — chỉ lưu khi properties thực sự thay đổi. Dùng `Add` cho entity mới, `Update` cho detached entity cần thay thế toàn bộ, `Attach` cho batch updates có chọn lọc.",
    q_en: "What is the difference between Add, Update, and Attach in EF Core?",
    a_en: "`Add()` marks the entity as new (INSERT on SaveChanges). `Update()` marks it as modified (UPDATE). `Attach()` adds to the context without marking modified — only saves when properties actually change. Use `Add` for new entities, `Update` for detached entities you are replacing entirely, `Attach` for selective batch updates.",
  },
  {
    id: 2749,
    category: "Entity Framework",
    subcategory: "Entity Framework",
    level: "advanced",
    q: "Làm thế nào để chạy migrations trong CI/CD pipeline production?",
    a: "Tạo idempotent migration scripts riêng và review trước khi deploy. Không bao giờ gọi `db.Database.Migrate()` khi app khởi động trong production — nhiều instance chạy đồng thời sẽ gây lock contention. Apply migrations trong deployment pipeline trước khi app start. Dùng `Sql()` cho các custom operations ngoài model changes. Luôn test migrations trên staging trước.",
    q_en: "How do you run migrations in a production CI/CD pipeline?",
    a_en: "Generate idempotent migration scripts separately and review them before deployment. Never call `db.Database.Migrate()` at app startup in production — multiple concurrent instances cause lock contention. Apply migrations in the deployment pipeline before the app starts. Use `Sql()` for custom operations beyond model changes. Always test migrations on staging first.",
  },
  {
    id: 2750,
    category: "Entity Framework",
    subcategory: "Entity Framework",
    level: "intermediate",
    q: "Change tracking trong EF Core là gì và khi nào nên tắt?",
    a: "Change tracking theo dõi các entity đã tải, phát hiện sửa đổi trong quá trình `SaveChanges()`. Tính năng này tiêu tốn bộ nhớ và xử lý. Tắt bằng `.AsNoTracking()` khi đọc dữ liệu mà không có ý định sửa. Cho bulk operations, dùng `ExecuteUpdate()` và `ExecuteDelete()` để bypass tracking hoàn toàn. Cân bằng giữa overhead của tracking và sự tiện lợi của `SaveChanges()`.",
    q_en: "What is change tracking in EF Core and when should you disable it?",
    a_en: "Change tracking monitors loaded entities, detecting modifications during `SaveChanges()`. It consumes memory and processing overhead. Disable it with `.AsNoTracking()` when reading data without intending to modify it. For bulk operations, use `ExecuteUpdate()` and `ExecuteDelete()` which bypass tracking entirely. Balance the tracking overhead against the convenience of automatic `SaveChanges()`.",
  },

  // === ASP.NET CORE ===
  {
    id: 2751,
    category: "ASP.NET",
    subcategory: "ASP.NET",
    level: "intermediate",
    q: "Middleware trong ASP.NET Core là gì và hoạt động như thế nào?",
    a: "Middleware là các C# class xử lý HTTP request/response theo pipeline. Mỗi middleware nhận request, có thể sửa đổi nó, chuyển tiếp cho middleware tiếp theo qua `next()`, rồi xử lý response trên đường về. Middleware phổ biến: authentication, authorization, logging, CORS. Thứ tự rất quan trọng — exception handler đặt trước, CORS trước auth, auth trước authorization.",
    q_en: "What is middleware in ASP.NET Core and how does it work?",
    a_en: "Middleware are C# classes that process HTTP requests and responses in a pipeline. Each middleware receives a request, optionally modifies it, passes it to the next middleware via `next()`, then handles the response on the way back. Common middleware: authentication, authorization, logging, CORS. Order matters — exception handlers first, CORS before auth, auth before authorization.",
  },
  {
    id: 2752,
    category: "ASP.NET",
    subcategory: "ASP.NET",
    level: "intermediate",
    q: "Dependency Injection trong ASP.NET Core là gì?",
    a: "Dependency Injection cung cấp cho object các dependency của nó thông qua constructor thay vì tự tạo. ASP.NET Core có built-in IoC container (`IServiceProvider`). Đăng ký service trong `Program.cs`: `services.AddScoped<IService, Service>()`. Container quản lý lifetime (Transient, Scoped, Singleton). DI giúp code testable, loosely coupled và dễ bảo trì.",
    q_en: "What is Dependency Injection in ASP.NET Core?",
    a_en: "Dependency injection provides objects their required dependencies through constructors rather than having them create their own. ASP.NET Core includes a built-in IoC container (`IServiceProvider`). Register services in `Program.cs`: `services.AddScoped<IService, Service>()`. The container manages lifetimes (Transient, Scoped, Singleton), enabling testability, loose coupling, and maintainability.",
  },
  {
    id: 2753,
    category: "ASP.NET",
    subcategory: "ASP.NET",
    level: "intermediate",
    q: "3 lifetime của Dependency Injection trong ASP.NET Core là gì và dùng khi nào?",
    a: 'Transient: tạo instance mới cho mỗi lần resolve (stateless utilities). Scoped: cùng instance trong một HTTP request (DbContext, UnitOfWork). Singleton: một instance duy nhất suốt đời app (logging, configuration). Không bao giờ inject Scoped vào Singleton — gây "captive dependency" bug nghiêm trọng. Chọn lifetime phù hợp với đặc tính stateful của service.',
    q_en: "What are the three DI lifetimes in ASP.NET Core and when should you use each?",
    a_en: 'Transient: a new instance for every resolve (stateless utilities). Scoped: the same instance per HTTP request (DbContext, UnitOfWork). Singleton: one instance for the entire application lifetime (logging, configuration). Never inject a Scoped service into a Singleton — this creates a "captive dependency" bug. Match the lifetime to the service\'s stateful characteristics.',
  },
  {
    id: 2754,
    category: "ASP.NET",
    subcategory: "ASP.NET",
    level: "intermediate",
    q: "Routing trong ASP.NET Core hoạt động như thế nào?",
    a: "Routing ánh xạ HTTP request đến controller action. Có hai cách: conventional routing dùng pattern định sẵn (`{controller}/{action}/{id}`) và attribute routing dùng `[Route]` attribute trực tiếp trên controller/action. Attribute routing cho kiểm soát chi tiết hơn. Route parameters bind vào action parameters; query parameters cũng bind tương tự. Routing phối hợp với model binding để invoke đúng action.",
    q_en: "How does routing work in ASP.NET Core?",
    a_en: "Routing maps incoming HTTP requests to controller actions. Two approaches: conventional routing using predefined patterns (`{controller}/{action}/{id}`), and attribute routing using `[Route]` attributes on controllers/actions. Attribute routing provides finer control. Route parameters bind to action parameters; query parameters bind similarly. Routing combines with model binding to invoke the correct action.",
  },
  {
    id: 2755,
    category: "ASP.NET",
    subcategory: "ASP.NET",
    level: "intermediate",
    q: "Model binding trong ASP.NET Core là gì?",
    a: "Model binding tự động chuyển đổi HTTP request data (form fields, route data, query strings, JSON body) thành .NET objects. Bind form fields vào action parameters theo tên, JSON body vào complex types, route parameters vào action parameters. Dùng `[FromBody]`, `[FromRoute]`, `[FromQuery]` để chỉ định nguồn rõ ràng. Giúp loại bỏ boilerplate parsing thủ công.",
    q_en: "What is model binding in ASP.NET Core?",
    a_en: "Model binding automatically converts HTTP request data (form fields, route data, query strings, JSON body) to .NET objects. It binds form fields to action parameters by name, JSON body to complex types, and route parameters to action parameters. Use `[FromBody]`, `[FromRoute]`, `[FromQuery]` to specify sources explicitly. This eliminates manual parsing boilerplate.",
  },
  {
    id: 2756,
    category: "ASP.NET",
    subcategory: "ASP.NET",
    level: "advanced",
    q: "Action filter và middleware khác nhau như thế nào trong ASP.NET Core?",
    a: "Middleware xử lý tất cả request trước khi routing, dùng cho cross-cutting concerns toàn cục (logging, CORS, exception handling). Action filter chỉ chạy trên controller action đã được match, có quyền truy cập vào action context và parameters — dùng cho logic controller-specific (authorization, validation, caching). Middleware chạy sớm hơn và rộng hơn; filter chính xác hơn.",
    q_en: "What is the difference between an action filter and middleware in ASP.NET Core?",
    a_en: "Middleware processes all requests before routing and is used for global cross-cutting concerns (logging, CORS, exception handling). Action filters run only on matched controller actions with access to action context and parameters, making them suitable for controller-specific logic (authorization, validation, caching). Middleware executes earlier and globally; filters are more targeted.",
  },
  {
    id: 2757,
    category: "ASP.NET",
    subcategory: "ASP.NET",
    level: "intermediate",
    q: "HttpContext là gì và cách sử dụng như thế nào?",
    a: "`HttpContext` đóng gói thông tin HTTP request/response, có thể truy cập qua `ControllerBase.HttpContext` hoặc `IHttpContextAccessor`. Truy cập request properties: `Request.Headers`, `Request.Query`, `Request.Path`, `User` identity. Dùng cho các thao tác context-specific như set response headers, đọc user claims, hay truyền dữ liệu qua request pipeline.",
    q_en: "What is HttpContext and how do you use it in ASP.NET Core?",
    a_en: "`HttpContext` encapsulates HTTP request/response information, accessible via `ControllerBase.HttpContext` or `IHttpContextAccessor`. Access request properties: `Request.Headers`, `Request.Query`, `Request.Path`, and the `User` identity. Use it for context-specific operations like setting response headers, reading user claims, or passing data through the request pipeline.",
  },
  {
    id: 2758,
    category: "ASP.NET",
    subcategory: "ASP.NET",
    level: "intermediate",
    q: "`IActionResult` và `ActionResult<T>` khác nhau như thế nào?",
    a: "`IActionResult` là interface cho tất cả action return types. `ActionResult` là implementation non-generic. `ActionResult<T>` cho phép trả về cả typed response thành công lẫn error results. Các implementation phổ biến: `OkObjectResult` (200), `NotFoundResult` (404), `BadRequestResult` (400), `CreatedAtActionResult` (201). Dùng `ActionResult<T>` trong Web API để đặc tả rõ kiểu trả về.",
    q_en: "What is the difference between IActionResult and ActionResult<T> in ASP.NET Core?",
    a_en: "`IActionResult` is the interface for all action return types. `ActionResult` is the non-generic implementation. `ActionResult<T>` enables returning either a successful typed response or error results in the same method. Common implementations: `OkObjectResult` (200), `NotFoundResult` (404), `BadRequestResult` (400). Use `ActionResult<T>` in Web APIs to clearly specify the success return type.",
  },
  {
    id: 2759,
    category: "ASP.NET",
    subcategory: "ASP.NET",
    level: "intermediate",
    q: "Kestrel là gì và liên quan đến IIS như thế nào?",
    a: "Kestrel là cross-platform web server nhẹ được tích hợp sẵn trong ASP.NET Core. Đủ dùng cho development và nhiều production deployment. Microsoft khuyến nghị đặt sau reverse proxy (IIS, Nginx, Apache) để tăng security, performance và reliability. IIS có thể host Kestrel qua reverse proxy hoặc host ASP.NET Core trực tiếp qua ANCM (ASP.NET Core Module).",
    q_en: "What is Kestrel and how does it relate to IIS in ASP.NET Core?",
    a_en: "Kestrel is a cross-platform, lightweight web server built into ASP.NET Core. It is sufficient for development and many production deployments. Microsoft recommends pairing it with a reverse proxy (IIS, Nginx, Apache) for added security, performance, and reliability. IIS can host Kestrel via reverse proxy or host ASP.NET Core directly via the ANCM module.",
  },
  {
    id: 2760,
    category: "ASP.NET",
    subcategory: "ASP.NET",
    level: "intermediate",
    q: "Làm thế nào để implement authentication và authorization trong ASP.NET Core?",
    a: "Authentication xác minh danh tính người dùng; authorization kiểm tra quyền hạn. Dùng `AddAuthentication()` để đăng ký auth scheme (JWT, cookies, OAuth) và `AddAuthorization()` để cấu hình policies. Middleware: `app.UseAuthentication()` phải đặt trước `app.UseAuthorization()` — thứ tự quan trọng. Dùng `[Authorize]` attribute trên controllers/actions; dùng claim-based policies cho kiểm soát chi tiết.",
    q_en: "How do you implement authentication and authorization in ASP.NET Core?",
    a_en: "Authentication verifies user identity; authorization checks permissions. Use `AddAuthentication()` to register auth schemes (JWT, cookies, OAuth) and `AddAuthorization()` to configure policies. In middleware: `app.UseAuthentication()` must come before `app.UseAuthorization()` — order matters. Use the `[Authorize]` attribute on controllers/actions and claim-based policies for fine-grained control.",
  },

  // === C# 12/13 FEATURES ===
  {
    id: 2761,
    category: "C#",
    subcategory: "Cơ Bản",
    level: "intermediate",
    q: "Primary constructor trong C# 12 là gì và nó giải quyết vấn đề gì?",
    a: "Primary constructor cho phép khai báo tham số constructor trực tiếp trên class/struct: `public class Person(string name, int age) {}`. Các tham số này có phạm vi dùng được trong toàn bộ class body. Giúp loại bỏ boilerplate khởi tạo field lặp đi lặp lại, code gọn hơn cho data class. Không chỉ dành cho record — từ C# 12 áp dụng cho mọi class/struct. \n\n**Lưu ý:** compiler không tự tạo backing field trong non-record class, cần tự gán tham số vào field khi cần lưu trữ.",
    a_en: "Primary constructors allow declaring constructor parameters directly on class/struct declarations: `public class Person(string name, int age) {}`. Parameters are in scope throughout the class body, eliminating repetitive field initialization boilerplate and improving readability for data classes. Not limited to records — available on all classes/structs since C# 12. \n\n**Note:** compiler does not auto-generate backing fields in non-record classes; assign parameters to fields manually when storage is needed.",
    q_en: "What are primary constructors in C# 12 and what problems do they solve?",
  },
  {
    id: 2762,
    category: "C#",
    subcategory: "Cơ Bản",
    level: "beginner",
    q: "Collection expression trong C# 12 là gì và có lợi thế gì về hiệu năng?",
    a: "Collection expression dùng cú pháp ngoặc vuông `[1, 2, 3]` để khởi tạo array, list và span thay cho `new List<int> { 1, 2, 3 }` truyền thống. Hỗ trợ spread operator để ghép collection: `int[] combined = [..array1, ..array2]`. Code ngắn gọn hơn; compiler tối ưu hóa initialization, tránh cấp phát trung gian không cần thiết. Đây là chuẩn code hiện đại trong C#, tương đồng cú pháp với Python/JavaScript.",
    a_en: "Collection expressions use bracket syntax `[1, 2, 3]` to initialize arrays, lists, and spans instead of `new List<int> { 1, 2, 3 }`. They support the spread operator to merge collections: `int[] combined = [..array1, ..array2]`. The compiler optimizes initialization to avoid intermediate allocations. This is the modern C# standard, aligning with Python/JS syntax familiarity.",
    q_en: "What are collection expressions in C# 12 and what are their performance advantages?",
  },

  // === .NET 8/9 FEATURES ===
  {
    id: 2765,
    category: "ASP.NET",
    subcategory: "ASP.NET",
    level: "intermediate",
    q: "Minimal APIs trong .NET 8 là gì và khi nào nên dùng thay controller?",
    a: 'Minimal APIs khai báo HTTP endpoint trực tiếp trong `Program.cs` mà không cần class controller: `app.MapPost("/users", handler)`. Phù hợp cho microservice nhỏ, REST API đơn giản vì ít boilerplate. .NET 8 cung cấp Minimal APIs tương thích AOT. Giới hạn: ít cấu trúc hơn cho ứng dụng phức tạp. Controller vẫn phù hợp cho ứng dụng lớn, nhiều tính năng. Nên chọn Minimal APIs khi cần khởi tạo nhanh, độ phức tạp thấp.',
    a_en: 'Minimal APIs declare HTTP endpoints directly in `Program.cs` without controller classes: `app.MapPost("/users", handler)`. They reduce boilerplate for simple CRUD and microservice scenarios. .NET 8 provides AOT-compatible minimal APIs. Trade-off: less structure for complex applications. Controllers remain preferable for large, feature-rich apps. Choose Minimal APIs for rapid startup and low-complexity scenarios.',
    q_en: "What are Minimal APIs in .NET 8 and when should you use them over controllers?",
  },
  {
    id: 2766,
    category: "ASP.NET",
    subcategory: "ASP.NET",
    level: "intermediate",
    q: "Blazor với render modes thống nhất là gì và điểm khác biệt so với Blazor Server và WebAssembly truyền thống?",
    a: 'Blazor với render modes thống nhất (từ .NET 8+) kết hợp server-side và WebAssembly rendering trong cùng một ứng dụng — render phía server ban đầu để tải nhanh, sau đó chuyển sang WebAssembly phía client cho tương tác. Một codebase duy nhất xử lý cả hai mô hình. Giải quyết bài toán "phải chọn Blazor Server hoặc WebAssembly". Đơn giản hóa việc xây dựng hybrid app C# full-stack hiện đại. Yêu cầu .NET 8+.',
    a_en: 'Blazor unified rendering (from .NET 8+) combines server-side and WebAssembly rendering in a single application — render server-side initially for fast startup, then shift to WebAssembly client-side for interactivity. A single codebase handles both models, resolving the "choose Blazor Server OR WebAssembly" dilemma. It simplifies hybrid full-stack C# development and requires .NET 8+.',
    q_en: "What is Blazor unified rendering and how does it differ from traditional Blazor Server and WebAssembly?",
  },
  {
    id: 2767,
    category: "ASP.NET",
    subcategory: "ASP.NET",
    level: "advanced",
    q: "Native AOT compilation là gì và tại sao quan trọng cho cloud deployment?",
    a: "Native AOT biên dịch C# IL thành native machine code tại build time thay vì JIT lúc runtime. \n\n**Lợi ích:** khởi động cực nhanh (<50–100ms (AOT) so với 200–500ms (JIT)), bộ nhớ thấp hơn, không có JIT pause. Publish: `dotnet publish -r linux-x64 -p:PublishAot=true`. Đánh đổi: binary lớn hơn, không dùng reflection tùy tiện (cần trim metadata). Quan trọng với Kubernetes, serverless (Azure Function Apps) và workload nhạy cảm về chi phí. Tính năng production sẵn sàng từ .NET 8+.",
    a_en: "Native AOT compiles C# IL to native machine code at build time instead of JIT at runtime. \n\n**Benefits:** fast startup (<50–100ms (AOT) vs 200–500ms (JIT)), lower memory footprint, no JIT pauses. Publish command: `dotnet publish -r linux-x64 -p:PublishAot=true`. Trade-offs: larger binary size, no arbitrary runtime reflection without trimming metadata. Critical for Kubernetes, serverless (Function Apps), and cost-sensitive cloud workloads. Production-ready from .NET 8+.",
    q_en: "What is Native AOT compilation and why is it important for cloud deployments?",
  },
  {
    id: 2768,
    category: "ASP.NET",
    subcategory: "ASP.NET",
    level: "advanced",
    q: "YARP là gì và giải quyết vấn đề gì trong kiến trúc microservices?",
    a: "YARP (Yet Another Reverse Proxy) là reverse proxy mã nguồn mở của Microsoft xây dựng trên ASP.NET Core. Định tuyến request đến backend services, hỗ trợ load balancing, authentication, rate limiting. Giải quyết: routing request, service discovery, quản lý traffic mà không cần proxy ngoài (Nginx, HAProxy). Hiệu năng cao hơn proxy truyền thống nhờ nền tảng .NET. Lựa chọn thay thế API Gateway. Lý tưởng cho kiến trúc microservices thuần .NET.",
    a_en: "YARP (Yet Another Reverse Proxy) is Microsoft's open-source reverse proxy built on ASP.NET Core. It routes requests to backend services and handles load balancing, authentication, and rate limiting. YARP eliminates the need for external proxies (Nginx, HAProxy) for request routing, service discovery, and traffic management. It outperforms traditional reverse proxies due to the .NET runtime and is ideal for microservices architectures.",
    q_en: "What is YARP and what problems does it solve in microservices architecture?",
  },
  {
    id: 2769,
    category: "ASP.NET",
    subcategory: "ASP.NET",
    level: "advanced",
    q: "gRPC và Minimal APIs khác nhau như thế nào khi dùng cho giao tiếp giữa các service?",
    a: "gRPC dùng HTTP/2 với binary serialization (Protocol Buffers); Minimal APIs dùng REST với JSON text. gRPC: nhanh hơn, tốn băng thông ít hơn, hỗ trợ streaming thực sự (server, client, bidirectional). REST: đơn giản hơn, browser-friendly, tooling phổ biến. Chọn gRPC cho backend-to-backend hiệu năng cao; dùng REST cho public API và browser client. .NET 8+ hỗ trợ cả hai tốt như nhau. Quyết định dựa trên yêu cầu về latency, băng thông và client đích.",
    a_en: "gRPC uses HTTP/2 binary serialization (Protocol Buffers); Minimal APIs use REST with JSON. gRPC offers faster throughput, lower bandwidth usage, and true streaming support (server, client, bidirectional). REST is simpler, browser-friendly, and uses standard tooling. Choose gRPC for high-performance backend-to-backend communication; use REST for public APIs and browser clients. .NET 8+ supports both equally well. Decision depends on latency, bandwidth, and target client requirements.",
    q_en: "How do gRPC and Minimal APIs differ for inter-service communication?",
  },

  // === MODERN PATTERNS ===
  {
    id: 2771,
    category: "ASP.NET",
    subcategory: "ASP.NET",
    level: "intermediate",
    q: "Health Checks trong ASP.NET Core là gì và tại sao cần thiết cho ứng dụng cloud?",
    a: "Health checks expose trạng thái ứng dụng qua HTTP endpoint (`/health`). Implement `IHealthCheck`: trả về `HealthCheckResult.Healthy/Degraded/Unhealthy`. Container orchestrator (Kubernetes) dùng liveness probe (có nên restart?) và readiness probe (có nhận traffic?). Kiểm tra: kết nối database, external API, bộ nhớ. Cấu hình: `builder.Services.AddHealthChecks().AddDbContextCheck<MyDbContext>()`. Thiết yếu cho auto-healing deployment và zero-downtime update.",
    a_en: "Health checks expose application status via HTTP endpoints (`/health`). Implement `IHealthCheck` returning `HealthCheckResult.Healthy/Degraded/Unhealthy`. Container orchestrators (Kubernetes) use liveness probes (should it restart?) and readiness probes (should it accept traffic?). Checks cover database connectivity, external APIs, and memory usage. Configure via `builder.Services.AddHealthChecks().AddDbContextCheck<MyDbContext>()`. Essential for auto-healing deployments and zero-downtime updates.",
    q_en: "What are health checks in ASP.NET Core and why are they critical for cloud applications?",
  },
  {
    id: 2772,
    category: "ASP.NET",
    subcategory: "ASP.NET",
    level: "advanced",
    q: "CQRS là gì và khi nào nên kết hợp với MediatR?",
    a: "CQRS (Command Query Responsibility Segregation) tách biệt thao tác ghi (command) và đọc (query) thành các handler riêng. MediatR implement pattern Mediator — in-process messaging bus. Command thay đổi state; query chỉ trả về dữ liệu. \n\n**Lợi ích:** scale độc lập, DTO khác nhau cho read/write, pipeline middleware cho cross-cutting concerns. Sai lầm phổ biến: dùng CQRS cho CRUD đơn giản — chỉ nên áp dụng khi độ phức tạp thực sự yêu cầu model read/write khác nhau.",
    a_en: "CQRS (Command Query Responsibility Segregation) separates write operations (commands) from read operations (queries) into distinct handlers. MediatR implements the Mediator pattern as an in-process messaging bus. Commands modify state; queries return data. \n\n**Benefits:** independent scaling, different DTOs for reads/writes, pipeline middleware for cross-cutting concerns. Common mistake: applying CQRS to simple CRUD — only implement when complexity genuinely requires separate read/write models.",
    q_en: "What is CQRS and when should you implement it with MediatR?",
  },
  {
    id: 2773,
    category: "ASP.NET",
    subcategory: "ASP.NET",
    level: "advanced",
    q: "MediatR pipeline behavior là gì và dùng để xử lý cross-cutting concerns như thế nào?",
    a: "MediatR pipeline behavior chặn request trước khi handler thực thi. Đăng ký behavior: `services.AddTransient(typeof(IPipelineBehavior<,>), typeof(LoggingBehavior<,>))`. Dùng để logging, validation, caching, authorization mà không sửa từng handler. Thứ tự behavior quan trọng — thường là: validation → caching → logging → handler. Giữ handler tập trung vào business logic; giảm boilerplate. Chuẩn công nghiệp cho clean architecture với MediatR.",
    a_en: "MediatR pipeline behaviors intercept requests before handlers execute. Register behaviors via `services.AddTransient(typeof(IPipelineBehavior<,>), typeof(LoggingBehavior<,>))`. They enable logging, validation, caching, and authorization without modifying individual handlers. Behavior order matters — typically: validation → caching → logging → handler. Handlers stay focused on business logic; cross-cutting concerns are decoupled. Industry standard for clean architecture with MediatR.",
    q_en: "What are MediatR pipeline behaviors and how do they handle cross-cutting concerns?",
  },
  {
    id: 2774,
    category: "ASP.NET",
    subcategory: "ASP.NET",
    level: "intermediate",
    q: "Keyed Services (`[FromKeyedServices]`) trong .NET 8 giải quyết vấn đề gì?",
    a: 'Khi có nhiều implementation của cùng một interface, DI truyền thống chỉ resolve được implementation cuối đăng ký. Keyed services cho phép đặt tên: `services.AddKeyedScoped<INotifier, EmailNotifier>("email")`. Inject bằng key: `[FromKeyedServices("email")] INotifier notifier`. Giải quyết: factory pattern phức tạp, service locator antipattern, nhập nhằng naming. Tính năng mới từ .NET 8; sạch hơn factory method. Dùng khi interface có nhiều implementation và context quyết định dùng cái nào.',
    a_en: 'When multiple implementations share an interface, traditional DI resolves only the last registered implementation. Keyed services allow naming registrations: `services.AddKeyedScoped<INotifier, EmailNotifier>("email")`. Inject by key: `[FromKeyedServices("email")] INotifier notifier`. This resolves complex factory patterns, the service locator antipattern, and naming ambiguity. A .NET 8+ feature that is cleaner than factory methods. Use when an interface has multiple implementations and context determines which is needed.',
    q_en: "What problem do Keyed Services ([FromKeyedServices]) solve in .NET 8 dependency injection?",
  },
  {
    id: 2775,
    category: "ASP.NET",
    subcategory: "ASP.NET",
    level: "advanced",
    q: "Captive dependency problem trong DI là gì và làm thế nào để tránh?",
    a: 'Captive dependency xảy ra khi scoped service bị inject vào singleton — service scoped sống mãi cùng singleton, không bao giờ được dispose đúng chu kỳ. \n\n**Ví dụ:** `IUnitOfWork` (scoped) inject vào `AppService` (singleton), UnitOfWork bị "giam" mãi, scope không bao giờ kết thúc. Hậu quả: memory leak, dữ liệu stale, connection pool cạn kiệt. Phòng tránh: không bao giờ inject scoped vào singleton; dùng factory pattern hoặc `IServiceScopeFactory` để tạo scope tạm thời khi cần. Singleton nên stateless.',
    a_en: "Captive dependency occurs when a scoped service is injected into a singleton — the scoped service lives as long as the singleton, never disposed at the correct scope boundary. \n\n**Example:** `IUnitOfWork` (scoped) injected into `AppService` (singleton); UnitOfWork is captured and its scope never ends. Consequences: memory leaks, stale data, and connection pool exhaustion. Prevention: never inject scoped services into singletons; use `IServiceScopeFactory` to create temporary scopes when needed. Singletons should be stateless.",
    q_en: "What is the captive dependency problem in DI and how do you avoid it?",
  },
];
