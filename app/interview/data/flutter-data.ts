import type { QAItem } from "../interview-data";

export const FLUTTER_DATA: QAItem[] = [
  // ─── Dart Cơ Bản ───────────────────────────────────────────────────────────
  {
    id: 2801,
    category: "Dart",
    subcategory: "Dart Cơ Bản",
    level: "beginner",
    q: "Sự khác nhau giữa `var`, `dynamic`, và `final` trong Dart là gì?",
    a: "`var` cho phép Dart tự suy luận kiểu dữ liệu một lần lúc khai báo và không thể thay đổi kiểu sau đó. `dynamic` cho phép kiểu thay đổi trong runtime, bỏ qua kiểm tra kiểu tĩnh. `final` tạo biến không thể gán lại sau khi khởi tạo. Dùng `var` cho biến cục bộ rõ ràng, hạn chế `dynamic` vì phá vỡ type safety, dùng `final` cho giá trị không đổi.",
    q_en: "What is the difference between `var`, `dynamic`, and `final` in Dart?",
    a_en: "`var` lets Dart infer the type once at declaration and it cannot change thereafter. `dynamic` allows the type to change at runtime, deferring type checking. `final` creates a variable that cannot be reassigned after initialization. Use `var` for local variables where type is obvious, `dynamic` sparingly, and `final` for values that won't change after assignment.",
  },
  {
    id: 2802,
    category: "Dart",
    subcategory: "Dart Cơ Bản",
    level: "beginner",
    q: "Null safety trong Dart là gì và tại sao nó quan trọng?",
    a: "Null safety nghĩa là các biến không thể chứa `null` trừ khi được đánh dấu nullable bằng `?`. Điều này giúp phòng ngừa lỗi `NullPointerException` ở runtime bằng cách phát hiện lỗi ngay lúc compile. Với null safety, lập trình viên phải có chủ đích rõ ràng về biến nào có thể null, giúp code an toàn hơn và trình biên dịch tối ưu hiệu quả hơn.",
    q_en: "What is null safety in Dart and why does it matter?",
    a_en: "Null safety means variables cannot contain `null` unless explicitly marked as nullable with `?`. This prevents `NullPointerException` errors at runtime by catching them at compile time instead. With null safety, you must be intentional about which variables can be null, making code safer and allowing compiler optimizations.",
  },
  {
    id: 2804,
    category: "Dart",
    subcategory: "Dart Cơ Bản",
    level: "beginner",
    q: "Toán tử null assertion (`!`) là gì và khi nào nên dùng?",
    a: "Toán tử `!` yêu cầu Dart xử lý biến nullable như non-nullable mà không kiểm tra. \n\n**Ví dụ:** `String name = nullableName!` — điều này khẳng định `nullableName` không phải null. Chỉ dùng khi bạn chắc chắn 100% biến không null; nếu sai sẽ ném lỗi runtime. Lạm dụng `!` thường cho thấy thiết kế chưa tốt.",
    q_en: "What is the null assertion operator (`!`) and when should you use it?",
    a_en: "The `!` operator tells Dart to treat a nullable variable as non-nullable without checking. Use it only when you're 100% certain a variable won't be null; if wrong, it throws a runtime exception. Overuse indicates poor design and should be avoided.",
  },
  {
    id: 2805,
    category: "Dart",
    subcategory: "Dart Cơ Bản",
    level: "beginner",
    q: "Các toán tử null-aware trong Dart là gì? Nêu ba ví dụ.",
    a: 'Toán tử null-aware giúp xử lý giá trị nullable an toàn. `?.` truy cập thuộc tính chỉ khi đối tượng khác null: `person?.name`. `??` cung cấp giá trị mặc định nếu bên trái là null: `name ?? "Unknown"`. `??=` chỉ gán nếu biến đang là null: `count ??= 0`. Các toán tử này giúp code ngắn gọn và tránh lỗi null.',
    q_en: "What are null-aware operators in Dart? Name three examples.",
    a_en: "Null-aware operators safely handle nullable values. `?.` accesses properties only if the object is not null. `??` provides a default value if the left side is null. `??=` assigns only if the variable is null. These prevent null errors and reduce boilerplate code.",
  },
  {
    id: 2806,
    category: "Dart",
    subcategory: "Dart Cơ Bản",
    level: "beginner",
    q: "Giải thích sự khác nhau giữa `List`, `Set` và `Map` trong Dart.",
    a: '`List` là collection có thứ tự và cho phép trùng lặp: `[1, 2, 2, 3]`. `Set` là collection không có thứ tự với các phần tử duy nhất: `{1, 2, 3}`. `Map` lưu trữ cặp key-value: `{"name": "John", "age": 30}`. Chọn `List` khi thứ tự quan trọng, `Set` cho giá trị duy nhất và tra cứu O(1), `Map` cho truy xuất theo key.',
    q_en: "Explain the difference between a `List`, `Set`, and `Map` in Dart.",
    a_en: "`List` is an ordered collection allowing duplicates. `Set` is an unordered collection with unique elements only. `Map` stores key-value pairs. Choose `List` when order matters, `Set` for unique values and O(1) lookups, `Map` for key-based data retrieval.",
  },
  {
    id: 2807,
    category: "Dart",
    subcategory: "Dart Cơ Bản",
    level: "beginner",
    q: "`Future` trong Dart là gì và khác `Stream` như thế nào?",
    a: "`Future` đại diện cho một giá trị bất đồng bộ duy nhất sẽ có trong tương lai — giống như một lời hứa cho một kết quả. `Stream` đại diện cho nhiều sự kiện bất đồng bộ theo thời gian — như một chuỗi các giá trị liên tục. Dùng `Future` cho tác vụ một lần (gọi API), dùng `Stream` cho dữ liệu liên tục (cảm biến, WebSocket).",
    q_en: "What is a `Future` in Dart and how is it different from a `Stream`?",
    a_en: "A `Future` represents a single asynchronous value that will be available in the future. A `Stream` represents multiple asynchronous events over time. Use `Future` for single operations like API calls, `Stream` for continuous data like sensor readings or WebSocket messages.",
  },
  {
    id: 2808,
    category: "Dart",
    subcategory: "Dart Cơ Bản",
    level: "beginner",
    q: "Cú pháp `async`/`await` trong Dart hoạt động như thế nào?",
    a: "`async` đánh dấu hàm là bất đồng bộ; `await` tạm dừng thực thi cho đến khi `Future` hoàn thành. \n\n**Ví dụ:** `Future<String> fetchData() async { String data = await api.get(); return data; }`. Điều này giúp code bất đồng bộ đọc dễ như code đồng bộ. `await` chỉ hoạt động trong hàm `async`.",
    q_en: "What is the `async`/`await` syntax in Dart?",
    a_en: "`async` marks a function as asynchronous; `await` pauses execution until a `Future` completes. This makes async code readable like synchronous code. Both must be used together — `await` only works inside `async` functions.",
  },
  {
    id: 2809,
    category: "Dart",
    subcategory: "Dart Cơ Bản",
    level: "beginner",
    q: "Type promotion trong Dart là gì?",
    a: "Type promotion là khi Dart tự động thu hẹp kiểu của biến dựa trên luồng điều khiển. Nếu bạn có `String?` và kiểm tra `if (name != null)`, trong khối đó Dart coi `name` là `String` non-nullable mà không cần `!`. Điều này xảy ra tự động với kiểm tra null, `is` checks, và toán tử logic, giúp giảm boilerplate.",
    q_en: "What is type promotion in Dart?",
    a_en: "Type promotion is when Dart narrows a variable's type based on control flow. If you have a nullable `String?` and check `if (name != null)`, inside that block Dart treats `name` as non-nullable `String` without needing `!`. This happens automatically with null checks, `is` checks, and logical operators.",
  },
  {
    id: 2810,
    category: "Dart",
    subcategory: "Dart Cơ Bản",
    level: "beginner",
    q: "Named parameters và positional parameters trong hàm Dart khác nhau như thế nào?",
    a: 'Positional parameters được truyền theo thứ tự: `void greet(String name, int age)` gọi là `greet("John", 30)`. Named parameters dùng dấu ngoặc nhọn: `void greet({String? name, int? age})` gọi là `greet(name: "John", age: 30)`. Named parameters có thể tùy chọn; kết hợp từ khóa `required` để bắt buộc chúng.',
    q_en: "What are named and positional parameters in Dart functions?",
    a_en: "Positional parameters are passed in order. Named parameters use curly braces and are called by name. Named parameters can be optional; combine with the `required` keyword to make them mandatory. Named params improve readability for functions with many arguments.",
  },
  {
    id: 2811,
    category: "Dart",
    subcategory: "Dart Cơ Bản",
    level: "beginner",
    q: "Closure trong Dart là gì?",
    a: 'Closure là một hàm "đóng gói" (capture) các biến từ phạm vi bao quanh và có thể truy cập chúng ngay cả sau khi hàm ngoài đã kết thúc. \n\n**Ví dụ:** `int makeAdder(int x) { return (int y) => x + y; }` — hàm bên trong "giữ lại" biến `x`. Closure rất hữu ích cho callbacks và lập trình hàm trong Dart.',
    q_en: "What is a closure in Dart?",
    a_en: "A closure is a function that captures variables from its surrounding scope and can access them even after the outer function has finished. Closures are powerful for callbacks and functional programming patterns in Dart.",
  },
  {
    id: 2812,
    category: "Dart",
    subcategory: "Dart Cơ Bản",
    level: "beginner",
    q: "Sự khác nhau giữa `extends`, `implements`, và `with` (mixin) trong Dart là gì?",
    a: '`extends` kế thừa từ lớp cha, tái sử dụng code: `class Dog extends Animal`. `implements` coi lớp như một interface và buộc override tất cả method (không tái sử dụng code): `class Dog implements Animal`. `with` thêm hành vi mixin mà không cần kế thừa: `class Dog with Sound`. Dùng `extends` cho "là một", `implements` cho "hành động như", `with` để chia sẻ code.',
    q_en: "What is the difference between `extends`, `implements`, and `with` (mixin) in Dart?",
    a_en: '`extends` inherits from a parent class for code reuse. `implements` treats a class as a contract/interface and you override all its methods with no code reuse. `with` adds mixin behavior without inheritance. Use `extends` for "is-a", `implements` for "acts-like", `with` for code sharing.',
  },

  // ─── Widget & UI ────────────────────────────────────────────────────────────
  {
    id: 2813,
    category: "Flutter Widgets",
    subcategory: "Widget & UI",
    level: "beginner",
    q: "Sự khác nhau giữa `StatelessWidget` và `StatefulWidget` là gì?",
    a: "`StatelessWidget` là immutable — sau khi build xong, nó không thể thay đổi. Hàm `build()` chỉ được gọi một lần trừ khi parent rebuild. `StatefulWidget` duy trì state có thể thay đổi qua `setState()`, kích hoạt rebuild. Dùng `StatelessWidget` cho UI tĩnh (nhãn văn bản, icon), `StatefulWidget` cho component tương tác (form, toggle). Luôn ưu tiên `StatelessWidget` vì hiệu năng tốt hơn.",
    q_en: "What is the difference between `StatelessWidget` and `StatefulWidget`?",
    a_en: "`StatelessWidget` is immutable — once built, it cannot change. `StatefulWidget` maintains state that can change via `setState()`, triggering rebuilds. Use `StatelessWidget` for static UI, `StatefulWidget` for interactive components. Always prefer `StatelessWidget` for performance.",
  },
  {
    id: 2814,
    category: "Flutter Widgets",
    subcategory: "Widget & UI",
    level: "beginner",
    q: "`BuildContext` là gì và tại sao nó quan trọng?",
    a: "`BuildContext` là tham chiếu đến vị trí của widget trong cây widget, cung cấp quyền truy cập vào các dịch vụ như `Theme`, `MediaQuery`, `Navigator` và `ScaffoldState`. Mọi widget đều có `BuildContext` được truyền vào hàm `build()`. Nó cần thiết để điều hướng, hiển thị dialog, truy cập dữ liệu theme và đọc thuộc tính thiết bị.",
    q_en: "What is `BuildContext` and why is it important?",
    a_en: "`BuildContext` is a reference to a widget's location in the widget tree and provides access to services like `Theme`, `MediaQuery`, `Navigator`, and `ScaffoldState`. It's essential for navigating, showing dialogs, accessing theme data, and reading device properties.",
  },
  {
    id: 2815,
    category: "Flutter Widgets",
    subcategory: "Widget & UI",
    level: "intermediate",
    q: "Giải thích widget tree, element tree và render tree trong Flutter.",
    a: "Widget tree mô tả cấu trúc UI (blueprint bất biến). Element tree theo dõi các widget tồn tại và vòng đời của chúng (có thể thay đổi). Render tree xử lý layout và vẽ lên màn hình. Khi gọi `setState()`, widget rebuild, element cập nhật tham chiếu, và render tree chỉ vẽ lại vùng bị ảnh hưởng. Hiểu sự tách biệt này giải thích tại sao Flutter hiệu quả.",
    q_en: "Explain the widget tree, element tree, and render tree in Flutter.",
    a_en: "The widget tree describes the UI structure (immutable blueprint). The element tree tracks which widgets exist and their lifecycle (mutable). The render tree handles layout and painting. When you call `setState()`, widgets rebuild, elements update references, and the render tree repaints only affected areas.",
  },
  {
    id: 2816,
    category: "Flutter Widgets",
    subcategory: "Widget & UI",
    level: "intermediate",
    q: "Mục đích của widget `Key` là gì và khi nào nên dùng?",
    a: "Key giúp bảo tồn state của widget khi thứ tự danh sách con thay đổi. Không có key, Flutter khớp widget theo kiểu và vị trí, dẫn đến lẫn lộn state. Dùng `ValueKey` cho giá trị đơn giản, `ObjectKey` cho đối tượng phức tạp. Cần thiết cho `ListView.builder`, animated lists hoặc drag-drop. Không có key khi reorder danh sách `StatefulWidget` sẽ hoán đổi state của chúng.",
    q_en: "What is the purpose of widget `Key`s and when should you use them?",
    a_en: "Keys preserve widget state when the list of children changes order. Without keys, Flutter matches widgets by type and position, causing state corruption. Use `ValueKey` for simple values, `ObjectKey` for complex objects. Essential for `ListView.builder`, animated lists, or drag-drop.",
  },
  {
    id: 2817,
    category: "Flutter Widgets",
    subcategory: "Widget & UI",
    level: "intermediate",
    q: "Sự khác nhau giữa `ValueKey`, `ObjectKey` và `UniqueKey` là gì?",
    a: "`ValueKey` xác định widget bằng một giá trị cụ thể; hai widget cùng giá trị được coi là giống nhau. `ObjectKey` dùng tham chiếu danh tính của đối tượng; mỗi đối tượng duy nhất có key riêng. `UniqueKey` luôn tạo danh tính duy nhất, hữu ích khi muốn mỗi instance khác biệt. Tránh tạo `UniqueKey` trong `build()` — điều đó phá vỡ mục đích bảo tồn state.",
    q_en: "What is the difference between `ValueKey`, `ObjectKey`, and `UniqueKey`?",
    a_en: "`ValueKey` identifies widgets by a specific value. `ObjectKey` uses an object's reference identity. `UniqueKey` always creates a unique identity. Avoid creating `UniqueKey` in `build()` — it defeats state preservation.",
  },
  {
    id: 2818,
    category: "Flutter Widgets",
    subcategory: "Widget & UI",
    level: "intermediate",
    q: "`const` constructor là gì và tại sao quan trọng cho hiệu năng Flutter?",
    a: '`const` constructor tạo hằng số compile-time, bất biến và có thể tái sử dụng. Widget `const` bỏ qua rebuild nếu tham số không đổi, cải thiện hiệu năng đáng kể. Flutter có thể gộp các đối tượng `const` giống nhau thành một instance, giảm bộ nhớ. Luôn dùng `const` cho widget với tham số cố định: `const Text("Hello")`. Tránh tạo `UniqueKey` trong `const` vì sẽ mất lợi ích.',
    q_en: "What is `const` constructor and why is it important for Flutter performance?",
    a_en: "A `const` constructor creates compile-time constants that are immutable and reusable. `const` widgets bypass rebuilds if their parameters don't change, significantly improving performance. Flutter can merge identical `const` objects into a single instance, reducing memory usage.",
  },
  {
    id: 2819,
    category: "Flutter Widgets",
    subcategory: "Widget & UI",
    level: "intermediate",
    q: "Hệ thống layout của Flutter hoạt động thế nào: `Column`, `Row`, `Expanded` và `Flexible` dùng ra sao?",
    a: "`Row` xếp widget theo chiều ngang; `Column` xếp theo chiều dọc. Mặc định chúng chiếm không gian tối thiểu. `Expanded` buộc con lấp đầy không gian còn lại đều nhau. `Flexible` cho phép con chiếm thêm không gian nhưng có thể nhỏ hơn nếu cần. Dùng `MainAxisAlignment` và `CrossAxisAlignment` để kiểm soát khoảng cách và căn chỉnh.",
    q_en: "Explain the Flutter layout system: how do `Column`, `Row`, `Expanded`, and `Flexible` work?",
    a_en: "`Row` lays widgets horizontally; `Column` vertically. By default, they take minimum space. `Expanded` forces children to fill remaining space equally. `Flexible` allows children to take extra space but can be smaller if needed. Use `MainAxisAlignment` and `CrossAxisAlignment` for spacing and alignment.",
  },
  {
    id: 2820,
    category: "Flutter Widgets",
    subcategory: "Widget & UI",
    level: "intermediate",
    q: "`setState()` là gì và tại sao gọi nó lại kích hoạt rebuild?",
    a: "`setState()` là method trong `State` đánh dấu widget cần rebuild và thông báo cho Flutter framework. Gọi `setState()` không rebuild ngay lập tức — nó lên lịch rebuild trong frame tiếp theo. Thay đổi state bên trong `setState()` được phản ánh trong widget khi rebuild. Không dùng `setState()` cho cập nhật liên tục (animation, stream); dùng `AnimatedBuilder` hoặc state management thay thế.",
    q_en: "What is `setState()` and why does calling it trigger a rebuild?",
    a_en: "`setState()` marks the widget as needing rebuild and notifies the Flutter framework. It schedules a rebuild during the next frame, not immediately. Never use `setState()` for frequent updates like animations; use `AnimatedBuilder` or state management solutions instead.",
  },
  {
    id: 2821,
    category: "Flutter Widgets",
    subcategory: "Widget & UI",
    level: "intermediate",
    q: "Vòng đời của `StatefulWidget` là gì? Liệt kê các method quan trọng.",
    a: "`initState()` — gọi một lần khi widget được thêm vào (khởi tạo controller, listener). `didChangeDependencies()` — gọi sau initState và khi dependency thay đổi. `build()` — gọi thường xuyên, trả về widget tree. `didUpdateWidget()` — gọi khi parent rebuild với thuộc tính khác. `dispose()` — gọi một lần khi xóa (dọn dẹp tài nguyên, đóng stream). Nắm rõ vòng đời này để tránh memory leak.",
    q_en: "What is the lifecycle of a `StatefulWidget`? List the key methods.",
    a_en: "`initState()` is called once on insert. `didChangeDependencies()` is called after initState and when dependencies change. `build()` is called frequently. `didUpdateWidget()` is called when parent rebuilds with different properties. `dispose()` is called on removal — clean up resources here.",
  },
  {
    id: 2822,
    category: "Flutter Widgets",
    subcategory: "Widget & UI",
    level: "intermediate",
    q: "Tại sao `dispose()` quan trọng và nên dọn dẹp gì trong đó?",
    a: "`dispose()` được gọi khi widget bị xóa vĩnh viễn khỏi cây. Cần dọn dẹp tài nguyên để tránh memory leak: đóng stream, dispose animation controller, hủy đăng ký change notifier, hủy timer. Không dispose đúng cách khiến app giữ tham chiếu đến object đã chết, dần dần tiêu tốn bộ nhớ cho đến khi app crash.",
    q_en: "Why is `dispose()` important and what should you clean up there?",
    a_en: "`dispose()` is called when a widget is permanently removed from the tree. Clean up resources here to prevent memory leaks: close streams, dispose animation controllers, unsubscribe from change notifiers, cancel timers. Failure to dispose causes gradual memory consumption.",
  },
  {
    id: 2823,
    category: "Flutter Widgets",
    subcategory: "Widget & UI",
    level: "intermediate",
    q: "Sự khác nhau giữa `hot reload` và `hot restart` trong Flutter là gì?",
    a: "Hot reload bảo tồn state của app và tải lại code nhanh, chỉ chạy lại `build()` mà không chạy lại `initState()` hay `main()`. Hot restart phá hủy toàn bộ state, chạy lại `main()` và `initState()`, biên dịch lại app hoàn toàn. Dùng hot reload cho chỉnh sửa UI (~100ms), hot restart khi thay đổi khởi tạo state hoặc định nghĩa class (~1-2s).",
    q_en: "What is the difference between `hot reload` and `hot restart` in Flutter?",
    a_en: "Hot reload preserves app state and quickly reloads code changes, rerunning only `build()` methods. Hot restart destroys all state, re-runs `main()` and `initState()`, fully recompiling the app. Use hot reload for UI tweaks, hot restart when you change state initialization or class definitions.",
  },
  {
    id: 2824,
    category: "Flutter Widgets",
    subcategory: "Widget & UI",
    level: "intermediate",
    q: "Mục đích của `RepaintBoundary` là gì và khi nào nên dùng?",
    a: "`RepaintBoundary` cô lập một subtree để nó vẽ lại độc lập mà không ảnh hưởng toàn bộ cây. Dùng nó xung quanh widget vẽ lại thường xuyên (animation, progress indicator). Khi con của `RepaintBoundary` thay đổi, chỉ subtree đó mới vẽ lại. Lạm dụng tạo quá nhiều boundary và giảm hiệu năng; chỉ dùng tiết kiệm cho các điểm hot đã được xác định.",
    q_en: "What's the purpose of `RepaintBoundary` and when should you use it?",
    a_en: "`RepaintBoundary` isolates a subtree so it repaints independently without affecting the entire tree. Use it around widgets that repaint frequently like animations or progress indicators. Misuse creates too many boundaries and reduces performance; use sparingly only around known hot spots.",
  },

  // ─── State Management ───────────────────────────────────────────────────────
  {
    id: 2825,
    category: "Flutter State",
    subcategory: "State Management",
    level: "intermediate",
    q: "State management là gì và tại sao quan trọng?",
    a: "State management là cách xử lý dữ liệu thay đổi trong app (input người dùng, kết quả API). Quản lý state kém gây ra bug, memory leak và code khó bảo trì. Quản lý state tốt tách UI khỏi logic, giúp test dễ dàng và tái sử dụng code. Khi app lớn, chọn đúng chiến lược (`setState`, Provider, Riverpod, BLoC) trở nên thiết yếu.",
    q_en: "What is state management and why is it important?",
    a_en: "State management is how you handle data that changes in your app. Poor state management causes bugs, memory leaks, and hard-to-maintain code. Good state management separates UI from logic, makes testing easy, and enables code reuse across the application.",
  },
  {
    id: 2826,
    category: "Flutter State",
    subcategory: "State Management",
    level: "intermediate",
    q: "Những vấn đề gì khi dùng `setState()` cho app phức tạp?",
    a: "`setState()` rebuild toàn bộ cây con của widget đó, gây vấn đề hiệu năng với cây widget lớn. Logic nghiệp vụ trộn lẫn với UI làm code khó test. Không scale được — nhiều cập nhật state làm code rối rắm khó debug. State bị giới hạn trong một widget — chia sẻ state giữa các widget ở xa trở nên cực kỳ phức tạp. Với bất kỳ thứ gì ngoài widget đơn giản, hãy dùng state management chuyên dụng.",
    q_en: "What are the problems with using `setState()` for complex apps?",
    a_en: "`setState()` rebuilds that widget's entire subtree, causing performance issues with large trees. Business logic is mixed with UI making testing impossible. State is scoped to one widget — sharing state between distant widgets is cumbersome. For anything beyond simple widgets, use dedicated state management.",
  },
  {
    id: 2827,
    category: "Flutter State",
    subcategory: "State Management",
    level: "intermediate",
    q: "Provider pattern là gì và hoạt động như thế nào?",
    a: "Provider là thư viện state management dùng `ChangeNotifier` để thông báo listener khi state thay đổi. Widget lắng nghe qua `Consumer` hoặc `watch()` và chỉ rebuild khi dữ liệu chúng phụ thuộc thay đổi. Provider nhẹ, được Flutter chính thức khuyến nghị và phù hợp cho app nhỏ đến vừa. Bọc app với `MultiProvider`, định nghĩa provider cho data, và consume trong widget.",
    q_en: "What is the Provider pattern and how does it work?",
    a_en: "Provider uses `ChangeNotifier` to notify listeners when state changes. Widgets listen via `Consumer` or `watch()` and rebuild only when data they depend on changes. Provider is lightweight, officially endorsed, and great for small-to-medium apps.",
  },
  {
    id: 2828,
    category: "Flutter State",
    subcategory: "State Management",
    level: "intermediate",
    q: "Riverpod là gì và khác Provider như thế nào?",
    a: "Riverpod (kế thừa tinh thần của Provider, cùng tác giả) hoàn toàn type-safe, không phụ thuộc `BuildContext`, và dùng functional provider. Riverpod sinh code lúc compile-time giúp phòng ngừa nhiều bug. Riverpod hỗ trợ parameterized provider, test tốt hơn và auto-dispose. Dùng Riverpod cho project mới; hiện đại hơn Provider nhưng learning curve hơi cao hơn.",
    q_en: "What is Riverpod and how is it different from Provider?",
    a_en: "Riverpod is fully type-safe, doesn't depend on `BuildContext`, and uses functional providers with compile-time code generation. It supports parameterized providers, better testing, and auto-dispose. Use Riverpod for new projects; it's more modern than Provider but has a slightly steeper learning curve.",
  },
  {
    id: 2829,
    category: "Flutter State",
    subcategory: "State Management",
    level: "intermediate",
    q: "Giải thích pattern BLoC và tại sao interviewer hay hỏi về nó.",
    a: "BLoC (Business Logic Component) tách logic nghiệp vụ khỏi UI thông qua kiến trúc event-in/state-out. Bạn dispatch `Event` vào BLoC, BLoC xử lý và emit ra `State` mới. BLoC có thể test được, đảm bảo kiến trúc nhất quán, và scale tốt trong team lớn. BLoC cần boilerplate nhiều hơn Provider nhưng cung cấp cấu trúc và khả năng truy vết tốt hơn cho app phức tạp.",
    q_en: "Explain the BLoC pattern and why interviewers ask about it.",
    a_en: "BLoC (Business Logic Component) separates business logic from UI via an event-in/state-out architecture. You dispatch `Event`s into a BLoC, which processes them and emits new `State`s. BLoCs are testable, enforce consistent architecture, and scale well on large teams.",
  },
  {
    id: 2830,
    category: "Flutter State",
    subcategory: "State Management",
    level: "intermediate",
    q: "So sánh Provider, Riverpod và BLoC: khi nào dùng cái nào?",
    a: 'Provider: app đơn giản, setup nhanh, hỗ trợ chính thức. Riverpod: type-safe, hiện đại, test tốt hơn, parameterized provider, xu hướng ngày càng phổ biến. BLoC: team lớn, app phức tạp, cần truy vết event, các ngành có quy định chặt (ngân hàng). Không có lựa chọn "tốt nhất" — phải phù hợp giải pháp với vấn đề. Interviewer muốn bạn hiểu đánh đổi.',
    q_en: "Compare Provider, Riverpod, and BLoC: when would you use each?",
    a_en: 'Provider is simplest and officially supported. Riverpod is type-safe, modern, with better testing. BLoC is most opinionated, best for large teams, complex apps, and regulated industries. There\'s no "best" — fit the solution to the problem based on team size and app complexity.',
  },
  {
    id: 2831,
    category: "Flutter State",
    subcategory: "State Management",
    level: "intermediate",
    q: "`ChangeNotifier` là gì và khi nào dùng nó?",
    a: "`ChangeNotifier` là class đơn giản thông báo listener khi state thay đổi thông qua `notifyListeners()`. `extend ChangeNotifier` để tạo đối tượng observable. Provider theo dõi `ChangeNotifier` và rebuild khi `notifyListeners()` được gọi. Nó nhẹ nhưng cần quản lý thông báo thủ công. Cho state đơn giản (theme, auth, user data), `ChangeNotifier` thường đủ dùng.",
    q_en: "What is `ChangeNotifier` and when do you use it?",
    a_en: "`ChangeNotifier` notifies listeners when state changes via `notifyListeners()`. Extend it to create observable objects that Provider can watch and rebuild widgets accordingly. It's lightweight but requires manual notification management.",
  },
  {
    id: 2832,
    category: "Flutter State",
    subcategory: "State Management",
    level: "intermediate",
    q: "`Selector` trong Provider là gì và tại sao dùng nó thay vì `Consumer`?",
    a: "`Consumer` theo dõi toàn bộ provider và rebuild bất cứ khi nào nó thay đổi. `Selector` cho phép chỉ theo dõi một phần cụ thể của state: `Selector<UserProvider, String>(selector: (_, user) => user.name, ...)` chỉ rebuild khi `name` thay đổi, không phải khi `age` thay đổi. Kiểm soát chi tiết này ngăn rebuild không cần thiết và cải thiện hiệu năng trong provider phức tạp.",
    q_en: "What is `Selector` in Provider and why use it instead of `Consumer`?",
    a_en: "`Consumer` watches an entire provider and rebuilds whenever it changes. `Selector` lets you watch only a specific part of state, rebuilding only when that specific slice changes. This fine-grained control prevents unnecessary rebuilds and improves performance in complex providers.",
  },
  {
    id: 2833,
    category: "Flutter State",
    subcategory: "State Management",
    level: "intermediate",
    q: "`GetIt` là gì và liên quan đến state management như thế nào?",
    a: "`GetIt` là service locator cho dependency injection. Bạn đăng ký singleton hoặc factory: `getIt.registerSingleton<Repository>(Repository())`, rồi truy cập ở bất kỳ đâu: `getIt<Repository>()`. Bản thân nó không phải state management mà giúp clean architecture bằng cách quản lý dependency. Thường dùng kết hợp BLoC hoặc Riverpod để inject repository và service vào business logic.",
    q_en: "What is `GetIt` and how does it relate to state management?",
    a_en: "`GetIt` is a service locator for dependency injection. Register singletons or factories and access them anywhere in the app. It's not state management itself but enables clean architecture by managing dependencies, often used alongside BLoC or Riverpod.",
  },
  {
    id: 2834,
    category: "Flutter State",
    subcategory: "State Management",
    level: "intermediate",
    q: "Làm thế nào để test code state management (Provider/Riverpod/BLoC)?",
    a: "Provider: Mock `ChangeNotifier`, dùng `ProviderContainer` để test. Riverpod: Dùng `ProviderContainer`, override provider bằng mock. BLoC: Test trực tiếp với `bloc_test`, xác minh event → state transition. Không bao giờ phụ thuộc `BuildContext` trong code cần test; tách logic nghiệp vụ vào service class. State management tốt thì test dễ; nếu test khó thì kiến trúc cần xem lại.",
    q_en: "How do you test state management code (Provider/Riverpod/BLoC)?",
    a_en: "For Provider: mock `ChangeNotifier`s, use `ProviderContainer` for testing. For Riverpod: override providers with mocks. For BLoC: use `bloc_test`, verify event-to-state transitions. Never depend on `BuildContext` in testable code; if testing is hard, your architecture needs rework.",
  },

  // ─── Navigation & Routing ───────────────────────────────────────────────────
  {
    id: 2835,
    category: "Flutter",
    subcategory: "Navigation & Routing",
    level: "intermediate",
    q: "Sự khác nhau giữa `Navigator` cũ (v1) và các cách tiếp cận routing hiện đại là gì?",
    a: "Navigator v1 dùng lệnh imperative `Navigator.push()` và `Navigator.pop()` — bạn quản lý navigation stack thủ công. Navigator 2.0 giới thiệu declarative routing qua Router API. GoRouter (được khuyến nghị hiện nay) xây trên đó, cung cấp URL-based, type-safe declarative routing. GoRouter đơn giản hơn và tự động xử lý deep linking, trở thành lựa chọn hiện đại cho project mới.",
    q_en: "What is the difference between the old `Navigator` (v1) and modern routing approaches?",
    a_en: "Navigator v1 uses imperative `Navigator.push()` and `Navigator.pop()` calls to manually manage the navigation stack. GoRouter (recommended now) provides URL-based declarative routing with automatic deep linking support, making it the modern choice for new projects.",
  },
  {
    id: 2836,
    category: "Flutter",
    subcategory: "Navigation & Routing",
    level: "intermediate",
    q: "GoRouter là gì và giải quyết vấn đề gì?",
    a: "GoRouter là routing package được Flutter chính thức khuyến nghị. Cung cấp URL-based declarative routing với path/query parameter. Tự động xử lý deep linking, hỗ trợ nested navigation với `ShellRoute`, thêm redirect logic cho auth guard, và cho phép type-safe route với code generation. Thay vì quản lý Navigator stack thủ công, bạn khai báo tất cả route trước và để GoRouter xử lý navigation.",
    q_en: "What is GoRouter and what problems does it solve?",
    a_en: "GoRouter is the officially recommended routing package for Flutter. It provides URL-based declarative routes with path/query parameters, handles deep linking automatically, supports nested navigation with `ShellRoute`, and adds redirect logic for auth guards.",
  },
  {
    id: 2837,
    category: "Flutter",
    subcategory: "Navigation & Routing",
    level: "intermediate",
    q: "Làm thế nào để truyền dữ liệu giữa các màn hình trong Flutter?",
    a: 'Cách 1 (Navigator cũ): Truyền qua constructor: `Navigator.push(context, MaterialPageRoute(builder: (_) => DetailPage(item: item)))`. Cách 2 (GoRouter): Dùng path parameter: `route: "/detail/:id"` và truy cập qua `GoRouterState`. Cách 3 (State Management): Lưu data trong provider/BLoC, truy cập từ bất kỳ màn hình nào. GoRouter với path parameter là sạch nhất và hỗ trợ deep linking.',
    q_en: "How do you pass data between screens in Flutter?",
    a_en: "Method 1 (old Navigator): pass via constructor in MaterialPageRoute builder. Method 2 (GoRouter): use path parameters and access via `GoRouterState`. Method 3 (State Management): store data in provider/BLoC, access from any screen. GoRouter with path parameters is cleanest and supports deep linking.",
  },
  {
    id: 2838,
    category: "Flutter",
    subcategory: "Navigation & Routing",
    level: "intermediate",
    q: "Deep linking là gì và GoRouter hỗ trợ nó như thế nào?",
    a: "Deep linking là mở app tại màn hình cụ thể thông qua URL (như `myapp://detail/123`). Deep link đến từ push notification, web link hoặc system intent. GoRouter hỗ trợ deep linking tự động — định nghĩa route như path: `GoRoute(path: '/detail/:id', ...)` và GoRouter khớp URL với màn hình đúng. Điều này cho phép universal links (iOS) và app links (Android) mà không cần setup thêm.",
    q_en: "What is deep linking and how does GoRouter support it?",
    a_en: "Deep linking opens your app at a specific screen via a URL. GoRouter supports deep linking automatically by matching URL paths to the correct screens. This enables universal links on iOS and app links on Android without extra setup.",
  },
  {
    id: 2839,
    category: "Flutter",
    subcategory: "Navigation & Routing",
    level: "intermediate",
    q: "Làm thế nào để xử lý navigation state với GoRouter?",
    a: "GoRouter cung cấp `GoRouterState` với location, parameter, extra data: `String id = state.pathParameters['id']!`. Với nested navigation (bottom tab có stack độc lập), dùng `ShellRoute`. Với navigation dựa trên auth, dùng `redirect()` để kiểm tra state auth trước khi build page: `if (!isLoggedIn) return '/login'`. Điều này tập trung hóa logic navigation.",
    q_en: "How do you handle navigation state with GoRouter?",
    a_en: "GoRouter provides `GoRouterState` with route location, parameters, and extra data. For nested navigation use `ShellRoute`. For auth-based navigation, use the `redirect()` callback to check auth state before building pages, keeping all navigation logic centralized.",
  },
  {
    id: 2840,
    category: "Flutter",
    subcategory: "Navigation & Routing",
    level: "intermediate",
    q: "`ShellRoute` trong GoRouter là gì và khi nào dùng?",
    a: "`ShellRoute` bọc nhiều route với một parent chia sẻ (như bottom navigation bar). Route bên trong `ShellRoute` build trong context của parent đó, duy trì navigation stack độc lập cho mỗi tab. Đây là cách đúng để triển khai bottom tab navigation: mỗi tab có lịch sử điều hướng riêng, nhấn tab hiển thị lịch sử của nó thay vì bắt đầu mới. Không có `ShellRoute`, chuyển tab sẽ làm nút back hoạt động sai.",
    q_en: "What is `ShellRoute` in GoRouter and when do you use it?",
    a_en: "`ShellRoute` wraps multiple routes with a shared parent like a bottom navigation bar. Routes inside maintain independent navigation stacks for each tab. This is the correct way to implement bottom tab navigation — without `ShellRoute`, switching tabs breaks the back button behavior.",
  },
  {
    id: 2841,
    category: "Flutter",
    subcategory: "Navigation & Routing",
    level: "intermediate",
    q: "Làm thế nào để triển khai authentication guard với GoRouter?",
    a: "Dùng callback `redirect` để kiểm tra state auth trước khi build route. \n\n**Ví dụ:** nếu user chưa đăng nhập và cố truy cập route được bảo vệ, redirect về `/login`. Kết hợp với Riverpod để `redirect` tự động phản ứng khi auth state thay đổi. Cách này xử lý tất cả route tập trung, không cần kiểm tra auth trong từng widget.",
    q_en: "How do you implement authentication guards with GoRouter?",
    a_en: "Use the `redirect` callback to check auth state before building routes. If the user isn't logged in and tries to access a protected route, redirect them to the login screen. This approach handles all routes centrally without auth checks scattered across widgets.",
  },
  {
    id: 2842,
    category: "Flutter",
    subcategory: "Navigation & Routing",
    level: "intermediate",
    q: "Những vấn đề gì xảy ra với Navigator 1.0 named routes khi app lớn?",
    a: "Navigator 1.0 yêu cầu quản lý navigation state và back stack thủ công. Named route hoạt động nhưng xử lý deep linking kém — phải thêm nhiều boilerplate. Navigation phức tạp (nested stack, conditional routing) trở nên rối. Toàn bộ navigation tree là global, khó để lý luận. GoRouter giải quyết tất cả bằng cách cung cấp declarative, URL-based routing tự động xử lý back stack và deep linking.",
    q_en: "What problems occur with Navigator 1.0 named routes at scale?",
    a_en: "Navigator 1.0 requires manually managing the navigation state and back stack. Named routes handle deep linking poorly, requiring significant boilerplate. Complex navigation becomes messy. GoRouter solves these by providing declarative, URL-based routing that automatically handles back stack and deep linking.",
  },

  // ─── Architecture ───────────────────────────────────────────────────────────
  {
    id: 2843,
    category: "Flutter",
    subcategory: "Architecture",
    level: "intermediate",
    q: "Clean architecture là gì và tại sao quan trọng cho app lớn?",
    a: "Clean architecture tách code thành các layer: Presentation (UI, widget), Domain (logic nghiệp vụ, entity, use case), Data (repository, data source). Mỗi layer độc lập và có thể test. Sự tách biệt này cho phép thay đổi UI framework, hoán đổi data source, và test logic nghiệp vụ mà không động vào UI. App lớn không có clean architecture sẽ trở thành spaghetti code khó bảo trì khi phát triển.",
    q_en: "What is clean architecture and why is it important for large apps?",
    a_en: "Clean architecture separates code into layers: Presentation (UI), Domain (business logic, entities, use cases), and Data (repositories, data sources). Each layer is independent and testable. Large apps without clean architecture become unmaintainable as code grows.",
  },
  {
    id: 2844,
    category: "Flutter",
    subcategory: "Architecture",
    level: "intermediate",
    q: "Repository pattern là gì và hoạt động như thế nào?",
    a: "Repository là lớp abstraction giữa domain logic và data source. Thay vì UI gọi trực tiếp API, bạn gọi `Repository.getUser(id)` — method này ẩn đi việc data đến từ network, cache hay database. Interface repository được định nghĩa trong domain layer; implementation trong data layer. Điều này giúp dễ dàng hoán đổi data source, hỗ trợ offline-first, và test bằng cách mock repository.",
    q_en: "What is the repository pattern and how does it work?",
    a_en: "Repository is an abstraction layer between domain logic and data sources. Instead of UI directly calling APIs, you call a repository method that abstracts whether data comes from network, cache, or database. This enables easy swapping of data sources, offline-first functionality, and mocking for tests.",
  },
  {
    id: 2845,
    category: "Flutter",
    subcategory: "Architecture",
    level: "intermediate",
    q: "Dependency injection là gì và cách triển khai trong Flutter?",
    a: "Dependency injection là truyền dependency (service, repository) vào class thay vì tạo chúng bên trong. Thay vì `class UserBloc { final repo = UserRepository(); }`, hãy inject: `class UserBloc { UserBloc(this.repo); final UserRepository repo; }`. Điều này cho phép test (mock repository) và linh hoạt (hoán đổi implementation). Dùng `GetIt` cho service locator pattern hoặc truyền dependency qua constructor.",
    q_en: "What is dependency injection and how do you implement it in Flutter?",
    a_en: "Dependency injection is passing dependencies into a class instead of creating them inside. This enables testing with mocked dependencies and flexibility to swap implementations. Use `GetIt` for service locator pattern or pass dependencies through constructor chains.",
  },
  {
    id: 2846,
    category: "Flutter",
    subcategory: "Architecture",
    level: "intermediate",
    q: "Giải thích sự khác nhau giữa singleton và factory pattern trong Flutter.",
    a: "Singleton tạo một instance cho toàn bộ app: `getIt.registerSingleton<Repository>(Repository())`. Dùng cho tài nguyên chia sẻ (database, API client, repository). Factory tạo instance mới mỗi lần: `getIt.registerFactory<UserBloc>(() => UserBloc(repo))`. Dùng cho BLoC (mỗi màn hình cần state độc lập). Dùng sai gây state leak (chia sẻ state có thể thay đổi) hoặc lãng phí bộ nhớ.",
    q_en: "Explain the difference between singleton and factory patterns in Flutter.",
    a_en: "Singleton creates one instance for the entire app — use for shared resources like databases and API clients. Factory creates a new instance each time — use for BLoCs where each screen needs independent state. Misuse causes state leaks or unnecessary memory consumption.",
  },
  {
    id: 2847,
    category: "Flutter",
    subcategory: "Architecture",
    level: "intermediate",
    q: "MVVM architecture trong Flutter là gì và khác MVC như thế nào?",
    a: "MVC (Model-View-Controller): Controller xử lý input và cập nhật Model, View hiển thị Model. MVVM (Model-View-ViewModel): ViewModel expose data và logic UI cần. UI bind vào ViewModel. ViewModel không biết về UI. MVVM testable hơn MVC vì ViewModel không có dependency vào UI framework. Flutter không bắt buộc MVVM nhưng Provider + ViewModel class đạt được pattern này rất gọn.",
    q_en: "What is MVVM architecture in Flutter and how does it differ from MVC?",
    a_en: "MVC has a Controller handling input and updating Model for the View. MVVM has a ViewModel exposing data and logic without knowing about UI. MVVM is more testable because ViewModel has no UI framework dependencies. Provider combined with ViewModel classes implements MVVM elegantly in Flutter.",
  },
  {
    id: 2848,
    category: "Flutter",
    subcategory: "Architecture",
    level: "intermediate",
    q: "Use-case layer trong clean architecture là gì và tại sao cần nó?",
    a: "Use case đóng gói logic nghiệp vụ cho một tính năng: `GetUserUseCase`, `LoginUseCase`. Mỗi use case là một class với method `call()` nhận tham số và trả về kết quả. Use case nằm ở domain layer, không phụ thuộc framework, và dễ test cao. Chúng nối cầu giữa UI (trigger use case) và repository (lấy data). Use case được thiết kế tốt có thể tái sử dụng và test độc lập.",
    q_en: "What is the use-case layer in clean architecture and why include it?",
    a_en: "Use cases encapsulate business logic for single features, each as a class with a `call()` method. They are domain-level, framework-agnostic, and highly testable. They bridge the gap between UI triggers and repositories, and well-designed use cases are reusable and testable in isolation.",
  },
  {
    id: 2849,
    category: "Flutter",
    subcategory: "Architecture",
    level: "intermediate",
    q: "Cách tổ chức folder/package trong app Flutter clean architecture như thế nào?",
    a: "Cấu trúc phổ biến: `lib/presentation/` (UI, widget, BLoC/ViewModel), `lib/domain/` (entity, repository interface, use case), `lib/data/` (API client, local DB, repository implementation), `lib/config/` (cấu hình app, constant). Mỗi feature có thể có domain/data/presentation riêng. Điều này giữ code có tổ chức, dễ điều hướng và test.",
    q_en: "How do you structure folders/packages in a clean architecture Flutter app?",
    a_en: "Common structure: `presentation/` for UI and BLoCs/ViewModels, `domain/` for entities and use cases, `data/` for API clients and repository implementations, `config/` for app configuration. Each feature can have its own domain/data/presentation subdirectory.",
  },
  {
    id: 2850,
    category: "Flutter",
    subcategory: "Architecture",
    level: "intermediate",
    q: "Sự khác nhau giữa entity và model trong Flutter architecture là gì?",
    a: "Entity đại diện cho khái niệm domain (đối tượng nghiệp vụ cốt lõi): `User(id, name, email)`. Entity là class Dart thuần, không phụ thuộc framework và dễ test, nằm ở domain layer. Model là biểu diễn API/database với serialization: `UserModel.toJson()`. Model nằm ở data layer. Dùng entity trong business logic, model trong API/DB, và mapper function để chuyển đổi giữa chúng.",
    q_en: "What is the difference between entity and model in Flutter architecture?",
    a_en: "Entity represents a domain concept — a pure Dart class in the domain layer, framework-agnostic and testable. Model is an API/database representation with serialization in the data layer. Use entities in business logic, models in API/DB, and mapper functions to convert between them.",
  },

  // ─── Nâng Cao ───────────────────────────────────────────────────────────────
  {
    id: 2851,
    category: "Flutter",
    subcategory: "Nâng Cao",
    level: "advanced",
    q: "Isolate trong Dart là gì và khi nào dùng chúng?",
    a: "Isolate là các luồng thực thi riêng biệt không chia sẻ bộ nhớ — mỗi isolate có heap và event loop riêng. Dùng isolate cho công việc tốn CPU (parse JSON lớn, mã hóa, xử lý ảnh) để tránh block main UI thread. Tạo bằng `Isolate.spawn()` và giao tiếp qua message passing. Isolate nhẹ hơn OS thread nhưng nặng hơn async/await, nên dùng tiết kiệm.",
    q_en: "What are isolates in Dart and when do you use them?",
    a_en: "Isolates are separate execution threads that don't share memory — each has its own heap and event loop. Use isolates for CPU-intensive work (JSON parsing, encryption, image processing) to avoid blocking the main UI thread. They are lighter than OS threads but heavier than async/await.",
  },
  {
    id: 2852,
    category: "Flutter",
    subcategory: "Nâng Cao",
    level: "advanced",
    q: "Platform channel là gì và khi nào dùng chúng?",
    a: "Platform channel cho phép giao tiếp giữa Dart và code native (Kotlin/Java cho Android, Swift/ObjC cho iOS). Dùng khi cần tính năng Flutter không hỗ trợ: truy cập phần cứng đặc thù, dùng thư viện native, hoặc kiểm soát OS chi tiết. Giao tiếp là bất đồng bộ thông qua message passing. Platform channel là lối thoát cho native functionality nhưng thêm độ phức tạp, nên ưu tiên dùng plugin có sẵn.",
    q_en: "What are platform channels and when do you use them?",
    a_en: "Platform channels enable communication between Dart and native code (Kotlin/Java for Android, Swift/ObjC for iOS). Use them for features Flutter doesn't support like device-specific hardware or native libraries. Communication is asynchronous via message passing.",
  },
  {
    id: 2853,
    category: "Flutter",
    subcategory: "Nâng Cao",
    level: "advanced",
    q: "Làm thế nào để xử lý lỗi và exception trong code Dart bất đồng bộ?",
    a: "Dùng try-catch trong hàm async: `try { await api.get(); } catch (e) { print(e); }`. Bọc `Future` chain bằng `.catchError()`: `future.catchError((e) => defaultValue)`. Với stream, truyền `onError` vào listen: `stream.listen(onData, onError: (e) => handle(e))`. Lan truyền lỗi lên trên; không im lặng nuốt chúng. Lỗi không được xử lý sẽ crash app hoặc ghi vào crash reporting service.",
    q_en: "How do you handle errors and exceptions in async Dart code?",
    a_en: "Use try-catch inside async functions. Wrap `Future` chains with `.catchError()`. For streams, pass `onError` to the listen call. Always propagate errors up; never swallow them silently. Unhandled errors crash the app or get logged to crash reporting services.",
  },
  {
    id: 2854,
    category: "Flutter",
    subcategory: "Nâng Cao",
    level: "advanced",
    q: "`Future.wait()` là gì và khi nào dùng?",
    a: "`Future.wait()` chờ nhiều Future hoàn thành, sau đó trả về list kết quả: `List results = await Future.wait([future1, future2, future3])`. Nếu bất kỳ Future nào throw, toàn bộ `wait()` sẽ throw. Dùng cho các tác vụ song song: fetch nhiều API endpoint cùng lúc, load nhiều asset. Hiệu quả hơn việc gọi `await` tuần tự vì các tác vụ chạy đồng thời.",
    q_en: "What is `Future.wait()` and when do you use it?",
    a_en: "`Future.wait()` waits for multiple futures to complete then returns a list of results. If any future throws, the entire `wait()` throws. Use for parallel operations like fetching multiple API endpoints simultaneously — more efficient than sequential `await` calls.",
  },
  {
    id: 2855,
    category: "Flutter",
    subcategory: "Nâng Cao",
    level: "advanced",
    q: "Làm thế nào để profile và tối ưu hiệu năng app Flutter?",
    a: "Dùng DevTools để profile CPU, memory và frame rendering. Kiểm tra jank (frame bị bỏ) qua tab Performance. Xác định frame chậm và dùng profiler xem function nào tốn thời gian. Với memory leak, chụp heap snapshot và so sánh trước/sau. Tối ưu bằng: dùng `const`, lazy-load list, tránh widget tree quá sâu, chuyển công việc nặng sang isolate, và dùng `RepaintBoundary` đúng chỗ.",
    q_en: "How do you profile and optimize Flutter app performance?",
    a_en: "Use DevTools to profile CPU, memory, and frame rendering. Check for jank (dropped frames) via the Performance tab. For memory leaks, take heap snapshots and compare. Optimize by using `const`, lazy-loading lists, avoiding deep widget trees, and moving heavy work to isolates.",
  },
  {
    id: 2856,
    category: "Flutter",
    subcategory: "Nâng Cao",
    level: "advanced",
    q: "Sự khác nhau giữa `ListView.builder` và `ListView` cho danh sách dài là gì?",
    a: "`ListView` build tất cả con trước — cuộn qua danh sách 1000 item rất chậm và tốn bộ nhớ. `ListView.builder` lazily build item hiện trên màn hình: chỉ ~10 item tồn tại trong bộ nhớ mọi lúc. Luôn dùng `ListView.builder` cho danh sách động. `SliverList` là giải pháp nâng cao cho scrolling phức tạp (kết hợp list và grid). Nhầm lẫn dùng `ListView` thay `ListView.builder` là bug hiệu năng phổ biến.",
    q_en: "What is the difference between `ListView.builder` and `ListView` for long lists?",
    a_en: "`ListView` builds all children upfront — slow and memory-intensive for large lists. `ListView.builder` lazily builds only visible items, keeping only ~10 items in memory at once. Always use `ListView.builder` for dynamic lists. Using `ListView` instead is a common performance bug.",
  },
  {
    id: 2857,
    category: "Flutter",
    subcategory: "Nâng Cao",
    level: "advanced",
    q: "Làm thế nào để triển khai CI/CD cho app Flutter?",
    a: "Dùng GitHub Actions, Fastlane hoặc Firebase App Distribution. Pipeline thông thường: lint code → chạy test → build APK/IPA → upload lên app store hoặc Firebase. Tự động hóa trên pull request để phát hiện vấn đề sớm. Các tool: `flutter analyze` để lint, `flutter test` cho unit/widget test, `flutter build apk` cho release build, `firebase-tools` để phân phối. CI/CD tốt ngăn các release bị lỗi.",
    q_en: "How do you implement CI/CD for Flutter apps?",
    a_en: "Use GitHub Actions, Fastlane, or Firebase App Distribution. Common pipeline: lint → run tests → build APK/IPA → upload to app store or Firebase. Automate on pull requests to catch issues early. Good CI/CD prevents broken releases from reaching users.",
  },
  {
    id: 2858,
    category: "Flutter",
    subcategory: "Nâng Cao",
    level: "advanced",
    q: "Sự khác nhau giữa debug, profile và release build là gì?",
    a: "Debug: chậm, không tối ưu, có debug symbol, bật hot reload, chạy JIT. Dùng khi phát triển. Profile: tối ưu như release nhưng giữ profiling tool; hot reload bị tắt. Dùng để test hiệu năng mà không có artifact của debug. Release: tối ưu hoàn toàn, minified, biên dịch native code với AOT, kích thước nhỏ nhất, hiệu năng tốt nhất. Dùng để phân phối. Chọn mode phù hợp cho từng tác vụ.",
    q_en: "What are the differences between debug, profile, and release builds in Flutter?",
    a_en: "Debug: slow, unoptimized, enables hot reload, runs with JIT — use during development. Profile: optimized like release but includes profiling tools — use to test performance accurately. Release: fully optimized, AOT compiled, smallest size, best performance — use for distribution.",
  },
  {
    id: 2859,
    category: "Flutter",
    subcategory: "Nâng Cao",
    level: "advanced",
    q: "Làm thế nào để test app Flutter? Sự khác nhau giữa unit, widget và integration test là gì?",
    a: "Unit test kiểm tra function/class độc lập: `test('Math', () { expect(2+2, equals(4)); })`. Widget test kiểm tra widget mà không render thực: `testWidgets('Button', (tester) { await tester.tap(find.byType(Button)); })`. Integration test kiểm tra toàn bộ app trên thiết bị/emulator thật. Unit test nhanh và đáng tin cậy. Widget test phát hiện bug UI. Integration test phát hiện vấn đề thực tế. Dùng cả ba theo mô hình kim tự tháp.",
    q_en: "How do you test Flutter apps? What's the difference between unit, widget, and integration tests?",
    a_en: "Unit tests test functions/classes in isolation. Widget tests test widgets without rendering to screen. Integration tests test the entire app on a real device/emulator. Use all three in a balanced test pyramid — unit tests are fastest and most reliable, integration tests catch real-world issues.",
  },
  {
    id: 2860,
    category: "Flutter",
    subcategory: "Nâng Cao",
    level: "advanced",
    q: "`WidgetsFlutterBinding` là gì và khi nào dùng nó?",
    a: "`WidgetsFlutterBinding` khởi tạo Flutter engine. Dùng `WidgetsFlutterBinding.ensureInitialized()` trước khi chạy app để khởi tạo plugin/service trước `runApp()`. Trường hợp dùng phổ biến: setup `GetIt` trước khi app khởi động, khởi tạo code native, cấu hình logging. Không có nó, một số khởi tạo bất đồng bộ sẽ thất bại vì binding chưa sẵn sàng. Luôn gọi trong `main()` nếu có setup trước app.",
    q_en: "What is `WidgetsFlutterBinding` and when do you use it?",
    a_en: "`WidgetsFlutterBinding.ensureInitialized()` initializes the Flutter engine before `runApp()`. Use it when you need to initialize plugins, native code, or services before the app starts. Without it, some async initialization fails because the binding isn't ready yet.",
  },
  {
    id: 2861,
    category: "Dart",
    subcategory: "Dart Cơ Bản",
    level: "intermediate",
    q: "Records trong Dart là gì và chúng giúp ích thế nào khi cần trả về nhiều giá trị từ một hàm?",
    a: "Records là kiểu dữ liệu nhẹ, bất biến, cho phép nhóm nhiều giá trị lại mà không cần tạo class. Bạn có thể dùng cú pháp vị trí `(String, int)` hoặc tên trường `({String name, int age})`. Để trả về từ hàm: `(String, int) fetchUser() => ('Alice', 30);` và destructure bằng `var (name, age) = fetchUser();`. Records thay thế các workaround dùng Map hay List khi cần trả nhiều giá trị, code ngắn hơn và type-safe hơn.",
    q_en: "What are Records in Dart and how do they enable multi-value returns?",
    a_en: "Records are lightweight, immutable aggregates bundling multiple values (positional or named fields) without needing a class. \n\n**Example:** `(String, int) fetchUser() => ('Alice', 30);` then destructure with `var (name, age) = fetchUser();`. They replace Map/List workarounds for multi-return functions with type-safe, concise syntax.",
  },
  {
    id: 2862,
    category: "Dart",
    subcategory: "Dart Cơ Bản",
    level: "intermediate",
    q: "Pattern matching trong Dart 3 hoạt động như thế nào? Cho ví dụ thực tế.",
    a: "Pattern matching cho phép khớp giá trị và destructure cùng lúc. Ví dụ với switch: `switch (shape) { case Circle(radius: var r) => print('Circle r=$r'); case Rectangle(width: var w, height: var h) => print('Rect ${w}x${h}'); }`. Bạn cũng có thể thêm guard clause: `case (int a, int b) when a > b => print('$a lớn hơn $b');`. Patterns còn dùng được trong khai báo biến: `var (x, y) = getPoint();`. Giúp code declarative hơn hẳn so với chuỗi if/else.",
    q_en: "How does pattern matching work in Dart 3? Give a practical example.",
    a_en: "Patterns match values and destructure them simultaneously. \n\n**Example:** `switch (shape) { case Circle(radius: var r) => print(r); case Rectangle(width: var w, height: var h) => print('${w}x${h}'); }`. Guard clauses add conditions: `case (int a, int b) when a > b`. Patterns also work in variable declarations: `var (x, y) = getPoint();`. Far more declarative than if/else chains.",
  },
  {
    id: 2863,
    category: "Dart",
    subcategory: "Dart Cơ Bản",
    level: "intermediate",
    q: "Switch expression trong Dart khác switch statement như thế nào và khi nào nên dùng?",
    a: "Switch expression trả về giá trị trực tiếp và bắt buộc phải exhaustive (phủ hết mọi trường hợp). Cú pháp dùng `=>`: `String grade = score switch { > 90 => 'A', > 80 => 'B', _ => 'F' };`. Switch statement thì không trả về giá trị, phù hợp khi cần thực thi side effect. Dùng switch expression khi bạn muốn transform một giá trị sang giá trị khác—code gọn và compiler sẽ báo lỗi nếu thiếu case.",
    q_en: "How does a switch expression differ from a switch statement in Dart, and when should you use it?",
    a_en: "Switch expressions return a value directly and require exhaustiveness. Syntax uses `=>`: `String grade = score switch { > 90 => 'A', > 80 => 'B', _ => 'F' };`. Switch statements execute side effects without returning values. Use switch expressions for value transformation — the compiler enforces exhaustiveness, catching missing cases at compile-time.",
  },
  {
    id: 2864,
    category: "Dart",
    subcategory: "Dart Cơ Bản",
    level: "advanced",
    q: "Class modifier trong Dart 3 (`base`, `interface`, `final`) dùng để làm gì?",
    a: '`base`: chỉ cho phép extend, không cho implement—dùng khi muốn chia sẻ logic implementation nhưng kiểm soát interface. `interface`: chỉ cho phép implement, không cho extend—dùng khi muốn định nghĩa contract API. `final`: cấm cả extend lẫn implement—dùng để "đóng" class hoàn toàn. Các modifier này enforce ý định kiến trúc ngay ở compile-time, ngăn việc dùng sai inheritance. \n\n**Ví dụ:** `base class Animal {}` — class khác chỉ có thể extend, không thể implement trực tiếp.',
    q_en: "What do class modifiers (`base`, `interface`, `final`) do in Dart 3?",
    a_en: "`base` allows extending but not implementing; `interface` allows implementing but not extending; `final` forbids both. They enforce architectural intent at compile-time, preventing inheritance misuse. \n\n**Example:** `base class Animal {}` — other classes can only extend it, not implement it directly.",
  },
  {
    id: 2865,
    category: "Dart",
    subcategory: "Dart Cơ Bản",
    level: "advanced",
    q: "Sealed class trong Dart là gì và tại sao nó hữu ích hơn abstract class trong một số trường hợp?",
    a: "Sealed class giới hạn các subclass phải được định nghĩa trong cùng một library, tạo ra tập hợp đóng và biết trước. Khác với abstract class (ai cũng có thể extend từ bất kỳ đâu), sealed class đảm bảo exhaustiveness trong pattern matching—compiler báo lỗi nếu switch thiếu case. \n\n**Ví dụ:** `sealed class Shape {}; class Circle extends Shape {}; class Square extends Shape {};`—switch trên Shape bắt buộc phải handle cả Circle lẫn Square. Rất phù hợp để mô hình hóa sum type như Result, State.",
    q_en: "What is a sealed class in Dart and why is it more useful than an abstract class in some cases?",
    a_en: "Sealed classes restrict subclasses to a known, closed set in the same library. Unlike abstract classes (extendable from anywhere), sealed classes guarantee exhaustiveness in pattern matching — the compiler errors if a switch is missing a case. Perfect for modeling sum types like Result or State.",
  },
  {
    id: 2866,
    category: "Flutter",
    subcategory: "Nâng Cao",
    level: "intermediate",
    q: "Impeller là gì và tại sao Flutter chuyển sang dùng nó làm rendering engine mặc định?",
    a: 'Impeller là rendering engine mới của Flutter, thay thế Skia. Mặc định trên iOS từ Flutter 3.10, và trên Android (API 29+) từ Flutter 3.27. Điểm khác biệt chính: Impeller pre-compile toàn bộ shader lúc build, trong khi Skia compile shader lần đầu tiên khi chạy (JIT), gây ra "shader jank"—giật hình khi người dùng lần đầu chạm vào UI. Impeller dùng Metal trên iOS và Vulkan trên Android, tận dụng GPU API hiện đại. Kết quả: frame time ổn định hơn, không còn giật đột ngột, trải nghiệm người dùng mượt hơn đáng kể.',
    q_en: "What is Impeller and why did Flutter switch to it as the default rendering engine?",
    a_en: 'Impeller is Flutter\'s new rendering engine, replacing Skia. Default on iOS since Flutter 3.10, and on Android (API 29+) since Flutter 3.27. The key difference: Impeller pre-compiles all shaders at build time, while Skia compiled them JIT on first use, causing "shader jank" (sudden stutters on first touch). Impeller uses Metal on iOS and Vulkan on Android for predictable frame times and smoother UX.',
  },
  {
    id: 2867,
    category: "Flutter",
    subcategory: "Flutter Widgets",
    level: "intermediate",
    q: "WebAssembly (WASM) cải thiện hiệu năng Flutter Web như thế nào?",
    a: "Mặc định Flutter Web compile Dart sang JavaScript, phải qua parser và interpreter của trình duyệt. Với WASM, Dart compile ra native bytecode chạy trực tiếp trên VM của browser, bỏ qua parsing overhead. Build bằng `flutter build web --wasm`. Kết quả: tác vụ CPU-intensive chạy nhanh hơn ~1.5–3x tùy workload. Giới hạn: cần trình duyệt hỗ trợ WasmGC (Chrome 119+, Firefox 120+), bundle size lớn hơn JS đáng kể. Đây là bước ngoặt để Flutter Web cạnh tranh với React/Vue về hiệu năng.",
    q_en: "How does WebAssembly (WASM) improve Flutter Web performance?",
    a_en: "By default Flutter Web compiles Dart to JavaScript, which needs parsing and interpretation. With WASM, Dart compiles to native bytecode running directly in the browser VM, skipping parsing overhead. Build with `flutter build web --wasm`. Result: ~1.5–3x faster CPU-intensive tasks depending on workload. \n\n**Note:** initial bundle size is larger than the JS build. Requires WasmGC support (Chrome 119+, Firefox 120+). A major step for Flutter Web performance parity with React/Vue.",
  },
  {
    id: 2868,
    category: "Flutter",
    subcategory: "Nâng Cao",
    level: "beginner",
    q: "Tree-shaking trong Flutter release build là gì và tại sao nó giảm kích thước app?",
    a: "Tree-shaking là quá trình tự động loại bỏ code, class, method không được dùng trong quá trình build. Flutter chỉ giữ lại code reachable từ `main()`. \n\n**Ví dụ:** nếu bạn import một package lớn nhưng chỉ dùng một hàm, chỉ hàm đó được đưa vào app. Tree-shaking chỉ chạy ở release mode (`flutter build apk --release`). Có thể giảm 30-50% kích thước app. Để phân tích xem code nào còn lại, dùng `--analyze-size` hoặc DevTools Size Analyzer.",
    q_en: "What is tree-shaking in Flutter release builds and why does it reduce app size?",
    a_en: "Tree-shaking removes unused classes, methods, and packages during the build, keeping only code reachable from `main()`. Only runs in release mode (`flutter build apk --release`). Can reduce app size by 30-50%. Use `--analyze-size` flag or DevTools Size Analyzer to inspect what remains.",
  },
  {
    id: 2869,
    category: "Flutter",
    subcategory: "Flutter State",
    level: "intermediate",
    q: "Signals trong Flutter là gì và khi nào nên dùng thay vì Riverpod?",
    a: "Signals (xuất hiện cuối 2024) là hệ thống reactive nguyên bản lấy cảm hứng từ SolidJS, cho phép cập nhật UI theo kiểu fine-grained—chỉ widget nào phụ thuộc vào signal đó mới rebuild, không phải cả cây widget. Khai báo: `final count = signal(0);`, dùng trong widget: `Watch((context) => Text('${count.value}'))`. Riverpod phù hợp hơn cho global/shared state phức tạp, cần dependency injection. Signals lý tưởng cho local state performance-critical, ít boilerplate, phạm vi component nhỏ. \n\n**Lưu ý:** Signals còn rất mới, adoption tại Việt Nam còn thấp.",
    q_en: "What are Signals in Flutter and when should you use them over Riverpod?",
    a_en: "Signals (late 2024) are a native reactive system inspired by SolidJS, enabling fine-grained reactivity — only widgets depending on a signal rebuild, not the entire tree. Declare with `signal(0)`, consume with `Watch`. Use Signals for performance-critical, locally-scoped state with minimal boilerplate; use Riverpod for complex global/shared state requiring dependency injection. \n\n**Note:** Signals is still very new with low adoption in Vietnam.",
  },
  {
    id: 2870,
    category: "Flutter",
    subcategory: "Flutter State",
    level: "advanced",
    q: "Riverpod (2.x trở lên) khác Provider như thế nào về bộ nhớ và an toàn kiểu?",
    a: "Provider giữ tất cả instance trong bộ nhớ đến khi bạn xóa thủ công. Riverpod (2.x trở lên) tự động dispose provider khi không còn listener nào—giảm memory ~20-25% ở app lớn. Về type safety: Riverpod dùng code generation (`@riverpod`) tạo provider type-safe, phát hiện lỗi lúc compile thay vì runtime. Provider hay gặp lỗi `ProviderNotFoundException` lúc chạy mà không có cảnh báo sớm. Riverpod còn hỗ trợ `AsyncValue` để handle loading/error state một cách nhất quán.",
    q_en: "How does Riverpod (2.x+) differ from Provider in terms of memory efficiency and type safety?",
    a_en: "Provider keeps all instances alive until manual cleanup. Riverpod (2.x+) auto-disposes unused providers, reducing memory by ~20-25% in large apps. For type safety, Riverpod uses code generation (`@riverpod`) to create compile-time-safe providers, catching errors earlier than Provider's runtime `ProviderNotFoundException`. Riverpod also provides `AsyncValue` for consistent loading/error handling.",
  },
  {
    id: 2871,
    category: "Flutter",
    subcategory: "Flutter State",
    level: "advanced",
    q: "Khi nào nên chọn BLoC thay vì Riverpod để quản lý state?",
    a: "BLoC phù hợp cho team lớn cần kiến trúc event-driven nghiêm ngặt và khả năng audit trail—mọi thay đổi state đều là kết quả của một event rõ ràng, dễ trace khi debug production. Ngành tài chính, y tế thường yêu cầu điều này. Riverpod đơn giản hơn, ít boilerplate hơn, lý tưởng cho hầu hết app consumer. Dùng BLoC khi: team lớn cần quy trình rõ ràng, cần log event để compliance, hoặc đã có codebase BLoC lớn. Riverpod cho phần còn lại.",
    q_en: "When should you choose BLoC over Riverpod for state management?",
    a_en: "Choose BLoC for large enterprise teams needing strict event-driven architecture with audit trails — every state change results from an explicit event, simplifying production debugging. Finance and healthcare often require this. Riverpod is simpler with less boilerplate, ideal for most consumer apps. Use BLoC for compliance-heavy, regulated industries or large teams needing strict traceability.",
  },
  {
    id: 2872,
    category: "Flutter",
    subcategory: "Flutter State",
    level: "advanced",
    q: "Làm thế nào để implement offline persistence với Riverpod?",
    a: "Kết hợp `AsyncNotifier` với local storage (Hive, Isar, hoặc SharedPreferences). Pattern: khi `build()` chạy, đọc cache local trước rồi fetch API—nếu fetch thành công thì cập nhật cache. Dùng `ref.invalidate()` hoặc `ref.refresh()` để force refresh. \n\n**Ví dụ:** `@riverpod class PostsNotifier extends AsyncNotifier<List<Post>> { Future<List<Post>> build() async { final cached = await db.getPosts(); state = AsyncValue.data(cached); final fresh = await api.getPosts(); await db.savePosts(fresh); return fresh; } }`. Kết quả: app show dữ liệu cũ ngay lập tức, cập nhật khi có mạng.",
    q_en: "How do you implement offline persistence with Riverpod?",
    a_en: "Combine `AsyncNotifier` with local storage (Hive, Isar, SharedPreferences). Pattern: in `build()`, read local cache first then fetch the API — on success, update the cache. \n\n**Example:** return cached data immediately via `AsyncValue.data(cached)`, then update state with fresh API data. This gives instant data display with background sync when network is available.",
  },
  {
    id: 2873,
    category: "Flutter",
    subcategory: "Flutter Widgets",
    level: "intermediate",
    q: "Những điểm khác biệt chính khi build Flutter Web so với các framework web truyền thống là gì?",
    a: "Flutter Web compile Dart ra JS (hoặc WASM) và render vào Canvas/HTML, không phải DOM thực. Lợi thế: tái sử dụng code với mobile, không cần học JS ecosystem. \n\n**Nhược điểm:** bundle size ~3MB+ (nặng hơn SPA thông thường), SEO khó vì nội dung trong Canvas, tích hợp với thư viện DOM JS phức tạp hơn. Không dùng Flutter Web cho trang marketing cần SEO tốt. Phù hợp nhất cho app nội bộ, dashboard, hoặc tool cần chia sẻ code với mobile—nơi code reuse quan trọng hơn SEO.",
    q_en: "What are the key differences between building Flutter Web vs traditional web frameworks?",
    a_en: "Flutter Web compiles Dart to JS or WASM and renders to Canvas/HTML, not the real DOM. \n\n**Advantages:** code reuse with mobile, no JS ecosystem friction. \n\n**Disadvantages:** ~3MB+ bundle size, poor SEO (content in Canvas), complex DOM JS library integration. Avoid for SEO-heavy marketing sites. Best for internal apps, dashboards, or tools where mobile code reuse matters more than SEO.",
  },
  {
    id: 2874,
    category: "Flutter",
    subcategory: "Flutter Widgets",
    level: "intermediate",
    q: "Những điểm cần chú ý khi xây dựng Flutter Desktop (Windows, macOS)?",
    a: "Desktop yêu cầu xử lý keyboard, mouse, window management—không có trên mobile. Responsive layout quan trọng hơn vì màn hình lớn và DPI khác nhau. Truy cập native API phải qua platform channel (file system, menu bar, notifications). Packaging khác nhau theo platform: MSIX (Windows), DMG (macOS), tar.gz (Linux). Test trên phần cứng thật vì emulator không phản ánh DPI scaling thực tế. Tính thêm ~30% thời gian phát triển so với mobile do phức tạp hơn về UX.",
    q_en: "What are the platform-specific considerations for Flutter Desktop (Windows/macOS)?",
    a_en: "Desktop requires keyboard/mouse handling, window management, and responsive layouts for larger screens — all absent on mobile. Native API access requires platform channels (file system, menu bar, notifications). Packaging differs per platform: MSIX (Windows), DMG (macOS), tar.gz (Linux). Test on real hardware for accurate DPI scaling. Budget ~30% extra development time vs mobile.",
  },
  {
    id: 2876,
    category: "Flutter",
    subcategory: "Nâng Cao",
    level: "intermediate",
    q: "Dio khác gì với package `http` mặc định và khi nào nên dùng Dio?",
    a: "Package `http` là HTTP client đơn giản, đủ dùng cho request cơ bản. Dio là HTTP client mạnh hơn với: interceptor (middleware để log, thêm token tự động, retry), request/response transformer, quản lý timeout chi tiết, upload file multipart, download file với progress tracking, và cancel token. Kết hợp với Retrofit để tạo type-safe API client từ annotation. Dùng `http` khi app nhỏ, đơn giản. Dùng Dio khi cần interceptor cho auth, logging, hoặc error handling nhất quán trên toàn app.",
    q_en: "How does Dio differ from the default `http` package and when should you use it?",
    a_en: "The `http` package is a simple HTTP client for basic requests. Dio adds interceptors (middleware for logging, auto-adding auth tokens, retries), request/response transformation, file uploads with progress tracking, and cancel tokens. Pair with Retrofit for annotation-based, type-safe API clients. Use `http` for small apps; use Dio when you need auth interceptors, consistent error handling, or file operations.",
  },
  {
    id: 2877,
    category: "Flutter",
    subcategory: "Nâng Cao",
    level: "intermediate",
    q: "Làm thế nào để tích hợp Firebase Authentication vào Flutter và những pitfall phổ biến là gì?",
    a: "Dùng package `firebase_auth`: `await FirebaseAuth.instance.signInWithEmailAndPassword(email: email, password: password)`. Lắng nghe trạng thái auth bằng `authStateChanges()` stream—đây là cách đúng để biết user đã đăng nhập chưa. Pitfall phổ biến: (1) không lắng nghe `authStateChanges()` mà kiểm tra `currentUser` ngay lập tức—có thể null khi app mới mở; (2) quên setup SHA-1 trên Android; (3) quên thêm entitlement trên iOS; (4) không wrap trong try/catch dẫn đến crash khi mất mạng. Luôn test cả trường hợp offline.",
    q_en: "How do you integrate Firebase Authentication in Flutter and what are common pitfalls?",
    a_en: "Use `firebase_auth`: `await FirebaseAuth.instance.signInWithEmailAndPassword(email: email, password: password)`. Listen to auth state via `authStateChanges()` stream — the correct way to know if a user is signed in. Common pitfalls: checking `currentUser` immediately on startup (may be null), missing Android SHA-1 setup, missing iOS entitlements, and not wrapping auth calls in try/catch for network errors.",
  },
  {
    id: 2878,
    category: "Flutter",
    subcategory: "Nâng Cao",
    level: "advanced",
    q: "Shorebird code push là gì và khi nào nên dùng thay vì update thông qua App Store?",
    a: "Shorebird là dịch vụ over-the-air (OTA) cho phép cập nhật code Dart của Flutter mà không cần qua App Store hay Play Store review. Deploy hotfix trong vài phút thay vì chờ 1-3 ngày review. Hỗ trợ A/B testing tính năng, rollback nhanh nếu có bug. Giới hạn: chỉ update được Dart code—không thể thay đổi native plugin, asset, hay Dart VM. Dùng Shorebird cho: bugfix khẩn cấp, copy change, logic update nhỏ. Vẫn dùng App Store cho: major feature, thay đổi UI lớn, cập nhật native code—để đảm bảo version control và rollback đầy đủ.",
    q_en: "What is Shorebird code push and when should you use it instead of App Store/Play Store updates?",
    a_en: "Shorebird is an OTA service that pushes Dart code updates without app store review, delivering fixes in minutes vs 1-3 day review cycles. Supports A/B testing and quick rollback. Limitation: only updates Dart code — cannot change native plugins, assets, or the Dart VM. Use Shorebird for critical hotfixes, copy changes, and small logic updates. Use app stores for major features, large UI changes, and native code updates to maintain full version control.",
  },
];
