import type { QAItem } from "../interview-data";

export const HTML_CSS_DATA: QAItem[] = [
  // === HTML: Cơ Bản (1-15) ===
  {
    id: 1,
    category: "HTML",
    subcategory: "Cơ Bản",
    level: "beginner",
    q: "HTML là gì? Nêu những đặc điểm chính của HTML?",
    a: "HTML (HyperText Markup Language) là ngôn ngữ đánh dấu siêu văn bản dùng để tạo cấu trúc trang web.\n\nĐặc điểm chính:\n- Sử dụng hệ thống thẻ (tags) để cấu trúc nội dung\n- Hỗ trợ đa phương tiện (hình ảnh, video, âm thanh)\n- Độc lập nền tảng (chạy trên mọi trình duyệt)\n- Dễ học và dễ sử dụng",
    q_en: "What is HTML? What are its main characteristics?",
    a_en: "HTML (HyperText Markup Language) is a markup language used to create the structure of web pages.\n\nKey characteristics:\n- Uses a tag-based system\n- Supports multimedia\n- Platform-independent\n- Easy to learn and use",
  },
  {
    id: 2,
    category: "HTML",
    subcategory: "Cơ Bản",
    level: "beginner",
    q: "Sự khác biệt giữa HTML, XHTML và HTML5 là gì?",
    a: "Những điểm khác biệt chính:\n\n- **HTML**: Cú pháp linh hoạt, không bắt buộc đóng một số thẻ.\n- **XHTML**: Dựa trên XML, yêu cầu cú pháp nghiêm ngặt (phải đóng tất cả các thẻ, thuộc tính phải viết thường và có giá trị).\n- **HTML5**: Là phiên bản mới nhất, bổ sung thêm nhiều thẻ mang tính ngữ nghĩa (semantic tags như `<header>`, `<nav>`, `<article>`) và cung cấp nhiều APIs hữu ích (Canvas, Geolocation, Web Storage).",
    q_en: "What are the differences between HTML, XHTML, and HTML5?",
    a_en: "Main differences:\n\n- **HTML**: Has flexible syntax, some tags don't need to be closed.\n- **XHTML**: Based on XML, follows strict rules (all tags must be closed, lowercase only).\n- **HTML5**: The latest version, introduces semantic elements (`<header>`, `<nav>`, `<section>`) and new APIs (Canvas, Geolocation, Web Storage).",
  },
  {
    id: 3,
    category: "HTML",
    subcategory: "Cơ Bản",
    level: "beginner",
    q: "DOCTYPE là gì? Tại sao cần khai báo DOCTYPE?",
    a: "DOCTYPE (Document Type Declaration) khai báo phiên bản HTML đang sử dụng, đặt ở dòng đầu tiên. HTML5 chỉ cần `<!DOCTYPE html>`. Giúp trình duyệt render đúng chuẩn (standards mode). Không có DOCTYPE → quirks mode: box model, CSS parsing khác nhau giữa các browsers, gây lỗi hiển thị.",
    q_en: "What is DOCTYPE? Why is it necessary?",
    a_en: "DOCTYPE (Document Type Declaration) declares the HTML version being used and must be placed on the very first line. HTML5 only needs `<!DOCTYPE html>`. It tells the browser to render in standards mode. Without DOCTYPE, the browser enters quirks mode — causing inconsistent box model and CSS parsing across browsers, leading to display bugs.",
  },
  {
    id: 4,
    category: "HTML",
    subcategory: "Cơ Bản",
    level: "beginner",
    q: "Cấu trúc cơ bản của một tài liệu HTML bao gồm những gì?",
    a: "Cấu trúc cơ bản của tài liệu HTML bao gồm các phần chính sau:\n\n- `<!DOCTYPE html>`: Khai báo loại tài liệu cho trình duyệt\n- `<html>`: Phần tử gốc (root element) bọc toàn bộ trang\n- `<head>`: Chứa siêu dữ liệu (metadata) như `<title>`, `<meta>`, `<link>`, `<style>`\n- `<body>`: Chứa toàn bộ nội dung sẽ hiển thị trên trang web",
    q_en: "What does the basic structure of an HTML document consist of?",
    a_en: "The basic structure consists of:\n\n- `<!DOCTYPE html>`: Declares the document type\n- `<html>`: The root element\n- `<head>`: Contains metadata like `<title>`, `<meta>`, `<link>`, `<script>`\n- `<body>`: Contains the visible page content",
  },
  {
    id: 5,
    category: "HTML",
    subcategory: "Cơ Bản",
    level: "beginner",
    q: "Thẻ `<head>` được sử dụng để làm gì?",
    a: "Chứa metadata của trang web, không hiển thị trực tiếp. Bao gồm: `<title>`, `<meta>` (charset, viewport, description), `<link>` (CSS), `<script>`, `<style>`, và các thẻ Open Graph.",
    q_en: "What is the `<head>` tag used for?",
    a_en: "It holds page metadata that is not directly displayed. This includes: `<title>`, `<meta>` (charset, viewport, description), `<link>` (CSS), `<script>`, `<style>`, and Open Graph tags.",
  },
  {
    id: 6,
    category: "HTML",
    subcategory: "Cơ Bản",
    level: "beginner",
    q: "Thẻ `<body>` được sử dụng để làm gì?",
    a: "Chứa toàn bộ nội dung hiển thị trên trang web: văn bản, hình ảnh, video, form, bảng biểu. Mỗi trang HTML chỉ có một thẻ `<body>`. Body là anh em với `<head>`, cả hai là con của `<html>`. Mọi thứ user nhìn thấy trên trang đều nằm trong body. Lỗi hay gặp: đặt script/link CSS vào body thay vì head làm chậm render.",
    q_en: "What is the `<body>` tag used for?",
    a_en: "It contains all visible page content: text, images, video, forms, tables. Each HTML page has only one `<body>`. Body is a sibling of `<head>`, both children of `<html>`. Everything users see on the page lives inside body. Common mistake: placing scripts/CSS links in body instead of head, which slows rendering.",
  },
  {
    id: 7,
    category: "HTML",
    subcategory: "Cơ Bản",
    level: "beginner",
    q: "Sự khác biệt giữa tag và element trong HTML là gì?",
    a: "Tag là cú pháp đánh dấu (`<p>`, `</p>`). Element là tổng thể gồm thẻ mở + nội dung + thẻ đóng (`<p>Hello</p>`). Một số element tự đóng như `<img>`, `<br>`, `<input>`.",
    q_en: "What is the difference between a tag and an element in HTML?",
    a_en: "A tag is the markup syntax (`<p>`, `</p>`). An element is the complete unit: opening tag + content + closing tag (`<p>Hello</p>`). Some elements are self-closing, like `<img>`, `<br>`, and `<input>`.",
  },
  {
    id: 8,
    category: "HTML",
    subcategory: "Cơ Bản",
    level: "beginner",
    q: "Element block và element inline khác nhau như thế nào?",
    a: "Block element chiếm toàn bộ chiều rộng, bắt đầu dòng mới (`<div>`, `<p>`, `<h1>`). Inline element chỉ chiếm chiều rộng cần thiết (`<span>`, `<a>`, `<strong>`). Block chứa được inline, nhưng thường không ngược lại. Ngoại lệ HTML5: `<a>` có thể bọc block elements.",
    q_en: "How do block-level and inline elements differ?",
    a_en: "Block elements take up the full width and start on a new line (`<div>`, `<p>`, `<h1>`). Inline elements only take up as much width as needed (`<span>`, `<a>`, `<strong>`). Block can contain inline, but not usually the reverse. HTML5 exception: `<a>` can wrap block elements.",
  },
  {
    id: 9,
    category: "HTML",
    subcategory: "Cơ Bản",
    level: "beginner",
    q: "Những block-level element phổ biến có những gì?",
    a: "Các block-level element phổ biến bao gồm: `<div>` (container chung), `<p>` (đoạn văn), `<h1>` đến `<h6>` (tiêu đề), `<ul>`/`<ol>`/`<li>` (danh sách), `<table>` (bảng), `<form>` (biểu mẫu). Ngoài ra còn có các thẻ semantic của HTML5 như `<header>`, `<footer>`, `<section>`, `<article>`, `<nav>`, `<main>`, `<aside>`. Đặc điểm chung của tất cả block-level elements là chúng luôn bắt đầu trên một dòng mới và chiếm toàn bộ chiều rộng có sẵn của phần tử cha.",
    q_en: "What are common block-level elements?",
    a_en: "Common block-level elements include: `<div>` (generic container), `<p>` (paragraph), `<h1>` through `<h6>` (headings), `<ul>`/`<ol>`/`<li>` (lists), `<table>`, and `<form>`. HTML5 semantic elements also include `<header>`, `<footer>`, `<section>`, `<article>`, `<nav>`, `<main>`, `<aside>`. All block-level elements always start on a new line and occupy the full available width of their parent.",
  },
  {
    id: 10,
    category: "HTML",
    subcategory: "Cơ Bản",
    level: "beginner",
    q: "Những inline element phổ biến có những gì?",
    a: "Các inline element phổ biến gồm: `<span>` (container inline chung), `<a>` (liên kết), `<strong>` và `<em>` (nhấn mạnh), `<img>` (hình ảnh), `<input>` và `<button>` (form controls), `<label>` (nhãn form), `<code>` (đoạn mã). Ngoài ra còn có `<br>` (ngắt dòng), `<small>`, `<sub>`, `<sup>`, `<abbr>`, `<cite>`. Đặc điểm chung là chúng không bắt đầu dòng mới, chỉ chiếm đúng chiều rộng cần thiết cho nội dung bên trong, và không thể set width/height trực tiếp (trừ replaced elements như `<img>`, `<input>`).",
    q_en: "What are common inline elements?",
    a_en: "Common inline elements include: `<span>` (generic inline container), `<a>` (link), `<strong>` and `<em>` (emphasis), `<img>`, `<input>` and `<button>` (form controls), `<label>`, `<code>`. Others: `<br>`, `<small>`, `<sub>`, `<sup>`, `<abbr>`, `<cite>`. They do not start a new line, only take up the width needed by their content, and cannot have width/height set directly (except replaced elements like `<img>` and `<input>`).",
  },
  {
    id: 11,
    category: "HTML",
    subcategory: "Cơ Bản",
    level: "beginner",
    q: "Inline-block element là gì?",
    a: "Kết hợp đặc tính inline và block: hiển thị trên cùng dòng như inline nhưng có thể set width/height/margin/padding như block. Dùng CSS `display: inline-block`. Ứng dụng phổ biến: tạo navigation menu ngang, grid layout đơn giản, buttons cạnh nhau. Khác inline: có thể set kích thước. Khác block: không chiếm hết dòng.",
    q_en: "What is an inline-block element?",
    a_en: "It combines inline and block characteristics: it appears on the same line like an inline element but allows setting width/height/margin/padding like a block. Applied with `display: inline-block`. Common uses: horizontal nav menus, simple grid layouts, side-by-side buttons. Unlike inline: can set dimensions. Unlike block: doesn't take up the full line.",
  },
  {
    id: 12,
    category: "HTML",
    subcategory: "Cơ Bản",
    level: "beginner",
    q: "Attribute là gì? Cho ví dụ?",
    a: 'Attribute cung cấp thông tin bổ sung cho HTML element, viết trong thẻ mở theo cú pháp `name="value"`. \n\n**Ví dụ:** `<a href="https://example.com" target="_blank">Link</a>`. Các attribute phổ biến: `href`, `src`, `class`, `id`, `style`, `alt`, `type`, `placeholder`. Một số attribute không cần giá trị (boolean): `disabled`, `checked`, `required`. \n\n**Lưu ý:** attribute name không phân biệt hoa thường nhưng giá trị thì có.',
    q_en: "What is an attribute? Give examples.",
    a_en: 'An attribute provides additional information about an HTML element, written inside the opening tag using the syntax `name="value"`. \n\n**Example:** `<a href="https://example.com" target="_blank">Link</a>`. Common attributes: `href`, `src`, `class`, `id`, `style`, `alt`, `type`, `placeholder`. Some attributes require no value (boolean): `disabled`, `checked`, `required`. \n\n**Note:** attribute names are case-insensitive, but values are.',
  },
  {
    id: 13,
    category: "HTML",
    subcategory: "Cơ Bản",
    level: "beginner",
    q: "Boolean attribute là gì? Cho ví dụ?",
    a: 'Boolean attribute là loại attribute trong HTML chỉ có hai trạng thái: có mặt trong thẻ nghĩa là true, vắng mặt nghĩa là false — giá trị gán cho nó không quan trọng. Ví dụ, viết `<input disabled>` hoặc `<input disabled="">` hoặc `<input disabled="disabled">` đều cho kết quả giống nhau là input bị vô hiệu hóa. Các boolean attribute phổ biến bao gồm `disabled`, `checked`, `selected`, `required`, `readonly`, `autofocus`, `hidden`, `multiple`. Một lỗi rất hay gặp là viết `disabled="false"` với ý định bật lại — nhưng thực tế element vẫn bị disable vì browser chỉ kiểm tra attribute có tồn tại hay không, không quan tâm giá trị.',
    q_en: "What is a boolean attribute? Give examples.",
    a_en: 'A boolean attribute in HTML has two states: present means true, absent means false — the assigned value doesn\'t matter. For example, `<input disabled>`, `<input disabled="">`, and `<input disabled="disabled">` all produce the same result. Common boolean attributes: `disabled`, `checked`, `selected`, `required`, `readonly`, `autofocus`, `hidden`, `multiple`. A common mistake is writing `disabled="false"` to re-enable — but the element stays disabled because the browser only checks for the attribute\'s presence, not its value.',
  },
  {
    id: 14,
    category: "HTML",
    subcategory: "Cơ Bản",
    level: "beginner",
    q: "Custom attribute (data-*) là gì?",
    a: 'Cho phép lưu trữ dữ liệu tùy chỉnh trên HTML element mà không vi phạm HTML spec. Cú pháp: `data-user-id="123"`. Truy cập qua JS bằng `element.dataset.userId` (tự convert kebab-case sang camelCase). Ứng dụng: truyền data từ HTML sang JS, CSS selectors (`[data-active]`), testing (data-testid). React dùng data attributes để pass info cho DOM.',
    q_en: "What are custom data attributes (data-*)?",
    a_en: 'They allow storing custom data on HTML elements without violating the HTML spec. Syntax: `data-user-id="123"`. Access via JS with `element.dataset.userId` (automatically converts kebab-case to camelCase). Uses: passing data from HTML to JS, CSS selectors (`[data-active]`), testing hooks (data-testid). React uses data attributes to pass info to the DOM.',
  },
  {
    id: 15,
    category: "HTML",
    subcategory: "Cơ Bản",
    level: "intermediate",
    q: "Thuộc tính `id` và `class` khác nhau như thế nào?",
    a: "`id` phải duy nhất trên toàn trang, chỉ gán cho 1 element duy nhất — dùng cho anchor links (`#section`) và `getElementById()`. `class` dùng lại được, nhiều element chia sẻ cùng class, 1 element có thể có nhiều class. CSS specificity: id (0,1,0,0) cao hơn class (0,0,1,0), nên tránh dùng id để style vì khó override. Trong React, dùng `htmlFor` thay `for` và `className` thay `class`.",
    q_en: "How do the `id` and `class` attributes differ?",
    a_en: "`id` must be unique across the entire page, assigned to only one element — used for anchor links (`#section`) and `getElementById()`. `class` is reusable; multiple elements can share a class, and one element can have multiple classes. CSS specificity: id (0,1,0,0) is higher than class (0,0,1,0), so avoid using id for styling as it's hard to override. In React, use `htmlFor` instead of `for` and `className` instead of `class`.",
  },

  // === HTML: Semantic (16-30) ===
  {
    id: 16,
    category: "HTML",
    subcategory: "Semantic",
    level: "beginner",
    q: "Semantic HTML là gì? \n\n**Tại sao quan trọng**?",
    a: "Semantic HTML dùng thẻ có ý nghĩa mô tả nội dung (`<article>`, `<nav>`, `<header>`, `<main>`, `<aside>`) thay vì thẻ chung (`<div>`, `<span>`). Quan trọng vì: (1) SEO — search engines hiểu cấu trúc trang tốt hơn. (2) Accessibility — screen readers navigate bằng semantic landmarks. (3) Maintainability — code tự mô tả, đọc hiểu nhanh hơn. (4) Consistency — team dùng cùng conventions.",
    q_en: "What is semantic HTML? Why does it matter?",
    a_en: "Semantic HTML uses tags that describe the meaning of content (`<article>`, `<nav>`, `<header>`, `<main>`, `<aside>`) rather than generic containers (`<div>`, `<span>`). It matters because: (1) SEO — search engines better understand page structure. (2) Accessibility — screen readers navigate using semantic landmarks. (3) Maintainability — self-documenting code is easier to read. (4) Consistency — teams share the same conventions.",
  },
  {
    id: 17,
    category: "HTML",
    subcategory: "Semantic",
    level: "beginner",
    q: "Sự khác biệt giữa `<div>` và `<section>` là gì?",
    a: "`<div>` là container chung không có ý nghĩa ngữ nghĩa — dùng khi chỉ cần nhóm để style hoặc layout. `<section>` đại diện cho phần nội dung có chủ đề liên quan, thường có `<h2>` hoặc heading đi kèm. Screen readers nhận diện `<section>` là landmark region. Ví dụ thực tế: một trang product dùng `<section>` cho phần mô tả, reviews, specs — không dùng `<div>` cho các khối nội dung có ý nghĩa riêng biệt.",
    q_en: "What is the difference between `<div>` and `<section>`?",
    a_en: "`<div>` is a generic container with no semantic meaning — use it when you only need to group elements for styling or layout. `<section>` represents a thematically related block of content, typically accompanied by a heading (`<h2>`). Screen readers recognize `<section>` as a landmark region. Practical example: a product page uses `<section>` for the description, reviews, and specs — not `<div>` for distinct content blocks.",
  },
  {
    id: 18,
    category: "HTML",
    subcategory: "Semantic",
    level: "beginner",
    q: "`<article>` và `<section>` khác nhau thế nào?",
    a: "`<article>` là nội dung độc lập, có thể đứng riêng và vẫn có nghĩa (bài blog, comment, card sản phẩm, tweet). `<section>` là nhóm nội dung liên quan trong cùng tài liệu, cần context của trang. Test nhanh: nếu có thể syndicate nội dung đó ra site khác mà vẫn hiểu được → dùng `<article>`. Article thường có `<header>`, `<footer>`, byline riêng. Article có thể chứa nhiều `<section>` và `<section>` có thể chứa nhiều `<article>`.",
    q_en: "How do `<article>` and `<section>` differ?",
    a_en: "`<article>` is self-contained content that makes sense on its own (blog post, comment, product card, tweet). `<section>` groups related content within the same document and depends on page context. Quick test: if you can syndicate the content to another site and it still makes sense, use `<article>`. Articles usually have their own `<header>`, `<footer>`, and byline. Articles can contain multiple `<section>` elements, and sections can contain multiple `<article>` elements.",
  },
  {
    id: 19,
    category: "HTML",
    subcategory: "Semantic",
    level: "beginner",
    q: "`<header>`, `<footer>`, `<nav>`, `<main>`, `<aside>` dùng khi nào?",
    a: "`<header>`: phần giới thiệu/navigation của trang hoặc section (có thể dùng nhiều lần). `<footer>`: thông tin cuối trang (copyright, liên hệ, links). `<nav>`: navigation links chính — screen readers cho phép nhảy thẳng đến đây. `<main>`: nội dung chính, duy nhất 1 per page, bỏ qua cho screen readers. `<aside>`: nội dung liên quan gián tiếp (sidebar, related posts, ads). \n\n**Lưu ý:** `<header>` và `<footer>` không nhất thiết chỉ dùng ở top/bottom trang — mỗi `<article>` cũng có thể có header riêng.",
    q_en: "When should you use `<header>`, `<footer>`, `<nav>`, `<main>`, `<aside>`?",
    a_en: "`<header>`: introductory content or navigation for a page or section (can be used multiple times). `<footer>`: bottom of page info (copyright, contact, links). `<nav>`: primary navigation links — screen readers can jump directly here. `<main>`: primary page content, only one per page, screen readers can skip to it. `<aside>`: indirectly related content (sidebar, related posts, ads). \n\n**Note:** `<header>` and `<footer>` aren't limited to top/bottom of page — each `<article>` can have its own header.",
  },
  {
    id: 20,
    category: "HTML",
    subcategory: "Semantic",
    level: "beginner",
    q: "`<figure>` và `<figcaption>` dùng để làm gì?",
    a: '`<figure>` nhóm nội dung minh họa có thể tách ra mà không ảnh hưởng luồng văn bản chính: ảnh, biểu đồ, code snippet, video. `<figcaption>` là chú thích mô tả, đặt đầu hoặc cuối `<figure>`. \n\n**Ví dụ:** `<figure><img src="chart.png" alt="..."><figcaption>Biểu đồ doanh thu Q1</figcaption></figure>`. `<figcaption>` thay thế `alt` cho screen readers nên không cần lặp thông tin. Khác với chỉ dùng `<img>`: figure semantically đóng gói ảnh kèm caption.',
    q_en: "What are `<figure>` and `<figcaption>` used for?",
    a_en: '`<figure>` groups illustrative content that can be moved without affecting the main text flow: images, charts, code snippets, videos. `<figcaption>` provides a caption describing the figure, placed at the top or bottom. \n\n**Example:** `<figure><img src="chart.png" alt="..."><figcaption>Q1 Revenue Chart</figcaption></figure>`. `<figcaption>` supplements `alt` for screen readers, so avoid repeating the same information. Compared to using `<img>` alone, figure semantically groups the image with its caption.',
  },
  {
    id: 21,
    category: "HTML",
    subcategory: "Semantic",
    level: "intermediate",
    q: "`<mark>`, `<strong>`, `<em>` khác nhau thế nào?",
    a: "`<mark>` highlight text có liên quan đến context hiện tại — ví dụ kết quả tìm kiếm (nền vàng mặc định). `<strong>` biểu thị tầm quan trọng cao (in đậm) — screen readers có thể đọc mạnh hơn. `<em>` nhấn mạnh ngữ điệu như nói chuyện (in nghiêng). Cả ba có ý nghĩa ngữ nghĩa. Ngược lại, `<b>` và `<i>` chỉ là visual styling không có semantic meaning. Ví dụ đúng: `<strong>Cảnh báo:</strong>` không tắt máy khi đang cập nhật.",
    q_en: "How do `<mark>`, `<strong>`, and `<em>` differ?",
    a_en: "`<mark>` highlights text relevant to the current context — e.g., search results (default yellow background). `<strong>` indicates high importance (bold) — screen readers may read it with stronger emphasis. `<em>` stresses spoken emphasis, like spoken tone (italic). All three carry semantic meaning. In contrast, `<b>` and `<i>` are purely visual with no semantic meaning. Correct usage: `<strong>Warning:</strong>` do not power off during update.",
  },
  {
    id: 22,
    category: "HTML",
    subcategory: "Semantic",
    level: "intermediate",
    q: "Thẻ `<time>` được sử dụng để làm gì?",
    a: "Đánh dấu ngày/giờ theo format machine-readable giúp search engines và screen readers hiểu đúng. Attribute `datetime` chứa giá trị ISO 8601: `<time datetime=\"2024-03-15T09:00\">Thứ Sáu, 15/3/2024</time>`. Quan trọng cho SEO (Google hiểu publication date), calendar events, và tính 'thời gian trước' (3 giờ trước). Nếu không có `datetime`, nội dung text bên trong phải là ngày hợp lệ. Dùng trong blog posts, events, changelogs.",
    q_en: "What is the `<time>` element used for?",
    a_en: "It marks up dates/times in a machine-readable format, helping search engines and screen readers interpret them correctly. The `datetime` attribute holds an ISO 8601 value: `<time datetime=\"2024-03-15T09:00\">Friday, March 15, 2024</time>`. Important for SEO (Google understands publication dates), calendar events, and relative time display ('3 hours ago'). If `datetime` is absent, the text content must be a valid date. Used in blog posts, events, and changelogs.",
  },
  {
    id: 23,
    category: "HTML",
    subcategory: "Semantic",
    level: "intermediate",
    q: "Semantic HTML ảnh hưởng SEO như thế nào?",
    a: 'Semantic HTML giúp search engine crawlers hiểu cấu trúc và nội dung trang chính xác hơn. `<article>` đánh dấu nội dung chính, `<h1>` là keyword chính, `<nav>` thể hiện cấu trúc site. Google dùng structured data từ semantic markup cho rich snippets. Ngoài ra, `<img alt="...">` đóng góp vào image search. Trang semantic thường có bounce rate thấp hơn vì UX tốt hơn — Google tính đây là quality signal. Kết hợp semantic HTML với Schema.org JSON-LD cho kết quả tốt nhất.',
    q_en: "How does semantic HTML affect SEO?",
    a_en: 'Semantic HTML helps search engine crawlers understand the structure and content of a page more accurately. `<article>` marks the main content, `<h1>` identifies the primary keyword, and `<nav>` reflects site structure. Google uses structured data from semantic markup to create rich snippets. `<img alt="...">` also contributes to image search. Pages with good semantics tend to have lower bounce rates due to better UX — a quality signal Google factors in. Combining semantic HTML with Schema.org JSON-LD gives the best results.',
  },
  {
    id: 24,
    category: "HTML",
    subcategory: "Semantic",
    level: "intermediate",
    q: "Khi nào dùng `<dl>`, `<dt>`, `<dd>`?",
    a: "Description list dùng cho cặp thuật ngữ-định nghĩa hoặc key-value pairs. `<dl>` bọc toàn bộ, `<dt>` (term) là thuật ngữ, `<dd>` (description) là giải thích. Một `<dt>` có thể có nhiều `<dd>`. Phù hợp nhất cho: FAQ (câu hỏi là dt, câu trả lời là dd), glossary, metadata (Tác giả: ..., Ngày: ..., Thể loại: ...). Lỗi hay gặp: dùng `<ul>` + `<li>` cho danh sách key-value vì tiện hơn, nhưng `<dl>` semantic và accessible hơn.",
    q_en: "When should you use `<dl>`, `<dt>`, and `<dd>`?",
    a_en: "Description lists are for term-definition pairs or key-value data. `<dl>` wraps the whole list, `<dt>` (term) is the term, `<dd>` (description) is the definition. A single `<dt>` can have multiple `<dd>` elements. Best suited for: FAQ (question as dt, answer as dd), glossaries, metadata (Author: ..., Date: ..., Category: ...). Common mistake: using `<ul>` + `<li>` for key-value lists because it feels simpler — but `<dl>` is more semantic and accessible.",
  },
  {
    id: 25,
    category: "HTML",
    subcategory: "Semantic",
    level: "intermediate",
    q: "Thẻ `<details>` và `<summary>` hoạt động thế nào?",
    a: "`<details>` tạo widget accordion/disclosure native không cần JS. `<summary>` là tiêu đề luôn hiển thị, click vào toggle nội dung bên trong. Thêm attribute `open` để mặc định mở. Trình duyệt tự thêm mũi tên toggle, style bằng `details > summary { list-style: none }` để xóa. \n\n**Ứng dụng thực tế:** FAQ sections, spoiler tags, filter panels. Hữu ích khi muốn accordion đơn giản mà không import thư viện JS. CSS `details[open]` để style khi đang mở.",
    q_en: "How do `<details>` and `<summary>` work?",
    a_en: "`<details>` creates a native accordion/disclosure widget without any JavaScript. `<summary>` is the always-visible title; clicking it toggles the content inside. Add the `open` attribute to expand by default. Browsers add a toggle arrow automatically; style it with `details > summary { list-style: none }` to remove it. Practical uses: FAQ sections, spoiler tags, filter panels. Useful when you want a simple accordion without importing a JS library. Use `details[open]` in CSS to style the expanded state.",
  },

  // === HTML: HTML5 Features (26-40) ===
  {
    id: 26,
    category: "HTML",
    subcategory: "HTML5",
    level: "beginner",
    q: "HTML5 có những tính năng mới nào so với HTML4?",
    a: "HTML5 mang đến nhiều cải tiến lớn so với HTML4, bao gồm các thẻ semantic (`<header>`, `<nav>`, `<article>`, `<section>`) giúp cấu trúc trang rõ ràng hơn, và hỗ trợ đa phương tiện native với `<audio>`/`<video>` mà không cần Flash. Về lưu trữ, HTML5 cung cấp Web Storage (localStorage, sessionStorage) thay thế cookies cho nhiều trường hợp, cùng với IndexedDB cho dữ liệu lớn. Ngoài ra còn có Canvas/SVG cho đồ họa, Geolocation API, Web Workers cho xử lý nền, form validation tích hợp với các input types mới (email, date, number), Drag & Drop API, và WebSocket cho giao tiếp real-time.",
    q_en: "What new features does HTML5 introduce compared to HTML4?",
    a_en: "HTML5 brings major improvements including semantic elements (`<header>`, `<nav>`, `<article>`, `<section>`) for clearer page structure, and native multimedia support with `<audio>`/`<video>` without Flash. For storage, HTML5 provides Web Storage (localStorage, sessionStorage) as a replacement for cookies in many cases, along with IndexedDB for larger data. It also includes Canvas/SVG for graphics, the Geolocation API, Web Workers for background processing, built-in form validation with new input types (email, date, number), Drag & Drop API, and WebSocket for real-time communication.",
  },
  {
    id: 27,
    category: "HTML",
    subcategory: "HTML5",
    level: "beginner",
    q: "Canvas và SVG khác nhau thế nào?",
    a: "Canvas là pixel-based (raster), vẽ bằng JavaScript, tốt cho game/animation phức tạp. SVG là vector-based (XML), scale không mất chất lượng, tốt cho icon/chart/logo. SVG accessible hơn vì là DOM elements.",
    q_en: "How do Canvas and SVG differ?",
    a_en: "Canvas is pixel-based (raster), drawn using JavaScript, best for complex games and animations. SVG is vector-based (XML), scales without quality loss, best for icons, charts, and logos. SVG is more accessible because it consists of DOM elements.",
  },
  {
    id: 28,
    category: "HTML",
    subcategory: "HTML5",
    level: "intermediate",
    q: "Local Storage và Session Storage khác nhau thế nào?",
    a: "Local Storage lưu vĩnh viễn (cho đến khi xóa thủ công), ~5MB per origin. Session Storage chỉ tồn tại trong phiên (đóng tab = mất). Cả hai lưu key-value dạng string, không gửi lên server như cookies.",
    q_en: "How do localStorage and sessionStorage differ?",
    a_en: "localStorage persists permanently (until manually cleared), ~5MB per origin. sessionStorage lasts only for the session (closing the tab clears it). Both store key-value pairs as strings and are not sent to the server like cookies.",
  },
  {
    id: 29,
    category: "HTML",
    subcategory: "HTML5",
    level: "intermediate",
    q: "Web Worker là gì?",
    a: "Web Worker chạy JavaScript trong background thread riêng biệt, không block UI thread chính. Dùng cho tác vụ nặng: parse CSV lớn, image processing, crypto calculations. Giao tiếp 2 chiều qua `postMessage()` và lắng nghe `message` event. Giới hạn: không truy cập DOM, window, document. Ví dụ thực tế: Figma dùng Workers để render canvas mà không đơ UI. 3 loại: Dedicated Worker (1-1), Shared Worker (nhiều tabs), Service Worker (proxy network).",
    q_en: "What is a Web Worker?",
    a_en: "A Web Worker runs JavaScript in a separate background thread, preventing it from blocking the main UI thread. Used for heavy tasks: parsing large CSVs, image processing, crypto calculations. Communicates bidirectionally via `postMessage()` and listening to `message` events. Limitations: no access to DOM, window, or document. Real-world example: Figma uses Workers to render the canvas without freezing the UI. 3 types: Dedicated Worker (1-to-1), Shared Worker (multiple tabs), Service Worker (network proxy).",
  },
  {
    id: 30,
    category: "HTML",
    subcategory: "HTML5",
    level: "intermediate",
    q: "Service Worker là gì? Khác Web Worker thế nào?",
    a: "Service Worker là proxy giữa browser và network, cho phép offline caching, push notifications, background sync. Khác Web Worker: Service Worker persist qua sessions, can thiệp network requests, dùng cho PWA.",
    q_en: "What is a Service Worker? How does it differ from a Web Worker?",
    a_en: "A Service Worker acts as a proxy between the browser and the network, enabling offline caching, push notifications, and background sync. Unlike Web Workers: Service Workers persist across sessions, can intercept network requests, and are used for PWAs.",
  },
  {
    id: 31,
    category: "HTML",
    subcategory: "HTML5",
    level: "intermediate",
    q: "Progressive Web App (PWA) là gì?",
    a: "Ứng dụng web có trải nghiệm gần native app: offline, installable, push notifications. Yêu cầu: HTTPS, Service Worker, Web App Manifest. Dùng manifest.json khai báo icon, theme, start_url.",
    q_en: "What is a Progressive Web App (PWA)?",
    a_en: "A web application with an experience close to a native app: offline support, installability, push notifications. Requirements: HTTPS, Service Worker, Web App Manifest. Use manifest.json to declare icons, theme, and start_url.",
  },
  {
    id: 32,
    category: "HTML",
    subcategory: "HTML5",
    level: "intermediate",
    q: "IndexedDB là gì? Khi nào nên dùng?",
    a: "Database NoSQL trong browser, lưu trữ lượng lớn structured data (vượt 5MB của localStorage). API async, hỗ trợ index, transactions. Dùng cho offline-first apps, caching lớn, phức tạp hơn localStorage.",
    q_en: "What is IndexedDB? When should you use it?",
    a_en: "A NoSQL database in the browser for storing large amounts of structured data (beyond localStorage's 5MB limit). The API is async and supports indexes and transactions. Use for offline-first apps and complex caching scenarios that are too large or complex for localStorage.",
  },
  {
    id: 33,
    category: "HTML",
    subcategory: "HTML5",
    level: "advanced",
    q: "Geolocation API hoạt động thế nào?",
    a: "Truy cập vị trí thiết bị qua `navigator.geolocation.getCurrentPosition()`. Yêu cầu permission từ user. Trả về latitude/longitude/accuracy. Có `watchPosition()` theo dõi liên tục. Chỉ hoạt động trên HTTPS.",
    q_en: "How does the Geolocation API work?",
    a_en: "Accesses the device's location via `navigator.geolocation.getCurrentPosition()`. Requires user permission. Returns latitude, longitude, and accuracy. Use `watchPosition()` to track movement continuously. Only works over HTTPS.",
  },
  {
    id: 34,
    category: "HTML",
    subcategory: "HTML5",
    level: "advanced",
    q: "History API dùng để làm gì?",
    a: "Cho phép thao tác browser history mà không reload trang. `pushState()` thêm entry mới, `replaceState()` thay entry hiện tại, `popstate` event lắng nghe back/forward. Nền tảng cho SPA routing.",
    q_en: "What is the History API used for?",
    a_en: "Allows manipulating the browser history without reloading the page. `pushState()` adds a new entry, `replaceState()` replaces the current one, and the `popstate` event listens for back/forward navigation. It is the foundation of SPA routing.",
  },
  {
    id: 35,
    category: "HTML",
    subcategory: "HTML5",
    level: "advanced",
    q: "WebSocket khác HTTP thế nào?",
    a: "HTTP hoạt động theo mô hình request-response, nghĩa là client phải gửi yêu cầu trước thì server mới trả về dữ liệu, và mỗi request tạo một connection mới. WebSocket ngược lại, sau khi handshake ban đầu qua HTTP và upgrade lên giao thức WebSocket, nó duy trì một persistent connection full-duplex — cả client và server đều có thể gửi dữ liệu bất cứ lúc nào mà không cần chờ request. WebSocket phù hợp cho các ứng dụng cần real-time như chat, live notifications, collaborative editing, multiplayer games, và live data feeds (giá cổ phiếu, sports scores).",
    q_en: "How does WebSocket differ from HTTP?",
    a_en: "HTTP follows a request-response model — the client must send a request before the server can respond, and each request creates a new connection. WebSocket, after an initial HTTP handshake and protocol upgrade, maintains a persistent full-duplex connection — both client and server can send data at any time without waiting for a request. WebSocket is ideal for real-time applications like chat, live notifications, collaborative editing, multiplayer games, and live data feeds (stock prices, sports scores).",
  },
  {
    id: 36,
    category: "HTML",
    subcategory: "HTML5",
    level: "advanced",
    q: "Intersection Observer API là gì?",
    a: "API async theo dõi khi element xuất hiện/biến mất khỏi viewport. Thay thế scroll event listener (tốn performance). Dùng cho lazy loading images, infinite scroll, trigger animations khi element visible.",
    q_en: "What is the Intersection Observer API?",
    a_en: "An async API that watches when an element enters or leaves the viewport. Replaces scroll event listeners (which are expensive on performance). Used for lazy loading images, infinite scroll, and triggering animations when elements become visible.",
  },
  {
    id: 37,
    category: "HTML",
    subcategory: "HTML5",
    level: "advanced",
    q: "MutationObserver là gì?",
    a: "API theo dõi thay đổi DOM (thêm/xóa nodes, thay đổi attributes, text content). Thay thế Mutation Events (deprecated). Dùng cho tracking DOM changes, auto-formatting, accessibility monitoring.",
    q_en: "What is MutationObserver?",
    a_en: "An API that monitors DOM changes (adding/removing nodes, attribute changes, text content updates). Replaces Mutation Events (deprecated). Used for tracking DOM changes, auto-formatting, and accessibility monitoring.",
  },
  {
    id: 38,
    category: "HTML",
    subcategory: "HTML5",
    level: "advanced",
    q: "ResizeObserver là gì?",
    a: "API theo dõi khi element thay đổi kích thước. Hiệu quả hơn window resize event vì target specific elements. Dùng cho responsive components, chart resizing, dynamic layouts không phụ thuộc media queries.",
    q_en: "What is ResizeObserver?",
    a_en: "An API that watches when an element changes size. More efficient than the window resize event because it targets specific elements. Used for responsive components, chart resizing, and dynamic layouts that don't rely on media queries.",
  },
  {
    id: 39,
    category: "HTML",
    subcategory: "HTML5",
    level: "advanced",
    q: "Drag and Drop API hoạt động thế nào?",
    a: "Cho phép kéo-thả elements native. Events: `dragstart`, `drag`, `dragover`, `drop`, `dragend`. Dùng `dataTransfer` object để truyền data. Cần `e.preventDefault()` trên `dragover` để cho phép drop.",
    q_en: "How does the Drag and Drop API work?",
    a_en: "Enables native drag-and-drop behavior. Events: `dragstart`, `drag`, `dragover`, `drop`, `dragend`. Use the `dataTransfer` object to pass data. You must call `e.preventDefault()` on `dragover` to allow dropping.",
  },
  {
    id: 40,
    category: "HTML",
    subcategory: "HTML5",
    level: "advanced",
    q: "Web Components gồm những công nghệ nào?",
    a: "Gồm 3 công nghệ: Custom Elements (tạo thẻ HTML tùy chỉnh), Shadow DOM (encapsulate styles/markup), HTML Templates (`<template>` và `<slot>`). Cho phép tạo reusable components không phụ thuộc framework.",
    q_en: "What technologies make up Web Components?",
    a_en: "Three technologies: Custom Elements (create custom HTML tags), Shadow DOM (encapsulate styles and markup), and HTML Templates (`<template>` and `<slot>`). Together they enable creating reusable components independent of any framework.",
  },

  // === HTML: Forms (41-60) ===
  {
    id: 41,
    category: "HTML",
    subcategory: "Forms",
    level: "beginner",
    q: "Method GET và POST khác nhau thế nào?",
    a: "GET gửi dữ liệu qua URL (query string), giới hạn kích thước, có thể bookmark. POST gửi trong body request, không giới hạn kích thước. \n\n**Lưu ý:** POST không tự bảo mật hơn GET — cần HTTPS để bảo vệ dữ liệu. GET cho truy vấn, POST cho dữ liệu lớn/thay đổi state.",
    q_en: "How do GET and POST methods differ?",
    a_en: "GET sends data via the URL (query string), has a size limit, and is bookmarkable. POST sends data in the request body, has no size limit. \n\n**Note:** POST is not inherently more secure than GET — HTTPS is needed to protect data in both cases. Use GET for queries, POST for large data or state-changing operations.",
  },
  {
    id: 42,
    category: "HTML",
    subcategory: "Forms",
    level: "beginner",
    q: "Các input types trong HTML5 có những gì?",
    a: "text, password, email, number, tel, url, date, time, datetime-local, month, week, color, range, file, checkbox, radio, hidden, search, submit, reset, button. Mỗi type có validation và UI riêng.",
    q_en: "What input types does HTML5 provide?",
    a_en: "text, password, email, number, tel, url, date, time, datetime-local, month, week, color, range, file, checkbox, radio, hidden, search, submit, reset, button. Each type has its own built-in validation and UI.",
  },
  {
    id: 43,
    category: "HTML",
    subcategory: "Forms",
    level: "beginner",
    q: "`<label>` element quan trọng thế nào?",
    a: '`<label>` liên kết mô tả với form control theo 2 cách: dùng `for` khớp với `id` của input (`<label for="email">Email</label><input id="email">`), hoặc wrapping trực tiếp. Click vào label = focus/check input — tăng click area đáng kể trên mobile. Screen readers đọc label khi user focus vào input. Lỗi phổ biến: chỉ dùng placeholder thay label — placeholder biến mất khi gõ, không accessible. Placeholder nên là gợi ý, không phải label.',
    q_en: "Why is the `<label>` element important?",
    a_en: '`<label>` associates a description with a form control in two ways: using `for` matching an input\'s `id` (`<label for="email">Email</label><input id="email">`), or by wrapping the input directly. Clicking the label focuses or checks the input — significantly increasing the tap target on mobile. Screen readers read the label when the user focuses on the input. Common mistake: using only a placeholder instead of a label — placeholder disappears when typing and is not accessible. Placeholder should be a hint, not a label replacement.',
  },
  {
    id: 44,
    category: "HTML",
    subcategory: "Forms",
    level: "beginner",
    q: "`<select>` và `<datalist>` khác nhau thế nào?",
    a: "`<select>` là dropdown cho phép chọn từ danh sách cố định — user không thể nhập giá trị ngoài list. `<datalist>` gắn vào `<input>` qua `list` attribute, cung cấp gợi ý tự động điền nhưng user vẫn gõ giá trị tùy ý. Ví dụ datalist: ô tìm kiếm gợi ý nhưng cho phép search từ khóa mới. Dùng `<select>` khi giá trị phải thuộc tập hợp cố định (quốc gia, danh mục). Dùng `<datalist>` khi muốn gợi ý nhưng không giới hạn input.",
    q_en: "How do `<select>` and `<datalist>` differ?",
    a_en: "`<select>` is a dropdown that allows choosing from a fixed list — users cannot type a value outside the list. `<datalist>` attaches to an `<input>` via the `list` attribute, providing autocomplete suggestions while still allowing arbitrary input. \n\n**Example:** a search box that suggests terms but lets you search anything new. Use `<select>` when the value must come from a fixed set (country, category). Use `<datalist>` when you want suggestions but don't want to restrict input.",
  },
  {
    id: 45,
    category: "HTML",
    subcategory: "Forms",
    level: "beginner",
    q: "`<fieldset>` và `<legend>` dùng để làm gì?",
    a: "`<fieldset>` nhóm các form controls có liên quan nhau, tạo border visual và semantic grouping. `<legend>` là tiêu đề mô tả cho nhóm đó. Đặc biệt quan trọng cho nhóm radio/checkbox: screen readers đọc legend trước mỗi option (ví dụ: 'Giới tính: Nam / Nữ'). Có thể dùng `disabled` trên `<fieldset>` để disable toàn bộ group inputs bên trong. Ứng dụng: form đăng ký nhiều section (Thông tin cá nhân, Địa chỉ, Thanh toán).",
    q_en: "What are `<fieldset>` and `<legend>` used for?",
    a_en: "`<fieldset>` groups related form controls, adding a visual border and semantic grouping. `<legend>` is the descriptive title for the group. Especially important for radio/checkbox groups: screen readers read the legend before each option (e.g., 'Gender: Male / Female'). You can apply `disabled` to a `<fieldset>` to disable all inputs inside it at once. Use case: multi-section registration forms (Personal Info, Address, Payment).",
  },
  {
    id: 46,
    category: "HTML",
    subcategory: "Forms",
    level: "intermediate",
    q: "HTML5 form validation có những attribute nào?",
    a: "`required` (bắt buộc), `pattern` (regex), `min`/`max` (số/ngày), `minlength`/`maxlength` (chuỗi), `step` (bước nhảy). Input type email/url tự validate format. Dùng `:valid`/`:invalid` CSS pseudo-classes.",
    q_en: "What validation attributes does HTML5 provide for forms?",
    a_en: "`required` (mandatory), `pattern` (regex), `min`/`max` (number/date), `minlength`/`maxlength` (string), `step` (increment). Input types like email and url automatically validate format. Use `:valid`/`:invalid` CSS pseudo-classes for visual feedback.",
  },
  {
    id: 47,
    category: "HTML",
    subcategory: "Forms",
    level: "intermediate",
    q: "`readonly` và `disabled` khác nhau thế nào?",
    a: "`readonly`: user không thể chỉnh sửa nhưng vẫn focus, copy, và giá trị vẫn được submit cùng form. `disabled`: không tương tác được, không focusable, giá trị KHÔNG submit (bị loại khỏi form data). Visual: disabled làm mờ element, readonly giữ nguyên appearance. Dùng `readonly` khi muốn hiển thị giá trị nhưng vẫn cần submit (số tham chiếu tự động). Dùng `disabled` khi field không áp dụng và không cần gửi lên server. \n\n**Lưu ý:** `readonly` chỉ hoạt động với input/textarea, không với select.",
    q_en: "How do `readonly` and `disabled` differ?",
    a_en: "`readonly`: the user cannot edit but can still focus, copy, and the value is submitted with the form. `disabled`: no interaction allowed, not focusable, and the value is NOT submitted (excluded from form data). Visually: disabled grays out the element, readonly keeps its normal appearance. Use `readonly` when you want to display a value but still need to submit it (auto-generated reference numbers). Use `disabled` when the field doesn't apply and should not be sent. \n\n**Note:** `readonly` only works with input/textarea, not select.",
  },
  {
    id: 48,
    category: "HTML",
    subcategory: "Forms",
    level: "intermediate",
    q: "Enctype attribute có những giá trị nào?",
    a: "`application/x-www-form-urlencoded` (mặc định, encode key=value). `multipart/form-data` (bắt buộc khi upload file). `text/plain` (không encode, dùng cho debug). Upload file phải dùng multipart.",
    q_en: "What values does the enctype attribute accept?",
    a_en: "`application/x-www-form-urlencoded` (default, encodes key=value pairs). `multipart/form-data` (required for file uploads). `text/plain` (no encoding, for debugging only). File uploads must use multipart.",
  },
  {
    id: 49,
    category: "HTML",
    subcategory: "Forms",
    level: "intermediate",
    q: "Autocomplete attribute hoạt động thế nào?",
    a: "Gợi ý trình duyệt tự điền form. Values: `on`/`off`, hoặc cụ thể: `name`, `email`, `tel`, `address-line1`, `cc-number`. Cải thiện UX, giảm nhập liệu. Có thể set trên form hoặc từng input.",
    q_en: "How does the autocomplete attribute work?",
    a_en: "It hints to the browser to autofill form fields. Values: `on`/`off`, or specific tokens: `name`, `email`, `tel`, `address-line1`, `cc-number`. Improves UX by reducing manual input. Can be set on the form element or individual inputs.",
  },
  {
    id: 50,
    category: "HTML",
    subcategory: "Forms",
    level: "intermediate",
    q: "Constraint Validation API là gì?",
    a: `API JavaScript kiểm tra và tùy chỉnh form validation.

- Methods: \`checkValidity()\`, \`reportValidity()\`, \`setCustomValidity()\`
- Properties: \`validity.valueMissing\`, \`validity.typeMismatch\`
- Override message lỗi mặc định của browser.`,
    q_en: "What is the Constraint Validation API?",
    a_en: `A JavaScript API for checking and customizing form validation.

- Methods: \`checkValidity()\`, \`reportValidity()\`, \`setCustomValidity()\`
- Properties: \`validity.valueMissing\`, \`validity.typeMismatch\`
- Allows overriding the browser's default error messages.`,
  },
  {
    id: 51,
    category: "HTML",
    subcategory: "Forms",
    level: "intermediate",
    q: "FormData API dùng để làm gì?",
    a: "Thu thập dữ liệu form dạng key-value pairs. `new FormData(formElement)` tự động lấy tất cả fields. Hỗ trợ file upload. Dùng với `fetch()` để submit form bằng JavaScript. Methods: `append()`, `get()`, `entries()`.",
    q_en: "What is the FormData API used for?",
    a_en: "Collects form data as key-value pairs. `new FormData(formElement)` automatically captures all fields. Supports file uploads. Used with `fetch()` to submit forms via JavaScript. Methods: `append()`, `get()`, `entries()`.",
  },
  {
    id: 52,
    category: "HTML",
    subcategory: "Forms",
    level: "advanced",
    q: "`<input type='file'>` có những tính năng gì?",
    a: "`accept` giới hạn file type (`.jpg,.png`, `image/*`). `multiple` cho phép chọn nhiều file. `capture` mở camera trên mobile. Truy cập files qua `input.files` (FileList). Dùng FileReader để đọc content.",
    q_en: "What features does `<input type='file'>` support?",
    a_en: "`accept` restricts file types (`.jpg,.png`, `image/*`). `multiple` allows selecting several files at once. `capture` opens the camera on mobile. Access selected files via `input.files` (FileList). Use FileReader to read file contents.",
  },
  {
    id: 53,
    category: "HTML",
    subcategory: "Forms",
    level: "advanced",
    q: "Làm thế nào để tạo custom form validation?",
    a: "Dùng `setCustomValidity(message)` trên input element. Message rỗng = valid. Kết hợp `pattern` attribute với regex, lắng nghe `invalid` event. Có thể dùng `novalidate` trên form rồi validate bằng JS.",
    q_en: "How do you create custom form validation?",
    a_en: "Use `setCustomValidity(message)` on the input element. An empty message means valid. Combine with the `pattern` attribute and regex, and listen to the `invalid` event. You can also add `novalidate` to the form and handle all validation manually in JavaScript.",
  },
  {
    id: 54,
    category: "HTML",
    subcategory: "Forms",
    level: "advanced",
    q: "Content Editable là gì?",
    a: '`contenteditable="true"` biến bất kỳ element nào thành editable. Dùng cho rich text editors. Kết hợp `document.execCommand()` (deprecated) hoặc Selection/Range API. Khó kiểm soát output HTML.',
    q_en: "What is contentEditable?",
    a_en: '`contenteditable="true"` turns any element into an editable region. Used for rich text editors. Combine with `document.execCommand()` (deprecated) or the Selection/Range API. The HTML output can be difficult to control.',
  },
  {
    id: 55,
    category: "HTML",
    subcategory: "Forms",
    level: "advanced",
    q: "Làm sao tối ưu form cho mobile?",
    a: "Dùng đúng input type (email, tel, number) để hiện đúng keyboard. Set `inputmode` attribute. Dùng `autocomplete` hints. Touch-friendly size (min 44x44px). Label rõ ràng. Tránh hover-only interactions.",
    q_en: "How do you optimize a form for mobile?",
    a_en: "Use the correct input type (email, tel, number) to trigger the appropriate keyboard. Set the `inputmode` attribute. Provide `autocomplete` hints. Ensure touch-friendly size (min 44x44px). Use clear labels. Avoid hover-only interactions.",
  },

  // === HTML: Accessibility (56-70) ===
  {
    id: 56,
    category: "HTML",
    subcategory: "Accessibility",
    level: "beginner",
    q: "Accessibility (a11y) là gì? \n\n**Tại sao quan trọng**?",
    a: "Accessibility (a11y — viết tắt 11 chữ cái giữa) là khả năng web được dùng bởi mọi người, kể cả người khuyết tật (thị giác, thính giác, vận động, nhận thức). Quan trọng vì: (1) Đạo đức — ~15% dân số thế giới có dạng khuyết tật nào đó. (2) Luật pháp — ADA (Mỹ), WCAG 2.1 AA là yêu cầu pháp lý ở nhiều nơi. (3) Business — site accessible rank SEO tốt hơn, reach rộng hơn. (4) UX — closed captions giúp cả người xem không tiếng ồn; good contrast giúp mọi người dưới nắng.",
    q_en: "What is accessibility (a11y)? Why does it matter?",
    a_en: "Accessibility (a11y — abbreviating the 11 letters in between) is the practice of making the web usable by everyone, including people with disabilities (visual, auditory, motor, cognitive). It matters because: (1) Ethics — ~15% of the world's population has some form of disability. (2) Law — ADA (US), WCAG 2.1 AA are legal requirements in many regions. (3) Business — accessible sites rank better in SEO and reach a wider audience. (4) UX — closed captions help all users in noisy environments; good contrast helps everyone in bright sunlight.",
  },
  {
    id: 57,
    category: "HTML",
    subcategory: "Accessibility",
    level: "beginner",
    q: "ARIA là gì?",
    a: 'ARIA (Accessible Rich Internet Applications) là tập hợp attributes bổ sung accessibility cho HTML khi semantic không đủ. 3 loại: (1) Roles — xác định vai trò (`role="button"`, `role="dialog"`). (2) States — trạng thái (`aria-expanded="true"`, `aria-disabled`). (3) Properties — thuộc tính (`aria-label`, `aria-describedby`). Nguyên tắc: dùng native HTML trước, ARIA là giải pháp cuối cùng.',
    q_en: "What is ARIA?",
    a_en: 'ARIA (Accessible Rich Internet Applications) is a set of attributes that add accessibility to HTML when native semantics are insufficient. 3 categories: (1) Roles — define purpose (`role="button"`, `role="dialog"`). (2) States — current state (`aria-expanded="true"`, `aria-disabled`). (3) Properties — characteristics (`aria-label`, `aria-describedby`). Rule: use native HTML first; ARIA is the last resort.',
  },
  {
    id: 58,
    category: "HTML",
    subcategory: "Accessibility",
    level: "beginner",
    q: "Alt text quan trọng thế nào?",
    a: 'Alt text mô tả hình ảnh cho screen readers và hiện khi ảnh không load được. 3 trường hợp: ảnh mang thông tin — mô tả ngắn gọn nội dung/mục đích (không bắt đầu bằng \'Hình ảnh của...\'); ảnh là link — mô tả đích đến; ảnh decorative thuần túy — `alt=""` để screen reader bỏ qua. Quan trọng cho SEO: Google Images index dựa vào alt text. Lỗi phổ biến: `alt="image123.jpg"` (filename), `alt="logo"` quá chung chung, hoặc bỏ qua `alt` hoàn toàn.',
    q_en: "Why is alt text important?",
    a_en: 'Alt text describes images for screen readers and appears when images fail to load. Three cases: informational image — briefly describe content/purpose (don\'t start with \'Image of...\'); linked image — describe the destination; purely decorative image — use `alt=""` so screen readers skip it. SEO: Google Images indexes based on alt text. Common mistakes: `alt="image123.jpg"` (filename), `alt="logo"` (too vague), or omitting `alt` entirely.',
  },
  {
    id: 59,
    category: "HTML",
    subcategory: "Accessibility",
    level: "intermediate",
    q: "WCAG là gì? Có bao nhiêu cấp độ?",
    a: "Web Content Accessibility Guidelines - tiêu chuẩn quốc tế về accessibility. 3 cấp: A (cơ bản), AA (khuyến nghị, yêu cầu pháp lý), AAA (cao nhất). 4 nguyên tắc: Perceivable, Operable, Understandable, Robust.",
    q_en: "What is WCAG? How many conformance levels are there?",
    a_en: "Web Content Accessibility Guidelines — the international standard for web accessibility. 3 levels: A (basic), AA (recommended, often a legal requirement), AAA (highest). 4 principles: Perceivable, Operable, Understandable, Robust.",
  },
  {
    id: 60,
    category: "HTML",
    subcategory: "Accessibility",
    level: "intermediate",
    q: "aria-label, aria-labelledby, aria-describedby khác nhau?",
    a: "`aria-label` gán label trực tiếp bằng string. `aria-labelledby` reference đến id của element chứa label. `aria-describedby` thêm mô tả bổ sung. labelledby override aria-label. describedby là thông tin phụ.",
    q_en: "How do aria-label, aria-labelledby, and aria-describedby differ?",
    a_en: "`aria-label` assigns a label directly as a string. `aria-labelledby` references the id of an element containing the label. `aria-describedby` adds supplementary description. `aria-labelledby` overrides `aria-label`. `aria-describedby` provides secondary information.",
  },
  {
    id: 61,
    category: "HTML",
    subcategory: "Accessibility",
    level: "intermediate",
    q: "Keyboard navigation quan trọng thế nào?",
    a: "Hàng triệu users chỉ dùng keyboard: người khuyết tật vận động, người dùng screen readers, power users. Mọi interactive element phải focusable và operable bằng keyboard. Phím chuẩn: Tab/Shift+Tab navigate, Enter/Space activate buttons, Escape đóng modals, mũi tên trong widget (menu, slider). Focus indicator (outline) phải luôn hiển thị — đừng `outline: none` mà không có alternative. Test nhanh: unplug chuột và thử dùng toàn bộ tính năng chỉ bằng keyboard.",
    q_en: "Why is keyboard navigation important?",
    a_en: "Millions of users rely only on keyboards: people with motor disabilities, screen reader users, and power users. Every interactive element must be focusable and operable by keyboard. Standard keys: Tab/Shift+Tab to navigate, Enter/Space to activate buttons, Escape to close modals, arrow keys inside widgets (menus, sliders). Focus indicators (outline) must always be visible — never `outline: none` without a visual alternative. Quick test: unplug your mouse and try using every feature with keyboard alone.",
  },
  {
    id: 62,
    category: "HTML",
    subcategory: "Accessibility",
    level: "intermediate",
    q: "tabindex attribute hoạt động thế nào?",
    a: '`tabindex="0"`: thêm element vào tab order tự nhiên. `tabindex="-1"`: focusable bằng JS nhưng không tab được. `tabindex > 0`: đặt thứ tự ưu tiên (tránh dùng, gây confuse). Mặc định: links, buttons, inputs đã focusable.',
    q_en: "How does the tabindex attribute work?",
    a_en: '`tabindex="0"`: adds an element to the natural tab order. `tabindex="-1"`: focusable via JavaScript but not reachable by Tab. `tabindex > 0`: sets explicit priority order (avoid — confuses users). By default: links, buttons, and inputs are already focusable.',
  },
  {
    id: 63,
    category: "HTML",
    subcategory: "Accessibility",
    level: "intermediate",
    q: "Color contrast yêu cầu gì theo WCAG?",
    a: "WCAG AA (tiêu chuẩn khuyến nghị): text thường cần tỷ lệ contrast tối thiểu 4.5:1, text lớn (18px+ hoặc 14px+ bold) cần 3:1. WCAG AAA: 7:1 và 4.5:1. Contrast ratio tính giữa màu text và màu nền. \n\n**Ví dụ:** chữ xám nhạt #999 trên nền trắng chỉ đạt ~2.85:1 — fail AA. Tools kiểm tra: Lighthouse DevTools, WebAIM Contrast Checker, axe DevTools extension. \n\n**Lưu ý:** không dùng màu là cách duy nhất truyền thông tin (ví dụ: chỉ dùng màu đỏ/xanh cho lỗi/thành công).",
    q_en: "What does WCAG require for color contrast?",
    a_en: "WCAG AA (recommended standard): regular text needs a minimum contrast ratio of 4.5:1; large text (18px+ or 14px+ bold) needs 3:1. WCAG AAA: 7:1 and 4.5:1. Contrast ratio is calculated between text color and background. \n\n**Example:** light gray #999 on white is only ~2.85:1 — fails AA. Tools: Lighthouse DevTools, WebAIM Contrast Checker, axe DevTools. \n\n**Note:** don't use color as the only way to convey information (e.g., only red/green for error/success).",
  },
  {
    id: 64,
    category: "HTML",
    subcategory: "Accessibility",
    level: "advanced",
    q: "aria-live dùng khi nào?",
    a: "Thông báo cho screen readers về nội dung thay đổi động. `polite`: đợi user idle rồi đọc. `assertive`: ngắt ngay để đọc. Dùng cho notifications, chat messages, live search results, form errors.",
    q_en: "When should you use aria-live?",
    a_en: "Announces dynamically changed content to screen readers. `polite`: waits for the user to be idle, then reads. `assertive`: interrupts immediately. Used for notifications, chat messages, live search results, and form errors.",
  },
  {
    id: 65,
    category: "HTML",
    subcategory: "Accessibility",
    level: "advanced",
    q: "Skip links là gì và tại sao cần?",
    a: 'Skip link là `<a>` ẩn ở đầu trang, hiện ra khi nhận focus bằng Tab đầu tiên, cho phép nhảy thẳng đến `#main-content`. Giúp keyboard/screen reader users không phải tab qua toàn bộ navigation (có thể 30+ links) ở mỗi trang. Implement: `<a class="skip-link" href="#main">Bỏ qua navigation</a>` + CSS `.skip-link { position: absolute; top: -40px } .skip-link:focus { top: 0 }`. WCAG 2.4.1 yêu cầu bypass mechanism. Test: nhấn Tab ngay sau khi load trang, link phải hiện ra.',
    q_en: "What are skip links and why are they needed?",
    a_en: 'A skip link is a hidden `<a>` at the top of the page that appears when focused via the first Tab keypress, allowing users to jump directly to `#main-content`. It saves keyboard/screen reader users from tabbing through all navigation links (potentially 30+) on every page. Implementation: `<a class="skip-link" href="#main">Skip navigation</a>` + CSS `.skip-link { position: absolute; top: -40px } .skip-link:focus { top: 0 }`. WCAG 2.4.1 requires a bypass mechanism. Test: press Tab immediately after the page loads; the link must appear.',
  },
  {
    id: 66,
    category: "HTML",
    subcategory: "Accessibility",
    level: "advanced",
    q: "Focus management trong SPA quan trọng thế nào?",
    a: "Khi route thay đổi trong SPA, focus phải chuyển đến nội dung mới (heading hoặc main). Không có page reload tự nhiên nên screen readers không biết nội dung đổi. Dùng `focus()` và `aria-live` regions.",
    q_en: "Why is focus management important in SPAs?",
    a_en: "When a route changes in a SPA, focus must move to the new content (a heading or main area). There's no natural page reload, so screen readers don't know the content has changed. Use `focus()` and `aria-live` regions to manage this.",
  },
  {
    id: 67,
    category: "HTML",
    subcategory: "Accessibility",
    level: "advanced",
    q: "Heading hierarchy (h1-h6) tại sao quan trọng?",
    a: "Screen reader users dùng shortcut để nhảy qua headings (phím H trong NVDA/JAWS) — heading là cách chính để navigate page. Thứ tự phải liên tục (h1 → h2 → h3), không được nhảy cấp (h1 → h3). Mỗi page nên có đúng 1 `<h1>` là tiêu đề chính. Google coi heading hierarchy là SEO signal. Lỗi phổ biến: dùng `<h3>` vì thích cỡ chữ — hãy dùng CSS để style kích thước, không dùng heading sai cấp. `<h1>` không cần là element đầu tiên nhìn thấy nhưng phải là quan trọng nhất về nội dung.",
    q_en: "Why is the heading hierarchy (h1-h6) important?",
    a_en: "Screen reader users use shortcuts to navigate by headings (H key in NVDA/JAWS) — headings are the primary way to navigate a page. The hierarchy must be sequential (h1 → h2 → h3), with no skipped levels (e.g., h1 → h3). Each page should have exactly one `<h1>` as the primary title. Google treats heading hierarchy as an SEO signal. Common mistake: using `<h3>` just because you like the font size — use CSS for sizing, never misuse heading levels. `<h1>` doesn't have to be the first visible element, but it must be the most important one content-wise.",
  },

  // === HTML: SEO (68-80) ===
  {
    id: 68,
    category: "HTML",
    subcategory: "SEO",
    level: "beginner",
    q: "Meta description là gì?",
    a: 'Meta description là thẻ `<meta name="description" content="...">` mô tả ngắn gọn nội dung trang (tối ưu 150-160 ký tự). Hiển thị dưới title trong search results (SERP). Không trực tiếp ảnh hưởng ranking nhưng ảnh hưởng CTR (click-through rate) — mô tả hấp dẫn = nhiều click hơn. Mỗi trang phải có description unique, chứa keyword tự nhiên, có call-to-action. Nếu bỏ qua, Google tự tạo snippet từ nội dung trang — thường không tối ưu.',
    q_en: "What is a meta description?",
    a_en: 'The meta description is `<meta name="description" content="...">`, a brief summary of the page content (ideally 150-160 characters). It appears below the title in search results (SERP). It doesn\'t directly affect ranking but does affect CTR — a compelling description means more clicks. Each page should have a unique description, include keywords naturally, and include a call-to-action. If omitted, Google generates its own snippet from the page content — which is usually not optimal.',
  },
  {
    id: 69,
    category: "HTML",
    subcategory: "SEO",
    level: "beginner",
    q: "Open Graph meta tags là gì?",
    a: "Open Graph (OG) là protocol của Facebook kiểm soát cách trang hiển thị khi share trên mạng xã hội. Tags quan trọng: `og:title` (tiêu đề share), `og:description` (mô tả), `og:image` (ảnh preview, khuyến nghị 1200×630px), `og:url` (URL canonical). LinkedIn, Discord, Slack cũng dùng OG. Twitter/X dùng `twitter:card` riêng. Thiếu OG tags → preview xấu khi share → giảm click. Next.js có `metadata.openGraph` API để set dễ dàng.",
    q_en: "What are Open Graph meta tags?",
    a_en: "Open Graph (OG) is a Facebook protocol that controls how a page appears when shared on social media. Key tags: `og:title` (share title), `og:description` (description), `og:image` (preview image, recommended 1200x630px), `og:url` (canonical URL). LinkedIn, Discord, and Slack also use OG. Twitter/X uses its own `twitter:card`. Missing OG tags → ugly previews when shared → fewer clicks. Next.js provides `metadata.openGraph` API for easy setup.",
  },
  {
    id: 70,
    category: "HTML",
    subcategory: "SEO",
    level: "intermediate",
    q: "Canonical tag dùng để làm gì?",
    a: '`<link rel="canonical" href="...">` chỉ định URL chính thức khi có nhiều URL cùng nội dung (duplicate content). Giúp search engines consolidate ranking signals về 1 URL, tránh penalize duplicate.',
    q_en: "What is the canonical tag used for?",
    a_en: '`<link rel="canonical" href="...">` specifies the authoritative URL when multiple URLs share the same content (duplicate content). It helps search engines consolidate ranking signals to a single URL, avoiding duplicate content penalties.',
  },
  {
    id: 71,
    category: "HTML",
    subcategory: "SEO",
    level: "intermediate",
    q: "Structured data (Schema.org) là gì?",
    a: "Markup dữ liệu có cấu trúc giúp search engines hiểu nội dung tốt hơn. Dùng JSON-LD (khuyến nghị), Microdata hoặc RDFa. Cho phép rich snippets (rating, price, FAQ) trong kết quả tìm kiếm.",
    q_en: "What is structured data (Schema.org)?",
    a_en: "Structured data markup helps search engines understand content more deeply. Use JSON-LD (recommended), Microdata, or RDFa. Enables rich snippets (ratings, prices, FAQs) in search results.",
  },
  {
    id: 72,
    category: "HTML",
    subcategory: "SEO",
    level: "intermediate",
    q: "Robots meta tag và robots.txt khác nhau?",
    a: 'Meta robots (`<meta name="robots" content="noindex,nofollow">`) áp dụng per-page. robots.txt áp dụng toàn site, ngăn crawlers truy cập paths cụ thể. robots.txt không ngăn indexing nếu page có backlinks.',
    q_en: "How do robots meta tag and robots.txt differ?",
    a_en: 'Robots meta tag (`<meta name="robots" content="noindex,nofollow">`) applies per page. robots.txt applies site-wide, preventing crawlers from accessing specific paths. robots.txt does not prevent indexing if the page has backlinks.',
  },
  {
    id: 73,
    category: "HTML",
    subcategory: "SEO",
    level: "intermediate",
    q: "Sitemap XML quan trọng thế nào?",
    a: "Danh sách URLs giúp search engines phát hiện và crawl trang hiệu quả. Quan trọng cho site lớn, trang mới, trang ít backlinks. Chứa URL, lastmod, changefreq, priority. Submit qua Google Search Console.",
    q_en: "Why is an XML sitemap important?",
    a_en: "A list of URLs that helps search engines discover and crawl pages efficiently. Critical for large sites, newly launched pages, and pages with few backlinks. Contains URL, lastmod, changefreq, and priority. Submit through Google Search Console.",
  },
  {
    id: 74,
    category: "HTML",
    subcategory: "SEO",
    level: "advanced",
    q: "Core Web Vitals ảnh hưởng SEO thế nào?",
    a: `Google dùng Core Web Vitals (LCP, INP, CLS) làm ranking factor.

- LCP: thời gian load element lớn nhất (<2.5s)
- CLS: layout shift (<0.1)
- INP (thay thế FID từ 3/2024): phản hồi tương tác (<200ms)
- Ảnh hưởng cả mobile và desktop ranking.`,
    q_en: "How do Core Web Vitals affect SEO rankings?",
    a_en: `Google uses Core Web Vitals (LCP, INP, CLS) as a ranking factor.

- LCP: time to render the largest element (<2.5s)
- CLS: layout shift (<0.1)
- INP (replaced FID in March 2024): interaction response time (<200ms)
- Affects both mobile and desktop rankings.`,
  },
  {
    id: 75,
    category: "HTML",
    subcategory: "SEO",
    level: "advanced",
    q: "Hreflang attribute dùng khi nào?",
    a: 'Hreflang cho search engines biết trang có phiên bản ngôn ngữ hoặc vùng miền khác nhau. \n\n**Ví dụ:** `<link rel="alternate" hreflang="vi" href="/vi/trang">` và `<link rel="alternate" hreflang="en" href="/en/page">`. Bắt buộc phải là 2 chiều: trang tiếng Việt trỏ sang tiếng Anh VÀ ngược lại. Dùng `hreflang="x-default"` cho trang fallback mặc định. Giúp Google hiển thị đúng phiên bản cho từng thị trường, tránh duplicate content penalization. Sai hreflang là lỗi phổ biến nhất trong SEO đa ngôn ngữ.',
    q_en: "When should you use the hreflang attribute?",
    a_en: 'Hreflang tells search engines that a page has versions in different languages or regions. \n\n**Example:** `<link rel="alternate" hreflang="vi" href="/vi/page">` and `<link rel="alternate" hreflang="en" href="/en/page">`. The relationship must be bidirectional: each page must reference all others AND itself. Use `hreflang="x-default"` for the default fallback page. Helps Google show the correct version per market and avoids duplicate content penalization. Incorrect hreflang is one of the most common multilingual SEO mistakes.',
  },

  // === CSS: Cơ Bản (76-90) ===
  {
    id: 76,
    category: "CSS",
    subcategory: "Cơ Bản",
    level: "beginner",
    q: "CSS là gì? Có mấy cách sử dụng CSS?",
    a: "CSS (Cascading Style Sheets) định dạng giao diện HTML. 3 cách: Inline (attribute style), Internal (thẻ `<style>` trong head), External (file .css riêng qua `<link>`). External được khuyến nghị nhất vì tách biệt và tái sử dụng.",
    q_en: "What is CSS? How many ways can it be applied?",
    a_en: "CSS (Cascading Style Sheets) defines the visual presentation of HTML. 3 methods: Inline (style attribute), Internal (`<style>` tag in head), External (separate .css file via `<link>`). External is recommended as it separates concerns and enables reuse.",
  },
  {
    id: 77,
    category: "CSS",
    subcategory: "Cơ Bản",
    level: "beginner",
    q: "Cascade trong CSS nghĩa là gì?",
    a: "Cơ chế quyết định style nào được áp dụng khi có xung đột. Thứ tự ưu tiên: importance (!important) > specificity > source order. Browser default < user styles < author styles. Cascade giúp quản lý styles phức tạp.",
    q_en: "What does 'cascade' mean in CSS?",
    a_en: "The mechanism that determines which style is applied when there are conflicts. Priority order: importance (!important) > specificity > source order. Browser defaults < user styles < author styles. Cascade helps manage complex styling scenarios.",
  },
  {
    id: 78,
    category: "CSS",
    subcategory: "Cơ Bản",
    level: "beginner",
    q: "Specificity trong CSS được tính thế nào?",
    a: "Specificity tính theo 3 cột (a,b,c): inline (1,0,0,0) > ID (0,1,0,0) > class/pseudo-class (0,0,1,0) > element (0,0,0,1). Không cộng dồn giữa các cột. \n\n**Ví dụ:** `#nav .link:hover` = 100+10+10 = 120. Specificity cao hơn thắng, bằng nhau thì source order quyết định.",
    q_en: "How is specificity calculated in CSS?",
    a_en: "Specificity is calculated using 3 columns (a,b,c): inline (1,0,0,0) > ID (0,1,0,0) > class/pseudo-class (0,0,1,0) > element (0,0,0,1). Columns do not carry over between each other. \n\n**Example:** `#nav .link:hover` = 100+10+10 = 120. Higher specificity wins; equal specificity defers to source order.",
  },
  {
    id: 79,
    category: "CSS",
    subcategory: "Cơ Bản",
    level: "beginner",
    q: "Inheritance trong CSS là gì?",
    a: "Một số CSS properties tự động kế thừa từ parent sang child (color, font-family, font-size, line-height). Các properties về box model (margin, padding, border) KHÔNG kế thừa. Dùng `inherit`, `initial`, `unset` để override.",
    q_en: "What is inheritance in CSS?",
    a_en: "Some CSS properties automatically pass from parent to child elements (color, font-family, font-size, line-height). Box model properties (margin, padding, border) do NOT inherit. Use `inherit`, `initial`, `unset` to override inherited behavior.",
  },
  {
    id: 80,
    category: "CSS",
    subcategory: "Cơ Bản",
    level: "beginner",
    q: "Đơn vị px, em, rem, %, vw, vh khác nhau?",
    a: "`px`: tuyệt đối. `em`: relative với font-size parent. `rem`: relative với root (html) font-size. `%`: relative với parent. `vw`/`vh`: % viewport width/height. rem được khuyến nghị cho responsive design.",
    q_en: "How do px, em, rem, %, vw, and vh differ?",
    a_en: "`px`: absolute unit. `em`: relative to the parent element's font-size. `rem`: relative to the root (`html`) font-size. `%`: relative to the parent. `vw`/`vh`: percentage of viewport width/height. `rem` is recommended for responsive design.",
  },
  {
    id: 81,
    category: "CSS",
    subcategory: "Cơ Bản",
    level: "beginner",
    q: "!important dùng khi nào? Tại sao nên tránh?",
    a: "Override tất cả specificity rules. Nên tránh vì: khó debug, khó maintain, phá vỡ cascade tự nhiên. Chỉ dùng cho utility classes hoặc override third-party styles khi không có cách khác.",
    q_en: "When should you use !important? Why should you avoid it?",
    a_en: "It overrides all specificity rules. Avoid it because: it makes debugging hard, breaks the natural cascade, and becomes difficult to maintain. Only use it for utility classes or when overriding third-party styles with no other option.",
  },
  {
    id: 82,
    category: "CSS",
    subcategory: "Cơ Bản",
    level: "intermediate",
    q: "CSS reset và normalize CSS khác nhau?",
    a: "CSS Reset xóa toàn bộ browser default styles (margin, padding = 0). Normalize CSS giữ các default hữu ích, chỉ sửa inconsistencies giữa browsers. Normalize phổ biến hơn vì giữ được base styling hợp lý.",
    q_en: "How do CSS reset and normalize CSS differ?",
    a_en: "CSS Reset removes all browser default styles (margin, padding = 0). Normalize CSS preserves useful defaults and only fixes inconsistencies between browsers. Normalize is more popular because it keeps a sensible base styling.",
  },
  {
    id: 83,
    category: "CSS",
    subcategory: "Cơ Bản",
    level: "intermediate",
    q: "SASS/SCSS là gì? Lợi ích?",
    a: "CSS preprocessor thêm tính năng: variables ($color), nesting, mixins, functions, partials, inheritance (@extend). `.scss` dùng cú pháp giống CSS (curly braces), `.sass` dùng indentation. Modern Sass: dùng `@use`/`@forward` thay `@import` (deprecated). Compile ra CSS thuần.",
    q_en: "What is SASS/SCSS? What are its benefits?",
    a_en: "A CSS preprocessor that adds features: variables ($color), nesting, mixins, functions, partials, and inheritance (@extend). `.scss` uses CSS-like syntax (curly braces), `.sass` uses indentation. Modern Sass: use `@use`/`@forward` instead of `@import` (deprecated). Compiles down to plain CSS.",
  },

  // === CSS: Selectors (84-95) ===
  {
    id: 84,
    category: "CSS",
    subcategory: "Selectors",
    level: "beginner",
    q: "Pseudo-class và pseudo-element khác nhau?",
    a: "Pseudo-class chọn state của element (`:hover`, `:focus`, `:first-child`, `:nth-child`). Pseudo-element tạo/chọn phần ảo (::before, ::after, ::first-line). Pseudo-class dùng 1 dấu hai chấm, pseudo-element dùng 2.",
    q_en: "How do pseudo-classes and pseudo-elements differ?",
    a_en: "Pseudo-classes select the state of an element (`:hover`, `:focus`, `:first-child`, `:nth-child`). Pseudo-elements create or select virtual parts (::before, ::after, ::first-line). Pseudo-classes use one colon, pseudo-elements use two.",
  },
  {
    id: 85,
    category: "CSS",
    subcategory: "Selectors",
    level: "beginner",
    q: ":nth-child() và :nth-of-type() khác nhau?",
    a: "`:nth-child(n)` đếm tất cả siblings. `:nth-of-type(n)` chỉ đếm siblings cùng tag. \n\n**Ví dụ:** `p:nth-child(2)` = phần tử thứ 2 NẾU là p. `p:nth-of-type(2)` = phần tử p thứ 2 bất kể vị trí.",
    q_en: "How do :nth-child() and :nth-of-type() differ?",
    a_en: "`:nth-child(n)` counts all siblings. `:nth-of-type(n)` counts only siblings of the same tag. \n\n**Example:** `p:nth-child(2)` matches the second element IF it's a `p`. `p:nth-of-type(2)` matches the second `p` element regardless of its position.",
  },
  {
    id: 86,
    category: "CSS",
    subcategory: "Selectors",
    level: "intermediate",
    q: "::before và ::after dùng để làm gì?",
    a: "Tạo pseudo-element trước/sau content của element. Bắt buộc có `content` property. Dùng cho decorative elements (icons, shapes, borders), tooltips, clearfix. Không nên dùng cho nội dung quan trọng (screen readers có thể bỏ qua).",
    q_en: "What are ::before and ::after used for?",
    a_en: "They create pseudo-elements before/after an element's content. The `content` property is required. Used for decorative elements (icons, shapes, borders), tooltips, and clearfix. Avoid using them for meaningful content as screen readers may ignore them.",
  },
  {
    id: 87,
    category: "CSS",
    subcategory: "Selectors",
    level: "intermediate",
    q: ":has() selector là gì?",
    a: "Parent selector trong CSS (trước đây không có). Chọn element có chứa selector con. \n\n**Ví dụ:** `article:has(img)` chọn article có chứa img. `div:has(> p)` chọn div có p là direct child. Game-changer cho CSS.",
    q_en: "What is the :has() selector?",
    a_en: "A parent selector in CSS (previously impossible). Selects elements that contain a matching child selector. \n\n**Example:** `article:has(img)` selects articles containing an image. `div:has(> p)` selects divs with a direct `p` child. A game-changer for CSS.",
  },
  {
    id: 88,
    category: "CSS",
    subcategory: "Selectors",
    level: "intermediate",
    q: "Combinator selectors (>, +, ~, space) khác nhau?",
    a: "Space (descendant): bất kỳ con cháu nào. `>` (child): chỉ con trực tiếp. `+` (adjacent sibling): anh em liền kề ngay sau. `~` (general sibling): tất cả anh em sau. \n\n**Ví dụ:** `div > p` vs `div p`.",
    q_en: "How do combinator selectors (>, +, ~, space) differ?",
    a_en: "Space (descendant): any descendant. `>` (child): direct children only. `+` (adjacent sibling): the immediately following sibling. `~` (general sibling): all following siblings. \n\n**Example:** `div > p` vs `div p`.",
  },
  {
    id: 89,
    category: "CSS",
    subcategory: "Selectors",
    level: "intermediate",
    q: "Attribute selectors có những dạng nào?",
    a: "`[attr]`: có attribute. `[attr=value]`: giá trị chính xác. `[attr^=value]`: bắt đầu bằng. `[attr$=value]`: kết thúc bằng. `[attr*=value]`: chứa. `[attr~=value]`: chứa word. Case-insensitive với `i` flag.",
    q_en: "What forms do attribute selectors take?",
    a_en: "`[attr]`: has the attribute. `[attr=value]`: exact value match. `[attr^=value]`: starts with. `[attr$=value]`: ends with. `[attr*=value]`: contains. `[attr~=value]`: contains as a whole word. Add `i` flag for case-insensitive matching.",
  },
  {
    id: 90,
    category: "CSS",
    subcategory: "Selectors",
    level: "advanced",
    q: ":is(), :where(), :not() khác nhau?",
    a: "`:is()` và `:where()` đều match danh sách selectors, nhưng `:is()` giữ specificity cao nhất trong danh sách, `:where()` luôn specificity = 0. `:not()` loại trừ selector. Cả ba giúp viết CSS ngắn gọn hơn.",
    q_en: "How do :is(), :where(), and :not() differ?",
    a_en: "`:is()` and `:where()` both match a list of selectors, but `:is()` keeps the highest specificity of any selector in the list, while `:where()` always has specificity of 0. `:not()` excludes matching elements. All three help write more concise CSS.",
  },

  // === CSS: Box Model & Layout (91-110) ===
  {
    id: 91,
    category: "CSS",
    subcategory: "Box Model",
    level: "beginner",
    q: "Box model gồm những gì?",
    a: "4 lớp từ trong ra: content (nội dung), padding (khoảng đệm), border (viền), margin (khoảng cách ngoài). Mặc định width/height chỉ tính content. Dùng `box-sizing: border-box` để tính luôn padding + border.",
    q_en: "What does the box model consist of?",
    a_en: "4 layers from inside out: content, padding (inner spacing), border, margin (outer spacing). By default, width/height only applies to content. Use `box-sizing: border-box` to include padding and border in the width/height calculation.",
  },
  {
    id: 92,
    category: "CSS",
    subcategory: "Box Model",
    level: "beginner",
    q: "box-sizing: content-box vs border-box?",
    a: "`content-box` (mặc định): width/height chỉ tính content, padding+border cộng thêm. `border-box`: width/height bao gồm content+padding+border. border-box dễ quản lý hơn, nên dùng `* { box-sizing: border-box }`.",
    q_en: "What is the difference between box-sizing: content-box and border-box?",
    a_en: "`content-box` (default): width/height applies only to content; padding and border are added on top. `border-box`: width/height includes content + padding + border. `border-box` is easier to manage — use `* { box-sizing: border-box }`.",
  },
  {
    id: 93,
    category: "CSS",
    subcategory: "Box Model",
    level: "intermediate",
    q: "Margin collapsing là gì?",
    a: "Khi 2 vertical margins tiếp xúc nhau, chúng merge thành 1 margin (lấy giá trị lớn hơn). Xảy ra giữa siblings, parent-child (không có padding/border ngăn cách). Không xảy ra với horizontal margins, flexbox, grid items.",
    q_en: "What is margin collapsing?",
    a_en: "When two vertical margins touch, they merge into one margin (taking the larger value). Occurs between siblings and parent-child elements (when no padding/border separates them). Does NOT occur with horizontal margins, flex items, or grid items.",
  },
  {
    id: 94,
    category: "CSS",
    subcategory: "Box Model",
    level: "intermediate",
    q: "Overflow property có những giá trị nào?",
    a: "`visible` (mặc định, tràn ra ngoài). `hidden` (cắt nội dung tràn). `scroll` (luôn hiện scrollbar). `auto` (scrollbar khi cần). `clip` (như hidden nhưng cấm programmatic scroll). Dùng `overflow-x`/`overflow-y` riêng.",
    q_en: "What values does the overflow property accept?",
    a_en: "`visible` (default, content overflows). `hidden` (clips overflowing content). `scroll` (always shows scrollbar). `auto` (shows scrollbar only when needed). `clip` (like hidden but prevents programmatic scrolling). Use `overflow-x`/`overflow-y` separately for each axis.",
  },
  {
    id: 95,
    category: "CSS",
    subcategory: "Display & Position",
    level: "beginner",
    q: "display: none, visibility: hidden, opacity: 0 khác nhau?",
    a: "`display: none`: xóa khỏi layout, không chiếm không gian. `visibility: hidden`: ẩn nhưng vẫn chiếm không gian. `opacity: 0`: trong suốt, vẫn chiếm không gian VÀ nhận events. none không animatable.",
    q_en: "How do display: none, visibility: hidden, and opacity: 0 differ?",
    a_en: "`display: none`: removes from layout, takes up no space. `visibility: hidden`: invisible but still occupies space. `opacity: 0`: fully transparent, still occupies space AND receives pointer events. `display: none` cannot be animated.",
  },
  {
    id: 96,
    category: "CSS",
    subcategory: "Display & Position",
    level: "beginner",
    q: "Position property có những giá trị nào?",
    a: "`static` (mặc định, theo flow). `relative` (offset từ vị trí gốc, vẫn chiếm space). `absolute` (thoát flow, relative với positioned ancestor). `fixed` (relative với viewport). `sticky` (hybrid relative+fixed).",
    q_en: "What values does the position property accept?",
    a_en: "`static` (default, follows document flow). `relative` (offset from its normal position, still takes up space). `absolute` (removed from flow, positioned relative to the nearest positioned ancestor). `fixed` (relative to the viewport). `sticky` (hybrid of relative and fixed).",
  },
  {
    id: 97,
    category: "CSS",
    subcategory: "Display & Position",
    level: "intermediate",
    q: "Position sticky hoạt động thế nào?",
    a: "Kết hợp relative và fixed. Element scroll bình thường cho đến khi đạt threshold (top/bottom), sau đó 'dính' tại vị trí đó. Cần set ít nhất 1 trong top/right/bottom/left. Parent phải có overflow visible.",
    q_en: "How does position: sticky work?",
    a_en: "Combines relative and fixed behavior. The element scrolls normally until it reaches a defined threshold (top/bottom), then 'sticks' there. At least one of top/right/bottom/left must be set. The parent element must have `overflow: visible`.",
  },
  {
    id: 98,
    category: "CSS",
    subcategory: "Display & Position",
    level: "intermediate",
    q: "Z-index hoạt động thế nào?",
    a: "Kiểm soát thứ tự xếp chồng (stacking order) của positioned elements. Giá trị cao hơn nằm trên. Chỉ hoạt động với position khác static. Tạo stacking context mới với z-index + positioned hoặc opacity < 1.",
    q_en: "How does z-index work?",
    a_en: "Controls the stacking order of positioned elements. Higher values appear on top. Only works on elements with a position other than static. Creates a new stacking context when combined with a non-auto z-index or `opacity < 1`.",
  },
  {
    id: 99,
    category: "CSS",
    subcategory: "Display & Position",
    level: "intermediate",
    q: "Stacking context là gì?",
    a: "Nhóm 3D ảo chứa các elements cùng xếp chồng. Được tạo bởi: root element, positioned + z-index, opacity < 1, transform, filter. Z-index chỉ so sánh trong cùng stacking context, không xuyên qua.",
    q_en: "What is a stacking context?",
    a_en: "A virtual 3D group containing elements that are stacked together. Created by: root element, positioned elements with a z-index, opacity < 1, transform, filter. Z-index values are only compared within the same stacking context — they cannot 'bleed' across contexts.",
  },
  {
    id: 100,
    category: "CSS",
    subcategory: "Display & Position",
    level: "advanced",
    q: "Transform có ảnh hưởng layout không?",
    a: "Không. Transform (translate, rotate, scale) chỉ thay đổi visual, không ảnh hưởng flow layout. Element vẫn chiếm space gốc. Tạo stacking context mới. Translate tốt hơn top/left cho animations vì dùng GPU compositing.",
    q_en: "Does transform affect layout?",
    a_en: "No. Transform (translate, rotate, scale) only changes the visual rendering — it does not affect document flow. The element still occupies its original space. It creates a new stacking context. `translate` is better than `top`/`left` for animations because it uses GPU compositing.",
  },

  // === CSS: Flexbox (101-115) ===
  {
    id: 101,
    category: "CSS",
    subcategory: "Flexbox",
    level: "beginner",
    q: "Flexbox là gì? Khi nào nên dùng?",
    a: "Layout 1 chiều (row hoặc column) cho phép căn chỉnh, phân bổ không gian linh hoạt giữa items. Dùng cho: navigation bars, card layouts, centering, form layouts. Grid phù hợp hơn cho layout 2 chiều.",
    q_en: "What is Flexbox? When should you use it?",
    a_en: "A one-dimensional layout system (row or column) for flexible alignment and space distribution between items. Use for: navigation bars, card layouts, centering, form layouts. CSS Grid is better suited for two-dimensional layouts.",
  },
  {
    id: 102,
    category: "CSS",
    subcategory: "Flexbox",
    level: "beginner",
    q: "justify-content và align-items khác nhau?",
    a: "`justify-content` căn chỉnh theo main axis (mặc định horizontal): flex-start, center, flex-end, space-between, space-around, space-evenly. `align-items` căn theo cross axis (mặc định vertical): stretch, center, flex-start, flex-end, baseline.",
    q_en: "How do justify-content and align-items differ?",
    a_en: "`justify-content` aligns along the main axis (horizontal by default): flex-start, center, flex-end, space-between, space-around, space-evenly. `align-items` aligns along the cross axis (vertical by default): stretch, center, flex-start, flex-end, baseline.",
  },
  {
    id: 103,
    category: "CSS",
    subcategory: "Flexbox",
    level: "beginner",
    q: "flex-direction có những giá trị nào?",
    a: "`row` (mặc định, trái→phải). `row-reverse` (phải→trái). `column` (trên→dưới). `column-reverse` (dưới→trên). Thay đổi direction cũng đổi main/cross axis, ảnh hưởng justify-content/align-items.",
    q_en: "What values does flex-direction accept?",
    a_en: "`row` (default, left to right). `row-reverse` (right to left). `column` (top to bottom). `column-reverse` (bottom to top). Changing direction also changes the main and cross axes, which affects how justify-content and align-items behave.",
  },
  {
    id: 104,
    category: "CSS",
    subcategory: "Flexbox",
    level: "intermediate",
    q: "flex-grow, flex-shrink, flex-basis là gì?",
    a: "`flex-basis`: kích thước ban đầu (trước grow/shrink). `flex-grow`: tỷ lệ phân bổ không gian thừa (0 = không grow). `flex-shrink`: tỷ lệ co lại khi thiếu space (0 = không shrink). Shorthand: `flex: grow shrink basis`.",
    q_en: "What are flex-grow, flex-shrink, and flex-basis?",
    a_en: "`flex-basis`: the initial size before growing/shrinking. `flex-grow`: ratio for distributing extra space (0 = don't grow). `flex-shrink`: ratio for shrinking when there's insufficient space (0 = don't shrink). Shorthand: `flex: grow shrink basis`.",
  },
  {
    id: 105,
    category: "CSS",
    subcategory: "Flexbox",
    level: "intermediate",
    q: "flex-wrap hoạt động thế nào?",
    a: "`nowrap` (mặc định, tất cả trên 1 dòng, co lại nếu cần). `wrap` (xuống dòng khi hết space). `wrap-reverse` (xuống dòng ngược). Khi wrap, dùng `align-content` căn chỉnh các dòng.",
    q_en: "How does flex-wrap work?",
    a_en: "`nowrap` (default, all items on one line, shrink if needed). `wrap` (items wrap to the next line when space runs out). `wrap-reverse` (wraps in reverse direction). When wrapping, use `align-content` to align the rows.",
  },
  {
    id: 106,
    category: "CSS",
    subcategory: "Flexbox",
    level: "intermediate",
    q: "Gap property trong Flexbox là gì?",
    a: "Tạo khoảng cách giữa flex items mà không cần margin. `gap: 16px` (cả 2 chiều), `row-gap` và `column-gap` riêng. \n\n**Ưu điểm:** không gây margin collapse, không có khoảng cách thừa ở edge.",
    q_en: "What is the gap property in Flexbox?",
    a_en: "Creates spacing between flex items without using margins. `gap: 16px` applies to both axes; `row-gap` and `column-gap` apply separately. \n\n**Benefits:** no margin collapsing issues, no extra spacing at the container edges.",
  },
  {
    id: 107,
    category: "CSS",
    subcategory: "Flexbox",
    level: "intermediate",
    q: "align-self dùng để làm gì?",
    a: "Override align-items cho 1 flex item cụ thể. Values: auto (theo parent), flex-start, flex-end, center, baseline, stretch. Cho phép 1 item căn khác so với các items còn lại trong container.",
    q_en: "What is align-self used for?",
    a_en: "Overrides `align-items` for a single flex item. Values: auto (follows parent), flex-start, flex-end, center, baseline, stretch. Allows one item to align differently from the rest in the container.",
  },
  {
    id: 108,
    category: "CSS",
    subcategory: "Flexbox",
    level: "advanced",
    q: "Làm thế nào để center cả horizontal và vertical bằng Flexbox?",
    a: "Container: `display: flex; justify-content: center; align-items: center;`. Hoặc: `display: flex;` + child: `margin: auto;`. Đây là cách đơn giản nhất để center element trong CSS hiện đại.",
    q_en: "How do you center an element both horizontally and vertically using Flexbox?",
    a_en: "On the container: `display: flex; justify-content: center; align-items: center;`. Or: `display: flex;` on the container + `margin: auto;` on the child. This is the simplest way to center in modern CSS.",
  },

  // === CSS: Grid (109-125) ===
  {
    id: 109,
    category: "CSS",
    subcategory: "Grid",
    level: "beginner",
    q: "CSS Grid là gì? Khác Flexbox thế nào?",
    a: "Layout 2 chiều (rows VÀ columns cùng lúc). Flexbox cho 1 chiều (row hoặc column). Grid tốt cho page layout tổng thể, Flexbox cho component layout. Có thể kết hợp cả hai.",
    q_en: "What is CSS Grid? How does it differ from Flexbox?",
    a_en: "A two-dimensional layout system (rows AND columns simultaneously). Flexbox is one-dimensional (row or column). Grid is better for overall page layout; Flexbox is better for component-level layout. They are often used together.",
  },
  {
    id: 110,
    category: "CSS",
    subcategory: "Grid",
    level: "beginner",
    q: "fr unit là gì?",
    a: "Fractional unit - đơn vị tỷ lệ trong Grid. `grid-template-columns: 1fr 2fr` = cột 2 gấp đôi cột 1. Fr phân bổ không gian còn lại sau khi trừ fixed sizes. Linh hoạt hơn % vì tự tính gap.",
    q_en: "What is the fr unit?",
    a_en: "Fractional unit — a proportional unit in Grid. `grid-template-columns: 1fr 2fr` means column 2 is twice the width of column 1. Fr distributes remaining space after subtracting fixed sizes. More flexible than percentages because it automatically accounts for gaps.",
  },
  {
    id: 111,
    category: "CSS",
    subcategory: "Grid",
    level: "intermediate",
    q: "grid-template-columns và grid-template-rows hoạt động thế nào?",
    a: "Định nghĩa số lượng và kích thước columns/rows. \n\n**Ví dụ:** `grid-template-columns: 200px 1fr 1fr` = 1 cột 200px + 2 cột equal. Dùng `repeat(3, 1fr)` cho 3 cột bằng nhau. Kết hợp minmax(), auto-fit, auto-fill.",
    q_en: "How do grid-template-columns and grid-template-rows work?",
    a_en: "They define the number and size of columns/rows. \n\n**Example:** `grid-template-columns: 200px 1fr 1fr` = one 200px column + two equal columns. Use `repeat(3, 1fr)` for three equal columns. Combine with `minmax()`, `auto-fit`, and `auto-fill` for responsive layouts.",
  },
  {
    id: 112,
    category: "CSS",
    subcategory: "Grid",
    level: "intermediate",
    q: "auto-fit và auto-fill khác nhau?",
    a: "Cả hai dùng với `repeat()` để tự động tạo columns. `auto-fill`: tạo tracks rỗng nếu còn space. `auto-fit`: collapse tracks rỗng, items stretch fill space. auto-fit phổ biến hơn cho responsive grids.",
    q_en: "How do auto-fit and auto-fill differ?",
    a_en: "Both are used with `repeat()` to automatically create columns. `auto-fill`: creates empty tracks if space remains. `auto-fit`: collapses empty tracks, allowing items to stretch and fill the space. `auto-fit` is more commonly used for responsive grids.",
  },
  {
    id: 113,
    category: "CSS",
    subcategory: "Grid",
    level: "intermediate",
    q: "minmax() function dùng để làm gì?",
    a: "Giới hạn min/max kích thước track. `minmax(200px, 1fr)` = tối thiểu 200px, tối đa bằng nhau. Kết hợp auto-fit: `repeat(auto-fit, minmax(250px, 1fr))` tạo responsive grid không cần media queries.",
    q_en: "What is the minmax() function used for?",
    a_en: "Sets a minimum and maximum size for a grid track. `minmax(200px, 1fr)` = at least 200px, at most an equal share. Combined with auto-fit: `repeat(auto-fit, minmax(250px, 1fr))` creates a responsive grid without media queries.",
  },
  {
    id: 114,
    category: "CSS",
    subcategory: "Grid",
    level: "intermediate",
    q: "Grid template areas là gì?",
    a: "Đặt tên cho vùng trong grid bằng strings: `grid-template-areas: 'header header' 'sidebar main' 'footer footer'`. Items dùng `grid-area: header`. Trực quan, dễ đọc. Dùng `.` cho ô trống.",
    q_en: "What are grid template areas?",
    a_en: "Named regions in a grid defined using strings: `grid-template-areas: 'header header' 'sidebar main' 'footer footer'`. Items use `grid-area: header` to occupy those regions. Visual, readable, and intuitive. Use `.` for empty cells.",
  },
  {
    id: 115,
    category: "CSS",
    subcategory: "Grid",
    level: "advanced",
    q: "Subgrid là gì?",
    a: "Cho phép grid item kế thừa track definitions từ parent grid. `grid-template-columns: subgrid`. Giải quyết vấn đề align content giữa các grid items. Firefox 71+, Safari 16+, Chrome 117+. Hiện đã widely supported.",
    q_en: "What is subgrid?",
    a_en: "Allows a grid item to inherit track definitions from its parent grid. `grid-template-columns: subgrid`. Solves the problem of aligning content across grid items. Firefox 71+, Safari 16+, Chrome 117+. Now widely supported.",
  },
  {
    id: 116,
    category: "CSS",
    subcategory: "Grid",
    level: "advanced",
    q: "Grid placement (grid-column, grid-row) hoạt động thế nào?",
    a: "`grid-column: 1 / 3` = từ line 1 đến line 3 (span 2 columns). `grid-row: 1 / -1` = full height. `span 2` = chiếm 2 tracks. Dùng named lines hoặc numbers. Negative values đếm từ cuối.",
    q_en: "How does grid placement (grid-column, grid-row) work?",
    a_en: "`grid-column: 1 / 3` spans from line 1 to line 3 (2 columns wide). `grid-row: 1 / -1` spans full height. `span 2` occupies 2 tracks. Named lines or numbers can be used. Negative values count from the end.",
  },

  // === CSS: Responsive & Animations (117-140) ===
  {
    id: 117,
    category: "CSS",
    subcategory: "Responsive",
    level: "beginner",
    q: "Responsive design là gì?",
    a: "Thiết kế web tự điều chỉnh giao diện theo kích thước màn hình (desktop, tablet, mobile). Dùng fluid layouts (%, fr), flexible images, media queries. Mobile-first approach được khuyến nghị.",
    q_en: "What is responsive design?",
    a_en: "Designing websites that automatically adapt their layout to the screen size (desktop, tablet, mobile). Uses fluid layouts (%, fr), flexible images, and media queries. Mobile-first approach is recommended.",
  },
  {
    id: 118,
    category: "CSS",
    subcategory: "Responsive",
    level: "beginner",
    q: "Mobile-first approach là gì?",
    a: "Viết CSS cho mobile trước (base styles), dùng `min-width` media queries thêm styles cho màn hình lớn hơn. \n\n**Ưu điểm:** performance tốt hơn trên mobile, progressive enhancement, code gọn hơn.",
    q_en: "What is the mobile-first approach?",
    a_en: "Writing CSS for mobile first (base styles), then using `min-width` media queries to add styles for larger screens. \n\n**Benefits:** better performance on mobile, progressive enhancement, and cleaner code.",
  },
  {
    id: 119,
    category: "CSS",
    subcategory: "Responsive",
    level: "beginner",
    q: "Media queries dùng như thế nào?",
    a: "`@media (min-width: 768px) { ... }` áp dụng styles khi viewport >= 768px. Có thể kết hợp: `@media (min-width: 768px) and (max-width: 1024px)`. Breakpoints phổ biến: 640, 768, 1024, 1280px.",
    q_en: "How are media queries used?",
    a_en: "`@media (min-width: 768px) { ... }` applies styles when the viewport is >= 768px. Can be combined: `@media (min-width: 768px) and (max-width: 1024px)`. Common breakpoints: 640, 768, 1024, 1280px.",
  },
  {
    id: 120,
    category: "CSS",
    subcategory: "Responsive",
    level: "intermediate",
    q: "clamp() function dùng để làm gì?",
    a: "`clamp(min, preferred, max)` giới hạn giá trị trong khoảng. \n\n**Ví dụ:** `font-size: clamp(1rem, 2.5vw, 2rem)` = fluid typography tự co giãn. Thay thế nhiều media queries, tạo responsive sizing mượt.",
    q_en: "What is the clamp() function used for?",
    a_en: "`clamp(min, preferred, max)` constrains a value within a range. \n\n**Example:** `font-size: clamp(1rem, 2.5vw, 2rem)` = fluid typography that scales automatically. Replaces multiple media queries and creates smooth responsive sizing.",
  },
  {
    id: 121,
    category: "CSS",
    subcategory: "Responsive",
    level: "intermediate",
    q: "Container queries là gì?",
    a: "Responsive theo kích thước container thay vì viewport. `@container (min-width: 400px) { ... }`. Container phải có `container-type: inline-size`. Giải quyết vấn đề component responsive độc lập với page layout.",
    q_en: "What are container queries?",
    a_en: "Responsive styling based on the container's size rather than the viewport. `@container (min-width: 400px) { ... }`. The container must have `container-type: inline-size`. Solves the problem of making components responsive independently of page layout.",
  },
  {
    id: 122,
    category: "CSS",
    subcategory: "Responsive",
    level: "intermediate",
    q: "Viewport meta tag quan trọng thế nào?",
    a: '`<meta name="viewport" content="width=device-width, initial-scale=1.0">` bắt buộc cho responsive. Không có nó, mobile browsers zoom out để hiện toàn bộ trang desktop. device-width = screen width thực tế.',
    q_en: "Why is the viewport meta tag important?",
    a_en: '`<meta name="viewport" content="width=device-width, initial-scale=1.0">` is required for responsive design. Without it, mobile browsers zoom out to show the full desktop page. `device-width` equals the actual screen width.',
  },
  {
    id: 123,
    category: "CSS",
    subcategory: "Responsive",
    level: "intermediate",
    q: "Responsive images (srcset, sizes) hoạt động thế nào?",
    a: "`srcset` cung cấp nhiều phiên bản ảnh khác kích thước. `sizes` cho browser biết ảnh chiếm bao nhiêu viewport. Browser tự chọn ảnh phù hợp. Tiết kiệm bandwidth trên thiết bị nhỏ.",
    q_en: "How do responsive images (srcset, sizes) work?",
    a_en: "`srcset` provides multiple image versions at different sizes. `sizes` tells the browser how much viewport width the image will occupy. The browser automatically picks the most appropriate image, saving bandwidth on smaller devices.",
  },
  {
    id: 124,
    category: "CSS",
    subcategory: "Animations",
    level: "beginner",
    q: "CSS transition và animation khác nhau?",
    a: "Transition: chuyển đổi giữa 2 trạng thái khi trigger (hover, class change). Animation (@keyframes): phức tạp hơn, nhiều bước, có thể tự chạy, lặp lại. Transition cho interactions đơn giản, animation cho effects phức tạp.",
    q_en: "How do CSS transition and animation differ?",
    a_en: "Transition: smoothly changes between two states when triggered (hover, class change). Animation (@keyframes): more powerful, supports multiple steps, can run automatically, and can loop. Use transitions for simple interactions, animations for complex effects.",
  },
  {
    id: 125,
    category: "CSS",
    subcategory: "Animations",
    level: "beginner",
    q: "Transition properties gồm những gì?",
    a: "CSS transition gồm 4 properties chính: `transition-property` chỉ định CSS property nào sẽ được animate (ví dụ `opacity`, `transform`, hoặc `all`), `transition-duration` là thời gian chuyển đổi (ví dụ `0.3s`), `transition-timing-function` kiểm soát tốc độ biến đổi theo thời gian (`ease`, `linear`, `ease-in-out`, hoặc `cubic-bezier()` tùy chỉnh), và `transition-delay` là thời gian chờ trước khi bắt đầu. Có thể viết gọn bằng shorthand: `transition: opacity 0.3s ease 0s`, hoặc animate nhiều properties: `transition: opacity 0.3s, transform 0.5s`.",
    q_en: "What are the transition properties?",
    a_en: "CSS transition has 4 main properties: `transition-property` (which CSS property to animate, e.g., `opacity`, `transform`, or `all`), `transition-duration` (how long, e.g., `0.3s`), `transition-timing-function` (speed curve: `ease`, `linear`, `ease-in-out`, or custom `cubic-bezier()`), and `transition-delay` (wait time before starting). Shorthand: `transition: opacity 0.3s ease 0s`. Animate multiple properties: `transition: opacity 0.3s, transform 0.5s`.",
  },
  {
    id: 126,
    category: "CSS",
    subcategory: "Animations",
    level: "intermediate",
    q: "@keyframes hoạt động thế nào?",
    a: "Định nghĩa animation steps: `@keyframes fadeIn { from { opacity: 0 } to { opacity: 1 } }`. Dùng % cho nhiều bước: `0% { } 50% { } 100% { }`. Áp dụng: `animation: fadeIn 0.5s ease forwards`.",
    q_en: "How do @keyframes work?",
    a_en: "Defines animation steps: `@keyframes fadeIn { from { opacity: 0 } to { opacity: 1 } }`. Use percentages for multiple steps: `0% { } 50% { } 100% { }`. Apply with: `animation: fadeIn 0.5s ease forwards`.",
  },
  {
    id: 127,
    category: "CSS",
    subcategory: "Animations",
    level: "intermediate",
    q: "will-change property dùng để làm gì?",
    a: "Hint cho browser chuẩn bị trước cho animation/transition. `will-change: transform` tạo composite layer riêng, dùng GPU. Chỉ dùng khi thực sự cần, xóa khi animation xong. Lạm dụng tốn memory.",
    q_en: "What is the will-change property used for?",
    a_en: "Hints to the browser to prepare for an upcoming animation or transition. `will-change: transform` creates a separate compositing layer, offloading to the GPU. Only use it when truly necessary, and remove it after the animation finishes. Overuse wastes memory.",
  },
  {
    id: 128,
    category: "CSS",
    subcategory: "Animations",
    level: "advanced",
    q: "Tại sao transform/opacity animate mượt hơn top/left?",
    a: "transform và opacity chỉ trigger composite step (GPU). top/left trigger layout + paint + composite (CPU-intensive). Layout recalculation tốn thời gian vì ảnh hưởng tất cả elements. Luôn ưu tiên transform: translate() cho di chuyển.",
    q_en: "Why do transform/opacity animations run smoother than top/left?",
    a_en: "transform and opacity only trigger the compositing step (GPU). top/left triggers layout + paint + compositing (CPU-intensive). Layout recalculation is expensive because it affects all other elements. Always prefer `transform: translate()` for movement animations.",
  },
  {
    id: 129,
    category: "CSS",
    subcategory: "Animations",
    level: "advanced",
    q: "Reflow và repaint là gì?",
    a: `Ba bước render pipeline của browser, xếp theo mức độ tốn tài nguyên:

- Reflow (layout): tính toán lại vị trí/kích thước elements — tốn nhất. Gây ra bởi: thay đổi width/height.
- Repaint: vẽ lại pixels — ít tốn hơn. Gây ra bởi: thay đổi color.
- Composite: ghép layers — ít tốn nhất. Gây ra bởi: transform.`,
    q_en: "What are reflow and repaint?",
    a_en: `Three browser rendering pipeline steps, ordered by cost:

- Reflow (layout): recalculates positions and sizes of elements — most expensive. Triggered by: changing width/height.
- Repaint: redraws pixels — less expensive. Triggered by: changing color.
- Compositing: merges layers — least expensive. Triggered by: changing transform.`,
  },

  // === CSS: Modern Features (130-150) ===
  {
    id: 130,
    category: "CSS",
    subcategory: "Modern CSS",
    level: "beginner",
    q: "CSS Custom Properties (biến CSS) là gì?",
    a: "Biến trong CSS: `--primary: #2563eb`. Truy cập: `var(--primary)`. Khai báo trong `:root` cho global. Có cascade/inheritance, thay đổi runtime bằng JS. Hỗ trợ fallback: `var(--color, blue)`.",
    q_en: "What are CSS custom properties (CSS variables)?",
    a_en: "Variables in CSS: `--primary: #2563eb`. Access with: `var(--primary)`. Declare in `:root` for global scope. They participate in cascade and inheritance, and can be changed at runtime via JavaScript. Support fallbacks: `var(--color, blue)`.",
  },
  {
    id: 131,
    category: "CSS",
    subcategory: "Modern CSS",
    level: "intermediate",
    q: "CSS Custom Properties khác SASS variables thế nào?",
    a: `Hai loại biến trong CSS ecosystem với cách hoạt động khác nhau:

- Custom Properties: runtime, tham gia cascade/inherit, thay đổi bằng JS, scope theo DOM. Mạnh hơn cho theming và responsive.
- SASS variables: compile-time, flat scope, không thay đổi runtime. Tốt hơn cho calculations phức tạp.`,
    q_en: "How do CSS custom properties differ from SASS variables?",
    a_en: `Two types of variables in the CSS ecosystem with different behavior:

- Custom Properties: runtime, participate in cascade/inheritance, changeable via JS, scoped to the DOM. More powerful for theming and responsive design.
- SASS variables: compile-time, flat scope, cannot change at runtime. Better for complex compile-time calculations.`,
  },
  {
    id: 132,
    category: "CSS",
    subcategory: "Modern CSS",
    level: "intermediate",
    q: "@supports rule dùng để làm gì?",
    a: "Feature detection trong CSS. `@supports (display: grid) { ... }` chỉ áp dụng styles khi browser hỗ trợ. Dùng `not`, `and`, `or` kết hợp. Progressive enhancement: base styles + @supports cho features mới.",
    q_en: "What is the @supports rule used for?",
    a_en: "CSS feature detection. `@supports (display: grid) { ... }` only applies styles if the browser supports the feature. Use `not`, `and`, `or` for combinations. Progressive enhancement: base styles + `@supports` for newer features.",
  },
  {
    id: 133,
    category: "CSS",
    subcategory: "Modern CSS",
    level: "intermediate",
    q: "CSS logical properties là gì?",
    a: "CSS logical properties thay thế các physical properties (left, right, top, bottom) bằng các giá trị logic (inline-start, inline-end, block-start, block-end) để CSS tự động thích ứng theo hướng viết của ngôn ngữ. Ví dụ, `margin-inline-start` thay cho `margin-left` — khi chuyển từ LTR (tiếng Anh) sang RTL (tiếng Ả Rập), margin sẽ tự động đổi sang bên phải mà không cần viết thêm CSS. Tương tự, `padding-block-start` thay `padding-top`, `border-inline-end` thay `border-right`. Đây là tính năng quan trọng cho internationalization (i18n) và giúp tạo layout linh hoạt hơn với mọi ngôn ngữ.",
    q_en: "What are CSS logical properties?",
    a_en: "CSS logical properties replace physical directional properties (left, right, top, bottom) with logical equivalents (inline-start, inline-end, block-start, block-end) so CSS automatically adapts to the writing direction. For example, `margin-inline-start` replaces `margin-left` — when switching from LTR (English) to RTL (Arabic), the margin automatically moves to the right without extra CSS. Similarly, `padding-block-start` replaces `padding-top`, and `border-inline-end` replaces `border-right`. This is essential for internationalization (i18n) and creates more flexible layouts across all languages.",
  },
  {
    id: 134,
    category: "CSS",
    subcategory: "Modern CSS",
    level: "intermediate",
    q: "aspect-ratio property dùng để làm gì?",
    a: "Duy trì tỷ lệ width/height. `aspect-ratio: 16/9` hoặc `aspect-ratio: 1` (square). Thay thế padding-bottom hack cũ. Kết hợp object-fit cho images/videos giữ tỷ lệ trong container.",
    q_en: "What is the aspect-ratio property used for?",
    a_en: "Maintains a width-to-height ratio. `aspect-ratio: 16/9` or `aspect-ratio: 1` (square). Replaces the old padding-bottom hack. Combine with `object-fit` to keep images/videos in ratio within their container.",
  },
  {
    id: 135,
    category: "CSS",
    subcategory: "Modern CSS",
    level: "intermediate",
    q: "scroll-snap CSS là gì?",
    a: "CSS scroll-snap là tính năng cho phép tạo hiệu ứng scroll 'bắt dính' vào các vị trí cố định mà không cần JavaScript. Container cần khai báo `scroll-snap-type: x mandatory` (hoặc `y` cho scroll dọc), còn mỗi item con khai báo `scroll-snap-align: start` (hoặc `center`, `end`) để chỉ định điểm bắt dính. Giá trị `mandatory` buộc scroll phải dừng chính xác tại điểm snap, trong khi `proximity` chỉ snap khi scroll dừng đủ gần. Tính năng này rất hữu ích để tạo carousels, image galleries, full-page scroll sections mà không cần thư viện JS bên ngoài.",
    q_en: "What is CSS scroll-snap?",
    a_en: "CSS scroll-snap lets you create a 'snapping' scroll effect that locks to fixed positions without JavaScript. The container declares `scroll-snap-type: x mandatory` (or `y` for vertical scroll), and each child item declares `scroll-snap-align: start` (or `center`, `end`) to specify the snap point. The `mandatory` value forces the scroll to stop exactly at a snap point, while `proximity` only snaps when the scroll stops close enough. Useful for carousels, image galleries, and full-page scroll sections without external JS libraries.",
  },
  {
    id: 136,
    category: "CSS",
    subcategory: "Modern CSS",
    level: "advanced",
    q: "Color spaces mới trong CSS (oklch, lab) là gì?",
    a: "oklch/oklab: perceptually uniform color spaces - thay đổi lightness/chroma/hue đều đặn hơn sRGB. Gamut rộng hơn (P3 displays). `color: oklch(70% 0.15 250)`. Tốt hơn cho design systems, gradients không bị muddy.",
    q_en: "What are new color spaces in CSS (oklch, lab)?",
    a_en: "oklch/oklab: perceptually uniform color spaces — changes in lightness/chroma/hue appear visually even, unlike sRGB. Wider color gamut (P3 displays). `color: oklch(70% 0.15 250)`. Better for design systems and gradients that don't look muddy.",
  },
  {
    id: 137,
    category: "CSS",
    subcategory: "Modern CSS",
    level: "advanced",
    q: "CSS Nesting native hoạt động thế nào?",
    a: "CSS Nesting cho phép viết selectors lồng nhau trực tiếp trong CSS thuần, tương tự cú pháp SASS mà không cần preprocessor. Ký tự `&` đại diện cho selector cha, ví dụ: `.card { & .title { font-size: 1.5rem; } &:hover { box-shadow: 0 4px 8px rgba(0,0,0,0.1); } }`. Với relaxed syntax (Chrome 120+, Safari 17.2+, Firefox 117+), có thể bỏ `&` khi nesting element selectors: `.card { h2 { ... } }`. Tính năng này giúp CSS dễ đọc hơn, giảm lặp selector, và giảm đáng kể nhu cầu sử dụng SASS/SCSS chỉ vì mục đích nesting.",
    q_en: "How does native CSS Nesting work?",
    a_en: "CSS Nesting allows writing nested selectors directly in plain CSS without a preprocessor, similar to SASS syntax. The `&` character represents the parent selector. \n\n**Example:** `.card { & .title { font-size: 1.5rem; } &:hover { box-shadow: 0 4px 8px rgba(0,0,0,0.1); } }`. With relaxed syntax (Chrome 120+, Safari 17.2+, Firefox 117+), you can omit `&` when nesting element selectors: `.card { h2 { ... } }`. This makes CSS more readable, reduces selector repetition, and significantly reduces the need for SASS/SCSS just for nesting.",
  },
  {
    id: 138,
    category: "CSS",
    subcategory: "Modern CSS",
    level: "advanced",
    q: "@layer (Cascade Layers) là gì?",
    a: "Quản lý specificity theo layers: `@layer base, components, utilities`. Styles trong layer có thứ tự ưu tiên rõ ràng bất kể specificity. Layer sau override layer trước. Giải quyết specificity wars.",
    q_en: "What are @layer (Cascade Layers)?",
    a_en: "Manages specificity using named layers: `@layer base, components, utilities`. Styles in later layers override earlier ones regardless of specificity. This eliminates specificity wars and provides a clear, predictable layering system.",
  },

  // === CSS: Architecture & Performance (139-155) ===
  {
    id: 139,
    category: "CSS",
    subcategory: "Architecture",
    level: "beginner",
    q: "BEM (Block Element Modifier) là gì?",
    a: `Naming convention CSS: \`block__element--modifier\`. Tránh nesting sâu, dễ maintain.

- Block: component độc lập — \`.card\`
- Element: phần trong block — \`.card__title\`
- Modifier: biến thể — \`.card--featured\``,
    q_en: "What is BEM (Block Element Modifier)?",
    a_en: `A CSS naming convention: \`block__element--modifier\`. Avoids deep nesting and is easy to maintain.

- Block: standalone component — \`.card\`
- Element: a part inside the block — \`.card__title\`
- Modifier: a variation — \`.card--featured\``,
  },
  {
    id: 140,
    category: "CSS",
    subcategory: "Architecture",
    level: "intermediate",
    q: "Utility-first CSS (Tailwind) là gì?",
    a: 'Methodology viết styles bằng utility classes nhỏ trực tiếp trong HTML: `class="flex items-center gap-4 p-4 bg-white rounded-lg"`. Tailwind CSS là framework phổ biến nhất. \n\n**Ưu điểm:** phát triển nhanh, design consistent, không đặt tên class, tree-shake tự động xóa unused CSS (bundle nhỏ). Nhược: HTML verbose, learning curve ban đầu. So với BEM: ít abstraction hơn, productivity cao hơn cho hầu hết teams.',
    q_en: "What is utility-first CSS (Tailwind)?",
    a_en: 'A methodology of writing styles using small utility classes directly in HTML: `class="flex items-center gap-4 p-4 bg-white rounded-lg"`. Tailwind CSS is the most popular framework. \n\n**Benefits:** fast development, consistent design, no class naming needed, automatic tree-shaking (small bundle). Downside: verbose HTML, initial learning curve. Compared to BEM: less abstraction, higher productivity for most teams.',
  },
  {
    id: 141,
    category: "CSS",
    subcategory: "Architecture",
    level: "intermediate",
    q: "CSS Modules là gì?",
    a: "File CSS scoped theo component, class names tự động unique (hash). `import styles from './Button.module.css'`. Tránh conflicts, dùng với React/Next.js. Compile time, không runtime overhead.",
    q_en: "What are CSS Modules?",
    a_en: "CSS files scoped to a component, with class names automatically made unique (hashed). `import styles from './Button.module.css'`. Prevents class name conflicts. Used with React/Next.js. Compile-time, no runtime overhead.",
  },
  {
    id: 142,
    category: "CSS",
    subcategory: "Architecture",
    level: "intermediate",
    q: "CSS-in-JS là gì? Ưu nhược điểm?",
    a: "CSS-in-JS là phương pháp viết CSS trực tiếp trong JavaScript, với các thư viện phổ biến như styled-components và Emotion. Ưu điểm chính là styles được scoped tự động theo component nên không lo xung đột class names, hỗ trợ dynamic styling dựa trên props/state, và code CSS nằm cùng chỗ với component logic giúp dễ quản lý. Nhược điểm là có runtime cost vì phải generate CSS tại thời điểm chạy, tăng bundle size, và cấu hình SSR phức tạp hơn. Xu hướng mới đang chuyển sang zero-runtime CSS-in-JS như Vanilla Extract và Panda CSS — compile CSS tại build time nên không có runtime overhead.",
    q_en: "What is CSS-in-JS? What are its pros and cons?",
    a_en: "CSS-in-JS writes CSS directly inside JavaScript, with popular libraries like styled-components and Emotion. \n\n**Pros:** styles are automatically scoped to components (no class conflicts), supports dynamic styling based on props/state, and keeps CSS co-located with component logic. \n\n**Cons:** runtime cost because CSS is generated at runtime, increases bundle size, and SSR configuration is more complex. The newer trend is zero-runtime CSS-in-JS like Vanilla Extract and Panda CSS — which compile CSS at build time with no runtime overhead.",
  },
  {
    id: 143,
    category: "CSS",
    subcategory: "Performance",
    level: "intermediate",
    q: "Critical CSS là gì?",
    a: 'CSS tối thiểu cần thiết để render above-the-fold content (phần user nhìn thấy đầu tiên). Cách dùng: inline critical CSS trong `<style>` tag ở `<head>`, phần CSS còn lại load async bằng `<link rel="preload">` hoặc JS injection. Giải quyết vấn đề render-blocking CSS. Tools: Critical (npm), Critters (Webpack plugin). Cải thiện FCP và LCP đáng kể, đặc biệt trên mobile/3G.',
    q_en: "What is critical CSS?",
    a_en: 'The minimum CSS needed to render above-the-fold content (the first thing users see). Approach: inline critical CSS in a `<style>` tag in `<head>`, and load the rest asynchronously via `<link rel="preload">` or JS injection. Solves the render-blocking CSS problem. Tools: Critical (npm), Critters (Webpack plugin). Significantly improves FCP and LCP, especially on mobile/3G.',
  },
  {
    id: 144,
    category: "CSS",
    subcategory: "Performance",
    level: "intermediate",
    q: "PurgeCSS dùng để làm gì?",
    a: "Xóa CSS không sử dụng bằng cách scan HTML/JS files tìm class names. Giảm bundle size đáng kể (đặc biệt với Tailwind). Tích hợp sẵn trong Tailwind CSS. Cấu hình content paths chính xác để tránh xóa nhầm.",
    q_en: "What is PurgeCSS used for?",
    a_en: "Removes unused CSS by scanning HTML/JS files for class names. Drastically reduces bundle size (especially with Tailwind). Built into Tailwind CSS by default. Configure content paths carefully to avoid accidentally removing needed styles.",
  },
  {
    id: 145,
    category: "CSS",
    subcategory: "Performance",
    level: "intermediate",
    q: "font-display property có những giá trị nào?",
    a: "`auto` (browser quyết định). `block` (ẩn text đến khi font load). `swap` (hiện fallback, swap khi ready - khuyến nghị). `fallback` (swap nhanh hoặc giữ fallback). `optional` (dùng nếu cached, không nếu chưa). swap tốt cho hiển thị nhanh (tránh FOIT) nhưng có thể gây CLS nếu không dùng `size-adjust`. Dùng `optional` nếu ưu tiên CLS.",
    q_en: "What values does font-display accept?",
    a_en: "`auto` (browser decides). `block` (hides text until font loads). `swap` (shows fallback, swaps when ready — recommended). `fallback` (swaps quickly or keeps fallback). `optional` (uses font if cached, otherwise uses fallback permanently). `swap` is best for fast text display (avoids FOIT) but may cause CLS if not paired with `size-adjust`. Use `optional` if you prioritize a stable CLS.",
  },
  {
    id: 146,
    category: "CSS",
    subcategory: "Performance",
    level: "advanced",
    q: "CSS containment (contain property) là gì?",
    a: "`contain: layout paint size` giới hạn scope rendering của element. Browser tối ưu: không recalculate bên ngoài contain boundary. `content-visibility: auto` auto-skip rendering off-screen elements. Cải thiện rendering performance.",
    q_en: "What is CSS containment (the contain property)?",
    a_en: "`contain: layout paint size` limits the rendering scope of an element. The browser optimizes by not recalculating outside the contain boundary. `content-visibility: auto` automatically skips rendering off-screen elements. Both improve rendering performance.",
  },
  {
    id: 147,
    category: "CSS",
    subcategory: "Performance",
    level: "advanced",
    q: "Layout thrashing là gì? Cách tránh?",
    a: "Đọc layout property (offsetHeight) rồi ngay lập tức ghi style → browser phải recalculate layout liên tục. Tránh bằng: batch reads trước writes, dùng transform thay top/left, dùng requestAnimationFrame.",
    q_en: "What is layout thrashing? How do you avoid it?",
    a_en: "Reading a layout property (like `offsetHeight`) immediately followed by writing a style forces the browser to repeatedly recalculate layout. Avoid it by: batching all reads before writes, using transform instead of top/left, and using requestAnimationFrame.",
  },

  // === CSS: Advanced Topics (148-200) ===
  {
    id: 148,
    category: "CSS",
    subcategory: "Advanced",
    level: "intermediate",
    q: "Backdrop-filter dùng để làm gì?",
    a: "Property `backdrop-filter` áp dụng các hiệu ứng filter (blur, brightness, contrast, grayscale) cho phần background nằm phía sau element, thay vì cho chính element đó. Ví dụ, `backdrop-filter: blur(10px)` kết hợp với `background: rgba(255,255,255,0.3)` tạo hiệu ứng frosted glass (kính mờ) rất phổ biến trong thiết kế hiện đại. Để hoạt động đúng, element phải có background semi-transparent để phần nền phía sau có thể nhìn xuyên qua. Ứng dụng thực tế bao gồm navigation bars trong suốt, modal overlays, và card designs kiểu glassmorphism.",
    q_en: "What is the backdrop-filter property used for?",
    a_en: "`backdrop-filter` applies filter effects (blur, brightness, contrast, grayscale) to the area behind an element, rather than to the element itself. For example, `backdrop-filter: blur(10px)` combined with `background: rgba(255,255,255,0.3)` creates a frosted glass effect popular in modern design. The element must have a semi-transparent background so the area behind it shows through. Real-world uses: transparent navigation bars, modal overlays, and glassmorphism card designs.",
  },
  {
    id: 149,
    category: "CSS",
    subcategory: "Advanced",
    level: "intermediate",
    q: "Mix-blend-mode dùng để làm gì?",
    a: "Blend colors giữa element và background (giống Photoshop blend modes). Values: multiply, screen, overlay, darken, lighten, color-dodge, etc. Dùng cho text effects, image overlays, creative designs.",
    q_en: "What is mix-blend-mode used for?",
    a_en: "Blends colors between an element and its background (similar to Photoshop blend modes). Values: multiply, screen, overlay, darken, lighten, color-dodge, etc. Used for text effects, image overlays, and creative designs.",
  },
  {
    id: 150,
    category: "CSS",
    subcategory: "Advanced",
    level: "intermediate",
    q: "CSS filters có những loại nào?",
    a: "`blur()`, `brightness()`, `contrast()`, `grayscale()`, `hue-rotate()`, `invert()`, `opacity()`, `saturate()`, `sepia()`, `drop-shadow()`. Kết hợp nhiều filters: `filter: blur(2px) brightness(1.2)`. GPU-accelerated.",
    q_en: "What CSS filter types are available?",
    a_en: "`blur()`, `brightness()`, `contrast()`, `grayscale()`, `hue-rotate()`, `invert()`, `opacity()`, `saturate()`, `sepia()`, `drop-shadow()`. Combine multiple filters: `filter: blur(2px) brightness(1.2)`. GPU-accelerated.",
  },
  {
    id: 151,
    category: "CSS",
    subcategory: "Advanced",
    level: "intermediate",
    q: "Gradient (linear, radial, conic) là gì?",
    a: "`linear-gradient(direction, color1, color2)`: dải màu thẳng. `radial-gradient()`: hình tròn/elip. `conic-gradient()`: hình quạt (pie chart). Dùng cho backgrounds, borders, text effects. Có thể repeating.",
    q_en: "What are linear, radial, and conic gradients?",
    a_en: "`linear-gradient(direction, color1, color2)`: straight color strip. `radial-gradient()`: circular or elliptical. `conic-gradient()`: fan/pie-shaped. Used for backgrounds, borders, and text effects. All support `repeating-` variants.",
  },
  {
    id: 152,
    category: "CSS",
    subcategory: "Advanced",
    level: "intermediate",
    q: "object-fit và object-position dùng cho gì?",
    a: "`object-fit` kiểm soát cách image/video fit trong container: `cover` (fill, có thể crop), `contain` (fit, có thể letterbox), `fill` (stretch), `none`. `object-position` set vị trí focal point.",
    q_en: "What are object-fit and object-position used for?",
    a_en: "`object-fit` controls how an image/video fits within its container: `cover` (fills, may crop), `contain` (fits, may letterbox), `fill` (stretches), `none`. `object-position` sets the focal point of the content within the container.",
  },
  {
    id: 153,
    category: "CSS",
    subcategory: "Advanced",
    level: "intermediate",
    q: "clip-path dùng để làm gì?",
    a: "Cắt element theo hình dạng: `circle()`, `ellipse()`, `polygon()`, `inset()`, `path()`. \n\n**Ví dụ:** `clip-path: polygon(50% 0, 100% 100%, 0 100%)` tạo tam giác. Dùng cho creative shapes, reveal effects.",
    q_en: "What is clip-path used for?",
    a_en: "Clips an element into a shape: `circle()`, `ellipse()`, `polygon()`, `inset()`, `path()`. \n\n**Example:** `clip-path: polygon(50% 0, 100% 100%, 0 100%)` creates a triangle. Used for creative shapes and reveal effects.",
  },
  {
    id: 154,
    category: "CSS",
    subcategory: "Advanced",
    level: "intermediate",
    q: "Pointer-events property dùng khi nào?",
    a: "`pointer-events: none` làm element 'transparent' với mouse/touch - events pass through đến element phía dưới. Dùng cho overlays không cản tương tác, disabled states, decorative elements.",
    q_en: "When should you use pointer-events?",
    a_en: "`pointer-events: none` makes an element 'transparent' to mouse/touch — events pass through to the element below. Used for overlays that shouldn't block interaction, disabled states, and decorative elements.",
  },
  {
    id: 155,
    category: "CSS",
    subcategory: "Advanced",
    level: "advanced",
    q: "Scroll-driven animations là gì?",
    a: "Scroll-driven animations là API CSS mới cho phép gắn animation theo vị trí scroll thay vì theo thời gian. Có hai loại timeline: `animation-timeline: scroll()` liên kết animation với scroll progress của một scroll container (ví dụ progress bar chạy từ 0% đến 100% khi cuộn trang), và `animation-timeline: view()` kích hoạt animation khi element đi vào hoặc rời khỏi viewport. API này thay thế hoàn toàn các JS scroll event listeners vốn tốn performance, phù hợp cho hiệu ứng parallax, reveal animations, và progress indicators. Hiện đã hỗ trợ trên Chrome 115+ và Firefox 128+.",
    q_en: "What are scroll-driven animations?",
    a_en: "A new CSS API that ties animations to scroll position instead of time. Two timeline types: `animation-timeline: scroll()` links an animation to the scroll progress of a scroll container (e.g., a progress bar going 0% to 100% as the user scrolls); `animation-timeline: view()` triggers when an element enters or leaves the viewport. This replaces performance-expensive JS scroll event listeners and is great for parallax, reveal animations, and progress indicators. Supported in Chrome 115+ and Firefox 128+.",
  },
  {
    id: 156,
    category: "CSS",
    subcategory: "Advanced",
    level: "advanced",
    q: "CSS Anchor Positioning là gì?",
    a: "API mới cho phép position element relative với anchor element khác không cần JS. `anchor-name`, `position-anchor`, `inset-area`. Dùng cho tooltips, popovers, dropdown menus. Chrome 125+.",
    q_en: "What is CSS Anchor Positioning?",
    a_en: "A new API for positioning elements relative to any anchor element without JavaScript. Uses `anchor-name`, `position-anchor`, and `inset-area`. Ideal for tooltips, popovers, and dropdown menus. Chrome 125+.",
  },
  {
    id: 157,
    category: "CSS",
    subcategory: "Advanced",
    level: "advanced",
    q: "@property rule dùng để làm gì?",
    a: "Rule `@property` cho phép đăng ký CSS custom property với kiểu dữ liệu cụ thể, giá trị mặc định và quy tắc kế thừa. \n\n**Ví dụ:** `@property --hue { syntax: '<angle>'; initial-value: 0deg; inherits: false; }` khai báo biến `--hue` có kiểu là góc. Lợi ích lớn nhất là cho phép animate các custom properties — bình thường browser không biết cách interpolate custom properties nên không thể transition, nhưng với `@property` khai báo kiểu `<color>` hoặc `<angle>`, browser có thể animate mượt mà giữa các giá trị. Ứng dụng phổ biến nhất là animate gradient colors và tạo hiệu ứng chuyển màu phức tạp.",
    q_en: "What is the @property rule used for?",
    a_en: "The `@property` rule registers a CSS custom property with a specific type, default value, and inheritance behavior. \n\n**Example:** `@property --hue { syntax: '<angle>'; initial-value: 0deg; inherits: false; }` declares `--hue` as an angle type. The biggest benefit is enabling animation of custom properties — browsers normally can't interpolate custom properties, but declaring a type like `<color>` or `<angle>` with `@property` allows smooth transitions. Most commonly used to animate gradient colors and create complex color shift effects.",
  },
  {
    id: 158,
    category: "CSS",
    subcategory: "Advanced",
    level: "advanced",
    q: "CSS View Transitions API là gì?",
    a: "View Transitions API tạo hiệu ứng chuyển đổi mượt mà giữa các trạng thái DOM hoặc giữa các trang. Khi gọi `document.startViewTransition()`, browser tự động chụp snapshot trạng thái trước, áp dụng thay đổi DOM, rồi animate sự khác biệt giữa snapshot cũ và mới. Có hai loại: same-document transitions (Chrome 111+) dùng cho thay đổi trong cùng trang như list reordering, tab switching, morph animations; và cross-document transitions (Chrome 126+) dùng cho chuyển trang trong MPA. Đây là bước tiến lớn giúp tạo trải nghiệm page transitions giống native app mà trước đây cần rất nhiều JavaScript phức tạp.",
    q_en: "What is the CSS View Transitions API?",
    a_en: "The View Transitions API creates smooth animated transitions between DOM states or between pages. When you call `document.startViewTransition()`, the browser automatically captures a snapshot of the previous state, applies the DOM change, then animates the difference. Two types: same-document transitions (Chrome 111+) for in-page changes like list reordering, tab switching, and morph animations; and cross-document transitions (Chrome 126+) for page navigation in MPAs. A major step forward for achieving native app-like page transitions that previously required complex JavaScript.",
  },
  {
    id: 159,
    category: "CSS",
    subcategory: "Advanced",
    level: "advanced",
    q: "Popover API và CSS liên quan là gì?",
    a: `Popover API là tính năng native HTML tạo popover, tooltip, dropdown mà không cần JS cho logic hiển thị/ẩn.

- Cách dùng: thêm \`popover\` vào element, \`popovertarget\` trên trigger: \`<button popovertarget="my-pop">Open</button><div id="my-pop" popover>Content</div>\`
- Browser tự xử lý: top layer (trên mọi z-index), light dismiss (click ngoài để đóng), focus management.
- CSS styling: \`:popover-open\` khi đang mở, \`::backdrop\` cho overlay phía sau.
- Thay thế nhiều thư viện JS popover phức tạp trước đây.`,
    q_en: "What is the Popover API and its related CSS?",
    a_en: `The Popover API is a native HTML feature for creating popovers, tooltips, and dropdowns without JavaScript for show/hide logic.

- Usage: add \`popover\` to an element and \`popovertarget\` on the trigger: \`<button popovertarget="my-pop">Open</button><div id="my-pop" popover>Content</div>\`
- Browser handles: top layer placement (above all z-index), light dismiss (click outside to close), focus management.
- CSS styling: \`:popover-open\` when visible, \`::backdrop\` for the overlay behind.
- Replaces many complex JS popover libraries.`,
  },
  {
    id: 160,
    category: "CSS",
    subcategory: "Advanced",
    level: "advanced",
    q: "Tại sao nên dùng CSS Grid cho page layout thay vì float/position?",
    a: "Grid: 2D layout, responsive native (auto-fit, minmax), semantic, overlap dễ, gap spacing. Float: hack từ text wrapping, clearfix issues, fragile. Position: thoát flow, không responsive. Grid là standard hiện đại, được tất cả browsers hỗ trợ.",
    q_en: "Why should you use CSS Grid for page layouts instead of float/position?",
    a_en: "Grid: 2D layout, natively responsive (auto-fit, minmax), semantic, supports overlapping easily, has gap spacing. Float: a hack repurposed from text wrapping, requires clearfix, fragile. Position: removes from flow, not responsive. Grid is the modern standard, supported by all browsers.",
  },

  // === Thêm CSS nâng cao (161-200) ===
  {
    id: 161,
    category: "CSS",
    subcategory: "Advanced",
    level: "intermediate",
    q: "Text truncation (ellipsis) thực hiện thế nào?",
    a: 'Để cắt ngắn text và hiển thị dấu ba chấm (...) có hai cách tùy theo số dòng. Với single line, dùng ba properties kết hợp: `white-space: nowrap` ngăn text xuống dòng, `overflow: hidden` ẩn phần tràn, và `text-overflow: ellipsis` thêm dấu "...". Với multi-line (ví dụ giới hạn 3 dòng), dùng: `display: -webkit-box; -webkit-box-orient: vertical; -webkit-line-clamp: 3; overflow: hidden`. Mặc dù mang prefix `-webkit-`, thuộc tính `line-clamp` đã được hỗ trợ rộng rãi trên tất cả các trình duyệt hiện đại.',
    q_en: "How do you implement text truncation with ellipsis?",
    a_en: "There are two approaches depending on the number of lines. For single line, combine three properties: `white-space: nowrap` (prevents text from wrapping), `overflow: hidden` (hides overflow), and `text-overflow: ellipsis` (adds '...'). For multi-line (e.g., limit to 3 lines): `display: -webkit-box; -webkit-box-orient: vertical; -webkit-line-clamp: 3; overflow: hidden`. Despite the `-webkit-` prefix, `line-clamp` is now widely supported across all modern browsers.",
  },
  {
    id: 162,
    category: "CSS",
    subcategory: "Advanced",
    level: "intermediate",
    q: "CSS counter là gì?",
    a: "Biến đếm tự động trong CSS. `counter-reset: section` khởi tạo. `counter-increment: section` tăng. `content: counter(section)` hiển thị trong ::before/::after. Dùng cho tự động đánh số headings, lists tùy chỉnh.",
    q_en: "What are CSS counters?",
    a_en: "Automatic counting variables in CSS. `counter-reset: section` initializes. `counter-increment: section` increments. `content: counter(section)` displays the value in `::before`/`::after`. Used for auto-numbering headings and customizing list styles.",
  },
  {
    id: 163,
    category: "CSS",
    subcategory: "Advanced",
    level: "intermediate",
    q: "Variable fonts là gì?",
    a: "1 file font chứa nhiều variations (weight, width, slant) thay vì nhiều files riêng. Giảm requests và file size. Dùng `font-variation-settings` hoặc CSS properties (font-weight: 100-900 liên tục). Smooth animations giữa weights.",
    q_en: "What are variable fonts?",
    a_en: "A single font file that contains multiple variations (weight, width, slant) instead of separate files for each. Reduces HTTP requests and file size. Use `font-variation-settings` or standard CSS properties (`font-weight: 100-900` as a continuous range). Enables smooth animations between font weights.",
  },
  {
    id: 164,
    category: "CSS",
    subcategory: "Advanced",
    level: "intermediate",
    q: "prefers-reduced-motion media query dùng khi nào?",
    a: "Tôn trọng accessibility settings của user muốn giảm animations. `@media (prefers-reduced-motion: reduce) { * { animation: none; transition: none; } }`. Quan trọng cho users có vestibular disorders.",
    q_en: "When should you use the prefers-reduced-motion media query?",
    a_en: "To respect accessibility settings for users who prefer less animation. `@media (prefers-reduced-motion: reduce) { * { animation: none; transition: none; } }`. Critical for users with vestibular disorders who can experience nausea from motion.",
  },
  {
    id: 165,
    category: "CSS",
    subcategory: "Advanced",
    level: "intermediate",
    q: "prefers-color-scheme media query là gì?",
    a: "Media query `prefers-color-scheme` cho phép phát hiện người dùng đang sử dụng chế độ dark hay light mode trên hệ điều hành, từ đó áp dụng styles phù hợp. \n\n**Ví dụ:** `@media (prefers-color-scheme: dark) { body { background: #1a1a1a; color: #eee; } }`. Cách triển khai phổ biến nhất là kết hợp với CSS custom properties: khai báo biến màu ở `:root` cho light mode (base styles), rồi override trong media query dark để đổi bảng màu. Ngoài ra có thể dùng class-based toggle (thêm class `.dark` vào `<html>`) để cho phép user tự chọn theme và lưu preference vào localStorage.",
    q_en: "What is the prefers-color-scheme media query?",
    a_en: "It detects whether the user has their OS set to dark or light mode, allowing you to apply appropriate styles. \n\n**Example:** `@media (prefers-color-scheme: dark) { body { background: #1a1a1a; color: #eee; } }`. The most common approach combines it with CSS custom properties: declare color variables in `:root` for light mode, then override them in the media query for dark mode. Alternatively, use a class-based toggle (adding a `.dark` class to `<html>`) so users can manually choose a theme and save their preference in localStorage.",
  },
  {
    id: 166,
    category: "CSS",
    subcategory: "Responsive",
    level: "intermediate",
    q: "picture element và source dùng thế nào?",
    a: `\`<picture>\` chứa nhiều \`<source>\` với media conditions hoặc type khác nhau, fallback \`<img>\`.

- Art direction: thay đổi ảnh khác nhau theo viewport.
- Format: cung cấp WebP/AVIF với fallback JPEG.
- Browser tự chọn source phù hợp nhất.`,
    q_en: "How are the picture element and source used?",
    a_en: `\`<picture>\` contains multiple \`<source>\` elements with different media conditions or file types, with a fallback \`<img>\`.

- Art direction: serve different images per viewport size.
- Format selection: provide WebP/AVIF with JPEG fallback.
- The browser automatically picks the most appropriate source.`,
  },
  {
    id: 167,
    category: "CSS",
    subcategory: "Responsive",
    level: "advanced",
    q: "Fluid typography công thức thế nào?",
    a: "Fluid typography là kỹ thuật để font-size tự co giãn mượt mà theo viewport width, thay vì nhảy đột ngột qua media queries. Cách phổ biến nhất là dùng `clamp()`: `font-size: clamp(1rem, 0.5rem + 1.5vw, 2rem)` — font sẽ tối thiểu 1rem, tối đa 2rem, và co giãn tuyến tính ở giữa. Công thức tính giá trị preferred: `minSize + (maxSize - minSize) * ((100vw - minViewport) / (maxViewport - minViewport))`, trong đó minViewport và maxViewport là khoảng viewport bạn muốn font co giãn. Để tính nhanh có thể dùng tool utopia.fyi — chỉ cần nhập min/max font size và viewport range, tool sẽ generate sẵn giá trị clamp.",
    q_en: "What is the formula for fluid typography?",
    a_en: "Fluid typography smoothly scales font size with viewport width instead of jumping abruptly at media query breakpoints. The most common approach uses `clamp()`: `font-size: clamp(1rem, 0.5rem + 1.5vw, 2rem)` — the font scales between 1rem and 2rem proportionally. The preferred value formula is: `minSize + (maxSize - minSize) * ((100vw - minViewport) / (maxViewport - minViewport))`. For quick calculation, use the tool at utopia.fyi — just enter min/max font size and viewport range and it generates the clamp value.",
  },
  {
    id: 168,
    category: "CSS",
    subcategory: "Architecture",
    level: "intermediate",
    q: "Tailwind CSS so với CSS thuần - khi nào dùng gì?",
    a: "Tailwind: projects lớn, team, consistent design, rapid prototyping. CSS thuần: projects nhỏ, custom creative designs, learning. Tailwind tốt cho apps, CSS thuần tốt cho sites cần unique styling.",
    q_en: "Tailwind CSS vs plain CSS — when should you use each?",
    a_en: "Tailwind: large projects, teams, consistent design systems, rapid prototyping. Plain CSS: small projects, unique creative designs, learning CSS fundamentals. Tailwind excels for apps; plain CSS excels for sites requiring highly unique styling.",
  },
  {
    id: 169,
    category: "CSS",
    subcategory: "Architecture",
    level: "advanced",
    q: "Design tokens là gì?",
    a: 'Design tokens là các giá trị thiết kế cơ bản (colors, spacing, typography, shadows, border-radius) được đặt tên và lưu trữ dưới dạng platform-agnostic, thường là file JSON. \n\n**Ví dụ:** `{ "color-primary": "#2563eb", "spacing-md": "16px" }`. Tokens này sau đó được transform sang nhiều format khác nhau như CSS custom properties, SASS variables, hoặc JavaScript objects — tạo ra single source of truth cho toàn bộ design system. Khi designer thay đổi một giá trị trong tokens, tất cả platforms (web, iOS, Android) đều cập nhật theo. Các tools phổ biến để quản lý và build tokens bao gồm Style Dictionary (Amazon) và Figma Tokens plugin.',
    q_en: "What are design tokens?",
    a_en: 'Design tokens are the fundamental design values (colors, spacing, typography, shadows, border-radius) that are named and stored in a platform-agnostic format, typically JSON. \n\n**Example:** `{ "color-primary": "#2563eb", "spacing-md": "16px" }`. These tokens are then transformed into platform-specific formats like CSS custom properties, SASS variables, or JavaScript objects — creating a single source of truth for an entire design system. When a designer updates a token value, all platforms (web, iOS, Android) update automatically. Popular tools for managing and building tokens include Style Dictionary (Amazon) and the Figma Tokens plugin.',
  },
  {
    id: 170,
    category: "CSS",
    subcategory: "Performance",
    level: "advanced",
    q: "content-visibility: auto là gì?",
    a: "Skip rendering off-screen elements hoàn toàn. Browser chỉ render khi element gần viewport. Cải thiện initial rendering performance đáng kể cho long pages. Cần `contain-intrinsic-size` để giữ scroll height chính xác.",
    q_en: "What is content-visibility: auto?",
    a_en: "Skips rendering off-screen elements entirely. The browser only renders elements as they approach the viewport. Significantly improves initial rendering performance for long pages. Pair with `contain-intrinsic-size` to maintain accurate scroll height.",
  },
  {
    id: 171,
    category: "CSS",
    subcategory: "Advanced",
    level: "intermediate",
    q: "box-shadow vs filter: drop-shadow() khác nhau?",
    a: "`box-shadow` theo hình chữ nhật (box). `drop-shadow` theo shape thực tế (hỗ trợ transparent PNG, clip-path). drop-shadow không hỗ trợ spread value và inset. box-shadow nhanh hơn.",
    q_en: "How does box-shadow differ from filter: drop-shadow()?",
    a_en: "`box-shadow` follows the rectangular box shape. `drop-shadow` follows the actual visual shape (supports transparent PNGs and clip-path). `drop-shadow` doesn't support a spread value or inset. `box-shadow` is faster to render.",
  },
  {
    id: 172,
    category: "CSS",
    subcategory: "Advanced",
    level: "intermediate",
    q: "currentColor keyword dùng để làm gì?",
    a: "Reference đến giá trị `color` hiện tại. Dùng cho: `border-color: currentColor`, `fill: currentColor` (SVG). Kế thừa color từ parent. Giảm lặp code, SVG icons tự thay đổi màu theo text.",
    q_en: "What is the currentColor keyword used for?",
    a_en: "References the current `color` value of an element. Used for: `border-color: currentColor`, `fill: currentColor` (SVG). Inherits color from the parent. Reduces code duplication and allows SVG icons to automatically change color with the text.",
  },
  {
    id: 173,
    category: "CSS",
    subcategory: "Advanced",
    level: "intermediate",
    q: "isolation property dùng để làm gì?",
    a: "`isolation: isolate` tạo stacking context mới. Element bên trong không blend với bên ngoài. Giải quyết z-index conflicts. Dùng khi component cần isolated z-index scope.",
    q_en: "What is the isolation property used for?",
    a_en: "`isolation: isolate` creates a new stacking context. Elements inside do not blend with those outside. Solves z-index conflicts. Use when a component needs an isolated z-index scope.",
  },
  {
    id: 174,
    category: "CSS",
    subcategory: "Advanced",
    level: "advanced",
    q: "CSS Houdini là gì?",
    a: "Tập hợp APIs cho phép extend CSS engine: Paint API (custom backgrounds), Layout API (custom display), Typed OM, Properties & Values API. Cho phép tạo CSS features mới bằng JavaScript. Hỗ trợ còn hạn chế.",
    q_en: "What is CSS Houdini?",
    a_en: "A set of APIs that expose the CSS engine for extension: Paint API (custom backgrounds), Layout API (custom display), Typed OM, and Properties & Values API. Allows creating new CSS features using JavaScript. Browser support is still limited.",
  },
  {
    id: 175,
    category: "CSS",
    subcategory: "Responsive",
    level: "intermediate",
    q: "Breakpoints nên đặt theo thiết bị hay theo content?",
    a: "Nên theo content. Resize browser và thêm breakpoint khi layout 'vỡ'. Không phụ thuộc device-specific sizes (thay đổi liên tục). Content-based breakpoints linh hoạt hơn, future-proof.",
    q_en: "Should breakpoints be set by device or by content?",
    a_en: "By content. Resize the browser and add a breakpoint when the layout 'breaks'. Don't rely on device-specific sizes (they change constantly). Content-based breakpoints are more flexible and future-proof.",
  },
  {
    id: 176,
    category: "CSS",
    subcategory: "Architecture",
    level: "intermediate",
    q: "OOCSS (Object-Oriented CSS) là gì?",
    a: "Tách structure khỏi skin, tách container khỏi content. Tạo reusable objects: `.btn` (structure) + `.btn-primary` (skin). Giảm duplication, tăng scalability. Nicole Sullivan sáng tạo. Nguyên tắc nền tảng cho BEM, Tailwind.",
    q_en: "What is OOCSS (Object-Oriented CSS)?",
    a_en: "Separates structure from skin, and container from content. Creates reusable objects: `.btn` (structure) + `.btn-primary` (skin). Reduces duplication, improves scalability. Created by Nicole Sullivan. The foundational principle behind BEM and Tailwind.",
  },
  {
    id: 177,
    category: "CSS",
    subcategory: "Architecture",
    level: "intermediate",
    q: "SMACSS (Scalable and Modular Architecture for CSS) là gì?",
    a: "Phân loại CSS thành 5 categories: Base (defaults), Layout (page sections), Module (reusable components), State (overrides), Theme (visual theming). Giúp tổ chức CSS quy mô lớn. Jonathan Snook sáng tạo.",
    q_en: "What is SMACSS (Scalable and Modular Architecture for CSS)?",
    a_en: "Categorizes CSS into 5 groups: Base (defaults), Layout (page sections), Module (reusable components), State (overrides), Theme (visual theming). Helps organize large-scale CSS. Created by Jonathan Snook.",
  },
  {
    id: 178,
    category: "CSS",
    subcategory: "Performance",
    level: "intermediate",
    q: "Composite layers trong browser là gì?",
    a: "Browser tách elements thành layers riêng, composite chúng bằng GPU. Trigger composite layer: transform, opacity, will-change, position: fixed. Tách layer = animate mượt hơn nhưng tốn memory. Kiểm tra trong DevTools > Layers.",
    q_en: "What are composite layers in the browser?",
    a_en: "The browser separates elements into distinct layers and composites them using the GPU. Composite layer triggers: transform, opacity, will-change, position: fixed. Separate layers enable smoother animations but consume more memory. Inspect in DevTools > Layers.",
  },
  {
    id: 179,
    category: "CSS",
    subcategory: "Performance",
    level: "intermediate",
    q: "CSS selector performance quan trọng không?",
    a: "Với browser hiện đại, selector performance gần như không đáng lo. Browser đọc selectors phải-sang-trái. Tránh: universal * selector lồng sâu, attribute selectors phức tạp. Focus vào: giảm CSS file size, critical CSS, giảm unused CSS.",
    q_en: "Does CSS selector performance matter?",
    a_en: "With modern browsers, selector performance is almost negligible. Browsers read selectors right-to-left. Avoid: deeply nested universal `*` selectors, complex attribute selectors. Focus on: reducing CSS file size, using critical CSS, and removing unused CSS.",
  },
  {
    id: 180,
    category: "CSS",
    subcategory: "Advanced",
    level: "intermediate",
    q: "Scrollbar styling thực hiện thế nào?",
    a: `Có hai cách style scrollbar trong CSS:

- Webkit (Chrome/Safari): \`::-webkit-scrollbar\`, \`::-webkit-scrollbar-thumb\`, \`::-webkit-scrollbar-track\` — chi tiết hơn nhưng non-standard.
- Standard (Firefox/mới): \`scrollbar-width: thin\`, \`scrollbar-color: thumb-color track-color\` — ít tùy chỉnh hơn nhưng chuẩn W3C.`,
    q_en: "How do you style the scrollbar with CSS?",
    a_en: `There are two approaches to styling scrollbars in CSS:

- Webkit (Chrome/Safari): \`::-webkit-scrollbar\`, \`::-webkit-scrollbar-thumb\`, \`::-webkit-scrollbar-track\` — more detailed but non-standard.
- Standard (Firefox/modern): \`scrollbar-width: thin\`, \`scrollbar-color: thumb-color track-color\` — less customizable but W3C standard.`,
  },
  {
    id: 181,
    category: "CSS",
    subcategory: "Advanced",
    level: "intermediate",
    q: "Shape-outside dùng để làm gì?",
    a: "Wrap text quanh hình dạng phi chữ nhật. `shape-outside: circle(50%)` hoặc `ellipse()`, `polygon()`, `url(image.png)`. Chỉ hoạt động với floated elements. Dùng cho magazine-style text layouts.",
    q_en: "What is shape-outside used for?",
    a_en: "Wraps text around non-rectangular shapes. `shape-outside: circle(50%)`, `ellipse()`, `polygon()`, or `url(image.png)`. Only works with floated elements. Used for magazine-style text layouts.",
  },
  {
    id: 182,
    category: "CSS",
    subcategory: "Advanced",
    level: "intermediate",
    q: "accent-color property là gì?",
    a: "Thay đổi màu mặc định của form controls (checkbox, radio, range, progress) chỉ với 1 property. `accent-color: #2563eb`. Đơn giản nhất để style form controls consistently mà không cần custom components.",
    q_en: "What is the accent-color property?",
    a_en: "Changes the default color of form controls (checkbox, radio, range, progress) with just one property. `accent-color: #2563eb`. The simplest way to consistently style form controls without building custom components.",
  },
  {
    id: 183,
    category: "CSS",
    subcategory: "Advanced",
    level: "intermediate",
    q: "CSS math functions (calc, min, max, clamp) khác nhau?",
    a: "`calc()`: tính toán (mixed units: `calc(100% - 2rem)`). `min()`: giá trị nhỏ nhất. `max()`: giá trị lớn nhất. `clamp(min, preferred, max)`: giới hạn trong khoảng. Kết hợp: `width: min(90%, 1200px)` thay max-width.",
    q_en: "How do CSS math functions (calc, min, max, clamp) differ?",
    a_en: "`calc()`: performs calculations (mixed units: `calc(100% - 2rem)`). `min()`: picks the smaller value. `max()`: picks the larger value. `clamp(min, preferred, max)`: constrains within a range. Combine them: `width: min(90%, 1200px)` as a cleaner alternative to max-width.",
  },
  {
    id: 184,
    category: "CSS",
    subcategory: "Selectors",
    level: "advanced",
    q: ":focus-visible vs :focus khác nhau?",
    a: "`:focus` trigger mọi lúc element nhận focus (cả mouse click). `:focus-visible` chỉ khi focus cần visible indicator (keyboard navigation). Dùng `:focus-visible` cho focus rings: chỉ hiện khi dùng keyboard, không khi click.",
    q_en: "How do :focus-visible and :focus differ?",
    a_en: "`:focus` triggers whenever an element receives focus (including via mouse click). `:focus-visible` triggers only when the focus indicator needs to be visible (keyboard navigation). Use `:focus-visible` for focus rings: show them for keyboard users, hide them for mouse clicks.",
  },
  {
    id: 185,
    category: "CSS",
    subcategory: "Modern CSS",
    level: "advanced",
    q: "CSS scope proposal (@scope) là gì?",
    a: "`@scope (.card) to (.card__content)` giới hạn styles trong phạm vi. Selectors chỉ match bên trong scope root, dừng tại scope limit. Giải quyết style leaking, thay thế Shadow DOM cho component scoping. Chrome 118+.",
    q_en: "What is the @scope proposal in CSS?",
    a_en: "`@scope (.card) to (.card__content)` constrains styles within a scope. Selectors only match inside the scope root and stop at the scope limit. Solves style leaking and serves as a replacement for Shadow DOM in component scoping. Chrome 118+.",
  },
  {
    id: 186,
    category: "CSS",
    subcategory: "Modern CSS",
    level: "advanced",
    q: "color-mix() function dùng để làm gì?",
    a: "Hàm `color-mix()` cho phép trộn hai màu theo tỷ lệ chỉ định ngay trong CSS. \n\n**Ví dụ:** `color-mix(in srgb, red 30%, blue)` trộn 30% đỏ với 70% xanh dương trong color space sRGB. Ứng dụng thực tế rất đa dạng: tạo hover states bằng cách trộn màu gốc với trắng/đen (`color-mix(in oklch, var(--primary), white 20%)` cho hiệu ứng lighten), tạo palette tự động từ một base color, hoặc tạo hiệu ứng tương tự opacity mà không cần rgba. Hàm này hỗ trợ nhiều color spaces như srgb, oklch, lab — trong đó oklch cho kết quả trộn tự nhiên và đồng đều nhất về mặt thị giác.",
    q_en: "What is the color-mix() function used for?",
    a_en: "The `color-mix()` function blends two colors at a specified ratio directly in CSS. \n\n**Example:** `color-mix(in srgb, red 30%, blue)` mixes 30% red with 70% blue in the sRGB color space. Practical uses: hover states by mixing the base color with white/black (`color-mix(in oklch, var(--primary), white 20%)` for a lighten effect), auto-generating palettes from a base color, or creating opacity-like effects without rgba. The function supports multiple color spaces like srgb, oklch, and lab — with oklch giving the most visually natural and uniform blending results.",
  },
  {
    id: 187,
    category: "CSS",
    subcategory: "Modern CSS",
    level: "intermediate",
    q: "Tại sao oklch tốt hơn hsl cho design systems?",
    a: "oklch (lightness, chroma, hue) vượt trội hơn hsl vì nó là perceptually uniform — nghĩa là khi giữ nguyên lightness và chroma mà chỉ thay đổi hue, mắt người vẫn cảm nhận độ sáng như nhau (hsl không đảm bảo điều này, ví dụ màu vàng hsl trông sáng hơn màu xanh dương cùng lightness). Điều này cực kỳ quan trọng khi tạo color palettes cho design system: các màu primary, secondary, error đều có perceived brightness đồng đều, giúp UI nhất quán và dễ đọc. Ngoài ra, oklch hỗ trợ gamut rộng hơn sRGB (hỗ trợ P3 displays), cho phép sử dụng những màu sắc sống động hơn trên các màn hình hiện đại.",
    q_en: "Why is oklch better than hsl for design systems?",
    a_en: "oklch (lightness, chroma, hue) is superior to hsl because it is perceptually uniform — when you keep lightness and chroma the same and only change hue, the perceived brightness remains consistent (hsl doesn't guarantee this: e.g., yellow hsl appears brighter than blue at the same lightness value). This is critical when building color palettes for design systems: primary, secondary, and error colors all have equal perceived brightness, making the UI visually consistent and easy to read. Additionally, oklch supports a wider gamut than sRGB (supports P3 displays), enabling more vivid colors on modern screens.",
  },
  {
    id: 188,
    category: "CSS",
    subcategory: "Advanced",
    level: "advanced",
    q: "Tại sao tránh dùng vw cho font-size đơn lẻ?",
    a: "vw không respects user zoom settings (accessibility issue). Font quá nhỏ trên mobile, quá lớn trên desktop. Dùng `clamp()` kết hợp rem + vw: `clamp(1rem, 0.5rem + 1vw, 1.5rem)`. Đảm bảo min/max bounds.",
    q_en: "Why should you avoid using bare vw for font-size?",
    a_en: "vw alone doesn't respect user zoom settings, which is an accessibility issue. Fonts become too small on mobile and too large on desktop. Instead, use `clamp()` combining rem and vw: `clamp(1rem, 0.5rem + 1vw, 1.5rem)`. This ensures sensible minimum and maximum bounds.",
  },
  {
    id: 189,
    category: "CSS",
    subcategory: "Performance",
    level: "advanced",
    q: "Cách tối ưu Cumulative Layout Shift (CLS)?",
    a: "Để tối ưu CLS (Cumulative Layout Shift — chỉ số đo mức độ layout bị dịch chuyển bất ngờ), cần thực hiện nhiều biện pháp. Đầu tiên, luôn khai báo `width` và `height` (hoặc `aspect-ratio`) cho images và videos để browser reserve đúng không gian trước khi tải xong. Tiếp theo, reserve không gian cố định cho ads, embeds, và dynamic content bằng `min-height` để tránh đẩy nội dung bên dưới khi chúng load. Với web fonts, dùng `font-display: swap` kết hợp `size-adjust` trong `@font-face` để fallback font có kích thước gần giống font chính, giảm layout shift khi font swap. Cuối cùng, tuyệt đối tránh inject nội dung mới phía trên viewport hiện tại (như banner notification) vì sẽ đẩy toàn bộ nội dung xuống.",
    q_en: "How do you optimize Cumulative Layout Shift (CLS)?",
    a_en: "To minimize CLS (which measures unexpected layout shifts), take several steps. First, always declare `width` and `height` (or `aspect-ratio`) on images and videos so the browser reserves space before they load. Second, reserve fixed space for ads, embeds, and dynamic content with `min-height` to prevent content below from shifting. For web fonts, use `font-display: swap` combined with `size-adjust` in `@font-face` so the fallback font matches the primary font's dimensions, reducing the layout shift during font swap. Finally, absolutely avoid injecting new content above the current viewport (like a notification banner) as it pushes all content downward.",
  },
  {
    id: 190,
    category: "CSS",
    subcategory: "Architecture",
    level: "advanced",
    q: "Atomic CSS (functional CSS) philosophy là gì?",
    a: "Mỗi class làm 1 việc: `.flex`, `.mt-4`, `.text-center`. Composition qua nhiều classes. Ưu: bundle size nhỏ (reuse classes), no naming, predictable. Nhược: verbose HTML. Tailwind CSS là implementation phổ biến nhất.",
    q_en: "What is atomic CSS (functional CSS) philosophy?",
    a_en: "Each class does one thing: `.flex`, `.mt-4`, `.text-center`. Composition through multiple classes. \n\n**Pros:** small bundle size (reused classes), no naming needed, predictable behavior. \n\n**Cons:** verbose HTML. Tailwind CSS is its most popular implementation.",
  },
  {
    id: 191,
    category: "CSS",
    subcategory: "Architecture",
    level: "advanced",
    q: "Khi nào nên dùng CSS-in-JS vs CSS Modules vs Tailwind?",
    a: "Mỗi phương pháp phù hợp với từng tình huống khác nhau. CSS-in-JS (styled-components, Emotion) phù hợp khi cần dynamic theming phức tạp dựa trên props/state và xây dựng component libraries, nhưng có runtime cost. CSS Modules phù hợp khi cần scoped styles đơn giản, SSR-friendly và zero runtime overhead — rất tốt cho Next.js projects. Tailwind CSS phù hợp cho rapid development, giữ design consistent qua design tokens, và cho bundle size nhỏ nhờ tree-shaking tự động. Xu hướng hiện tại đang nghiêng về zero-runtime solutions (Tailwind, CSS Modules) vì performance tốt hơn so với runtime CSS-in-JS, đặc biệt với Server Components trong React.",
    q_en: "When should you use CSS-in-JS vs CSS Modules vs Tailwind?",
    a_en: "Each approach suits different scenarios. CSS-in-JS (styled-components, Emotion) is suited for complex dynamic theming based on props/state and building component libraries, but has a runtime cost. CSS Modules are ideal for simple scoped styles, SSR-friendly output, and zero runtime overhead — great for Next.js projects. Tailwind CSS suits rapid development, consistent design via tokens, and small bundles via automatic tree-shaking. The current trend leans toward zero-runtime solutions (Tailwind, CSS Modules) due to better performance, especially with React Server Components.",
  },
  {
    id: 192,
    category: "CSS",
    subcategory: "Advanced",
    level: "intermediate",
    q: "Gradient border thực hiện thế nào?",
    a: "Có ba cách chính để tạo gradient border trong CSS. Cách 1 dùng `border-image: linear-gradient(to right, #f00, #00f) 1` — đơn giản nhất nhưng không hỗ trợ `border-radius` nên không thể bo góc. Cách 2 dùng background gradient kết hợp padding: set background là gradient, rồi dùng một element con hoặc pseudo-element với background solid che phần content, để lộ gradient ở viền — cách này hỗ trợ border-radius. Cách 3 nâng cao hơn dùng `@property` để animate gradient border bằng conic-gradient, tạo hiệu ứng viền xoay rất ấn tượng.",
    q_en: "How do you create a gradient border?",
    a_en: "Three main techniques. First: `border-image: linear-gradient(to right, #f00, #00f) 1` — simplest but doesn't support `border-radius`. Second: use background gradient with padding — set the background as a gradient, then use a pseudo-element with a solid background to cover the content area, revealing the gradient only at the border; this supports border-radius. Third: use `@property` to animate a gradient border with `conic-gradient`, creating an impressive spinning border effect.",
  },
  {
    id: 193,
    category: "CSS",
    subcategory: "Advanced",
    level: "intermediate",
    q: "Dark mode implementation best practices?",
    a: "Để triển khai dark mode hiệu quả, bước quan trọng nhất là dùng CSS custom properties cho tất cả màu sắc thay vì hardcode — ví dụ khai báo `--bg: #fff; --text: #1a1a1a` ở `:root` rồi override trong dark mode. Có hai cách detect: `@media (prefers-color-scheme: dark)` tự động theo system setting, hoặc class-based toggle (thêm class `.dark` vào `<html>`) cho phép user chủ động chọn và lưu preference vào localStorage. Khi chọn bảng màu dark, nên dùng oklch color space để tạo palette tự động với perceived brightness đồng đều, và luôn kiểm tra contrast ratios đạt WCAG AA (4.5:1 cho text thường). Tránh dùng pure black (#000) cho background vì gây mỏi mắt — nên dùng dark gray (#1a1a1a hoặc tương tự).",
    q_en: "What are dark mode implementation best practices?",
    a_en: "The most important step is using CSS custom properties for all colors instead of hardcoding — declare `--bg: #fff; --text: #1a1a1a` in `:root` and override them in dark mode. Two detection approaches: `@media (prefers-color-scheme: dark)` automatically follows the system setting, or class-based toggle (adding `.dark` to `<html>`) lets users choose and save preference in localStorage. When choosing dark mode colors, use the oklch color space to auto-generate palettes with consistent perceived brightness, and always check contrast ratios meet WCAG AA (4.5:1 for normal text). Avoid pure black (#000) for backgrounds — it strains the eyes; use dark gray (#1a1a1a or similar) instead.",
  },
  {
    id: 194,
    category: "CSS",
    subcategory: "Advanced",
    level: "intermediate",
    q: "user-select property dùng khi nào?",
    a: "`user-select: none` ngăn chọn text (buttons, drag handles). `user-select: all` chọn toàn bộ khi click (code blocks, tokens). `user-select: text` (mặc định). Cẩn thận: none ảnh hưởng accessibility.",
    q_en: "When should you use the user-select property?",
    a_en: "`user-select: none` prevents text selection (buttons, drag handles). `user-select: all` selects everything with a single click (code blocks, tokens). `user-select: text` is the default. Be careful: `none` can affect accessibility.",
  },
  {
    id: 195,
    category: "CSS",
    subcategory: "Advanced",
    level: "intermediate",
    q: "touch-action property dùng để làm gì?",
    a: "Property `touch-action` kiểm soát cách browser xử lý các touch gestures trên element. Giá trị `touch-action: none` vô hiệu hóa hoàn toàn zoom và scroll — phù hợp cho canvas drawing, games, hoặc custom gesture handlers. Giá trị `manipulation` chỉ cho phép pan và pinch-zoom, disable double-tap zoom — điều này loại bỏ độ trễ 300ms click delay trên mobile, giúp buttons và links phản hồi nhanh hơn. Các giá trị khác bao gồm `pan-x` (chỉ cho phép scroll ngang), `pan-y` (chỉ scroll dọc), `pinch-zoom` (chỉ cho phép zoom). Đây là property quan trọng cho các UI tương tác nhiều trên touch devices.",
    q_en: "What is the touch-action property used for?",
    a_en: "`touch-action` controls how the browser handles touch gestures on an element. `touch-action: none` disables all zooming and scrolling — suitable for canvas drawing, games, or custom gesture handlers. `manipulation` allows only pan and pinch-zoom, disabling double-tap zoom — this removes the 300ms click delay on mobile, making buttons and links feel more responsive. Other values include `pan-x` (horizontal scrolling only), `pan-y` (vertical only), and `pinch-zoom` (zoom only). An important property for touch-heavy interactive UIs.",
  },
  {
    id: 196,
    category: "CSS",
    subcategory: "Advanced",
    level: "advanced",
    q: "CSS Typed Object Model (Typed OM) là gì?",
    a: "JavaScript API thao tác CSS values dưới dạng typed objects thay vì strings. `el.attributeStyleMap.set('width', CSS.px(100))`. Performance tốt hơn, type-safe, less error-prone. Một phần của Houdini. Chrome supported.",
    q_en: "What is the CSS Typed Object Model (Typed OM)?",
    a_en: "A JavaScript API for manipulating CSS values as typed objects instead of strings. `el.attributeStyleMap.set('width', CSS.px(100))`. Better performance, type-safe, and less error-prone. Part of the CSS Houdini project. Currently supported in Chrome.",
  },
  {
    id: 197,
    category: "CSS",
    subcategory: "Responsive",
    level: "advanced",
    q: "Container queries so với media queries - khi nào dùng gì?",
    a: "Media queries dựa trên viewport width nên phù hợp cho page-level responsive — thay đổi layout tổng thể của trang khi kích thước màn hình thay đổi (ví dụ sidebar collapse trên mobile). Container queries dựa trên kích thước container cha nên phù hợp cho component-level responsive — component tự điều chỉnh layout dựa trên không gian được cấp, bất kể nằm ở đâu trên trang. Trong thực tế nên kết hợp cả hai: media queries cho overall page layout (grid columns, sidebar visibility), container queries cho reusable components (card hiển thị horizontal hay vertical tùy container width). Container queries giải quyết vấn đề lớn nhất của media queries: cùng một component đặt trong sidebar nhỏ và main content rộng cần layout khác nhau.",
    q_en: "Container queries vs media queries — when should you use each?",
    a_en: "Media queries are based on viewport width, making them suitable for page-level responsive design — changing the overall layout when the screen size changes (e.g., collapsing a sidebar on mobile). Container queries are based on the parent container's size, making them ideal for component-level responsive design — a component adjusts its own layout based on the space it's given, regardless of where it's placed on the page. In practice, combine both: media queries for overall page layout (grid columns, sidebar visibility), container queries for reusable components (a card displaying horizontally or vertically depending on container width). Container queries solve the biggest limitation of media queries: the same component placed in a narrow sidebar and a wide content area needs different layouts.",
  },
  {
    id: 198,
    category: "CSS",
    subcategory: "Performance",
    level: "advanced",
    q: "Cách optimize Largest Contentful Paint (LCP) bằng CSS?",
    a: 'Để tối ưu LCP (thời gian render phần tử lớn nhất trên viewport) bằng CSS, cần thực hiện nhiều bước. Inline critical CSS trực tiếp trong `<head>` để browser render above-the-fold content ngay mà không phải đợi tải file CSS bên ngoài. Preload hero images và fonts bằng `<link rel="preload">` để browser tải sớm nhất có thể. Tránh dùng `@import` trong CSS vì nó tạo serial loading chain — thay bằng nhiều `<link>` tags song song. Với web fonts, dùng `font-display: swap` để text hiển thị ngay với fallback font. Nếu LCP element dùng background image, nên chuyển sang thẻ `<img>` vì browser phát hiện `<img>` sớm hơn trong quá trình parse. Cuối cùng, giảm CSS file size bằng minification và loại bỏ unused CSS.',
    q_en: "How do you optimize Largest Contentful Paint (LCP) with CSS?",
    a_en: 'To improve LCP (the time it takes to render the largest element in the viewport) with CSS: inline critical CSS directly in `<head>` so the browser can render above-the-fold content immediately without waiting for external CSS files. Preload hero images and fonts with `<link rel="preload">` for the earliest possible loading. Avoid `@import` in CSS as it creates a serial loading chain — use multiple `<link>` tags instead for parallel loading. For web fonts, use `font-display: swap` so text appears immediately with a fallback font. If the LCP element uses a background image, switch to an `<img>` tag since the browser detects `<img>` earlier during parsing. Finally, reduce CSS file size through minification and removing unused CSS.',
  },
  {
    id: 199,
    category: "CSS",
    subcategory: "Performance",
    level: "advanced",
    q: "Cách giảm CSS bundle size?",
    a: "Có nhiều chiến lược để giảm CSS bundle size hiệu quả. Đầu tiên, loại bỏ unused CSS bằng PurgeCSS hoặc Tailwind JIT mode — scan HTML/JS files và chỉ giữ lại các class thực sự được sử dụng, có thể giảm đến 90% file size với Tailwind. Tiếp theo, dùng CSS Modules hoặc scoped CSS để tree-shake styles không dùng theo component. Minification bằng cssnano nén code bằng cách xóa whitespace, shorten colors, và merge rules. Dùng shorthand properties (`margin: 10px 20px` thay vì 4 dòng riêng) để giảm duplication. Code splitting CSS theo route đảm bảo mỗi trang chỉ load CSS cần thiết. Cuối cùng, bật Brotli/gzip compression ở server level, và dùng Coverage tab trong DevTools để audit và phát hiện CSS thừa.",
    q_en: "How do you reduce CSS bundle size?",
    a_en: "Several strategies work well. First, remove unused CSS with PurgeCSS or Tailwind JIT mode — which scans HTML/JS files and keeps only used classes, potentially reducing file size by 90% with Tailwind. Next, use CSS Modules or scoped CSS to tree-shake unused component styles. Minification with cssnano compresses code by removing whitespace, shortening colors, and merging rules. Use shorthand properties (`margin: 10px 20px` instead of 4 separate lines) to reduce duplication. Code-split CSS per route so each page only loads what it needs. Finally, enable Brotli/gzip compression at the server level and use the Coverage tab in DevTools to audit and detect unused CSS.",
  },
  {
    id: 200,
    category: "CSS",
    subcategory: "Advanced",
    level: "advanced",
    q: "Tương lai CSS: những features nào đang phát triển?",
    a: "Interop 2024-2025 focus: anchor positioning, scroll-driven animations, view transitions, @scope, CSS nesting, trigonometric functions, relative color syntax. CSS đang evolve nhanh, giảm nhu cầu JS cho nhiều UI patterns.",
    q_en: "What is the future of CSS? Which features are in development?",
    a_en: "Interop 2024-2025 focus areas: anchor positioning, scroll-driven animations, view transitions, @scope, CSS nesting, trigonometric functions, and relative color syntax. CSS is evolving rapidly, reducing the need for JavaScript in many common UI patterns.",
  },
];
