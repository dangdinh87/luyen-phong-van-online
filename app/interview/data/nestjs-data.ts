import { QAItem } from "../interview-data";

export const NESTJS_DATA: QAItem[] = [
  {
    id: 3260,
    category: "NestJS",
    subcategory: "Core",
    level: "beginner",
    q: "NestJS là gì? Tại sao nên dùng NestJS thay vì Express thuần?",
    a: "NestJS là framework Node.js xây dựng trên TypeScript, lấy cảm hứng từ Angular. Nó cung cấp kiến trúc module rõ ràng với `@Module()`, `@Controller()`, `@Injectable()` và Dependency Injection tích hợp sẵn.\n\nLý do chọn NestJS thay Express thuần: kiến trúc có cấu trúc (Modules/Controllers/Services phân tách rõ ràng), TypeScript-first với decorators và metadata reflection, IoC container tích hợp dễ test, hỗ trợ Microservices/GraphQL/WebSockets/gRPC. NestJS vẫn chạy trên Express (hoặc Fastify) bên dưới, nhưng thêm lớp abstraction giúp code dễ maintain và scale hơn.",
    q_en: "What is NestJS? Why use NestJS instead of plain Express?",
    a_en: "NestJS is a Node.js framework built on TypeScript, inspired by Angular. It provides a clear modular architecture with `@Module()`, `@Controller()`, `@Injectable()` and built-in Dependency Injection.\n\nReasons to choose NestJS over plain Express: structured architecture (Modules/Controllers/Services clearly separated), TypeScript-first with decorators and metadata reflection, built-in IoC container for easy testing, supports Microservices/GraphQL/WebSockets/gRPC. NestJS still runs on Express (or Fastify) under the hood, but adds an abstraction layer that makes code easier to maintain and scale.",
  },
  {
    id: 3261,
    category: "NestJS",
    subcategory: "Core",
    level: "beginner",
    q: "Module trong NestJS là gì? Giải thích cấu trúc @Module decorator.",
    a: "Module là đơn vị tổ chức cơ bản trong NestJS, nhóm các thành phần liên quan lại. Mỗi app có ít nhất một root module (`AppModule`).\n\n`@Module()` nhận một object với 4 thuộc tính: `imports` (modules khác cần dùng), `controllers` (xử lý HTTP requests), `providers` (services, repositories, guards...), và `exports` (providers cho phép modules khác sử dụng). Chỉ những providers được `exports` mới có thể được inject ở module khác.\n\nCác loại module: Feature Module nhóm theo tính năng (`UsersModule`, `AuthModule`), Shared Module export providers để tái sử dụng, Global Module dùng `@Global()` để providers available toàn app không cần import, Dynamic Module cấu hình runtime qua `forRoot()` / `forRootAsync()`.",
    q_en: "What is a Module in NestJS? Explain the @Module decorator structure.",
    a_en: "A Module is the basic organizational unit in NestJS, grouping related components together. Every app has at least one root module (`AppModule`).\n\n`@Module()` accepts an object with 4 properties: `imports` (other modules needed), `controllers` (handle HTTP requests), `providers` (services, repositories, guards...), and `exports` (providers other modules can use). Only exported providers can be injected in other modules.\n\nModule types: Feature Module groups by feature (`UsersModule`, `AuthModule`), Shared Module exports providers for reuse, Global Module uses `@Global()` so providers are available app-wide without importing, Dynamic Module configures at runtime via `forRoot()` / `forRootAsync()`.",
  },
  {
    id: 3262,
    category: "NestJS",
    subcategory: "Core",
    level: "beginner",
    q: "Controller trong NestJS làm gì? Cách định nghĩa routes với decorators?",
    a: "Controller chịu trách nhiệm nhận HTTP requests và trả về responses. Controller map routes đến handler methods thông qua decorators.\n\n`@Controller('users')` đặt base route `/users`. Các HTTP method decorators: `@Get()`, `@Post()`, `@Patch()`, `@Put()`, `@Delete()`. Có thể thêm path vào decorator như `@Get(':id')` để tạo route động.\n\nParameter decorators để extract data từ request: `@Param('id')` lấy route param, `@Query()` lấy query string, `@Body()` lấy request body, `@Headers()` lấy headers, `@Req()` / `@Res()` để access raw request/response (dùng `@Res()` sẽ mất một số tính năng NestJS như interceptors).",
    q_en: "What does a Controller do in NestJS? How to define routes with decorators?",
    a_en: "A Controller is responsible for receiving HTTP requests and returning responses. Controllers map routes to handler methods via decorators.\n\n`@Controller('users')` sets the base route `/users`. HTTP method decorators: `@Get()`, `@Post()`, `@Patch()`, `@Put()`, `@Delete()`. You can add a path like `@Get(':id')` for dynamic routes.\n\nParameter decorators to extract data from requests: `@Param('id')` gets route param, `@Query()` gets query string, `@Body()` gets request body, `@Headers()` gets headers, `@Req()` / `@Res()` accesses raw request/response (using `@Res()` bypasses some NestJS features like interceptors).",
  },
  {
    id: 3263,
    category: "NestJS",
    subcategory: "Core",
    level: "beginner",
    q: "Provider và Dependency Injection trong NestJS hoạt động như thế nào?",
    a: "Provider là bất kỳ class nào được annotate với `@Injectable()` — services, repositories, factories, helpers. NestJS quản lý vòng đời và inject chúng tự động thông qua constructor injection.\n\nCách hoạt động: khai báo provider trong `providers` array của module, NestJS IoC container tạo instance và inject vào các class phụ thuộc qua constructor. Reflector đọc TypeScript metadata để biết type cần inject.\n\nScope của providers: `DEFAULT` (Singleton) — một instance cho toàn app, `REQUEST` — instance mới cho mỗi request, `TRANSIENT` — instance mới mỗi lần inject. Custom providers cho phép linh hoạt hơn: `useValue` để inject giá trị cụ thể, `useFactory` để tạo provider với logic phức tạp, `useClass` để swap implementation.",
    q_en: "How do Providers and Dependency Injection work in NestJS?",
    a_en: "A Provider is any class annotated with `@Injectable()` — services, repositories, factories, helpers. NestJS manages their lifecycle and injects them automatically via constructor injection.\n\nHow it works: declare providers in the module's `providers` array, the NestJS IoC container creates instances and injects them into dependent classes via constructors. The Reflector reads TypeScript metadata to know which types to inject.\n\nProvider scopes: `DEFAULT` (Singleton) — one instance for the whole app, `REQUEST` — new instance per request, `TRANSIENT` — new instance each time injected. Custom providers offer flexibility: `useValue` to inject a specific value, `useFactory` to create a provider with complex logic, `useClass` to swap implementations.",
  },
  {
    id: 3264,
    category: "NestJS",
    subcategory: "Middleware",
    level: "intermediate",
    q: "Giải thích Request Lifecycle trong NestJS: Middleware → Guards → Interceptors → Pipes → Controller → Exception Filters.",
    a: "NestJS xử lý request qua pipeline theo thứ tự cố định: Middleware chạy đầu tiên (logging, CORS, session — access raw req/res), tiếp theo Guards kiểm tra authorization (trả về `true`/`false`), rồi Interceptors pre-processing (transform request trước khi vào handler), sau đó Pipes validate và transform input data, tiếp theo Controller Handler thực thi business logic chính, rồi Interceptors post-processing (transform response), cuối cùng Exception Filters bắt và format errors thành response chuẩn.\n\nMiddleware và Guards đều chạy trước handler nhưng Guards có access vào `ExecutionContext` nên biết handler nào sẽ được gọi. Exception Filters chỉ hoạt động khi có exception, nếu không có lỗi thì bỏ qua. Mỗi layer có thể áp dụng theo thứ tự: global → controller → route.",
    q_en: "Explain the NestJS Request Lifecycle: Middleware → Guards → Interceptors → Pipes → Controller → Exception Filters.",
    a_en: "NestJS processes requests through a fixed-order pipeline: Middleware runs first (logging, CORS, session — accesses raw req/res), then Guards check authorization (return `true`/`false`), then Interceptors pre-process (transform request before handler), then Pipes validate and transform input data, then the Controller Handler executes main business logic, then Interceptors post-process (transform response), and finally Exception Filters catch and format errors.\n\nMiddleware and Guards both run before the handler, but Guards have access to `ExecutionContext` so they know which handler will be called. Exception Filters only activate when an exception is thrown. Each layer can be applied in order: global → controller → route.",
  },
  {
    id: 3265,
    category: "NestJS",
    subcategory: "Guards",
    level: "intermediate",
    q: "Guards trong NestJS là gì? Cách implement JWT Auth Guard?",
    a: "Guards quyết định request có được phép đi tiếp không (authorization). Khác với Middleware, Guards implement interface `CanActivate` và có access vào `ExecutionContext` — biết được handler nào sẽ được gọi, rất hữu ích cho role-based access control.\n\nJWT Auth Guard hoạt động: extract Bearer token từ header `Authorization`, verify token bằng `JwtService.verify()`, nếu hợp lệ attach payload vào `request.user` và trả về `true`, ngược lại throw `UnauthorizedException`. Guards có thể áp dụng với `@UseGuards()` ở mức route, controller, hoặc global qua `APP_GUARD` provider.\n\nPublic decorator pattern: dùng `SetMetadata('isPublic', true)` với `@Public()` decorator, trong guard đọc metadata qua `Reflector` để bỏ qua authentication cho các route công khai.",
    q_en: "What are Guards in NestJS? How to implement a JWT Auth Guard?",
    a_en: "Guards decide whether a request should proceed (authorization). Unlike Middleware, Guards implement the `CanActivate` interface and have access to `ExecutionContext` — knowing which handler will be called, making them ideal for role-based access control.\n\nJWT Auth Guard works by: extracting the Bearer token from the `Authorization` header, verifying it with `JwtService.verify()`, if valid attaching the payload to `request.user` and returning `true`, otherwise throwing `UnauthorizedException`. Guards can be applied with `@UseGuards()` at route, controller, or globally via the `APP_GUARD` provider.\n\nPublic decorator pattern: use `SetMetadata('isPublic', true)` with a `@Public()` decorator, then in the guard read metadata via `Reflector` to skip authentication for public routes.",
  },
  {
    id: 3266,
    category: "NestJS",
    subcategory: "Pipes",
    level: "intermediate",
    q: "Pipes trong NestJS là gì? Cách dùng ValidationPipe với class-validator?",
    a: "Pipes có hai use-case chính: validation (throw exception nếu data không hợp lệ) và transformation (chuyển đổi input sang dạng mong muốn). Pipes implement interface `PipeTransform` với method `transform(value, metadata)`.\n\n`ValidationPipe` của NestJS kết hợp với `class-validator` để validate DTO tự động. Cấu hình quan trọng: `whitelist: true` loại bỏ các properties không khai báo trong DTO, `forbidNonWhitelisted: true` throw error thay vì strip, `transform: true` tự động chuyển đổi type (string sang number), `enableImplicitConversion: true` convert dựa trên TypeScript type.\n\nDTO dùng decorators từ `class-validator` như `@IsString()`, `@IsEmail()`, `@MinLength()`, `@IsOptional()`, `@IsEnum()`. `class-transformer` cung cấp `@Transform()` để biến đổi giá trị trước khi validate. Pipe global đăng ký qua `app.useGlobalPipes()` hoặc `APP_PIPE` provider.",
    q_en: "What are Pipes in NestJS? How to use ValidationPipe with class-validator?",
    a_en: "Pipes have two main use cases: validation (throw exception if data is invalid) and transformation (convert input to the desired type). Pipes implement the `PipeTransform` interface with a `transform(value, metadata)` method.\n\nNestJS's `ValidationPipe` combined with `class-validator` automatically validates DTOs. Key options: `whitelist: true` removes undeclared properties, `forbidNonWhitelisted: true` throws an error instead of stripping, `transform: true` auto-converts types (string to number), `enableImplicitConversion: true` converts based on TypeScript types.\n\nDTOs use decorators from `class-validator` like `@IsString()`, `@IsEmail()`, `@MinLength()`, `@IsOptional()`, `@IsEnum()`. `class-transformer` provides `@Transform()` to mutate values before validation. Global pipes are registered via `app.useGlobalPipes()` or the `APP_PIPE` provider.",
  },
  {
    id: 3267,
    category: "NestJS",
    subcategory: "Interceptors",
    level: "intermediate",
    q: "Interceptors trong NestJS hoạt động như thế nào? Nêu các use-case phổ biến.",
    a: "Interceptors wrap việc thực thi handler, cho phép chạy code trước và sau handler. Chúng implement `NestInterceptor` với method `intercept(context, next)` trả về `Observable`. Gọi `next.handle()` để tiếp tục pipeline, dùng RxJS operators để transform.\n\nUse-cases phổ biến: Response transform — dùng `map()` để wrap tất cả response trong object chuẩn `{ success: true, data, timestamp }`. Logging — ghi thời gian xử lý với `tap()`. Caching — kiểm tra cache trước, nếu hit thì return `of(cachedData)` bỏ qua handler. Timeout — dùng `timeout(5000)` throw `TimeoutError` sau 5 giây. Error mapping — `catchError()` để transform exceptions.\n\nApply với `@UseInterceptors()` ở route/controller hoặc global qua `APP_INTERCEPTOR`. Khác Guard và Pipe, Interceptors chạy cả trước lẫn sau handler nên có thể transform response.",
    q_en: "How do Interceptors work in NestJS? List common use cases.",
    a_en: "Interceptors wrap handler execution, allowing code to run both before and after the handler. They implement `NestInterceptor` with an `intercept(context, next)` method returning an `Observable`. Call `next.handle()` to continue the pipeline, and use RxJS operators to transform.\n\nCommon use cases: Response transform — use `map()` to wrap all responses in a standard `{ success: true, data, timestamp }` object. Logging — record processing time with `tap()`. Caching — check cache first, if hit return `of(cachedData)` bypassing the handler. Timeout — use `timeout(5000)` to throw `TimeoutError` after 5 seconds. Error mapping — `catchError()` to transform exceptions.\n\nApply with `@UseInterceptors()` at route/controller level or globally via `APP_INTERCEPTOR`. Unlike Guards and Pipes, Interceptors run both before and after the handler, making them capable of transforming responses.",
  },
  {
    id: 3268,
    category: "NestJS",
    subcategory: "Filters",
    level: "intermediate",
    q: "Exception Filters trong NestJS là gì? Cách tạo global error handler.",
    a: "Exception Filters bắt các exceptions được throw trong ứng dụng và format response lỗi. NestJS có built-in filter xử lý `HttpException` và các subclass của nó. Nếu exception không phải `HttpException`, NestJS trả về 500 Internal Server Error mặc định.\n\nCustom global filter implement `ExceptionFilter` với method `catch(exception, host)`. `host.switchToHttp()` lấy req/res. Trong filter có thể check `instanceof HttpException` để lấy status code, log lỗi, format response chuẩn với `statusCode`, `message`, `timestamp`, `path`.\n\nBuilt-in HTTP exceptions: `NotFoundException`, `BadRequestException`, `UnauthorizedException`, `ForbiddenException`, `ConflictException`, `InternalServerErrorException`... Đăng ký global qua `app.useGlobalFilters()` hoặc preferred là `{ provide: APP_FILTER, useClass: ... }` để hỗ trợ DI.",
    q_en: "What are Exception Filters in NestJS? How to create a global error handler.",
    a_en: "Exception Filters catch exceptions thrown in the application and format error responses. NestJS has a built-in filter that handles `HttpException` and its subclasses. If an exception is not an `HttpException`, NestJS returns a 500 Internal Server Error by default.\n\nA custom global filter implements `ExceptionFilter` with a `catch(exception, host)` method. `host.switchToHttp()` provides access to req/res. Inside the filter you can check `instanceof HttpException` to get the status code, log the error, and format a standard response with `statusCode`, `message`, `timestamp`, `path`.\n\nBuilt-in HTTP exceptions: `NotFoundException`, `BadRequestException`, `UnauthorizedException`, `ForbiddenException`, `ConflictException`, `InternalServerErrorException`... Register globally via `app.useGlobalFilters()` or preferably `{ provide: APP_FILTER, useClass: ... }` to support DI.",
  },
  {
    id: 3269,
    category: "NestJS",
    subcategory: "TypeORM",
    level: "intermediate",
    q: "Cách tích hợp TypeORM với NestJS? Repository pattern hoạt động như thế nào?",
    a: "TypeORM là ORM phổ biến nhất với NestJS, sử dụng Data Mapper pattern thông qua Repository. Setup với `TypeOrmModule.forRoot()` trong `AppModule` cấu hình connection (type, host, port, credentials, entities path), `synchronize: false` trong production — dùng migrations thay.\n\nEntity định nghĩa bằng `@Entity()` decorator với các column decorators: `@PrimaryGeneratedColumn()`, `@Column()`, `@CreateDateColumn()`, `@UpdateDateColumn()`. Relations: `@OneToMany()`, `@ManyToOne()`, `@ManyToMany()`, `@OneToOne()`.\n\nTrong feature module, `TypeOrmModule.forFeature([Entity])` đăng ký Repository. Service inject `@InjectRepository(Entity)` để dùng `Repository<Entity>` với các methods: `find()`, `findOne()`, `create()`, `save()`, `update()`, `delete()`. Query Builder cho queries phức tạp: `createQueryBuilder('alias').leftJoinAndSelect().where().getMany()`.",
    q_en: "How to integrate TypeORM with NestJS? How does the Repository pattern work?",
    a_en: "TypeORM is the most popular ORM with NestJS, using the Data Mapper pattern via Repository. Set up with `TypeOrmModule.forRoot()` in `AppModule` to configure the connection (type, host, port, credentials, entities path). Use `synchronize: false` in production — use migrations instead.\n\nEntities are defined with the `@Entity()` decorator and column decorators: `@PrimaryGeneratedColumn()`, `@Column()`, `@CreateDateColumn()`, `@UpdateDateColumn()`. Relations: `@OneToMany()`, `@ManyToOne()`, `@ManyToMany()`, `@OneToOne()`.\n\nIn feature modules, `TypeOrmModule.forFeature([Entity])` registers the Repository. Services inject `@InjectRepository(Entity)` to use `Repository<Entity>` with methods: `find()`, `findOne()`, `create()`, `save()`, `update()`, `delete()`. Query Builder handles complex queries: `createQueryBuilder('alias').leftJoinAndSelect().where().getMany()`.",
  },
  {
    id: 3270,
    category: "NestJS",
    subcategory: "Auth",
    level: "intermediate",
    q: "Implement JWT Authentication flow trong NestJS với @nestjs/jwt và Passport?",
    a: "JWT Auth flow trong NestJS gồm các bước: install `@nestjs/jwt`, `@nestjs/passport`, `passport`, `passport-jwt`, `bcryptjs`. Tạo `AuthModule` import `JwtModule.registerAsync()` với `ConfigService` để lấy `JWT_SECRET` và `expiresIn`.\n\n`AuthService` có method `login()`: tìm user theo email, compare password với `bcrypt.compare()`, nếu đúng sign JWT với payload `{ sub: userId, email, role }` và trả về `access_token` (ngắn hạn 15m) + `refresh_token` (dài hạn 7d).\n\n`JwtStrategy` extends `PassportStrategy(Strategy)` với config `jwtFromRequest: ExtractJwt.fromAuthHeaderAsBearerToken()` và `secretOrKey`. Method `validate(payload)` attach user vào request. Guard `JwtAuthGuard` extends `AuthGuard('jwt')` từ Passport. `@Public()` decorator dùng `SetMetadata` để bỏ qua guard cho login/register endpoints.",
    q_en: "Implement JWT Authentication flow in NestJS with @nestjs/jwt and Passport?",
    a_en: "JWT Auth flow in NestJS involves these steps: install `@nestjs/jwt`, `@nestjs/passport`, `passport`, `passport-jwt`, `bcryptjs`. Create `AuthModule` importing `JwtModule.registerAsync()` with `ConfigService` to get `JWT_SECRET` and `expiresIn`.\n\n`AuthService` has a `login()` method: finds user by email, compares password with `bcrypt.compare()`, if correct signs JWT with payload `{ sub: userId, email, role }` and returns `access_token` (short-lived 15m) + `refresh_token` (long-lived 7d).\n\n`JwtStrategy` extends `PassportStrategy(Strategy)` with config `jwtFromRequest: ExtractJwt.fromAuthHeaderAsBearerToken()` and `secretOrKey`. The `validate(payload)` method attaches user to request. `JwtAuthGuard` extends `AuthGuard('jwt')` from Passport. The `@Public()` decorator uses `SetMetadata` to skip the guard for login/register endpoints.",
  },
  {
    id: 3271,
    category: "NestJS",
    subcategory: "Config",
    level: "beginner",
    q: "ConfigModule và ConfigService trong NestJS hoạt động như thế nào?",
    a: "`@nestjs/config` giúp quản lý environment variables an toàn với type-safety. `ConfigModule.forRoot()` với `isGlobal: true` cho phép inject `ConfigService` ở bất kỳ module nào mà không cần import lại.\n\nValidation schema với Joi: `validationSchema: Joi.object({ PORT: Joi.number().default(3000), JWT_SECRET: Joi.string().min(32).required() })` — app sẽ fail ngay khi start nếu env vars thiếu hoặc sai format.\n\nNamespaced config với `registerAs('database', () => ({ host: process.env.DB_HOST, port: parseInt(process.env.DB_PORT) }))` cho phép nhóm config và inject type-safe qua `@Inject(databaseConfig.KEY) private dbConfig: ConfigType<typeof databaseConfig>`. `ConfigService.get<string>('KEY')` dùng khi không cần namespace, `ConfigService.get<string>('database.host')` với dot notation cho nested config.",
    q_en: "How do ConfigModule and ConfigService work in NestJS?",
    a_en: "`@nestjs/config` helps manage environment variables safely with type-safety. `ConfigModule.forRoot()` with `isGlobal: true` allows injecting `ConfigService` in any module without re-importing.\n\nValidation schema with Joi: `validationSchema: Joi.object({ PORT: Joi.number().default(3000), JWT_SECRET: Joi.string().min(32).required() })` — the app fails immediately on startup if env vars are missing or malformed.\n\nNamespaced config with `registerAs('database', () => ({ host: process.env.DB_HOST, port: parseInt(process.env.DB_PORT) }))` groups config and allows type-safe injection via `@Inject(databaseConfig.KEY) private dbConfig: ConfigType<typeof databaseConfig>`. Use `ConfigService.get<string>('KEY')` without namespace, or `ConfigService.get<string>('database.host')` with dot notation for nested config.",
  },
  {
    id: 3272,
    category: "NestJS",
    subcategory: "Testing",
    level: "intermediate",
    q: "Cách viết unit tests và integration tests trong NestJS?",
    a: "NestJS dùng Jest và cung cấp `Test.createTestingModule()` để tạo test environment với DI container đầy đủ.\n\nUnit test service: tạo mock repository với `jest.fn()` cho mỗi method, đăng ký trong module với `{ provide: getRepositoryToken(Entity), useValue: mockRepo }`. Test từng method riêng lẻ, verify calls với `expect(mock).toHaveBeenCalledWith()`, reset mocks với `jest.clearAllMocks()` sau mỗi test.\n\nE2E/Integration test: dùng `Test.createTestingModule({ imports: [AppModule] })`, override providers với `.overrideProvider(Service).useValue(mockService)`, tạo app với `moduleFixture.createNestApplication()`, init global pipes/guards, dùng `supertest` để call real HTTP endpoints. Ưu tiên override service thay vì repository cho integration tests để test controller + service logic mà không cần DB thật.",
    q_en: "How to write unit tests and integration tests in NestJS?",
    a_en: "NestJS uses Jest and provides `Test.createTestingModule()` to create a test environment with a full DI container.\n\nUnit testing a service: create a mock repository with `jest.fn()` for each method, register it in the module with `{ provide: getRepositoryToken(Entity), useValue: mockRepo }`. Test each method in isolation, verify calls with `expect(mock).toHaveBeenCalledWith()`, reset mocks with `jest.clearAllMocks()` after each test.\n\nE2E/Integration test: use `Test.createTestingModule({ imports: [AppModule] })`, override providers with `.overrideProvider(Service).useValue(mockService)`, create the app with `moduleFixture.createNestApplication()`, init global pipes/guards, and use `supertest` to call real HTTP endpoints. Prefer overriding the service rather than the repository for integration tests to test controller + service logic without a real database.",
  },
  {
    id: 3273,
    category: "NestJS",
    subcategory: "Microservices",
    level: "advanced",
    q: "NestJS Microservices hoạt động như thế nào? Các transport layers phổ biến?",
    a: "NestJS Microservices là pattern giao tiếp giữa services dùng message-passing thay vì HTTP. `NestFactory.createMicroservice()` tạo microservice với transport layer được chọn.\n\nTransport layers: TCP cho internal services latency thấp, Redis cho Pub/Sub và simple messaging, NATS cho lightweight cloud-native messaging, Kafka cho high-throughput event streaming, RabbitMQ cho complex routing với dead-letter queues, gRPC cho strongly-typed high-performance calls.\n\nHai pattern messaging: `@MessagePattern('event')` cho Request/Response (sender chờ reply), `@EventPattern('event')` cho Fire-and-forget (không chờ reply). API Gateway dùng `ClientsModule.register()` inject `ClientProxy`, gọi `client.send('pattern', data)` cho request/response trả về Observable, `client.emit('pattern', data)` cho fire-and-forget. Hybrid app có thể serve cả HTTP và Microservices cùng lúc với `app.connectMicroservice()`.",
    q_en: "How do NestJS Microservices work? What are the common transport layers?",
    a_en: "NestJS Microservices use message-passing instead of HTTP for inter-service communication. `NestFactory.createMicroservice()` creates a microservice with the chosen transport layer.\n\nTransport layers: TCP for low-latency internal services, Redis for Pub/Sub and simple messaging, NATS for lightweight cloud-native messaging, Kafka for high-throughput event streaming, RabbitMQ for complex routing with dead-letter queues, gRPC for strongly-typed high-performance calls.\n\nTwo messaging patterns: `@MessagePattern('event')` for Request/Response (sender waits for reply), `@EventPattern('event')` for Fire-and-forget (no reply expected). API Gateways use `ClientsModule.register()` to inject a `ClientProxy`, calling `client.send('pattern', data)` for request/response (returns Observable) or `client.emit('pattern', data)` for fire-and-forget. Hybrid apps can serve both HTTP and Microservices simultaneously with `app.connectMicroservice()`.",
  },
  {
    id: 3274,
    category: "NestJS",
    subcategory: "TypeORM",
    level: "intermediate",
    q: "Prisma với NestJS — setup và so sánh với TypeORM?",
    a: "Prisma là ORM thế hệ mới với type-safety tuyệt vời, ngày càng được ưa dùng thay TypeORM. Schema định nghĩa trong `prisma/schema.prisma` với cú pháp riêng, `prisma generate` tạo Prisma Client type-safe hoàn toàn.\n\nSetup NestJS: tạo `PrismaService` extends `PrismaClient` implements `OnModuleInit`, gọi `this.$connect()` trong `onModuleInit()`. Wrap trong `@Global() @Module()` để dùng toàn app. Prisma Client API rất fluent: `prisma.user.findMany({ include, where, orderBy })`, `prisma.user.create({ data })`, transactions với `prisma.$transaction([])`.\n\nSo sánh Prisma vs TypeORM: Prisma có type-safety tuyệt vời (auto-generated types từ schema), `prisma migrate dev` rõ ràng an toàn hơn `synchronize: true` của TypeORM. TypeORM quen thuộc với Java/Spring developers, hỗ trợ Active Record pattern. Prisma không hỗ trợ MongoDB aggregation pipeline tốt bằng Mongoose. Hiện tại Prisma được cộng đồng ưa chuộng hơn cho dự án mới.",
    q_en: "Prisma with NestJS — setup and comparison with TypeORM?",
    a_en: "Prisma is a next-generation ORM with excellent type-safety, increasingly preferred over TypeORM. The schema is defined in `prisma/schema.prisma` with its own syntax, and `prisma generate` creates a fully type-safe Prisma Client.\n\nNestJS setup: create a `PrismaService` extending `PrismaClient` implementing `OnModuleInit`, calling `this.$connect()` in `onModuleInit()`. Wrap in a `@Global() @Module()` for app-wide use. Prisma Client API is fluent: `prisma.user.findMany({ include, where, orderBy })`, `prisma.user.create({ data })`, transactions with `prisma.$transaction([])`.\n\nPrisma vs TypeORM: Prisma has excellent type-safety (auto-generated types from schema), `prisma migrate dev` is safer than TypeORM's `synchronize: true`. TypeORM is familiar to Java/Spring developers and supports Active Record pattern. Prisma handles MongoDB aggregation pipelines less well than Mongoose. Prisma is currently the community preference for new projects.",
  },
  {
    id: 3275,
    category: "NestJS",
    subcategory: "Core",
    level: "intermediate",
    q: "File upload trong NestJS với Multer — upload single, multiple file và validate?",
    a: "NestJS tích hợp Multer qua `@nestjs/platform-express` để xử lý `multipart/form-data`. Không cần install thêm gì với Express adapter.\n\nUpload single file: dùng `@UseInterceptors(FileInterceptor('fieldName', options))` và `@UploadedFile()` decorator. Options quan trọng: `storage` — `diskStorage()` lưu disk hoặc `memoryStorage()` lưu buffer (dùng khi upload S3), `fileFilter` để reject file không hợp lệ, `limits.fileSize` giới hạn kích thước.\n\nUpload multiple files: `FilesInterceptor('fieldName', maxCount)` với `@UploadedFiles()`. `ParseFilePipe` với validators `MaxFileSizeValidator` và `FileTypeValidator` là cách clean nhất để validate. Upload S3: dùng `memoryStorage()` để lấy `file.buffer`, gọi AWS SDK `s3.putObject()` với buffer và `file.mimetype`. Luôn generate tên file ngẫu nhiên để tránh xung đột và directory traversal.",
    q_en: "File upload in NestJS with Multer — single, multiple files and validation?",
    a_en: "NestJS integrates Multer via `@nestjs/platform-express` to handle `multipart/form-data`. No additional installation is needed with the Express adapter.\n\nSingle file upload: use `@UseInterceptors(FileInterceptor('fieldName', options))` and the `@UploadedFile()` decorator. Key options: `storage` — `diskStorage()` saves to disk or `memoryStorage()` keeps it as a buffer (use when uploading to S3), `fileFilter` to reject invalid files, `limits.fileSize` to cap file size.\n\nMultiple file upload: `FilesInterceptor('fieldName', maxCount)` with `@UploadedFiles()`. `ParseFilePipe` with `MaxFileSizeValidator` and `FileTypeValidator` is the cleanest validation approach. S3 upload: use `memoryStorage()` to get `file.buffer`, then call AWS SDK `s3.putObject()` with the buffer and `file.mimetype`. Always generate random filenames to avoid conflicts and directory traversal.",
  },
  {
    id: 3276,
    category: "NestJS",
    subcategory: "Decorators",
    level: "intermediate",
    q: "Custom Decorators trong NestJS — cách tạo @CurrentUser, @Roles, @Public?",
    a: "Custom Decorators giúp code sạch hơn và tái sử dụng được. NestJS cung cấp `createParamDecorator` cho param decorators và `SetMetadata` để đính kèm metadata.\n\n`@CurrentUser`: dùng `createParamDecorator((data, ctx) => ctx.switchToHttp().getRequest().user)`. Có thể nhận tham số để extract field cụ thể: `@CurrentUser('email')` trả về `user.email`, `@CurrentUser()` trả về toàn bộ user object. Dùng trong controller thay vì `@Request() req` rồi `req.user`.\n\n`@Roles(...roles)`: dùng `SetMetadata('roles', roles)` để đính kèm metadata. `RolesGuard` implement `CanActivate`, dùng `Reflector.getAllAndOverride()` để đọc required roles từ handler và class, so sánh với `request.user.role`.\n\n`@Public()`: `SetMetadata(IS_PUBLIC_KEY, true)`, đọc trong `JwtAuthGuard` — nếu `isPublic === true` thì return true ngay mà không verify token. Pattern này cho phép global guard nhưng vẫn có public routes.",
    q_en: "Custom Decorators in NestJS — how to create @CurrentUser, @Roles, @Public?",
    a_en: "Custom Decorators make code cleaner and reusable. NestJS provides `createParamDecorator` for param decorators and `SetMetadata` to attach metadata.\n\n`@CurrentUser`: use `createParamDecorator((data, ctx) => ctx.switchToHttp().getRequest().user)`. It can accept a parameter to extract a specific field: `@CurrentUser('email')` returns `user.email`, `@CurrentUser()` returns the full user object. Use this in controllers instead of `@Request() req` and then `req.user`.\n\n`@Roles(...roles)`: use `SetMetadata('roles', roles)` to attach metadata. `RolesGuard` implements `CanActivate`, using `Reflector.getAllAndOverride()` to read required roles from the handler and class, then comparing with `request.user.role`.\n\n`@Public()`: `SetMetadata(IS_PUBLIC_KEY, true)`, read in `JwtAuthGuard` — if `isPublic === true` return true immediately without verifying token. This pattern allows a global guard while still supporting public routes.",
  },
  {
    id: 3277,
    category: "NestJS",
    subcategory: "WebSockets",
    level: "advanced",
    q: "WebSockets trong NestJS — cách implement real-time features với @WebSocketGateway?",
    a: "NestJS hỗ trợ WebSockets qua `@WebSocketGateway()` decorator, tích hợp với socket.io hoặc ws. Gateway là class đặc biệt giống Controller nhưng xử lý WebSocket events.\n\nGateway implement 3 interfaces: `OnGatewayInit` (sau khi khởi tạo), `OnGatewayConnection` (client connect), `OnGatewayDisconnect` (client disconnect). `@WebSocketServer()` inject server instance. `@SubscribeMessage('event')` handle event từ client, tương tự `@Get()` trong controller. `@MessageBody()` và `@ConnectedSocket()` là param decorators cho WS.\n\nBroadcast: `this.server.emit()` gửi cho tất cả, `this.server.to(room).emit()` gửi cho room, `client.to(room).emit()` gửi cho room trừ sender. Guards và Interceptors hoạt động với WS Gateway giống HTTP. Namespace với `namespace: '/chat'` trong decorator options để tách biệt connections.",
    q_en: "WebSockets in NestJS — how to implement real-time features with @WebSocketGateway?",
    a_en: "NestJS supports WebSockets via the `@WebSocketGateway()` decorator, integrating with socket.io or ws. A Gateway is a special class similar to a Controller but handling WebSocket events.\n\nGateways implement 3 interfaces: `OnGatewayInit` (after initialization), `OnGatewayConnection` (client connects), `OnGatewayDisconnect` (client disconnects). `@WebSocketServer()` injects the server instance. `@SubscribeMessage('event')` handles events from clients, similar to `@Get()` in controllers. `@MessageBody()` and `@ConnectedSocket()` are param decorators for WebSockets.\n\nBroadcasting: `this.server.emit()` sends to all, `this.server.to(room).emit()` sends to a room, `client.to(room).emit()` sends to a room excluding the sender. Guards and Interceptors work the same on WS Gateways as on HTTP. Use `namespace: '/chat'` in decorator options to separate connections.",
  },
  {
    id: 3278,
    category: "NestJS",
    subcategory: "Performance",
    level: "advanced",
    q: "Tối ưu performance NestJS: Fastify, Caching, Compression, Rate Limiting?",
    a: "Fastify adapter thay Express: dùng `FastifyAdapter` từ `@nestjs/platform-fastify`, nhanh hơn 2-3x nhờ JSON serialization tối ưu và request/response lifecycle hiệu quả hơn. Nhưng một số middleware Express không tương thích, cần kiểm tra.\n\nCaching: `@nestjs/cache-manager` với Redis store qua `CacheModule.registerAsync()`. `@UseInterceptors(CacheInterceptor)` tự động cache GET responses, `@CacheTTL(seconds)` override TTL cho route cụ thể. Programmatic cache với `CacheManager.get/set/del`.\n\nCompression: `app.use(compression())` giảm response size 60-70% cho text content.\n\nRate Limiting: `@nestjs/throttler` với `ThrottlerModule.forRoot()` config nhiều tiers (short/long). `APP_GUARD` với `ThrottlerGuard` apply global, `@Throttle()` override cho route cụ thể, `@SkipThrottle()` bỏ qua.\n\nDB connection pooling: TypeORM `extra.max` tăng max connections theo CPU cores. Lazy loading modules với `LazyModuleLoader` cho microservices để giảm startup time.",
    q_en: "NestJS performance optimization: Fastify, Caching, Compression, Rate Limiting?",
    a_en: "Fastify adapter over Express: use `FastifyAdapter` from `@nestjs/platform-fastify`, 2-3x faster due to optimized JSON serialization and request/response lifecycle. However, some Express middleware is incompatible — check before switching.\n\nCaching: `@nestjs/cache-manager` with Redis store via `CacheModule.registerAsync()`. `@UseInterceptors(CacheInterceptor)` automatically caches GET responses, `@CacheTTL(seconds)` overrides TTL for specific routes. Programmatic cache with `CacheManager.get/set/del`.\n\nCompression: `app.use(compression())` reduces response size by 60-70% for text content.\n\nRate Limiting: `@nestjs/throttler` with `ThrottlerModule.forRoot()` configuring multiple tiers (short/long). `APP_GUARD` with `ThrottlerGuard` applies globally, `@Throttle()` overrides per route, `@SkipThrottle()` bypasses it.\n\nDB connection pooling: TypeORM `extra.max` increases max connections based on CPU cores. Lazy loading modules with `LazyModuleLoader` in microservices reduces startup time.",
  },
  {
    id: 3279,
    category: "NestJS",
    subcategory: "Advanced",
    level: "advanced",
    q: "Middleware trong NestJS — functional vs class middleware, apply theo routes?",
    a: "Middleware thực thi trước Guards, có access vào raw `req`/`res`/`next` giống Express. Dùng cho logging, CORS, session, body parsing, request tracking.\n\nFunctional middleware là function đơn giản `(req, res, next) => { next() }` — dùng khi không cần DI. Class middleware implement `NestMiddleware` với method `use(req, res, next)` và `@Injectable()` — dùng khi cần inject services.\n\nApply middleware trong module `implements NestModule` với `configure(consumer: MiddlewareConsumer)`. `consumer.apply(Middleware).forRoutes('*')` apply cho tất cả. `.forRoutes({ path: 'users', method: RequestMethod.GET })` hoặc `.forRoutes(UsersController)` cho specific routes/controllers. `.exclude()` loại trừ routes cụ thể. Có thể chain nhiều middleware: `.apply(M1, M2, M3)`. Global middleware (Express-style) dùng `app.use(helmet(), cors(), express.json())` trong `main.ts` trước khi listen.",
    q_en: "Middleware in NestJS — functional vs class middleware, apply by routes?",
    a_en: "Middleware executes before Guards and has access to raw `req`/`res`/`next` like Express. Used for logging, CORS, session, body parsing, and request tracking.\n\nFunctional middleware is a simple function `(req, res, next) => { next() }` — use when DI is not needed. Class middleware implements `NestMiddleware` with a `use(req, res, next)` method and `@Injectable()` — use when services need to be injected.\n\nApply middleware in a module `implements NestModule` with `configure(consumer: MiddlewareConsumer)`. `consumer.apply(Middleware).forRoutes('*')` applies to all. `.forRoutes({ path: 'users', method: RequestMethod.GET })` or `.forRoutes(UsersController)` targets specific routes/controllers. `.exclude()` exempts certain routes. Chain multiple middleware: `.apply(M1, M2, M3)`. Global middleware (Express-style) uses `app.use(helmet(), cors(), express.json())` in `main.ts` before listening.",
  },
  {
    id: 3280,
    category: "NestJS",
    subcategory: "Core",
    level: "beginner",
    q: "Swagger / OpenAPI documentation trong NestJS — setup và các annotations phổ biến?",
    a: "NestJS tích hợp Swagger qua `@nestjs/swagger`. Setup trong `main.ts`: `DocumentBuilder` cấu hình title/description/version/auth, `SwaggerModule.createDocument()` tạo document, `SwaggerModule.setup('api/docs', app, document)` serve UI.\n\nAnnotations trên DTO: `@ApiProperty({ example, description })` cho required fields, `@ApiPropertyOptional()` cho optional fields. Hỗ trợ `enum`, `type`, `minLength`, `maxLength`, `default`.\n\nAnnotations trên Controller: `@ApiTags('GroupName')` nhóm endpoints trong Swagger UI, `@ApiBearerAuth()` chỉ định cần JWT, `@ApiOperation({ summary })` mô tả endpoint, `@ApiResponse({ status, type, description })` document các response codes. Param annotations: `@ApiParam()` cho route params, `@ApiQuery()` cho query params. `PickType`, `OmitType`, `PartialType` từ `@nestjs/swagger` để tạo derived DTOs tái sử dụng schema.",
    q_en: "Swagger / OpenAPI documentation in NestJS — setup and common annotations?",
    a_en: "NestJS integrates Swagger via `@nestjs/swagger`. Setup in `main.ts`: use `DocumentBuilder` to configure title/description/version/auth, `SwaggerModule.createDocument()` to build the document, `SwaggerModule.setup('api/docs', app, document)` to serve the UI.\n\nDTO annotations: `@ApiProperty({ example, description })` for required fields, `@ApiPropertyOptional()` for optional fields. Supports `enum`, `type`, `minLength`, `maxLength`, `default`.\n\nController annotations: `@ApiTags('GroupName')` groups endpoints in the Swagger UI, `@ApiBearerAuth()` marks JWT requirement, `@ApiOperation({ summary })` describes the endpoint, `@ApiResponse({ status, type, description })` documents response codes. Param annotations: `@ApiParam()` for route params, `@ApiQuery()` for query params. Use `PickType`, `OmitType`, `PartialType` from `@nestjs/swagger` to create derived DTOs that reuse the schema.",
  },
];
