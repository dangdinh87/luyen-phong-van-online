import type { QAItem } from "../interview-data";

export const PHP_LARAVEL_DATA: QAItem[] = [
  // ─── PHP Cơ Bản ───────────────────────────────────────────────────────────

  {
    id: 2601,
    category: "PHP",
    subcategory: "PHP Cơ Bản",
    level: "beginner",
    q: "PHP là gì và tên viết tắt này có nghĩa là gì?",
    a: "PHP viết tắt của Hypertext Preprocessor, là ngôn ngữ lập trình phía server dùng để tạo trang web động. Khi người dùng truy cập một trang PHP, server sẽ thực thi mã PHP rồi trả về HTML cho trình duyệt. Khác với JavaScript chạy trên trình duyệt, PHP chạy hoàn toàn trên server trước khi gửi kết quả về cho client. PHP là mã nguồn mở, đa nền tảng và tích hợp tốt với các cơ sở dữ liệu như MySQL, PostgreSQL.",
    q_en: "What is PHP and what does it stand for?",
    a_en: "PHP stands for Hypertext Preprocessor. It's a server-side scripting language used to create dynamic web pages. When a user requests a PHP page, the server executes the PHP code and sends the HTML output to the browser. Unlike JavaScript which runs in the browser, PHP runs on the server before sending results to the client. It's open-source, cross-platform, and integrates seamlessly with databases like MySQL and PostgreSQL.",
  },
  {
    id: 2602,
    category: "PHP",
    subcategory: "PHP Cơ Bản",
    level: "beginner",
    q: "Superglobal trong PHP là gì? Cho ví dụ.",
    a: "Superglobal là các biến được PHP định nghĩa sẵn, có thể truy cập ở bất kỳ đâu trong script mà không cần khai báo `global`. Các superglobal quan trọng: `$_GET` (dữ liệu gửi qua URL), `$_POST` (dữ liệu gửi ẩn qua form), `$_SERVER` (thông tin server), `$_FILES` (file được upload), `$_COOKIE` (dữ liệu cookie), `$_SESSION` (lưu trữ session), `$GLOBALS` (chứa tất cả biến toàn cục). PHP tự động gán giá trị cho các biến này và có thể dùng trong mọi hàm hay phạm vi.",
    q_en: "What are PHP superglobals? Give examples.",
    a_en: "Superglobals are pre-defined variables available everywhere in a script without declaring global. The most important ones are: `$_GET` (collects form data sent via URL), `$_POST` (collects form data sent hidden), `$_SERVER` (contains server and execution environment info), `$_FILES` (contains uploaded files), `$_COOKIE` (contains cookie data), `$_SESSION` (stores user data across pages), and `$GLOBALS` (contains all global variables). These variables are automatically populated by PHP and can be accessed from any function or scope.",
  },
  {
    id: 2603,
    category: "PHP",
    subcategory: "PHP Cơ Bản",
    level: "beginner",
    q: "Sự khác biệt giữa `$_GET` và `$_POST` là gì?",
    a: "Cả hai đều thu thập dữ liệu từ form nhưng khác nhau về tính bảo mật và giới hạn dữ liệu. `$_GET` đính kèm dữ liệu vào URL (dễ nhìn thấy), giới hạn khoảng 2KB và không an toàn cho dữ liệu nhạy cảm. `$_POST` gửi dữ liệu trong body của request (ẩn với URL), không giới hạn dung lượng và an toàn hơn cho mật khẩu hay thông tin cá nhân. Dùng `$_GET` khi lọc/tìm kiếm và muốn bookmark URL, dùng `$_POST` cho các form nhập dữ liệu nhạy cảm.",
    q_en: "What's the difference between $_GET and $_POST?",
    a_en: "Both collect form data but differ in visibility and security. `$_GET` appends data to the URL making it visible (http://example.com?name=John), has a 2KB limit, and is less secure for sensitive data. `$_POST` sends data in the request body (hidden from URL), has much larger data capacity, and is more secure for passwords or personal information. Use `$_GET` for filtering/searching where visibility helps, and `$_POST` for sensitive data entry.",
  },
  {
    id: 2604,
    category: "PHP",
    subcategory: "PHP Cơ Bản",
    level: "beginner",
    q: "Quy tắc đặt tên biến trong PHP là gì?",
    a: "Biến trong PHP phải bắt đầu bằng ký tự `$`, tiếp theo là chữ cái (a-z, A-Z) hoặc dấu gạch dưới, chỉ chứa ký tự chữ số và dấu gạch dưới, không được có khoảng trắng, và phân biệt chữ hoa chữ thường (`$name` và `$Name` là hai biến khác nhau). Ví dụ hợp lệ: `$myVar`, `$_count`, `$user123`. Ví dụ không hợp lệ: `$123var` (bắt đầu bằng số), `$my-var` (có dấu gạch ngang), `$my var` (có khoảng trắng).",
    q_en: "What are the rules for naming PHP variables?",
    a_en: "PHP variables must follow these rules: start with `$` symbol, followed by a letter (a-z, A-Z) or underscore, contain only alphanumeric characters and underscores, cannot have spaces, and are case-sensitive (so `$name` and `$Name` are different). Valid examples: `$myVar`, `$_count`, `$user123`. Invalid examples: `$123var` (starts with number), `$my-var` (contains hyphen), `$my var` (contains space).",
  },
  {
    id: 2605,
    category: "PHP",
    subcategory: "PHP Cơ Bản",
    level: "beginner",
    q: "PHP có những kiểu dữ liệu nào?",
    a: 'PHP có 8 kiểu dữ liệu: String (chuỗi như "Hello"), Integer (số nguyên), Float (số thập phân), Boolean (true/false), Array (tập hợp giá trị), Object (thực thể của lớp), NULL (không có giá trị), và Resource (tham chiếu tài nguyên bên ngoài). Bốn kiểu đầu là scalar (kiểu đơn giản), Array và Object là kiểu phức hợp, NULL là kiểu đặc biệt, còn Resource dùng cho kết nối database hay file handle.',
    q_en: "What are PHP data types and what are the main ones?",
    a_en: 'PHP has 8 data types: String (text like "Hello"), Integer (whole numbers without decimals), Float (numbers with decimals), Boolean (true/false), Array (collection of values), Object (instance of a class), NULL (no value), and Resource (reference to external resource). The first four are scalars (simple types), Array and Object are compound types, NULL is special, and Resource is used for database connections and file handles.',
  },
  {
    id: 2606,
    category: "PHP",
    subcategory: "PHP Cơ Bản",
    level: "beginner",
    q: "Sự khác biệt giữa toán tử `==` và `===` trong PHP là gì?",
    a: '`==` kiểm tra giá trị có bằng nhau không nhưng cho phép chuyển đổi kiểu dữ liệu. Ví dụ `"5" == 5` trả về `true` vì PHP tự đổi chuỗi thành số. Trong khi `===` (so sánh nghiêm ngặt) kiểm tra cả giá trị lẫn kiểu dữ liệu, nên `"5" === 5` trả về `false`. Luôn dùng `===` trong code thực tế để tránh lỗi do PHP tự động ép kiểu và giúp code rõ ràng hơn.',
    q_en: "What is the difference between == and === operators?",
    a_en: 'Both are comparison operators but with different strictness. `==` checks if values are equal but allows type conversion. For example, `"5" == 5` returns true because PHP converts the string to a number. The `===` (strict equal) checks both value AND type, so `"5" === 5` returns false. Always use `===` in production code to avoid unexpected type juggling bugs and improve code clarity.',
  },
  {
    id: 2607,
    category: "PHP",
    subcategory: "PHP Cơ Bản",
    level: "beginner",
    q: "Cách làm việc với mảng trong PHP? Có mấy loại mảng?",
    a: 'PHP có ba loại mảng: Indexed array (truy cập theo vị trí: `$fruits[0]`), Associative array (truy cập theo key: `$person["name"]`), và Multidimensional array (mảng chứa mảng khác). Tạo mảng indexed: `$arr = ["apple", "banana"]`, mảng associative: `$arr = ["name" => "John", "age" => 30]`. Các hàm thông dụng: `count()` đếm phần tử, `array_push()` thêm phần tử, `array_merge()` gộp mảng, `in_array()` kiểm tra tồn tại, `array_keys()` và `array_values()` lấy key hoặc value.',
    q_en: "How do you work with arrays in PHP? What are the main types?",
    a_en: 'PHP has three array types: Indexed arrays (accessed by position: `$fruits[0]`), Associative arrays (accessed by keys: `$person["name"]`), and Multidimensional arrays (arrays containing arrays). Create indexed with `$arr = ["apple", "banana"]`, associative with `$arr = ["name" => "John", "age" => 30]`. Common functions: `count()` gets array length, `array_push()` adds elements, `array_merge()` combines arrays, `in_array()` checks existence, `array_keys()` and `array_values()` retrieve keys or values.',
  },
  {
    id: 2608,
    category: "PHP",
    subcategory: "PHP Cơ Bản",
    level: "beginner",
    q: "Các hàm xử lý chuỗi phổ biến trong PHP là gì?",
    a: 'Các hàm chuỗi quan trọng gồm: `strlen()` (lấy độ dài), `strpos()` (tìm vị trí chuỗi con), `str_replace()` (thay thế text), `substr()` (cắt chuỗi), `strtolower()` và `strtoupper()` (đổi chữ hoa/thường), `trim()` (xóa khoảng trắng đầu cuối), `explode()` (tách chuỗi thành mảng), `implode()` (ghép mảng thành chuỗi), `htmlspecialchars()` (escape HTML để bảo mật). \n\n**Ví dụ:** `explode(",", "apple,banana,orange")` tạo ra `["apple", "banana", "orange"]`.',
    q_en: "What are common string functions in PHP?",
    a_en: 'Essential string functions include: `strlen()` (get length), `strpos()` (find position of substring), `str_replace()` (replace text), `substr()` (extract portion), `strtolower()` and `strtoupper()` (change case), `trim()` (remove whitespace), `explode()` (split string into array), `implode()` (join array into string), `htmlspecialchars()` (escape HTML for security). For example: `explode(",", "apple,banana,orange")` creates `["apple", "banana", "orange"]`.',
  },
  {
    id: 2609,
    category: "PHP",
    subcategory: "PHP Cơ Bản",
    level: "beginner",
    q: "Sự khác biệt giữa `include` và `require` trong PHP là gì?",
    a: "Cả hai đều nhúng file PHP bên ngoài, nhưng khác nhau ở cách xử lý lỗi. `include` chỉ cảnh báo (E_WARNING) nếu file không tồn tại và tiếp tục chạy, còn `require` báo lỗi nghiêm trọng (E_ERROR) và dừng thực thi ngay. `include_once` và `require_once` chỉ nhúng file một lần duy nhất trong cùng script. Dùng `require` cho các file thiết yếu (như kết nối database) và `include` cho các thành phần không bắt buộc (như sidebar template).",
    q_en: "What is the difference between include and require?",
    a_en: "Both include external PHP files, but `include` produces a warning (E_WARNING) if the file isn't found and continues script execution, while `require` throws a fatal error (E_ERROR) and stops execution immediately. Additionally, `include_once` and `require_once` include a file only if it hasn't been included before in the same script. Use `require` for critical files (like database connection) and `include` for optional components (like sidebar templates).",
  },
  {
    id: 2610,
    category: "PHP",
    subcategory: "PHP Cơ Bản",
    level: "beginner",
    q: "Làm thế nào để xử lý dữ liệu form trong PHP?",
    a: 'Truy cập dữ liệu form qua superglobal: method GET dùng `$_GET["fieldname"]`, POST dùng `$_POST["fieldname"]`. Luôn validate và sanitize: kiểm tra tồn tại với `isset($_POST["email"])`, validate định dạng với `filter_var()`, escape output với `htmlspecialchars()` để ngăn XSS. \n\n**Ví dụ:** `$email = htmlspecialchars($_POST["email"]); if(filter_var($email, FILTER_VALIDATE_EMAIL)) { // hợp lệ }`. Không bao giờ tin tưởng dữ liệu từ người dùng—luôn validate trước khi sử dụng.',
    q_en: "How do you handle form data in PHP?",
    a_en: 'Access form data using superglobals: for GET method use `$_GET["fieldname"]`, for POST use `$_POST["fieldname"]`. Always validate and sanitize: check if data exists with `isset($_POST["email"])`, validate format with `filter_var()`, escape output with `htmlspecialchars()` to prevent XSS. \n\n**Example:** `$email = htmlspecialchars($_POST["email"]); if(filter_var($email, FILTER_VALIDATE_EMAIL)) { // valid }`. Never trust user input—always validate before using.',
  },
  {
    id: 2611,
    category: "PHP",
    subcategory: "PHP Cơ Bản",
    level: "beginner",
    q: "Các loại lỗi trong PHP là gì và cách xử lý chúng?",
    a: "PHP có ba loại lỗi chính: Notice (không nghiêm trọng, không dừng script như truy cập biến chưa định nghĩa), Warning (nghiêm trọng hơn nhưng script vẫn chạy như mở file không hợp lệ), và Fatal error (nghiêm trọng, dừng thực thi như gọi hàm chưa định nghĩa). Xử lý lỗi bằng: `error_reporting(E_ALL)` hiển thị mọi lỗi, `set_error_handler()` xử lý tùy chỉnh, hoặc dùng try-catch cho exceptions. Trên môi trường production, ghi lỗi vào file log thay vì hiển thị cho người dùng.",
    q_en: "What are PHP error types and how are they handled?",
    a_en: "PHP has three main error types: Notices (non-critical, doesn't stop execution like undefined variable), Warnings (more serious but script continues like opening invalid file), and Fatal errors (severe, stops execution like calling undefined function). Handle errors with error handlers: set `error_reporting(E_ALL)` to show all errors, use `set_error_handler()` for custom handling, or use try-catch blocks for exceptions. In production, log errors to files instead of displaying to users.",
  },
  {
    id: 2612,
    category: "PHP",
    subcategory: "PHP Cơ Bản",
    level: "beginner",
    q: "Cách định nghĩa và sử dụng hằng số (constant) trong PHP?",
    a: 'Định nghĩa hằng số bằng `define("CONSTANT_NAME", value)` hoặc `const CONSTANT_NAME = value;`. Hằng số không có dấu `$`, không thể thay đổi sau khi định nghĩa, là global mà không cần dùng từ khóa `global`, và phân biệt chữ hoa thường theo mặc định. Dùng hằng cho giá trị cấu hình: `define("DB_HOST", "localhost"); echo DB_HOST;`. Nên dùng hằng thay biến cho các giá trị không bao giờ thay đổi trong quá trình thực thi.',
    q_en: "How do you define and use constants in PHP?",
    a_en: 'Define constants with `define("CONSTANT_NAME", value)` or `const CONSTANT_NAME = value;`. Constants have no dollar sign, cannot be changed after definition, are global without needing the `global` keyword, and are case-sensitive by default. Use constants for configuration values: `define("DB_HOST", "localhost"); echo DB_HOST;`. Constants are preferred over variables for values that never change during execution.',
  },

  // ─── PHP OOP ──────────────────────────────────────────────────────────────

  {
    id: 2613,
    category: "PHP",
    subcategory: "PHP OOP",
    level: "intermediate",
    q: "Class và Object trong PHP khác nhau như thế nào?",
    a: "Class là bản thiết kế (blueprint) định nghĩa thuộc tính và phương thức, còn Object là một thực thể cụ thể được tạo ra từ class đó. Hãy nghĩ class như khuôn bánh và object như những chiếc bánh thực sự. Khai báo class: `class User { public $name; public function getName() { return $this->name; } }`. Tạo object: `$user = new User();`. Mỗi object có bản sao thuộc tính riêng nhưng chia sẻ chung phần code của phương thức.",
    q_en: "What are classes and objects in PHP? What's the difference?",
    a_en: "A class is a blueprint or template defining properties (attributes) and methods (functions). An object is a specific instance created from the class. Think of class like a cookie cutter and objects like the actual cookies. Define a class with `class User { public $name; public function getName() { return $this->name; } }`. Create an object with `$user = new User();`. Each object has its own copy of properties but shares method code.",
  },
  {
    id: 2614,
    category: "PHP",
    subcategory: "PHP OOP",
    level: "intermediate",
    q: "Kế thừa (inheritance) trong PHP là gì?",
    a: "Kế thừa cho phép class con kế thừa thuộc tính và phương thức từ class cha, giúp tái sử dụng code. Dùng `class Student extends User` để kế thừa. Class con có thể truy cập các thành phần public và protected của cha, override phương thức cha, và thêm chức năng mới. Gọi phương thức cha bằng `parent::methodName()`. \n\n**Ví dụ:** `class Admin extends User` nghĩa là Admin có tất cả thuộc tính của User nhưng bổ sung thêm các phương thức đặc thù của admin. Giúp giảm trùng lặp code và tạo ra cấu trúc phân cấp hợp lý.",
    q_en: "What is inheritance in PHP?",
    a_en: "Inheritance allows a child class to inherit properties and methods from a parent class, promoting code reuse. Use `class Student extends User` to inherit. The child class can access public and protected members of parent, override parent methods, and add new functionality. Use parent::methodName() to call parent's version. \n\n**Example:** `class Admin extends User` means Admin gets User's properties but can have additional admin-specific methods. Reduces code duplication and creates logical hierarchies.",
  },
  {
    id: 2615,
    category: "PHP",
    subcategory: "PHP OOP",
    level: "intermediate",
    q: "PHP có hỗ trợ đa kế thừa không? Làm thế nào để đạt được chức năng tương tự?",
    a: "PHP không hỗ trợ đa kế thừa (một class chỉ có thể extends một class cha). Thay vào đó, dùng interface: một class có thể implement nhiều interface. \n\n**Ví dụ:** `class Employee implements Payable, Manageable` trong đó cả hai interface đều định nghĩa contract mà class phải thực hiện. Ngoài ra còn có trait để tái sử dụng code: `trait Logger { public function log() {} }` rồi `class User { use Logger; }` để chia sẻ phương thức giữa các class không liên quan mà không cần kế thừa.",
    q_en: "Does PHP support multiple inheritance? How do you achieve similar functionality?",
    a_en: "No, PHP doesn't support multiple inheritance (a class can only extend one parent). Instead, use interfaces: a class can implement multiple interfaces. \n\n**Example:** `class Employee implements Payable, Manageable` where both interfaces define contracts the class must fulfill. Also use traits for code reuse: `trait Logger { public function log() {} }` then `class User { use Logger; }` to share methods across unrelated classes without inheritance.",
  },
  {
    id: 2616,
    category: "PHP",
    subcategory: "PHP OOP",
    level: "intermediate",
    q: "Interface trong PHP là gì và khi nào nên dùng?",
    a: "Interface định nghĩa một hợp đồng (contract)—tập hợp các phương thức mà class implement bắt buộc phải có. Khai báo: `interface UserRepository { public function find($id); public function save($user); }`. Bất kỳ class nào implement interface này đều phải triển khai tất cả phương thức đó. \n\n**Lợi ích:** đảm bảo tính nhất quán giữa các implementations, cho phép polymorphism, dễ viết test với mock. Dùng interface khi cần đảm bảo các phương thức nhất định tồn tại bất kể cách implement cụ thể là gì.",
    q_en: "What are interfaces and when should you use them?",
    a_en: "Interfaces define a contract—a set of methods that implementing classes must provide. Define with `interface UserRepository { public function find($id); public function save($user); }`. Any class implementing this interface must implement all these methods. \n\n**Benefits:** enforce consistency across implementations, enable polymorphism (treat different classes uniformly), make testing easier with mock implementations. Use interfaces when you need to guarantee certain methods exist regardless of implementation.",
  },
  {
    id: 2617,
    category: "PHP",
    subcategory: "PHP OOP",
    level: "intermediate",
    q: "Trait trong PHP là gì và chúng giải quyết vấn đề gì?",
    a: 'Trait cho phép tái sử dụng phương thức trong nhiều class không liên quan mà không cần kế thừa. Khai báo: `trait Logger { public function log($msg) { echo $msg; } }`. Dùng trong bất kỳ class nào: `class User { use Logger; }` và `class Product { use Logger; }` thì cả hai đều có phương thức log. Trait giải quyết vấn đề trùng lặp code khi nhiều class cần chức năng giống nhau nhưng không chia sẻ class cha. Chúng giống như tái sử dụng "ngang" trong khi kế thừa là tái sử dụng "dọc".',
    q_en: "What are Traits in PHP and how do they solve problems?",
    a_en: 'Traits allow reusing methods in multiple unrelated classes without inheritance. Define with `trait Logger { public function log($msg) { echo $msg; } }`. Use in any class: `class User { use Logger; }` and `class Product { use Logger; }` now both have the log method. Traits solve the problem of code duplication when multiple classes need the same functionality but don\'t share a parent. They\'re like "horizontal" reuse versus inheritance\'s "vertical" reuse.',
  },
  {
    id: 2618,
    category: "PHP",
    subcategory: "PHP OOP",
    level: "intermediate",
    q: "Magic methods trong PHP là gì? Cho ví dụ.",
    a: 'Magic methods là các phương thức đặc biệt tự động được gọi khi có hành động tương ứng xảy ra. Các magic method quan trọng: `__construct()` (chạy khi tạo object), `__destruct()` (chạy khi hủy object), `__get()` (truy cập thuộc tính không tồn tại), `__set()` (gán thuộc tính không tồn tại), `__call()` (gọi phương thức không tồn tại), `__toString()` (khi object được in như chuỗi). \n\n**Ví dụ:** truy cập `$user->email` private sẽ kích hoạt `__get("email")` để kiểm soát quyền truy cập. Magic method phải là public (trừ construct/destruct).',
    q_en: "What are magic methods in PHP? Give examples.",
    a_en: 'Magic methods are special methods that execute automatically in response to certain actions. Key ones: `__construct()` (runs when object created), `__destruct()` (runs when object destroyed), `__get()` (called when accessing inaccessible property), `__set()` (called when setting inaccessible property), `__call()` (called when accessing undefined method), `__toString()` (when object printed as string). \n\n**Example:** accessing private `$user->email` triggers `__get("email")` to control access. Magic methods must be public (except construct/destruct).',
  },
  {
    id: 2619,
    category: "PHP",
    subcategory: "PHP OOP",
    level: "intermediate",
    q: "Sự khác biệt giữa static method và instance method là gì?",
    a: "Instance method thuộc về object và truy cập dữ liệu riêng của object qua `$this`. Gọi bằng `$object->method()`. Static method thuộc về chính class, không phải instance, và không thể truy cập `$this`. Khai báo với `static function` và gọi bằng `ClassName::method()`. \n\n**Ví dụ:** `User::count()` (static, đếm tổng user) vs `$user->getName()` (instance, lấy tên user cụ thể). Static method hữu ích cho utility functions, factory methods, hoặc theo dõi dữ liệu cấp class.",
    q_en: "What is the difference between static and instance methods?",
    a_en: "Instance methods belong to an object and access object-specific data via `$this`. Call with `$object->method()`. Static methods belong to the class itself, not instances, and cannot access `$this`. Define with `static function` and call with `ClassName::method()` or `$object::method()`. \n\n**Example:** `User::count()` (static, returns total users) vs `$user->getName()` (instance, returns specific user's name). Static methods are useful for utility functions, factory methods, or tracking class-level data.",
  },
  {
    id: 2620,
    category: "PHP",
    subcategory: "PHP OOP",
    level: "intermediate",
    q: "Access modifier (public, private, protected) trong PHP là gì?",
    a: 'Access modifier kiểm soát khả năng hiển thị của thuộc tính và phương thức: `public` truy cập được từ mọi nơi (trong/ngoài class và subclass), `private` chỉ truy cập được bên trong class đó (không phải subclass), `protected` truy cập được trong class và subclass nhưng không từ bên ngoài. \n\n**Ví dụ:** `private $password` ngăn truy cập trực tiếp như `$user->password = "hacked"`, buộc phải dùng setter method để kiểm tra. Dùng `private` cho dữ liệu nội bộ, `protected` để subclass dùng, `public` cho giao diện ra bên ngoài.',
    q_en: "What are access modifiers (public, private, protected)?",
    a_en: 'Access modifiers control visibility of properties and methods: `public` accessible everywhere (inside/outside class and subclasses), `private` accessible only inside that class (not subclasses), `protected` accessible inside class and subclasses but not outside. \n\n**Example:** `private $password` prevents direct access like `$user->password = "hacked"`, forcing use of setter method for validation. Use `private` for internal data, `protected` for subclass access, `public` for intended external interfaces. Encapsulation improves security and maintainability.',
  },

  // ─── PHP Nâng Cao ─────────────────────────────────────────────────────────

  {
    id: 2621,
    category: "PHP",
    subcategory: "PHP Nâng Cao",
    level: "advanced",
    q: "PDO là gì và cách phòng chống SQL injection bằng PDO?",
    a: 'PDO (PHP Data Objects) là lớp trừu tượng cung cấp giao diện nhất quán để truy cập database. Phòng chống SQL injection bằng prepared statements: thay vì `"SELECT * FROM users WHERE id=$id"`, dùng `$stmt = $pdo->prepare("SELECT * FROM users WHERE id=?"); $stmt->execute([$id]);`. Placeholder `?` đảm bảo input của người dùng được xử lý như dữ liệu, không phải code thực thi. Named placeholder cũng tương tự: `"... WHERE id=:id"` rồi `execute([":id" => $id])`. Database engine sẽ không bao giờ parse tham số như SQL code.',
    q_en: "What is PDO and how does it prevent SQL injection?",
    a_en: 'PDO (PHP Data Objects) is an abstraction layer providing consistent database access. Prevent SQL injection with prepared statements: instead of `"SELECT * FROM users WHERE id=$id"`, use `$stmt = $pdo->prepare("SELECT * FROM users WHERE id=?"); $stmt->execute([$id]);`. The placeholder `?` ensures user input is treated as data, not executable code. Named placeholders work similarly: `"... WHERE id=:id"` then `execute([":id" => $id])`. The database engine never interprets the parameter as SQL code.',
  },
  {
    id: 2622,
    category: "PHP",
    subcategory: "PHP Nâng Cao",
    level: "advanced",
    q: "Session trong PHP hoạt động như thế nào và khác gì so với Cookie?",
    a: 'Session lưu dữ liệu người dùng trên server để duy trì trạng thái giữa các request. Bắt đầu bằng `session_start()`, rồi truy cập qua `$_SESSION["key"] = value`. Dữ liệu ở trên server, chỉ gửi session ID về trình duyệt qua cookie. Cookie lưu dữ liệu trên client, gửi kèm mọi request, giới hạn ~4KB, và người dùng có thể sửa đổi. Session an toàn hơn vì dữ liệu ẩn với client, phù hợp cho dữ liệu nhạy cảm. Dùng `session_destroy()` để kết thúc session, `unset($_SESSION["key"])` để xóa dữ liệu cụ thể.',
    q_en: "How do PHP sessions work and what's the difference from cookies?",
    a_en: 'Sessions store user data on the server, maintaining state across requests. Start with `session_start()`, then access with `$_SESSION["key"] = value`. Data persists on server, only a session ID sent to browser in a cookie. Cookies store data on the client, sent with every request, limited to ~4KB, and users can modify them. Sessions are more secure (data hidden from client), better for sensitive data. Use `session_destroy()` to end session, `unset($_SESSION["key"])` to remove specific data.',
  },
  {
    id: 2623,
    category: "PHP",
    subcategory: "PHP Nâng Cao",
    level: "advanced",
    q: "Namespace trong PHP là gì và tại sao nên dùng?",
    a: "Namespace tổ chức code và tránh xung đột tên. Khai báo với `namespace MyApp\\Models;` ở đầu file. Nhiều class có thể cùng tên nếu ở namespace khác nhau: `MyApp\\Models\\User` và `Admin\\Models\\User`. Import bằng `use MyApp\\Models\\User as MyUser;`. \n\n**Lợi ích:** tổ chức codebase lớn một cách logic, ngăn ô nhiễm global namespace, cho phép autoloading theo chuẩn PSR, cải thiện khả năng bảo trì. Nếu không có namespace, toàn bộ code cạnh tranh trong global namespace gây ra xung đột.",
    q_en: "What are namespaces and why should you use them?",
    a_en: "Namespaces organize code and avoid naming conflicts. Define with `namespace MyApp\\Models;` at the top of file. Multiple classes can have same name if in different namespaces: `MyApp\\Models\\User` vs `Admin\\Models\\User`. Import with `use MyApp\\Models\\User as MyUser;`. \n\n**Benefits:** organize large codebases logically, prevent global namespace pollution, enable autoloading with PSR standards, improve code maintainability. Without namespaces, all code competes for global name space causing conflicts.",
  },
  {
    id: 2624,
    category: "PHP",
    subcategory: "PHP Nâng Cao",
    level: "advanced",
    q: "Autoloading trong PHP là gì và cách triển khai?",
    a: 'Autoloading tự động tải file class mà không cần `require` thủ công. Dùng `spl_autoload_register()` để định nghĩa autoloader tùy chỉnh, hoặc tốt hơn là dùng autoloader của Composer theo chuẩn PSR-4. Với Composer, chỉ cần khai báo namespace và đường dẫn file trong `composer.json`, sau đó `require "vendor/autoload.php"` tải mọi thứ. \n\n**Ví dụ:** namespace `App\\Models\\User` tự động tải từ `app/Models/User.php`. Autoloading cải thiện tổ chức code và mở rộng tốt hơn so với require thủ công.',
    q_en: "What is autoloading and how do you implement it?",
    a_en: 'Autoloading automatically loads class files without manual `require` statements. Use `spl_autoload_register()` to define custom autoloader, or better, use Composer\'s autoloader which follows PSR-4 standards. With Composer, just declare namespace and file path in `composer.json`, then `require "vendor/autoload.php"` loads everything. \n\n**Example:** namespace `App\\Models\\User` automatically loads from `app/Models/User.php`. Autoloading improves code organization and scales better than manual requiring.',
  },
  {
    id: 2625,
    category: "PHP",
    subcategory: "PHP Nâng Cao",
    level: "advanced",
    q: "Generator trong PHP là gì và khi nào nên dùng?",
    a: "Generator là hàm dùng `yield` để trả về nhiều giá trị từng cái một mà không cần tải tất cả vào bộ nhớ. Khai báo: `function numbers() { yield 1; yield 2; yield 3; }` rồi lặp với `foreach(numbers() as $num)`. Tiết kiệm bộ nhớ cho dataset lớn: xử lý file với `yield` đọc từng dòng thay vì tải toàn bộ file. Dùng `yield $key => $value` cho cặp key-value. Generator chậm hơn cho dataset nhỏ nhưng không thể thiếu khi xử lý file lớn, streams hay chuỗi vô hạn.",
    q_en: "What are generators in PHP and when do you use them?",
    a_en: "Generators are functions that use `yield` to return multiple values one at a time without loading all into memory. Define with `function numbers() { yield 1; yield 2; yield 3; }` then iterate with `foreach(numbers() as $num)`. Memory-efficient for large datasets: processing a file with `yield` reads one line at a time instead of loading entire file. Use `yield $key => $value` for key-value pairs. Generators are slower for small datasets but essential for processing large files, streams, or infinite sequences.",
  },
  {
    id: 2626,
    category: "PHP",
    subcategory: "PHP Nâng Cao",
    level: "advanced",
    q: "Sự khác biệt giữa `unset()` và `unlink()` là gì?",
    a: '`unset()` xóa biến khỏi bộ nhớ, giải phóng giá trị của nó nhưng không xóa file nếu biến đó chứa tên file. Dùng để dọn dẹp: `unset($tempVar)`. `unlink()` xóa file khỏi hệ thống: `unlink("oldfile.txt")` xóa vĩnh viễn file đó. Lỗi phổ biến là dùng `unset()` nghĩ rằng nó xóa file—luôn dùng `unlink()` để xóa file. Trong ngữ cảnh database, `unset($result)` chỉ xóa biến kết quả còn kết nối vẫn tồn tại.',
    q_en: "What is the difference between unset() and unlink()?",
    a_en: '`unset()` removes a variable from memory, clearing its value but not deleting the file if it\'s a filename. Use for cleanup: `unset($tempVar)`. `unlink()` deletes files from the filesystem: `unlink("oldfile.txt")` permanently removes the file. In database context, `unset($result)` clears result variable while connection persists. Common mistake: using `unset()` thinking it deletes files—always use `unlink()` for file deletion.',
  },
  {
    id: 2627,
    category: "PHP",
    subcategory: "PHP Nâng Cao",
    level: "advanced",
    q: "Làm thế nào để phòng chống tấn công XSS trong PHP?",
    a: 'Tấn công XSS chèn script độc hại qua input của người dùng. Phòng chống bằng: (1) Luôn escape output với `htmlspecialchars()` chuyển đổi `<script>` thành `&lt;script&gt;`, (2) Validate và sanitize input với `filter_var()`, (3) Thiết lập Content Security Policy header: `header("Content-Security-Policy: default-src \'self\'")`, (4) Không bao giờ in trực tiếp dữ liệu người dùng. \n\n**Ví dụ:** `echo htmlspecialchars($_GET["search"], ENT_QUOTES, "UTF-8");` hiển thị an toàn. Nguyên tắc vàng: không tin bất cứ gì từ người dùng, escape mọi thứ trước khi hiển thị.',
    q_en: "How do you prevent Cross-Site Scripting (XSS) attacks in PHP?",
    a_en: 'XSS attacks inject malicious scripts via user input. Prevent with: (1) Always escape output with `htmlspecialchars()` converting `<script>` to `&lt;script&gt;`, (2) Validate and sanitize input with `filter_var()`, (3) Use content security policy headers: `header("Content-Security-Policy: default-src \'self\'")`, (4) Never directly output user data. \n\n**Example:** `echo htmlspecialchars($_GET["search"], ENT_QUOTES, "UTF-8");` safely displays user input. The golden rule: trust nothing from users, escape everything before displaying.',
  },
  {
    id: 2628,
    category: "PHP",
    subcategory: "PHP Nâng Cao",
    level: "advanced",
    q: "Cách xử lý ngoại lệ (exception) trong PHP?",
    a: 'Dùng khối try-catch-finally: `try { code có thể lỗi; } catch(Exception $e) { xử lý lỗi; } finally { code dọn dẹp; }`. Ném ngoại lệ tùy chỉnh: `throw new Exception("Không tìm thấy user");`. Tạo class exception riêng bằng cách extends `Exception` để phân loại lỗi cụ thể. Khối `finally` luôn chạy dù có exception hay không. \n\n**Ví dụ:** `try { $file = fopen("missing.txt", "r"); } catch(Exception $e) { echo "Lỗi: " . $e->getMessage(); }`. Xử lý exception đúng cách ngăn crash và cung cấp thông tin lỗi có ý nghĩa.',
    q_en: "How do you handle exceptions in PHP?",
    a_en: 'Use try-catch-finally blocks: `try { code that might fail; } catch(Exception $e) { handle error; } finally { cleanup code; }`. Throw custom exceptions: `throw new Exception("User not found");`. Create custom exception classes extending `Exception` for specific error types. The finally block always runs regardless of exception. \n\n**Example:** `try { $file = fopen("missing.txt", "r"); } catch(Exception $e) { echo "Error: " . $e->getMessage(); }`. Proper exception handling prevents crashes and provides meaningful error information.',
  },
  {
    id: 2629,
    category: "PHP",
    subcategory: "PHP Nâng Cao",
    level: "advanced",
    q: "Type hinting trong PHP là gì và tại sao quan trọng?",
    a: "Type hinting chỉ định kiểu dữ liệu kỳ vọng của tham số và kiểu trả về: `function getUserById(int $id): User { ... }`. \n\n**Lợi ích:** phát hiện lỗi sớm (truyền string thay vì int sẽ báo lỗi ngay), cải thiện khả năng đọc code, hỗ trợ IDE autocomplete, và đóng vai trò tài liệu. Kiểu scalar: int, string, float, bool. Kiểu object: tên class. Nullable: `?int` cho phép int hoặc null. Kiểu trả về quan trọng: `function isDone(): bool` rõ ràng cho biết hàm trả về gì. Type hint không bắt buộc nhưng rất khuyến nghị cho code dễ bảo trì.",
    q_en: "What is type hinting and why is it important?",
    a_en: "Type hinting specifies expected parameter and return types: `function getUserById(int $id): User { ... }`. \n\n**Benefits:** catch errors early (passing string instead of int fails immediately), improve code readability, enable IDE autocompletion, and serve as documentation. Scalar types: int, string, float, bool. Object types: class names. Arrays: array type. Nullable types: `?int` allows int or null. Return types essential: `function isDone(): bool` clearly shows what's returned. Type hints are optional but highly recommended for maintainable code.",
  },
  {
    id: 2630,
    category: "PHP",
    subcategory: "PHP Nâng Cao",
    level: "advanced",
    q: "Abstract class trong PHP là gì và khi nào nên dùng?",
    a: 'Abstract class là class không đầy đủ, không thể khởi tạo trực tiếp—chỉ có thể kế thừa. Dùng `abstract class Animal { abstract public function sound(); }`. Subclass bắt buộc phải triển khai abstract method: `class Dog extends Animal { public function sound() { return "Woof"; } }`. Abstract class có thể có method cụ thể và thuộc tính, khác với interface. Dùng khi muốn áp đặt cấu trúc nhưng có sẵn một số implementation mặc định: interface chỉ cho contracts thuần túy, abstract class cho chức năng cơ sở được chia sẻ.',
    q_en: "What are abstract classes and when do you use them?",
    a_en: 'Abstract classes are incomplete classes that cannot be instantiated directly—only extended. Use `abstract class Animal { abstract public function sound(); }`. Subclasses must implement abstract methods: `class Dog extends Animal { public function sound() { return "Woof"; } }`. Abstract classes can have concrete methods and properties, unlike interfaces. Use when you want to enforce structure with some default implementation: interfaces for contracts only, abstract classes for shared base functionality.',
  },

  // ─── Laravel Cơ Bản ───────────────────────────────────────────────────────

  {
    id: 2631,
    category: "Laravel",
    subcategory: "Laravel Cơ Bản",
    level: "beginner",
    q: "Laravel là gì và nó giải quyết những vấn đề gì?",
    a: "Laravel là framework PHP hiện đại với cú pháp thanh lịch và bộ công cụ phong phú cho phép phát triển ứng dụng nhanh. Nó giải quyết các vấn đề phổ biến: routing (ánh xạ URL), templating (engine Blade), truy cập database (Eloquent ORM), xác thực, validation, quản lý session và testing. Tuân theo mô hình MVC để tách biệt các mối quan tâm. Tính năng tích hợp sẵn tiết kiệm thời gian phát triển so với tự viết từ đầu. Tài liệu đầy đủ, cộng đồng sôi động và cập nhật thường xuyên khiến nó trở thành chuẩn công nghiệp cho dự án PHP.",
    q_en: "What is Laravel and what problems does it solve?",
    a_en: "Laravel is a modern PHP web framework providing elegant syntax and rich tools for rapid application development. It solves common problems: routing (URL mapping), templating (Blade engine), database access (Eloquent ORM), authentication, validation, session management, and testing. Follows MVC pattern separating concerns. Built-in features save development time vs. writing everything from scratch. Great documentation, active community, and regular updates make it industry standard for PHP projects.",
  },
  {
    id: 2632,
    category: "Laravel",
    subcategory: "Laravel Cơ Bản",
    level: "beginner",
    q: "Routing trong Laravel là gì và cách định nghĩa route?",
    a: 'Routing ánh xạ URL đến controller action. Định nghĩa trong `routes/web.php`: `Route::get("/users", [UserController::class, "index"])` ánh xạ GET request đến /users vào method index của UserController. Các method khác: `Route::post()`, `Route::put()`, `Route::delete()`. Route parameter: `Route::get("/users/{id}", ...)` bắt segment URL. Route name: `Route::get(...)->name("users.show")` cho phép tạo link bằng `route("users.show", ["id" => 1])`. Middleware: `Route::get(...)->middleware("auth")` thêm kiểm tra xác thực. Từ Laravel 8+ nên dùng cú pháp `[Controller::class, "method"]` thay vì string.',
    q_en: "What is routing in Laravel and how do you define routes?",
    a_en: 'Routing maps URLs to controller actions. Define in `routes/web.php`: `Route::get("/users", [UserController::class, "index"])` maps GET request to /users URL to UserController\'s index method. Other methods: `Route::post()`, `Route::put()`, `Route::delete()`. Route parameters: `Route::get("/users/{id}", ...)` captures URL segment. Route names: `Route::get(...)->name("users.show")` enables linking with `route("users.show", ["id" => 1])`. Middleware protection: `Route::get(...)->middleware("auth")` adds authentication check. Since Laravel 8+ use `[Controller::class, "method"]` syntax instead of string notation.',
  },
  {
    id: 2633,
    category: "Laravel",
    subcategory: "Laravel Cơ Bản",
    level: "beginner",
    q: "Blade templating engine trong Laravel là gì?",
    a: 'Blade là engine template đơn giản nhưng mạnh mẽ của Laravel. Biên dịch cú pháp mustache `{{ }}` thành code PHP, được cache để tăng hiệu suất. Các directive quan trọng: `{{ $variable }}` in biến (đã escape), `{!! $html !!}` in HTML thô, `@if`, `@foreach`, `@for` cho cấu trúc điều khiển, `@include("template")` nhúng view khác, `@yield("content")` trong layouts. \n\n**Ví dụ:** `@foreach($users as $user) <p>{{ $user->name }}</p> @endforeach`. Blade tách biệt trình bày khỏi logic, cải thiện khả năng bảo trì và dễ đọc.',
    q_en: "What is the Blade templating engine?",
    a_en: 'Blade is Laravel\'s simple yet powerful templating engine. Compiles mustache syntax `{{ }}` into PHP code, cached for performance. Key directives: `{{ $variable }}` outputs variable (escaped), `{!! $html !!}` outputs unescaped HTML, `@if`, `@foreach`, `@for` for control structures, `@include("template")` includes other views, `@yield("content")` in layouts. \n\n**Example:** `@foreach($users as $user) <p>{{ $user->name }}</p> @endforeach`. Blade separates presentation from logic, improving maintainability and readability.',
  },
  {
    id: 2634,
    category: "Laravel",
    subcategory: "Laravel Cơ Bản",
    level: "beginner",
    q: "Migration trong Laravel là gì và cách sử dụng?",
    a: 'Migration là định nghĩa schema database được quản lý bằng version control. Tạo bằng `php artisan make:migration create_users_table`. Định nghĩa bảng trong method `up()`: `Schema::create("users", function(Blueprint $table) { $table->increments("id"); $table->string("name"); ... })` và rollback trong `down()`. Chạy tất cả migration bằng `php artisan migrate`. \n\n**Ưu điểm:** theo dõi thay đổi schema trong git, rollback dễ dàng, nhất quán giữa các môi trường, tài liệu hóa cấu trúc bảng. Migration giải quyết vấn đề đồng bộ database trong team và khi deploy.',
    q_en: "What are Laravel migrations and how do you use them?",
    a_en: 'Migrations are version-controlled database schema definitions. Create with `php artisan make:migration create_users_table`. Define tables in `up()` method: `Schema::create("users", function(Blueprint $table) { $table->increments("id"); $table->string("name"); ... })` and rollback in `down()`. Run all pending with `php artisan migrate`. \n\n**Advantages:** track schema changes in version control, easy rollback, consistent across environments, document table structure. Migrations solve database synchronization problems across team and deployments.',
  },
  {
    id: 2635,
    category: "Laravel",
    subcategory: "Laravel Cơ Bản",
    level: "beginner",
    q: "Mô hình MVC là gì và Laravel áp dụng như thế nào?",
    a: 'MVC tách biệt các mối quan tâm: Model (logic dữ liệu/database), View (giao diện/UI), Controller (xử lý request/logic nghiệp vụ). Request của người dùng đi đến Router, gọi Controller thích hợp. Controller truy vấn Model lấy dữ liệu, rồi render View với dữ liệu đó. \n\n**Ví dụ:** GET /users → UsersController@index → User::all() → view("users.index", ["users" => $users]). Việc tách biệt cho phép developer làm việc độc lập, cải thiện khả năng test, và bảo trì code. Cấu trúc thư mục của Laravel (`app/Models`, `resources/views`, `app/Http/Controllers`) thực thi MVC.',
    q_en: "What is the MVC pattern and how does Laravel use it?",
    a_en: 'MVC separates concerns: Model (data/database logic), View (presentation/UI), Controller (request handling/business logic). User requests go to Router, which calls appropriate Controller. Controller queries Model for data, then renders View with that data. \n\n**Example:** GET /users → UsersController@index → User::all() → view("users.index", ["users" => $users]). Separation allows developers to work independently, improves testing, and makes code maintainable. Laravel\'s folder structure (`app/Models`, `resources/views`, `app/Http/Controllers`) enforces MVC.',
  },
  {
    id: 2636,
    category: "Laravel",
    subcategory: "Laravel Cơ Bản",
    level: "beginner",
    q: "Artisan là gì và có những lệnh phổ biến nào?",
    a: "Artisan là giao diện dòng lệnh của Laravel để tự động hóa các tác vụ phổ biến. Các lệnh thiết yếu: `php artisan migrate` (chạy migration), `php artisan make:controller UserController` (tạo controller), `php artisan make:model User` (tạo model), `php artisan serve` (khởi động server dev), `php artisan tinker` (shell tương tác để test), `php artisan route:list` (xem tất cả route), `php artisan cache:clear` (xóa cache). Artisan tiết kiệm thời gian bằng cách tự động hóa các tác vụ lặp đi lặp lại và đảm bảo tính nhất quán trong việc tạo code.",
    q_en: "What is Artisan and what are some common commands?",
    a_en: "Artisan is Laravel's command-line interface for common tasks. Essential commands: `php artisan migrate` (run database migrations), `php artisan make:controller UserController` (create controller), `php artisan make:model User` (create model), `php artisan serve` (start development server), `php artisan tinker` (interactive shell for testing), `php artisan route:list` (show all routes), `php artisan cache:clear` (clear cache). Artisan saves time automating repetitive tasks and enforces consistent code generation.",
  },
  {
    id: 2637,
    category: "Laravel",
    subcategory: "Laravel Cơ Bản",
    level: "beginner",
    q: "Controller trong Laravel là gì và có những method nào?",
    a: 'Controller xử lý logic request, thường tương ứng với một resource (User, Product). Tạo bằng `php artisan make:controller ProductController --resource` sinh ra các method CRUD: `index()` (hiển thị tất cả), `show($id)` (hiển thị một), `create()` (hiển thị form tạo), `store()` (lưu từ form), `edit()` (hiển thị form sửa), `update()` (lưu sửa đổi), `destroy()` (xóa). Controller xử lý request, tương tác với Model, và trả về View hay JSON. \n\n**Ví dụ:** `public function store(Request $request) { $product = Product::create($request->all()); return redirect()->route("products.show", $product); }`.',
    q_en: "What are Laravel controllers and what do they do?",
    a_en: 'Controllers handle request logic, typically corresponding to resources (User, Product). Create with `php artisan make:controller ProductController --resource` generating CRUD methods: `index()` (show all), `show($id)` (show one), `create()` (show form), `store()` (save from form), `edit()` (show edit form), `update()` (save edits), `destroy()` (delete). Controllers process requests, interact with Models, and return Views or JSON. \n\n**Example:** `public function store(Request $request) { $product = Product::create($request->all()); return redirect()->route("products.show", $product); }`.',
  },
  {
    id: 2638,
    category: "Laravel",
    subcategory: "Laravel Cơ Bản",
    level: "beginner",
    q: "Dependency injection trong Laravel hoạt động như thế nào?",
    a: "Dependency injection tự động cung cấp các dependency cần thiết cho class. Thay vì `new UserRepository()` bên trong controller, type-hint trong method: `public function index(UserRepository $repo) { return $repo->all(); }`. Service container của Laravel tự động khởi tạo và inject class. \n\n**Lợi ích:** dễ test hơn (inject mock repository), coupling lỏng lẻo (đổi implementation mà không sửa controller), code gọn hơn. Laravel xử lý injection tự động cho route controller, middleware, commands. Giúp code linh hoạt và dễ test mà không cần khởi tạo thủ công.",
    q_en: "What is dependency injection in Laravel?",
    a_en: "Dependency injection automatically provides required dependencies to classes. Instead of `new UserRepository()` inside controller, type-hint in method: `public function index(UserRepository $repo) { return $repo->all(); }`. Laravel's service container automatically instantiates and injects the class. \n\n**Benefits:** easier testing (inject mock repository), loose coupling (change implementation without modifying controller), cleaner code. Laravel handles injection automatically for route controllers, middleware, commands. Makes code flexible and testable without manual instantiation.",
  },
  {
    id: 2639,
    category: "Laravel",
    subcategory: "Laravel Cơ Bản",
    level: "intermediate",
    q: "Service Provider trong Laravel là gì?",
    a: "Service Provider bootstrap các dịch vụ của ứng dụng. Đăng ký trong mảng `providers` của `config/app.php`. Có hai method chính: `register()` (bind vào container) và `boot()` (truy cập các service đã bind). \n\n**Ví dụ:** AppServiceProvider đăng ký custom service, MailServiceProvider cài đặt mail. Định nghĩa binding: `$this->app->bind(PaymentInterface::class, StripePayment::class)`. Dùng cho: đăng ký event listener, publish config file, mở rộng tính năng. Provider chạy sớm trong lifecycle của Laravel, lý tưởng cho các tác vụ thiết lập. Tạo custom provider bằng `php artisan make:provider CustomServiceProvider`.",
    q_en: "What is a service provider in Laravel?",
    a_en: "Service Providers bootstrap application services. Register in `config/app.php` `providers` array. Two main methods: `register()` (bind into container) and `boot()` (access bound services). \n\n**Example:** AppServiceProvider registers custom services, MailServiceProvider sets up mail service. Define binding: `$this->app->bind(PaymentInterface::class, StripePayment::class)`. Use for: registering event listeners, publishing config files, extending features. Providers run early in Laravel lifecycle, ideal for setup tasks. Create custom provider with `php artisan make:provider CustomServiceProvider`.",
  },
  {
    id: 2640,
    category: "Laravel",
    subcategory: "Laravel Cơ Bản",
    level: "intermediate",
    q: "Validation rules trong Laravel là gì và cách validate dữ liệu form?",
    a: 'Validate dữ liệu đầu vào với rules trong controller: `$request->validate(["email" => "required|email", "name" => "required|string|max:100"])`. Các rule phổ biến: `required` (bắt buộc), `email` (email hợp lệ), `unique:table` (chưa có trong DB), `min:5|max:20` (độ dài), `confirmed` (khớp với field_confirmation), `regex:/pattern/` (khớp regex). Hiển thị lỗi trong view: `@if($errors->any()) @foreach($errors->all() as $error) {{ $error }} @endforeach @endif`. Validation bảo vệ khỏi lưu dữ liệu xấu, cải thiện UX với thông báo lỗi, và ngăn chặn vấn đề bảo mật.',
    q_en: "What are Laravel validation rules and how do you validate form data?",
    a_en: 'Validate incoming data with rules in controller: `$request->validate(["email" => "required|email", "name" => "required|string|max:100"])`. Common rules: `required` (must exist), `email` (valid email), `unique:table` (not already in DB), `min:5|max:20` (length), `confirmed` (matches field_confirmation), `regex:/pattern/` (matches regex). Show errors in view: `@if($errors->any()) @foreach($errors->all() as $error) {{ $error }} @endforeach @endif`. Validation saves from storing bad data, improves UX with error messages, prevents security issues.',
  },

  // ─── Eloquent & Database ──────────────────────────────────────────────────

  {
    id: 2641,
    category: "Laravel",
    subcategory: "Eloquent & Database",
    level: "intermediate",
    q: "Eloquent ORM là gì và tại sao tốt hơn SQL thuần?",
    a: 'Eloquent là lớp trừu tượng database của Laravel cung cấp giao diện hướng đối tượng để thao tác database. Thay vì SQL thuần `SELECT * FROM users WHERE id=1`, dùng `User::find(1)`. \n\n**Lợi ích:** không phụ thuộc vào database cụ thể (dễ chuyển đổi), tự động phòng chống SQL injection, code dễ đọc và bảo trì, relationships được tích hợp sẵn, tính năng tối ưu hóa query. Mỗi bảng có class Model tương ứng với các property khớp cột. Các method như `where()`, `orWhere()`, `orderBy()` có thể chain: `User::where("age", ">", 18)->orderBy("name")->get()`. Giảm code, tăng bảo mật, dễ test.',
    q_en: "What is Eloquent ORM and why is it better than raw SQL?",
    a_en: 'Eloquent is Laravel\'s database abstraction providing object-oriented interface to databases. Instead of raw SQL `SELECT * FROM users WHERE id=1`, use `User::find(1)`. \n\n**Benefits:** database-agnostic (switch databases easily), prevents SQL injection automatically, readable and maintainable code, relationships built-in, query optimization features. Each table has corresponding Model class with properties matching columns. Methods like `where()`, `orWhere()`, `orderBy()` chain queries fluently: `User::where("age", ">", 18)->orderBy("name")->get()`. Reduces code, improves security, enables testing.',
  },
  {
    id: 2642,
    category: "Laravel",
    subcategory: "Eloquent & Database",
    level: "intermediate",
    q: "Model trong Laravel là gì và cách tạo chúng?",
    a: 'Model đại diện cho bảng database theo cách hướng đối tượng. Tạo bằng `php artisan make:model User`. Các property: `protected $table` (override tên bảng), `protected $fillable` (cột được mass-assign như name, email), `protected $hidden` (loại khỏi output như password), `protected $casts` (ép kiểu như created_at thành date). \n\n**Ví dụ:** `$user = User::create(["name" => "John", "email" => "john@example.com"]);` tạo bản ghi. Mass assignment yêu cầu mảng `$fillable` để bảo mật—ngăn gán các trường nhạy cảm. Model kết nối cấu trúc database với code PHP.',
    q_en: "What are Laravel models and how do you create them?",
    a_en: 'Models represent database tables in object-oriented way. Create with `php artisan make:model User`. Properties: `protected $table` (override table name), `protected $fillable` (mass-assignable columns like name, email), `protected $hidden` (exclude from output like password), `protected $casts` (type casting like created_at as date). \n\n**Example:** `$user = User::create(["name" => "John", "email" => "john@example.com"]);` creates record. Mass assignment requires `$fillable` array for security—prevents assigning sensitive fields. Models connect database structure to PHP code.',
  },
  {
    id: 2643,
    category: "Laravel",
    subcategory: "Eloquent & Database",
    level: "intermediate",
    q: "Eloquent relationships là gì và có những loại nào?",
    a: 'Relationships định nghĩa mối liên kết giữa các model. One-to-Many: User có nhiều Post. Định nghĩa trong User model: `public function posts() { return $this->hasMany(Post::class); }`. Truy cập: `$user->posts`. Many-to-One (nghịch đảo): Post belongs to User. One-to-One: User có một Profile. Many-to-Many: Post có nhiều Tag. \n\n**Ví dụ:** `$post->tags()->attach($tagId)` tạo liên kết. Polymorphic: nhiều model cùng liên kết đến một model (như Comment trên cả Post và Video). Relationships cho phép query dữ liệu trực quan: `$user->posts()->where("published", true)->get()` thay vì SQL join phức tạp.',
    q_en: "What are Eloquent relationships and what types exist?",
    a_en: 'Relationships define connections between models. One-to-Many: User has many Posts. Define in User model: `public function posts() { return $this->hasMany(Post::class); }`. Then access: `$user->posts`. Many-to-One (inverse): Post belongs to User. One-to-One: User has one Profile. Many-to-Many: Posts have many Tags. \n\n**Example:** `$post->tags()->attach($tagId)` associates. Polymorphic: multiple models relate to one (like Comments on Posts and Videos). Relationships enable intuitive data querying: `$user->posts()->where("published", true)->get()` vs complex SQL joins.',
  },
  {
    id: 2644,
    category: "Laravel",
    subcategory: "Eloquent & Database",
    level: "intermediate",
    q: "Vấn đề N+1 query là gì và cách khắc phục trong Laravel?",
    a: 'Vấn đề N+1 xảy ra khi lấy records cha rồi truy cập relationship trên từng cái. \n\n**Ví dụ:** `$users = User::all();` (1 query), sau đó `foreach($users as $user) { echo $user->posts; }` (N query thêm). Kết quả: 1+N query tổng cộng rất kém hiệu quả. Khắc phục bằng eager loading: `User::with("posts")->get()` tải tất cả user và post chỉ trong 2 query. Chain: `User::with("posts.comments")->get()`. Dùng `load()` để lazy eager loading: `$users->load("posts")`. Kiểm tra bằng debugbar để xác nhận số lượng query. Eager loading rất quan trọng cho hiệu suất.',
    q_en: "What is the N+1 query problem and how do you fix it?",
    a_en: 'N+1 problem happens when fetching parent records, then accessing relationship on each one. \n\n**Example:** `$users = User::all();` (1 query), then `foreach($users as $user) { echo $user->posts; }` (N additional queries). Result: 1 + N queries total is inefficient. Fix with eager loading: `User::with("posts")->get()` loads all users AND their posts in just 2 queries. Also `with()` chaining: `User::with("posts.comments")->get()`. Use `load()` for lazy loading: `$users->load("posts")`. Check with debugbar to verify query count. Eager loading critical for performance.',
  },
  {
    id: 2645,
    category: "Laravel",
    subcategory: "Eloquent & Database",
    level: "intermediate",
    q: "Query scope trong Laravel là gì?",
    a: 'Scope là các ràng buộc query có thể tái sử dụng. Local scope trong model: `public function scopeActive($query) { return $query->where("active", true); }` rồi dùng `User::active()->get()`. Global scope áp dụng tự động, implement `GlobalScope`: hữu ích cho soft delete. Scope giảm trùng lặp và tăng khả năng đọc. \n\n**Ví dụ:** thay vì viết `User::where("active", true)->where("age", ">", 18)->get()` ở nhiều nơi, tạo `scopeAdults()` và `scopeActive()` rồi chain `User::active()->adults()->get()`. Scope giữ query DRY và đóng gói business logic trong model.',
    q_en: "What are query scopes in Laravel?",
    a_en: 'Scopes are reusable query constraints. Local scope in model: `public function scopeActive($query) { return $query->where("active", true); }` then use `User::active()->get()`. Global scope applied automatically, implement `GlobalScope`: useful for soft deletes. Scopes reduce duplication and improve readability. \n\n**Example:** instead of `User::where("active", true)->where("age", ">", 18)->get()` everywhere, create `scopeAdults()` and `scopeActive()` then chain `User::active()->adults()->get()`. Scopes keep queries DRY and encapsulate business logic in models.',
  },
  {
    id: 2646,
    category: "Laravel",
    subcategory: "Eloquent & Database",
    level: "intermediate",
    q: "Accessor và Mutator trong Eloquent là gì?",
    a: 'Accessor biến đổi dữ liệu khi đọc, mutator biến đổi khi ghi. Định nghĩa accessor: `protected function name(): Attribute { return Attribute::make(get: fn($value) => ucfirst($value)); }` tự động viết hoa chữ cái đầu khi truy cập. Mutator: `protected function email(): Attribute { return Attribute::make(set: fn($value) => strtolower($value)); }` chuyển sang chữ thường khi lưu. \n\n**Ví dụ:** `$user->email = "JOHN@EXAMPLE.COM"` lưu thành chữ thường, `echo $user->name` hiển thị viết hoa dù lưu là chữ thường. Accessor/mutator tập trung logic định dạng vào một chỗ.',
    q_en: "What are accessors and mutators in Eloquent?",
    a_en: 'Accessors transform data when reading, mutators transform when writing. Define accessor: `protected function name(): Attribute { return Attribute::make(get: fn($value) => ucfirst($value)); }` automatically capitalizes name when accessed. Mutator: `protected function email(): Attribute { return Attribute::make(set: fn($value) => strtolower($value)); }` converts to lowercase on save. \n\n**Example:** `$user->email = "JOHN@EXAMPLE.COM"` stores as lowercase, `echo $user->name` (John) displays capitalized even if stored as john. Accessors/mutators centralize formatting logic.',
  },
  {
    id: 2647,
    category: "Laravel",
    subcategory: "Eloquent & Database",
    level: "intermediate",
    q: "Soft delete trong Laravel là gì?",
    a: "Soft delete đánh dấu record là đã xóa mà không thực sự xóa khỏi database. Thêm vào model: `use SoftDeletes;` và migration thêm `$table->softDeletes();` tạo cột `deleted_at`. Query tự động loại trừ record đã soft-delete: `User::all()` không bao gồm đã xóa. Truy cập bằng: `User::withTrashed()->get()` (bao gồm đã xóa), `User::onlyTrashed()->get()` (chỉ đã xóa), `$user->restore()` (khôi phục), `$user->forceDelete()` (xóa vĩnh viễn). \n\n**Lợi ích:** giữ dữ liệu để audit/phục hồi, hoàn tác xóa nhầm, tuân thủ quy định giữ bản ghi. Soft delete tăng độ an toàn dữ liệu.",
    q_en: "What is soft delete in Laravel?",
    a_en: "Soft delete marks records as deleted without removing from database. Add to model: `use SoftDeletes;` and migration includes `$table->softDeletes();` creating `deleted_at` column. Queries automatically exclude soft-deleted records: `User::all()` excludes deleted. Access with `User::withTrashed()->get()` (includes deleted), `User::onlyTrashed()->get()` (only deleted), `$user->restore()` (undelete), `$user->forceDelete()` (permanently remove). \n\n**Benefits:** preserve data for auditing/recovery, undo accidental deletions, comply with regulations keeping records. Soft deletes improve data safety without permanent loss.",
  },
  {
    id: 2649,
    category: "Laravel",
    subcategory: "Eloquent & Database",
    level: "intermediate",
    q: "Factory và Seeder trong Laravel là gì?",
    a: 'Factory tạo dữ liệu giả để test: `php artisan make:factory UserFactory`. Định nghĩa: `public function definition() { return ["name" => $this->faker->name, "email" => $this->faker->unique()->safeEmail]; }` rồi dùng `User::factory()->create()` hoặc `User::factory(10)->create()` cho 10 bản ghi. Seeder populate database: `php artisan make:seeder UserSeeder` rồi gọi factory `User::factory(100)->create()` và chạy bằng `php artisan db:seed`. Thiết yếu cho development (dữ liệu giả) và testing (dữ liệu nhất quán). Factory dùng thư viện Faker để tạo dữ liệu ngẫu nhiên thực tế.',
    q_en: "What are database factories and seeders in Laravel?",
    a_en: 'Factories generate fake data for testing: `php artisan make:factory UserFactory`. Define: `public function definition() { return ["name" => $this->faker->name, "email" => $this->faker->unique()->safeEmail]; }` then use `User::factory()->create()` or `User::factory(10)->create()` for 10 records. Seeders populate database: `php artisan make:seeder UserSeeder` then call factory `User::factory(100)->create()` and run with `php artisan db:seed`. Essential for development (fake data) and testing (consistent test data). Factories use Faker library for realistic random data.',
  },
  {
    id: 2650,
    category: "Laravel",
    subcategory: "Eloquent & Database",
    level: "intermediate",
    q: "Query builder trong Laravel là gì và có những ưu điểm gì?",
    a: 'Query builder là giao diện fluent để xây dựng query mà không cần SQL thuần. \n\n**Ví dụ:** `DB::table("users")->where("age", ">", 18)->orderBy("name")->get();`. \n\n**Ưu điểm:** không phụ thuộc database (dễ chuyển đổi), tự động phòng SQL injection (tham số được escape), cú pháp chain dễ đọc, hỗ trợ IDE autocomplete. Các method: `select()`, `where()`, `orWhere()`, `join()`, `groupBy()`, `having()`, `orderBy()`, `limit()`, `insert()`, `update()`, `delete()`. So với SQL thuần `DB::raw(...)`, query builder an toàn và dễ bảo trì hơn. Query builder là nền tảng của Eloquent.',
    q_en: "What is a query builder in Laravel and what are its advantages?",
    a_en: 'Query builder is fluent interface for building queries without raw SQL. \n\n**Example:** `DB::table("users")->where("age", ">", 18)->orderBy("name")->get();`. \n\n**Advantages:** database-agnostic (easily switch databases), prevents SQL injection (parameters escaped automatically), readable chain syntax, IDE autocomplete support. Methods: `select()`, `where()`, `orWhere()`, `join()`, `groupBy()`, `having()`, `orderBy()`, `limit()`, `insert()`, `update()`, `delete()`. Compared to raw SQL `DB::raw(...)`, query builder is safer and more maintainable. Query builder is foundation for Eloquent.',
  },

  // ─── Laravel Nâng Cao ─────────────────────────────────────────────────────

  {
    id: 2651,
    category: "Laravel",
    subcategory: "Laravel Nâng Cao",
    level: "advanced",
    q: "Service Container trong Laravel là gì và hoạt động như thế nào?",
    a: "Service Container là IoC (Inversion of Control) container quản lý các dependency của class. Thay vì tạo dependency thủ công, đăng ký một lần: `app()->bind(UserRepository::class, MySQLUserRepository::class)`. Sau đó inject ở bất kỳ đâu: `public function __construct(UserRepository $repo)`. Container tự động khởi tạo class đúng. \n\n**Lợi ích:** một điểm để thay đổi implementation (swap repository dễ dàng), cấu hình tập trung, cho phép test với mock, lazy loading service. Truy cập trực tiếp: `app()->make(UserRepository::class)` hoặc dùng facade. Service Container là lõi của Laravel cho phép dependency injection toàn bộ ứng dụng.",
    q_en: "What is the Laravel service container and how does it work?",
    a_en: "Service Container is IoC (Inversion of Control) container managing class dependencies. Instead of manually creating dependencies, register once: `app()->bind(UserRepository::class, MySQLUserRepository::class)`. Then inject anywhere: `public function __construct(UserRepository $repo)`. Container automatically instantiates correct class. \n\n**Benefits:** single point for changing implementations (swap repositories), centralized configuration, enables testing with mocks, lazy loading services. Access directly: `app()->make(UserRepository::class)` or use facades. Service container is Laravel's core enabling dependency injection throughout.",
  },
  {
    id: 2652,
    category: "Laravel",
    subcategory: "Laravel Nâng Cao",
    level: "advanced",
    q: "Event trong Laravel là gì và cách sử dụng?",
    a: "Event cho phép giao tiếp loosely coupled—khi điều gì đó xảy ra (event UserCreated), các listener phản ứng. Tạo event: `php artisan make:event UserCreated` với property `public $user`. Tạo listener: `php artisan make:listener SendWelcomeEmail --event=UserCreated`. Đăng ký trong `EventServiceProvider`. Dispatch từ model: `UserCreated::dispatch($user)`. Listener thực thi đồng bộ theo mặc định, hoặc queue với `implements ShouldQueue`. Event tách biệt code—logic tạo user không biết về email, notification. Giúp code dễ bảo trì và mở rộng hơn khi thêm chức năng mới.",
    q_en: "What are Laravel events and how do you use them?",
    a_en: "Events enable decoupled communication—when something happens (UserCreated event), listeners react. Create event: `php artisan make:event UserCreated` with `public $user` property. Create listener: `php artisan make:listener SendWelcomeEmail --event=UserCreated`. Register in `EventServiceProvider`. Dispatch from model: `UserCreated::dispatch($user)`. Listeners execute synchronously by default, or queue with `implements ShouldQueue`. Events decouple code—user creation logic doesn't know about emails, notifications. Makes code more maintainable and extensible.",
  },
  {
    id: 2653,
    category: "Laravel",
    subcategory: "Laravel Nâng Cao",
    level: "advanced",
    q: "Queue trong Laravel là gì và tại sao nên dùng?",
    a: "Queue trì hoãn các tác vụ tốn thời gian sang background worker. Thay vì gửi email trong request (làm chậm user), queue job: `Mail::queue(new SendEmail($user))` trả về ngay. Worker xử lý các queued job bất đồng bộ. Tạo job: `php artisan make:job ProcessPayment` với method `handle()`. Cấu hình trong `.env` với `QUEUE_DRIVER=redis`. Chạy worker: `php artisan queue:work`. \n\n**Lợi ích:** response nhanh hơn, xử lý traffic spike, retry job thất bại tự động, xử lý theo thứ tự. Thiết yếu cho: gửi email, xử lý ảnh, tạo báo cáo, gọi API. Queue cải thiện UX và độ tin cậy hệ thống.",
    q_en: "What are Laravel queues and why use them?",
    a_en: "Queues defer time-consuming tasks to background workers. Instead of sending email during request (slowing user down), queue job: `Mail::queue(new SendEmail($user))` returns immediately. Workers process queued jobs asynchronously. Create job: `php artisan make:job ProcessPayment` with `handle()` method. Configure in `.env` with `QUEUE_DRIVER=redis`. Run workers: `php artisan queue:work`. \n\n**Benefits:** faster responses, handle traffic spikes, retry failed jobs automatically, process tasks in order. Essential for: sending emails, image processing, report generation, API calls. Queues improve user experience and system reliability.",
  },
  {
    id: 2654,
    category: "Laravel",
    subcategory: "Laravel Nâng Cao",
    level: "advanced",
    q: "Middleware trong Laravel là gì và cách tạo custom middleware?",
    a: 'Middleware lọc các HTTP request/response. Tạo: `php artisan make:middleware CheckAge` với `handle($request, $next)` trả về `$next($request)`. Đăng ký global trong `kernel.php` `$middleware` (chạy mọi request) hoặc route-specific trong `$routeMiddleware`. \n\n**Ví dụ:** `Route::get("/admin", function() {...})->middleware("admin")` kiểm tra quyền admin. Middleware phổ biến: authentication (kiểm tra đăng nhập), CORS (cross-origin request), CSRF (ngăn tấn công). Middleware chain: `handle()` chạy trước controller, `$next()` tiếp tục đến middleware/controller tiếp theo, response đi ngược lại. Middleware xử lý cross-cutting concerns bên ngoài logic chính.',
    q_en: "What is middleware in Laravel and how do you create custom middleware?",
    a_en: 'Middleware filters HTTP requests/responses. Create: `php artisan make:middleware CheckAge` with `handle($request, $next)` returning `$next($request)`. Register globally in `kernel.php` `$middleware` (runs on every request) or route-specific in `$routeMiddleware`. \n\n**Example:** `Route::get("/admin", function() {...})->middleware("admin")` checks admin status. Common middleware: authentication (verify logged in), CORS (cross-origin requests), CSRF (prevent attacks). Middleware chains: `handle()` runs before controller, `$next()` continues to next middleware/controller, response continues backwards. Middleware handles cross-cutting concerns outside main logic.',
  },
  {
    id: 2656,
    category: "Laravel",
    subcategory: "Laravel Nâng Cao",
    level: "advanced",
    q: "Broadcasting trong Laravel là gì và khi nào nên dùng?",
    a: 'Broadcasting gửi dữ liệu thời gian thực đến client kết nối qua WebSocket. Tạo channel: `php artisan make:channel OrderChannel` xác định ai có thể lắng nghe. Broadcast event trong controller: `broadcast(new OrderShipped($order))`. Frontend dùng Laravel Echo đăng ký: `Echo.channel("order." + orderId).listen("OrderShipped", (e) => { ... })`. Use case: thông báo (tin nhắn mới xuất hiện ngay), presence channel (xem ai đang online), collaborative editing, live dashboard. Yêu cầu server hỗ trợ WebSocket (Pusher, Ably, hoặc self-hosted Soketi). Broadcasting tạo ra trải nghiệm real-time thay vì polling lãng phí tài nguyên.',
    q_en: "What is broadcasting in Laravel and when do you use it?",
    a_en: 'Broadcasting sends real-time data to connected clients via WebSockets. Create channel: `php artisan make:channel OrderChannel` authorizing who can listen. Broadcast event in controller: `broadcast(new OrderShipped($order))`. Frontend with Laravel Echo subscribes: `Echo.channel("order." + orderId).listen("OrderShipped", (e) => { ... })`. Use cases: notifications (new message appears instantly), presence channels (show who\'s online), collaborative editing, live dashboards. Requires server supporting WebSockets (Pusher, Ably, or self-hosted Soketi). Broadcasting creates interactive real-time experiences vs. polling which wastes resources.',
  },
  {
    id: 2657,
    category: "Laravel",
    subcategory: "Laravel Nâng Cao",
    level: "advanced",
    q: "Policy trong Laravel là gì và cách authorize action?",
    a: 'Policy tập trung hóa logic phân quyền. Tạo: `php artisan make:policy PostPolicy --model=Post` định nghĩa các method như `view()`, `create()`, `update()`, `delete()`. Đăng ký trong `AuthServiceProvider`. \n\n**Ví dụ:** `public function update(User $user, Post $post) { return $user->id === $post->user_id; }`. Dùng trong controller: `$this->authorize("update", $post);` hoặc inline `if (auth()->user()->can("update", $post))`. \n\n**Lợi ích:** quy tắc auth tập trung, tái sử dụng qua nhiều controller, dễ test, dễ đọc. Policy cải thiện bảo mật bằng cách ngăn các hành động trái phép với quy tắc phân quyền rõ ràng.',
    q_en: "What are Laravel policies and how do you authorize actions?",
    a_en: 'Policies centralize authorization logic. Create: `php artisan make:policy PostPolicy --model=Post` defining methods like `view()`, `create()`, `update()`, `delete()`. Register in `AuthServiceProvider`. \n\n**Example:** `public function update(User $user, Post $post) { return $user->id === $post->user_id; }`. Use in controller: `$this->authorize("update", $post);` or inline `if (auth()->user()->can("update", $post))`. \n\n**Benefits:** centralized auth rules, reusable across controllers, testable, readable. Policies improve security by preventing unauthorized actions with clear authorization rules.',
  },
  {
    id: 2658,
    category: "Laravel",
    subcategory: "Laravel Nâng Cao",
    level: "advanced",
    q: "Testing trong Laravel là gì và có những loại test nào?",
    a: 'Laravel cung cấp công cụ PHPUnit testing: Unit test kiểm tra logic của một class đơn lẻ, Feature test kiểm tra chức năng end-to-end. Ví dụ feature test: `$this->post("/login", ["email" => "...", "password" => "..."])->assertRedirect("/dashboard");`. Dùng assertion: `$this->assertEquals()`, `$this->assertDatabaseHas()`, `$this->assertStatus(200)`. Mocking: `$this->mock(UserRepository::class, function($mock) { ... });`. \n\n**Lợi ích:** phát hiện bug sớm, refactor an toàn, document hành vi mong muốn, ngăn regression. Tạo test: `php artisan make:test UserTest --unit` (unit) hoặc không có `--unit` (feature). Test là nền tảng của độ tin cậy và khả năng bảo trì.',
    q_en: "What is Laravel testing and what are the main testing types?",
    a_en: 'Laravel provides tools for PHPUnit testing: Unit tests verify single class logic, Feature tests test end-to-end functionality. Example feature test: `$this->post("/login", ["email" => "...", "password" => "..."])->assertRedirect("/dashboard");`. Use assertions: `$this->assertEquals()`, `$this->assertDatabaseHas()`, `$this->assertStatus(200)`. Mocking: `$this->mock(UserRepository::class, function($mock) { ... });`. \n\n**Benefits:** catch bugs early, safe refactoring, document intended behavior, prevent regressions. Create tests: `php artisan make:test UserTest --unit` (unit) or without `--unit` (feature). Testing critical for reliability and maintainability.',
  },
  {
    id: 2659,
    category: "Laravel",
    subcategory: "Laravel Nâng Cao",
    level: "advanced",
    q: "Facade pattern trong Laravel là gì?",
    a: "Facade cung cấp giao diện giống static đến các service trong container. `Auth::user()` gọi authentication service bên dưới theo kiểu static. Các facade khác: `DB::table()`, `Cache::get()`, `Mail::send()`. Bên dưới: facade `Auth` resolve thành `\\Illuminate\\Auth\\AuthManager` từ container. \n\n**Ưu điểm:** cú pháp static tiện lợi, vẫn có thể inject dependency, ý định rõ ràng, dễ mock trong test. Thay vì inject `AuthManager` khắp nơi, dùng `Auth::user()`. Tạo custom facade: extend class `Facade` và định nghĩa `getFacadeAccessor()` trả về key trong container. Facade cân bằng sự tiện lợi với lợi ích của dependency injection.",
    q_en: "What is the Laravel facade pattern?",
    a_en: "Facades provide static-like interface to underlying services in container. `Auth::user()` calls underlying authentication service statically. Other facades: `DB::table()`, `Cache::get()`, `Mail::send()`. Behind scenes: `Auth` facade resolves to `\\Illuminate\\Auth\\AuthManager` from container. \n\n**Advantages:** convenient static syntax, still dependency-injectable, clear intent, easy to mock in tests. \n\n**Example:** instead of injecting `AuthManager` everywhere, use `Auth::user()`. Create custom facade: extend `Facade` class and define `getFacadeAccessor()` returning container key. Facades balance convenience (static look) with dependency injection benefits.",
  },
  {
    id: 2660,
    category: "Laravel",
    subcategory: "Laravel Nâng Cao",
    level: "advanced",
    q: "Collection trong Laravel là gì và cách sử dụng?",
    a: 'Collection cung cấp các method mạnh mẽ để làm việc với mảng dữ liệu. Eloquent query trả về collection: `$users = User::all();` rồi `$users->map(fn($u) => $u->email)->unique()->sort()`. Các method quan trọng: `map()` (biến đổi), `filter()` (lọc), `each()` (lặp), `pluck()` (lấy cột), `groupBy()` (nhóm), `chunk()` (chia nhỏ), `diff()` (so sánh), `merge()` (gộp). \n\n**Ví dụ:** `User::all()->where("active", true)->pluck("email")->all()` lấy email user đang hoạt động. Collection hỗ trợ chaining, lazy loading, và functional programming. Gọn hơn vòng lặp thủ công, ít bug hơn, hiệu suất được tối ưu.',
    q_en: "What are Laravel collections and how do you use them?",
    a_en: 'Collections provide powerful methods for working with arrays. Eloquent queries return collections: `$users = User::all();` then `$users->map(fn($u) => $u->email)->unique()->sort()`. Key methods: `map()` (transform), `filter()` (select matching), `each()` (iterate), `pluck()` (extract column), `groupBy()` (group items), `chunk()` (split into groups), `diff()` (compare), `merge()` (combine). \n\n**Example:** `User::all()->where("active", true)->pluck("email")->all()` gets active user emails. Collections chainable, lazy-load, support functional programming. Better than manual loops: cleaner, fewer bugs, optimized performance.',
  },

  // ─── PHP 8.3/8.4 Modern Features ──────────────────────────────────────────

  {
    id: 2661,
    category: "PHP",
    subcategory: "PHP Nâng Cao",
    level: "intermediate",
    q: "Typed class constants trong PHP 8.3 là gì và tại sao nên dùng?",
    a: 'Typed class constants cho phép khai báo kiểu dữ liệu cho hằng số trong class, tương tự typed properties. \n\n**Ví dụ:** `public const int MAX_USERS = 1000;` hoặc `public const string API_KEY = "secret";`. \n\n**Lợi ích:** tăng type safety, IDE autocomplete chính xác hơn, ngăn nhầm kiểu dữ liệu vô tình, tự document code. Kiểu phải khớp với giá trị gán tại thời điểm khai báo—PHP sẽ báo lỗi nếu không khớp. Đây là tính năng nhỏ nhưng quan trọng giúp codebase PHP lớn dễ bảo trì hơn.',
    q_en: "What are typed class constants in PHP 8.3 and why are they useful?",
    a_en: 'Typed class constants allow declaring the type of a class constant, similar to typed properties. \n\n**Example:** `public const int MAX_USERS = 1000;` or `public const string API_KEY = "secret";`. \n\n**Benefits:** improved type safety, better IDE autocompletion, prevent accidental type mismatches, self-documenting code. The type must match the assigned value at declaration time—PHP will error on mismatch. A small but important feature that makes large PHP codebases more maintainable.',
  },
  {
    id: 2662,
    category: "PHP",
    subcategory: "PHP Nâng Cao",
    level: "advanced",
    q: "Fibers trong PHP 8.1 là gì và khác threads như thế nào?",
    a: 'Fibers là các luồng nhẹ (lightweight) ở user-space, cho phép tạm dừng và tiếp tục thực thi code mà không cần đa luồng OS. \n\n**Ví dụ:** `$fiber = new Fiber(function() { $value = Fiber::suspend("hello"); echo $value; }); $fiber->start(); $fiber->resume("world");`. Khác với threads: Fibers không tạo tiến trình OS riêng, tiêu tốn ít bộ nhớ hơn, không cần lock/mutex. Ứng dụng: xử lý nhiều I/O đồng thời (API call, query DB) trong một request. Các thư viện như Amp và ReactPHP dùng Fibers để xây dựng server phi đồng bộ hiệu năng cao.',
    q_en: "What are Fibers in PHP 8.1 and how are they different from threads?",
    a_en: 'Fibers are lightweight user-space coroutines that enable pausing and resuming code execution without OS-level threading. \n\n**Example:** `$fiber = new Fiber(function() { $value = Fiber::suspend("hello"); echo $value; }); $fiber->start(); $fiber->resume("world");`. Unlike threads: Fibers don\'t create separate OS processes, consume less memory, no need for locks/mutexes. Use cases: handling multiple concurrent I/O (API calls, DB queries) in one request. Libraries like Amp and ReactPHP use Fibers to build high-performance async servers.',
  },
  {
    id: 2664,
    category: "PHP",
    subcategory: "PHP OOP",
    level: "intermediate",
    q: "Readonly classes trong PHP 8.2 có những ràng buộc gì?",
    a: "Readonly classes ngăn sửa đổi các public property sau khi khởi tạo. Ràng buộc cụ thể: mọi property phải có kiểu (không được khai báo untyped), không thể có static property, không dùng được `#[AllowDynamicProperties]`, và mỗi property phải được gán giá trị trong constructor. \n\n**Ví dụ:** `readonly class Address { public function __construct(public string $street, public string $city) {} }`. Rất phù hợp để xây dựng Value Object và DTO bất biến. \n\n**Lưu ý:** class con cũng phải là readonly nếu muốn extend readonly class.",
    q_en: "What are the constraints of readonly classes introduced in PHP 8.2?",
    a_en: "Readonly classes prevent property modification after instantiation. Specific constraints: all properties must be typed (no untyped declarations allowed), cannot declare static properties, cannot use `#[AllowDynamicProperties]`, and every property must receive a value in the constructor. \n\n**Example:** `readonly class Address { public function __construct(public string $street, public string $city) {} }`. Ideal for immutable Value Objects and DTOs. \n\n**Note:** child classes must also be readonly when extending a readonly class.",
  },
  {
    id: 2665,
    category: "PHP",
    subcategory: "PHP OOP",
    level: "intermediate",
    q: "Enums trong PHP 8.1 là gì và khi nào nên dùng thay constants?",
    a: "Enums định nghĩa tập hợp giá trị cố định, hợp lệ với type safety đầy đủ. \n\n**Ví dụ:** `enum OrderStatus { case PENDING; case SHIPPED; case DELIVERED; }` rồi `$order->status = OrderStatus::PENDING;`. Hơn constants ở điểm: type-checked (không thể gán giá trị tùy ý), hỗ trợ method bên trong enum, tích hợp với Eloquent qua cast. Backed enum có value: `enum Status: string { case ACTIVE = 'active'; case INACTIVE = 'inactive'; }` lưu vào DB dễ dàng. Dùng cho: trạng thái đơn hàng, vai trò người dùng, phương thức thanh toán—bất kỳ tập giá trị có hạn nào cần type safety.",
    q_en: "What are Enums in PHP 8.1 and when should you use them instead of constants?",
    a_en: "Enums define a fixed, valid set of values with full type safety. \n\n**Example:** `enum OrderStatus { case PENDING; case SHIPPED; case DELIVERED; }` then `$order->status = OrderStatus::PENDING;`. Better than constants: type-checked (no arbitrary assignment), support methods inside the enum, integrate with Eloquent via casts. Backed enums have values: `enum Status: string { case ACTIVE = 'active'; case INACTIVE = 'inactive'; }` for easy DB storage. Use for: order statuses, user roles, payment methods—any finite set of values needing type safety.",
  },
  {
    id: 2666,
    category: "PHP",
    subcategory: "PHP Nâng Cao",
    level: "advanced",
    q: "Value Objects trong PHP là gì và khi nào nên dùng thay vì kiểu nguyên thủy?",
    a: "Value Objects là các object bất biến đại diện cho khái niệm nghiệp vụ (Money, Email, Address) thay vì dùng kiểu nguyên thủy dễ sai. \n\n**Ví dụ:** thay `$price = 99.99` (float dễ lỗi làm tròn), dùng `new Money(9999, 'VND')` với validation trong constructor. \n\n**Lợi ích:** type safety, ngôn ngữ domain rõ ràng, đóng gói logic validation, ngăn trạng thái không hợp lệ. `new Email('user@example.com')` sẽ throw exception nếu format sai ngay khi tạo object. Laravel Casts có thể tự hydrate Value Object từ DB. Áp dụng sớm để bắt bug tại compile-time thay vì runtime.",
    q_en: "What are Value Objects in PHP and when should you use them instead of primitives?",
    a_en: "Value Objects are immutable objects representing domain concepts (Money, Email, Address) instead of error-prone primitives. \n\n**Example:** instead of `$price = 99.99` (float rounding bug-prone), use `new Money(9999, 'VND')` with constructor validation. \n\n**Benefits:** type safety, clear domain language, encapsulated validation logic, prevent invalid states. `new Email('user@example.com')` throws exception on invalid format at creation time. Laravel Casts can hydrate Value Objects from DB automatically. Adopt early to catch bugs at object creation rather than at runtime.",
  },

  // ─── Laravel 11/12 Modern Stack ────────────────────────────────────────────

  {
    id: 2667,
    category: "Laravel",
    subcategory: "Laravel Nâng Cao",
    level: "intermediate",
    q: "Laravel Reverb là gì và khác Pusher như thế nào?",
    a: "Reverb là WebSocket server first-party của Laravel, ra mắt cùng Laravel 11 (2024) và hỗ trợ từ Laravel 10+ để broadcasting sự kiện real-time. Khác với Pusher (dịch vụ bên thứ ba tính phí): Reverb tự host (self-hosted), mã nguồn mở, miễn phí, và dùng cùng Broadcasting facade API nên có thể chuyển đổi mà không cần sửa code. Cấu hình trong `config/reverb.php`. Frontend vẫn dùng Laravel Echo như bình thường. \n\n**Ưu điểm:** không phụ thuộc external service, giảm chi phí, scale linh hoạt. Phù hợp cho: thông báo real-time, live dashboard, tính năng cộng tác, chat.",
    q_en: "What is Laravel Reverb and how does it differ from Pusher?",
    a_en: "Reverb is Laravel's first-party WebSocket server, launched with Laravel 11 (2024) and supports Laravel 10+ for real-time event broadcasting. Unlike Pusher (paid third-party service): Reverb is self-hosted, open-source, free, and uses the same Broadcasting facade API so you can switch without code changes. Configure in `config/reverb.php`. Frontend still uses Laravel Echo as usual. \n\n**Benefits:** no external service dependency, reduced costs, flexible scaling. Ideal for: real-time notifications, live dashboards, collaborative features, chat.",
  },
  {
    id: 2668,
    category: "Laravel",
    subcategory: "Laravel Nâng Cao",
    level: "intermediate",
    q: "Laravel Pennant là gì và cách dùng để quản lý feature flags?",
    a: "Pennant là package feature flag chính thức của Laravel (tích hợp sẵn từ Laravel 11+) để kiểm soát khả năng hiển thị tính năng. Hỗ trợ flag đơn giản và logic phức tạp theo class. \n\n**Ví dụ:** `Feature::define('new-checkout', fn() => rand(1,100) <= 50)` cho A/B testing, hoặc `Feature::define(PurchaseButton::class, fn($user) => $user->isPremium())` cho feature theo user. Dùng trong Blade: `@feature('flag-name')`, trong controller: `Feature::active('flag-name')`. Cho phép: deploy không downtime, rollout từng bước, thử nghiệm A/B, tắt/bật tính năng mà không cần deploy lại.",
    q_en: "What is Laravel Pennant and how does it enable feature flag management?",
    a_en: "Pennant is Laravel's official feature flag package (built-in from Laravel 11+) for controlling feature visibility. Supports simple string flags and complex class-based logic. \n\n**Example:** `Feature::define('new-checkout', fn() => rand(1,100) <= 50)` for A/B testing, or `Feature::define(PurchaseButton::class, fn($user) => $user->isPremium())` for user-specific features. Use in Blade: `@feature('flag-name')`, in controllers: `Feature::active('flag-name')`. Enables: zero-downtime deployments, gradual rollouts, A/B experiments, toggling features without redeployment.",
  },
  {
    id: 2669,
    category: "Laravel",
    subcategory: "Laravel Cơ Bản",
    level: "intermediate",
    q: "Pest là gì và tại sao các project Laravel mới nên dùng thay PHPUnit?",
    a: "Pest là testing framework hiện đại được xây dựng trên PHPUnit, dùng cú pháp fluent và `expect()` assertion thay vì `$this->assert*` cồng kềnh. \n\n**Lợi ích:** code test gọn sạch hơn, parallel testing mặc định, watch mode và profiling tích hợp, hỗ trợ architecture testing và snapshot testing, docs đẹp. Ví dụ Pest: `test('user có thể đăng nhập', function () { expect(true)->toBeTrue(); })`. Laravel docs chính thức khuyến nghị Pest mặc định. Không phải thay thế hoàn toàn PHPUnit—Pest chạy trên PHPUnit, chỉ là layer API đẹp hơn. Vẫn tương thích 100% với PHPUnit test cũ.",
    q_en: "What is Pest and why should new Laravel projects use it over PHPUnit?",
    a_en: "Pest is a modern testing framework built on PHPUnit using fluent syntax and `expect()` assertions instead of verbose `$this->assert*` methods. \n\n**Benefits:** cleaner test code, parallel testing by default, built-in watch mode and profiling, architecture testing and snapshot testing support, beautiful docs. \n\n**Example:** `test('user can login', function () { expect(true)->toBeTrue(); })`. Laravel official docs recommend Pest by default. Not a full replacement—Pest runs on PHPUnit, just a better API layer. 100% compatible with existing PHPUnit tests.",
  },
  {
    id: 2670,
    category: "Laravel",
    subcategory: "Laravel Cơ Bản",
    level: "intermediate",
    q: "Sự khác biệt giữa Sanctum và Passport cho API authentication là gì?",
    a: "Cả hai đều xác thực Laravel API nhưng phục vụ nhu cầu khác nhau. Sanctum: nhẹ, phù hợp cho SPA cùng domain và personal access token, dùng session cho SPA và token DB-less cho mobile/external app. Passport: triển khai đầy đủ OAuth 2.0, phù hợp cho ứng dụng bên thứ ba cần tích hợp (như OAuth của GitHub), nặng hơn nhưng linh hoạt cho B2B. Quy tắc đơn giản: Sanctum cho frontend của chính bạn (SPA, mobile app công ty), Passport khi bên khác cần tích hợp vào hệ thống. Từ Laravel 11+, Sanctum là default được khuyến nghị cho hầu hết trường hợp.",
    q_en: "What is the difference between Sanctum and Passport for API authentication?",
    a_en: "Both authenticate Laravel APIs but serve different needs. Sanctum: lightweight, good for same-domain SPAs and personal access tokens, session-based for SPAs and DB-less tokens for mobile/external apps. Passport: full OAuth 2.0 implementation for third-party app authentication (like GitHub OAuth apps), heavier but more flexible for B2B scenarios. Simple rule: Sanctum for your own frontend (SPA, company mobile app), Passport when external parties need to integrate into your system. From Laravel 11+, Sanctum is the recommended default for most use cases.",
  },
  {
    id: 2671,
    category: "Laravel",
    subcategory: "Eloquent & Database",
    level: "intermediate",
    q: "API Resources trong Laravel là gì và tại sao không trả về raw model data?",
    a: "API Resources chuyển đổi Eloquent model thành JSON response nhất quán mà không lộ schema DB. \n\n**Ví dụ:** `return UserResource::collection($users)` thay vì `return $users->toJson()`. \n\n**Lợi ích:** tự ẩn các field nhạy cảm (password, internal ID), transform format dữ liệu (snake_case sang camelCase), include relationship có điều kiện với `->when()`, tạo API contract ổn định cho frontend, version response độc lập với DB schema. Ngăn vô tình expose password hash, soft-deleted data. Tạo bằng: `php artisan make:resource UserResource`. Là best practice bắt buộc với bất kỳ API public nào.",
    q_en: "What are API Resources in Laravel and why not return raw model data?",
    a_en: "API Resources transform Eloquent models into consistent JSON responses without exposing database schema. \n\n**Example:** `return UserResource::collection($users)` vs `return $users->toJson()`. \n\n**Benefits:** automatically hide sensitive fields (passwords, internal IDs), transform data formats (snake_case to camelCase), conditionally include relationships with `->when()`, provide stable API contracts for frontend teams, version responses independently from DB schema. Prevents accidental exposure of password hashes, soft-deleted data. Create with: `php artisan make:resource UserResource`. Mandatory best practice for any public API.",
  },
  {
    id: 2672,
    category: "Laravel",
    subcategory: "Laravel Nâng Cao",
    level: "advanced",
    q: "DTOs (Data Transfer Objects) là gì và cách cải thiện kiến trúc code?",
    a: "DTO là các value object đơn giản đóng gói dữ liệu request/response không có business logic, đảm bảo type safety. \n\n**Ví dụ:** `class CreateUserDTO { public function __construct(public string $name, public string $email, public int $age) {} }`. \n\n**Lợi ích:** type hinting rõ ràng thay vì unpack array mù quáng, bắt lỗi type tại compile time, self-document tham số mong muốn, IDE autocomplete đầy đủ, dễ refactor qua nhiều controller/service. Phân biệt với Eloquent model (entity DB có ORM logic). Với PHP 8.2+ readonly class, DTO trở nên bất biến và an toàn hơn: `readonly class CreateUserDTO { ... }`.",
    q_en: "What are DTOs (Data Transfer Objects) and how do they improve code architecture?",
    a_en: "DTOs are simple value objects encapsulating request/response data without business logic, ensuring type safety. \n\n**Example:** `class CreateUserDTO { public function __construct(public string $name, public string $email, public int $age) {} }`. \n\n**Benefits:** clear type hinting instead of blind array unpacking, catch type errors at compile time, self-document expected parameters, full IDE autocompletion, easy refactoring across controllers/services. Distinguish from Eloquent models (DB entities with ORM logic). With PHP 8.2+ readonly classes, DTOs become immutable and safer: `readonly class CreateUserDTO { ... }`.",
  },

  // ─── Testing & Architecture ─────────────────────────────────────────────────

  {
    id: 2673,
    category: "Laravel",
    subcategory: "Laravel Nâng Cao",
    level: "advanced",
    q: "Architecture testing trong Laravel với Pest là gì và tại sao quan trọng?",
    a: "Architecture testing kiểm tra quy tắc cấu trúc code mà không cần thực thi logic, phát hiện vi phạm design sớm. Ví dụ với Pest: `arch('Service không được truy cập trực tiếp DB')->expect('App\\\\Services')->not->toUseClasses(['Illuminate\\\\Database\\\\Eloquent\\\\Model'])`. \n\n**Lợi ích:** ngăn tight coupling, bắt dependency injection bị bỏ qua, phát hiện cross-module dependency trái phép, enforce naming convention. Dùng cho: đảm bảo model nằm trong `Models/`, service trong `Services/`, controller chỉ gọi service. Vi phạm bị bắt tại CI thay vì runtime—tiết kiệm debug thời gian thực.",
    q_en: "What is architecture testing in Laravel with Pest and why is it important?",
    a_en: "Architecture testing enforces code structure rules without executing logic, catching design violations early. Example with Pest: `arch('Services should not access DB directly')->expect('App\\\\Services')->not->toUseClasses(['Illuminate\\\\Database\\\\Eloquent\\\\Model'])`. \n\n**Benefits:** prevent tight coupling, catch bypassed dependency injection, detect unauthorized cross-module dependencies, enforce naming conventions. Use for: ensuring models stay in `Models/`, services in `Services/`, controllers only call services. Violations caught at CI time instead of runtime—saves live debugging effort.",
  },
  {
    id: 2675,
    category: "Laravel",
    subcategory: "Laravel Cơ Bản",
    level: "intermediate",
    q: "Sự khác biệt giữa Feature tests và Unit tests trong Laravel là gì?",
    a: "Feature tests kiểm tra toàn bộ chu trình request/response bao gồm route, middleware, authentication, validation và database—bắt bug thực tế người dùng gặp. Unit tests cô lập từng component (Service, Model method) mà không có HTTP layer. Ví dụ feature test: kiểm tra flow đăng nhập end-to-end với DB thực. Ví dụ unit test: kiểm tra logic hash password trong isolation. Best practice 2026: viết 70% feature test, 30% unit test vì feature test mang lại ROI cao hơn cho Laravel app thông thường. Unit test phù hợp cho business logic phức tạp (tính giá, thuật toán). Pest nhấn mạnh feature test là chiến lược test chính.",
    q_en: "What is the difference between feature tests and unit tests in Laravel?",
    a_en: "Feature tests exercise the full request/response cycle including routes, middleware, authentication, validation, and database—catching realistic bugs users experience. Unit tests isolate individual components (Services, Model methods) without the HTTP layer. Example feature test: testing full login flow end-to-end with real DB. Example unit test: testing password hashing logic in isolation. 2026 best practice: write 70% feature tests, 30% unit tests since feature tests provide higher ROI for typical Laravel apps. Unit tests suit complex business logic (pricing engines, algorithms). Pest emphasizes feature tests as the primary testing strategy.",
  },
  {
    id: 2676,
    category: "Laravel",
    subcategory: "Laravel Nâng Cao",
    level: "advanced",
    q: "Livewire 3 là gì và cách thay đổi cách phát triển full-stack với Laravel?",
    a: "Livewire 3 là framework Laravel để xây dựng component full-stack reactive bằng PHP thuần—không cần JavaScript. State phía server đồng bộ với frontend qua AJAX request tối ưu, loại bỏ việc quản lý API contract. \n\n**Ví dụ:** form validation, live search, cập nhật giỏ hàng đều xử lý bằng PHP. \n\n**Ưu điểm:** ít JavaScript hơn, tái sử dụng Laravel validation trong UI, tận dụng Laravel tooling. \n\n**Nhược điểm:** thêm latency so với client-side (tốt cho tần suất thấp, không phù hợp tần suất cao), tải server lớn hơn. Dùng cho: admin panel, dashboard, CRUD interface. Không phù hợp: game real-time, cập nhật tần suất rất cao. Ngày càng phổ biến như alternative cho SPA.",
    q_en: "What is Livewire 3 and how does it change Laravel full-stack development?",
    a_en: "Livewire 3 is a Laravel framework for building reactive full-stack components with pure PHP—no JavaScript needed. Server-side state syncs to the frontend via optimized AJAX requests, eliminating API contract management. \n\n**Example:** form validation, live search, shopping cart updates all handled in PHP. \n\n**Advantages:** less JavaScript, reuse Laravel validation rules in UI, leverage existing Laravel tooling. Drawbacks: added latency vs client-side (fine for low frequency, bad for high frequency interactions), higher server load. Use for: admin panels, dashboards, CRUD interfaces. Avoid for: real-time games, very high-frequency updates. Growing as a popular alternative to full SPA architecture.",
  },
];
