import type { QAItem } from "../interview-data";

export const SEO_DATA: QAItem[] = [
  // === SEO: Cơ Bản (1071-1085) ===
  {
    id: 1071,
    category: "SEO",
    subcategory: "Cơ Bản",
    level: "beginner",
    q: "SEO là gì? Tại sao frontend developer cần hiểu SEO?",
    a: "SEO (Search Engine Optimization) là tối ưu hóa website để đạt thứ hạng cao trên Google, Bing, và các công cụ tìm kiếm. Frontend developer trực tiếp ảnh hưởng SEO vì: (1) HTML structure quyết định cách Google hiểu nội dung (semantic tags, heading hierarchy), (2) Performance ảnh hưởng ranking (Core Web Vitals là ranking signal từ 2021), (3) Rendering strategy quyết định Google có index được content không (CSR vs SSR), (4) Technical SEO implementation (meta tags, structured data, sitemap) thường do frontend làm. Một trang web đẹp nhưng SEO kém = không ai tìm thấy. Thực tế: 68% online experiences bắt đầu từ search engine, 75% users không bao giờ scroll qua page 1.",
    q_en: "What is SEO? Why does a frontend developer need to understand it?",
    a_en: "SEO (Search Engine Optimization) is the practice of optimizing a website to rank higher on Google, Bing, and other search engines. Frontend developers directly impact SEO because: (1) HTML structure determines how Google understands content (semantic tags, heading hierarchy), (2) Performance affects ranking (Core Web Vitals became a ranking signal in 2021), (3) Rendering strategy determines whether Google can index content (CSR vs SSR), (4) Technical SEO implementation (meta tags, structured data, sitemap) is typically handled by frontend developers. A visually stunning website with poor SEO means nobody will find it. In practice: 68% of online experiences start with a search engine, and 75% of users never scroll past page 1.",
  },
  {
    id: 1072,
    category: "SEO",
    subcategory: "Cơ Bản",
    level: "beginner",
    q: "Meta tags quan trọng nhất cho SEO là gì? Cách viết title và description hiệu quả?",
    a: "**Title tag** (`<title>`): quan trọng nhất, hiển thị trên tab browser và kết quả tìm kiếm. Quy tắc: 50-60 ký tự, chứa keyword chính ở đầu, unique cho mỗi trang, format: 'Primary Keyword - Secondary | Brand'. **Meta description** (`<meta name='description'>`): mô tả trang hiển thị dưới title trên Google. Quy tắc: 150-160 ký tự, chứa keyword tự nhiên, có call-to-action, unique. **Meta robots** (`<meta name='robots'>`): điều khiển indexing — `index,follow` (mặc định), `noindex` (không index), `nofollow` (không follow links). **Canonical** (`<link rel='canonical'>`): chỉ định URL chính thức khi có duplicate content. **Open Graph** (og:title, og:description, og:image): kiểm soát preview khi share trên social media.",
    q_en: "What are the most important meta tags for SEO? How do you write an effective title and description?",
    a_en: "**Title tag** (`<title>`): the most important tag, displayed on browser tabs and search results. Rules: 50-60 characters, primary keyword near the start, unique per page, format: 'Primary Keyword - Secondary | Brand'. **Meta description** (`<meta name='description'>`): page description shown beneath the title on Google. Rules: 150-160 characters, include keywords naturally, include a call-to-action, unique per page. **Meta robots** (`<meta name='robots'>`): controls indexing — `index,follow` (default), `noindex` (exclude from index), `nofollow` (do not follow links). **Canonical** (`<link rel='canonical'>`): specifies the authoritative URL when duplicate content exists. **Open Graph** (og:title, og:description, og:image): controls how content appears when shared on social media.",
  },
  {
    id: 1073,
    category: "SEO",
    subcategory: "Cơ Bản",
    level: "beginner",
    q: "Semantic HTML ảnh hưởng SEO như thế nào?",
    a: "Semantic HTML giúp search engine hiểu cấu trúc và ý nghĩa nội dung, trực tiếp ảnh hưởng ranking. Các thẻ quan trọng: `<header>` (phần đầu trang), `<nav>` (navigation — Google hiểu đây là menu), `<main>` (nội dung chính — Google ưu tiên index phần này), `<article>` (bài viết độc lập — có thể xuất hiện trong Google News), `<section>` (phần nội dung có chủ đề), `<aside>` (sidebar — Google biết đây không phải nội dung chính), `<footer>` (chân trang). **Heading hierarchy**: chỉ 1 `<h1>` mỗi trang (chứa keyword chính), `<h2>` cho sections, `<h3>` cho sub-sections — Google dùng heading để hiểu topic hierarchy. Sai lầm phổ biến: dùng `<div>` cho mọi thứ, nhiều `<h1>`, skip heading levels (h1 → h3).",
    q_en: "How does semantic HTML affect SEO?",
    a_en: "Semantic HTML helps search engines understand the structure and meaning of content, directly impacting rankings. Key tags: `<header>` (page header), `<nav>` (navigation — Google recognizes this as a menu), `<main>` (main content — Google prioritizes indexing this section), `<article>` (standalone article — can appear in Google News), `<section>` (themed content section), `<aside>` (sidebar — Google understands this is not primary content), `<footer>` (page footer). **Heading hierarchy**: only one `<h1>` per page (containing the primary keyword), `<h2>` for sections, `<h3>` for sub-sections — Google uses headings to understand topic hierarchy. Common mistakes: using `<div>` for everything, multiple `<h1>` tags, and skipping heading levels (h1 → h3).",
  },
  {
    id: 1074,
    category: "SEO",
    subcategory: "Cơ Bản",
    level: "beginner",
    q: "robots.txt là gì? Cách cấu hình đúng?",
    a: "`robots.txt` là file text đặt ở root domain (example.com/robots.txt) hướng dẫn search engine bots nên/không nên crawl phần nào của website. Syntax: `User-agent: *` (áp dụng cho tất cả bots), `Disallow: /admin` (không crawl /admin), `Allow: /admin/public` (ngoại lệ cho phép), `Sitemap: https://example.com/sitemap.xml`. Lưu ý quan trọng: robots.txt chỉ là 'yêu cầu', bot xấu có thể bỏ qua. `Disallow` KHÔNG giống `noindex` — trang vẫn có thể xuất hiện trên Google nếu có backlinks. Muốn chắc chắn không index → dùng `<meta name='robots' content='noindex'>`. Sai lầm: block CSS/JS files khiến Google không render được trang → ảnh hưởng SEO nghiêm trọng.",
    q_en: "What is robots.txt? How do you configure it correctly?",
    a_en: "`robots.txt` is a text file placed at the root domain (example.com/robots.txt) that instructs search engine bots which parts of the website to crawl or avoid. Syntax: `User-agent: *` (applies to all bots), `Disallow: /admin` (do not crawl /admin), `Allow: /admin/public` (allow this exception), `Sitemap: https://example.com/sitemap.xml`. Important notes: robots.txt is just a 'request' — malicious bots may ignore it. `Disallow` is NOT the same as `noindex` — a page can still appear in Google if it has backlinks pointing to it. To guarantee non-indexing, use `<meta name='robots' content='noindex'>`. A common mistake is blocking CSS/JS files, which prevents Google from rendering the page and seriously hurts SEO.",
  },
  {
    id: 1075,
    category: "SEO",
    subcategory: "Cơ Bản",
    level: "beginner",
    q: "Sitemap XML là gì? Khi nào cần và cách tạo?",
    a: "Sitemap XML (`sitemap.xml`) là file liệt kê tất cả URLs quan trọng của website, giúp search engine khám phá và index nhanh hơn. Format: `<urlset>` chứa nhiều `<url>`, mỗi url có `<loc>` (URL), `<lastmod>` (ngày cập nhật), `<changefreq>` (tần suất thay đổi), `<priority>` (0.0-1.0). Khi nào CẦN: website lớn (>500 trang), trang mới tạo chưa có backlinks, website có nhiều content động (blog, e-commerce), trang deep link (>3 clicks từ homepage). Khi nào KHÔNG cần: website nhỏ (<50 trang) với navigation tốt. Trong Next.js: dùng `app/sitemap.ts` export function trả về mảng URLs — Next.js tự generate sitemap.xml. Giới hạn: max 50,000 URLs hoặc 50MB mỗi sitemap, dùng sitemap index cho website lớn hơn.",
    q_en: "What is an XML sitemap? When do you need one, and how do you create it?",
    a_en: "An XML sitemap (`sitemap.xml`) is a file listing all important URLs on a website, helping search engines discover and index content faster. Format: `<urlset>` contains multiple `<url>` entries, each with `<loc>` (URL), `<lastmod>` (last modified date), `<changefreq>` (update frequency), and `<priority>` (0.0–1.0). When you NEED one: large websites (>500 pages), newly launched sites without backlinks, sites with lots of dynamic content (blog, e-commerce), and pages that are deep in the site structure (>3 clicks from homepage). When you DON'T need one: small sites (<50 pages) with good navigation. In Next.js: use `app/sitemap.ts` exporting a function that returns an array of URLs — Next.js auto-generates sitemap.xml. Limits: max 50,000 URLs or 50MB per sitemap; use a sitemap index for larger sites.",
  },
  {
    id: 1076,
    category: "SEO",
    subcategory: "Cơ Bản",
    level: "beginner",
    q: "Canonical URL là gì? Khi nào cần sử dụng?",
    a: "Canonical URL (`<link rel='canonical' href='...'>`) cho Google biết URL nào là 'bản gốc' khi có nhiều URLs chứa cùng nội dung. \n\n**Ví dụ:** `/products?sort=price` và `/products?sort=name` cùng nội dung → canonical trỏ về `/products`. Khi nào CẦN: (1) URL parameters tạo duplicate (filters, sort, pagination), (2) HTTP vs HTTPS, www vs non-www, (3) AMP pages trỏ về original, (4) Syndicated content (bài viết đăng trên nhiều site), (5) Mobile URL khác desktop URL. Nếu không có canonical, Google tự chọn 'bản gốc' — có thể chọn sai. Trong Next.js: dùng `metadata.alternates.canonical` trong `generateMetadata()`. \n\n**Lưu ý:** canonical phải là absolute URL, self-referencing canonical cũng tốt (mỗi trang canonical trỏ về chính nó).",
    q_en: "What is a canonical URL? When should you use it?",
    a_en: "A canonical URL (`<link rel='canonical' href='...'>`) tells Google which URL is the 'original' version when multiple URLs share the same content. For example: `/products?sort=price` and `/products?sort=name` show the same content — the canonical should point to `/products`. When you NEED it: (1) URL parameters creating duplicates (filters, sorting, pagination), (2) HTTP vs HTTPS or www vs non-www versions, (3) AMP pages pointing to the original, (4) Syndicated content (articles published on multiple sites), (5) Mobile URLs that differ from desktop URLs. Without a canonical, Google picks its own 'original' — which may be the wrong choice. In Next.js: use `metadata.alternates.canonical` inside `generateMetadata()`. \n\n**Note:** canonical URLs must be absolute, and self-referencing canonicals (each page pointing to itself) are also recommended.",
  },

  // === SEO: Rendering & Crawling (1077-1085) ===
  {
    id: 1077,
    category: "SEO",
    subcategory: "Rendering",
    level: "intermediate",
    q: "CSR, SSR, SSG ảnh hưởng SEO như thế nào? Cách Google crawl SPA?",
    a: "**CSR (Client-Side Rendering)**: HTML ban đầu trống, JS render content → Google phải chờ JS execute (second wave indexing). Rủi ro: Googlebot có thể timeout, content không index đầy đủ, crawl budget lãng phí. **SSR (Server-Side Rendering)**: HTML đầy đủ từ server → Google index ngay lập tức, tốt nhất cho SEO. **SSG (Static Site Generation)**: HTML pre-built → nhanh nhất, SEO tuyệt vời cho content tĩnh. Google crawl SPA: Googlebot dùng Chromium headless, render JS rồi mới index — nhưng có hàng đợi dài (render queue), có thể mất vài ngày đến vài tuần. Thực tế: SSR/SSG luôn tốt hơn CSR cho SEO. Nếu bắt buộc CSR: đảm bảo critical content trong initial HTML, dùng dynamic rendering cho bot.",
    q_en: "How do CSR, SSR, and SSG affect SEO? How does Google crawl SPAs?",
    a_en: "**CSR (Client-Side Rendering)**: The initial HTML is essentially empty; JS renders the content — Google must wait for JS to execute (second-wave indexing). Risks: Googlebot may time out, content may not be fully indexed, and crawl budget is wasted. **SSR (Server-Side Rendering)**: Fully-rendered HTML is delivered from the server — Google can index it immediately; this is the best approach for SEO. **SSG (Static Site Generation)**: HTML is pre-built — the fastest option and excellent for SEO with static content. Google crawling SPAs: Googlebot uses headless Chromium to render JS before indexing — but there is a long render queue, and indexing can take days or even weeks. In practice: SSR/SSG is always better than CSR for SEO. If CSR is unavoidable: ensure critical content is in the initial HTML and use dynamic rendering for bots.",
  },
  {
    id: 1078,
    category: "SEO",
    subcategory: "Rendering",
    level: "intermediate",
    q: "Core Web Vitals là gì? Ảnh hưởng thế nào đến SEO ranking?",
    a: "Core Web Vitals (CWV) là 3 metrics đo trải nghiệm người dùng, là ranking signal của Google từ 2021. **LCP (Largest Contentful Paint)**: thời gian render phần tử lớn nhất (hero image, heading) — target: <2.5s. Cải thiện: optimize images (WebP, lazy load), preload fonts, SSR/SSG. **INP (Interaction to Next Paint)**: thời gian phản hồi khi user interact (click, tap) — target: <200ms. Cải thiện: code split, defer heavy JS, reduce main thread work. **CLS (Cumulative Layout Shift)**: mức độ layout bị 'nhảy' — target: <0.1. Cải thiện: set width/height cho images/videos, font-display swap, avoid dynamic content injection above fold. Google Search Console báo CWV cho từng URL. Trang có CWV tốt được ưu tiên ranking khi 2 trang có relevance tương đương.",
    q_en: "What are Core Web Vitals? How do they affect SEO rankings?",
    a_en: "Core Web Vitals (CWV) are three metrics that measure real user experience, and have been a Google ranking signal since 2021. **LCP (Largest Contentful Paint)**: measures how long it takes to render the largest element (hero image, heading) — target: <2.5s. Improve by: optimizing images (WebP, lazy load), preloading fonts, using SSR/SSG. **INP (Interaction to Next Paint)**: measures responsiveness to user interactions (clicks, taps) — target: <200ms. Improve by: code splitting, deferring heavy JS, reducing main thread work. **CLS (Cumulative Layout Shift)**: measures unexpected layout shifts — target: <0.1. Improve by: setting explicit width/height on images/videos, using font-display swap, and avoiding injecting dynamic content above the fold. Google Search Console reports CWV scores per URL. Pages with strong CWV scores gain a ranking advantage when competing pages have similar relevance.",
  },
  {
    id: 1079,
    category: "SEO",
    subcategory: "Rendering",
    level: "intermediate",
    q: "Lazy loading ảnh hưởng SEO như thế nào? Best practices?",
    a: "Lazy loading trì hoãn tải resources cho đến khi cần → giảm initial load time, cải thiện LCP. Nhưng nếu dùng sai sẽ HẠI SEO: (1) Không lazy load above-the-fold images (LCP element) — dùng `loading='eager'` hoặc `priority` trong next/image. (2) Googlebot CÓ scroll page nhưng có giới hạn — content quá deep có thể không được index. (3) Dùng native `loading='lazy'` thay vì JS-based solution — Googlebot hỗ trợ native lazy loading. (4) Luôn có `alt` text cho images. (5) Dùng `<noscript>` fallback cho critical images. Trong Next.js: `next/image` tự động lazy load mặc định, set `priority={true}` cho LCP image. Best practice: lazy load everything below the fold, eager load above the fold.",
    q_en: "How does lazy loading affect SEO? What are the best practices?",
    a_en: "Lazy loading defers resource loading until needed — reducing initial load time and improving LCP. However, misuse can HURT SEO: (1) Never lazy load above-the-fold images (the LCP element) — use `loading='eager'` or the `priority` prop in next/image. (2) Googlebot does scroll the page but has limits — content too far down may not get indexed. (3) Use native `loading='lazy'` instead of JS-based solutions — Googlebot supports native lazy loading. (4) Always include `alt` text on images. (5) Use `<noscript>` fallbacks for critical images. In Next.js: `next/image` lazy loads by default — set `priority={true}` for the LCP image. Best practice: lazy load everything below the fold, eagerly load everything above it.",
  },
  {
    id: 1080,
    category: "SEO",
    subcategory: "Technical",
    level: "intermediate",
    q: "Structured Data (Schema.org) là gì? Lợi ích cho SEO?",
    a: "Structured Data là JSON-LD markup giúp Google hiểu nội dung trang ở mức semantic — không chỉ text mà còn ý nghĩa (đây là product, đây là review, đây là recipe). Format phổ biến: JSON-LD trong `<script type='application/ld+json'>`. \n\n**Lợi ích:** **Rich Snippets** — kết quả tìm kiếm đặc biệt với rating stars, price, availability, FAQ accordion, breadcrumbs, sitelinks. CTR tăng 20-30% với rich snippets. Schema phổ biến cho frontend: `Article` (blog), `Product` (e-commerce), `FAQPage` (FAQ), `BreadcrumbList` (breadcrumbs), `Organization` (company info), `WebSite` (search box trong Google). Trong Next.js: tạo component `<JsonLd>` render `<script>` tag với JSON.stringify(data). Validate bằng Google Rich Results Test.",
    q_en: "What is Structured Data (Schema.org)? What are the SEO benefits?",
    a_en: "Structured Data is JSON-LD markup that helps Google understand page content at a semantic level — not just raw text, but its meaning (this is a product, this is a review, this is a recipe). The most common format: JSON-LD embedded in a `<script type='application/ld+json'>` tag. \n\n**Benefits:** **Rich Snippets** — enhanced search results featuring star ratings, price, availability, FAQ accordions, breadcrumbs, and sitelinks. Rich snippets can increase CTR by 20-30%. Common schemas for frontend developers: `Article` (blog), `Product` (e-commerce), `FAQPage` (FAQ sections), `BreadcrumbList` (breadcrumbs), `Organization` (company info), `WebSite` (Google sitelinks search box). In Next.js: create a `<JsonLd>` component that renders a `<script>` tag with JSON.stringify(data). Validate using Google's Rich Results Test.",
  },
  {
    id: 1081,
    category: "SEO",
    subcategory: "Technical",
    level: "intermediate",
    q: "Open Graph và Twitter Cards là gì? Cách implement trong Next.js?",
    a: "**Open Graph (OG)**: meta tags chuẩn Facebook để kiểm soát preview khi share link trên social media (Facebook, LinkedIn, Zalo, Telegram). Tags quan trọng: `og:title`, `og:description`, `og:image` (1200x630px recommended), `og:url`, `og:type`. **Twitter Cards**: tương tự OG nhưng cho Twitter/X — `twitter:card` ('summary_large_image' phổ biến nhất), `twitter:title`, `twitter:description`, `twitter:image`. Trong Next.js App Router: dùng `metadata` export hoặc `generateMetadata()` — Next.js tự generate OG và Twitter meta tags. Dynamic OG images: dùng `opengraph-image.tsx` (hoặc `@vercel/og`) để generate image động chứa title, author, date. Ảnh hưởng SEO gián tiếp: share preview đẹp → nhiều click → nhiều traffic → nhiều backlinks → ranking tăng.",
    q_en: "What are Open Graph and Twitter Cards? How do you implement them in Next.js?",
    a_en: "**Open Graph (OG)**: Facebook's standard meta tags for controlling how links appear when shared on social media (Facebook, LinkedIn, Zalo, Telegram). Key tags: `og:title`, `og:description`, `og:image` (1200x630px recommended), `og:url`, `og:type`. **Twitter Cards**: similar to OG but specific to Twitter/X — `twitter:card` ('summary_large_image' is most common), `twitter:title`, `twitter:description`, `twitter:image`. In Next.js App Router: use `metadata` export or `generateMetadata()` — Next.js automatically generates OG and Twitter meta tags. Dynamic OG images: use `opengraph-image.tsx` (or `@vercel/og`) to generate dynamic images that include the title, author, and date. Indirect SEO benefit: a compelling share preview leads to more clicks, more traffic, more backlinks, and ultimately higher rankings.",
  },
  {
    id: 1082,
    category: "SEO",
    subcategory: "Technical",
    level: "intermediate",
    q: "Internal linking ảnh hưởng SEO như thế nào? Best practices?",
    a: "Internal links là links giữa các trang trong cùng website. Ảnh hưởng SEO: (1) **Crawlability**: Google khám phá trang mới qua internal links — trang không có link trỏ đến = 'orphan page', có thể không được index. (2) **PageRank distribution**: mỗi trang có 'link equity', internal links phân phối equity đó — trang được nhiều internal links trỏ đến sẽ rank cao hơn. (3) **Context**: anchor text của internal link giúp Google hiểu trang đích nói về gì. Best practices: dùng descriptive anchor text (không 'click here'), link từ high-authority pages đến important pages, sử dụng breadcrumbs, related posts, sidebar navigation. Trong React/Next.js: dùng `<Link>` component (không phải `<a>`) để SPA navigation nhưng vẫn render `<a>` cho SEO.",
    q_en: "How does internal linking affect SEO? What are the best practices?",
    a_en: "Internal links connect pages within the same website. SEO impact: (1) **Crawlability**: Google discovers new pages via internal links — pages with no inbound links are 'orphan pages' and may never be indexed. (2) **PageRank distribution**: each page carries 'link equity'; internal links pass that equity to other pages — pages with more internal links pointing to them tend to rank higher. (3) **Context**: the anchor text of internal links helps Google understand what the destination page is about. Best practices: use descriptive anchor text (not just 'click here'), link from high-authority pages to important ones, and leverage breadcrumbs, related posts, and sidebar navigation. In React/Next.js: use the `<Link>` component (not a plain `<a>` tag) for SPA navigation while still rendering a proper `<a>` tag for SEO.",
  },
  {
    id: 1083,
    category: "SEO",
    subcategory: "Technical",
    level: "advanced",
    q: "Crawl budget là gì? Cách tối ưu cho website lớn?",
    a: "Crawl budget là số lượng pages mà Googlebot crawl trong một khoảng thời gian nhất định. Gồm 2 yếu tố: **Crawl rate limit** (tốc độ crawl không gây quá tải server) và **Crawl demand** (Google muốn crawl bao nhiêu dựa trên popularity/freshness). Quan trọng khi website >10,000 pages. Lãng phí crawl budget: (1) duplicate content (URL parameters, www/non-www), (2) soft 404s (trang trống return 200), (3) redirect chains (A→B→C→D), (4) infinite spaces (calendar, filters tạo vô hạn URL combinations). Tối ưu: canonical tags, robots.txt block unnecessary paths, clean URL structure, pagination với rel=next/prev, sitemap chỉ chứa important pages, server response time <500ms, dùng 410 thay vì 404 cho permanently deleted pages.",
    q_en: "What is crawl budget? How do you optimize it for large websites?",
    a_en: "Crawl budget is the number of pages Googlebot crawls within a given time period. It consists of two factors: **Crawl rate limit** (the speed at which Googlebot crawls without overloading the server) and **Crawl demand** (how much Google wants to crawl based on popularity and freshness). This matters most for websites with over 10,000 pages. Common crawl budget waste: (1) duplicate content (URL parameters, www vs non-www), (2) soft 404s (empty pages returning a 200 status), (3) redirect chains (A→B→C→D), (4) infinite URL spaces (calendars, filters creating endless URL combinations). Optimization tips: canonical tags, blocking unnecessary paths in robots.txt, clean URL structure, pagination with rel=next/prev, sitemaps listing only important pages, server response time under 500ms, and using 410 instead of 404 for permanently deleted pages.",
  },
  {
    id: 1084,
    category: "SEO",
    subcategory: "Technical",
    level: "advanced",
    q: "Internationalization (i18n) SEO: hreflang là gì? Cách implement?",
    a: `\`hreflang\` attribute cho Google biết trang nào dành cho ngôn ngữ/quốc gia nào — tránh duplicate content penalty khi có nhiều phiên bản ngôn ngữ. Format: \`<link rel='alternate' hreflang='vi' href='https://example.com/vi/page'>\`.

Quy tắc:
- Mỗi trang phải có hreflang trỏ đến TẤT CẢ phiên bản ngôn ngữ (kể cả chính nó — self-referencing).
- hreflang phải reciprocal (trang A trỏ B, trang B phải trỏ lại A).
- Dùng \`x-default\` cho fallback language.

Format language code: 'vi' (ngôn ngữ), 'en-US' (ngôn ngữ-quốc gia). Trong Next.js: cấu hình i18n routing trong next.config, generateMetadata() với alternates.languages.

Sai lầm: hreflang không reciprocal (Google bỏ qua), mixed signals (hreflang nói tiếng Việt nhưng content tiếng Anh), quên x-default.`,
    q_en: "What is hreflang in i18n SEO? How do you implement it?",
    a_en: `The \`hreflang\` attribute tells Google which page is intended for which language or country — preventing duplicate content penalties when multiple language versions exist. Format: \`<link rel='alternate' hreflang='vi' href='https://example.com/vi/page'>\`.

Rules:
- Every page must have hreflang annotations pointing to ALL language versions (including itself — self-referencing).
- hreflang annotations must be reciprocal (if page A points to page B, page B must point back to page A).
- Use \`x-default\` as the fallback language.

Language code format: 'vi' (language only), 'en-US' (language-country). In Next.js: configure i18n routing in next.config.js and use \`generateMetadata()\` with \`alternates.languages\`.

Common mistakes: non-reciprocal hreflang (Google ignores it), mixed signals (hreflang says Vietnamese but content is in English), and forgetting x-default.`,
  },
  {
    id: 1085,
    category: "SEO",
    subcategory: "Technical",
    level: "advanced",
    q: "JavaScript SEO: những lỗi phổ biến khiến Google không index được content?",
    a: "**Lỗi rendering**: (1) Content trong useEffect/componentDidMount — Google có thể không chờ async data fetch, (2) Client-only routing (React Router) không có SSR fallback — Google thấy blank page, (3) Error boundaries catch lỗi nhưng render fallback UI — Google index error message thay vì content. **Lỗi meta tags**: (4) Dynamic title/description set bằng JS — Google có thể lấy title trước khi JS chạy, (5) Thiếu canonical cho dynamic routes. **Lỗi links**: (6) Navigation bằng onClick + router.push thay vì `<a href>` — Google không follow JS click events, (7) Infinite scroll không có pagination fallback. **Lỗi resources**: (8) robots.txt block JS/CSS bundles — Google không render được trang, (9) Bundle quá lớn khiến Googlebot timeout (>5s). Fix chung: dùng SSR/SSG, test bằng Google Search Console URL Inspection (xem Google thấy gì), dùng `<Link>` component cho navigation.",
    q_en: "JavaScript SEO: what are the common mistakes that prevent Google from indexing content?",
    a_en: "**Rendering issues**: (1) Content inside useEffect/componentDidMount — Google may not wait for async data fetching, (2) Client-only routing (React Router) without SSR fallback — Google sees a blank page, (3) Error boundaries catching errors and rendering fallback UI — Google indexes the error message instead of real content. **Meta tag issues**: (4) Dynamic title/description set via JS — Google may capture the title before JS executes, (5) Missing canonicals for dynamic routes. **Link issues**: (6) Navigation using onClick + router.push instead of `<a href>` — Google does not follow JS click events, (7) Infinite scroll without a pagination fallback. **Resource issues**: (8) robots.txt blocking JS/CSS bundles — Google cannot render the page, (9) Oversized bundles causing Googlebot to time out (>5s). General fixes: use SSR/SSG, test with Google Search Console URL Inspection (to see exactly what Google sees), and use a `<Link>` component for all navigation.",
  },

  // === SEO: Next.js Specific (1086-1095) ===
  {
    id: 1086,
    category: "SEO",
    subcategory: "Next.js SEO",
    level: "intermediate",
    q: "Metadata API trong Next.js App Router hoạt động như thế nào?",
    a: "Next.js App Router cung cấp Metadata API mạnh mẽ để quản lý SEO. 2 cách: (1) **Static metadata**: export `const metadata: Metadata = { title: '...', description: '...' }` từ layout.tsx hoặc page.tsx. (2) **Dynamic metadata**: export `async function generateMetadata({ params }) { ... return { title, description } }` — fetch data rồi generate meta tags. Metadata cascade: layout metadata → page metadata (page override layout). Tính năng: `title.template` (pattern cho child pages, ví dụ '%s | MyApp'), `openGraph`, `twitter`, `robots`, `alternates` (canonical, hreflang), `icons` (favicon), `verification` (Google Search Console). Next.js tự động: generate `<title>`, `<meta>`, OG tags, Twitter cards, canonical URL từ metadata object. Không cần thư viện bên ngoài như react-helmet.",
    q_en: "How does the Metadata API work in Next.js App Router?",
    a_en: "Next.js App Router provides a powerful Metadata API for managing SEO. Two approaches: (1) **Static metadata**: export `const metadata: Metadata = { title: '...', description: '...' }` from layout.tsx or page.tsx. (2) **Dynamic metadata**: export `async function generateMetadata({ params }) { ... return { title, description } }` — fetch data and generate meta tags dynamically. Metadata cascade: layout metadata → page metadata (page-level overrides the layout). Features include: `title.template` (pattern for child pages, e.g., '%s | MyApp'), `openGraph`, `twitter`, `robots`, `alternates` (canonical, hreflang), `icons` (favicon), and `verification` (Google Search Console). Next.js automatically generates `<title>`, `<meta>`, OG tags, Twitter cards, and canonical URLs from the metadata object — no external library like react-helmet needed.",
  },
  {
    id: 1087,
    category: "SEO",
    subcategory: "Next.js SEO",
    level: "intermediate",
    q: "Cách tạo dynamic sitemap và robots.txt trong Next.js App Router?",
    a: "**Sitemap**: tạo `app/sitemap.ts` (hoặc `.js`) export default function trả về mảng `MetadataRoute.Sitemap`. Mỗi item có `url`, `lastModified`, `changeFrequency`, `priority`. Có thể fetch data từ DB/CMS để generate dynamic URLs. Cho website lớn: tạo `app/sitemap/[id]/route.ts` để generate multiple sitemaps (sitemap index). **robots.txt**: tạo `app/robots.ts` export default function trả về `MetadataRoute.Robots` object với `rules` (User-Agent, allow, disallow) và `sitemap` URL. \n\n**Ví dụ:** `{ rules: { userAgent: '*', allow: '/', disallow: '/admin/' }, sitemap: 'https://example.com/sitemap.xml' }`. Cả 2 đều type-safe với TypeScript, auto-generate tại build time, không cần plugin.",
    q_en: "How do you create a dynamic sitemap and robots.txt in Next.js App Router?",
    a_en: "**Sitemap**: create `app/sitemap.ts` (or `.js`) with a default export function that returns a `MetadataRoute.Sitemap` array. Each item includes `url`, `lastModified`, `changeFrequency`, and `priority`. You can fetch data from a DB or CMS to generate dynamic URLs. For large sites: create `app/sitemap/[id]/route.ts` to generate multiple sitemaps (a sitemap index). **robots.txt**: create `app/robots.ts` with a default export function that returns a `MetadataRoute.Robots` object containing `rules` (User-Agent, allow, disallow) and a `sitemap` URL. \n\n**Example:** `{ rules: { userAgent: '*', allow: '/', disallow: '/admin/' }, sitemap: 'https://example.com/sitemap.xml' }`. Both are TypeScript type-safe, auto-generated at build time, and require no plugins.",
  },
  {
    id: 1088,
    category: "SEO",
    subcategory: "Next.js SEO",
    level: "intermediate",
    q: "next/image tối ưu SEO và performance như thế nào?",
    a: "`next/image` component tối ưu images tự động: (1) **Lazy loading** mặc định — chỉ load khi gần viewport, giảm initial page weight. (2) **Responsive images** — tự generate srcset với nhiều sizes, browser chọn size phù hợp. (3) **Format optimization** — auto convert sang WebP/AVIF nếu browser hỗ trợ, giảm 30-50% file size. (4) **Prevent CLS** — bắt buộc width/height hoặc fill, tránh layout shift. (5) **Priority loading** — set `priority={true}` cho LCP image (above-the-fold). SEO impact: LCP cải thiện (ranking signal), CLS giảm (ranking signal), smaller images = faster page = better crawl experience. Luôn thêm `alt` text mô tả ảnh cho Google Image Search và accessibility.",
    q_en: "How does next/image optimize SEO and performance?",
    a_en: "The `next/image` component automatically optimizes images: (1) **Lazy loading** by default — images only load when near the viewport, reducing initial page weight. (2) **Responsive images** — automatically generates a srcset at multiple sizes; the browser selects the most appropriate one. (3) **Format optimization** — auto-converts to WebP/AVIF when supported, reducing file size by 30-50%. (4) **Prevents CLS** — requires explicit width/height or fill prop, preventing layout shifts. (5) **Priority loading** — set `priority={true}` for the LCP image (above the fold). SEO impact: improved LCP (a ranking signal), reduced CLS (a ranking signal), and smaller images mean faster pages, which improves the crawl experience. Always include a descriptive `alt` attribute for Google Image Search and accessibility.",
  },
  {
    id: 1089,
    category: "SEO",
    subcategory: "Next.js SEO",
    level: "advanced",
    q: "Dynamic OG images trong Next.js là gì? Cách tạo?",
    a: "Dynamic OG images là ảnh preview được generate tự động cho mỗi trang — thay vì 1 ảnh chung cho cả website. Dùng `@vercel/og` (Satori) hoặc Next.js convention file `app/blog/[slug]/opengraph-image.tsx`. Cơ chế: viết JSX/HTML → Satori convert sang SVG → convert sang PNG. Có thể fetch data (title, author, date) rồi render lên ảnh. \n\n**Ví dụ:** blog post share trên Facebook hiển thị ảnh có title bài viết, author avatar, ngày đăng — professional hơn nhiều so với generic image. Performance: generate tại build time (SSG) hoặc on-demand (ISR), cache kết quả. CTR impact: trang có OG image đẹp và unique có CTR cao hơn 2-3x khi share trên social media.",
    q_en: "What are dynamic OG images in Next.js? How do you create them?",
    a_en: "Dynamic OG images are auto-generated preview images unique to each page — rather than one generic image for the entire site. Use `@vercel/og` (powered by Satori) or the Next.js file convention `app/blog/[slug]/opengraph-image.tsx`. \n\n**How it works:** write JSX/HTML → Satori converts it to SVG → converts to PNG. You can fetch data (title, author, date) and render it on the image. \n\n**Example:** sharing a blog post on Facebook shows an image with the post title, author avatar, and publish date — far more professional than a generic placeholder. Performance: generated at build time (SSG) or on-demand (ISR), with results cached. CTR impact: pages with unique, compelling OG images can see 2-3x higher CTR when shared on social media.",
  },
  {
    id: 1090,
    category: "SEO",
    subcategory: "Next.js SEO",
    level: "advanced",
    q: "generateStaticParams và ISR ảnh hưởng SEO thế nào?",
    a: "`generateStaticParams` pre-render dynamic routes tại build time (SSG) → HTML sẵn sàng, Google index ngay, TTFB cực nhanh. \n\n**Ví dụ:** blog có 100 posts → generate 100 static HTML pages. **ISR (Incremental Static Regeneration)**: kết hợp tốt nhất của SSG và SSR — serve cached HTML, background revalidate sau N giây. SEO benefits: (1) Fast TTFB → Google crawl nhanh hơn, (2) HTML đầy đủ → index ngay không cần render JS, (3) Luôn fresh content với revalidate. Pattern: `generateStaticParams` cho known routes (categories, popular posts), fallback dynamic rendering cho routes mới (long-tail). Với `revalidate: 3600` (1 giờ): Google luôn thấy content mới nhất, users luôn thấy fast page.",
    q_en: "How do generateStaticParams and ISR affect SEO?",
    a_en: "`generateStaticParams` pre-renders dynamic routes at build time (SSG) — HTML is ready immediately, Google can index it right away, and TTFB is extremely fast. \n\n**Example:** a blog with 100 posts generates 100 static HTML pages. **ISR (Incremental Static Regeneration)**: combines the best of SSG and SSR — serves cached HTML while revalidating in the background after N seconds. SEO benefits: (1) Fast TTFB → Google crawls faster, (2) Full HTML → indexed immediately without needing to render JS, (3) Always-fresh content with revalidation. Pattern: use `generateStaticParams` for known routes (categories, popular posts) and fallback to dynamic rendering for new routes (long-tail). With `revalidate: 3600` (1 hour): Google always sees the latest content and users always get a fast page.",
  },
  {
    id: 1091,
    category: "SEO",
    subcategory: "Performance SEO",
    level: "intermediate",
    q: "Page speed ảnh hưởng SEO ranking như thế nào? Cách đo và cải thiện?",
    a: "Google xác nhận page speed là ranking signal từ 2018 (mobile) và 2021 (Core Web Vitals). Trang chậm bị phạt ranking và crawl budget giảm. **Cách đo**: Google PageSpeed Insights (lab + field data), Lighthouse (Chrome DevTools), Google Search Console (Core Web Vitals report), WebPageTest (waterfall analysis). **Cải thiện**: (1) Images: WebP/AVIF, lazy load, responsive sizes, CDN. (2) JavaScript: code splitting, tree shaking, defer non-critical JS, remove unused dependencies. (3) CSS: critical CSS inline, purge unused CSS, avoid render-blocking. (4) Fonts: font-display: swap, preload critical fonts, subset fonts. (5) Server: CDN, HTTP/2, compression (Brotli > gzip), edge caching. (6) Framework: SSR/SSG thay vì CSR, streaming. Target: LCP <2.5s, INP <200ms, CLS <0.1.",
    q_en: "How does page speed affect SEO rankings? How do you measure and improve it?",
    a_en: "Google confirmed page speed as a ranking signal in 2018 (mobile) and again in 2021 with Core Web Vitals. Slow pages face ranking penalties and reduced crawl budget. **How to measure**: Google PageSpeed Insights (lab + field data), Lighthouse (Chrome DevTools), Google Search Console (Core Web Vitals report), and WebPageTest (waterfall analysis). **How to improve**: (1) Images: WebP/AVIF, lazy loading, responsive sizes, CDN. (2) JavaScript: code splitting, tree shaking, defer non-critical JS, remove unused dependencies. (3) CSS: inline critical CSS, purge unused CSS, avoid render-blocking stylesheets. (4) Fonts: font-display: swap, preload critical fonts, subset fonts. (5) Server: CDN, HTTP/2, Brotli compression (better than gzip), edge caching. (6) Framework: SSR/SSG instead of CSR, streaming. Targets: LCP <2.5s, INP <200ms, CLS <0.1.",
  },
  {
    id: 1092,
    category: "SEO",
    subcategory: "Performance SEO",
    level: "advanced",
    q: "Cách tối ưu Largest Contentful Paint (LCP) trong React/Next.js?",
    a: "LCP đo thời gian render element lớn nhất trên viewport (thường là hero image, heading, hoặc video poster). Target: <2.5s. **Xác định LCP element**: dùng Chrome DevTools Performance tab hoặc web-vitals library. **Tối ưu theo loại**: (1) Image LCP: `priority` prop trong next/image, preload với `<link rel='preload'>`, avoid lazy load, dùng CDN, responsive sizes, WebP. (2) Text LCP: font-display swap, preload font file, avoid web font nếu được (system font stack). (3) Video poster: preload poster image. **Server optimizations**: reduce TTFB (<600ms) bằng SSG/ISR/edge functions, streaming SSR. **Tránh**: redirect chains, render-blocking CSS/JS lớn, client-side data fetch trước khi render LCP, quá nhiều third-party scripts (analytics, chat widgets). Trong Next.js: `next/image` với `priority`, `next/font` với `display: swap`, App Router streaming.",
    q_en: "How do you optimize Largest Contentful Paint (LCP) in React/Next.js?",
    a_en: "LCP measures how long it takes to render the largest element in the viewport (typically a hero image, heading, or video poster). Target: <2.5s. **Identify the LCP element**: use the Chrome DevTools Performance tab or the web-vitals library. **Optimizations by type**: (1) Image LCP: use the `priority` prop in next/image, preload with `<link rel='preload'>`, avoid lazy loading, use a CDN, responsive sizes, and WebP. (2) Text LCP: use font-display swap, preload the font file, avoid web fonts if possible (system font stack). (3) Video poster: preload the poster image. **Server optimizations**: reduce TTFB (<600ms) using SSG/ISR/edge functions and streaming SSR. **Avoid**: redirect chains, large render-blocking CSS/JS, client-side data fetching before the LCP renders, and too many third-party scripts (analytics, chat widgets). In Next.js: use `next/image` with `priority`, `next/font` with `display: swap`, and App Router streaming.",
  },
  {
    id: 1093,
    category: "SEO",
    subcategory: "Cơ Bản",
    level: "beginner",
    q: "Alt text cho images: tại sao quan trọng và cách viết đúng?",
    a: "Alt text (`alt` attribute trên `<img>`) mô tả nội dung ảnh cho: (1) **Search engines** — Google không 'nhìn' ảnh, dùng alt text để hiểu ảnh và rank trên Google Images, (2) **Screen readers** — người khiếm thị nghe mô tả ảnh, (3) **Fallback** — hiển thị khi ảnh không load được. Cách viết tốt: mô tả cụ thể, ngắn gọn (125 ký tự), chứa keyword tự nhiên (không keyword stuffing). Ví dụ tốt: `alt='Golden retriever puppy playing fetch in park'`. Ví dụ xấu: `alt='dog'`, `alt='image1.jpg'`, `alt='best dog training tips buy now click here'`. Khi nào để trống (`alt=''`): decorative images không mang thông tin (icons, dividers, background patterns). Ảnh text (infographic, banner): alt text nên chứa text trong ảnh.",
    q_en: "Alt text for images: why is it important and how do you write it correctly?",
    a_en: "Alt text (the `alt` attribute on `<img>`) describes image content for: (1) **Search engines** — Google cannot visually 'see' images; it uses alt text to understand them and rank on Google Images, (2) **Screen readers** — visually impaired users hear the image description, (3) **Fallback** — displayed when an image fails to load. How to write good alt text: be specific and concise (under 125 characters), include keywords naturally (no keyword stuffing). Good example: `alt='Golden retriever puppy playing fetch in park'`. Bad examples: `alt='dog'`, `alt='image1.jpg'`, `alt='best dog training tips buy now click here'`. When to leave it empty (`alt=''`): decorative images that carry no information (icons, dividers, background patterns). For text images (infographics, banners): the alt text should include the text shown in the image.",
  },
  {
    id: 1094,
    category: "SEO",
    subcategory: "Technical",
    level: "intermediate",
    q: "URL structure ảnh hưởng SEO thế nào? Best practices?",
    a: "URL là signal nhẹ cho Google hiểu nội dung trang, và ảnh hưởng CTR trên search results (user đọc URL trước khi click). **Best practices**: (1) Ngắn gọn, descriptive: `/blog/react-query-tutorial` tốt hơn `/blog/post?id=123`. (2) Dùng hyphens (-) ngăn cách từ, KHÔNG dùng underscores (_) — Google coi hyphen là word separator, underscore không. (3) Lowercase only — `/About` và `/about` là 2 URLs khác nhau (duplicate content). (4) Tránh parameters khi có thể — `/products/shoes` tốt hơn `/products?category=shoes`. (5) Tránh thay đổi URL — nếu bắt buộc, 301 redirect URL cũ → mới. (6) Include keyword target — nhưng tự nhiên. (7) Tránh nested quá sâu — `/a/b/c/d/e/page` kém hơn `/a/page`. Trong Next.js: file-based routing tạo clean URLs tự động.",
    q_en: "How does URL structure affect SEO? What are the best practices?",
    a_en: "URLs are a lightweight signal that helps Google understand a page's content, and they influence CTR in search results (users read the URL before deciding to click). **Best practices**: (1) Keep them short and descriptive: `/blog/react-query-tutorial` is better than `/blog/post?id=123`. (2) Use hyphens (-) to separate words, NOT underscores (_) — Google treats hyphens as word separators, underscores do not separate words. (3) Lowercase only — `/About` and `/about` are treated as two different URLs (duplicate content). (4) Avoid query parameters when possible — `/products/shoes` is better than `/products?category=shoes`. (5) Avoid changing URLs — if unavoidable, use a 301 redirect from the old URL to the new one. (6) Include target keywords — but keep it natural. (7) Avoid deep nesting — `/a/b/c/d/e/page` is worse than `/a/page`. In Next.js: file-based routing generates clean URLs automatically.",
  },
  {
    id: 1095,
    category: "SEO",
    subcategory: "Technical",
    level: "advanced",
    q: "SEO audit checklist cho frontend developer: cần kiểm tra những gì?",
    a: "**Technical**: (1) Tất cả pages return đúng status code (200, 301, 404), (2) sitemap.xml valid và submitted lên Google Search Console, (3) robots.txt không block important resources, (4) Canonical tags đúng trên mọi trang, (5) HTTPS enabled (ranking signal). **Content**: (6) Mỗi trang có unique title (50-60 chars) và meta description (150-160 chars), (7) Đúng 1 `<h1>` chứa primary keyword, (8) Images có alt text, (9) Internal links với descriptive anchor text. **Performance**: (10) Core Web Vitals pass (LCP <2.5s, INP <200ms, CLS <0.1), (11) Mobile-friendly (responsive design), (12) No render-blocking resources. **Structured Data**: (13) JSON-LD markup cho relevant schema types, (14) Validate qua Rich Results Test. **Crawlability**: (15) No orphan pages, (16) No redirect chains/loops, (17) No duplicate content without canonical. Tools: Google Search Console (free, essential), Lighthouse, Screaming Frog (crawl audit), Ahrefs/SEMrush (backlinks, keywords).",
    q_en: "SEO audit checklist for frontend developers: what should you check?",
    a_en: "**Technical**: (1) All pages return the correct status code (200, 301, 404), (2) sitemap.xml is valid and submitted to Google Search Console, (3) robots.txt is not blocking important resources, (4) Canonical tags are correct on every page, (5) HTTPS is enabled (a ranking signal). **Content**: (6) Every page has a unique title (50-60 chars) and meta description (150-160 chars), (7) Exactly one `<h1>` containing the primary keyword, (8) Images have alt text, (9) Internal links use descriptive anchor text. **Performance**: (10) Core Web Vitals pass (LCP <2.5s, INP <200ms, CLS <0.1), (11) Mobile-friendly (responsive design), (12) No render-blocking resources. **Structured Data**: (13) JSON-LD markup for relevant schema types, (14) Validated using Rich Results Test. **Crawlability**: (15) No orphan pages, (16) No redirect chains or loops, (17) No duplicate content without a canonical. Tools: Google Search Console (free, essential), Lighthouse, Screaming Frog (crawl audit), Ahrefs/SEMrush (backlinks, keywords).",
  },
];
