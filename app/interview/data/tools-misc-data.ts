import type { QAItem } from "../interview-data";

export const TOOLS_MISC_DATA: QAItem[] = [
  // === STATE MANAGEMENT: Redux & RTK (601-615) ===
  {
    id: 601,
    category: "State Management",
    subcategory: "Redux",
    level: "beginner",
    q: "Redux là gì? Tại sao chúng ta cần sử dụng Redux?",
    a: "Redux là thư viện quản lý state tập trung cho JavaScript, thường dùng với React. Nó lưu toàn bộ state ứng dụng trong một store duy nhất, giúp dễ dàng theo dõi, debug và chia sẻ state giữa các component không liên kết trực tiếp. Ví dụ thực tế: trong app có UserProfile ở header và ShoppingCart ở sidebar cùng cần thông tin user đang đăng nhập — không cần prop drilling qua nhiều cấp, cả hai đọc thẳng từ Redux store. Redux đặc biệt có giá trị khi dùng Redux DevTools để time-travel debug, replay lại từng action để tái hiện bug. Pitfall: nhiều team dùng Redux cho mọi thứ kể cả state local của form — điều đó làm code phức tạp không cần thiết, chỉ nên dùng Redux cho state thật sự cần chia sẻ toàn app.",
    q_en: "What is Redux? Why do we need to use it?",
    a_en: "Redux is a centralized state management library for JavaScript, commonly used with React. It stores the entire application state in a single store, making it easy to track, debug, and share state between components that are not directly connected. Practical example: an app with a UserProfile in the header and a ShoppingCart in the sidebar both need the currently logged-in user's info — instead of prop drilling through multiple levels, both components read directly from the Redux store. Redux is especially valuable when using Redux DevTools for time-travel debugging, replaying each action to reproduce bugs. Pitfall: many teams use Redux for everything including local form state — this adds unnecessary complexity. Redux should only be used for state that genuinely needs to be shared across the entire app.",
  },
  {
    id: 602,
    category: "State Management",
    subcategory: "Redux",
    level: "beginner",
    q: "Ba nguyên tắc cốt lõi của Redux là gì?",
    a: "Ba nguyên tắc cốt lõi của Redux: (1) Single source of truth — toàn bộ state nằm trong một store duy nhất, giúp đồng bộ data giữa các component và dễ serialize/restore state (ví dụ: lưu state vào localStorage rồi hydrate lại khi reload). (2) State is read-only — không được mutate state trực tiếp, chỉ thay đổi qua dispatch action, điều này tạo ra audit trail rõ ràng cho mọi thay đổi, là nền tảng để Redux DevTools time-travel hoạt động. (3) Changes are made with pure functions — reducer phải là pure function, cùng input luôn cho cùng output, không có side effects. Pitfall phổ biến: vi phạm nguyên tắc (3) bằng cách gọi API hoặc `Date.now()` trong reducer — side effects phải nằm trong middleware (thunk/saga). Nhiều developer mới cũng vi phạm nguyên tắc (2) bằng cách `state.items.push(item)` trong Redux thuần — chỉ Redux Toolkit với Immer mới cho phép cú pháp này.",
    q_en: "What are the three core principles of Redux?",
    a_en: "The three core principles of Redux: (1) Single source of truth — all state lives in a single store, keeping data in sync across components and making it easy to serialize/restore state (e.g., saving to localStorage and rehydrating on reload). (2) State is read-only — state must never be mutated directly; changes happen only by dispatching actions, creating a clear audit trail and enabling Redux DevTools time-travel. (3) Changes are made with pure functions — reducers must be pure functions: same input always produces same output, no side effects. Common pitfalls: violating principle (3) by calling an API or `Date.now()` inside a reducer — side effects belong in middleware (thunk/saga). Many new developers also violate principle (2) by writing `state.items.push(item)` in plain Redux — only Redux Toolkit with Immer permits that syntax.",
  },
  {
    id: 603,
    category: "State Management",
    subcategory: "Redux",
    level: "beginner",
    q: "Action trong Redux là gì? Cấu trúc của một action như thế nào?",
    a: "Action là plain JavaScript object mô tả sự kiện xảy ra trong ứng dụng, bắt buộc có trường `type` (string định danh) và thường có `payload` chứa dữ liệu. Ví dụ: `{ type: 'cart/addItem', payload: { id: 1, name: 'iPhone', qty: 1 } }`. Quy ước đặt tên `type` theo format `domain/eventName` giúp dễ đọc trong DevTools. Action creator là function trả về action object: `const addItem = (item) => ({ type: 'cart/addItem', payload: item })` — giúp tránh typo và tái sử dụng. Với Redux Toolkit, `createSlice` tự động tạo action creators và action types, không cần viết tay. Pitfall: đừng đặt non-serializable values vào action (Promise, class instance, function) vì sẽ phá vỡ DevTools time-travel và middleware như `redux-persist`.",
    q_en: "What is an action in Redux? What is the structure of an action?",
    a_en: "An action is a plain JavaScript object describing an event that occurred in the application. It must have a `type` field (a string identifier) and typically includes a `payload` with data. Example: `{ type: 'cart/addItem', payload: { id: 1, name: 'iPhone', qty: 1 } }`. The convention of naming `type` as `domain/eventName` makes it easy to read in DevTools. An action creator is a function that returns an action object: `const addItem = (item) => ({ type: 'cart/addItem', payload: item })` — prevents typos and enables reuse. With Redux Toolkit, `createSlice` automatically generates action creators and action types. Pitfall: never put non-serializable values in actions (Promises, class instances, functions) as this breaks DevTools time-travel and middleware like `redux-persist`.",
  },
  {
    id: 604,
    category: "State Management",
    subcategory: "Redux",
    level: "beginner",
    q: "Reducer trong Redux là gì? Cách viết một reducer đúng chuẩn?",
    a: "Reducer là pure function nhận `(state, action)` và trả về state mới — không được mutate state trực tiếp mà phải tạo bản sao mới. Ví dụ đúng: `case 'ADD_TODO': return [...state, action.payload]` thay vì `state.push(action.payload)`. Pure function nghĩa là không có side effects, không gọi API, không random — cùng input luôn cho cùng output, điều này giúp Redux DevTools time-travel hoạt động được. Luôn có `default: return state` để tránh trả về `undefined` khi action không khớp. Với Redux Toolkit, dùng Immer bên trong nên được phép viết `state.todos.push(item)` mà vẫn immutable thật sự.",
    q_en: "What is a reducer in Redux? How do you write a proper reducer?",
    a_en: "A reducer is a pure function that takes `(state, action)` and returns a new state — it must never mutate state directly; it must return a new copy. Correct example: `case 'ADD_TODO': return [...state, action.payload]` instead of `state.push(action.payload)`. Being a pure function means no side effects, no API calls, no randomness — the same input always produces the same output, which is what enables Redux DevTools time-travel. Always include `default: return state` to avoid returning `undefined` when no action matches. With Redux Toolkit, Immer is used internally, so you can write `state.todos.push(item)` and it remains truly immutable.",
  },
  {
    id: 605,
    category: "State Management",
    subcategory: "Redux",
    level: "beginner",
    q: "Store trong Redux là gì? Các phương thức chính của store là gì?",
    a: "Store là object trung tâm lưu trữ toàn bộ state tree của ứng dụng, được tạo bằng `configureStore()` (RTK) hoặc `createStore()` (legacy). Ba phương thức chính: `getState()` trả về state hiện tại, `dispatch(action)` gửi action qua middleware rồi đến reducer để cập nhật state, `subscribe(listener)` đăng ký callback được gọi sau mỗi dispatch. Trong thực tế với React, bạn hiếm khi gọi trực tiếp các phương thức này vì `react-redux` đã wrap chúng qua `useSelector` (thay getState + subscribe) và `useDispatch` (thay dispatch). Pitfall: chỉ nên có MỘT store duy nhất trong app — nhiều store phá vỡ nguyên tắc single source of truth và làm DevTools không hoạt động đúng. Với RTK, `configureStore` tự động setup Redux DevTools, thunk middleware, và development checks (phát hiện accidental mutation).",
    q_en: "What is the Store in Redux? What are its main methods?",
    a_en: "The store is the central object holding the entire application state tree, created with `configureStore()` (RTK) or `createStore()` (legacy). Three main methods: `getState()` returns the current state, `dispatch(action)` sends an action through middleware then to the reducer to update state, `subscribe(listener)` registers a callback called after every dispatch. In practice with React, you rarely call these methods directly because `react-redux` wraps them via `useSelector` (replaces getState + subscribe) and `useDispatch` (replaces dispatch). Pitfall: there should only be ONE store in an app — multiple stores break the single source of truth principle and prevent DevTools from working correctly. With RTK, `configureStore` automatically sets up Redux DevTools, thunk middleware, and development checks (detecting accidental mutations).",
  },
  {
    id: 606,
    category: "State Management",
    subcategory: "Redux",
    level: "intermediate",
    q: "Redux Toolkit (RTK) là gì? Tại sao nên dùng RTK thay vì Redux thuần?",
    a: "Redux Toolkit (RTK) là bộ công cụ chính thức và là cách duy nhất được khuyến nghị để viết Redux hiện đại — Redux thuần giờ được coi là legacy. RTK giải quyết 3 vấn đề lớn của Redux thuần: (1) quá nhiều boilerplate (actions, action types, reducers, combineReducers riêng biệt) → `createSlice` gộp tất cả vào một chỗ. (2) Immutable update phức tạp (`{...state, nested: {...state.nested, value: newVal}}`) → Immer bên trong cho phép viết `state.nested.value = newVal`. (3) Async logic verbose → `createAsyncThunk` xử lý pending/fulfilled/rejected tự động. Thêm RTK Query cho data fetching và caching, cạnh tranh trực tiếp với React Query. Ví dụ thực tế: 1 feature Redux thuần cần 4-5 files (types, actions, reducer, selectors, thunks), RTK chỉ cần 1 file slice. Pitfall: khi migrate từ Redux thuần sang RTK, đừng rewrite toàn bộ — RTK tương thích ngược, migrate từng slice một.",
    q_en: "What is Redux Toolkit (RTK)? Why use RTK instead of plain Redux?",
    a_en: "Redux Toolkit (RTK) is the official toolset and the only recommended way to write modern Redux — plain Redux is now considered legacy. RTK solves three major problems with plain Redux: (1) excessive boilerplate (separate actions, action types, reducers, combineReducers) → `createSlice` consolidates everything. (2) Complex immutable updates (`{...state, nested: {...state.nested, value: newVal}}`) → Immer allows writing `state.nested.value = newVal`. (3) Verbose async logic → `createAsyncThunk` handles pending/fulfilled/rejected automatically. RTK Query handles data fetching and caching, directly competing with React Query. Practical example: one Redux feature requires 4-5 files (types, actions, reducer, selectors, thunks); RTK needs just one slice file. Pitfall: when migrating from plain Redux to RTK, don't rewrite everything at once — RTK is backward-compatible; migrate one slice at a time.",
  },
  {
    id: 607,
    category: "State Management",
    subcategory: "RTK",
    level: "intermediate",
    q: "createSlice trong RTK hoạt động như thế nào?",
    a: "`createSlice` là API quan trọng nhất của RTK, nhận object gồm `name` (prefix cho action types), `initialState`, và `reducers` (object chứa các reducer functions). Nó tự động tạo action creators và action types — ví dụ: reducer `increment` trong slice tên 'counter' tạo ra action type `counter/increment` và action creator `counterSlice.actions.increment()`. Bên trong dùng Immer nên viết `state.value += 1` thay vì `return { ...state, value: state.value + 1 }` — code ngắn hơn 3-4 lần với nested state. Export 2 thứ: `counterSlice.actions` (dùng trong component dispatch) và `counterSlice.reducer` (dùng trong configureStore). Pitfall thường gặp: trong reducers của createSlice, hoặc mutate state HOẶC return state mới, KHÔNG làm cả hai — Immer sẽ throw error. Ví dụ sai: `state.items.push(item); return state;`.",
    q_en: "How does createSlice work in RTK?",
    a_en: "`createSlice` is the most important API in RTK. It accepts an object with `name` (prefix for action types), `initialState`, and `reducers` (an object of reducer functions). It automatically generates action creators and action types — for example, a reducer named `increment` in a slice called 'counter' produces action type `counter/increment` and action creator `counterSlice.actions.increment()`. Immer is used internally, so you can write `state.value += 1` instead of `return { ...state, value: state.value + 1 }` — 3-4x less code for nested state. Export two things: `counterSlice.actions` (used in components to dispatch) and `counterSlice.reducer` (used in configureStore). Common pitfall: inside createSlice reducers, either mutate state OR return a new state — never both. Immer will throw an error. Wrong example: `state.items.push(item); return state;`",
  },
  {
    id: 608,
    category: "State Management",
    subcategory: "RTK",
    level: "intermediate",
    q: "createAsyncThunk được dùng để làm gì? Các trạng thái lifecycle của nó là gì?",
    a: "`createAsyncThunk` tạo thunk action creator xử lý async logic (API calls), tự động dispatch 3 lifecycle actions: `pending` (request bắt đầu → show loading), `fulfilled` (thành công → update data), `rejected` (thất bại → show error). Ví dụ: `createAsyncThunk('users/fetch', async (userId, { rejectWithValue }) => { try { return await api.getUser(userId) } catch (err) { return rejectWithValue(err.response.data) } })`. Xử lý trong slice dùng `extraReducers` với builder pattern: `builder.addCase(fetchUser.pending, (state) => { state.loading = true })`. Pitfall: luôn dùng `rejectWithValue` thay vì throw error trực tiếp — nếu không, error object không serializable và DevTools hiển thị sai. Cũng nên dùng `condition` option để cancel thunk nếu data đã có trong cache, tránh duplicate API calls. Với RTK Query, nhiều use case của createAsyncThunk có thể thay thế hoàn toàn mà không cần viết reducer.",
    q_en: "What is createAsyncThunk used for? What are its lifecycle states?",
    a_en: "`createAsyncThunk` creates a thunk action creator that handles async logic (API calls), automatically dispatching three lifecycle actions: `pending` (request started → show loading), `fulfilled` (success → update data), `rejected` (failure → show error). Example: `createAsyncThunk('users/fetch', async (userId, { rejectWithValue }) => { try { return await api.getUser(userId) } catch (err) { return rejectWithValue(err.response.data) } })`. Handle in a slice using `extraReducers` with builder pattern: `builder.addCase(fetchUser.pending, (state) => { state.loading = true })`. Pitfall: always use `rejectWithValue` instead of throwing errors directly — otherwise the error object is not serializable and DevTools display it incorrectly. Also use the `condition` option to cancel the thunk if data is already cached, avoiding duplicate API calls. With RTK Query, many createAsyncThunk use cases can be replaced entirely without writing a reducer.",
  },
  {
    id: 609,
    category: "State Management",
    subcategory: "RTK",
    level: "intermediate",
    q: "Selector trong Redux là gì? Tại sao nên dùng createSelector?",
    a: "Selector là function nhận toàn bộ Redux state và trích xuất/tính toán phần data cần thiết, ví dụ: `const selectActiveTodos = state => state.todos.filter(t => !t.completed)`. Vấn đề: `filter()` tạo array mới mỗi lần gọi → `useSelector` thấy reference khác → component re-render dù data không đổi. `createSelector` từ Reselect giải quyết bằng memoization: chỉ tính toán lại khi input selectors trả về giá trị khác. Ví dụ: `createSelector([selectTodos, selectFilter], (todos, filter) => todos.filter(t => t.status === filter))` — nếu todos và filter không đổi, trả về kết quả cached. Pitfall phổ biến: tạo selector bên trong component khiến memoization không hoạt động vì selector mới được tạo mỗi render — luôn định nghĩa selector bên ngoài component hoặc dùng `useMemo`. Trong app lớn, selector composition (selector dùng selector khác) giúp tái sử dụng logic tính toán.",
    q_en: "What is a selector in Redux? Why should you use createSelector?",
    a_en: "A selector is a function that takes the full Redux state and extracts or computes the data needed, e.g., `const selectActiveTodos = state => state.todos.filter(t => !t.completed)`. Problem: `filter()` creates a new array on every call → `useSelector` sees a different reference → component re-renders even when data hasn't changed. `createSelector` from Reselect solves this with memoization: it only recomputes when input selectors return different values. Example: `createSelector([selectTodos, selectFilter], (todos, filter) => todos.filter(t => t.status === filter))` — returns cached result if todos and filter haven't changed. Common pitfall: defining a selector inside a component means memoization doesn't work because a new selector is created on every render — always define selectors outside components or use `useMemo`. In large apps, selector composition (selectors using other selectors) promotes reuse of computed logic.",
  },
  {
    id: 610,
    category: "State Management",
    subcategory: "RTK",
    level: "advanced",
    q: "RTK Query là gì? Lợi ích so với cách fetch data thủ công?",
    a: "RTK Query là data fetching và caching layer tích hợp sẵn trong Redux Toolkit, cạnh tranh trực tiếp với React Query/SWR. Ưu điểm so với fetch thủ công: tự động quản lý loading/error/success states (không cần viết 3 cases trong reducer), cache data theo endpoint + params, tự động refetch khi args thay đổi, deduplication (10 components cùng gọi `useGetUserQuery(1)` chỉ 1 request). Cache invalidation qua tag system: `providesTags: ['Post']` ở query và `invalidatesTags: ['Post']` ở mutation — khi mutation xong, tất cả queries với tag 'Post' tự động refetch. So với React Query: RTK Query tích hợp sẵn Redux store (không cần thêm thư viện), nhưng React Query có API linh hoạt hơn và không bắt buộc dùng Redux. Pitfall: nếu project đã dùng React Query, thêm RTK Query tạo 2 cache layers trùng lặp — chọn một.",
    q_en: "What is RTK Query? What are its advantages over manual data fetching?",
    a_en: "RTK Query is a data fetching and caching layer built into Redux Toolkit, competing directly with React Query/SWR. Advantages over manual fetching: automatically manages loading/error/success states (no need to write 3 cases in a reducer), caches data by endpoint and params, automatically refetches when args change, and deduplicates requests (10 components calling `useGetUserQuery(1)` results in only one HTTP request). Cache invalidation via tag system: `providesTags: ['Post']` on the query and `invalidatesTags: ['Post']` on the mutation — when a mutation completes, all queries with the 'Post' tag automatically refetch. Compared to React Query: RTK Query integrates into the Redux store (no extra library), but React Query has a more flexible API and doesn't require Redux. Pitfall: if a project already uses React Query, adding RTK Query creates two duplicate cache layers — pick one.",
  },
  {
    id: 611,
    category: "State Management",
    subcategory: "RTK",
    level: "advanced",
    q: "Cách định nghĩa API với createApi trong RTK Query như thế nào?",
    a: "`createApi` là core API của RTK Query, định nghĩa toàn bộ endpoints cho một base URL. Cấu hình `baseQuery` (thường `fetchBaseQuery({ baseUrl: '/api' })`) xác định cách gọi API, có thể custom để thêm auth headers hay handle token refresh. Mỗi endpoint là query (GET) hoặc mutation (POST/PUT/DELETE), tự động tạo hooks: `getUsers` → `useGetUsersQuery()`, `addUser` → `useAddUserMutation()`. Cache invalidation dùng tag system: `tagTypes: ['User']`, query `providesTags: ['User']`, mutation `invalidatesTags: ['User']` — khi add user xong, danh sách users tự refetch. Ví dụ: `endpoints: (builder) => ({ getUsers: builder.query({ query: () => '/users', providesTags: ['User'] }), addUser: builder.mutation({ query: (body) => ({ url: '/users', method: 'POST', body }), invalidatesTags: ['User'] }) })`. Pitfall: chỉ nên có MỘT `createApi` per base URL, dùng `injectEndpoints` để code-split endpoints ra nhiều files.",
    q_en: "How do you define an API with createApi in RTK Query?",
    a_en: "`createApi` is the core API of RTK Query, defining all endpoints for a base URL. The `baseQuery` config (typically `fetchBaseQuery({ baseUrl: '/api' })`) determines how API calls are made; it can be customized to add auth headers or handle token refresh. Each endpoint is a query (GET) or mutation (POST/PUT/DELETE), automatically generating hooks: `getUsers` → `useGetUsersQuery()`, `addUser` → `useAddUserMutation()`. Cache invalidation uses a tag system: `tagTypes: ['User']`, query with `providesTags: ['User']`, mutation with `invalidatesTags: ['User']` — after adding a user, the user list automatically refetches. Example: `endpoints: (builder) => ({ getUsers: builder.query({ query: () => '/users', providesTags: ['User'] }), addUser: builder.mutation({ query: (body) => ({ url: '/users', method: 'POST', body }), invalidatesTags: ['User'] }) })`. Pitfall: only one `createApi` per base URL — use `injectEndpoints` to code-split endpoints across files.",
  },
  {
    id: 612,
    category: "State Management",
    subcategory: "Redux",
    level: "intermediate",
    q: "Redux middleware là gì? Cho ví dụ về middleware phổ biến?",
    a: "Middleware là function nằm giữa `dispatch(action)` và reducer, có thể inspect, modify, delay, hoặc cancel action trước khi đến reducer. Flow: `dispatch → middleware1 → middleware2 → reducer`. Middleware phổ biến: `redux-thunk` (cho phép dispatch function thay vì object, xử lý async — RTK include mặc định), `redux-saga` (dùng generator functions cho complex async flows như race conditions, cancellation, debounce), `redux-logger` (log action type, prev state, next state — hữu ích cho debug). Ví dụ custom middleware: `const logger = store => next => action => { console.log(action.type); return next(action) }`. Pitfall: thứ tự middleware quan trọng — thunk phải ở đầu để dispatch(function) được xử lý trước khi đến middleware khác. Với RTK, `configureStore` tự setup thunk + serialization check + immutability check, chỉ cần thêm custom middleware qua `middleware` option nếu cần.",
    q_en: "What is Redux middleware? Give examples of common middleware.",
    a_en: "Middleware is a function that sits between `dispatch(action)` and the reducer, able to inspect, modify, delay, or cancel an action before it reaches the reducer. Flow: `dispatch → middleware1 → middleware2 → reducer`. Common middleware: `redux-thunk` (allows dispatching functions instead of objects for async logic — included in RTK by default), `redux-saga` (uses generator functions for complex async flows like race conditions, cancellation, debounce), `redux-logger` (logs action type, prev state, next state — useful for debugging). Example custom middleware: `const logger = store => next => action => { console.log(action.type); return next(action) }`. Pitfall: middleware order matters — thunk must come first so dispatched functions are processed before other middleware. With RTK, `configureStore` automatically sets up thunk, serialization checks, and immutability checks; add custom middleware via the `middleware` option only when needed.",
  },
  {
    id: 613,
    category: "State Management",
    subcategory: "Redux",
    level: "advanced",
    q: "Redux DevTools hoạt động như thế nào? Tính năng time-travel debugging là gì?",
    a: "Redux DevTools là browser extension record toàn bộ action history theo timeline — mỗi action được log kèm payload, prev state và next state diff. Time-travel mechanism: DevTools lưu snapshot state sau mỗi action; bạn có thể 'jump' đến bất kỳ snapshot bằng click vào action trong history list, app render lại đúng state tại thời điểm đó mà không cần reload hay reproduce thủ công. Tính năng chính: (1) Action log với filter theo type, (2) State diff highlighting thay đổi màu xanh/đỏ, (3) Replay — chạy lại từng action từ đầu, (4) Skip action — loại bỏ action cụ thể để test what-if, (5) Import/export state JSON để share bug reproduction, (6) Dispatcher panel để dispatch action thủ công. Workflow thực tế: user báo bug ở màn hình checkout → export state JSON từ session của họ → import vào DevTools của developer → jump từng action để tìm chính xác điểm gây ra bug. Yêu cầu quan trọng: state và action payload PHẢI serializable — không được là Date, Function, Promise hay class instance — vi phạm khiến DevTools crash. RTK tích hợp sẵn serializability check ở development mode để cảnh báo ngay khi có non-serializable data.",
    q_en: "How does Redux DevTools work? What is time-travel debugging?",
    a_en: "Redux DevTools is a browser extension that records the entire action history as a timeline — each action is logged with its payload, previous state, and next state diff. Time-travel mechanism: DevTools saves a state snapshot after each action; you can 'jump' to any snapshot by clicking an action in the history list, and the app re-renders to exactly that state without needing to reload or manually reproduce the bug. Key features: (1) Action log with type filter, (2) State diff with color-coded changes, (3) Replay — re-run all actions from the start, (4) Skip action — remove a specific action to test what-if scenarios, (5) Import/export state JSON for sharing bug reproductions, (6) Dispatcher panel to manually dispatch actions. Real-world workflow: user reports a bug at checkout → export their session state JSON → import into developer's DevTools → step through each action to pinpoint exactly where the bug occurred. Important requirement: all state and action payloads MUST be serializable — no Dates, Functions, Promises, or class instances. Violations cause DevTools to crash. RTK includes a serializability check in development mode to warn immediately about non-serializable data.",
  },
  {
    id: 614,
    category: "State Management",
    subcategory: "Redux",
    level: "intermediate",
    q: "useSelector và useDispatch hook trong React-Redux hoạt động như thế nào?",
    a: "useSelector nhận selector function, subscribe vào Redux store, và dùng strict === comparison (reference equality) để quyết định có re-render không. Pitfall quan trọng: nếu selector trả về object/array mới mỗi lần gọi — ví dụ `useSelector(s => s.items.filter(...))` — reference luôn khác nên component re-render liên tục dù data không đổi. Fix: dùng `shallowEqual` từ react-redux làm argument thứ hai (`useSelector(selector, shallowEqual)`) để so sánh từng property, hoặc dùng `createSelector` (Reselect) để memoize. useDispatch trả về stable dispatch reference — không bao giờ thay đổi giữa các render, an toàn khi đặt vào dependency array. So sánh với connect() HOC cũ: connect() dùng shallowEqual mặc định nên ít gặp re-render issue hơn, nhưng code verbose (mapStateToProps, mapDispatchToProps) và wrap component trong HOC gây khó debug. useSelector/useDispatch code gọn hơn 50% nhưng developer phải tự xử lý equality. Ví dụ pitfall thực tế: `useSelector(s => ({ a: s.a, b: s.b }))` tạo object mới mỗi render → infinite re-render loop khi có dispatch trong useEffect — fix bằng 2 useSelector riêng biệt hoặc shallowEqual.",
    q_en: "How do the useSelector and useDispatch hooks work in React-Redux?",
    a_en: "useSelector takes a selector function, subscribes to the Redux store, and uses strict === comparison (reference equality) to decide whether to re-render. Important pitfall: if the selector returns a new object/array on every call — e.g., `useSelector(s => s.items.filter(...))` — the reference is always different, causing the component to re-render constantly even if data hasn't changed. Fix: use `shallowEqual` from react-redux as the second argument (`useSelector(selector, shallowEqual)`) to compare properties, or use `createSelector` (Reselect) for memoization. useDispatch returns a stable dispatch reference — it never changes between renders, making it safe to include in dependency arrays. Compared to the old `connect()` HOC: connect() uses shallowEqual by default so re-render issues are less common, but the code is verbose (mapStateToProps, mapDispatchToProps) and wraps components in HOCs that are harder to debug. useSelector/useDispatch reduces code by ~50% but developers must handle equality themselves. Practical pitfall: `useSelector(s => ({ a: s.a, b: s.b }))` creates a new object every render → potential infinite re-render loop if there's a dispatch in a useEffect — fix with two separate useSelectors or shallowEqual.",
  },
  {
    id: 615,
    category: "State Management",
    subcategory: "Redux",
    level: "advanced",
    q: "Khi nào nên dùng Redux và khi nào không cần?",
    a: "Framework decision: DÙNG Redux khi (1) shared state cần ở nhiều component cách xa nhau — ví dụ user auth info cần ở Header, Sidebar, Feed và Modal đồng thời; (2) state logic phức tạp với nhiều actions tương tác — shopping cart có coupon, discount, tax, shipping tính toán lẫn nhau; (3) cần action history/audit trail — ứng dụng financial, collaborative editing cần biết ai làm gì lúc nào; (4) team đã quen Redux với codebase lớn — migration cost cao hơn benefit. KHÔNG dùng Redux khi: (1) chủ yếu là server state — dùng React Query thay, Redux cho server data là over-engineering; (2) state chỉ local trong 1-2 component — useState là đủ; (3) app nhỏ hoặc team mới — learning curve Redux + RTK không cần thiết. Alternatives landscape: Zustand cho global client state đơn giản (~8KB, zero boilerplate), Jotai cho atomic fine-grained state, React Query cho server state (thay thế 80% Redux use cases), Context API cho low-frequency updates như theme/locale. Rule of thumb: nếu đang viết Redux chủ yếu để cache API data, hãy dùng React Query — Redux nên chỉ còn cho UI state thật sự global như auth, cart, notification preferences.",
    q_en: "When should you use Redux and when is it unnecessary?",
    a_en: "Framework decision — USE Redux when: (1) shared state is needed across many distant components — e.g., user auth info required simultaneously in Header, Sidebar, Feed, and Modal; (2) state logic is complex with many interacting actions — a shopping cart with coupons, discounts, taxes, and shipping that all affect each other; (3) you need an action history/audit trail — financial or collaborative editing apps that need to know who did what and when; (4) the team is already experienced with Redux in a large codebase — migration cost outweighs the benefit. DON'T use Redux when: (1) it's mostly server state — use React Query instead; Redux for server data is over-engineering; (2) state is only local to 1-2 components — useState is sufficient; (3) the app is small or the team is new — the Redux + RTK learning curve is unnecessary. Alternatives: Zustand for simple global client state (~8KB, zero boilerplate), Jotai for atomic fine-grained state, React Query for server state (replaces 80% of Redux use cases), Context API for low-frequency updates like theme/locale. Rule of thumb: if you're writing Redux primarily to cache API data, use React Query — Redux should be reserved for truly global UI state like auth, cart, and notification preferences.",
  },

  // === STATE MANAGEMENT: Zustand (616-625) ===
  {
    id: 616,
    category: "State Management",
    subcategory: "Zustand",
    level: "beginner",
    q: "Zustand là gì? So sánh với Redux về độ phức tạp?",
    a: "Zustand là thư viện quản lý state nhỏ gọn cho React, không yêu cầu boilerplate như Redux — không cần actions, reducers, dispatch, hay Provider wrapper. API chỉ gồm hàm `create` trả về một hook, state và actions định nghĩa cùng chỗ. Bundle size ~1KB so với Redux Toolkit ~40KB khi đầy đủ. Ví dụ tạo store đơn giản: `const useStore = create(set => ({ count: 0, inc: () => set(s => ({ count: s.count + 1 })) }))`, dùng trong component: `const count = useStore(s => s.count)`. Pitfall quan trọng: luôn dùng selector khi lấy state (`useStore(s => s.count)` thay vì `useStore()`) để tránh re-render không cần thiết khi phần state khác thay đổi.",
    q_en: "What is Zustand? How does it compare to Redux in terms of complexity?",
    a_en: "Zustand is a small, lightweight state management library for React that requires no boilerplate like Redux — no actions, reducers, dispatch, or Provider wrapper needed. The API consists of a single `create` function that returns a hook, with state and actions defined in the same place. Bundle size is ~1KB compared to Redux Toolkit at ~40KB fully loaded. Example of creating a simple store: `const useStore = create(set => ({ count: 0, inc: () => set(s => ({ count: s.count + 1 })) }))`, used in a component as `const count = useStore(s => s.count)`. Important pitfall: always use a selector when reading state (`useStore(s => s.count)` rather than `useStore()`) to avoid unnecessary re-renders when unrelated parts of state change.",
  },
  {
    id: 617,
    category: "State Management",
    subcategory: "Zustand",
    level: "beginner",
    q: "Cách tạo một store với Zustand như thế nào?",
    a: "Dùng `create` từ 'zustand', truyền vào initializer function nhận `(set, get)` và trả về object chứa cả state lẫn actions trong cùng một chỗ. `set()` merge partial state (không cần spread toàn bộ như Redux). `get()` đọc state hiện tại từ bên trong action. Ví dụ đầy đủ: `const useCartStore = create((set, get) => ({ items: [], total: 0, addItem: (item) => set((s) => ({ items: [...s.items, item], total: s.total + item.price })), clearCart: () => set({ items: [], total: 0 }), getItemCount: () => get().items.length }))`. Store trả về là một React hook, dùng trực tiếp: `const items = useCartStore(s => s.items)` hoặc `const addItem = useCartStore(s => s.addItem)`. Không cần Provider, không cần dispatch — gọi action như gọi function bình thường: `addItem(product)`. So sánh với Redux: 1 file slice Zustand thay thế được actions + actionTypes + reducer + selectors + thunk.",
    q_en: "How do you create a store with Zustand?",
    a_en: "Use `create` from 'zustand', passing an initializer function that receives `(set, get)` and returns an object containing both state and actions in one place. `set()` merges partial state (no need to spread everything like in Redux). `get()` reads the current state from within an action. Full example: `const useCartStore = create((set, get) => ({ items: [], total: 0, addItem: (item) => set((s) => ({ items: [...s.items, item], total: s.total + item.price })), clearCart: () => set({ items: [], total: 0 }), getItemCount: () => get().items.length }))`. The store returns a React hook, used directly: `const items = useCartStore(s => s.items)` or `const addItem = useCartStore(s => s.addItem)`. No Provider, no dispatch — call actions like regular functions: `addItem(product)`. Compared to Redux: one Zustand slice file replaces actions + actionTypes + reducer + selectors + thunks.",
  },
  {
    id: 618,
    category: "State Management",
    subcategory: "Zustand",
    level: "intermediate",
    q: "Cách định nghĩa actions trong Zustand store?",
    a: "Actions trong Zustand là plain functions sống cùng state trong store object — không có action types, không dispatch, không reducers. Có 3 pattern chính: (1) Partial update với set: `increment: () => set(s => ({ count: s.count + 1 }))`. (2) Computed values dùng get() để đọc state hiện tại bên trong action: `addItem: (item) => { const existing = get().items.find(i => i.id === item.id); set(s => ({ items: existing ? s.items.map(i => i.id === item.id ? {...i, qty: i.qty+1} : i) : [...s.items, item] })) }`. (3) Async actions — không cần thunk middleware, viết async/await trực tiếp: `fetchUser: async (id) => { set({ loading: true }); const user = await api.getUser(id); set({ user, loading: false }) }`. Multiple state updates trong một set call tự động batched. Pitfall: `set` merge shallow — nested object phải tự spread: `set(s => ({ config: { ...s.config, theme: 'dark' } }))`.",
    q_en: "How do you define actions in a Zustand store?",
    a_en: "Actions in Zustand are plain functions that live alongside state in the store object — no action types, no dispatch, no reducers. Three main patterns: (1) Partial update with set: `increment: () => set(s => ({ count: s.count + 1 }))`. (2) Reading current state with get() inside an action: `addItem: (item) => { const existing = get().items.find(i => i.id === item.id); set(s => ({ items: existing ? s.items.map(i => i.id === item.id ? {...i, qty: i.qty+1} : i) : [...s.items, item] })) }`. (3) Async actions — no thunk middleware needed, write async/await directly: `fetchUser: async (id) => { set({ loading: true }); const user = await api.getUser(id); set({ user, loading: false }) }`. Multiple state updates in a single set call are automatically batched. Pitfall: `set` does a shallow merge — nested objects must be manually spread: `set(s => ({ config: { ...s.config, theme: 'dark' } }))`.",
  },
  {
    id: 619,
    category: "State Management",
    subcategory: "Zustand",
    level: "intermediate",
    q: "Zustand khác Redux như thế nào về kiến trúc?",
    a: "Kiến trúc khác nhau căn bản: Redux theo Flux pattern — action object → dispatch → middleware → reducer → new state; Zustand không có khái niệm action types hay reducers — chỉ là functions gọi set(). Redux bắt buộc: Provider wrapper, combineReducers, action creators, selectors riêng biệt, middleware để async. Zustand không cần gì trong số đó. Redux enforces một flow dữ liệu nghiêm ngặt (tốt cho team lớn, audit), Zustand flexible hơn (tốt cho DX và speed). Về middleware: Redux có ecosystem middleware phong phú (thunk, saga, logger, sentry), Zustand có middleware nhẹ hơn (devtools, persist, immer) nhưng đủ dùng cho hầu hết cases. Về DevTools: cả hai đều kết nối được Redux DevTools Extension. Khi Zustand tỏa sáng: prototype nhanh, app vừa, team nhỏ. Khi Redux tỏa sáng: large team cần consistency, cần RTK Query, cần full audit trail, cần time-travel debugging end-to-end.",
    q_en: "How does Zustand differ from Redux architecturally?",
    a_en: "Fundamentally different architectures: Redux follows the Flux pattern — action object → dispatch → middleware → reducer → new state; Zustand has no concept of action types or reducers — just functions calling set(). Redux requires: a Provider wrapper, combineReducers, action creators, separate selectors, and middleware for async logic. Zustand needs none of that. Redux enforces a strict data flow (good for large teams and auditing); Zustand is more flexible (better DX and speed). On middleware: Redux has a rich ecosystem (thunk, saga, logger, sentry), Zustand has lighter middleware (devtools, persist, immer) but sufficient for most use cases. Both can connect to Redux DevTools Extension. Where Zustand shines: rapid prototyping, medium-sized apps, small teams. Where Redux shines: large teams needing consistency, projects requiring RTK Query, full audit trails, or end-to-end time-travel debugging.",
  },
  {
    id: 620,
    category: "State Management",
    subcategory: "Zustand",
    level: "intermediate",
    q: "Làm thế nào để tránh re-render không cần thiết trong Zustand?",
    a: "Zustand mặc định dùng strict === equality để so sánh — nếu selector trả về primitive (number, string, boolean) thì không vấn đề gì. Vấn đề xảy ra khi selector trả về object/array mới mỗi lần: `useStore(s => ({ a: s.a, b: s.b }))` tạo object mới mỗi render → component luôn re-render. Giải pháp: (1) Tách thành 2 selectors riêng: `const a = useStore(s => s.a); const b = useStore(s => s.b)`. (2) Dùng `useShallow` hook từ 'zustand/react/shallow' để shallow compare object/array: `const { a, b } = useStore(useShallow(s => ({ a: s.a, b: s.b })))`. (3) Dùng `createSelectors` pattern để auto-generate per-property selectors. Auto-selector pattern: `const createSelectors = (store) => { const s = store; s.use = {}; Object.keys(s.getState()).forEach(k => { s.use[k] = () => s(x => x[k]) }); return s }` → dùng `useStore.use.count()`. Pitfall khác: gọi `useStore()` không truyền selector → subscribe toàn bộ store, re-render khi bất kỳ state nào thay đổi.",
    q_en: "How do you prevent unnecessary re-renders in Zustand?",
    a_en: "Zustand uses strict === equality by default — if the selector returns a primitive (number, string, boolean) there's no issue. The problem arises when the selector returns a new object/array on every call: `useStore(s => ({ a: s.a, b: s.b }))` creates a new object every render → component always re-renders. Solutions: (1) Split into two separate selectors: `const a = useStore(s => s.a); const b = useStore(s => s.b)`. (2) Use the `useShallow` hook from 'zustand/react/shallow' for shallow comparison: `const { a, b } = useStore(useShallow(s => ({ a: s.a, b: s.b })))`. (3) Use the `createSelectors` pattern to auto-generate per-property selectors. Auto-selector pattern: `const createSelectors = (store) => { const s = store; s.use = {}; Object.keys(s.getState()).forEach(k => { s.use[k] = () => s(x => x[k]) }); return s }` → use as `useStore.use.count()`. Another pitfall: calling `useStore()` with no selector subscribes to the entire store and re-renders whenever any state changes.",
  },
  {
    id: 621,
    category: "State Management",
    subcategory: "Zustand",
    level: "intermediate",
    q: "Zustand middleware là gì? Cho ví dụ về devtools middleware?",
    a: "Zustand middleware dùng pattern higher-order function wrap store creator. Import từ 'zustand/middleware'. Compose nhiều middleware bằng cú pháp lồng nhau: `create(devtools(immer(persist(storeCreator, persistOptions)), devtoolsOptions))` — thứ tự quan trọng: devtools nên ở ngoài cùng để capture đúng actions. Middleware phổ biến: (1) `devtools` — kết nối Redux DevTools Extension, đặt tên action cho mỗi set call: `set(updater, false, 'addItem')` để log đẹp trong DevTools; (2) `immer` — cho phép mutate state trực tiếp trong set: `set(s => { s.items.push(item) })` thay vì phải spread; (3) `persist` — lưu/khôi phục state vào storage; (4) `subscribeWithSelector` — cho phép subscribe vào một phần state với selector và equality function, hữu ích cho side effects. Custom middleware: function nhận `(config) => (set, get, api) => config(set, get, api)` với logic thêm vào trước/sau set.",
    q_en: "What is Zustand middleware? Give an example of the devtools middleware.",
    a_en: "Zustand middleware uses a higher-order function pattern that wraps the store creator. Import from 'zustand/middleware'. Compose multiple middleware by nesting: `create(devtools(immer(persist(storeCreator, persistOptions)), devtoolsOptions))` — order matters: devtools should be outermost to capture actions correctly. Common middleware: (1) `devtools` — connects to Redux DevTools Extension; name each set call: `set(updater, false, 'addItem')` for clean DevTools logs; (2) `immer` — allows direct state mutation in set: `set(s => { s.items.push(item) })` instead of spreading; (3) `persist` — saves/restores state to storage; (4) `subscribeWithSelector` — allows subscribing to a state slice with a selector and equality function, useful for side effects. Custom middleware: a function taking `(config) => (set, get, api) => config(set, get, api)` with additional logic before/after set.",
  },
  {
    id: 622,
    category: "State Management",
    subcategory: "Zustand",
    level: "intermediate",
    q: "persist middleware trong Zustand hoạt động như thế nào?",
    a: "Middleware `persist` từ 'zustand/middleware' serialize state thành JSON rồi lưu vào storage, rehydrate khi app khởi động. Config quan trọng: `name` (storage key, bắt buộc), `partialize` để chọn phần state cần persist — tránh lưu thừa: `partialize: (s) => ({ user: s.user, theme: s.theme })\` (không lưu loading/error states), \`storage\` để override engine: \`createJSONStorage(() => sessionStorage)\` hoặc custom AsyncStorage cho React Native. \`onRehydrateStorage\` là lifecycle hook chạy khi rehydrate: \`onRehydrateStorage: () => (state, error) => { if (error) console.error('hydration failed', error) }\`. \`version\` + \`migrate\` để handle schema changes giữa các app versions: \`version: 2, migrate: (persistedState, version) => { if (version === 1) return { ...persistedState, newField: 'default' } }\`. Pitfall: persist không merge deep — nếu cấu trúc state thay đổi giữa releases mà không có migrate, rehydrate sẽ cho state không hợp lệ.",
    q_en: "How does the persist middleware in Zustand work?",
    a_en: "The \`persist\` middleware from 'zustand/middleware' serializes state to JSON and saves it to storage, rehydrating on app startup. Key config: \`name\` (storage key, required), \`partialize\` to select which state to persist — avoid persisting too much: \`partialize: (s) => ({ user: s.user, theme: s.theme })\` (don't persist loading/error states), \`storage\` to override the engine: \`createJSONStorage(() => sessionStorage)\` or a custom AsyncStorage for React Native. \`onRehydrateStorage\` is a lifecycle hook that runs on rehydration: \`onRehydrateStorage: () => (state, error) => { if (error) console.error('hydration failed', error) }\`. \`version\` + \`migrate\` handle schema changes between app versions: \`version: 2, migrate: (persistedState, version) => { if (version === 1) return { ...persistedState, newField: 'default' } }\`. Pitfall: persist does not deep merge — if the state structure changes between releases without a migrate function, rehydration will produce invalid state.",
  },
  {
    id: 623,
    category: "State Management",
    subcategory: "Zustand",
    level: "advanced",
    q: "Cách chia nhỏ Zustand store thành nhiều slices?",
    a: "Slice pattern giúp tổ chức store lớn thành các modules độc lập. Mỗi slice là một \`StateCreator\` function: \`const createCartSlice = (set, get) => ({ items: [], addItem: (item) => set(s => ({ items: [...s.items, item] })), clearCart: () => set({ items: [] }) })\`. Tương tự: \`const createUserSlice = (set) => ({ user: null, setUser: (u) => set({ user: u }), logout: () => set({ user: null }) })\`. Combine trong create: \`const useBoundStore = create((...a) => ({ ...createCartSlice(...a), ...createUserSlice(...a) }))\`. Với TypeScript: dùng \`StateCreator<BoundStore, [], [], CartSlice>\` để type đúng — BoundStore là type của toàn bộ combined store, CartSlice là type của slice này. Slice có thể cross-reference nhau qua \`get()\`: \`const createOrderSlice = (set, get) => ({ checkout: () => { const { items } = get(); const { user } = get(); return api.order(user.id, items) } })\`. Pitfall: tên state/action không được trùng giữa các slices — spread object sẽ overwrite.",
    q_en: "How do you split a Zustand store into multiple slices?",
    a_en: "The slice pattern organizes a large store into independent modules. Each slice is a \`StateCreator\` function: \`const createCartSlice = (set, get) => ({ items: [], addItem: (item) => set(s => ({ items: [...s.items, item] })), clearCart: () => set({ items: [] }) })\`. Similarly: \`const createUserSlice = (set) => ({ user: null, setUser: (u) => set({ user: u }), logout: () => set({ user: null }) })\`. Combine in create: \`const useBoundStore = create((...a) => ({ ...createCartSlice(...a), ...createUserSlice(...a) }))\`. With TypeScript: use \`StateCreator<BoundStore, [], [], CartSlice>\` to type correctly — BoundStore is the type of the full combined store, CartSlice is the type for this slice. Slices can cross-reference each other via \`get()\`: \`const createOrderSlice = (set, get) => ({ checkout: () => { const { items } = get(); const { user } = get(); return api.order(user.id, items) } })\`. Pitfall: state/action names must not collide between slices — spreading objects will overwrite duplicates.",
  },
  {
    id: 624,
    category: "State Management",
    subcategory: "Zustand",
    level: "advanced",
    q: "Cách sử dụng Zustand với TypeScript để có type safety?",
    a: "Pattern chuẩn với TypeScript: định nghĩa type cho toàn bộ store trước, rồi pass vào generic. Zustand v4+ dùng curried create: \`create<StoreType>()((set, get) => ({...}))\` — dấu \`()\` thứ hai bắt buộc để TypeScript infer đúng. Ví dụ: \`interface CartStore { items: CartItem[]; total: number; addItem: (item: CartItem) => void; clearCart: () => void }\` → \`const useCartStore = create<CartStore>()((set, get) => ({ items: [], total: 0, addItem: (item) => set(s => ({ items: [...s.items, item], total: s.total + item.price })), clearCart: () => set({ items: [], total: 0 }) }))\`. Với slices dùng \`StateCreator\` type: \`type CartSlice = { items: CartItem[]; addItem: (item: CartItem) => void }; const createCartSlice: StateCreator<BoundStore, [], [], CartSlice> = (set) => ({...})\`. Infer store type từ store: \`type StoreState = ReturnType<typeof useStore.getState>\`. Pitfall: không nên dùng \`create\` mà không có generic — mất type safety và set nhận \`any\`.",
    q_en: "How do you use Zustand with TypeScript for type safety?",
    a_en: "Standard TypeScript pattern: define the type for the entire store first, then pass it as a generic. Zustand v4+ uses curried create: \`create<StoreType>()((set, get) => ({...}))\` — the second \`()\` is required for TypeScript to infer types correctly. Example: \`interface CartStore { items: CartItem[]; total: number; addItem: (item: CartItem) => void; clearCart: () => void }\` → \`const useCartStore = create<CartStore>()((set, get) => ({ items: [], total: 0, addItem: (item) => set(s => ({ items: [...s.items, item], total: s.total + item.price })), clearCart: () => set({ items: [], total: 0 }) }))\`. For slices use the \`StateCreator\` type: \`type CartSlice = { items: CartItem[]; addItem: (item: CartItem) => void }; const createCartSlice: StateCreator<BoundStore, [], [], CartSlice> = (set) => ({...})\`. Infer store type from the store itself: \`type StoreState = ReturnType<typeof useStore.getState>\`. Pitfall: using \`create\` without a generic loses type safety and set accepts \`any\`.",
  },
  {
    id: 625,
    category: "State Management",
    subcategory: "Zustand",
    level: "advanced",
    q: "Khi nào nên chọn Zustand thay vì Redux Toolkit?",
    a: "Nuanced comparison: Developer Experience — Zustand thắng rõ rệt: zero config, zero boilerplate, học trong 30 phút. RTK phức tạp hơn nhưng có structure rõ ràng hơn. Ecosystem: RTK thắng — có RTK Query (data fetching + caching tích hợp), middleware ecosystem phong phú, tài liệu extensive. DevTools: cả hai đều kết nối Redux DevTools Extension, nhưng RTK log actions chi tiết hơn (mỗi action có type rõ ràng), Zustand cần đặt tên manual trong devtools middleware. Team size: Zustand tốt cho team nhỏ (flexibility, speed), RTK tốt hơn cho team lớn (enforced patterns, code reviews dễ hơn khi có conventions). Learning curve: Zustand ~2h, RTK ~2 ngày (createSlice, createAsyncThunk, RTK Query, tags system). Middleware: RTK có thunk/saga/logger ecosystem rộng hơn, Zustand middleware nhẹ nhàng hơn. Kết luận thực tế: nếu không cần RTK Query và team < 5 người → Zustand. Nếu cần data fetching layer tích hợp Redux hoặc team lớn cần consistency → RTK.",
    q_en: "When should you choose Zustand over Redux Toolkit?",
    a_en: "Nuanced comparison — Developer Experience: Zustand wins clearly: zero config, zero boilerplate, learnable in 30 minutes. RTK is more complex but provides clearer structure. Ecosystem: RTK wins — includes RTK Query (integrated data fetching + caching), a rich middleware ecosystem, and extensive documentation. DevTools: both connect to Redux DevTools Extension, but RTK logs actions in more detail (each action has a clear type), while Zustand requires manually naming set calls in the devtools middleware. Team size: Zustand suits small teams (flexibility, speed); RTK suits larger teams (enforced patterns, easier code reviews with conventions). Learning curve: Zustand ~2 hours, RTK ~2 days (createSlice, createAsyncThunk, RTK Query, tag system). Practical conclusion: if you don't need RTK Query and your team has fewer than 5 people → Zustand. If you need an integrated data fetching layer with Redux or a large team that needs consistency → RTK.",
  },

  // === STATE MANAGEMENT: React Query (626-640) ===
  {
    id: 626,
    category: "State Management",
    subcategory: "React Query",
    level: "beginner",
    q: "React Query (TanStack Query) là gì? Mục đích chính của nó là gì?",
    a: "React Query (nay là TanStack Query v5) là thư viện quản lý server state cho React, giải quyết bài toán mà useState + useEffect không làm tốt: fetching, caching, synchronizing và updating data từ API. Ví dụ thực tế: không cần React Query, bạn phải tự viết loading state, error handling, cache, deduplication, refetch khi focus window, retry khi fail — React Query làm tất cả tự động. Khác biệt cốt lõi với Redux: Redux quản lý client state (UI state, form state), React Query quản lý server state (data từ API có thể thay đổi bởi người khác bất kỳ lúc nào). Trong thực tế, 80% Redux code trong các dự án là để fetch và cache API data — React Query thay thế hoàn toàn phần đó.",
    q_en: "What is React Query (TanStack Query)? What is its main purpose?",
    a_en: "React Query (now TanStack Query v5) is a server state management library for React that solves problems useState + useEffect handle poorly: fetching, caching, synchronizing, and updating data from APIs. Practical example: without React Query, you'd have to manually implement loading state, error handling, caching, deduplication, refetching on window focus, and retry on failure — React Query does all of this automatically. Core difference from Redux: Redux manages client state (UI state, form state); React Query manages server state (API data that can change at any time, owned by the server). In practice, 80% of Redux code in most projects exists to fetch and cache API data — React Query replaces that entirely.",
  },
  {
    id: 627,
    category: "State Management",
    subcategory: "React Query",
    level: "beginner",
    q: "useQuery hook hoạt động như thế nào? Các tham số cơ bản là gì?",
    a: "useQuery là hook chính để đọc data, nhận 2 tham số bắt buộc: \`queryKey\` (mảng định danh duy nhất, ví dụ \`['todos', { status: 'active' }]\`) và \`queryFn\` (async function trả về data, ví dụ \`() => fetch('/api/todos').then(r => r.json())\`). Trả về object gồm: \`data\` (kết quả), \`isLoading\` (true lần fetch đầu), \`isFetching\` (true khi đang fetch kể cả background), \`isError\`/\`error\` (lỗi), \`isSuccess`, `status`. Cơ chế: component mount → check cache theo queryKey → nếu có data fresh thì dùng luôn, nếu stale thì vừa show cached data vừa refetch background. Đây là pattern stale-while-revalidate.",
    q_en: "How does the useQuery hook work? What are its basic parameters?",
    a_en: "useQuery is the primary hook for reading data. It takes two required parameters: `queryKey` (a unique identifier array, e.g., `['todos', { status: 'active' }]`) and `queryFn` (an async function returning data, e.g., `() => fetch('/api/todos').then(r => r.json())`). Returns an object with: `data` (result), `isLoading` (true on first fetch), `isFetching` (true whenever fetching, including background), `isError`/`error` (error info), `isSuccess`, `status`. How it works: component mounts → checks cache by queryKey → if data is fresh, uses it immediately; if stale, shows cached data while refetching in the background. This is the stale-while-revalidate pattern.",
  },
  {
    id: 628,
    category: "State Management",
    subcategory: "React Query",
    level: "intermediate",
    q: "useMutation hook dùng để làm gì? Cách xử lý optimistic updates?",
    a: "useMutation xử lý write operations (POST/PUT/DELETE) — khác useQuery chỉ đọc. Cung cấp `mutate()` (fire-and-forget) hoặc `mutateAsync()` (trả về Promise). States: `isPending`, `isError`, `isSuccess`, `data`, `error`. Optimistic update flow: (1) `onMutate`: cancel ongoing queries, snapshot data cũ, set cache mới ngay → UI cập nhật tức thì. (2) `onError`: nhận snapshot từ onMutate context, rollback cache. (3) `onSettled`: invalidate queries để sync với server. Ví dụ thực tế: user like bài viết → heart đỏ ngay lập tức (optimistic), nếu API fail → rollback heart về trắng.",
    q_en: "What is the useMutation hook used for? How do you handle optimistic updates?",
    a_en: "useMutation handles write operations (POST/PUT/DELETE) — unlike useQuery which only reads. Provides `mutate()` (fire-and-forget) or `mutateAsync()` (returns a Promise). States: `isPending`, `isError`, `isSuccess`, `data`, `error`. Optimistic update flow: (1) `onMutate`: cancel ongoing queries, snapshot old data, immediately set new cache → UI updates instantly. (2) `onError`: receive snapshot from onMutate context, rollback cache. (3) `onSettled`: invalidate queries to sync with server. Practical example: user likes a post → heart turns red immediately (optimistic); if the API fails → rollback the heart to white.",
  },
  {
    id: 629,
    category: "State Management",
    subcategory: "React Query",
    level: "intermediate",
    q: "Cache invalidation trong React Query hoạt động như thế nào?",
    a: "Cache invalidation là cơ chế đánh dấu data đã cũ (stale) để trigger refetch. Cách dùng: `queryClient.invalidateQueries({ queryKey: ['todos'] })` — tất cả queries có key bắt đầu bằng 'todos' bị đánh dấu stale. Nếu component đang mount thì refetch ngay, nếu không thì refetch khi component mount lại. Thường gọi sau mutation thành công: thêm todo mới → invalidate todo list → list tự động refetch. Ngoài ra có `queryClient.setQueryData()` để update cache trực tiếp mà không cần refetch — hữu ích khi mutation trả về data mới.",
    q_en: "How does cache invalidation work in React Query?",
    a_en: "Cache invalidation marks data as stale to trigger a refetch. Usage: `queryClient.invalidateQueries({ queryKey: ['todos'] })` — all queries whose key starts with 'todos' are marked stale. If a component is currently mounted, it refetches immediately; otherwise it refetches when the component mounts again. Typically called after a successful mutation: add a todo → invalidate the todo list → list automatically refetches. You can also use `queryClient.setQueryData()` to update the cache directly without refetching — useful when a mutation returns the updated data.",
  },
  {
    id: 630,
    category: "State Management",
    subcategory: "React Query",
    level: "intermediate",
    q: "staleTime và gcTime (cacheTime) trong React Query là gì? Sự khác biệt?",
    a: "Hai config quan trọng nhất quyết định caching behavior. **staleTime** (mặc định 0): thời gian data được coi là 'fresh' — trong khoảng này component mount mới sẽ dùng cached data mà KHÔNG refetch. Ví dụ: `staleTime: 5 * 60 * 1000` (5 phút) → trong 5 phút, navigate qua lại giữa các page sẽ show cached data ngay, không loading. **gcTime** (mặc định 5 phút): thời gian data ở lại trong cache SAU KHI không còn component nào subscribe. Khi hết gcTime, data bị xóa khỏi memory. Quan hệ: staleTime quyết định 'khi nào refetch', gcTime quyết định 'khi nào xóa cache'. staleTime nên ≤ gcTime.",
    q_en: "What are staleTime and gcTime (cacheTime) in React Query? What is the difference?",
    a_en: "Two of the most important config options controlling caching behavior. **staleTime** (default 0): how long data is considered 'fresh' — during this window, newly mounted components use cached data WITHOUT refetching. Example: `staleTime: 5 * 60 * 1000` (5 minutes) → navigating back and forth between pages within 5 minutes shows cached data instantly, no loading state. **gcTime** (default 5 minutes): how long data stays in cache AFTER no components are subscribed. When gcTime expires, data is removed from memory. Relationship: staleTime controls 'when to refetch', gcTime controls 'when to delete the cache'. staleTime should always be ≤ gcTime.",
  },
  {
    id: 631,
    category: "State Management",
    subcategory: "React Query",
    level: "intermediate",
    q: "Tại sao React Query được coi là giải pháp 'server state' không phải 'global state'?",
    a: "Có 2 loại state khác biệt bản chất: **Client state** (theme, sidebar open, form input) — thuộc sở hữu hoàn toàn của client, đồng bộ ngay lập tức, không bao giờ 'stale'. **Server state** (user profile, todo list, product catalog) — thuộc sở hữu của server, có thể bị thay đổi bởi người khác, cần đồng bộ liên tục, luôn có khả năng 'stale'. Redux trộn lẫn 2 loại này vào 1 store, dẫn đến phức tạp không cần thiết (loading states, error states, normalization). React Query tách biệt: nó CHỈ quản lý server state với caching, background refetch, retry, deduplication. Client state dùng useState/useReducer/Zustand. Kết quả: giảm 50-80% Redux code trong hầu hết projects.",
    q_en: "Why is React Query considered a 'server state' solution rather than 'global state'?",
    a_en: "There are two fundamentally different types of state: **Client state** (theme, sidebar open/closed, form input) — fully owned by the client, synchronous, never becomes stale. **Server state** (user profile, todo list, product catalog) — owned by the server, can be changed by others at any time, must be continuously synchronized, always potentially stale. Redux mixes both types into one store, creating unnecessary complexity (loading states, error states, normalization). React Query is separate: it ONLY manages server state with caching, background refetch, retry, and deduplication. Client state is handled by useState/useReducer/Zustand. Result: 50-80% reduction in Redux code in most projects.",
  },
  {
    id: 632,
    category: "State Management",
    subcategory: "React Query",
    level: "intermediate",
    q: "Cách cấu hình React Query với QueryClient và QueryClientProvider?",
    a: "QueryClient là singleton quản lý toàn bộ cache và config. Tạo BÊN NGOÀI component để tránh re-create mỗi render. defaultOptions áp dụng cho tất cả queries/mutations trừ khi override per-query: `new QueryClient({ defaultOptions: { queries: { staleTime: 60_000, gcTime: 5 * 60_000, retry: 2, retryDelay: (attempt) => Math.min(1000 * 2 ** attempt, 30_000), refetchOnWindowFocus: false } } })`. Retry strategy: mặc định retry 3 lần với exponential backoff, có thể set `retry: (count, error) => error.status !== 404 && count < 3` để không retry 404. Error handling defaults: từ v5 không còn `onError` global — dùng `QueryCache` listener: `new QueryClient({ queryCache: new QueryCache({ onError: (error) => toast.error(error.message) }) })`. staleTime strategy theo loại data: static data (currencies, countries) → `Infinity`, user data → 5 phút, real-time data → 0. Trong Next.js App Router, tạo QueryClient trong server component và dùng `makeQueryClient()` factory pattern để mỗi request có instance riêng tránh data leaking giữa users.",
    q_en: "How do you configure React Query with QueryClient and QueryClientProvider?",
    a_en: "QueryClient is a singleton managing the entire cache and config. Create it OUTSIDE any component to avoid re-creating it on every render. defaultOptions apply to all queries/mutations unless overridden per-query: `new QueryClient({ defaultOptions: { queries: { staleTime: 60_000, gcTime: 5 * 60_000, retry: 2, retryDelay: (attempt) => Math.min(1000 * 2 ** attempt, 30_000), refetchOnWindowFocus: false } } })`. Retry strategy: default 3 retries with exponential backoff; can be customized: `retry: (count, error) => error.status !== 404 && count < 3` to skip retrying 404s. Error handling from v5: global `onError` is no longer available — use a `QueryCache` listener: `new QueryClient({ queryCache: new QueryCache({ onError: (error) => toast.error(error.message) }) })`. staleTime strategy by data type: static data (currencies, countries) → `Infinity`, user data → 5 minutes, real-time data → 0. In Next.js App Router, create QueryClient in a server component and use a `makeQueryClient()` factory with React's `cache()` so each request gets its own instance, preventing data leaking between users.",
  },
  {
    id: 633,
    category: "State Management",
    subcategory: "React Query",
    level: "intermediate",
    q: "refetchOnWindowFocus trong React Query là gì? Khi nào nên tắt nó?",
    a: "refetchOnWindowFocus là behavior mặc định: khi user chuyển tab rồi quay lại, React Query tự động refetch tất cả stale queries. Mục đích: đảm bảo data luôn fresh — ví dụ user đang xem dashboard, chuyển qua Slack 5 phút, quay lại thì data cập nhật mới nhất. Nên tắt (`refetchOnWindowFocus: false`) khi: (1) data ít thay đổi (config, static content), (2) refetch gây UX tệ (reset scroll, flash loading), (3) API có rate limit. Có thể set global qua `defaultOptions` hoặc per-query. Tương tự có `refetchOnReconnect` (khi mạng khôi phục) và `refetchInterval` (polling định kỳ).",
    q_en: "What is refetchOnWindowFocus in React Query? When should you disable it?",
    a_en: "refetchOnWindowFocus is a default behavior: when the user switches tabs and comes back, React Query automatically refetches all stale queries. Purpose: ensures data stays fresh — e.g., user is viewing a dashboard, switches to Slack for 5 minutes, comes back, and sees the latest data. Disable it (`refetchOnWindowFocus: false`) when: (1) data rarely changes (config, static content), (2) refetching causes poor UX (scroll reset, loading flash), (3) the API has rate limits. Can be set globally via `defaultOptions` or per-query. Similarly, `refetchOnReconnect` (when network recovers) and `refetchInterval` (polling) follow the same pattern.",
  },
  {
    id: 634,
    category: "State Management",
    subcategory: "React Query",
    level: "advanced",
    q: "Prefetching trong React Query hoạt động như thế nào?",
    a: "Prefetching là kỹ thuật fetch data trước khi user cần, giúp loại bỏ loading state. Client-side pattern: prefetch on hover để khi user click vào link thì data đã sẵn sàng — `const handleMouseEnter = () => queryClient.prefetchQuery({ queryKey: ['post', id], queryFn: () => fetchPost(id), staleTime: 30_000 })`. Server-side với Next.js App Router: `const queryClient = new QueryClient(); await queryClient.prefetchQuery({ queryKey: ['posts'], queryFn: fetchPosts }); const dehydratedState = dehydrate(queryClient); return <HydrationBoundary state={dehydratedState}><PostList /></HydrationBoundary>` — client nhận data đã fetch sẵn, không có loading flash. `prefetchInfiniteQuery` cho infinite scroll pages: pre-load trang đầu tiên trên server, user thấy content ngay. Prefetch trong route handler (Next.js): đặt prefetch logic trong layout.tsx để data sẵn sàng trước khi page.tsx render. Pitfall: prefetchQuery không throw error (khác fetchQuery), query silently fails — luôn check error state ở component level.",
    q_en: "How does prefetching work in React Query?",
    a_en: "Prefetching fetches data before the user needs it, eliminating loading states. Client-side pattern: prefetch on hover so data is ready when the user clicks — `const handleMouseEnter = () => queryClient.prefetchQuery({ queryKey: ['post', id], queryFn: () => fetchPost(id), staleTime: 30_000 })`. Server-side with Next.js App Router: `const queryClient = new QueryClient(); await queryClient.prefetchQuery({ queryKey: ['posts'], queryFn: fetchPosts }); const dehydratedState = dehydrate(queryClient); return <HydrationBoundary state={dehydratedState}><PostList /></HydrationBoundary>` — the client receives already-fetched data with no loading flash. `prefetchInfiniteQuery` for infinite scroll: pre-load the first page on the server so content appears immediately. Prefetch in route handlers (Next.js): put prefetch logic in layout.tsx so data is ready before page.tsx renders. Pitfall: prefetchQuery does not throw errors (unlike fetchQuery) — queries silently fail; always check error state at the component level.",
  },
  {
    id: 635,
    category: "State Management",
    subcategory: "React Query",
    level: "advanced",
    q: "Infinite queries trong React Query là gì? Dùng cho pagination như thế nào?",
    a: "`useInfiniteQuery` là hook chuyên biệt cho infinite scroll và load-more pagination — thay vì fetch 1 page, nó quản lý nhiều pages trong 1 query. Cấu hình: `queryFn` nhận `pageParam` (cursor hoặc page number), `getNextPageParam` trả về param cho page tiếp theo (return undefined khi hết data). Kết quả: `data.pages` là mảng chứa data từng page, `data.pageParams` lưu params đã dùng. Gọi `fetchNextPage()` khi user scroll tới cuối (dùng IntersectionObserver). Ví dụ: social feed, product listing, chat history. Có thêm `fetchPreviousPage()` cho bi-directional infinite scroll.",
    q_en: "What are infinite queries in React Query? How do you use them for pagination?",
    a_en: "`useInfiniteQuery` is a specialized hook for infinite scroll and load-more pagination — instead of fetching one page, it manages multiple pages in a single query. Config: `queryFn` receives `pageParam` (a cursor or page number), `getNextPageParam` returns the param for the next page (return undefined when there is no more data). Result: `data.pages` is an array of data per page, `data.pageParams` stores the params used. Call `fetchNextPage()` when the user scrolls to the bottom (using IntersectionObserver). Use cases: social feeds, product listings, chat history. `fetchPreviousPage()` is also available for bi-directional infinite scroll.",
  },
  {
    id: 636,
    category: "State Management",
    subcategory: "React Query",
    level: "advanced",
    q: "Dependent queries trong React Query là gì?",
    a: "Dependent query (hay serial query) là query mà việc thực thi phụ thuộc vào kết quả của query khác. Dùng `enabled` option để kiểm soát: ví dụ fetch user trước, rồi mới fetch posts của user đó — `useQuery({ queryKey: ['posts', userId], queryFn: () => fetchPosts(userId), enabled: !!userId })`. Khi userId chưa có (query user chưa xong), enabled = false nên posts query không chạy. Khi userId có giá trị, enabled = true → tự động fetch. React Query handle loading states đúng cho cả chain: component thấy isLoading = true cho tới khi query cuối cùng hoàn thành. Tránh lạm dụng dependent queries vì tạo waterfall — nếu queries độc lập, chạy parallel tốt hơn.",
    q_en: "What are dependent queries in React Query?",
    a_en: "A dependent query (or serial query) is a query whose execution depends on the result of another query. Use the `enabled` option to control it: e.g., fetch user first, then fetch that user's posts — `useQuery({ queryKey: ['posts', userId], queryFn: () => fetchPosts(userId), enabled: !!userId })`. When userId is not yet available (user query not complete), enabled is false so the posts query doesn't run. When userId has a value, enabled becomes true → automatically fetches. React Query handles loading states correctly for the entire chain: the component sees isLoading = true until the last query in the chain completes. Avoid overusing dependent queries as they create waterfalls — if queries are independent, run them in parallel instead.",
  },
  {
    id: 637,
    category: "State Management",
    subcategory: "React Query",
    level: "intermediate",
    q: "Cách xử lý error trong React Query?",
    a: "React Query v5 thay đổi error handling so với v4: `onError` callback bị deprecated, thay bằng `throwOnError` hoặc `meta`. Retry strategies: mặc định retry 3 lần, exponential backoff — override: `retry: (failureCount, error) => error.status !== 401 && failureCount < 3` để không retry auth errors. Global error handling với QueryCache observer: `new QueryClient({ queryCache: new QueryCache({ onError: (error, query) => { if (query.meta?.showErrorToast) toast.error(error.message) } }) })` — per-query opt-in: `useQuery({ ..., meta: { showErrorToast: true } })`. Error Boundary integration: `throwOnError: true` khiến query throw error lên Error Boundary thay vì return `isError`. Hoặc dùng `useSuspenseQuery` — tự động throw lên Error Boundary. `error.message` vs `error.response.data`: với fetch API, HTTP errors không tự throw — phải check `if (!res.ok) throw new Error(res.statusText)` trong queryFn. Pitfall v5: `onError` ở useMutation vẫn hoạt động nhưng global `onError` trong defaultOptions đã bị xóa.",
    q_en: "How do you handle errors in React Query?",
    a_en: "React Query v5 changed error handling compared to v4: the `onError` callback is deprecated, replaced by `throwOnError` or `meta`. Retry strategies: default 3 retries with exponential backoff — override: `retry: (failureCount, error) => error.status !== 401 && failureCount < 3` to skip retrying auth errors. Global error handling with QueryCache observer: `new QueryClient({ queryCache: new QueryCache({ onError: (error, query) => { if (query.meta?.showErrorToast) toast.error(error.message) } }) })` — per-query opt-in: `useQuery({ ..., meta: { showErrorToast: true } })`. Error Boundary integration: `throwOnError: true` causes the query to throw up to the Error Boundary instead of returning `isError`. Or use `useSuspenseQuery` — automatically throws to the Error Boundary. `error.message` vs `error.response.data`: with the fetch API, HTTP errors do not throw automatically — you must check `if (!res.ok) throw new Error(res.statusText)` in queryFn. v5 pitfall: `onError` in useMutation still works but global `onError` in defaultOptions has been removed.",
  },
  {
    id: 638,
    category: "State Management",
    subcategory: "React Query",
    level: "intermediate",
    q: "QueryKey trong React Query nên được thiết kế như thế nào?",
    a: "QueryKey design là nền tảng của cache architecture. Rules: (1) Include ALL dependencies — bất cứ biến nào dùng trong queryFn phải có trong key: `['todos', userId, { status, page, sort }]`. (2) Dùng array, không dùng string thuần — array cho phép fuzzy matching khi invalidate. (3) Hierarchy từ general đến specific: `['todos']` → `['todos', todoId]` → `['todos', todoId, 'comments']`. Factory pattern (best practice): `const todoKeys = { all: ['todos'] as const, lists: () => [...todoKeys.all, 'list'] as const, list: (filters) => [...todoKeys.lists(), filters] as const, detail: (id) => [...todoKeys.all, 'detail', id] as const }` — dùng: `useQuery({ queryKey: todoKeys.detail(id) })`, invalidate: `queryClient.invalidateQueries({ queryKey: todoKeys.lists() })`. Fuzzy matching rules: `invalidateQueries({ queryKey: ['todos'] })` invalidates tất cả keys bắt đầu bằng 'todos' — cả `['todos', 1]` lẫn `['todos', { status: 'active' }]`. Serialization: React Query serialize key thành stable string cho cache lookup — objects được serialized theo key order.",
    q_en: "How should QueryKeys be designed in React Query?",
    a_en: "QueryKey design is the foundation of cache architecture. Rules: (1) Include ALL dependencies — any variable used in queryFn must be in the key: `['todos', userId, { status, page, sort }]`. (2) Use arrays, not plain strings — arrays allow fuzzy matching when invalidating. (3) Structure from general to specific: `['todos']` → `['todos', todoId]` → `['todos', todoId, 'comments']`. Factory pattern (best practice): `const todoKeys = { all: ['todos'] as const, lists: () => [...todoKeys.all, 'list'] as const, list: (filters) => [...todoKeys.lists(), filters] as const, detail: (id) => [...todoKeys.all, 'detail', id] as const }` — usage: `useQuery({ queryKey: todoKeys.detail(id) })`, invalidate: `queryClient.invalidateQueries({ queryKey: todoKeys.lists() })`. Fuzzy matching: `invalidateQueries({ queryKey: ['todos'] })` invalidates all keys starting with 'todos' — both `['todos', 1]` and `['todos', { status: 'active' }]`. Serialization: React Query serializes keys into a stable string for cache lookup — objects are serialized in key order.",
  },
  {
    id: 639,
    category: "State Management",
    subcategory: "React Query",
    level: "advanced",
    q: "Optimistic updates trong React Query thực hiện như thế nào?",
    a: "Optimistic update flow đầy đủ: khi user thực hiện action, update UI ngay lập tức trước khi API confirm, rollback nếu API fail. Implementation: `onMutate: async (newTodo) => { await queryClient.cancelQueries({ queryKey: ['todos'] }); const snapshot = queryClient.getQueryData(['todos']); queryClient.setQueryData(['todos'], (old) => [...old, { ...newTodo, id: 'temp-' + Date.now() }]); return { snapshot } }`. `onError: (err, newTodo, context) => { queryClient.setQueryData(['todos'], context.snapshot) }`. `onSettled: () => { queryClient.invalidateQueries({ queryKey: ['todos'] }) }`. Quan trọng: `cancelQueries` trước khi optimistic update — tránh race condition khi background refetch overwrite optimistic state. Context pattern: onMutate return value trở thành `context` trong onError/onSettled. Khi nào dùng optimistic vs simple invalidation: optimistic cho actions user expect instant feedback (like, follow, toggle), simple invalidation cho actions phức tạp hơn (payment, create order) vì rollback UX tệ hơn là loading nhỏ. Pitfall: optimistic ID ('temp-123') cần được replace bằng real ID sau khi server confirm — onSettled invalidate để refetch với real data.",
    q_en: "How do you implement optimistic updates in React Query?",
    a_en: "Full optimistic update flow: when the user performs an action, update the UI immediately before the API confirms, then rollback if the API fails. Implementation: `onMutate: async (newTodo) => { await queryClient.cancelQueries({ queryKey: ['todos'] }); const snapshot = queryClient.getQueryData(['todos']); queryClient.setQueryData(['todos'], (old) => [...old, { ...newTodo, id: 'temp-' + Date.now() }]); return { snapshot } }`. `onError: (err, newTodo, context) => { queryClient.setQueryData(['todos'], context.snapshot) }`. `onSettled: () => { queryClient.invalidateQueries({ queryKey: ['todos'] }) }`. Important: `cancelQueries` before the optimistic update — prevents race conditions where a background refetch overwrites the optimistic state. Context pattern: the onMutate return value becomes `context` in onError/onSettled. When to use optimistic vs simple invalidation: optimistic for actions where users expect instant feedback (like, follow, toggle); simple invalidation for complex actions (payment, creating an order) because rollback UX is worse than a small loading indicator. Pitfall: optimistic IDs ('temp-123') must be replaced with real IDs after server confirmation — onSettled invalidates to refetch with real data.",
  },
  {
    id: 640,
    category: "State Management",
    subcategory: "React Query",
    level: "advanced",
    q: "Cách kết hợp React Query với Next.js Server Components?",
    a: "Next.js App Router pattern với React Query: Server Components fetch data và dehydrate cache, Client Components nhận hydrated cache — không có loading flash. Pattern đầy đủ: (1) Tạo `getQueryClient()` factory với `cache()` từ React để mỗi request server có QueryClient riêng (tránh data leak giữa users). (2) Server Component: `const queryClient = getQueryClient(); await queryClient.prefetchQuery({ queryKey: ['posts'], queryFn: fetchPosts }); return <HydrationBoundary state={dehydrate(queryClient)}><PostList /></HydrationBoundary>`. (3) Client Component dùng `useQuery` bình thường — nhận data từ hydrated cache ngay, không request thêm nếu data còn fresh. Streaming hydration: có thể dùng với Suspense để stream từng phần page. RSC considerations: không thể dùng React Query hooks trong RSC (chỉ là async functions) — hooks chỉ trong Client Components. Nên prefetch ở server những gì quan trọng cho initial render, lazy load phần ít quan trọng hơn ở client. Pitfall: nếu không dùng `cache()` cho QueryClient factory, server-side QueryClient sẽ được share giữa requests → data leaking.",
    q_en: "How do you combine React Query with Next.js Server Components?",
    a_en: "Next.js App Router pattern with React Query: Server Components fetch data and dehydrate the cache; Client Components receive the hydrated cache — no loading flash. Full pattern: (1) Create a `getQueryClient()` factory using React's `cache()` so each server request gets its own QueryClient (prevents data leaking between users). (2) Server Component: `const queryClient = getQueryClient(); await queryClient.prefetchQuery({ queryKey: ['posts'], queryFn: fetchPosts }); return <HydrationBoundary state={dehydrate(queryClient)}><PostList /></HydrationBoundary>`. (3) Client Component uses `useQuery` normally — receives data from the hydrated cache immediately, with no additional request if data is still fresh. Streaming hydration: works with Suspense to stream page sections. RSC considerations: React Query hooks cannot be used in RSCs (they are async functions only) — hooks are Client Component only. Prefetch on the server what matters for initial render; lazy load less critical parts on the client. Pitfall: if you don't use `cache()` for the QueryClient factory, a single server-side QueryClient will be shared across all requests → data leaking between users.",
  },

  // === STATE MANAGEMENT: React Query Nâng Cao (1056-1070) ===
  {
    id: 1056,
    category: "State Management",
    subcategory: "React Query",
    level: "beginner",
    q: "QueryClientProvider là gì? Tại sao cần wrap app bằng nó?",
    a: "QueryClientProvider là React Context Provider cung cấp QueryClient instance cho toàn bộ component tree. QueryClient là 'bộ não' quản lý cache, chứa tất cả cached data, query states, và config. Mọi `useQuery`/`useMutation` bên trong đều cần access QueryClient. Setup: tạo `const queryClient = new QueryClient()` BÊN NGOÀI component (tránh re-create mỗi render), wrap app bằng `<QueryClientProvider client={queryClient}>`. Có thể cấu hình defaultOptions cho tất cả queries: `new QueryClient({ defaultOptions: { queries: { staleTime: 60_000, retry: 2 } } })`. Trong Next.js App Router, đặt ở root layout nhưng cần 'use client' wrapper.",
    q_en: "What is QueryClientProvider and how is it configured in React Query?",
    a_en: "QueryClientProvider is the React context provider that makes the QueryClient instance available to all components in the tree. Setup: create a QueryClient instance (configure defaultOptions like staleTime, gcTime, retry behavior); wrap the root component with <QueryClientProvider client={queryClient}>. The QueryClient manages the query cache, default options, and global state. In Next.js App Router: create the QueryClient inside a Client Component ('use client') to avoid sharing state between requests on the server — a common mistake is creating QueryClient at module level. Best practice: use useState or useRef to ensure the QueryClient is only created once per component lifecycle, not recreated on every render. Each user session should have its own QueryClient instance for proper isolation.",
  },
  {
    id: 1057,
    category: "State Management",
    subcategory: "React Query",
    level: "beginner",
    q: "Sự khác biệt giữa isLoading và isFetching trong useQuery?",
    a: "Đây là câu phỏng vấn rất phổ biến. **isLoading** (hay `status === 'pending'`): true CHỈ khi fetch lần đầu VÀ chưa có cached data — dùng để show loading skeleton/spinner toàn trang. **isFetching**: true MỌI KHI đang fetch, kể cả background refetch khi đã có cached data — dùng để show subtle indicator (spinning icon nhỏ, progress bar mờ). Ví dụ thực tế: user vào trang Products lần đầu → isLoading=true, show skeleton. User navigate đi rồi quay lại → có cached data nên isLoading=false (show data cũ ngay), nhưng isFetching=true (đang refetch background). Thêm `isRefetching` = isFetching && !isLoading (đang refetch có data cũ).",
    q_en: "What is the difference between isLoading and isFetching in React Query?",
    a_en: "isLoading is true only during the initial fetch when there is no cached data yet — the query is in the 'loading' state. It transitions to false once data is first received. isFetching is true whenever a fetch is in progress — including background refetches, refetches on window focus, and invalidations after mutations. It stays true during any network request for that query. Practical difference: isLoading is used to show a loading skeleton on initial render; isFetching is used to show a loading indicator during background updates. Example: if data is cached and stale, navigating back shows the stale data immediately (isLoading is false) while React Query fetches fresh data in the background (isFetching is true). In v5, isPending replaces isLoading for new semantics: isPending is true when there is no cached data regardless of whether a fetch is in progress.",
  },
  {
    id: 1058,
    category: "State Management",
    subcategory: "React Query",
    level: "intermediate",
    q: "Placeholder data và initial data trong React Query khác gì nhau?",
    a: "Hai option này phục vụ mục đích khác nhau. `placeholderData`: data tạm thời để fill UI trong khi fetch, KHÔNG được lưu vào cache, không có timestamp, `isPlaceholderData: true` để component biết đang hiển thị placeholder. Dùng khi: muốn show skeleton data có structure (ví dụ array 5 empty items), hoặc `placeholderData: (prev) => prev` để giữ data page trước khi chuyển page (keepPreviousData pattern). `initialData`: được treat như real cached data — có `dataUpdatedAt`, ảnh hưởng staleTime (nếu initialData đủ fresh thì không refetch ngay). Dùng khi: data đã có từ nguồn khác (ví dụ đã fetch list, giờ dùng item từ list làm initialData cho detail query: `initialData: () => queryClient.getQueryData(['todos'])?.find(t => t.id === id)`, `initialDataUpdatedAt: () => queryClient.getQueryDataState(['todos'])?.dataUpdatedAt`). Pitfall: dùng sai `initialData` với hardcoded value sẽ ngăn query refetch vì React Query nghĩ data còn fresh — luôn kết hợp với `initialDataUpdatedAt: 0` nếu muốn force refetch.",
    q_en: "What is placeholder data and initial data in React Query and how do they differ?",
    a_en: "Both provide data to display before the actual fetch completes, but they behave differently: initialData — treated as real cached data; cached to disk if persistQueryClient is used; marks query as stale (triggers background refetch if staleTime has passed); suitable for data pre-fetched on the server (SSR). placeholderData — treated as fake/temporary data; NOT persisted to cache; query still shows as loading (isPlaceholderData is true); replaced by real data when fetch completes; suitable for skeleton/placeholder UI where you want to show the query as still loading. In Next.js with SSR: use initialData with dehydrate/HydrationBoundary to pass server-fetched data to the client. Use placeholderData: keepPreviousData (or previousData function) when paginating to avoid blank states between page transitions.",
  },
  {
    id: 1059,
    category: "State Management",
    subcategory: "React Query",
    level: "intermediate",
    q: "select option trong useQuery dùng để làm gì?",
    a: "`select` là transform function chạy sau khi queryFn trả về data, trước khi component nhận — không ảnh hưởng gì đến cache (cache vẫn lưu raw data). Re-render optimization: React Query memoize select result — component chỉ re-render khi `select(data)` trả về giá trị khác (dùng structural equality). Ví dụ: nhiều components dùng cùng query `['todos']` nhưng mỗi component có `select` khác nhau — `select: d => d.filter(t => !t.done)` (incomplete) và `select: d => d.filter(t => t.done)` (complete) — component nào chỉ re-render khi phần data của nó thay đổi. Patterns thực tế: (1) Pick fields: `select: d => ({ name: d.name, avatar: d.avatar })` giảm re-render khi các field khác thay đổi. (2) Sort/filter: `select: d => [...d].sort((a,b) => b.date - a.date)`. (3) Normalize: `select: d => Object.fromEntries(d.map(i => [i.id, i]))`. Pitfall: nếu `select` là inline arrow function, memoization không hoạt động vì function reference thay đổi mỗi render — stabilize bằng useCallback hoặc định nghĩa outside component.",
    q_en: "What is the select option in useQuery and what are its use cases?",
    a_en: "The select option is a transformation function that runs on the fetched data before it is stored in the component's state. It does not affect the cached data — only what the component receives. Use cases: Data transformation — reshape API response to the format the component needs without modifying the cache; Data filtering — select only a subset of data; Computing derived values — calculate totals, sort lists; Performance optimization — when the select function is memoized, the component only re-renders when the selected data changes (not when other parts of the cache change). Best practice: memoize select functions with useCallback or define them as stable references to prevent unnecessary re-renders. Example: const { data: userNames } = useQuery({ queryKey: ['users'], queryFn: fetchUsers, select: (data) => data.map(u => u.name) })",
  },
  {
    id: 1060,
    category: "State Management",
    subcategory: "React Query",
    level: "intermediate",
    q: "Parallel queries và useQueries trong React Query là gì?",
    a: "Parallel queries là kỹ thuật fetch nhiều API endpoints cùng lúc thay vì tuần tự (waterfall). Cách 1: gọi nhiều `useQuery` hooks trong cùng component — React Query tự động chạy chúng parallel. Cách 2: `useQueries` hook nhận mảng query configs, hữu ích khi số lượng queries dynamic (ví dụ fetch details cho danh sách product IDs). Kết quả trả về mảng objects, mỗi object giống useQuery result. Ví dụ: dashboard cần user info + notifications + stats → 3 useQuery hooks chạy song song, tổng thời gian = query chậm nhất (thay vì cộng dồn). Lưu ý: React Suspense mode sẽ waterfall nếu mỗi query ở component khác nhau — dùng `useSuspenseQueries` để parallel trong Suspense.",
    q_en: "How do you run parallel queries in React Query?",
    a_en: "Multiple useQuery calls in the same component run in parallel automatically — React Query fires all queries simultaneously without waiting for each to complete. For a static number of parallel queries: simply call multiple useQuery hooks; they run concurrently. For a dynamic number of parallel queries (array of unknown length): use useQueries — accepts an array of query options and returns an array of query results. Example: const results = useQueries({ queries: ids.map(id => ({ queryKey: ['user', id], queryFn: () => fetchUser(id) })) }). useQueries is more flexible than a .map() with useQuery because React hooks cannot be called inside loops. For dependent parallel queries (some queries depend on others): use the enabled option to control when a query starts — enabled: !!userId fires only after userId is available.",
  },
  {
    id: 1061,
    category: "State Management",
    subcategory: "React Query",
    level: "intermediate",
    q: "Cách cancel queries trong React Query khi component unmount?",
    a: "React Query cung cấp `signal` (AbortSignal) qua queryFn context — pass signal vào fetch để enable cancellation: `queryFn: ({ signal }) => fetch(\`/api/users/${id}\`, { signal })`. Khi component unmount hoặc queryKey thay đổi, React Query tự động abort signal → browser hủy pending request. Với axios: `queryFn: ({ signal }) => axios.get(\`/api/users/${id}\`, { signal })` — axios v0.22+ support AbortSignal trực tiếp. Custom fetch wrapper: `async function fetchWithCancel(url, { signal }) { const res = await fetch(url, { signal }); if (!res.ok) throw new Error(res.statusText); return res.json() }`. Cancellation xảy ra khi: (1) component unmount trước khi fetch xong (user navigate đi), (2) queryKey thay đổi nhanh (search input) — request cũ bị cancel khi key mới, (3) `queryClient.cancelQueries()` được gọi manual (thường trong optimistic update onMutate). Pitfall: nếu queryFn không dùng signal, query vẫn complete dù component unmount — data được cache nhưng component đã gone, React Query tự discard result. Không dùng signal gây lãng phí bandwidth nhưng không gây bug.",
    q_en: "How do you cancel a query in React Query?",
    a_en: "React Query v5 automatically cancels in-flight queries when: the component unmounts, the query key changes, or a new fetch is triggered while one is already running. Manual cancellation is achieved via AbortSignal: the queryFn receives a signal in its context object that fires when React Query cancels the query. In the fetch API: queryFn: ({ signal }) => fetch(url, { signal }). In axios: queryFn: ({ signal }) => axios.get(url, { signal }). React Query v5 uses AbortController internally and passes the signal automatically. For imperative cancellation: queryClient.cancelQueries({ queryKey: ['todos'] }) — cancels all ongoing fetches for matching queries and removes them from the in-flight state. Proper cancellation prevents race conditions and unnecessary network requests when users navigate quickly.",
  },
  {
    id: 1062,
    category: "State Management",
    subcategory: "React Query",
    level: "advanced",
    q: "Query Filters trong React Query là gì? Dùng ở đâu?",
    a: "Query Filters là pattern matching system cho phép target nhóm queries khi thao tác hàng loạt. Dùng trong `invalidateQueries`, `refetchQueries`, `removeQueries`, `cancelQueries`. Filter options: `queryKey` (fuzzy match — `['todos']` match cả `['todos', 1]`), `type` ('active' = đang có subscriber, 'inactive' = cached nhưng không ai dùng, 'all'), `stale` (true/false), `fetchStatus` ('fetching'/'paused'/'idle'). Ví dụ thực tế: user logout → `queryClient.removeQueries({ type: 'all' })` xóa toàn bộ cache. Hoặc: mutation thành công → `invalidateQueries({ queryKey: ['todos'], type: 'active' })` chỉ refetch todos đang hiển thị.",
    q_en: "What are Query Filters in React Query?",
    a_en: "Query Filters are objects used to target specific queries when calling methods like invalidateQueries, cancelQueries, removeQueries, and refetchQueries. Filter properties: queryKey — partial or exact key matching; exact: true — only match queries with exactly this key; type — 'active' (has observers), 'inactive' (no observers), 'all'; stale — filter by stale state; predicate — function that receives a Query object and returns boolean for full custom filtering. Examples: queryClient.invalidateQueries({ queryKey: ['todos'] }) — invalidates all queries whose key starts with 'todos'; queryClient.invalidateQueries({ queryKey: ['todos'], exact: true }) — only invalidates ['todos'] exactly; queryClient.invalidateQueries({ type: 'active' }) — only refetches queries currently being observed. Filters are essential for targeted cache management after mutations.",
  },
  {
    id: 1063,
    category: "State Management",
    subcategory: "React Query",
    level: "advanced",
    q: "useSuspenseQuery khác gì useQuery? Khi nào dùng?",
    a: "`useSuspenseQuery` tích hợp React Suspense — thay vì return `isLoading`/`isError`, nó throw Promise (cho Suspense boundary bắt) hoặc throw Error (cho Error Boundary bắt). Kết quả: `data` luôn defined (TypeScript happy), không cần if/else cho loading/error states. Code gọn hơn: component chỉ chứa happy path. Suspense boundary ở parent xử lý loading, ErrorBoundary xử lý error. Khi nào dùng: project đã adopt Suspense pattern, muốn tách loading UI khỏi component logic. Khi nào KHÔNG dùng: cần kiểm soát loading state chi tiết, cần show partial data trong khi fetch. Lưu ý: nhiều useSuspenseQuery trong 1 component sẽ waterfall — dùng `useSuspenseQueries` để parallel.",
    q_en: "What is useSuspenseQuery and when should you use it?",
    a_en: "useSuspenseQuery is a variant of useQuery that integrates with React Suspense. Instead of returning isLoading, it throws a Promise when data is not yet available, letting React Suspense boundaries catch it and show a fallback. Benefits: cleaner component code — no need to handle loading/error states inline; works with React's concurrent features; consistent with server-side data loading patterns. Usage: wrap components using useSuspenseQuery with <Suspense fallback={<Skeleton />}>; for error states, use an ErrorBoundary. In Next.js App Router: works naturally with Server Components and streaming. Limitations: requires all data to be in the same Suspense boundary to avoid waterfall loading (use Promise.all or parallel queries); does not work with queries that have enabled: false. Best for: components where data is essential and must be available before rendering.",
  },
  {
    id: 1064,
    category: "State Management",
    subcategory: "React Query",
    level: "advanced",
    q: "persistQueryClient trong React Query dùng để làm gì?",
    a: "persistQueryClient cho phép serialize toàn bộ React Query cache vào storage, restore khi user reload → data hiển thị ngay lập tức không cần fetch lại. Setup: `import { persistQueryClient } from '@tanstack/react-query-persist-client'; import { createSyncStoragePersister } from '@tanstack/query-sync-storage-persister'`. `const persister = createSyncStoragePersister({ storage: window.localStorage }); persistQueryClient({ queryClient, persister, maxAge: 1000 * 60 * 60 * 24 })` — maxAge là thời gian data được phép restore (default 24h). Với React Native dùng `createAsyncStoragePersister` cho AsyncStorage. IndexedDB persister cho data lớn hơn localStorage limit (5MB). Khi app load: nếu persisted data còn trong maxAge, hydrate vào cache ngay → queries show cached data, background refetch nếu stale. `buster` option để invalidate toàn bộ persisted cache khi deploy version mới (thay đổi cấu trúc data). Pitfall: không persist sensitive data (tokens, personal info) vì localStorage không mã hóa. Dùng `dehydrateOptions.shouldDehydrateQuery` để filter queries nào được persist.",
    q_en: "What is persistQueryClient and when should you use it?",
    a_en: "persistQueryClient is a React Query plugin that persists the query cache to external storage (localStorage, IndexedDB, AsyncStorage) so cached data survives page refreshes. Setup: use createSyncStoragePersister (localStorage) or createAsyncStoragePersister (IndexedDB, AsyncStorage); wrap with PersistQueryClientProvider or call persistQueryClient() directly. Use cases: offline support — show cached data when offline; faster perceived performance — show stale cached data immediately on revisit while refetching in background; PWAs — combine with Service Workers for full offline capability. Configuration: maxAge — how long to keep persisted cache (default 24 hours); buster — cache version key for invalidating old persisted data on app updates. Important: sensitive data should not be persisted to localStorage — use secure storage or encrypt before persisting.",
  },
  {
    id: 1065,
    category: "State Management",
    subcategory: "React Query",
    level: "advanced",
    q: "Mutation side effects: onMutate, onSuccess, onError, onSettled hoạt động ra sao?",
    a: "useMutation cung cấp 4 lifecycle callbacks, có thể set ở cả mutation level và per-mutate() call level (per-call override mutation level). `onMutate(variables)`: chạy synchronously trước khi mutation request gửi đi — dùng để optimistic update cache, return value trở thành `context` trong onError/onSettled. Nếu return Promise, mutation chờ resolve. `onSuccess(data, variables, context)`: chạy khi API thành công với response data — thường dùng để invalidate related queries: `queryClient.invalidateQueries({ queryKey: ['todos'] })` hoặc setQueryData với response. `onError(error, variables, context)`: chạy khi API fail — context từ onMutate dùng để rollback: `queryClient.setQueryData(['todos'], context.previousTodos)`. `onSettled(data, error, variables, context)`: luôn chạy sau onSuccess hoặc onError (như finally) — an toàn để invalidate queries ở đây vì chạy cả khi success lẫn error. Thứ tự: onMutate → [mutationFn] → onSuccess/onError → onSettled. Pitfall: nếu cả mutation-level và call-level đều có onSuccess, cả hai đều chạy — mutation-level chạy trước.",
    q_en: "What are the mutation lifecycle callbacks in React Query?",
    a_en: "useMutation provides lifecycle callbacks at both the mutation level and per-call level: onMutate — fires before the mutation function; receives the variables; used for optimistic updates (update cache optimistically, return rollback context); onSuccess — fires when mutation succeeds; receives data, variables, and context; typically used to invalidate related queries; onError — fires when mutation fails; receives error, variables, and context (from onMutate); used to rollback optimistic updates; onSettled — fires on both success and error; used for cleanup. Two levels of callbacks: mutation-level (defined in useMutation options — run for all invocations); call-level (passed to mutate() or mutateAsync() — run only for that specific call). Execution order: onMutate → mutation function → onSuccess/onError → onSettled (at both levels, mutation-level runs before call-level).",
  },
  {
    id: 1066,
    category: "State Management",
    subcategory: "React Query",
    level: "intermediate",
    q: "keepPreviousData (placeholderData với identity function) dùng khi nào?",
    a: "keepPreviousData là v3/v4 API, trong v5 thay bằng `placeholderData: (previousData) => previousData` (identity function). Khi queryKey thay đổi (page 1 → page 2), thay vì show loading + clear data cũ, React Query giữ nguyên data của page 1 trong khi fetch page 2 — `isPlaceholderData: true` cho biết đang hiển thị data cũ. UX impact lớn với pagination: user thấy page 1 vẫn hiển thị (có thể mờ đi để indicate loading), page 2 load xong thì replace — không có flash trắng. Kết hợp với prefetch để UX hoàn hảo: `useEffect(() => { if (!isPlaceholderData && hasNextPage) { queryClient.prefetchQuery({ queryKey: ['todos', page + 1], queryFn: () => fetchTodos(page + 1) }) } }, [data, page, isPlaceholderData, queryClient])`. Cũng hữu ích với filter/search: khi user gõ query search, giữ previous results trong khi tìm kiếm mới → không flash empty. Pitfall: nếu dùng `select` transform, previousData cũng đã qua select — types match đúng.",
    q_en: "What is keepPreviousData (placeholderData with previous data) in React Query?",
    a_en: "keepPreviousData (v4) / placeholderData: keepPreviousData (v5) maintains the previous data on screen while new data is being fetched — preventing blank/loading states during transitions. Most useful for: Pagination — when changing pages, show current page data while next page loads instead of a blank state; Filtering — when applying filters, show current results while filtered results load; Any scenario where query key changes trigger a new fetch. In v5: placeholderData: (previousData) => previousData — receives the previous data as the first argument; isPlaceholderData is true while showing old data and fetching new. Example: useQuery({ queryKey: ['todos', page], queryFn: () => fetchTodos(page), placeholderData: keepPreviousData }) — import keepPreviousData from @tanstack/react-query. Combine with prefetching next page for the smoothest pagination experience.",
  },
  {
    id: 1067,
    category: "State Management",
    subcategory: "React Query",
    level: "advanced",
    q: "React Query DevTools là gì? Cách sử dụng?",
    a: "React Query DevTools (`@tanstack/react-query-devtools`) là công cụ debug chuyên dụng, hiển thị floating panel trong browser. Tính năng: xem tất cả queries và trạng thái real-time (fresh/stale/inactive/fetching), inspect cached data dạng JSON tree, xem query timing (khi nào fetch, bao lâu), manual trigger refetch/invalidate/remove/reset từng query, xem subscribers count. Setup: `import { ReactQueryDevtools } from '@tanstack/react-query-devtools'` → đặt `<ReactQueryDevtools initialIsOpen={false} />` bên trong QueryClientProvider. Tự động ẩn trong production build (tree-shaken). Rất hữu ích khi debug cache issues: tại sao data stale, query nào đang fetch, cache hit hay miss. Là 1 trong những lý do developer chọn React Query — visibility vào cache layer mà Redux DevTools không cho.",
    q_en: "What is React Query DevTools and how does it help development?",
    a_en: "React Query DevTools is a development tool that provides real-time visibility into the query cache state. Features: Query inspector — see all active queries with their keys, status (fresh, stale, fetching, inactive), data, and error; Cache explorer — browse the full cache contents; Manual controls — manually trigger refetches, invalidations, or remove queries; Query timeline — see when queries were fetched and when they will refetch. Setup: import { ReactQueryDevtools } from '@tanstack/react-query-devtools'; add <ReactQueryDevtools initialIsOpen={false} /> inside the QueryClientProvider. The DevTools are automatically excluded from production builds when NODE_ENV === 'production'. Best practices: keep DevTools in the layout component; use the floating button for quick access; inspect staleTime and gcTime to tune caching behavior. Essential for debugging cache invalidation issues and understanding query lifecycles.",
  },
  {
    id: 1068,
    category: "State Management",
    subcategory: "React Query",
    level: "advanced",
    q: "Cách xử lý race conditions trong React Query?",
    a: "Race condition xảy ra khi nhiều requests cùng lúc và response muộn hơn override response mới hơn — vấn đề kinh điển với useEffect + fetch. React Query giải quyết ở nhiều tầng: (1) Cancellation: khi queryKey thay đổi (ví dụ search input), React Query abort request cũ nếu queryFn nhận và dùng signal — `queryFn: ({ signal }) => fetch(url, { signal })`. (2) Deduplication: nếu query đang fetch và component khác request cùng queryKey, React Query không gửi thêm request mà share kết quả — chỉ 1 request. (3) Strict mode safety: React Query không commit kết quả của request đã bị replace bởi request mới hơn. (4) `enabled` pattern cho dependent queries: `enabled: !!userId` đảm bảo không fetch với stale userId. Comparison với useEffect: với useEffect bạn phải manually track và cancel với cleanup function `return () => { cancelled = true }` hoặc AbortController — dễ leak và dễ có bugs. React Query làm tất cả điều này tự động miễn là queryFn sử dụng signal.",
    q_en: "How does React Query handle race conditions?",
    a_en: "Race conditions occur when multiple requests are in flight and responses arrive out of order. React Query handles this automatically in several ways: Automatic cancellation — when a query refetches (key change, window focus), the previous in-flight request is cancelled via AbortSignal, so only the latest response updates the cache; Deduplication — if multiple components subscribe to the same query simultaneously, only one network request is made; Stale closure prevention — React Query's internal state management ensures the latest response always wins even without AbortSignal support from the fetcher. For mutations: use mutateAsync with proper error handling; avoid multiple simultaneous mutations on the same resource. Optimistic updates with onMutate/onError rollback pattern handles the case where a mutation fails after the UI was already updated optimistically.",
  },
  {
    id: 1069,
    category: "State Management",
    subcategory: "React Query",
    level: "intermediate",
    q: "enabled option trong useQuery dùng để làm gì?",
    a: "`enabled` option kiểm soát khi nào query được phép chạy — là boolean hoặc function trả về boolean. Khi `enabled: false`, query ở trạng thái 'disabled': không auto-fetch, status là 'pending' với `fetchStatus: 'idle'` (không phải loading). Khi enabled chuyển từ false sang true, query tự động fetch ngay. Use cases: (1) Dependent queries — chỉ fetch khi dependency sẵn sàng: `enabled: !!userId && !!orgId`. (2) Lazy queries — chỉ fetch khi user trigger: `const [enabled, setEnabled] = useState(false)` → button onClick: `setEnabled(true)`. (3) Conditional skip — skip query dựa trên route hay feature flag. (4) Pause queries khi offline: `enabled: isOnline`. Pattern với TypeScript: khi `enabled: !!userId`, TypeScript vẫn thấy `userId` có thể undefined trong queryFn — workaround: `queryFn: () => fetchUser(userId!)` hoặc `queryFn: () => fetchUser(userId as string)`. Pitfall: `enabled: someExpensiveCheck()` gọi mỗi render — wrap trong useMemo nếu check tốn kém. isLoading với disabled query: `isLoading` = true nếu enabled=false và chưa có data (status=pending) — check `fetchStatus === 'idle'` để phân biệt disabled vs loading.",
    q_en: "What is the enabled option in useQuery and what are its use cases?",
    a_en: "The enabled option controls whether a query should automatically run. When enabled is false, the query is in a 'disabled' state — it does not fetch, does not refetch on window focus, and does not retry on error. Use cases: Dependent queries — wait for a prerequisite value before fetching: enabled: !!userId fetches only after userId is defined; Conditional fetching — fetch only when certain conditions are met: enabled: isLoggedIn; Lazy queries — controlled by user action (search queries triggered by button click); Form-driven queries — fetch based on valid form state. Disabled queries still show cached data if available. To trigger a disabled query manually: const { refetch } = useQuery({ ..., enabled: false }); call refetch() on user action. In v5, enabled: false combined with placeholderData can show previous results while the query is paused.",
  },
  {
    id: 1070,
    category: "State Management",
    subcategory: "React Query",
    level: "advanced",
    q: "So sánh React Query với SWR? Khi nào chọn cái nào?",
    a: "So sánh chi tiết React Query (TanStack Query v5) vs SWR v2: Bundle size — SWR ~4KB, React Query ~13KB (nhưng features nhiều hơn 3x). API philosophy — SWR ưu tiên simplicity và convention, React Query ưu tiên control và flexibility. Mutations: React Query có `useMutation` hook đầy đủ (onMutate, onSuccess, onError, onSettled, optimistic updates), SWR không có mutation hook — thường dùng thư viện khác hoặc tự viết. DevTools: React Query có DevTools panel chi tiết, SWR không có DevTools chính thức. Infinite queries: cả hai có, nhưng React Query có `getNextPageParam` + `getPreviousPageParam` linh hoạt hơn. Prefetching + SSR: React Query có dehydrate/HydrationBoundary pattern rõ ràng hơn, SWR có `fallback` prop đơn giản hơn. Cache key: React Query dùng array (type-safe, fuzzy match), SWR dùng string/function. Offline support: React Query có `networkMode` option, SWR basic. Kết luận: chọn SWR khi app chủ yếu read-only data fetching, team muốn đơn giản, bundle size quan trọng, dùng nhiều với Next.js pages router. Chọn React Query khi cần mutations phức tạp, optimistic updates, DevTools cho debugging, team cần full-featured solution.",
    q_en: "What are the key differences between React Query and SWR?",
    a_en: "React Query (TanStack Query) and SWR are both data-fetching libraries for React, but with different philosophies: React Query: more feature-rich — mutations with lifecycle callbacks (onMutate, onSuccess, onError), optimistic updates, query invalidation, infinite queries, useQueries; granular cache control (staleTime, gcTime, query filters); Suspense support; DevTools; better for complex applications with many mutations and cache dependencies. SWR: simpler API, smaller bundle size; created by Vercel, integrates naturally with Next.js; good for read-heavy applications with simpler caching needs; supports Suspense; mutate() is simpler but less powerful than React Query mutations. When to choose: React Query for complex apps with many mutations, optimistic updates, and sophisticated cache management; SWR for simpler apps, smaller bundle requirements, or when deep Next.js integration is a priority.",
  },

  // === STATE MANAGEMENT: Recoil & Jotai (641-650) ===
  {
    id: 641,
    category: "State Management",
    subcategory: "Recoil",
    level: "intermediate",
    q: "Recoil là gì? Khái niệm atom trong Recoil là gì?",
    a: "Recoil là thư viện atomic state management từ Meta, thiết kế để giải quyết vấn đề fine-grained reactivity mà Redux và Context API không làm tốt. Atom là đơn vị state nhỏ nhất, độc lập, tương tự useState nhưng có thể chia sẻ giữa nhiều component. Ví dụ: `const darkModeAtom = atom({ key: 'darkMode', default: false })` — component nào dùng `useRecoilState(darkModeAtom)` chỉ re-render khi atom đó thay đổi, không bị ảnh hưởng bởi atoms khác. Đây là ưu điểm lớn so với Redux store monolithic hay Context làm re-render toàn cây. Tuy nhiên Recoil vẫn đang beta và ít được adopt hơn Zustand hay Jotai trong projects mới.",
    q_en: "What is Recoil? What is the concept of an atom in Recoil?",
    a_en: "Recoil is an atomic state management library from Meta, designed to solve fine-grained reactivity problems that Redux and the Context API handle poorly. An atom is the smallest independent unit of state, similar to useState but shareable across multiple components. Example: `const darkModeAtom = atom({ key: 'darkMode', default: false })` — any component using `useRecoilState(darkModeAtom)` re-renders only when that specific atom changes, unaffected by other atoms. This is a major advantage over a monolithic Redux store or Context which re-renders the entire tree. However, Recoil is still in beta and has seen less adoption than Zustand or Jotai in new projects.",
  },
  {
    id: 642,
    category: "State Management",
    subcategory: "Recoil",
    level: "intermediate",
    q: "Selector trong Recoil là gì? Khác gì với atom?",
    a: "Selector là derived state — pure function tính toán từ atoms/selectors khác, tự động memoize và chỉ re-compute khi dependencies thay đổi (dependency graph tự động). Ví dụ: `const totalPriceSelector = selector({ key: 'totalPrice', get: ({get}) => get(cartAtom).reduce((sum, item) => sum + item.price, 0) })`. Khác atom ở chỗ: không có initial value, không ghi trực tiếp (ngoại trừ writeable selector dùng thêm `set` function). Async selector: trả về Promise trong `get`, component tự suspend — `const userSelector = selector({ key: 'user', get: async ({get}) => await fetchUser(get(userIdAtom)) })`. Về memoization: Recoil giữ cache theo tổ hợp dependency values, giống Reselect nhưng không cần khai báo thủ công input selectors. Khác Redux selectors (Reselect) ở chỗ tự track dependencies và support async natively, còn Reselect chỉ sync và phải liệt kê inputs.",
    q_en: "What is a selector in Recoil? How does it differ from an atom?",
    a_en: "A selector is derived state — a pure function that computes from atoms or other selectors, automatically memoized and recomputed only when its dependencies change (the dependency graph is tracked automatically). Example: `const totalPriceSelector = selector({ key: 'totalPrice', get: ({get}) => get(cartAtom).reduce((sum, item) => sum + item.price, 0) })`. Unlike atoms: no initial value, no direct writes (except writeable selectors using an additional `set` function). Async selectors: return a Promise in `get`, the component suspends automatically — `const userSelector = selector({ key: 'user', get: async ({get}) => await fetchUser(get(userIdAtom)) })`. On memoization: Recoil caches by dependency value combinations, like Reselect but without manually declaring input selectors, and supports async natively. Difference from Redux selectors (Reselect): Recoil auto-tracks dependencies and supports async; Reselect is sync-only and requires explicitly listing inputs.",
  },
  {
    id: 643,
    category: "State Management",
    subcategory: "Recoil",
    level: "intermediate",
    q: "Cách đọc và ghi atom trong Recoil component?",
    a: "Ba hooks chính với performance implications khác nhau: `useRecoilState(atom)` trả về `[value, setter]` — component subscribe vào atom, re-render mỗi khi value thay đổi (dùng khi cần cả đọc lẫn ghi trong cùng component). `useRecoilValue(atom)` chỉ đọc — cũng subscribe và re-render khi value thay đổi, dùng cho display-only components. `useSetRecoilState(atom)` chỉ trả về setter function, KHÔNG subscribe vào atom — component sẽ KHÔNG re-render khi atom thay đổi, đây là điểm quan trọng về performance: ví dụ nút 'Add to Cart' chỉ cần ghi vào cartAtom mà không cần đọc giá trị nên dùng `useSetRecoilState` để tránh re-render không cần thiết. `useResetRecoilState(atom)` reset về defaultValue. Subscription model: mỗi atom có danh sách subscribers, khi atom thay đổi chỉ notify đúng những components đang subscribe, không gây re-render waterfall như Context.",
    q_en: "How do you read and write atoms in Recoil components?",
    a_en: "Three main hooks with different performance implications: `useRecoilState(atom)` returns `[value, setter]` — component subscribes to the atom and re-renders on every change (use when you need both read and write in the same component). `useRecoilValue(atom)` read-only — also subscribes and re-renders on change; use for display-only components. `useSetRecoilState(atom)` returns only the setter, does NOT subscribe to the atom — the component will NOT re-render when the atom changes. This is the key performance point: for example, an 'Add to Cart' button only needs to write to cartAtom without reading its value, so `useSetRecoilState` avoids unnecessary re-renders. `useResetRecoilState(atom)` resets to the defaultValue. Subscription model: each atom keeps a list of subscribers; when the atom changes, only the subscribing components are notified — no waterfall re-renders like with Context.",
  },
  {
    id: 644,
    category: "State Management",
    subcategory: "Jotai",
    level: "intermediate",
    q: "Jotai là gì? So sánh với Recoil?",
    a: "Jotai là thư viện atomic state management tương tự Recoil nhưng nhỏ gọn hơn (~3KB vs ~21KB) và API đơn giản hơn đáng kể. Điểm khác biệt chính: Jotai không yêu cầu `key` string duy nhất cho mỗi atom (Recoil bắt buộc, dễ gây conflict trong large codebase), không cần `RecoilRoot` wrapper (Jotai dùng WeakMap nên hoạt động không cần Provider). Cú pháp gần với useState hơn: `const countAtom = atom(0)`, trong component: `const [count, setCount] = useAtom(countAtom)`. Jotai cũng hỗ trợ derived atoms, async atoms với Suspense, và có thể dùng `atomWithStorage` để persist. Trong thực tế 2024-2026, Jotai được adopt nhiều hơn Recoil vì Recoil vẫn chưa ra stable release và Meta giảm investment.",
    q_en: "What is Jotai? How does it compare to Recoil?",
    a_en: "Jotai is an atomic state management library similar to Recoil but much smaller (~3KB vs ~21KB) with a significantly simpler API. Key differences: Jotai does not require a unique `key` string for each atom (Recoil requires it and collisions are a real issue in large codebases), and no `RecoilRoot` wrapper is needed (Jotai uses a WeakMap so it works without a Provider). The syntax is closer to useState: `const countAtom = atom(0)`, in a component: `const [count, setCount] = useAtom(countAtom)`. Jotai also supports derived atoms, async atoms with Suspense, and `atomWithStorage` for persistence. In practice through 2024-2026, Jotai has seen more adoption than Recoil because Recoil still hasn't shipped a stable release and Meta's investment has declined.",
  },
  {
    id: 645,
    category: "State Management",
    subcategory: "Jotai",
    level: "intermediate",
    q: "Cách tạo và sử dụng atom trong Jotai?",
    a: "Primitive atom: `const countAtom = atom(0)` — trong component dùng `useAtom(countAtom)` trả về `[value, setValue]`. Derived read-only: `const doubleAtom = atom(get => get(countAtom) * 2)` — dùng `useAtomValue`. Derived read-write: `atom(get => get(baseAtom), (get, set, newVal) => set(baseAtom, newVal * 2))`. `atomWithStorage('key', defaultVal)` từ `jotai/utils` tự đồng bộ localStorage, handle SSR hydration. `atomWithDefault(get => get(otherAtom))` tạo atom có thể override nhưng mặc định derive. Architecture Provider-less: Jotai dùng WeakMap lưu state theo atom object reference thay vì string key như Recoil — atoms tự nhiên unique, hỗ trợ code-split, không lo duplicate key. `useAtomValue` (chỉ đọc) và `useSetAtom` (chỉ ghi, không subscribe, không re-render) là hooks tối ưu performance.",
    q_en: "How do you create and use atoms in Jotai?",
    a_en: "Primitive atom: `const countAtom = atom(0)` — use `useAtom(countAtom)` in a component, returns `[value, setValue]`. Read-only derived: `const doubleAtom = atom(get => get(countAtom) * 2)` — use `useAtomValue`. Read-write derived: `atom(get => get(baseAtom), (get, set, newVal) => set(baseAtom, newVal * 2))`. `atomWithStorage('key', defaultVal)` from `jotai/utils` auto-syncs to localStorage and handles SSR hydration. `atomWithDefault(get => get(otherAtom))` creates an atom that can be overridden but defaults to a derived value. Provider-less architecture: Jotai uses a WeakMap to store state keyed by atom object reference instead of a string key like Recoil — atoms are naturally unique, support code-splitting, and have no risk of duplicate key collisions. `useAtomValue` (read-only) and `useSetAtom` (write-only, no subscription, no re-render) are the performance-optimized hooks.",
  },
  {
    id: 646,
    category: "State Management",
    subcategory: "Jotai",
    level: "advanced",
    q: "Async atoms trong Jotai hoạt động như thế nào?",
    a: "Async read atom: `const userAtom = atom(async (get) => { const id = get(userIdAtom); return await fetchUser(id); })` — component dùng `useAtomValue(userAtom)` sẽ tự suspend trong khi fetch (cần bọc `<Suspense>`). Đây là tích hợp với React Suspense tự nhiên nhất trong các state libs. `loadable(userAtom)` từ `jotai/utils` trả về `{ state: 'loading'|'hasData'|'hasError', data, error }` — tránh Suspense khi cần kiểm soát loading UI thủ công. `atomWithQuery` từ `jotai-tanstack-query` tích hợp TanStack Query vào atom: `const postsAtom = atomWithQuery(() => ({ queryKey: ['posts'], queryFn: fetchPosts }))`. Pitfall: async atom re-fetch mỗi khi dependency atom thay đổi, không có built-in stale-time như React Query — dùng `atomWithQuery` nếu cần caching strategy phức tạp.",
    q_en: "How do async atoms work in Jotai?",
    a_en: "Async read atom: `const userAtom = atom(async (get) => { const id = get(userIdAtom); return await fetchUser(id); })` — a component using `useAtomValue(userAtom)` will automatically suspend while fetching (requires wrapping with `<Suspense>`). This is the most natural React Suspense integration of any state library. `loadable(userAtom)` from `jotai/utils` returns `{ state: 'loading'|'hasData'|'hasError', data, error }` — avoids Suspense when you need manual control over the loading UI. `atomWithQuery` from `jotai-tanstack-query` integrates TanStack Query into an atom: `const postsAtom = atomWithQuery(() => ({ queryKey: ['posts'], queryFn: fetchPosts }))`. Pitfall: an async atom refetches whenever a dependency atom changes; there is no built-in stale-time like React Query — use `atomWithQuery` when you need a more sophisticated caching strategy.",
  },
  {
    id: 647,
    category: "State Management",
    subcategory: "Recoil",
    level: "advanced",
    q: "AtomFamily và selectorFamily trong Recoil là gì?",
    a: "atomFamily (Recoil): `const todoItemAtom = atomFamily({ key: 'TodoItem', default: id => ({ id, text: '', done: false }) })` — mỗi id tạo một atom độc lập: `useRecoilState(todoItemAtom(42))`. selectorFamily tương tự cho selectors có tham số: `const filteredTodosSelector = selectorFamily({ key: 'FilteredTodos', get: (filter) => ({get}) => get(todoListAtom).filter(t => t.status === filter) })`. Serialization của params: params phải serializable (string/number/plain object), vì Recoil dùng `JSON.stringify` để tạo cache key — object params phải có thứ tự properties nhất quán. Memory management: atoms trong family không bị GC tự động khi component unmount — trong app lớn cần dùng `useRecoilCallback` để cleanup hoặc dùng `effects` để reset. Jotai tương đương: `atomFamily` từ `jotai/utils`, đơn giản hơn và params có thể dùng Map nên linh hoạt hơn.",
    q_en: "What are atomFamily and selectorFamily in Recoil?",
    a_en: "atomFamily (Recoil): `const todoItemAtom = atomFamily({ key: 'TodoItem', default: id => ({ id, text: '', done: false }) })` — each id creates an independent atom: `useRecoilState(todoItemAtom(42))`. selectorFamily is similar for parameterized selectors: `const filteredTodosSelector = selectorFamily({ key: 'FilteredTodos', get: (filter) => ({get}) => get(todoListAtom).filter(t => t.status === filter) })`. Param serialization: params must be serializable (string/number/plain object) because Recoil uses `JSON.stringify` to create a cache key — object params must have consistent property ordering. Memory management: atoms in a family are not automatically garbage collected when a component unmounts — in large apps, use `useRecoilCallback` for cleanup or `effects` to reset. Jotai equivalent: `atomFamily` from `jotai/utils`, simpler, and params can use a Map so more flexible.",
  },
  {
    id: 648,
    category: "State Management",
    subcategory: "Recoil",
    level: "advanced",
    q: "So sánh bundle size và performance giữa Recoil, Jotai và Zustand?",
    a: "Bundle size (minified+gzipped): Jotai ~3KB, Zustand ~1KB (core) đến ~8KB với middleware, Recoil ~21KB. Re-render characteristics: tất cả đều fine-grained — chỉ re-render component subscribe đúng atom/slice thay đổi, khác Context API re-render cả cây. Jotai vs Zustand về perf: Jotai atomic model tốt cho graph-shaped state với nhiều derived values, Zustand tốt hơn khi cần batch updates và ít derived state. Memory: Recoil giữ cache selector theo dependency combinations — large selector graphs có thể tốn memory. Tree-shaking: Jotai và Zustand tree-shakeable tốt (ESM-first), Recoil ít hơn do internal atom registry global. Benchmark thực tế (js-framework-benchmark): cả ba tương đương nhau về raw render speed, bottleneck thường là React reconciliation chứ không phải state library. Khuyến nghị 2025: Zustand cho simplicity+tiny bundle, Jotai cho fine-grained atomic state, tránh Recoil cho project mới.",
    q_en: "Compare bundle size and performance between Recoil, Jotai, and Zustand.",
    a_en: "Bundle size (minified + gzipped): Jotai ~3KB, Zustand ~1KB (core) to ~8KB with middleware, Recoil ~21KB. Re-render characteristics: all three are fine-grained — only the components subscribed to the changed atom/slice re-render, unlike Context API which re-renders the whole tree. Jotai vs Zustand on performance: Jotai's atomic model is better for graph-shaped state with many derived values; Zustand is better when batch updates are needed and derived state is minimal. Memory: Recoil caches selectors by dependency value combinations — large selector graphs can consume significant memory. Tree-shaking: Jotai and Zustand tree-shake well (ESM-first); Recoil less so due to its global atom registry. Real-world benchmark (js-framework-benchmark): all three are equivalent in raw render speed; the bottleneck is usually React reconciliation, not the state library. Recommendation for 2025: Zustand for simplicity and tiny bundle, Jotai for fine-grained atomic state, avoid Recoil for new projects.",
  },
  {
    id: 649,
    category: "State Management",
    subcategory: "Jotai",
    level: "intermediate",
    q: "Jotai Provider là gì? Khi nào cần dùng?",
    a: "Provider-less (mặc định): Jotai dùng global WeakMap store — mọi component trong app share cùng atom values mà không cần wrap Provider. Khi nào cần Provider: (1) Testing — `<Provider store={createStore()}>` tạo isolated store mỗi test, tránh state leak giữa tests; (2) Micro-frontend — mỗi app widget có store riêng; (3) Reset toàn bộ atoms khi component unmount (ví dụ modal dialog). Store API: `const myStore = createStore(); myStore.get(atom); myStore.set(atom, value); myStore.sub(atom, callback)` — dùng để interact với atoms ngoài React (WebSocket handlers, analytics). Tradeoff: Provider-less tiện nhưng dễ gây test pollution nếu không reset; with-Provider an toàn hơn cho testing nhưng cần boilerplate. `<Provider store={store}>` cũng là cách implement DevTools hoặc persist toàn bộ store state.",
    q_en: "What is a Jotai Provider? When do you need to use it?",
    a_en: "Provider-less (default): Jotai uses a global WeakMap store — all components in the app share atom values without wrapping a Provider. When you DO need a Provider: (1) Testing — `<Provider store={createStore()}>` creates an isolated store per test, preventing state leaking between tests; (2) Micro-frontends — each app widget has its own store; (3) Resetting all atoms when a component unmounts (e.g., a modal dialog). Store API: `const myStore = createStore(); myStore.get(atom); myStore.set(atom, value); myStore.sub(atom, callback)` — useful for interacting with atoms outside React (WebSocket handlers, analytics). Trade-off: Provider-less is convenient but can cause test pollution if not reset; with-Provider is safer for testing but requires boilerplate. `<Provider store={store}>` is also the pattern for implementing DevTools or persisting the entire store state.",
  },
  {
    id: 650,
    category: "State Management",
    subcategory: "Recoil",
    level: "advanced",
    q: "Khi nào nên dùng atomic state management (Recoil/Jotai) thay vì Redux/Zustand?",
    a: "Decision matrix — dùng Atomic (Jotai/Recoil) khi: (1) State có cấu trúc graph, nhiều derived values từ base atoms (ví dụ: cart total, filtered lists, computed stats đều derive từ cùng nguồn); (2) Cần fine-grained updates — 1000 items list, mỗi item là 1 atom, chỉ item thay đổi mới re-render; (3) State không nhất thiết phải centralized — features khác nhau sở hữu atoms riêng; (4) Cần co-locate state với components sử dụng nó. Dùng Redux/Zustand khi: cần single source of truth có thể serialize/rehydrate, cần time-travel debugging, team quen với flux pattern, cần middleware pipeline (logging, side effects). Real use cases Atomic: collaborative editor (mỗi doc element là atom), form builder (mỗi field là atom), dashboard với nhiều widgets độc lập. Pitfall: atomic state khó debug hơn khi dependencies phức tạp — không có Redux DevTools tương đương để xem toàn bộ state graph.",
    q_en: "When should you use atomic state management (Recoil/Jotai) instead of Redux/Zustand?",
    a_en: "Decision matrix — use Atomic (Jotai/Recoil) when: (1) State has a graph structure with many derived values from base atoms (e.g., cart total, filtered lists, computed stats all deriving from the same source); (2) Fine-grained updates are needed — a list of 1,000 items where each item is its own atom, so only the changed item re-renders; (3) State doesn't need to be centralized — different features own their own atoms; (4) You want to co-locate state with the components that use it. Use Redux/Zustand when: you need a serializable/rehydratable single source of truth, time-travel debugging, the team is comfortable with Flux patterns, or you need a middleware pipeline (logging, side effects). Real atomic use cases: collaborative editors (each document element is an atom), form builders (each field is an atom), dashboards with many independent widgets. Pitfall: atomic state is harder to debug when dependencies are complex — there's no Redux DevTools equivalent to visualize the entire state graph.",
  },

  // === NODE.JS: Cơ Bản (651-660) ===
  {
    id: 651,
    category: "Node.js",
    subcategory: "Cơ Bản",
    level: "beginner",
    q: "Node.js là gì? Điểm khác biệt với JavaScript trên trình duyệt?",
    a: "Node.js là runtime environment cho phép chạy JavaScript trên server, được xây dựng trên V8 engine của Chrome và thư viện C++ libuv xử lý async I/O. Khác biệt cốt lõi với browser JS: không có DOM, window, document, localStorage — thay vào đó có `fs` (đọc/ghi file), `http` (tạo server), `path`, `os`, `crypto` và hàng nghìn npm packages. Ví dụ thực tế: `const fs = require('fs'); fs.readFile('data.json', 'utf8', (err, data) => console.log(data))` — đây là code không thể chạy trong browser. Node.js dùng event loop single-threaded nên xử lý hàng nghìn concurrent connections hiệu quả với I/O-heavy workloads như API server, real-time chat. Không phù hợp cho CPU-intensive tasks như video encoding vì block event loop.",
    q_en: "What is Node.js? How does it differ from JavaScript in the browser?",
    a_en: "Node.js is a runtime environment that allows JavaScript to run on the server, built on Chrome's V8 engine and the C++ library libuv for async I/O. Core differences from browser JS: no DOM, window, document, or localStorage — instead it provides `fs` (file read/write), `http` (create servers), `path`, `os`, `crypto`, and thousands of npm packages. Practical example: `const fs = require('fs'); fs.readFile('data.json', 'utf8', (err, data) => console.log(data))` — this code cannot run in a browser. Node.js uses a single-threaded event loop, making it efficient at handling thousands of concurrent connections for I/O-heavy workloads like API servers and real-time chat. It is not suitable for CPU-intensive tasks like video encoding because those block the event loop.",
  },
  {
    id: 652,
    category: "Node.js",
    subcategory: "Cơ Bản",
    level: "beginner",
    q: "V8 engine là gì? Vai trò của nó trong Node.js?",
    a: "V8 là JavaScript engine mã nguồn mở của Google viết bằng C++, được dùng trong Chrome và là trái tim của Node.js. Điểm khác biệt quan trọng: V8 không interpret JS từng dòng mà dùng JIT (Just-In-Time) compilation — biên dịch JS thành native machine code ngay lúc chạy, cho tốc độ nhanh gần bằng C++. Node.js nhúng V8 vào C++ runtime và bổ sung thêm libuv (async I/O), các built-in modules (fs, http, crypto...) để tạo thành môi trường chạy JS hoàn chỉnh ngoài browser. Thực tế ảnh hưởng đến dev: V8 có hidden class optimization — object có cùng shape (thứ tự properties) sẽ dùng chung internal class và chạy nhanh hơn, nên tránh thêm properties động vào object sau khi tạo trong hot paths.",
    q_en: "What is the V8 engine? What role does it play in Node.js?",
    a_en: "V8 is Google's open-source JavaScript engine written in C++, used in Chrome and at the heart of Node.js. The key distinction: V8 does not interpret JS line by line — it uses JIT (Just-In-Time) compilation, compiling JS into native machine code at runtime, achieving speeds close to C++. Node.js embeds V8 into a C++ runtime and adds libuv (async I/O) and built-in modules (fs, http, crypto...) to create a complete JS execution environment outside the browser. Practical impact for developers: V8 uses hidden class optimization — objects with the same shape (same property order) share an internal class and run faster. Avoid dynamically adding properties to objects after creation in hot code paths.",
  },
  {
    id: 653,
    category: "Node.js",
    subcategory: "Cơ Bản",
    level: "beginner",
    q: "Event-driven architecture trong Node.js nghĩa là gì?",
    a: "Event-driven architecture nghĩa là code phản ứng với events thay vì chạy tuần tự từ trên xuống. Trong Node.js, thay vì blocking chờ đợi I/O xong, bạn đăng ký callback và Node.js tiếp tục xử lý việc khác — khi I/O hoàn thành, event được emit và callback được gọi. Ví dụ cụ thể: `server.on('request', (req, res) => {...})` — server không blocking chờ từng request mà lắng nghe event 'request' liên tục. Điều này cho phép một thread đơn xử lý hàng nghìn connections đồng thời vì hầu hết thời gian Node.js không làm gì cả, chỉ chờ I/O. Pitfall: nếu bạn chạy heavy computation đồng bộ (vòng lặp triệu lần), event loop bị block và mọi requests khác phải chờ — đây là lý do Node.js không phù hợp cho CPU-intensive work.",
    q_en: "What does event-driven architecture mean in Node.js?",
    a_en: "Event-driven architecture means code reacts to events rather than executing top-to-bottom sequentially. In Node.js, instead of blocking while waiting for I/O to complete, you register a callback and Node.js continues processing other work — when the I/O finishes, an event is emitted and the callback is invoked. Concrete example: `server.on('request', (req, res) => {...})` — the server doesn't block waiting for each request; it continuously listens for 'request' events. This allows a single thread to handle thousands of concurrent connections because most of the time Node.js is doing nothing — just waiting on I/O. Pitfall: running heavy synchronous computation (a loop with millions of iterations) blocks the event loop and all other requests must wait — this is why Node.js is not suited for CPU-intensive work.",
  },
  {
    id: 654,
    category: "Node.js",
    subcategory: "Cơ Bản",
    level: "beginner",
    q: "Non-blocking I/O trong Node.js là gì? Tại sao quan trọng?",
    a: "Non-blocking I/O hoạt động nhờ libuv delegate I/O operations cho OS kernel hoặc thread pool: khi gọi `fs.readFile()`, Node.js đăng ký callback rồi trả control về event loop ngay lập tức, OS xử lý I/O ở background, khi xong thêm callback vào event queue để event loop xử lý. So với blocking model Apache (thread-per-request): Apache cấp 1 thread/request, thread block khi chờ DB query — 1000 concurrent requests cần 1000 threads (~1GB RAM). Node.js single-threaded xử lý 1000 requests trên 1 thread vì hầu hết thời gian chờ I/O là idle. Throughput thực tế: Node.js thường đạt 10k-50k req/s cho I/O-heavy API, so với Apache ~1k-5k req/s. Khi nào blocking xảy ra accidentaly: `fs.readFileSync()`, `JSON.parse(largeData)`, vòng lặp tính toán nặng, `crypto.pbkdf2Sync()` — tất cả đều block event loop và làm tất cả requests khác phải chờ. Solution: Worker Threads cho CPU-intensive, luôn dùng async variants.",
    q_en: "What is non-blocking I/O in Node.js? Why is it important?",
    a_en: "Non-blocking I/O works because libuv delegates I/O operations to the OS kernel or a thread pool: when `fs.readFile()` is called, Node.js registers a callback and immediately returns control to the event loop; the OS handles I/O in the background, and when done adds the callback to the event queue for the event loop to process. Compared to Apache's blocking model (thread-per-request): Apache allocates one thread per request, and threads block while waiting for a DB query — 1,000 concurrent requests require 1,000 threads (~1GB RAM). Node.js single-threaded handles 1,000 requests on one thread because most of the time waiting on I/O is idle. Real-world throughput: Node.js typically reaches 10k-50k req/s for I/O-heavy APIs, vs Apache's ~1k-5k req/s. When blocking happens accidentally: `fs.readFileSync()`, `JSON.parse(largeData)`, heavy computation loops, `crypto.pbkdf2Sync()` — all block the event loop and force all other requests to wait. Solution: use Worker Threads for CPU-intensive work and always use async variants.",
  },
  {
    id: 655,
    category: "Node.js",
    subcategory: "Cơ Bản",
    level: "beginner",
    q: "npm là gì? Phân biệt npm install, npm install --save-dev?",
    a: "npm (Node Package Manager) là công cụ quản lý packages mặc định đi kèm Node.js, với registry hơn 2 triệu packages. Phân biệt quan trọng: `npm install react` cài vào `dependencies` — những gì cần thiết để app chạy trên production. `npm install --save-dev jest typescript eslint` cài vào `devDependencies` — chỉ cần trong quá trình development, không được bundle vào production build. Ví dụ thực tế: khi deploy lên server, chạy `npm install --production` sẽ bỏ qua devDependencies, giảm đáng kể dung lượng `node_modules`. Pitfall hay gặp: cài nhầm package production vào devDependencies (app crash trên server) hoặc ngược lại (bloat production bundle). Ngoài npm còn có pnpm (nhanh hơn, tiết kiệm disk) và yarn đang được dùng phổ biến.",
    q_en: "What is npm? What is the difference between npm install and npm install --save-dev?",
    a_en: "npm (Node Package Manager) is the default package manager bundled with Node.js, with a registry of over 2 million packages. Key distinction: `npm install react` installs to `dependencies` — things the app needs to run in production. `npm install --save-dev jest typescript eslint` installs to `devDependencies` — only needed during development, not bundled into the production build. Practical example: when deploying to a server, running `npm install --production` skips devDependencies, significantly reducing `node_modules` size. Common pitfall: installing a production package into devDependencies (app crashes on server) or vice versa (bloated production bundle). Besides npm, pnpm (faster, disk-efficient) and Yarn are also widely used.",
  },
  {
    id: 656,
    category: "Node.js",
    subcategory: "Cơ Bản",
    level: "beginner",
    q: "package.json là gì? Các trường quan trọng trong package.json?",
    a: "package.json là manifest file của Node.js project. `scripts` hỗ trợ lifecycle hooks: `preinstall`/`postinstall` chạy trước/sau npm install, `prebuild`/`postbuild` bao quanh `build` — dùng để codegen, copy assets. `engines: { node: '>=18.0.0' }` báo CI/CD và người dùng version Node cần thiết, npm cảnh báo nếu không match. Entry points: `main` (CJS fallback), `module` (ESM cho bundlers hỗ trợ), `exports` (field mới nhất — conditional exports theo environment): `{ '.': { import: './dist/index.mjs', require: './dist/index.cjs' } }`. `type: 'module'` đặt default module system là ESM cho tất cả `.js` files. `workspaces: ['packages/*']` cho monorepo — npm install một lần, symlink packages. `peerDependencies` khai báo packages host app phải cung cấp (tránh duplicate React trong component libraries).",
    q_en: "What is package.json? What are its important fields?",
    a_en: "package.json is the manifest file for a Node.js project. `scripts` supports lifecycle hooks: `preinstall`/`postinstall` run before/after npm install, `prebuild`/`postbuild` wrap `build` — used for codegen and copying assets. `engines: { node: '>=18.0.0' }` tells CI/CD and users which Node version is required; npm warns if it doesn't match. Entry points: `main` (CJS fallback), `module` (ESM for supporting bundlers), `exports` (the newest field — conditional exports by environment): `{ '.': { import: './dist/index.mjs', require: './dist/index.cjs' } }`. `type: 'module'` sets the default module system to ESM for all `.js` files. `workspaces: ['packages/*']` for monorepos — single npm install, symlinked packages. `peerDependencies` declares packages the host app must provide (avoids duplicate React instances in component libraries).",
  },
  {
    id: 657,
    category: "Node.js",
    subcategory: "Cơ Bản",
    level: "beginner",
    q: "package-lock.json là gì? Tại sao nên commit file này?",
    a: "package-lock.json lưu exact versions + integrity hashes (sha512) của tất cả packages kể cả transitive dependencies — đảm bảo install reproducible trên mọi máy/CI. `npm install` cập nhật lock file nếu package.json thay đổi; `npm ci` (clean install) xóa node_modules rồi install chính xác từ lock file, không bao giờ update lock — dùng trong CI/CD để đảm bảo deterministic builds. Khi nào xóa và regenerate: khi lock file bị corrupt, sau major Node.js upgrade, hoặc khi muốn update tất cả dependencies lên latest compatible. Integrity field: `'integrity': 'sha512-abc...'` — npm verify hash sau download, phát hiện supply chain attacks (tampered packages). Tương đương: `yarn.lock` (Yarn), `pnpm-lock.yaml` (pnpm) — cùng mục đích nhưng format khác nhau. Quan trọng: KHÔNG commit lock file của library packages lên npm registry (chỉ commit lock của applications).",
    q_en: "What is package-lock.json? Why should you commit this file?",
    a_en: "package-lock.json stores the exact versions and integrity hashes (sha512) of all packages, including transitive dependencies — ensuring reproducible installs across all machines and CI. `npm install` updates the lock file when package.json changes; `npm ci` (clean install) deletes node_modules and installs exactly from the lock file, never updating it — use this in CI/CD for deterministic builds. When to delete and regenerate: when the lock file is corrupted, after a major Node.js upgrade, or when you want to update all dependencies to the latest compatible versions. Integrity field: `'integrity': 'sha512-abc...'` — npm verifies the hash after downloading, detecting supply chain attacks (tampered packages). Equivalents: `yarn.lock` (Yarn), `pnpm-lock.yaml` (pnpm) — same purpose, different format. Important: do NOT commit the lock file of library packages to the npm registry (only commit lock files for applications).",
  },
  {
    id: 658,
    category: "Node.js",
    subcategory: "Cơ Bản",
    level: "beginner",
    q: "CommonJS modules (require/module.exports) và ES Modules (import/export) trong Node.js khác nhau như thế nào?",
    a: "CommonJS (CJS): `require()` load synchronous và blocking — có thể require() ở bất kỳ đâu trong code kể cả trong if/function, module.exports là bất kỳ giá trị gì, không hỗ trợ tree-shaking. ESM: `import` phải ở top-level (static analysis), load asynchronous cho phép parallel fetch, tree-shakeable vì bundlers biết chính xác exports nào được dùng. ESM exclusive features: top-level `await`, import.meta.url, dynamic `import()`. Interop challenges: CJS có thể `require()` ESM file? Không được, phải dùng dynamic `import()`. ESM có thể import CJS? Có, nhưng chỉ default import. Dual package hazard: publish cả CJS lẫn ESM cùng package — nếu app load cả hai, sẽ có 2 instances của module (state, class không share được). Giải pháp: conditional exports trong package.json. `type: 'module'` trong package.json = tất cả `.js` là ESM, CJS phải đổi thành `.cjs`. Node.js 22+ hỗ trợ `--experimental-require-module` để require() ESM.",
    q_en: "How do CommonJS modules (require/module.exports) and ES Modules (import/export) differ in Node.js?",
    a_en: "CommonJS (CJS): `require()` loads synchronously and blocks — can be called anywhere in code including inside if/function blocks; module.exports can be any value; no tree-shaking support. ESM: `import` must be at the top level (static analysis), loads asynchronously enabling parallel fetching, tree-shakeable because bundlers know exactly which exports are used. ESM-exclusive features: top-level `await`, import.meta.url, dynamic `import()`. Interop challenges: can CJS `require()` an ESM file? No — must use dynamic `import()`. Can ESM import CJS? Yes, but only as a default import. Dual package hazard: publishing both CJS and ESM in the same package — if an app loads both, there will be two module instances (state, classes cannot be shared). Solution: conditional exports in package.json. `type: 'module'` in package.json means all `.js` files are ESM; CJS files must be renamed to `.cjs`. Node.js 22+ supports `--experimental-require-module` to `require()` ESM files.",
  },
  {
    id: 659,
    category: "Node.js",
    subcategory: "Cơ Bản",
    level: "intermediate",
    q: "process object trong Node.js cung cấp những gì?",
    a: "process là global object không cần require. `process.env`: chứa environment variables — KHÔNG bao giờ log toàn bộ `process.env` vì chứa secrets; validate với zod/envalid ngay startup để fail fast nếu thiếu. `process.argv`: array `[nodePath, scriptPath, ...userArgs]` — dùng `yargs` hoặc `commander` để parse thay vì thủ công. Signal handling: `process.on('SIGTERM', gracefulShutdown)` — xử lý khi container/PM2 dừng process (close DB connections, finish in-flight requests); `SIGINT` = Ctrl+C. `process.memoryUsage()` trả về `{ rss, heapTotal, heapUsed, external }` — monitor memory leaks bằng cách log định kỳ. `process.hrtime.bigint()` cho high-resolution timestamps (nanoseconds) để benchmark code. `process.exit(1)` exit với error code — quan trọng cho CI pipelines biết lệnh fail. `process.on('uncaughtException')` và `process.on('unhandledRejection')` — log error rồi `process.exit(1)`, không recover vì process state có thể corrupt.",
    q_en: "What does the process object provide in Node.js?",
    a_en: "process is a global object requiring no `require`. `process.env`: contains environment variables — NEVER log the entire `process.env` as it contains secrets; validate with zod/envalid at startup to fail fast if anything is missing. `process.argv`: array `[nodePath, scriptPath, ...userArgs]` — use `yargs` or `commander` to parse instead of doing it manually. Signal handling: `process.on('SIGTERM', gracefulShutdown)` — handles when a container/PM2 stops the process (close DB connections, finish in-flight requests); `SIGINT` = Ctrl+C. `process.memoryUsage()` returns `{ rss, heapTotal, heapUsed, external }` — monitor memory leaks by logging periodically. `process.hrtime.bigint()` for high-resolution timestamps (nanoseconds) to benchmark code. `process.exit(1)` exits with an error code — important for CI pipelines to detect failures. `process.on('uncaughtException')` and `process.on('unhandledRejection')` — log the error then `process.exit(1)`, don't try to recover as process state may be corrupted.",
  },
  {
    id: 660,
    category: "Node.js",
    subcategory: "Cơ Bản",
    level: "intermediate",
    q: "Sự khác biệt giữa __dirname và __filename trong Node.js?",
    a: "`__dirname` cho đường dẫn tuyệt đối đến thư mục chứa file đang chạy, `__filename` cho đường dẫn đến chính file đó. Ví dụ thực tế: `path.join(__dirname, 'templates', 'email.html')` để đọc file template bất kể app được chạy từ thư mục nào — nếu dùng đường dẫn tương đối `'./templates/email.html'` sẽ resolve từ `process.cwd()` (thư mục làm việc hiện tại) và có thể sai khi chạy từ nơi khác. Pitfall quan trọng với ES Modules: `__dirname` và `__filename` không tồn tại trong file `.mjs` hoặc khi `type: module` trong package.json. Thay thế: `import { fileURLToPath } from 'url'; import { dirname } from 'path'; const __filename = fileURLToPath(import.meta.url); const __dirname = dirname(__filename)`.",
    q_en: "What is the difference between __dirname and __filename in Node.js?",
    a_en: "`__dirname` provides the absolute path to the directory containing the currently executing file; `__filename` provides the path to the file itself. Practical example: `path.join(__dirname, 'templates', 'email.html')` reads a template file regardless of which directory the app is launched from — using a relative path `'./templates/email.html'` would resolve from `process.cwd()` (the current working directory) and could be wrong when the app is run from a different location. Important pitfall with ES Modules: `__dirname` and `__filename` do not exist in `.mjs` files or when `type: module` is set in package.json. Replacement: `import { fileURLToPath } from 'url'; import { dirname } from 'path'; const __filename = fileURLToPath(import.meta.url); const __dirname = dirname(__filename)`.",
  },

  // === NODE.JS: Event Loop & Async (661-670) ===
  {
    id: 661,
    category: "Node.js",
    subcategory: "Event Loop",
    level: "intermediate",
    q: "Event loop trong Node.js hoạt động như thế nào?",
    a: "Event loop là vòng lặp liên tục xử lý callbacks theo 6 phases: (1) Timers: thực thi callbacks của `setTimeout`/`setInterval` đã đến hạn; (2) Pending callbacks: I/O errors từ vòng trước; (3) Idle/prepare: internal use; (4) Poll: fetch I/O events mới — nếu queue empty và không có timers pending, block tại đây chờ I/O; (5) Check: `setImmediate` callbacks; (6) Close callbacks: `socket.on('close')`. Giữa MỖI phase, Node.js xử lý toàn bộ microtask queue: Promise callbacks (`then`/`catch`) + `queueMicrotask()` + `process.nextTick()` (nextTick ưu tiên hơn Promise). Starvation: nếu microtask queue không bao giờ empty (Promise resolve tạo Promise mới), event loop không bao giờ qua phase tiếp theo. Ví dụ thực tế execution order: `setTimeout(A, 0)` → `Promise.resolve().then(B)` → `process.nextTick(C)` — thứ tự chạy: C, B, A.",
    q_en: "How does the event loop work in Node.js?",
    a_en: "The event loop is a continuously running cycle that processes callbacks across 6 phases: (1) Timers: executes callbacks from `setTimeout`/`setInterval` that have elapsed; (2) Pending callbacks: I/O errors from the previous cycle; (3) Idle/prepare: internal use; (4) Poll: fetches new I/O events — if the queue is empty and no timers are pending, blocks here waiting for I/O; (5) Check: `setImmediate` callbacks; (6) Close callbacks: `socket.on('close')`. Between EACH phase, Node.js processes the entire microtask queue: Promise callbacks (`then`/`catch`) + `queueMicrotask()` + `process.nextTick()` (nextTick has higher priority than Promises). Starvation: if the microtask queue never empties (a resolved Promise creating new Promises), the event loop never advances to the next phase. Practical execution order example: `setTimeout(A, 0)` → `Promise.resolve().then(B)` → `process.nextTick(C)` — execution order: C, B, A.",
  },
  {
    id: 662,
    category: "Node.js",
    subcategory: "Event Loop",
    level: "intermediate",
    q: "Sự khác biệt giữa setTimeout(fn, 0) và setImmediate() là gì?",
    a: "setTimeout(fn, 0) thực ra là setTimeout(fn, 1) minimum — chạy trong timers phase nếu timer đã expired. setImmediate() chạy trong check phase (ngay sau poll phase). Ngoài I/O context (ví dụ main script): thứ tự giữa hai cái không đảm bảo — phụ thuộc vào system clock resolution và thời gian setup event loop. Trong I/O callback context: setImmediate() LUÔN chạy trước setTimeout(fn, 0) — vì I/O callback chạy ở poll phase, sau poll là check phase (setImmediate), sau đó mới quay lại timers phase. Ví dụ: `fs.readFile(file, () => { setImmediate(() => console.log('immediate')); setTimeout(() => console.log('timeout'), 0); })` — luôn in 'immediate' trước 'timeout'. Practical implication: khi muốn chạy code sau I/O operation hiện tại mà trước timers, dùng setImmediate.",
    q_en: "What is the difference between setTimeout(fn, 0) and setImmediate()?",
    a_en: "setTimeout(fn, 0) is actually setTimeout(fn, 1) minimum — runs in the timers phase when the timer has expired. setImmediate() runs in the check phase (immediately after the poll phase). Outside an I/O context (e.g., in the main script): the order between the two is not guaranteed — depends on system clock resolution and event loop setup time. Inside an I/O callback context: setImmediate() ALWAYS runs before setTimeout(fn, 0) — because I/O callbacks run in the poll phase, and after poll comes the check phase (setImmediate), then timers. Example: `fs.readFile(file, () => { setImmediate(() => console.log('immediate')); setTimeout(() => console.log('timeout'), 0); })` — always prints 'immediate' before 'timeout'. Practical implication: when you want code to run after the current I/O operation but before any timers, use setImmediate.",
  },
  {
    id: 663,
    category: "Node.js",
    subcategory: "Event Loop",
    level: "advanced",
    q: "process.nextTick() khác setImmediate() như thế nào?",
    a: "process.nextTick() không thuộc bất kỳ phase nào của event loop — callbacks được đặt vào nextTick queue và chạy ngay sau synchronous code hiện tại kết thúc, trước khi event loop tiếp tục phase tiếp theo (kể cả trước Promise microtasks). setImmediate() thuộc check phase. Thứ tự ưu tiên: synchronous code → nextTick queue → Promise microtasks → event loop phases (timers → poll → check/setImmediate). Starvation danger: recursive `process.nextTick()` sẽ block event loop mãi mãi — không bao giờ cho I/O callbacks chạy. Node.js docs khuyên dùng `setImmediate()` thay nextTick trong hầu hết trường hợp. Khi nextTick phù hợp: emit event sau constructor return (để listener đăng ký kịp), propagate error asynchronously trong API mà phải consistent async. Khi setImmediate phù hợp: chia nhỏ heavy computation qua nhiều iterations mà vẫn cho I/O xen vào.",
    q_en: "How does process.nextTick() differ from setImmediate()?",
    a_en: "process.nextTick() doesn't belong to any phase of the event loop — callbacks are placed in the nextTick queue and run immediately after the current synchronous code finishes, before the event loop continues to the next phase (even before Promise microtasks). setImmediate() belongs to the check phase. Priority order: synchronous code → nextTick queue → Promise microtasks → event loop phases (timers → poll → check/setImmediate). Starvation danger: recursive `process.nextTick()` will block the event loop forever — I/O callbacks will never run. Node.js docs recommend using `setImmediate()` instead of nextTick in most cases. When nextTick is appropriate: emitting events after a constructor returns (so listeners can register in time), propagating errors asynchronously in APIs that must be consistently async. When setImmediate is appropriate: breaking up heavy computation across multiple iterations while still allowing I/O to interleave.",
  },
  {
    id: 664,
    category: "Node.js",
    subcategory: "Async",
    level: "intermediate",
    q: "Callback hell là gì? Cách giải quyết?",
    a: "Callback hell (pyramid of doom) — ví dụ thực: `fs.readFile('a.txt', (err, data) => { if(err) throw err; db.query(data, (err, rows) => { if(err) throw err; sendEmail(rows[0], (err) => { if(err) throw err; console.log('done') }) }) })` — 3 cấp lồng nhau, error handling lặp lại, logic khó theo dõi. Giải pháp Promise chaining: `readFile('a.txt').then(data => db.query(data)).then(rows => sendEmail(rows[0])).catch(err => console.error(err))` — flat hơn nhưng còn awkward với multi-value. Giải pháp async/await: `try { const data = await readFile('a.txt'); const rows = await db.query(data); await sendEmail(rows[0]); } catch(err) { console.error(err); }` — đọc như synchronous code, try/catch natural. Error propagation khác nhau: callback phải check `err` mỗi cấp thủ công, một missed check = silent failure; Promise/async throw tự động bubble up đến .catch/try-catch. Pitfall async/await: `await` trong vòng lặp `for...of` sẽ chạy sequential — dùng `Promise.all(items.map(item => process(item)))` để parallel.",
    q_en: "What is callback hell? How do you solve it?",
    a_en: "Callback hell (pyramid of doom) — real example: `fs.readFile('a.txt', (err, data) => { if(err) throw err; db.query(data, (err, rows) => { if(err) throw err; sendEmail(rows[0], (err) => { if(err) throw err; console.log('done') }) }) })` — 3 levels of nesting, repeated error handling, logic hard to follow. Promise chaining solution: `readFile('a.txt').then(data => db.query(data)).then(rows => sendEmail(rows[0])).catch(err => console.error(err))` — flatter but still awkward with multi-value passing. async/await solution: `try { const data = await readFile('a.txt'); const rows = await db.query(data); await sendEmail(rows[0]); } catch(err) { console.error(err); }` — reads like synchronous code, natural try/catch. Error propagation differences: callbacks must manually check `err` at every level — one missed check means silent failure; Promise/async throws automatically bubble up to .catch/try-catch. Pitfall: `await` inside a `for...of` loop runs sequentially — use `Promise.all(items.map(item => process(item)))` for parallel execution.",
  },
  {
    id: 665,
    category: "Node.js",
    subcategory: "Async",
    level: "intermediate",
    q: "Promise trong Node.js hoạt động như thế nào? Các states của Promise?",
    a: "Promise là object đại diện cho async operation, có 3 states không thể revert: pending → fulfilled/rejected. Microtask queue: `.then()` callbacks được đặt vào microtask queue (ưu tiên cao hơn setTimeout), chạy sau synchronous code nhưng trước event loop phases. Static methods quan trọng: `Promise.all([p1,p2])` — chờ tất cả resolve, reject ngay khi 1 cái reject (fail-fast); `Promise.allSettled([p1,p2])` — chờ tất cả settle bất kể kết quả, trả về `[{status, value/reason}]` — dùng khi muốn biết kết quả từng cái; `Promise.race([p1,p2])` — resolve/reject theo cái đầu tiên settle (dùng cho timeout pattern); `Promise.any([p1,p2])` — resolve khi 1 cái fulfilled, reject chỉ khi tất cả reject. Error handling chain: nếu không có `.catch()`, unhandled rejection — Node.js 15+ crash process mặc định. `Promise.resolve(value)` tạo fulfilled promise ngay, hữu ích để normalize sync/async APIs. Pitfall: error swallowing trong `.then(onFulfilled)` không có `.catch()` — luôn chain `.catch()` hoặc dùng try/catch với await.",
    q_en: "How do Promises work in Node.js? What are the states of a Promise?",
    a_en: "A Promise is an object representing an async operation with 3 non-reversible states: pending → fulfilled/rejected. Microtask queue: `.then()` callbacks are placed in the microtask queue (higher priority than setTimeout), running after synchronous code but before event loop phases. Important static methods: `Promise.all([p1,p2])` — waits for all to resolve, rejects immediately if any rejects (fail-fast); `Promise.allSettled([p1,p2])` — waits for all to settle regardless of result, returns `[{status, value/reason}]` — use when you want the result of each individual promise; `Promise.race([p1,p2])` — resolves/rejects with the first to settle (use for timeout patterns); `Promise.any([p1,p2])` — resolves when any one fulfills, rejects only when all reject. Error handling: without a `.catch()`, an unhandled rejection — Node.js 15+ crashes the process by default. `Promise.resolve(value)` creates an immediately fulfilled promise, useful for normalizing sync/async APIs. Pitfall: error swallowing in `.then(onFulfilled)` without a `.catch()` — always chain `.catch()` or use try/catch with await.",
  },
  {
    id: 666,
    category: "Node.js",
    subcategory: "Async",
    level: "intermediate",
    q: "async/await trong Node.js là gì? Lợi ích so với Promise chains?",
    a: "async/await là syntactic sugar trên Promise — `async function` luôn return Promise, `await` pause execution của function đó (không block thread) cho đến khi Promise settle. Error handling: try/catch bắt rejected Promise như exception thông thường — `try { const data = await fetch(url).then(r => r.json()); } catch(e) { /* network error, JSON parse error */ }`. Parallel execution: sequential `await a(); await b()` tổng thời gian = a + b; parallel `const [ra, rb] = await Promise.all([a(), b()])` tổng thời gian = max(a, b). Pitfall #1 — sequential await trong loop: `for (const id of ids) { await fetchUser(id); }` chạy tuần tự, chậm. Fix: `await Promise.all(ids.map(id => fetchUser(id)))`. Pitfall #2 — error không được handle: `async function foo() { await riskyOp(); }` gọi `foo()` mà không await/catch = unhandled rejection. Top-level await: ESM modules hỗ trợ `await` ở top level (ngoài function) — hữu ích cho dynamic imports, DB init. Khi debug: stack traces của async/await rõ ràng hơn Promise chains nhiều.",
    q_en: "What is async/await in Node.js? What are its advantages over Promise chains?",
    a_en: "async/await is syntactic sugar over Promises — an `async function` always returns a Promise; `await` pauses execution of that function (without blocking the thread) until the Promise settles. Error handling: try/catch catches rejected Promises like regular exceptions — `try { const data = await fetch(url).then(r => r.json()); } catch(e) { /* network error, JSON parse error */ }`. Parallel execution: sequential `await a(); await b()` takes a + b total time; parallel `const [ra, rb] = await Promise.all([a(), b()])` takes max(a, b) total time. Pitfall #1 — sequential await in a loop: `for (const id of ids) { await fetchUser(id); }` runs one at a time. Fix: `await Promise.all(ids.map(id => fetchUser(id)))`. Pitfall #2 — unhandled errors: `async function foo() { await riskyOp(); }` called as `foo()` without await/catch = unhandled rejection. Top-level await: ESM modules support `await` at the top level (outside functions) — useful for dynamic imports and DB initialization. When debugging: async/await stack traces are far clearer than Promise chains.",
  },
  {
    id: 667,
    category: "Node.js",
    subcategory: "Streams",
    level: "advanced",
    q: "Streams trong Node.js là gì? Có những loại stream nào?",
    a: "Stream xử lý data theo chunks — memory efficiency cực cao: đọc file 1GB chỉ tốn ~64KB RAM (default highWaterMark) thay vì 1GB. Bốn loại: Readable (fs.createReadStream, HTTP req, process.stdin), Writable (fs.createWriteStream, HTTP res, process.stdout), Duplex (TCP socket — đọc và ghi độc lập), Transform (zlib.createGzip, crypto.createCipher — transform data flow through). `pipeline()` API (Node.js 10+) thay thế `.pipe()`: `await pipeline(readStream, transformStream, writeStream)` — tự cleanup và propagate errors (`.pipe()` không handle errors tốt). Stream consumers với `for await...of`: `for await (const chunk of readableStream) { process(chunk); }` — ergonomic nhất. Ví dụ thực: serve 1GB file với 0 timeout và thấp RAM: `const src = fs.createReadStream(filePath); const gzip = zlib.createGzip(); await pipeline(src, gzip, res)` — data chảy qua, không buffer toàn bộ. Backpressure: khi Writable chậm hơn Readable, `pipeline` tự pause Readable tránh OOM — đây là lý do không dùng `.pipe()` thủ công trong production.",
    q_en: "What are Streams in Node.js? What types of streams exist?",
    a_en: "Streams process data in chunks — extreme memory efficiency: reading a 1GB file uses only ~64KB of RAM (default highWaterMark) instead of 1GB. Four types: Readable (fs.createReadStream, HTTP req, process.stdin), Writable (fs.createWriteStream, HTTP res, process.stdout), Duplex (TCP socket — read and write independently), Transform (zlib.createGzip, crypto.createCipher — transforms data flowing through). `pipeline()` API (Node.js 10+) replaces `.pipe()`: `await pipeline(readStream, transformStream, writeStream)` — auto-cleanup and error propagation (`.pipe()` doesn't handle errors well). Stream consumers with `for await...of`: `for await (const chunk of readableStream) { process(chunk); }` — the most ergonomic approach. Practical example: serve a 1GB file with zero timeout and minimal RAM: `const src = fs.createReadStream(filePath); const gzip = zlib.createGzip(); await pipeline(src, gzip, res)` — data flows through without buffering the entire file. Backpressure: when the Writable is slower than the Readable, `pipeline` automatically pauses the Readable to prevent OOM — this is why you should not use `.pipe()` manually in production.",
  },
  {
    id: 668,
    category: "Node.js",
    subcategory: "Streams",
    level: "advanced",
    q: "pipe() trong Node.js streams là gì?",
    a: "pipe() kết nối Readable stream với Writable stream, tự động quản lý flow và backpressure. Dữ liệu chảy từ source đến destination theo chunks, không cần load toàn bộ vào memory. Ví dụ: `fs.createReadStream('file').pipe(gzip).pipe(response)` để stream file nén đến HTTP response.",
    q_en: "What is pipe() in Node.js streams?",
    a_en: "pipe() connects a Readable stream to a Writable stream, automatically managing flow and backpressure. Data flows from source to destination in chunks without loading everything into memory. Example: `fs.createReadStream('file').pipe(gzip).pipe(response)` to stream a compressed file directly to an HTTP response.",
  },
  {
    id: 669,
    category: "Node.js",
    subcategory: "Buffers",
    level: "intermediate",
    q: "Buffer trong Node.js là gì? Khi nào cần dùng?",
    a: "Buffer là class built-in xử lý binary data — vùng nhớ raw bytes nằm ngoài V8 heap. JavaScript thuần không có kiểu dữ liệu binary, Buffer lấp đầy khoảng trống này cho Node.js. Cần dùng khi: đọc file binary (ảnh, PDF, executable), xử lý network packets TCP/UDP, mã hóa/giải mã base64 (`Buffer.from('hello').toString('base64')`), tính hash với `crypto.createHash`. Ví dụ thực tế: upload ảnh qua API, `req.body` là Buffer chứa raw bytes của file, cần convert hoặc pipe trực tiếp lên S3. Pitfall: `Buffer.allocUnsafe(size)` nhanh hơn nhưng chứa dữ liệu cũ trong bộ nhớ — chỉ dùng khi sẽ ghi đè toàn bộ ngay sau đó, dùng `Buffer.alloc(size)` (zero-filled) cho trường hợp thông thường.",
    q_en: "What is a Buffer in Node.js? When do you need to use it?",
    a_en: "Buffer is a built-in class for handling binary data — a region of raw bytes that lives outside the V8 heap. Plain JavaScript has no binary data type; Buffer fills that gap in Node.js. When you need it: reading binary files (images, PDFs, executables), processing TCP/UDP network packets, base64 encoding/decoding (`Buffer.from('hello').toString('base64')`), computing hashes with `crypto.createHash`. Practical example: uploading an image via API, `req.body` is a Buffer containing the raw bytes of the file — either convert it or pipe it directly to S3. Pitfall: `Buffer.allocUnsafe(size)` is faster but contains leftover memory data — only use it when you'll immediately overwrite the entire buffer; use `Buffer.alloc(size)` (zero-filled) in all other cases.",
  },
  {
    id: 670,
    category: "Node.js",
    subcategory: "Event Loop",
    level: "advanced",
    q: "Libuv là gì? Vai trò trong Node.js?",
    a: "Libuv là C library đa nền tảng cung cấp event loop, async I/O và thread pool cho Node.js. Hai loại operations: (1) OS-native async (không dùng thread pool): network I/O (TCP/UDP) — dùng epoll (Linux), kqueue (macOS), IOCP (Windows), cực efficient; (2) Thread pool operations (dùng 4 threads mặc định vì OS không hỗ trợ async đầy đủ): file system I/O (fs.readFile), DNS lookup (dns.lookup — không phải dns.resolve), crypto operations (pbkdf2, scrypt, randomBytes). Thread pool size: tăng bằng env var `UV_THREADPOOL_SIZE=16` (max 1024) — quan trọng khi app làm nhiều file I/O hoặc crypto đồng thời, 4 threads mặc định tạo bottleneck. Ví dụ vấn đề: app gọi 10 `dns.lookup()` đồng thời, 4 threads xử lý 4 cái, 6 cái còn lại queue — tăng UV_THREADPOOL_SIZE hoặc dùng `dns.resolve()` (network I/O, không dùng thread pool). Libuv abstract sự khác biệt giữa OS — cùng Node.js code chạy trên Linux epoll và Windows IOCP mà không cần sửa.",
    q_en: "What is libuv? What role does it play in Node.js?",
    a_en: "libuv is a cross-platform C library providing the event loop, async I/O, and thread pool for Node.js. Two types of operations: (1) OS-native async (no thread pool): network I/O (TCP/UDP) — uses epoll (Linux), kqueue (macOS), IOCP (Windows), extremely efficient; (2) Thread pool operations (uses 4 threads by default because the OS doesn't fully support async): file system I/O (fs.readFile), DNS lookup (dns.lookup — not dns.resolve), crypto operations (pbkdf2, scrypt, randomBytes). Thread pool size: increase via `UV_THREADPOOL_SIZE=16` env var (max 1024) — important when the app does lots of concurrent file I/O or crypto; the default 4 threads creates a bottleneck. Example problem: app makes 10 concurrent `dns.lookup()` calls, 4 threads handle 4, the other 6 are queued — increase UV_THREADPOOL_SIZE or use `dns.resolve()` (network I/O, doesn't use the thread pool). libuv abstracts OS differences — the same Node.js code runs on Linux epoll and Windows IOCP without changes.",
  },

  // === NODE.JS: HTTP & Express (671-680) ===
  {
    id: 671,
    category: "Node.js",
    subcategory: "HTTP",
    level: "beginner",
    q: "Cách tạo HTTP server với module http trong Node.js?",
    a: "Module `http` là built-in, không cần cài thêm: `const server = http.createServer((req, res) => { res.writeHead(200, { 'Content-Type': 'application/json' }); res.end(JSON.stringify({ ok: true })); }); server.listen(3000)`. `req` chứa method, url, headers và là Readable stream (đọc body qua `req.on('data', chunk => ...)`). `res` là Writable stream — phải gọi `res.end()` để kết thúc, không thì client treo mãi. Trong thực tế hiếm ai dùng `http` module trực tiếp vì rất verbose — Express, Fastify, Hono wrap lại với routing, middleware, body parsing tiện hơn nhiều. Tuy nhiên hiểu `http` module giúp debug networking issues và hiểu cách các framework hoạt động bên dưới.",
    q_en: "How do you create an HTTP server using Node.js's http module?",
    a_en: "The `http` module is built-in — no installation needed: `const server = http.createServer((req, res) => { res.writeHead(200, { 'Content-Type': 'application/json' }); res.end(JSON.stringify({ ok: true })); }); server.listen(3000)`. `req` contains method, url, headers, and is a Readable stream (read the body via `req.on('data', chunk => ...)`). `res` is a Writable stream — you must call `res.end()` to close the connection; if you don't, the client hangs indefinitely. In practice, almost nobody uses the `http` module directly because it's very verbose — Express, Fastify, and Hono wrap it with routing, middleware, and body parsing. However, understanding the `http` module helps with debugging networking issues and understanding how frameworks work internally.",
  },
  {
    id: 672,
    category: "Node.js",
    subcategory: "Express",
    level: "beginner",
    q: "Express.js là gì? Tại sao phổ biến trong Node.js ecosystem?",
    a: "Express là web framework minimal cho Node.js, cung cấp routing và middleware system mà không áp đặt cấu trúc dự án. Phổ biến nhất trong Node.js ecosystem vì: API cực đơn giản (`app.get('/users', handler)`), middleware pipeline linh hoạt cho phép compose nhiều concerns (auth, logging, validation) thành chuỗi, và hệ sinh thái packages khổng lồ như `cors`, `helmet`, `multer`, `passport`. Express cũng là nền tảng để nhiều framework khác xây dựng lên như NestJS. Tuy nhiên nhược điểm là quá minimal — không có validation, ORM, hay structure built-in, dev phải tự lắp ghép. Với TypeScript projects hiện đại, nhiều team chuyển sang NestJS (opinionated, decorator-based) hoặc Fastify (nhanh hơn 2-3x, schema validation built-in).",
    q_en: "What is Express.js? Why is it so popular in the Node.js ecosystem?",
    a_en: "Express is a minimal web framework for Node.js that provides routing and a middleware system without imposing a project structure. It's the most popular Node.js framework because: its API is extremely simple (`app.get('/users', handler)`), its flexible middleware pipeline lets you compose multiple concerns (auth, logging, validation) into a chain, and it has a massive package ecosystem including `cors`, `helmet`, `multer`, and `passport`. Express also serves as the foundation for other frameworks like NestJS. However, its minimalism is also a drawback — no built-in validation, ORM, or structure means developers must assemble everything themselves. For modern TypeScript projects, many teams have moved to NestJS (opinionated, decorator-based) or Fastify (2-3x faster, built-in schema validation).",
  },
  {
    id: 673,
    category: "Node.js",
    subcategory: "Express",
    level: "beginner",
    q: "Middleware trong Express là gì? Cách hoạt động?",
    a: "Middleware là function signature `(req, res, next)` thực thi tuần tự theo thứ tự đăng ký — thứ tự là tuyệt đối. App-level: `app.use(helmet())` áp dụng cho toàn bộ app. Router-level: `router.use(authMiddleware)` chỉ cho nhóm routes. Error-handling middleware có 4 tham số `(err, req, res, next)` phải đăng ký SAU tất cả routes. Third-party ecosystem: `helmet` (security headers), `morgan` (logging), `compression` (gzip), `passport` (auth strategies). Flow: `next()` chuyển sang middleware tiếp theo, `next(err)` nhảy thẳng tới error handler bỏ qua tất cả non-error middleware, không gọi `next()` và không `res.send()` thì request treo mãi. Pitfall: đặt `express.json()` sau route handler → `req.body` undefined; đặt `cors()` sau route → preflight OPTIONS request trả 404.",
    q_en: "What is middleware in Express? How does it work?",
    a_en: "Middleware is a function with the signature `(req, res, next)` that executes sequentially in the order it is registered — the order is absolute. App-level: `app.use(helmet())` applies to the entire app. Router-level: `router.use(authMiddleware)` applies only to that group of routes. Error-handling middleware has 4 parameters `(err, req, res, next)` and must be registered AFTER all routes. Common third-party middleware: `helmet` (security headers), `morgan` (logging), `compression` (gzip), `passport` (auth strategies). Flow: `next()` passes control to the next middleware, `next(err)` jumps directly to the error handler skipping all non-error middleware, and not calling either `next()` or `res.send()` leaves the request hanging indefinitely. Pitfall: placing `express.json()` after a route handler means `req.body` is undefined for that route; placing `cors()` after routes means preflight OPTIONS requests return 404.",
  },
  {
    id: 674,
    category: "Node.js",
    subcategory: "Express",
    level: "intermediate",
    q: "Router trong Express là gì? Cách tổ chức routes?",
    a: "express.Router() tạo mini-app với routes và middleware riêng biệt — giải pháp chính để tổ chức code theo feature. Mounting pattern: `app.use('/api/v1/users', userRouter)` prefix tất cả routes trong router. Route-level middleware: `router.use(authMiddleware)` chỉ áp dụng cho routes trong router đó. Nested routers: `adminRouter.use('/users', adminUserRouter)` để nest sâu hơn. Versioned API pattern: `app.use('/api/v1', v1Router); app.use('/api/v2', v2Router)` — v1 và v2 hoàn toàn độc lập, dễ deprecate. Cấu trúc thực tế: `routes/users.ts` export router, `routes/index.ts` aggregate tất cả routers, `app.ts` chỉ `app.use('/api', mainRouter)`. Pitfall: `router.use(middleware)` sau `router.get(...)` thì route đó không được áp dụng middleware — phải đặt middleware TRƯỚC routes.",
    q_en: "What is a Router in Express? How do you organize routes?",
    a_en: "express.Router() creates a mini-app with its own routes and middleware — the primary way to organize code by feature. Mounting pattern: `app.use('/api/v1/users', userRouter)` prefixes all routes in the router. Route-level middleware: `router.use(authMiddleware)` applies only to routes within that router. Nested routers: `adminRouter.use('/users', adminUserRouter)` for deeper nesting. Versioned API pattern: `app.use('/api/v1', v1Router); app.use('/api/v2', v2Router)` — v1 and v2 are completely independent, easy to deprecate. Practical file structure: `routes/users.ts` exports a router, `routes/index.ts` aggregates all routers, `app.ts` simply does `app.use('/api', mainRouter)`. Pitfall: `router.use(middleware)` placed after `router.get(...)` means that route doesn't get the middleware applied — middleware must always be declared BEFORE the routes it should affect.",
  },
  {
    id: 675,
    category: "Node.js",
    subcategory: "Express",
    level: "intermediate",
    q: "REST API trong Express: cách định nghĩa các HTTP methods?",
    a: "Express map trực tiếp tới HTTP methods với semantics rõ ràng: GET (idempotent, safe — chỉ đọc, không side effects), POST (không idempotent — tạo resource mới, mỗi call tạo record mới), PUT (idempotent — replace toàn bộ resource, gửi thiếu field thì field đó bị null/default), PATCH (idempotent — partial update, chỉ gửi fields cần thay đổi), DELETE (idempotent — xóa, gọi nhiều lần kết quả như nhau). CRUD thực tế: `GET /users` (list), `POST /users` (create, trả 201), `GET /users/:id` (read), `PUT /users/:id` (replace, trả 200), `PATCH /users/:id` (update, trả 200), `DELETE /users/:id` (delete, trả 204 no content). Status codes quan trọng: 200 OK, 201 Created, 204 No Content, 400 Bad Request, 401 Unauthorized, 403 Forbidden, 404 Not Found, 409 Conflict, 422 Unprocessable Entity, 500 Internal Server Error. Content negotiation: `req.accepts('json')` kiểm tra client Accept header. Pitfall: dùng GET với body để filter — không đúng semantics, dùng query params thay thế.",
    q_en: "REST API in Express: how do you define HTTP methods?",
    a_en: "Express maps directly to HTTP methods with clear semantics: GET (idempotent, safe — read only, no side effects), POST (not idempotent — creates a new resource; each call creates a new record), PUT (idempotent — replaces the entire resource; missing fields are set to null/default), PATCH (idempotent — partial update; only send fields to change), DELETE (idempotent — delete; multiple calls produce the same result). Practical CRUD: `GET /users` (list), `POST /users` (create, return 201), `GET /users/:id` (read), `PUT /users/:id` (replace, return 200), `PATCH /users/:id` (update, return 200), `DELETE /users/:id` (delete, return 204 no content). Important status codes: 200 OK, 201 Created, 204 No Content, 400 Bad Request, 401 Unauthorized, 403 Forbidden, 404 Not Found, 409 Conflict, 422 Unprocessable Entity, 500 Internal Server Error. Content negotiation: `req.accepts('json')` checks the client's Accept header. Pitfall: using GET with a body for filtering — incorrect semantics; use query params instead.",
  },
  {
    id: 676,
    category: "Node.js",
    subcategory: "Express",
    level: "intermediate",
    q: "Error handling middleware trong Express hoạt động như thế nào?",
    a: "Error handling middleware có 4 tham số `(err, req, res, next)` — phải đăng ký CUỐI CÙNG sau tất cả routes. Pattern tốt nhất: tạo custom AppError class `class AppError extends Error { constructor(public statusCode: number, message: string, public isOperational = true) { super(message) } }` — phân biệt operational errors (404, validation fail — dự đoán được) vs programming errors (null reference — bugs). Async error wrapper để tránh try/catch lặp lại: `const asyncHandler = (fn) => (req, res, next) => Promise.resolve(fn(req, res, next)).catch(next)`. Centralized error handler kiểm tra `err.isOperational`: nếu true thì gửi message cho client, nếu false thì log và trả 500 generic. Express 5 async routes tự động forward error nên không cần wrapper nữa. Pitfall: quên `next` tham số thứ 4 → Express không nhận ra là error handler. Không gọi `next()` sau `res.json()` trong error handler → double response error.",
    q_en: "How does error handling middleware work in Express?",
    a_en: "Error handling middleware has 4 parameters `(err, req, res, next)` — it MUST be registered LAST, after all routes. Best pattern: create a custom AppError class `class AppError extends Error { constructor(public statusCode: number, message: string, public isOperational = true) { super(message) } }` — distinguishes operational errors (404, validation fail — expected) from programming errors (null reference — bugs). Async error wrapper to avoid repeated try/catch: `const asyncHandler = (fn) => (req, res, next) => Promise.resolve(fn(req, res, next)).catch(next)`. The centralized error handler checks `err.isOperational`: if true, send the message to the client; if false, log it and return a generic 500. Express 5 automatically forwards async errors so no wrapper is needed. Pitfall: forgetting the 4th `next` parameter means Express won't recognize it as an error handler. Not calling `next()` after `res.json()` in the error handler leads to a double-response error.",
  },
  {
    id: 677,
    category: "Node.js",
    subcategory: "Express",
    level: "intermediate",
    q: "CORS trong Express: cách cấu hình cho phép cross-origin requests?",
    a: "CORS là cơ chế browser bảo vệ user — server phải opt-in cho phép cross-origin requests. Preflight mechanism: browser tự động gửi OPTIONS request trước khi gửi request thật nếu là preflighted request (method không phải GET/POST/HEAD, hoặc header không phải simple header như Content-Type: application/json). Simple requests (GET với simple headers) không cần preflight. Cấu hình production đúng: `cors({ origin: ['https://app.example.com'], methods: ['GET','POST','PUT','PATCH','DELETE'], allowedHeaders: ['Content-Type','Authorization'], credentials: true })` — `credentials: true` bắt buộc khi gửi cookies/Authorization header. Quan trọng: khi `credentials: true`, origin KHÔNG được là `*` — phải liệt kê explicit. Debug tips: (1) kiểm tra response header `Access-Control-Allow-Origin` trong DevTools Network tab, (2) `credentials: true` nhưng server trả `*` → browser chặn, (3) đặt `cors()` TRƯỚC routes để OPTIONS preflight được handle. Pitfall: CORS là browser enforcement — Postman/curl không bị chặn, chỉ browser mới bị.",
    q_en: "CORS in Express: how do you configure it to allow cross-origin requests?",
    a_en: "CORS is a browser security mechanism — the server must opt in to allow cross-origin requests. Preflight mechanism: the browser automatically sends an OPTIONS request before the actual request if it is preflighted (non-simple methods like PUT/DELETE, or non-simple headers like Content-Type: application/json). Simple requests (GET with simple headers) don't require a preflight. Correct production config: `cors({ origin: ['https://app.example.com'], methods: ['GET','POST','PUT','PATCH','DELETE'], allowedHeaders: ['Content-Type','Authorization'], credentials: true })` — `credentials: true` is required when sending cookies or Authorization headers. Important: when `credentials: true`, the origin CANNOT be `*` — you must specify it explicitly. Debug tips: (1) check the `Access-Control-Allow-Origin` response header in DevTools Network tab, (2) `credentials: true` but server returns `*` → browser blocks it, (3) place `cors()` BEFORE routes so OPTIONS preflight requests are handled. Pitfall: CORS is enforced by browsers only — Postman, curl, and server-to-server requests are not affected.",
  },
  {
    id: 678,
    category: "Node.js",
    subcategory: "Express",
    level: "intermediate",
    q: "express.json() và express.urlencoded() middleware dùng để làm gì?",
    a: "express.json() parse Content-Type application/json thành `req.body` object — built-in từ Express 4.16+, không cần `body-parser` riêng. express.urlencoded({ extended: true }) parse HTML form data (application/x-www-form-urlencoded); `extended: true` dùng `qs` library cho phép nested objects, `extended: false` dùng `querystring` chỉ flat. Size limit mặc định 100kb — thay đổi: `express.json({ limit: '10mb' })`. Security: large payloads gây DoS — luôn set limit hợp lý, không để mặc định cho upload endpoint. Custom content types: `express.json({ type: 'application/vnd.api+json' })`. Raw parser: `express.raw({ type: 'application/octet-stream' })` cho binary. Text parser: `express.text()` cho plain text webhooks. Pitfall: `express.json()` không parse multipart/form-data (file upload) — cần `multer` riêng. Nếu `req.body` là undefined, kiểm tra middleware đã được đăng ký chưa và đúng thứ tự chưa.",
    q_en: "What do express.json() and express.urlencoded() middleware do?",
    a_en: "express.json() parses Content-Type application/json into a `req.body` object — built into Express 4.16+, no separate `body-parser` package needed. express.urlencoded({ extended: true }) parses HTML form data (application/x-www-form-urlencoded); `extended: true` uses the `qs` library allowing nested objects, `extended: false` uses `querystring` for flat data only. Default size limit is 100KB — change with: `express.json({ limit: '10mb' })`. Security: large payloads can cause DoS — always set a reasonable limit, especially on upload endpoints. Custom content types: `express.json({ type: 'application/vnd.api+json' })`. Raw parser: `express.raw({ type: 'application/octet-stream' })` for binary data. Text parser: `express.text()` for plain text webhooks. Pitfall: `express.json()` does NOT parse multipart/form-data (file uploads) — you need `multer` for that. If `req.body` is undefined, check that the middleware is registered and in the correct order.",
  },
  {
    id: 679,
    category: "Node.js",
    subcategory: "Express",
    level: "intermediate",
    q: "Rate limiting trong Express là gì? Cách implement?",
    a: "Rate limiting bảo vệ API khỏi spam, brute-force và DDoS. Fixed window: đếm requests trong window cố định (0:00-0:15) — dễ bypass bằng cách gửi burst ở cuối window cũ + đầu window mới. Sliding window: window trượt theo thời gian thực, chính xác hơn nhưng cần Redis. `express-rate-limit` dùng fixed window: `rateLimit({ windowMs: 15*60*1000, max: 100, standardHeaders: true, legacyHeaders: false })`. Distributed rate limiting với Redis: `rate-limit-redis` store để share state giữa nhiều server instances — bắt buộc trong cluster/multi-server deployment, nếu dùng in-memory thì mỗi instance có counter riêng, giới hạn thực tế là `max * numInstances`. Rate limit headers trả về client: `RateLimit-Limit`, `RateLimit-Remaining`, `RateLimit-Reset` (standardHeaders: true). Limits khác nhau per endpoint: auth endpoints (5 req/15min), API endpoints (100 req/15min), public endpoints (1000 req/15min). Per-user limits: `keyGenerator: (req) => req.user?.id || req.ip` — authenticated users có limit riêng theo userId thay vì IP. Pitfall: behind reverse proxy thì `req.ip` là IP của proxy — cần `app.set('trust proxy', 1)` để lấy IP thật từ `X-Forwarded-For`.",
    q_en: "What is rate limiting in Express? How do you implement it?",
    a_en: "Rate limiting protects APIs from spam, brute-force attacks, and DDoS. Fixed window: counts requests within a fixed window (0:00-0:15) — easy to bypass by sending a burst at the end of the old window and beginning of the new one. Sliding window: window slides in real time, more accurate but requires Redis. `express-rate-limit` uses a fixed window: `rateLimit({ windowMs: 15*60*1000, max: 100, standardHeaders: true, legacyHeaders: false })`. Distributed rate limiting with Redis: `rate-limit-redis` store to share state across multiple server instances — required in cluster/multi-server deployments; with in-memory storage, each instance has its own counter so the real limit is `max * numInstances`. Rate limit headers returned to client: `RateLimit-Limit`, `RateLimit-Remaining`, `RateLimit-Reset` (with standardHeaders: true). Different limits per endpoint: auth endpoints (5 req/15min), API endpoints (100 req/15min), public endpoints (1000 req/15min). Per-user limits: `keyGenerator: (req) => req.user?.id || req.ip` — authenticated users get limits by userId rather than IP. Pitfall: behind a reverse proxy `req.ip` is the proxy's IP — set `app.set('trust proxy', 1)` to get the real IP from `X-Forwarded-For`.",
  },
  {
    id: 680,
    category: "Node.js",
    subcategory: "Express",
    level: "advanced",
    q: "Sự khác biệt giữa Express 4 và Express 5 là gì?",
    a: "Express 5 (stable 2024): async error handling tự động (throw trong async route tự chuyển đến error handler, không cần try/catch), path matching cải tiến, một số breaking changes về routing syntax. Express 4 yêu cầu wrap async routes với try/catch hoặc dùng `express-async-handler`.",
    q_en: "What are the differences between Express 4 and Express 5?",
    a_en: "Express 5 (stable 2024): automatic async error handling (throwing inside an async route automatically forwards to the error handler without try/catch), improved path matching, and some breaking changes to routing syntax. Express 4 requires wrapping async routes with try/catch or using `express-async-handler`.",
  },

  // === NODE.JS: Advanced (681-700) ===
  {
    id: 681,
    category: "Node.js",
    subcategory: "Authentication",
    level: "intermediate",
    q: "JWT (JSON Web Token) là gì? Cấu trúc của JWT?",
    a: "JWT là chuẩn mở (RFC 7519) để truyền thông tin an toàn dưới dạng JSON, được ký số để đảm bảo tính toàn vẹn. Gồm 3 phần ngăn cách bởi dấu chấm, mỗi phần base64url-encoded: Header (`{alg: 'HS256', typ: 'JWT'}`), Payload (claims như `{sub: userId, exp: timestamp, role: 'admin'}`), Signature (HMAC của header+payload bằng secret key). Ưu điểm stateless: server không cần lưu session, chỉ cần verify signature — phù hợp microservices và horizontal scaling. Nhược điểm quan trọng: không thể revoke JWT trước khi hết hạn (không có centralized blacklist), nên access token phải có `exp` ngắn (15 phút) kết hợp refresh token. Pitfall phổ biến nhất: lưu JWT trong localStorage thay vì httpOnly cookie — dễ bị XSS đánh cắp.",
    q_en: "What is JWT (JSON Web Token)? What is the structure of a JWT?",
    a_en: "JWT is an open standard (RFC 7519) for securely transmitting information as JSON, signed to ensure integrity. It consists of 3 parts separated by dots, each base64url-encoded: Header (`{alg: 'HS256', typ: 'JWT'}`), Payload (claims such as `{sub: userId, exp: timestamp, role: 'admin'}`), Signature (HMAC of header+payload using a secret key). Stateless advantage: the server doesn't need to store sessions — it just verifies the signature — making JWTs ideal for microservices and horizontal scaling. Important downside: JWTs cannot be revoked before they expire (no centralized blacklist), so access tokens must have a short `exp` (15 minutes) combined with a refresh token. Most common pitfall: storing JWT in localStorage instead of an httpOnly cookie — vulnerable to XSS theft.",
  },
  {
    id: 682,
    category: "Node.js",
    subcategory: "Authentication",
    level: "intermediate",
    q: "Cách implement JWT authentication trong Express API?",
    a: "Full auth flow: POST /login → verify credentials → `jwt.sign({ sub: user.id, role: user.role }, secret, { expiresIn: '15m' })` tạo access token + `jwt.sign({ sub: user.id }, refreshSecret, { expiresIn: '7d' })` tạo refresh token → trả access token trong response body, refresh token trong httpOnly cookie (`Set-Cookie: refreshToken=...; HttpOnly; Secure; SameSite=Strict`). Token storage: httpOnly cookie bảo vệ khỏi XSS (JS không đọc được), nhưng cần CSRF protection; localStorage tiện hơn nhưng XSS đánh cắp được — httpOnly cookie là best practice cho web apps. Auth middleware: `const token = req.headers.authorization?.split(' ')[1]; const payload = jwt.verify(token, secret); req.user = payload`. Refresh token rotation: mỗi lần dùng refresh token thì issue cặp mới (access + refresh) và invalidate refresh token cũ — lưu refresh token hash trong DB để có thể revoke. POST /refresh nhận cookie → verify → trả access token mới. POST /logout xóa cookie và blacklist refresh token. Pitfall: không validate `alg` trong header → attacker đổi sang `alg: none`; dùng `{ algorithms: ['HS256'] }` trong verify options.",
    q_en: "How do you implement JWT authentication in an Express API?",
    a_en: "Full auth flow: POST /login → verify credentials → `jwt.sign({ sub: user.id, role: user.role }, secret, { expiresIn: '15m' })` creates an access token + `jwt.sign({ sub: user.id }, refreshSecret, { expiresIn: '7d' })` creates a refresh token → return access token in response body, refresh token in an httpOnly cookie (`Set-Cookie: refreshToken=...; HttpOnly; Secure; SameSite=Strict`). Token storage: httpOnly cookies protect against XSS (JS cannot read them) but require CSRF protection; localStorage is more convenient but can be stolen via XSS — httpOnly cookie is the best practice for web apps. Auth middleware: `const token = req.headers.authorization?.split(' ')[1]; const payload = jwt.verify(token, secret); req.user = payload`. Refresh token rotation: each use of a refresh token issues a new pair (access + refresh) and invalidates the old refresh token — store the refresh token hash in the DB to enable revocation. POST /refresh reads the cookie → verifies → returns a new access token. POST /logout clears the cookie and blacklists the refresh token. Pitfall: not validating `alg` in the header → attacker can change to `alg: none`; use `{ algorithms: ['HS256'] }` in verify options.",
  },
  {
    id: 683,
    category: "Node.js",
    subcategory: "Authentication",
    level: "intermediate",
    q: "bcrypt là gì? Cách dùng để hash password?",
    a: "bcrypt được thiết kế đặc biệt cho password hashing — khác hoàn toàn SHA-256/MD5 vốn được tối ưu để NHANH (SHA-256 hash 10 tỷ lần/giây trên GPU, bcrypt cost=12 chỉ ~250 lần/giây). Adaptive cost factor: `saltRounds` (cost) tăng thì thời gian tăng gấp đôi — cost=10 ~100ms, cost=12 ~400ms, cost=14 ~1.6s. Chọn cost sao cho ~250ms trên server của bạn và tăng dần theo năm khi phần cứng mạnh lên. Built-in salt: bcrypt tự tạo random salt unique per password và nhúng vào hash output — tránh rainbow table attacks và đảm bảo cùng password tạo hash khác nhau mỗi lần. Timing attacks: `bcrypt.compare()` là constant-time comparison, không dùng `===` để so sánh hash. Argon2 là alternative hiện đại hơn (winner PHC 2015): argon2id kháng GPU và side-channel attacks tốt hơn, được OWASP khuyến nghị năm 2023+; dùng `argon2` package trong Node.js. Pitfall: hash password trong service layer, không trong model hook — dễ bị double-hash nếu hook chạy lại khi update field khác.",
    q_en: "What is bcrypt? How do you use it to hash passwords?",
    a_en: "bcrypt was specifically designed for password hashing — completely unlike SHA-256/MD5 which are optimized to be FAST (SHA-256 can hash 10 billion times per second on a GPU; bcrypt with cost=12 only ~250 times per second). Adaptive cost factor: increasing `saltRounds` (cost) doubles the time — cost=10 ~100ms, cost=12 ~400ms, cost=14 ~1.6s. Choose cost so each hash takes ~250ms on your server and increase it over time as hardware improves. Built-in salt: bcrypt automatically generates a unique random salt per password and embeds it in the hash output — prevents rainbow table attacks and ensures the same password produces a different hash each time. Timing attacks: `bcrypt.compare()` is a constant-time comparison; never use `===` to compare hashes. Argon2 is a more modern alternative (PHC winner 2015): argon2id resists GPU and side-channel attacks better, recommended by OWASP since 2023+; use the `argon2` npm package in Node.js. Pitfall: hash passwords in the service layer, not in a model hook — double-hashing can occur if the hook re-runs when other fields are updated.",
  },
  {
    id: 684,
    category: "Node.js",
    subcategory: "Authentication",
    level: "advanced",
    q: "Access token và Refresh token khác nhau như thế nào?",
    a: "Access token short-lived (15 phút) gửi trong mỗi request — stateless, server không lưu gì. Refresh token long-lived (7-30 ngày) lưu trong httpOnly cookie, chỉ dùng để lấy access token mới tại endpoint `/auth/refresh`. Rotation strategy: mỗi lần dùng refresh token phải issue cặp token mới và invalidate token cũ (one-time-use) — nếu token cũ bị dùng lại là dấu hiệu bị đánh cắp, revoke toàn bộ session. Token blacklisting: lưu revoked JTI (JWT ID) trong Redis với TTL bằng thời gian còn lại của token — kiểm tra blacklist trong auth middleware. Sliding sessions: mỗi request hợp lệ extend session — issue refresh token mới nếu còn < 1 ngày. Security: lưu hash của refresh token trong DB, không plain text — nếu DB leak thì token vô dụng. Pitfall: không implement rotation → stolen refresh token dùng mãi đến hết hạn; không lưu vào httpOnly cookie → XSS đánh cắp được.",
    q_en: "How are access tokens and refresh tokens different?",
    a_en: "Access tokens are short-lived (15 minutes), sent with every request — stateless, the server stores nothing. Refresh tokens are long-lived (7-30 days), stored in an httpOnly cookie, used only to obtain a new access token at the `/auth/refresh` endpoint. Rotation strategy: each use of a refresh token must issue a new pair (access + refresh) and invalidate the old one (one-time-use) — if an old token is used again, it's a sign of theft; revoke the entire session. Token blacklisting: store the revoked JTI (JWT ID) in Redis with a TTL equal to the remaining lifetime of the token — check the blacklist in the auth middleware. Sliding sessions: each valid request extends the session — issue a new refresh token if less than 1 day remains. Security: store the hash of the refresh token in the DB, not plain text — if the DB leaks, the tokens are useless. Pitfall: not implementing rotation → a stolen refresh token is valid until it expires; not storing in an httpOnly cookie → XSS can steal it.",
  },
  {
    id: 685,
    category: "Node.js",
    subcategory: "Performance",
    level: "advanced",
    q: "Clustering trong Node.js là gì? Tại sao cần thiết?",
    a: "Node.js single-threaded chỉ dùng một CPU core — server 16-core lãng phí 15 cores. Cluster module: master process fork N worker processes (thường = số CPU cores: `os.cpus().length`), mỗi worker là Node.js process độc lập, share cùng port. Connection distribution: Linux dùng round-robin (master nhận connection rồi distribute), Windows dùng OS scheduling. IPC communication: `process.send(msg)` từ worker, `worker.on('message', handler)` ở master — dùng để share state nhỏ như worker health. Shared nothing architecture: workers không share memory, mỗi worker có heap riêng — session/cache phải dùng Redis thay vì in-memory. Sticky sessions: cần khi dùng WebSocket — load balancer phải route cùng client đến cùng worker (nginx `ip_hash`). PM2 cluster mode: `pm2 start app.js -i max` tự động quản lý cluster với zero-downtime reload (`pm2 reload app`) — reload từng worker một thay vì restart toàn bộ. Khi nào dùng cluster vs reverse proxy: cluster cho vertical scaling trên một server, Nginx/HAProxy cho horizontal scaling nhiều servers.",
    q_en: "What is clustering in Node.js? Why is it necessary?",
    a_en: "Node.js is single-threaded and only uses one CPU core — a 16-core server wastes 15 cores. Cluster module: the master process forks N worker processes (typically equal to the number of CPU cores: `os.cpus().length`), each worker is an independent Node.js process sharing the same port. Connection distribution: Linux uses round-robin (master receives the connection then distributes), Windows uses OS scheduling. IPC communication: `process.send(msg)` from the worker, `worker.on('message', handler)` on the master — used to share small state like worker health. Shared nothing architecture: workers don't share memory; each has its own heap — sessions/cache must use Redis, not in-memory storage. Sticky sessions: required for WebSockets — the load balancer must route the same client to the same worker (nginx `ip_hash`). PM2 cluster mode: `pm2 start app.js -i max` automatically manages the cluster with zero-downtime reload (`pm2 reload app`) — restarts workers one at a time instead of all at once. When to use cluster vs reverse proxy: cluster for vertical scaling on one server, Nginx/HAProxy for horizontal scaling across multiple servers.",
  },
  {
    id: 686,
    category: "Node.js",
    subcategory: "Performance",
    level: "advanced",
    q: "Worker Threads trong Node.js là gì? Khác gì với Cluster?",
    a: "Worker Threads chạy JS trong separate threads trong cùng process — chia sẻ memory qua SharedArrayBuffer, dùng Atomics để synchronize. Cluster tạo separate OS processes — không share memory, overhead IPC cao hơn. Worker Threads API: `const { Worker, isMainThread, parentPort, workerData } = require('worker_threads')`. Main thread: `new Worker('./worker.js', { workerData: { input } })`, lắng nghe `worker.on('message', result => ...)`. Worker thread: `parentPort.postMessage(result)`. Message passing qua `postMessage()` — data được structured clone (deep copy) trừ khi transfer ownership với Transferable (ArrayBuffer). SharedArrayBuffer + Atomics: chia sẻ raw memory không cần copy — dùng cho real-time collaboration, game state. Use cases thực tế: image resizing (sharp library), PDF generation, crypto operations, ML inference, large JSON parsing. Pitfall: Worker Threads không tăng tốc I/O-bound tasks — Node.js event loop đã xử lý I/O async; chỉ dùng cho CPU-bound tasks chiếm event loop > 10ms.",
    q_en: "What are Worker Threads in Node.js? How do they differ from Cluster?",
    a_en: "Worker Threads run JavaScript in separate threads within the same process — sharing memory via SharedArrayBuffer, with synchronization using Atomics. Cluster creates separate OS processes — no shared memory, and IPC overhead is higher. Worker Threads API: `const { Worker, isMainThread, parentPort, workerData } = require('worker_threads')`. Main thread: `new Worker('./worker.js', { workerData: { input } })`, listens with `worker.on('message', result => ...)`. Worker thread: `parentPort.postMessage(result)`. Message passing via `postMessage()` — data is structured-cloned (deep copy) unless ownership is transferred with a Transferable (ArrayBuffer). SharedArrayBuffer + Atomics: shares raw memory without copying — useful for real-time collaboration, game state. Real use cases: image resizing (sharp), PDF generation, crypto operations, ML inference, large JSON parsing. Pitfall: Worker Threads don't speed up I/O-bound tasks — Node.js event loop already handles I/O asynchronously; only use them for CPU-bound tasks that block the event loop for more than 10ms.",
  },
  {
    id: 687,
    category: "Node.js",
    subcategory: "Architecture",
    level: "advanced",
    q: "Microservices architecture với Node.js: ưu và nhược điểm?",
    a: "Communication patterns: sync REST/gRPC (request-response, coupling chặt hơn, dễ debug) vs async message queue — RabbitMQ/Kafka (loose coupling, resilient, nhưng eventual consistency). Service discovery: mỗi service đăng ký địa chỉ vào registry (Consul, etcd), services khác lookup thay vì hardcode URL — cần thiết khi services scale up/down động. API Gateway: single entry point cho clients, xử lý auth/rate-limiting/routing, tránh clients phải biết topology nội bộ (Kong, AWS API Gateway). Saga pattern cho distributed transactions: Choreography (services emit events, services khác react — loose coupling) vs Orchestration (central saga orchestrator điều phối — dễ debug hơn). Event sourcing: lưu toàn bộ events thay vì current state — audit log hoàn chỉnh, có thể replay để rebuild state. Nhược điểm thực tế: distributed tracing cần setup (Jaeger, Zipkin), network latency thay vì function call, integration tests phức tạp hơn, debugging khó hơn. Pitfall: microservices quá sớm — monolith trước, tách khi có bounded contexts rõ ràng và team đủ lớn.",
    q_en: "Microservices architecture with Node.js: what are the pros and cons?",
    a_en: "Communication patterns: sync REST/gRPC (request-response, tighter coupling, easier to debug) vs async message queue — RabbitMQ/Kafka (loose coupling, resilient, but eventual consistency). Service discovery: each service registers its address in a registry (Consul, etcd), and other services look up the address instead of hardcoding URLs — necessary when services scale up/down dynamically. API Gateway: a single entry point for clients, handling auth/rate-limiting/routing so clients don't need to know the internal topology (Kong, AWS API Gateway). Saga pattern for distributed transactions: Choreography (services emit events, others react — loose coupling) vs Orchestration (a central saga orchestrator coordinates — easier to debug). Event sourcing: store all events instead of current state — complete audit log, can replay to rebuild state. Real-world drawbacks: distributed tracing requires setup (Jaeger, Zipkin), network latency instead of function calls, more complex integration tests, harder debugging. Pitfall: adopting microservices too early — start with a monolith, extract when bounded contexts are clear and the team is large enough.",
  },
  {
    id: 688,
    category: "Node.js",
    subcategory: "Caching",
    level: "intermediate",
    q: "Caching trong Node.js API: các chiến lược phổ biến?",
    a: "Cache-aside (lazy loading): check cache → miss → fetch DB → write cache → return — pattern phổ biến nhất, code: `const cached = await redis.get(key); if (cached) return JSON.parse(cached); const data = await db.query(...); await redis.setex(key, 300, JSON.stringify(data)); return data`. Write-through: write cache + DB đồng thời — data luôn fresh nhưng write chậm hơn. Write-behind: write cache trước, async flush xuống DB — write rất nhanh nhưng risk mất data nếu cache crash. Cache invalidation strategies: TTL (đơn giản, chấp nhận stale data), event-based (on update → delete cache key), versioned keys (`user:${id}:v${version}`). TTL chọn sao cho: data thay đổi ít → TTL dài (1 giờ), data thay đổi nhiều → TTL ngắn (30s) hoặc invalidate on write. Cache warming: pre-populate cache khi startup để tránh cold start — gọi `warmCache()` sau khi server start. Thundering herd problem: nhiều requests đồng thời hit expired key → tất cả cùng query DB → DB quá tải. Fix: probabilistic early expiration (renew cache trước khi hết hạn), mutex lock (chỉ 1 request query DB, còn lại chờ), stale-while-revalidate. Pitfall: cache key collision — luôn dùng namespace `${service}:${resource}:${id}`.",
    q_en: "Caching in a Node.js API: what are the common strategies?",
    a_en: "Cache-aside (lazy loading): check cache → miss → fetch DB → write cache → return — the most common pattern; code: `const cached = await redis.get(key); if (cached) return JSON.parse(cached); const data = await db.query(...); await redis.setex(key, 300, JSON.stringify(data)); return data`. Write-through: write to cache and DB simultaneously — data is always fresh but writes are slower. Write-behind: write to cache first, async flush to DB — very fast writes but risk of data loss if cache crashes. Cache invalidation strategies: TTL (simple, accepts some stale data), event-based (on update → delete cache key), versioned keys (`user:${id}:v${version}`). TTL selection: rarely changing data → long TTL (1 hour); frequently changing data → short TTL (30s) or invalidate on write. Cache warming: pre-populate cache on startup to avoid cold start — call `warmCache()` after server starts. Thundering herd problem: many requests simultaneously hit an expired key → all query the DB → DB is overloaded. Fix: probabilistic early expiration (renew cache before it expires), mutex lock (only one request queries the DB while others wait), stale-while-revalidate. Pitfall: cache key collision — always use namespaces `${service}:${resource}:${id}`.",
  },
  {
    id: 689,
    category: "Node.js",
    subcategory: "Error Handling",
    level: "intermediate",
    q: "Cách xử lý unhandled Promise rejections trong Node.js?",
    a: "Node.js 14 trở về trước: unhandled rejection chỉ print warning, process tiếp tục — silent failure nguy hiểm. Node.js 15+: mặc định crash process với exit code 1 — breaking change. Hierarchy xử lý đúng: (1) try/catch trong async functions là chính, (2) `.catch()` chain cho fire-and-forget promises, (3) `process.on('unhandledRejection', (reason) => { logger.error('Unhandled rejection', reason); gracefulShutdown(1); })` là safety net cuối — không phải cơ chế chính. `process.on('uncaughtException', (err) => { logger.error(err); gracefulShutdown(1); })` cho sync throws. Monitoring: gửi đến Sentry trước khi shutdown — `Sentry.captureException(reason); await Sentry.flush(2000)`. Graceful shutdown: stop accepting requests, wait for in-flight, close DB, exit. Dùng ESLint rule `@typescript-eslint/no-floating-promises` để catch missing awaits lúc compile time — tốt hơn runtime detection.",
    q_en: "How do you handle unhandled Promise rejections in Node.js?",
    a_en: "Node.js 14 and earlier: unhandled rejections only print a warning and the process continues — silent failures are dangerous. Node.js 15+: crashes the process with exit code 1 by default — a breaking change. Proper handling hierarchy: (1) try/catch inside async functions is the primary mechanism, (2) `.catch()` chaining for fire-and-forget promises, (3) `process.on('unhandledRejection', (reason) => { logger.error('Unhandled rejection', reason); gracefulShutdown(1); })` as a final safety net — not the primary mechanism. `process.on('uncaughtException', (err) => { logger.error(err); gracefulShutdown(1); })` for synchronous throws. Monitoring: send to Sentry before shutting down — `Sentry.captureException(reason); await Sentry.flush(2000)`. Graceful shutdown: stop accepting requests, wait for in-flight requests, close DB connections, exit. Use the ESLint rule `@typescript-eslint/no-floating-promises` to catch missing awaits at compile time — better than runtime detection.",
  },
  {
    id: 690,
    category: "Node.js",
    subcategory: "File System",
    level: "intermediate",
    q: "fs module trong Node.js: fs.readFile vs fs.createReadStream khác nhau như thế nào?",
    a: "fs.readFile() load toàn bộ file vào RAM trước khi callback — file 100MB chiếm ít nhất 100MB heap; 100 concurrent requests = 10GB RAM, dễ OOM crash. fs.createReadStream() đọc theo chunks (default highWaterMark 64KB) — file 100MB chỉ dùng ~64KB RAM bất kể bao nhiêu requests đồng thời. Serve file thực tế: `fs.createReadStream(filePath).pipe(res)` stream thẳng đến TCP buffer không qua RAM. pipeline() API (Node 10+) tốt hơn pipe(): `await pipeline(fs.createReadStream(path), res)` — tự cleanup khi error, tránh memory leak khi client disconnect. Backpressure tự động: pipe/pipeline dừng đọc file khi client chậm, tiếp tục khi TCP buffer drain. Khi nào dùng readFile: config files < 1MB đọc lúc startup, JSON parse một lần. Khi nào dùng createReadStream: file download, log streaming, CSV export, bất kỳ file > vài MB.",
    q_en: "fs module in Node.js: how do fs.readFile and fs.createReadStream differ?",
    a_en: "fs.readFile() loads the entire file into RAM before calling the callback — a 100MB file occupies at least 100MB of heap; 100 concurrent requests = 10GB RAM, easy to cause an OOM crash. fs.createReadStream() reads in chunks (default highWaterMark 64KB) — a 100MB file only uses ~64KB of RAM regardless of how many concurrent requests are active. Serving files in practice: `fs.createReadStream(filePath).pipe(res)` streams directly to the TCP buffer without touching RAM. pipeline() API (Node 10+) is better than pipe(): `await pipeline(fs.createReadStream(path), res)` — auto-cleanup when an error occurs, prevents memory leaks when the client disconnects. Automatic backpressure: pipe/pipeline stops reading the file when the client is slow, resumes when the TCP buffer drains. When to use readFile: config files <1MB read at startup, one-time JSON parsing. When to use createReadStream: file downloads, log streaming, CSV exports, any file larger than a few MB.",
  },
  {
    id: 691,
    category: "Node.js",
    subcategory: "File System",
    level: "intermediate",
    q: "Sự khác biệt giữa fs.readFile và fs.readFileSync là gì?",
    a: "readFileSync blocking — giữ event loop hoàn toàn cho đến khi OS trả data, không xử lý request nào khác trong thời gian đó. readFile non-blocking — gửi I/O request đến libuv thread pool, event loop tự do xử lý requests khác. Impact thực tế: readFileSync đọc file 50ms trên server 1000 req/s → mỗi request xếp hàng bị delay 50ms cộng dồn → latency spike hàng giây. Khi sync chấp nhận được: (1) module initialization trước `server.listen()` — `const config = JSON.parse(fs.readFileSync('config.json', 'utf8'))` chạy một lần duy nhất lúc startup, không có traffic; (2) CLI tools; (3) build scripts. Quy tắc: bất kỳ code nào chạy SAU `server.listen()` trong request path — bắt buộc dùng async. Đo blocking: `node --prof app.js` → `node --prof-process isolate-*.log` → readFileSync trong hot path hiện rõ ràng với % CPU time.",
    q_en: "What is the difference between fs.readFile and fs.readFileSync?",
    a_en: "readFileSync blocks — it holds the event loop completely until the OS returns data; no other requests are processed during that time. readFile is non-blocking — it sends an I/O request to libuv's thread pool and the event loop is free to handle other requests. Real-world impact: readFileSync reading a file with 50ms latency on a server handling 1,000 req/s → every request in the queue is delayed by 50ms, compounding to seconds of cumulative latency. When sync is acceptable: (1) module initialization before `server.listen()` — `const config = JSON.parse(fs.readFileSync('config.json', 'utf8'))` runs once at startup before any traffic; (2) CLI tools; (3) build scripts. Rule: any code running AFTER `server.listen()` in the request path — must use async. Measuring blocking: `node --prof app.js` → `node --prof-process isolate-*.log` → readFileSync in a hot path shows up clearly with % CPU time.",
  },
  {
    id: 692,
    category: "Node.js",
    subcategory: "Environment",
    level: "beginner",
    q: "Environment variables trong Node.js: cách dùng và tại sao quan trọng?",
    a: "Environment variables tách config khỏi code — 12-factor app methodology: config là bất kỳ thứ gì thay đổi giữa environments (dev/staging/prod). Cấu trúc .env file: `DATABASE_URL=postgresql://...`, `JWT_SECRET=...`, `NODE_ENV=development`. dotenv: `require('dotenv').config()` load .env vào process.env, KHÔNG override existing env vars — production inject trực tiếp qua Docker/K8s/CI không cần .env file. dotenv-safe: kiểm tra required vars từ `.env.example` tại startup, throw error nếu thiếu thay vì silent undefined. Config validation với Zod: `const config = z.object({ DATABASE_URL: z.string().url(), PORT: z.coerce.number().default(3000), JWT_SECRET: z.string().min(32) }).parse(process.env)` — fail fast với error rõ ràng. Tạo `config.ts` module export typed config thay vì access `process.env` trực tiếp khắp nơi — dễ test và type-safe. Pitfall: `process.env.PORT` luôn là string, cần coerce sang number; commit .env vào git — dùng `.gitignore` và `.env.example` làm template.",
    q_en: "Environment variables in Node.js: how do you use them and why are they important?",
    a_en: "Environment variables separate config from code — 12-factor app methodology: config is anything that changes between environments (dev/staging/prod). .env file structure: `DATABASE_URL=postgresql://...`, `JWT_SECRET=...`, `NODE_ENV=development`. dotenv: `require('dotenv').config()` loads .env into process.env, does NOT override existing env vars — production injects variables directly via Docker/K8s/CI without needing a .env file. dotenv-safe: validates required vars from `.env.example` at startup, throws an error if any are missing instead of silently being undefined. Config validation with Zod: `const config = z.object({ DATABASE_URL: z.string().url(), PORT: z.coerce.number().default(3000), JWT_SECRET: z.string().min(32) }).parse(process.env)` — fail fast with a clear error. Create a `config.ts` module that exports typed config instead of accessing `process.env` directly throughout the codebase — easier to test and type-safe. Pitfall: `process.env.PORT` is always a string, needs coercion to a number; committing .env to git — use `.gitignore` and `.env.example` as a template.",
  },
  {
    id: 693,
    category: "Node.js",
    subcategory: "Performance",
    level: "advanced",
    q: "Event loop blocking trong Node.js là gì? Làm thế nào để tránh?",
    a: "Event loop blocking: synchronous code chiếm CPU lâu khiến event loop không xử lý được I/O callbacks, timers, requests khác. Ví dụ thực tế: (1) `JSON.parse(fs.readFileSync('huge.json'))` — 200MB JSON parse tốn ~2s block hoàn toàn; (2) regex backtracking — `/^(a+)+$/.test('aaaaaaaaab')` chạy exponential time với input độc hại (ReDoS); (3) crypto sync — `crypto.pbkdf2Sync()` trong request handler; (4) nested loops O(n²) trên large arrays. Đo blocking: `node --prof app.js` tạo V8 profile; `clinic doctor -- node app.js` từ clinic.js package hiển thị event loop lag rõ ràng; `perf_hooks` API đo `eventLoopUtilization()`. Fix: chia nhỏ task với `setImmediate()` để yield event loop giữa chunks; offload sang Worker Threads cho CPU-intensive; dùng streaming thay vì load toàn bộ vào memory; `safe-regex` package detect ReDoS vulnerable patterns. Threshold: bất kỳ synchronous operation > 10ms trong request handler là vấn đề cần xem xét.",
    q_en: "What is event loop blocking in Node.js? How do you avoid it?",
    a_en: "Event loop blocking: synchronous code occupies the CPU for so long that the event loop cannot process I/O callbacks, timers, or other requests. Real-world examples: (1) `JSON.parse(fs.readFileSync('huge.json'))` — parsing a 200MB JSON file takes ~2s and blocks completely; (2) regex backtracking — `/^(a+)+$/.test('aaaaaaaaab')` runs in exponential time with malicious input (ReDoS); (3) sync crypto — `crypto.pbkdf2Sync()` inside a request handler; (4) nested O(n²) loops on large arrays. Measuring blocking: `node --prof app.js` creates a V8 profile; `clinic doctor -- node app.js` from the clinic.js package shows event loop lag clearly; the `perf_hooks` API measures `eventLoopUtilization()`. Fixes: break up tasks with `setImmediate()` to yield the event loop between chunks; offload to Worker Threads for CPU-intensive work; use streaming instead of loading everything into memory; use the `safe-regex` package to detect ReDoS-vulnerable patterns. Threshold: any synchronous operation taking longer than 10ms in a request handler is worth investigating.",
  },
  {
    id: 694,
    category: "Node.js",
    subcategory: "Streams",
    level: "advanced",
    q: "Backpressure trong Node.js streams là gì? Tại sao quan trọng?",
    a: "Backpressure: Readable produce data nhanh hơn Writable consume — nếu không xử lý, data buffer trong memory vô hạn cho đến khi OOM crash. highWaterMark: ngưỡng buffer size (default 16KB cho object streams, 16384 bytes cho byte streams) — khi buffer vượt ngưỡng, stream signals slow down. Cơ chế manual: `writable.write(chunk)` trả về `false` khi internal buffer đầy → stop reading; lắng nghe `writable.on('drain', resume)` để tiếp tục. Ví dụ file upload: client upload 1GB/s, DB write chỉ 100MB/s — không có backpressure thì 900MB/s tích lũy trong RAM. `pipe()` tự xử lý nhưng không handle errors tốt. `pipeline()` API (stream/promises): `await pipeline(req, transform, fs.createWriteStream(dest))` — tự xử lý backpressure VÀ cleanup tất cả streams khi error/complete, không leak. Transform streams: implement `_transform(chunk, enc, callback)`, gọi `callback()` khi sẵn sàng nhận chunk tiếp theo — tự nhiên tạo backpressure. Pitfall: `.on('data', handler)` puts stream in flowing mode bỏ qua backpressure — dùng async iteration `for await (const chunk of readable)` thay thế.",
    q_en: "What is backpressure in Node.js streams? Why is it important?",
    a_en: "Backpressure: a Readable produces data faster than a Writable can consume it — without handling this, data buffers in memory indefinitely until an OOM crash. highWaterMark: the buffer size threshold (default 16KB for object streams, 16384 bytes for byte streams) — when the buffer exceeds this, the stream signals to slow down. Manual mechanism: `writable.write(chunk)` returns `false` when the internal buffer is full → stop reading; listen for `writable.on('drain', resume)` to continue. File upload example: client uploads at 1GB/s, DB writes only at 100MB/s — without backpressure, 900MB/s accumulates in RAM. `pipe()` handles this automatically but doesn't handle errors well. `pipeline()` API (stream/promises): `await pipeline(req, transform, fs.createWriteStream(dest))` — handles backpressure AND cleans up all streams on error/completion, no leaks. Transform streams: implement `_transform(chunk, enc, callback)`, call `callback()` when ready for the next chunk — naturally creates backpressure. Pitfall: `.on('data', handler)` puts the stream in flowing mode, bypassing backpressure — use async iteration `for await (const chunk of readable)` instead.",
  },
  {
    id: 695,
    category: "Node.js",
    subcategory: "Debugging",
    level: "intermediate",
    q: "Cách debug Node.js application?",
    a: "Breakpoint debugging: `node --inspect app.js` expose debugger tại port 9229; mở `chrome://inspect` → attach. `--inspect-brk` dừng tại dòng đầu tiên — dùng khi debug initialization code. VS Code launch.json: `{ type: 'node', request: 'launch', program: '${workspaceFolder}/src/index.ts', runtimeArgs: ['-r', 'ts-node/register'] }` — breakpoints trong TypeScript với source maps. Memory profiling: Chrome DevTools → Memory tab → Heap Snapshot → so sánh 2 snapshots tìm memory leak; retained size cho thấy objects đang giữ gì. CPU profiling: DevTools → Performance tab → Record → stress → flame graph, tìm hot functions. `clinic doctor -- node app.js`: tự phát hiện event loop delay, I/O issues, memory problems, generate HTML report. `0x app.js`: interactive CPU flamegraph, hiển thị call stacks chiếm CPU. Source maps: `sourceMap: true` trong tsconfig để debugger map JS → TS. Pitfall: `--inspect` trong production mà expose port ra internet → remote code execution vulnerability; bind tới localhost only.",
    q_en: "How do you debug a Node.js application?",
    a_en: "Breakpoint debugging: `node --inspect app.js` exposes the debugger on port 9229; open `chrome://inspect` → attach. `--inspect-brk` pauses at the first line — use when debugging initialization code. VS Code launch.json: `{ type: 'node', request: 'launch', program: '${workspaceFolder}/src/index.ts', runtimeArgs: ['-r', 'ts-node/register'] }` — TypeScript breakpoints with source maps. Memory profiling: Chrome DevTools → Memory tab → Heap Snapshot → compare two snapshots to find memory leaks; retained size shows what objects are holding onto. CPU profiling: DevTools → Performance tab → Record → stress the app → flame graph to find hot functions. `clinic doctor -- node app.js`: auto-detects event loop delay, I/O issues, memory problems, generates an HTML report. `0x app.js`: interactive CPU flamegraph showing call stacks consuming CPU. Source maps: `sourceMap: true` in tsconfig lets the debugger map JS → TS. Pitfall: `--inspect` in production with the port exposed to the internet → remote code execution vulnerability; bind to localhost only.",
  },
  {
    id: 696,
    category: "Node.js",
    subcategory: "Architecture",
    level: "advanced",
    q: "Event Emitter trong Node.js là gì? Cách dùng?",
    a: "EventEmitter implement Observer/pub-sub pattern — backbone của Node.js internals (streams, http.Server, process đều extend EventEmitter). API cơ bản: `.on('event', listener)` đăng ký persistent listener, `.once('event', listener)` tự remove sau lần emit đầu tiên, `.emit('event', ...args)` kích hoạt đồng bộ tất cả listeners, `.off('event', listener)` hoặc `.removeListener()` để unsubscribe. Memory leak warning: Node.js warn khi > 10 listeners trên cùng event (`MaxListenersExceededWarning`) — dùng `emitter.setMaxListeners(20)` nếu cố ý, hoặc fix leak. Thường quên `.removeListener()` trong component unmount/cleanup → listeners tích lũy. Error convention: event tên `'error'` đặc biệt — nếu emit `'error'` mà không có listener thì throw uncaughtException crash process. Practical patterns: `class OrderService extends EventEmitter { async createOrder(data) { const order = await db.save(data); this.emit('order:created', order); } }` — services khác lắng nghe event thay vì coupling trực tiếp. `.once()` dùng cho one-time setup: `server.once('listening', () => console.log('ready'))`. Async listeners: EventEmitter không await async listeners — unhandled promise trong listener → silent failure.",
    q_en: "What is an EventEmitter in Node.js? How do you use it?",
    a_en: "EventEmitter implements the Observer/pub-sub pattern — it's the backbone of Node.js internals (streams, http.Server, and process all extend EventEmitter). Core API: `.on('event', listener)` registers a persistent listener, `.once('event', listener)` auto-removes after the first emit, `.emit('event', ...args)` synchronously triggers all listeners, `.off('event', listener)` or `.removeListener()` to unsubscribe. Memory leak warning: Node.js warns when there are more than 10 listeners on the same event (`MaxListenersExceededWarning`) — use `emitter.setMaxListeners(20)` if intentional, or fix the leak. The most common leak: forgetting `.removeListener()` when a component unmounts/cleans up → listeners accumulate. Error convention: the `'error'` event name is special — emitting `'error'` with no listener throws an uncaughtException that crashes the process. Practical patterns: `class OrderService extends EventEmitter { async createOrder(data) { const order = await db.save(data); this.emit('order:created', order); } }` — other services listen to events instead of directly coupling. `.once()` for one-time setup: `server.once('listening', () => console.log('ready'))`. Async listeners: EventEmitter doesn't await async listeners — an unhandled promise in a listener causes silent failure.",
  },
  {
    id: 697,
    category: "Node.js",
    subcategory: "Security",
    level: "intermediate",
    q: "Các lỗ hổng bảo mật phổ biến trong Node.js API và cách phòng tránh?",
    a: "SQL injection (OWASP A03): `db.query('SELECT * FROM users WHERE id = ' + req.params.id)` → attacker truyền `1 OR 1=1` dump toàn bộ DB. Fix: parameterized queries `db.query('SELECT * FROM users WHERE id = $1', [id])` hoặc ORM. NoSQL injection MongoDB: `{ username: req.body.username }` với body `{ username: { $gt: '' } }` bypass auth. Fix: validate với Zod/Joi trước khi query. XSS (A07): lưu `<script>alert(1)</script>` vào DB rồi render unescaped → steal cookies. Fix: escape output, CSP headers, DOMPurify. ReDoS: `/(a+)+/.test(userInput)` với input `'aaaaaaaaab'` → exponential backtracking block event loop hàng giây. Fix: `safe-regex` package kiểm tra pattern, timeout regex execution. Directory traversal: `fs.readFile('/uploads/' + req.query.file)` với `file=../../etc/passwd`. Fix: `path.resolve()` + verify kết quả starts with allowed directory. Prototype pollution: `obj[userKey] = userValue` với key `__proto__` corrupt Object prototype. Fix: `Object.create(null)` cho plain objects, validate keys. Dependency vulnerabilities: `npm audit` + `npm audit fix`, `snyk` cho CI/CD. Helmet.js: một lệnh `app.use(helmet())` set 11 security headers (X-Frame-Options, HSTS, CSP cơ bản, nosniff).",
    q_en: "What are common security vulnerabilities in a Node.js API and how do you prevent them?",
    a_en: "SQL injection (OWASP A03): `db.query('SELECT * FROM users WHERE id = ' + req.params.id)` → attacker passes `1 OR 1=1` to dump the entire DB. Fix: parameterized queries `db.query('SELECT * FROM users WHERE id = $1', [id])` or use an ORM. NoSQL injection (MongoDB): `{ username: req.body.username }` with body `{ username: { $gt: '' } }` bypasses authentication. Fix: validate with Zod/Joi before querying. XSS (A07): storing `<script>alert(1)</script>` in the DB and rendering it unescaped → steals cookies. Fix: escape output, set CSP headers, use DOMPurify. ReDoS: `/（a+)+/.test(userInput)` with input `'aaaaaaaaab'` → exponential backtracking blocks the event loop for seconds. Fix: use the `safe-regex` package to check patterns, and add a timeout for regex execution. Directory traversal: `fs.readFile('/uploads/' + req.query.file)` with `file=../../etc/passwd`. Fix: `path.resolve()` and verify the result starts with the allowed directory. Prototype pollution: `obj[userKey] = userValue` with key `__proto__` corrupts the Object prototype. Fix: `Object.create(null)` for plain objects, validate keys. Dependency vulnerabilities: `npm audit` + `npm audit fix`, `snyk` in CI/CD. Helmet.js: `app.use(helmet())` sets 11 security headers in one line (X-Frame-Options, HSTS, basic CSP, nosniff).",
  },
  {
    id: 698,
    category: "Node.js",
    subcategory: "Architecture",
    level: "advanced",
    q: "Graceful shutdown trong Node.js là gì? Tại sao quan trọng?",
    a: "Graceful shutdown đảm bảo zero data loss và zero error responses khi deploy/restart. Implementation pattern: `process.on('SIGTERM', async () => { server.close(async () => { await db.disconnect(); await redis.quit(); process.exit(0); }); })` — `server.close()` stop accepting new connections nhưng chờ existing connections finish. Connection draining: set timeout 30s cho in-flight requests — nếu quá timeout force close. Health check: Kubernetes gửi SIGTERM sau khi remove pod khỏi load balancer (readiness probe fails) → khoảng trống 5-10s để LB drain traffic trước khi process nhận SIGTERM — config `terminationGracePeriodSeconds: 30` trong K8s. Kubernetes flow: SIGTERM → preStop hook (sleep 5s để LB drain) → app nhận SIGTERM → graceful shutdown → SIGKILL nếu quá `terminationGracePeriodSeconds`. Track in-flight: `let connections = 0; app.use((req, res, next) => { connections++; res.on('finish', () => connections--); next(); })` — chờ connections về 0 hoặc timeout. Pitfall: không handle SIGINT (Ctrl+C local dev) — thêm cả `process.on('SIGINT', handler)`. Docker `STOPSIGNAL SIGTERM` + `ENTRYPOINT` dùng exec form (không shell) để process nhận signal trực tiếp.",
    q_en: "What is graceful shutdown in Node.js? Why is it important?",
    a_en: "Graceful shutdown ensures zero data loss and zero error responses during deploys and restarts. Implementation pattern: `process.on('SIGTERM', async () => { server.close(async () => { await db.disconnect(); await redis.quit(); process.exit(0); }); })` — `server.close()` stops accepting new connections but waits for existing ones to finish. Connection draining: set a 30-second timeout for in-flight requests — force close after timeout. Health check: Kubernetes sends SIGTERM after removing the pod from the load balancer (readiness probe fails) → a 5-10 second gap allows the LB to drain traffic before the process receives SIGTERM — configure `terminationGracePeriodSeconds: 30` in K8s. Kubernetes flow: SIGTERM → preStop hook (sleep 5s for LB drain) → app receives SIGTERM → graceful shutdown → SIGKILL if beyond `terminationGracePeriodSeconds`. Track in-flight requests: `let connections = 0; app.use((req, res, next) => { connections++; res.on('finish', () => connections--); next(); })` — wait for connections to reach 0 or timeout. Pitfall: not handling SIGINT (Ctrl+C in local dev) — add `process.on('SIGINT', handler)` as well. Use Docker `STOPSIGNAL SIGTERM` + exec-form `ENTRYPOINT` (not shell form) so the process receives signals directly.",
  },
  {
    id: 699,
    category: "Node.js",
    subcategory: "Testing",
    level: "intermediate",
    q: "Cách viết unit tests cho Node.js API với Jest?",
    a: "Test pyramid cho Node.js API: unit tests (service/utils logic) → integration tests (controller + real DB) → e2e tests (full HTTP). Controller testing với supertest: `const res = await request(app).post('/users').send({ email: 'test@test.com' }); expect(res.status).toBe(201)` — không cần start server, supertest inject request trực tiếp. Service testing: mock DB layer với `jest.mock('../db')` → `(db.findUser as jest.Mock).mockResolvedValue({ id: 1 })` — test business logic độc lập với DB. Middleware testing: gọi middleware với mock `req`, `res`, `next` objects — verify `next()` được gọi hay `res.status()` được set. Integration tests: dùng test DB riêng (`TEST_DATABASE_URL`), chạy migrations trước test suite, truncate tables giữa tests (`afterEach`). Mock strategies: `jest.mock()` cho modules, `jest.spyOn()` cho methods, `jest.useFakeTimers()` cho time-dependent code. Test database setup: `globalSetup` chạy migration một lần, `beforeEach` truncate data, `afterAll` disconnect. Coverage: `jest --coverage` + `coverageThreshold: { global: { lines: 80 } }` enforce minimum. Pitfall: không isolate tests → test order dependent failures; mock tất cả external calls (HTTP, email) để tests deterministic.",
    q_en: "How do you write unit tests for a Node.js API with Jest?",
    a_en: "Test pyramid for a Node.js API: unit tests (service/utils logic) → integration tests (controller + real DB) → E2E tests (full HTTP). Controller testing with supertest: `const res = await request(app).post('/users').send({ email: 'test@test.com' }); expect(res.status).toBe(201)` — no need to start a real server; supertest injects requests directly. Service testing: mock the DB layer with `jest.mock('../db')` → `(db.findUser as jest.Mock).mockResolvedValue({ id: 1 })` — test business logic independently of the DB. Middleware testing: call middleware with mock `req`, `res`, and `next` objects — verify `next()` was called or `res.status()` was set correctly. Integration tests: use a separate test DB (`TEST_DATABASE_URL`), run migrations before the test suite, and truncate tables between tests (`afterEach`). Mock strategies: `jest.mock()` for modules, `jest.spyOn()` for methods, `jest.useFakeTimers()` for time-dependent code. Test database setup: `globalSetup` runs migrations once, `beforeEach` truncates data, `afterAll` disconnects. Coverage: `jest --coverage` + `coverageThreshold: { global: { lines: 80 } }` enforces a minimum. Pitfall: not isolating tests → test-order-dependent failures; mock all external calls (HTTP, email) to keep tests deterministic.",
  },
  {
    id: 700,
    category: "Node.js",
    subcategory: "Performance",
    level: "advanced",
    q: "PM2 là gì? Tại sao dùng PM2 trong production?",
    a: "PM2 là production process manager cho Node.js — giải quyết auto-restart, clustering, logging, monitoring trong một tool. Ecosystem config (`ecosystem.config.js`): `module.exports = { apps: [{ name: 'api', script: 'dist/index.js', instances: 'max', exec_mode: 'cluster', env: { NODE_ENV: 'production', PORT: 3000 }, max_memory_restart: '500M', error_file: 'logs/err.log', out_file: 'logs/out.log' }] }` — `pm2 start ecosystem.config.js`. Zero-downtime reload: `pm2 reload api` restart từng worker một (rolling restart), không như `pm2 restart` kill tất cả cùng lúc — dùng reload cho deploys. Monitoring dashboard: `pm2 monit` real-time CPU/RAM per process; `pm2 status` overview tất cả processes. Log management: `pm2 logs api --lines 100` xem logs; `pm2 flush` clear logs; tích hợp với logrotate để tránh disk full. Startup scripts: `pm2 startup` generate systemd/init script để PM2 tự start khi server reboot; `pm2 save` lưu current process list. `max_memory_restart`: tự restart process khi vượt ngưỡng RAM — safety net cho memory leaks. Pitfall: PM2 cluster mode dùng shared nothing — sessions/cache phải ở Redis; `pm2 reload` không graceful nếu app không handle SIGINT/SIGTERM.",
    q_en: "What is PM2? Why use it in production?",
    a_en: "PM2 is a production process manager for Node.js — providing auto-restart, clustering, logging, and monitoring in a single tool. Ecosystem config (`ecosystem.config.js`): `module.exports = { apps: [{ name: 'api', script: 'dist/index.js', instances: 'max', exec_mode: 'cluster', env: { NODE_ENV: 'production', PORT: 3000 }, max_memory_restart: '500M', error_file: 'logs/err.log', out_file: 'logs/out.log' }] }` — start with `pm2 start ecosystem.config.js`. Zero-downtime reload: `pm2 reload api` restarts workers one at a time (rolling restart), unlike `pm2 restart` which kills all at once — use reload for deploys. Monitoring dashboard: `pm2 monit` for real-time CPU/RAM per process; `pm2 status` for an overview of all processes. Log management: `pm2 logs api --lines 100` to view logs; `pm2 flush` to clear logs; integrate with logrotate to prevent disk from filling. Startup scripts: `pm2 startup` generates a systemd/init script so PM2 auto-starts on server reboot; `pm2 save` saves the current process list. `max_memory_restart`: auto-restarts the process when it exceeds a RAM threshold — a safety net for memory leaks. Pitfall: PM2 cluster mode uses shared-nothing — sessions/cache must be in Redis; `pm2 reload` is not graceful if the app doesn't handle SIGINT/SIGTERM.",
  },

  // === DATABASE: SQL Cơ Bản (701-715) ===
  {
    id: 701,
    category: "Database",
    subcategory: "SQL Cơ Bản",
    level: "beginner",
    q: "SQL là gì? Các loại SQL commands chính?",
    a: "SQL (Structured Query Language) là ngôn ngữ chuẩn để tương tác với cơ sở dữ liệu quan hệ như PostgreSQL, MySQL, SQLite. Được chia thành 4 nhóm lệnh: DDL (Data Definition Language) định nghĩa cấu trúc — `CREATE TABLE`, `ALTER TABLE`, `DROP TABLE`; DML (Data Manipulation Language) thao tác dữ liệu — `SELECT`, `INSERT`, `UPDATE`, `DELETE`; DCL (Data Control Language) phân quyền — `GRANT SELECT ON users TO readonly_user`; TCL (Transaction Control Language) quản lý giao dịch — `BEGIN`, `COMMIT`, `ROLLBACK`. Trong thực tế hàng ngày, dev dùng DML 90% thời gian, DDL khi migration, còn DCL và TCL do DBA hoặc DevOps quản lý. Hiểu phân loại này giúp debug lỗi quyền truy cập và thiết kế migration scripts đúng cách.",
    q_en: "What is SQL? What are the main types of SQL commands?",
    a_en: "SQL (Structured Query Language) is the standard language for interacting with relational databases like PostgreSQL, MySQL, and SQLite. It is divided into 4 groups: DDL (Data Definition Language) defines structure — `CREATE TABLE`, `ALTER TABLE`, `DROP TABLE`; DML (Data Manipulation Language) manipulates data — `SELECT`, `INSERT`, `UPDATE`, `DELETE`; DCL (Data Control Language) manages permissions — `GRANT SELECT ON users TO readonly_user`; TCL (Transaction Control Language) manages transactions — `BEGIN`, `COMMIT`, `ROLLBACK`. In daily practice, developers use DML 90% of the time, DDL during migrations, and DCL/TCL are typically handled by DBAs or DevOps. Understanding these categories helps with debugging permission errors and writing correct migration scripts.",
  },
  {
    id: 702,
    category: "Database",
    subcategory: "SQL Cơ Bản",
    level: "beginner",
    q: "Câu lệnh SELECT cơ bản trong SQL như thế nào?",
    a: "Thứ tự thực thi SQL (quan trọng để hiểu tại sao không dùng alias trong WHERE): FROM → JOIN → WHERE → GROUP BY → HAVING → SELECT → DISTINCT → ORDER BY → LIMIT/OFFSET. Câu query thực tế: `SELECT u.name, COUNT(o.id) AS order_count FROM users u LEFT JOIN orders o ON u.id = o.user_id WHERE u.created_at > '2024-01-01' GROUP BY u.id, u.name HAVING COUNT(o.id) > 5 ORDER BY order_count DESC LIMIT 20`. Aliasing: `SELECT price * 0.9 AS discounted_price` — alias trong SELECT không dùng được trong WHERE (chưa được tính), nhưng dùng được trong ORDER BY (PostgreSQL cho phép). DISTINCT: `SELECT DISTINCT city FROM users` loại trùng lặp — có sorting overhead, dùng GROUP BY khi cần performance tốt hơn. SELECT *: không dùng production — lấy data thừa, break khi thêm column, không dùng được covering index. Pitfall: LIMIT không có ORDER BY → kết quả non-deterministic, mỗi query trả về rows khác nhau.",
    q_en: "What does a basic SELECT statement look like in SQL?",
    a_en: "SQL execution order (important for understanding why you can't use aliases in WHERE): FROM → JOIN → WHERE → GROUP BY → HAVING → SELECT → DISTINCT → ORDER BY → LIMIT/OFFSET. A practical query: `SELECT u.name, COUNT(o.id) AS order_count FROM users u LEFT JOIN orders o ON u.id = o.user_id WHERE u.created_at > '2024-01-01' GROUP BY u.id, u.name HAVING COUNT(o.id) > 5 ORDER BY order_count DESC LIMIT 20`. Aliasing: `SELECT price * 0.9 AS discounted_price` — the alias in SELECT cannot be used in WHERE (not yet computed), but can be used in ORDER BY (PostgreSQL allows it). DISTINCT: `SELECT DISTINCT city FROM users` removes duplicates — has sorting overhead; use GROUP BY when performance matters more. SELECT *: avoid in production — fetches unnecessary data, breaks when columns are added, doesn't benefit from covering indexes. Pitfall: LIMIT without ORDER BY → non-deterministic results; each query may return different rows.",
  },
  {
    id: 703,
    category: "Database",
    subcategory: "SQL Cơ Bản",
    level: "beginner",
    q: "INSERT, UPDATE, DELETE trong SQL hoạt động như thế nào?",
    a: "Bulk INSERT: `INSERT INTO users (name, email) VALUES ('A', 'a@x.com'), ('B', 'b@x.com'), ('C', 'c@x.com')` — một round-trip thay vì N trips, nhanh hơn 10-100x với lượng lớn. RETURNING clause (PostgreSQL): `INSERT INTO users (name) VALUES ('Dinh') RETURNING id, created_at` — lấy generated values ngay không cần query thêm. ON CONFLICT (upsert): `INSERT INTO settings (user_id, key, value) VALUES (1, 'theme', 'dark') ON CONFLICT (user_id, key) DO UPDATE SET value = EXCLUDED.value, updated_at = NOW()` — atomic upsert không race condition. Safe UPDATE: luôn chạy SELECT với WHERE condition trước để verify rows affected; `UPDATE users SET role = 'admin' WHERE id = 5 RETURNING *` confirm kết quả. Safe DELETE: `BEGIN; DELETE FROM users WHERE created_at < '2020-01-01'; SELECT COUNT(*); ROLLBACK;` — dry run trong transaction để kiểm tra trước khi COMMIT. Pitfall: `UPDATE users SET active = false` không có WHERE → update toàn bộ bảng, không thể undo nếu không có backup; dùng `sql_safe_updates` mode hoặc review trước khi execute.",
    q_en: "How do INSERT, UPDATE, and DELETE work in SQL?",
    a_en: "Bulk INSERT: `INSERT INTO users (name, email) VALUES ('A', 'a@x.com'), ('B', 'b@x.com'), ('C', 'c@x.com')` — one round-trip instead of N, 10-100x faster for large volumes. RETURNING clause (PostgreSQL): `INSERT INTO users (name) VALUES ('Dinh') RETURNING id, created_at` — gets generated values immediately without an extra query. ON CONFLICT (upsert): `INSERT INTO settings (user_id, key, value) VALUES (1, 'theme', 'dark') ON CONFLICT (user_id, key) DO UPDATE SET value = EXCLUDED.value, updated_at = NOW()` — atomic upsert without race conditions. Safe UPDATE: always run the SELECT with the WHERE condition first to verify rows affected; `UPDATE users SET role = 'admin' WHERE id = 5 RETURNING *` confirms the result. Safe DELETE: `BEGIN; DELETE FROM users WHERE created_at < '2020-01-01'; SELECT COUNT(*); ROLLBACK;` — dry run inside a transaction to verify before committing. Pitfall: `UPDATE users SET active = false` without WHERE → updates every row; cannot be undone without a backup. Use `sql_safe_updates` mode or always review before executing.",
  },
  {
    id: 704,
    category: "Database",
    subcategory: "SQL Cơ Bản",
    level: "intermediate",
    q: "Các loại JOIN trong SQL: INNER JOIN, LEFT JOIN, RIGHT JOIN, FULL OUTER JOIN khác nhau như thế nào?",
    a: "Visualize bằng Venn diagrams: INNER JOIN = phần giao (chỉ rows có match cả 2 bảng); LEFT JOIN = toàn bộ vòng trái + phần giao (right side NULL nếu không match); RIGHT JOIN = ngược lại (hiếm dùng, viết lại thành LEFT JOIN cho dễ đọc); FULL OUTER JOIN = cả 2 vòng (NULL ở bên không có match); CROSS JOIN = Cartesian product M×N rows (nguy hiểm với bảng lớn). Use case thực tế: INNER JOIN khi chỉ muốn users CÓ orders; LEFT JOIN khi muốn TẤT CẢ users kể cả không có orders (NULL check để tìm users chưa mua: `WHERE o.id IS NULL\`). Performance: JOIN trên indexed foreign keys rất nhanh; JOIN trên non-indexed columns → Seq Scan + Hash Join chậm. Interview trap: \`SELECT * FROM A LEFT JOIN B ON A.id = B.a_id WHERE B.col = 'x'\` — WHERE filter trên B column biến LEFT JOIN thành INNER JOIN vì NULL rows bị lọc ra; phải dùng \`AND B.col = 'x'\` trong ON clause để giữ NULL rows. Self JOIN: \`SELECT e.name, m.name as manager FROM employees e LEFT JOIN employees m ON e.manager_id = m.id\` — join bảng với chính nó để resolve hierarchical data.",
    q_en: "What are the types of JOINs in SQL? How do INNER JOIN, LEFT JOIN, RIGHT JOIN, and FULL OUTER JOIN differ?",
    a_en: "Visualize with Venn diagrams: INNER JOIN = the intersection (only rows with a match in both tables); LEFT JOIN = entire left circle + intersection (right side is NULL if no match); RIGHT JOIN = the opposite (rarely used; rewrite as LEFT JOIN for readability); FULL OUTER JOIN = both circles (NULL where there's no match on either side); CROSS JOIN = Cartesian product M×N rows (dangerous with large tables). Practical use cases: INNER JOIN when you only want users WITH orders; LEFT JOIN when you want ALL users including those with no orders (check for NULL to find users who've never purchased: \`WHERE o.id IS NULL\`). Performance: JOINs on indexed foreign keys are very fast; JOINs on non-indexed columns trigger a Seq Scan + Hash Join, which is slow. Interview trap: \`SELECT * FROM A LEFT JOIN B ON A.id = B.a_id WHERE B.col = 'x'\` — the WHERE filter on the B column turns the LEFT JOIN into an INNER JOIN because NULL rows are filtered out. You must use \`AND B.col = 'x'\` in the ON clause to preserve NULL rows. Self JOIN: \`SELECT e.name, m.name as manager FROM employees e LEFT JOIN employees m ON e.manager_id = m.id\` — joining a table to itself to resolve hierarchical data.",
  },
  {
    id: 705,
    category: "Database",
    subcategory: "SQL Cơ Bản",
    level: "intermediate",
    q: "WHERE và HAVING trong SQL khác nhau như thế nào?",
    a: "Execution order là chìa khóa: WHERE chạy TRƯỚC GROUP BY (lọc individual rows), HAVING chạy SAU (lọc groups). WHERE không dùng được aggregate vì aggregation chưa xảy ra — \`WHERE COUNT(*) > 5\` là syntax error. HAVING dùng được aggregate: \`HAVING COUNT(*) > 5`, `HAVING SUM(amount) > 1000`. Ví dụ rõ ràng: `SELECT dept, COUNT(*) FROM employees WHERE salary > 50000 GROUP BY dept HAVING COUNT(*) > 3` — WHERE loại employees lương thấp trước, GROUP BY nhóm, HAVING giữ dept có > 3 employees lương cao. Performance: WHERE lọc trước GROUP BY → ít rows phải aggregate → nhanh hơn đáng kể với bảng lớn; luôn đẩy filters không cần aggregate vào WHERE. Sai lầm phổ biến: `HAVING dept = 'Engineering'` thay vì `WHERE dept = 'Engineering'` — hoạt động nhưng scan + group toàn bộ bảng rồi mới lọc; HAVING không dùng được index trên dept.",
    q_en: "How do WHERE and HAVING differ in SQL?",
    a_en: "Execution order is the key: WHERE runs BEFORE GROUP BY (filters individual rows), HAVING runs AFTER (filters groups). WHERE cannot use aggregates because aggregation hasn't happened yet — `WHERE COUNT(*) > 5` is a syntax error. HAVING can use aggregates: `HAVING COUNT(*) > 5`, `HAVING SUM(amount) > 1000`. Clear example: `SELECT dept, COUNT(*) FROM employees WHERE salary > 50000 GROUP BY dept HAVING COUNT(*) > 3` — WHERE removes low-salary employees first, GROUP BY groups them, HAVING keeps only departments with more than 3 high-salary employees. Performance: WHERE filters before GROUP BY → fewer rows to aggregate → significantly faster with large tables; always push filters that don't require aggregation into WHERE. Common mistake: `HAVING dept = 'Engineering'` instead of `WHERE dept = 'Engineering'` — functionally works but scans + groups the entire table before filtering; HAVING can't use an index on dept.",
  },
  {
    id: 706,
    category: "Database",
    subcategory: "SQL Cơ Bản",
    level: "intermediate",
    q: "GROUP BY trong SQL dùng để làm gì? Cho ví dụ?",
    a: "GROUP BY nhóm rows có cùng giá trị thành groups, mỗi group trả về một row. Quy tắc bắt buộc: mọi column trong SELECT phải hoặc là trong GROUP BY hoặc là aggregate function — `SELECT dept, name, COUNT(*) GROUP BY dept` lỗi vì `name` không trong GROUP BY và không phải aggregate (PostgreSQL strict, MySQL lenient). Ví dụ thực tế: `SELECT category, COUNT(*) as total, AVG(price) as avg_price, MAX(price) as max_price FROM products GROUP BY category ORDER BY total DESC`. Group by nhiều columns: `GROUP BY year, month` tạo group riêng cho mỗi cặp year-month. ROLLUP cho subtotals: `GROUP BY ROLLUP(dept, team)` tự động thêm rows tổng hợp cho mỗi dept và grand total — dùng cho báo cáo. CUBE: tất cả combinations của dimensions — `GROUP BY CUBE(dept, year)` tạo subtotals cho dept, year, và cả hai. GROUPING SETS: chỉ định chính xác những groupings cần: `GROUP BY GROUPING SETS ((dept), (year), ())` — flexible hơn ROLLUP/CUBE. Pitfall: GROUP BY column không có index → full scan + sort — cân nhắc thêm index.",
    q_en: "What is GROUP BY in SQL used for? Give an example.",
    a_en: "GROUP BY groups rows with the same value into groups, returning one row per group. Mandatory rule: every column in SELECT must either be in GROUP BY or be an aggregate function — `SELECT dept, name, COUNT(*) GROUP BY dept` is an error because `name` is neither in GROUP BY nor an aggregate (PostgreSQL is strict; MySQL is lenient). Practical example: `SELECT category, COUNT(*) as total, AVG(price) as avg_price, MAX(price) as max_price FROM products GROUP BY category ORDER BY total DESC`. Grouping by multiple columns: `GROUP BY year, month` creates a separate group for each year-month pair. ROLLUP for subtotals: `GROUP BY ROLLUP(dept, team)` automatically adds summary rows for each dept and a grand total — useful for reports. CUBE: all combinations of dimensions — `GROUP BY CUBE(dept, year)` creates subtotals for dept, year, and both combined. GROUPING SETS: specify exactly which groupings you need: `GROUP BY GROUPING SETS ((dept), (year), ())` — more flexible than ROLLUP/CUBE. Pitfall: GROUP BY on a non-indexed column → full scan + sort — consider adding an index.",
  },
  {
    id: 707,
    category: "Database",
    subcategory: "SQL Cơ Bản",
    level: "intermediate",
    q: "Aggregate functions trong SQL là gì? Các hàm phổ biến?",
    a: "Aggregate functions gộp nhiều rows thành một giá trị. NULL behavior quan trọng: COUNT(*) đếm tất cả rows kể cả NULL; COUNT(col) bỏ qua NULL — `COUNT(phone)` cho biết bao nhiêu users có số điện thoại; SUM/AVG/MAX/MIN đều bỏ qua NULL. DISTINCT trong aggregates: `COUNT(DISTINCT city)` đếm số cities duy nhất, `SUM(DISTINCT amount)` tổng các giá trị không trùng. STRING_AGG (PostgreSQL): `STRING_AGG(tag, ', ' ORDER BY tag)` nối values thành string trong group — `SELECT post_id, STRING_AGG(tag_name, ',') FROM post_tags GROUP BY post_id`. ARRAY_AGG: `ARRAY_AGG(email ORDER BY created_at)` tạo array từ group values. Conditional aggregation với FILTER: `COUNT(*) FILTER (WHERE status = 'active') AS active_count, COUNT(*) FILTER (WHERE status = 'inactive') AS inactive_count` — pivot-style aggregation trong một query thay vì nhiều subqueries. PERCENTILE: `PERCENTILE_CONT(0.5) WITHIN GROUP (ORDER BY salary)` tính median — không bị ảnh hưởng bởi outliers như AVG.",
    q_en: "What are aggregate functions in SQL? What are the common ones?",
    a_en: "Aggregate functions collapse multiple rows into a single value. NULL behavior is important: COUNT(*) counts all rows including NULLs; COUNT(col) skips NULLs — `COUNT(phone)` tells you how many users have a phone number; SUM/AVG/MAX/MIN all skip NULLs. DISTINCT in aggregates: `COUNT(DISTINCT city)` counts unique cities; `SUM(DISTINCT amount)` sums only non-duplicate values. STRING_AGG (PostgreSQL): `STRING_AGG(tag, ', ' ORDER BY tag)` concatenates values within a group — `SELECT post_id, STRING_AGG(tag_name, ',') FROM post_tags GROUP BY post_id`. ARRAY_AGG: `ARRAY_AGG(email ORDER BY created_at)` creates an array from group values. Conditional aggregation with FILTER: `COUNT(*) FILTER (WHERE status = 'active') AS active_count, COUNT(*) FILTER (WHERE status = 'inactive') AS inactive_count` — pivot-style aggregation in a single query instead of multiple subqueries. PERCENTILE: `PERCENTILE_CONT(0.5) WITHIN GROUP (ORDER BY salary)` computes the median — not affected by outliers like AVG.",
  },
  {
    id: 708,
    category: "Database",
    subcategory: "SQL Cơ Bản",
    level: "intermediate",
    q: "Index trong SQL là gì? Tại sao cần thiết?",
    a: "B-tree index: cấu trúc cây cân bằng, mỗi node chứa sorted keys và pointers — tìm kiếm O(log n) thay vì O(n) full scan. Bảng 10 triệu rows: full scan ~10s, B-tree index ~1ms. Covering index: index chứa TẤT CẢ columns cần cho query → database chỉ đọc index, không cần đọc table (Index Only Scan) — `CREATE INDEX idx_users_email_name ON users(email, name)` cover `SELECT name FROM users WHERE email = ?`. Partial index: index chỉ trên subset rows — `CREATE INDEX idx_active_users ON users(email) WHERE deleted_at IS NULL` nhỏ hơn, nhanh hơn full index nếu query luôn có `WHERE deleted_at IS NULL`. Khi KHÔNG nên index: bảng < 1000 rows (full scan nhanh hơn), columns ít cardinality (boolean, status với 2-3 values), bảng write-heavy (mỗi write phải update tất cả indexes). Index maintenance: REINDEX để rebuild index bị bloated; `pg_stat_user_indexes` xem index usage — drop indexes không được dùng. Pitfall: function trên indexed column bypass index — `WHERE LOWER(email) = ?` không dùng index trên `email`; tạo functional index `CREATE INDEX ON users(LOWER(email))` thay thế.",
    q_en: "What is an index in SQL? Why is it necessary?",
    a_en: "B-tree index: a balanced tree structure where each node contains sorted keys and pointers — lookup is O(log n) instead of O(n) full scan. A table with 10 million rows: full scan ~10s, B-tree index ~1ms. Covering index: the index contains ALL columns needed for the query → the database only reads the index, not the table (Index Only Scan) — `CREATE INDEX idx_users_email_name ON users(email, name)` covers `SELECT name FROM users WHERE email = ?`. Partial index: index covering only a subset of rows — `CREATE INDEX idx_active_users ON users(email) WHERE deleted_at IS NULL` — smaller and faster than a full index if queries always have `WHERE deleted_at IS NULL`. When NOT to index: tables with fewer than 1,000 rows (full scan is faster), low-cardinality columns (boolean, status with 2-3 values), write-heavy tables (every write must update all indexes). Index maintenance: REINDEX to rebuild bloated indexes; `pg_stat_user_indexes` to check index usage — drop unused indexes. Pitfall: a function on an indexed column bypasses the index — `WHERE LOWER(email) = ?` doesn't use the index on `email`; create a functional index `CREATE INDEX ON users(LOWER(email))` instead.",
  },
  {
    id: 709,
    category: "Database",
    subcategory: "SQL Cơ Bản",
    level: "intermediate",
    q: "Subquery trong SQL là gì? Khi nào dùng subquery vs JOIN?",
    a: "Uncorrelated subquery chạy một lần, kết quả dùng cho outer query: `SELECT * FROM products WHERE price > (SELECT AVG(price) FROM products)`. Correlated subquery tham chiếu outer query và chạy lại cho MỖI ROW của outer query — O(n²) complexity, rất chậm với bảng lớn: `SELECT * FROM employees e WHERE salary > (SELECT AVG(salary) FROM employees WHERE dept = e.dept)`. EXISTS vs IN: `EXISTS (SELECT 1 FROM orders WHERE user_id = u.id)` dừng ngay khi tìm thấy match đầu tiên (short-circuit), tốt cho large subsets; `IN (SELECT user_id FROM orders)` load toàn bộ list vào memory — `NOT IN` với NULL trong subquery trả về empty result set (NULL pitfall). Scalar subquery trong SELECT: `SELECT name, (SELECT COUNT(*) FROM orders WHERE user_id = u.id) AS order_count FROM users u` — tương đương LEFT JOIN nhưng thường chậm hơn vì chạy N lần. Lateral join (PostgreSQL): `FROM users u, LATERAL (SELECT * FROM orders WHERE user_id = u.id ORDER BY created_at DESC LIMIT 3) o` — như correlated subquery nhưng có thể reference multiple columns và return multiple rows, optimizer xử lý tốt hơn. Khi subquery tốt hơn JOIN: EXISTS checks, khi JOIN tạo fan-out (duplicate rows do one-to-many), scalar aggregates.",
    q_en: "What is a subquery in SQL? When should you use a subquery vs a JOIN?",
    a_en: "An uncorrelated subquery runs once; its result is used by the outer query: `SELECT * FROM products WHERE price > (SELECT AVG(price) FROM products)`. A correlated subquery references the outer query and re-runs for EVERY row of the outer query — O(n²) complexity, very slow on large tables: `SELECT * FROM employees e WHERE salary > (SELECT AVG(salary) FROM employees WHERE dept = e.dept)`. EXISTS vs IN: `EXISTS (SELECT 1 FROM orders WHERE user_id = u.id)` stops as soon as it finds the first match (short-circuit), great for large subsets; `IN (SELECT user_id FROM orders)` loads the entire list into memory — `NOT IN` with NULLs in the subquery returns an empty result set (the NULL pitfall). Scalar subquery in SELECT: `SELECT name, (SELECT COUNT(*) FROM orders WHERE user_id = u.id) AS order_count FROM users u` — equivalent to a LEFT JOIN but typically slower because it runs N times. Lateral join (PostgreSQL): `FROM users u, LATERAL (SELECT * FROM orders WHERE user_id = u.id ORDER BY created_at DESC LIMIT 3) o` — like a correlated subquery but can reference multiple columns and return multiple rows; the optimizer handles it better. When a subquery beats a JOIN: EXISTS checks, when a JOIN would fan out (duplicate rows due to one-to-many), scalar aggregates.",
  },
  {
    id: 710,
    category: "Database",
    subcategory: "SQL Cơ Bản",
    level: "advanced",
    q: "Window functions trong SQL là gì? Ví dụ về ROW_NUMBER, RANK?",
    a: "Window functions tính toán trên window (tập rows liên quan) nhưng KHÔNG collapse rows như GROUP BY — mỗi row giữ nguyên và nhận thêm computed value. OVER clause: `PARTITION BY` chia thành sub-windows (như GROUP BY nhưng không collapse), `ORDER BY` xác định thứ tự trong window. ROW_NUMBER(): số thứ tự duy nhất, không ties. RANK(): ties được cùng số, bỏ số sau ties (1,1,3). DENSE_RANK(): ties cùng số, không bỏ số (1,1,2). LAG/LEAD: `LAG(salary, 1) OVER (ORDER BY hire_date)` lấy giá trị row trước — tính month-over-month growth. Running totals: `SUM(amount) OVER (PARTITION BY user_id ORDER BY created_at ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW)` — cumulative sum. Frame specification: `ROWS BETWEEN 2 PRECEDING AND CURRENT ROW` (3-row moving average), `RANGE BETWEEN INTERVAL '7 days' PRECEDING AND CURRENT ROW` (rolling 7-day window). FIRST_VALUE/LAST_VALUE: giá trị đầu/cuối của window. Practical use case: `SELECT *, ROW_NUMBER() OVER (PARTITION BY user_id ORDER BY created_at DESC) AS rn FROM orders` → `WHERE rn = 1` lấy order mới nhất của mỗi user — pattern deduplication phổ biến.",
    q_en: "What are window functions in SQL? Give examples of ROW_NUMBER and RANK.",
    a_en: "Window functions compute over a window (a set of related rows) but do NOT collapse rows like GROUP BY — each row is preserved and receives an additional computed value. OVER clause: `PARTITION BY` divides into sub-windows (like GROUP BY but without collapsing), `ORDER BY` defines the order within the window. ROW_NUMBER(): unique sequential number, no ties. RANK(): ties get the same number, next number is skipped (1,1,3). DENSE_RANK(): ties get the same number, next number is NOT skipped (1,1,2). LAG/LEAD: `LAG(salary, 1) OVER (ORDER BY hire_date)` fetches the value from the previous row — useful for computing month-over-month growth. Running totals: `SUM(amount) OVER (PARTITION BY user_id ORDER BY created_at ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW)` — cumulative sum. Frame specification: `ROWS BETWEEN 2 PRECEDING AND CURRENT ROW` (3-row moving average), `RANGE BETWEEN INTERVAL '7 days' PRECEDING AND CURRENT ROW` (rolling 7-day window). FIRST_VALUE/LAST_VALUE: first/last value in the window. Practical use case: `SELECT *, ROW_NUMBER() OVER (PARTITION BY user_id ORDER BY created_at DESC) AS rn FROM orders` → `WHERE rn = 1` gets the most recent order per user — a common deduplication pattern.",
  },
  {
    id: 711,
    category: "Database",
    subcategory: "SQL Cơ Bản",
    level: "advanced",
    q: "EXPLAIN và EXPLAIN ANALYZE trong PostgreSQL dùng để làm gì?",
    a: "EXPLAIN hiển thị execution plan mà planner dự định dùng mà không chạy query. EXPLAIN ANALYZE thực sự chạy query và so sánh estimated vs actual — dùng trên production cẩn thận với DML. `EXPLAIN (ANALYZE, BUFFERS, FORMAT JSON)` cho thêm buffer hit/miss info. Đọc plan từ trong ra ngoài, từ dưới lên trên — node trong cùng chạy trước. Cost: `(startup_cost..total_cost)` là planner estimate, không phải milliseconds — dùng để so sánh tương đối. Actual time: `(actual time=0.043..1.2 rows=100)`. Plan nodes quan trọng: Seq Scan — đọc toàn bộ table, tốt cho large % of rows hoặc small tables; Index Scan — dùng index rồi fetch table rows (random I/O); Index Only Scan — chỉ đọc index, không cần table (covering index, rất nhanh); Bitmap Index Scan + Bitmap Heap Scan — batch random reads thành sequential, tốt cho medium % of rows; Hash Join — build hash table từ smaller relation, probe với larger; Nested Loop — tốt khi outer là nhỏ và inner có index; Merge Join — tốt khi cả hai đã sorted. Red flags: estimated rows vs actual rows chênh lệch lớn (stale statistics → `ANALYZE table`); Seq Scan trên bảng lớn trong WHERE condition → thiếu index; high Buffers read → data không trong cache.",
    q_en: "What are EXPLAIN and EXPLAIN ANALYZE used for in PostgreSQL?",
    a_en: "EXPLAIN shows the execution plan the planner intends to use without running the query. EXPLAIN ANALYZE actually runs the query and compares estimated vs actual results — use carefully on production with DML. `EXPLAIN (ANALYZE, BUFFERS, FORMAT JSON)` also shows buffer hit/miss info. Read the plan from inside out, bottom to top — the innermost node runs first. Cost: `(startup_cost..total_cost)` is the planner's estimate, not milliseconds — use for relative comparison. Actual time: `(actual time=0.043..1.2 rows=100)`. Important plan nodes: Seq Scan — reads the entire table; good for large % of rows or small tables. Index Scan — uses index then fetches table rows (random I/O). Index Only Scan — reads only the index, not the table (covering index, very fast). Bitmap Index Scan + Bitmap Heap Scan — batches random reads into sequential; good for medium % of rows. Hash Join — builds a hash table from the smaller relation, probes with the larger. Nested Loop — good when the outer set is small and inner has an index. Merge Join — good when both are already sorted. Red flags: large discrepancy between estimated vs actual rows (stale statistics → run `ANALYZE table`); Seq Scan on a large table in a WHERE condition → missing index; high Buffers read → data not in cache.",
  },
  {
    id: 712,
    category: "Database",
    subcategory: "SQL Cơ Bản",
    level: "beginner",
    q: "NULL trong SQL là gì? Cách kiểm tra NULL?",
    a: "NULL trong SQL đại diện cho giá trị không xác định hoặc thiếu — hoàn toàn khác với số 0, chuỗi rỗng `''`, hay `false`. Đây là nguồn gốc của nhiều bug khó phát hiện. Kiểm tra NULL phải dùng `IS NULL` / `IS NOT NULL`, tuyệt đối không dùng `= NULL` vì `NULL = NULL` trả về NULL (không phải TRUE), khiến WHERE clause không match gì cả. Aggregate functions như `SUM()`, `AVG()`, `MAX()` tự động bỏ qua NULL — `AVG(score)` chỉ tính trung bình các hàng có giá trị, không tính NULL là 0. Xử lý NULL khi select: `COALESCE(phone, 'N/A')` trả về giá trị đầu tiên không NULL. Pitfall trong JOIN: nếu foreign key có NULL, `INNER JOIN` sẽ loại bỏ hàng đó — dùng `LEFT JOIN` và kiểm tra NULL trong kết quả.",
    q_en: "What is NULL in SQL? How do you check for NULL?",
    a_en: "NULL in SQL represents an unknown or missing value — completely different from the number 0, an empty string `''`, or `false`. It is the source of many subtle bugs. Checking for NULL must use `IS NULL` / `IS NOT NULL` — never use `= NULL` because `NULL = NULL` returns NULL (not TRUE), causing the WHERE clause to match nothing. Aggregate functions like `SUM()`, `AVG()`, `MAX()` automatically skip NULLs — `AVG(score)` calculates the average only for rows with a value, not treating NULL as 0. Handling NULLs when selecting: `COALESCE(phone, 'N/A')` returns the first non-NULL value. Pitfall in JOINs: if a foreign key is NULL, an INNER JOIN will exclude that row — use LEFT JOIN and check for NULL in the result.",
  },
  {
    id: 713,
    category: "Database",
    subcategory: "SQL Cơ Bản",
    level: "intermediate",
    q: "UNION và UNION ALL trong SQL khác nhau như thế nào?",
    a: "UNION deduplicate bằng cách sort + compare toàn bộ result set — O(n log n) overhead, chậm với large results. UNION ALL giữ tất cả rows kể cả duplicates, không sort — luôn nhanh hơn, dùng khi biết không có duplicates hoặc duplicates là intentional. Column compatibility: cùng số columns và compatible (không nhất thiết identical) types — `SELECT id, name FROM users UNION SELECT order_id, product_name FROM orders` hợp lệ nếu types compatible. Column names lấy từ SELECT đầu tiên. ORDER BY chỉ được đặt ở cuối toàn bộ UNION, không được đặt trong từng SELECT — `(SELECT ... ORDER BY x) UNION (SELECT ...)` là error trong nhiều databases. INTERSECT: rows xuất hiện trong CẢ HAI queries — `SELECT user_id FROM premium_users INTERSECT SELECT user_id FROM active_users`. EXCEPT (MINUS trong Oracle): rows trong query đầu nhưng không có trong query thứ hai — `SELECT email FROM all_users EXCEPT SELECT email FROM unsubscribed`. Practical use case: `SELECT 'income' as type, amount FROM income UNION ALL SELECT 'expense', -amount FROM expenses ORDER BY date` — combine two different tables for unified report.",
    q_en: "How do UNION and UNION ALL differ in SQL?",
    a_en: "UNION deduplicates by sorting and comparing the entire result set — O(n log n) overhead; slow with large results. UNION ALL keeps all rows including duplicates without sorting — always faster; use it when you know there are no duplicates or duplicates are intentional. Column compatibility: same number of columns and compatible (not necessarily identical) types — `SELECT id, name FROM users UNION SELECT order_id, product_name FROM orders` is valid if types are compatible. Column names come from the first SELECT. ORDER BY can only appear at the very end of the full UNION, not inside individual SELECTs — `(SELECT ... ORDER BY x) UNION (SELECT ...)` is an error in most databases. INTERSECT: rows appearing in BOTH queries — `SELECT user_id FROM premium_users INTERSECT SELECT user_id FROM active_users`. EXCEPT (MINUS in Oracle): rows in the first query but not the second — `SELECT email FROM all_users EXCEPT SELECT email FROM unsubscribed`. Practical use case: `SELECT 'income' as type, amount FROM income UNION ALL SELECT 'expense', -amount FROM expenses ORDER BY date` — combining two different tables into a unified report.",
  },
  {
    id: 714,
    category: "Database",
    subcategory: "SQL Cơ Bản",
    level: "intermediate",
    q: "CTEs (Common Table Expressions) trong SQL là gì? Lợi ích?",
    a: "CTE (WITH clause) là named temporary result set tồn tại trong scope của query — `WITH active_users AS (SELECT * FROM users WHERE deleted_at IS NULL), premium AS (SELECT user_id FROM subscriptions WHERE plan = 'premium') SELECT u.* FROM active_users u JOIN premium p ON u.id = p.user_id`. Lợi ích: dễ đọc hơn nested subqueries, có thể reference nhiều lần trong query chính, hỗ trợ chaining CTEs. Materialization behavior (PostgreSQL): mặc định CTEs được materialized (tính một lần, lưu temp) — optimizer không thể push predicates vào CTE, đôi khi chậm hơn subquery; thêm `NOT MATERIALIZED` để optimizer inline: `WITH cte AS NOT MATERIALIZED (...)`. Recursive CTE — org hierarchy example: `WITH RECURSIVE org AS (SELECT id, name, manager_id, 0 AS depth FROM employees WHERE manager_id IS NULL UNION ALL SELECT e.id, e.name, e.manager_id, o.depth + 1 FROM employees e JOIN org o ON e.manager_id = o.id) SELECT * FROM org ORDER BY depth` — tìm toàn bộ cây tổ chức. CTE vs subquery performance: trong PostgreSQL < 12, CTE luôn materialized (optimization fence); PostgreSQL 12+ thông minh hơn. CTE trong DML: `WITH deleted AS (DELETE FROM logs WHERE created_at < NOW() - INTERVAL '90 days' RETURNING *) INSERT INTO archive SELECT * FROM deleted` — atomic delete + archive.",
    q_en: "What are CTEs (Common Table Expressions) in SQL? What are the benefits?",
    a_en: "A CTE (WITH clause) is a named temporary result set that exists within the scope of a query — `WITH active_users AS (SELECT * FROM users WHERE deleted_at IS NULL), premium AS (SELECT user_id FROM subscriptions WHERE plan = 'premium') SELECT u.* FROM active_users u JOIN premium p ON u.id = p.user_id`. Benefits: more readable than nested subqueries, can be referenced multiple times in the main query, supports chaining CTEs. Materialization behavior (PostgreSQL): by default CTEs are materialized (computed once, stored temporarily) — the optimizer cannot push predicates into the CTE; sometimes slower than a subquery. Add `NOT MATERIALIZED` to let the optimizer inline it: `WITH cte AS NOT MATERIALIZED (...)`. Recursive CTE — org hierarchy example: `WITH RECURSIVE org AS (SELECT id, name, manager_id, 0 AS depth FROM employees WHERE manager_id IS NULL UNION ALL SELECT e.id, e.name, e.manager_id, o.depth + 1 FROM employees e JOIN org o ON e.manager_id = o.id) SELECT * FROM org ORDER BY depth` — traverses the entire org tree. CTE vs subquery performance: in PostgreSQL < 12, CTEs are always materialized (optimization fence); PostgreSQL 12+ is smarter. CTEs in DML: `WITH deleted AS (DELETE FROM logs WHERE created_at < NOW() - INTERVAL '90 days' RETURNING *) INSERT INTO archive SELECT * FROM deleted` — atomic delete + archive.",
  },
  {
    id: 715,
    category: "Database",
    subcategory: "SQL Cơ Bản",
    level: "advanced",
    q: "Composite index là gì? Column ordering trong index quan trọng như thế nào?",
    a: "Composite index trên (a, b, c): có thể dùng cho filter trên (a), (a,b), (a,b,c) — leftmost prefix rule. KHÔNG dùng được cho (b) alone, (c) alone, hay (b,c) vì b không phải leftmost prefix. Ví dụ thực tế: index (status, created_at) — query `WHERE status = 'active' ORDER BY created_at DESC` dùng được hoàn toàn; query `WHERE created_at > '2024-01-01'` không dùng được vì bỏ qua status. Index skip scan (PostgreSQL 13+): trong một số cases optimizer có thể skip leading columns nếu cardinality thấp — nhưng không nên rely on. Thứ tự cột tối ưu: (1) equality conditions trước (=), (2) range conditions sau (>, <, BETWEEN), (3) ORDER BY columns cuối — vì sau range condition, index không hỗ trợ sort nữa. Ví dụ: `WHERE user_id = ? AND status = ? AND created_at > ?` → index (user_id, status, created_at). Covering composite index: `CREATE INDEX idx_orders_user_status_date ON orders(user_id, status, created_at, amount)` cover `SELECT created_at, amount FROM orders WHERE user_id = ? AND status = ?` — Index Only Scan, không đọc table. Pitfall: index (a, b) và index (b, a) là hai indexes khác nhau phục vụ different query patterns — đừng nhầm lẫn.",
    q_en: "What is a composite index? How important is column ordering in the index?",
    a_en: "A composite index on (a, b, c) can be used for filters on (a), (a,b), or (a,b,c) — the leftmost prefix rule. It CANNOT be used for (b) alone, (c) alone, or (b,c) because b is not the leftmost prefix. Practical example: index (status, created_at) — query `WHERE status = 'active' ORDER BY created_at DESC` fully uses it; query `WHERE created_at > '2024-01-01'` cannot use it because it skips status. Index skip scan (PostgreSQL 13+): in some cases the optimizer can skip leading columns if they have low cardinality — but don't rely on this. Optimal column order: (1) equality conditions first (=), (2) range conditions second (>, <, BETWEEN), (3) ORDER BY columns last — because after a range condition, the index can no longer support sorting. Example: `WHERE user_id = ? AND status = ? AND created_at > ?` → index (user_id, status, created_at). Covering composite index: `CREATE INDEX idx_orders_user_status_date ON orders(user_id, status, created_at, amount)` covers `SELECT created_at, amount FROM orders WHERE user_id = ? AND status = ?` — Index Only Scan, no table access. Pitfall: index (a, b) and index (b, a) are two different indexes serving different query patterns — don't confuse them.",
  },

  // === DATABASE: Database Design (716-725) ===
  {
    id: 716,
    category: "Database",
    subcategory: "Database Design",
    level: "intermediate",
    q: "Normalization trong database là gì? Tại sao quan trọng?",
    a: "Normalization loại bỏ redundancy và update anomalies. 1NF: mỗi cell atomic, không repeating groups — vi phạm: `tags: 'nodejs,react'` một cell nhiều values → tách thành bảng `user_tags(user_id, tag)`. 2NF (composite PK): mọi non-key column depend vào TOÀN BỘ PK — vi phạm: `order_items(order_id, product_id, product_name)` — product_name chỉ depend vào product_id → tách ra bảng `products`. 3NF: không có transitive dependency — vi phạm: `employees(id, dept_id, dept_name)` — dept_name depend vào dept_id chứ không phải id → tách thành bảng `departments`. Tại sao quan trọng: update anomaly — đổi tên department phải update N rows, dễ inconsistency; insert anomaly — không thể thêm department nếu chưa có employee; delete anomaly — xóa employee cuối xóa luôn thông tin dept. Thực tế: normalize đến 3NF là standard; BCNF/4NF cho edge cases; denormalize có chủ đích khi có profiling evidence.",
    q_en: "What is normalization in a database? Why is it important?",
    a_en: "Normalization eliminates redundancy and update anomalies. 1NF: every cell is atomic, no repeating groups — violation: `tags: 'nodejs,react'` storing multiple values in one cell → split into a `user_tags(user_id, tag)` table. 2NF (composite PK): every non-key column depends on the ENTIRE primary key — violation: `order_items(order_id, product_id, product_name)` — product_name only depends on product_id → split into a `products` table. 3NF: no transitive dependencies — violation: `employees(id, dept_id, dept_name)` — dept_name depends on dept_id, not on id → split into a `departments` table. Why it matters: update anomaly — changing a department name requires updating N rows, easy to get inconsistencies; insert anomaly — can't add a department if there are no employees yet; delete anomaly — deleting the last employee also deletes the department information. In practice: normalize to 3NF as a standard; BCNF/4NF for edge cases; denormalize intentionally only when you have profiling evidence.",
  },
  {
    id: 717,
    category: "Database",
    subcategory: "Database Design",
    level: "intermediate",
    q: "Primary key và Foreign key là gì? Vai trò trong database?",
    a: "Primary key: uniquely identifies mỗi row, NOT NULL, UNIQUE, immutable — thường là surrogate key (serial/UUID). Composite PK: `PRIMARY KEY (order_id, product_id)` trong junction tables. Foreign key: đảm bảo referential integrity — không thể insert `orders.user_id = 999` nếu users.id = 999 không tồn tại; không thể delete user nếu còn orders tham chiếu (trừ khi có CASCADE). FK constraint options: `RESTRICT` (default, block delete/update nếu có children), `CASCADE` (propagate delete/update xuống children), `SET NULL` (set FK = NULL khi parent deleted — cần FK column nullable), `SET DEFAULT` (set FK = default value), `NO ACTION` (like RESTRICT nhưng deferred). Indexing: PK tự động có index; FK column nên index để JOIN và DELETE parent nhanh — thiếu index trên FK gây full scan child table mỗi khi delete/update parent. Pitfall: FK constraints enforce data integrity nhưng có overhead mỗi INSERT/UPDATE/DELETE — một số teams disable FK trong high-throughput systems và enforce integrity ở application layer (trade-off consistency vs performance).",
    q_en: "What are primary keys and foreign keys? What role do they play in a database?",
    a_en: "Primary key: uniquely identifies each row, NOT NULL, UNIQUE, and immutable — typically a surrogate key (serial/UUID). Composite PK: `PRIMARY KEY (order_id, product_id)` in junction tables. Foreign key: enforces referential integrity — you cannot insert `orders.user_id = 999` if `users.id = 999` doesn't exist; you cannot delete a user if they still have orders (unless CASCADE is set). FK constraint options: `RESTRICT` (default, blocks delete/update if children exist), `CASCADE` (propagates delete/update down to children), `SET NULL` (sets FK = NULL when parent is deleted — FK column must be nullable), `SET DEFAULT` (sets FK = default value), `NO ACTION` (like RESTRICT but deferred). Indexing: PK automatically has an index; FK columns should also be indexed to speed up JOINs and parent DELETE operations — missing FK index causes a full scan of the child table on every parent delete/update. Pitfall: FK constraints enforce data integrity but add overhead on every INSERT/UPDATE/DELETE — some teams disable FKs in high-throughput systems and enforce integrity at the application layer (a trade-off between consistency and performance).",
  },
  {
    id: 718,
    category: "Database",
    subcategory: "Database Design",
    level: "intermediate",
    q: "Các loại relationships trong database design là gì?",
    a: "One-to-One: mỗi record A liên kết đúng một record B — implement bằng UNIQUE FK: `user_profiles.user_id REFERENCES users(id) UNIQUE`. Dùng để: tách optional/sensitive data (user + user_payment_info), tách heavy columns ít dùng, polymorphic extension tables. One-to-Many: một A có nhiều B — FK trên bảng Many: `posts.user_id REFERENCES users(id)`. Phổ biến nhất trong relational design. Many-to-Many: cần junction table với composite PK hoặc surrogate PK: `enrollments(student_id, course_id, enrolled_at, grade)` — junction table nên có thêm attributes liên quan đến relationship. Self-referential: `employees.manager_id REFERENCES employees(id)` — org hierarchy, category tree, comment threads. Polymorphic associations (anti-pattern trong SQL): `comments(id, content, commentable_type, commentable_id)` — commentable_id có thể tham chiếu posts hoặc videos — không có FK constraint, hard to maintain; prefer multiple FK columns với CHECK constraint, hoặc separate junction tables per type. Pattern chọn: xác định cardinality trước, sau đó FK đặt ở phía Many, junction table cho M:N.",
    q_en: "What are the types of relationships in database design?",
    a_en: "One-to-One: each record in A is linked to exactly one record in B — implement with a UNIQUE FK: `user_profiles.user_id REFERENCES users(id) UNIQUE`. Use for: separating optional/sensitive data (user + user_payment_info), splitting out heavy columns that are rarely accessed, polymorphic extension tables. One-to-Many: one A has many B — FK on the Many side: `posts.user_id REFERENCES users(id)`. Most common relationship in relational design. Many-to-Many: requires a junction table with a composite PK or surrogate PK: `enrollments(student_id, course_id, enrolled_at, grade)` — junction tables should carry attributes relevant to the relationship itself. Self-referential: `employees.manager_id REFERENCES employees(id)` — org hierarchies, category trees, comment threads. Polymorphic associations (anti-pattern in SQL): `comments(id, content, commentable_type, commentable_id)` — commentable_id can reference posts or videos — no FK constraint, hard to maintain; prefer multiple FK columns with a CHECK constraint, or separate junction tables per type. Pattern selection: determine cardinality first, then place the FK on the Many side; use a junction table for M:N.",
  },
  {
    id: 719,
    category: "Database",
    subcategory: "Database Design",
    level: "intermediate",
    q: "ER Diagram (Entity-Relationship Diagram) là gì? Các thành phần chính?",
    a: "ER Diagram trực quan hóa database schema trước khi implement — bắt buộc với schema phức tạp để tránh costly refactoring sau. Thành phần: Entities (bảng, hình chữ nhật), Attributes (columns, hình oval — key attribute gạch chân), Relationships (hình thoi nối entities), Cardinality notation. Crow's Foot notation phổ biến hơn Chen notation trong practice: ký hiệu trên đường relationship — `|` (one), `O` (zero), `<` hoặc `>` (many). Ví dụ đọc Crow's Foot: `users ||--o{ orders` = một user có không hoặc nhiều orders; `orders }|--|{ products` = many-to-many với mandatory participation. Tools: dbdiagram.io (code-based, version control friendly), draw.io, Lucidchart, ERDPlus. Prisma schema → auto-generate ERD với Prisma Studio. Bước thiết kế: (1) identify entities từ business requirements, (2) xác định attributes và PK cho mỗi entity, (3) xác định relationships và cardinality, (4) resolve M:N thành junction tables, (5) review với team trước khi viết DDL.",
    q_en: "What is an ER Diagram (Entity-Relationship Diagram)? What are its main components?",
    a_en: "An ER Diagram visualizes the database schema before implementation — mandatory for complex schemas to avoid costly refactoring later. Components: Entities (tables, rectangles), Attributes (columns, ovals — underline the key attribute), Relationships (diamonds connecting entities), Cardinality notation. Crow's Foot notation is more popular than Chen notation in practice: symbols on relationship lines — `|` (one), `O` (zero), `<` or `>` (many). Reading Crow's Foot example: `users ||--o{ orders` = one user has zero or many orders; `orders }|--|{ products` = many-to-many with mandatory participation on both sides. Tools: dbdiagram.io (code-based, version-control friendly), draw.io, Lucidchart, ERDPlus. Prisma schema → auto-generate ERDs with Prisma Studio. Design steps: (1) identify entities from business requirements, (2) determine attributes and PK for each entity, (3) identify relationships and cardinality, (4) resolve M:N relationships into junction tables, (5) review with the team before writing DDL.",
  },
  {
    id: 720,
    category: "Database",
    subcategory: "Database Design",
    level: "intermediate",
    q: "Cascade trong foreign key constraint là gì? ON DELETE CASCADE hoạt động thế nào?",
    a: "Cascade propagate action từ parent xuống child table tự động. ON DELETE CASCADE: xóa user → tự xóa tất cả orders, comments, sessions — tiện nhưng nguy hiểm nếu dùng nhầm. ON DELETE SET NULL: xóa category → products.category_id = NULL, products vẫn tồn tại — dùng khi child có thể tồn tại độc lập. ON DELETE RESTRICT (default): block delete parent nếu còn child — safe nhất, application phải xử lý explicitly. ON DELETE SET DEFAULT: set FK về default value. ON UPDATE CASCADE: hữu ích khi PK có thể thay đổi (natural keys) — ít dùng với surrogate keys vì chúng immutable. Cascade chains: `A → B → C` với CASCADE, xóa A tự xóa B rồi C — có thể không nhận ra. Kiểm tra cascade impact: `SELECT * FROM information_schema.referential_constraints WHERE unique_constraint_schema = 'public'`. Khi nào dùng CASCADE: ownership relationship rõ ràng (user → sessions, order → order_items — items không có nghĩa gì nếu không có order). Khi không nên: shared resources (xóa author không nên xóa books nếu books có value độc lập). Soft delete + CASCADE: khi dùng soft delete thì CASCADE không kích hoạt — phải implement cascade logic trong application.",
    q_en: "What is CASCADE in a foreign key constraint? How does ON DELETE CASCADE work?",
    a_en: "CASCADE propagates an action from the parent table down to child tables automatically. ON DELETE CASCADE: deleting a user → automatically deletes all their orders, comments, sessions — convenient but dangerous if misused. ON DELETE SET NULL: deleting a category → `products.category_id = NULL`, products still exist — use when child records can exist independently. ON DELETE RESTRICT (default): blocks deletion of the parent if children exist — the safest option; the application must explicitly handle it. ON DELETE SET DEFAULT: sets the FK to its default value. ON UPDATE CASCADE: useful when the PK can change (natural keys) — rarely used with surrogate keys since they are immutable. Cascade chains: `A → B → C` with CASCADE — deleting A automatically deletes B, then C — easy to not realize this. Check cascade impact: `SELECT * FROM information_schema.referential_constraints WHERE unique_constraint_schema = 'public'`. When to use CASCADE: clear ownership relationships (user → sessions, order → order_items — items have no meaning without the order). When not to use it: shared resources (deleting an author should not delete books if they have independent value). Soft delete + CASCADE: when using soft delete, CASCADE is not triggered — cascade logic must be implemented at the application level.",
  },
  {
    id: 721,
    category: "Database",
    subcategory: "Database Design",
    level: "advanced",
    q: "Denormalization là gì? Khi nào nên denormalize database?",
    a: "Denormalization cố ý thêm redundant data để eliminate expensive JOINs — chỉ làm sau khi có profiling evidence, không phải premature optimization. Patterns thực tế: (1) Duplicate column — lưu `orders.user_email` dù đã có `users.email` → report queries không cần JOIN users; (2) Computed/cached column — `users.post_count` update khi insert/delete post thay vì `COUNT(*)` mỗi lần; (3) Materialized view — pre-computed result set refresh định kỳ, PostgreSQL `CREATE MATERIALIZED VIEW` + `REFRESH MATERIALIZED VIEW CONCURRENTLY`; (4) Flatten nested — embed thông tin address vào orders thay vì JOIN addresses (order history cần snapshot tại thời điểm mua, không phải địa chỉ hiện tại); (5) Duplicate table cho different access patterns — `user_feeds` pre-computed thay vì JOIN users+posts+follows mỗi lần load feed. Trade-off: faster reads, slower writes, storage overhead, consistency risk — mỗi write phải update redundant data; dùng DB trigger hoặc application logic để maintain consistency. Khi nào: analytics/reporting queries với phức tạp JOINs, read > 100:1 write ratio, response time requirement không đáp ứng được sau indexing.",
    q_en: "What is denormalization and when should it be used?",
    a_en: "Denormalization is the process of intentionally adding redundant data or combining tables to improve read performance. Unlike normalization which reduces redundancy, denormalization accepts some data duplication to reduce complex JOINs. It is used when: read performance is the priority (reporting, analytics), queries with many JOINs are too slow, or certain data is read much more frequently than it is written. Example: instead of joining orders → order_items → products, you store product_name directly in order_items. The tradeoff is that data updates become more complex since the same information exists in multiple places.",
  },
  {
    id: 722,
    category: "Database",
    subcategory: "Database Design",
    level: "intermediate",
    q: "Surrogate key và Natural key khác nhau như thế nào?",
    a: "Natural key: dữ liệu thực có business meaning — email, SSN, ISBN, phone. Ưu: không cần join thêm để lấy giá trị, tự nhiên unique. Nhược: có thể thay đổi (user đổi email → phải update tất cả FKs), có thể không unique thực tế (SSN có thể duplicate do lỗi), lộ business data trong URLs. Surrogate key: ID nhân tạo không có business meaning — SERIAL/BIGSERIAL (auto-increment integer), UUID. Ưu: immutable (không bao giờ thay đổi), stable FK references, không lộ business info, đơn giản. Nhược: cần thêm UNIQUE constraint trên natural key nếu cần enforce uniqueness. Best practice: luôn dùng surrogate PK + UNIQUE constraint trên natural key nếu cần — `id BIGSERIAL PRIMARY KEY, email TEXT UNIQUE NOT NULL`. Candidate key: bất kỳ column/set nào có thể làm PK (unique + not null) — chọn một làm PK, còn lại add UNIQUE constraint.",
    q_en: "What is the difference between a surrogate key and a natural key?",
    a_en: "A natural key is a column that already exists in the business domain and uniquely identifies a record — for example, email, SSN, or product code. A surrogate key is an artificially created identifier, typically an auto-increment integer or UUID. Surrogate keys are preferred in practice because: they are stable (natural keys can change), shorter (better for foreign keys and indexes), and free from business meaning. Natural keys are occasionally used when the value already guarantees uniqueness and will never change — for example, ISO country codes. Most modern applications use surrogate keys (id) as primary keys.",
  },
  {
    id: 723,
    category: "Database",
    subcategory: "Database Design",
    level: "advanced",
    q: "Soft delete là gì? Ưu và nhược điểm so với hard delete?",
    a: "Soft delete: thêm `deleted_at TIMESTAMPTZ` (preferred hơn `is_deleted BOOLEAN` vì biết thêm thời điểm xóa), không xóa record. Implementation: mọi query phải thêm `WHERE deleted_at IS NULL` — dùng partial index để tối ưu: `CREATE INDEX idx_users_active ON users(email) WHERE deleted_at IS NULL`, query chỉ scan active records. Prisma middleware tự động add filter: `prisma.$use(async (params, next) => { if (params.action === 'findMany') params.args.where = { ...params.args.where, deletedAt: null }; return next(params); })`. Ưu điểm: undelete/recovery, audit trail (ai xóa khi nào), foreign key references không bị broken, GDPR right-to-erasure cần thêm bước anonymize. Nhược điểm: tất cả queries phải nhớ filter `deleted_at IS NULL` — dễ quên gây data leak; bảng phình to theo thời gian; UNIQUE constraints vẫn enforce trên deleted records — `email UNIQUE` không cho phép re-register sau khi soft-delete → dùng partial unique index `WHERE deleted_at IS NULL`. Hard delete vẫn cần cho: GDPR erasure requests, sensitive PII, log/audit records cũ sau retention period.",
    q_en: "What is soft delete and what are its advantages and disadvantages?",
    a_en: "Soft delete is a technique where instead of removing a record from the database, a flag is set to mark it as deleted (e.g., deleted_at timestamp or is_deleted boolean). Advantages: data can be recovered if accidentally deleted; audit trails and history are preserved; foreign key constraints are not violated. Disadvantages: all queries must include a WHERE deleted_at IS NULL condition; the table grows indefinitely; indexes become less efficient over time. In practice, soft delete is combined with tools like Prisma middleware or Sequelize paranoid mode to automatically filter deleted records without manually adding conditions to every query.",
  },
  {
    id: 724,
    category: "Database",
    subcategory: "Database Design",
    level: "intermediate",
    q: "UUID vs Auto-increment ID: khi nào nên dùng loại nào?",
    a: "Auto-increment (SERIAL/BIGSERIAL): 4/8 bytes, sequential → tốt cho B-tree index locality, dễ debug, nhưng lộ business info (competitors đoán số records), không an toàn cho distributed systems (cần central counter). UUID v4: 128-bit random, globally unique không cần coordination — perfect cho microservices/distributed; nhưng random → B-tree index fragmentation (random inserts vào random positions → page splits, bloat), 16 bytes (4x lớn hơn int), khó đọc/debug. UUID v7 (RFC 9562, 2024): time-ordered UUID — 48-bit Unix timestamp milliseconds prefix + random suffix → sequential như auto-increment nhưng globally unique. Lợi ích v7: B-tree friendly (sequential inserts → không fragmentation), globally unique, có thể sort by creation time, không lộ total count. PostgreSQL 17+ có `gen_random_uuid()` v4 built-in; v7 cần extension `pg_uuidv7` hoặc generate ở app layer (`uuidv7` npm package). CUID2/NanoID: alternatives — shorter, URL-safe, random nhưng không có timestamp prefix. Khi nào dùng UUID: public-facing IDs (URLs, APIs — tránh enumeration attacks), distributed systems, IDs cần generate ở client/app trước khi DB insert. Khi dùng BIGINT: internal tables không expose ra ngoài, performance-critical tables với many JOINs.",
    q_en: "When should you use UUID versus auto-increment ID as a primary key?",
    a_en: "Auto-increment ID (integer) is simpler, smaller in storage (4-8 bytes), naturally ordered, and performs better for indexes. UUID is a 128-bit random identifier (36 characters), globally unique across databases, harder to guess, and suitable for distributed systems. Use auto-increment ID when: the application runs on a single database, sequential ordering is needed, or storage efficiency matters. Use UUID when: merging data from multiple databases, exposing IDs in URLs (security — harder to enumerate), or building distributed/microservices systems. A common compromise is using auto-increment as the internal primary key and UUID as a public-facing identifier.",
  },
  {
    id: 725,
    category: "Database",
    subcategory: "Database Design",
    level: "advanced",
    q: "Database migration là gì? Tại sao quan trọng trong CI/CD?",
    a: "Migration là versioned scripts thay đổi schema một cách có kiểm soát — mỗi migration có `up` (apply) và `down` (rollback), được track trong `_migrations` table. Tools: Prisma Migrate (`prisma migrate dev` tạo migration từ schema diff, `prisma migrate deploy` apply trong production); Knex.js migrations (manual SQL-like JS); Flyway (Java ecosystem nhưng dùng được với bất kỳ DB, file-based SQL migrations `V1__init.sql`, `V2__add_users.sql`); Liquibase (XML/YAML/SQL, enterprise features). CI/CD flow: migrations chạy tự động trước khi deploy new code — đảm bảo schema sẵn sàng cho code mới. Zero-downtime migrations: thêm column nullable trước (backward compatible) → deploy code mới đọc column mới → backfill data → add NOT NULL constraint → remove old column (multi-step, mỗi step là separate deploy). Nguy hiểm: `ALTER TABLE` với `NOT NULL` không có default trên bảng lớn → lock toàn bộ bảng trong PostgreSQL (dùng `NOT VALID` + validate separately); `DROP COLUMN` không rollback được nếu đã có data. Best practices: migrations idempotent, không bao giờ edit migration đã deploy, test down migration trước khi merge, review migrations cẩn thận như code review — schema change là irreversible.",
    q_en: "What is database migration and what tools are used to manage it?",
    a_en: "Database migration is the process of making controlled, versioned changes to a database schema (adding tables, modifying columns, creating indexes...). Migrations are stored as files in version control so they can be applied consistently across all environments (dev, staging, production). Popular tools: Flyway and Liquibase (Java ecosystem), Alembic (Python/SQLAlchemy), Knex.js migrations, Prisma Migrate, Sequelize migrations, and db-migrate (Node.js). Best practices: each migration is irreversible (has an up and a down function), migrations are never edited after being applied to production, and they run automatically as part of the CI/CD pipeline.",
  },

  // === DATABASE: NoSQL & ORM (726-740) ===
  {
    id: 726,
    category: "Database",
    subcategory: "NoSQL",
    level: "beginner",
    q: "NoSQL là gì? Các loại NoSQL databases phổ biến?",
    a: "NoSQL (Not Only SQL) là nhóm databases không dùng relational model và SQL truyền thống, được thiết kế để giải quyết những bài toán mà SQL databases xử lý kém: schema linh hoạt, horizontal scaling, và throughput cao. Bốn loại chính với use case khác nhau: Document stores (MongoDB, CouchDB) — lưu JSON documents, phù hợp content management, user profiles; Key-Value stores (Redis, DynamoDB) — truy cập O(1) theo key, phù hợp caching và session; Column-family stores (Cassandra, HBase) — write-heavy workloads, time-series data ở quy mô lớn; Graph databases (Neo4j) — dữ liệu có quan hệ phức tạp như mạng xã hội, recommendation engine. Pitfall khi chọn NoSQL: không phải lúc nào cũng tốt hơn SQL — nếu data có quan hệ rõ ràng và cần ACID transactions, PostgreSQL thường là lựa chọn tốt hơn MongoDB.",
    q_en: "What are the main differences between SQL and NoSQL databases?",
    a_en: "SQL databases (relational): fixed schema, store data in tables with rows and columns, use ACID transactions, support complex JOINs, scale vertically. NoSQL databases: flexible schema, various data models (document, key-value, column-family, graph), scale horizontally, sacrifice some consistency for availability and performance. SQL is ideal for: financial transactions, complex reporting queries, data integrity requirements. NoSQL is ideal for: large-scale data with flexible schema, high write throughput, content with hierarchical structure. In practice, many applications use both: PostgreSQL for core business data, Redis for caching, MongoDB or Elasticsearch for search.",
  },
  {
    id: 727,
    category: "Database",
    subcategory: "NoSQL",
    level: "beginner",
    q: "MongoDB là gì? Document-based database hoạt động như thế nào?",
    a: "MongoDB lưu data dưới dạng BSON (Binary JSON) — superset của JSON hỗ trợ thêm types như Date, ObjectId, Binary. Mỗi document tự động có _id field với ObjectId gồm 12 bytes: 4 bytes timestamp + 3 bytes machine ID + 2 bytes PID + 3 bytes counter, đảm bảo uniqueness mà không cần central coordinator. Thay vì tables dùng collections, thay vì rows dùng documents — mỗi document trong cùng collection có thể có schema khác nhau. Flexible schema giúp khi data structure thay đổi thường xuyên (product catalog với attributes khác nhau per category), nhưng gây hại khi cần consistency — ví dụ field age có thể lưu string '25' hay number 25 tùy document dẫn đến bugs khó phát hiện. Pitfall phổ biến: thiếu indexes dẫn đến full collection scan. MongoDB 3.6+ hỗ trợ schema validation với $jsonSchema để enforce structure khi cần.",
    q_en: "What are the main types of NoSQL databases and when should you use each?",
    a_en: "Document (MongoDB, CouchDB): stores JSON/BSON documents, flexible schema, good for content management, catalogs, user profiles. Key-Value (Redis, DynamoDB): extremely fast, simple key→value lookup, ideal for caching, sessions, leaderboards. Wide-Column (Cassandra, HBase): optimized for time-series data and high write throughput, ideal for IoT, logs, analytics. Graph (Neo4j, Amazon Neptune): optimized for relationship queries, ideal for social networks, recommendation engines, fraud detection. Choosing type depends on: data structure, query patterns, scale requirements, and consistency needs.",
  },
  {
    id: 728,
    category: "Database",
    subcategory: "NoSQL",
    level: "intermediate",
    q: "So sánh MongoDB và PostgreSQL: khi nào chọn cái nào?",
    a: "Chọn PostgreSQL khi: data có quan hệ rõ ràng, cần ACID transactions, schema cố định, complex queries với JOINs. PostgreSQL còn có JSONB type — lưu JSON với GIN index, query JSON fields nhanh — là middle ground khi cần vừa relational vừa flexible. Chọn MongoDB khi: data có cấu trúc variable/nested, cần horizontal scaling đơn giản, document-centric (content, catalog, user profiles). Performance: MongoDB nhanh hơn cho document retrieval không cần JOINs; PostgreSQL mạnh hơn cho complex aggregation nhiều bảng. Ecosystem: Mongoose (ODM) cho MongoDB vs Prisma/TypeORM cho PostgreSQL. Decision criteria thực tế: nếu team đã quen SQL, cần transactions phức tạp, hoặc data có many-to-many relations — chọn PostgreSQL; nếu cần schema evolution nhanh, team Node.js, data hierarchy sâu — chọn MongoDB. Pitfall: nhiều team chọn MongoDB vì 'flexible' rồi implement manual joins trong code — tệ hơn cả SQL.",
    q_en: "What are the advantages and disadvantages of MongoDB?",
    a_en: "Advantages: flexible schema (no need to define structure upfront), native JSON storage (convenient for JavaScript/Node.js), horizontal scaling with built-in sharding, aggregation pipeline for complex data processing, replica set for high availability. Disadvantages: does not support multi-document ACID transactions by default (supported since version 4.0 but complex), memory-intensive compared to SQL, no JOIN support (must denormalize or use $lookup which is slower), consistency is weaker than traditional RDBMS. MongoDB is best suited for applications with frequently changing schemas or naturally hierarchical/document-structured data.",
  },
  {
    id: 729,
    category: "Database",
    subcategory: "ORM",
    level: "intermediate",
    q: "ORM (Object-Relational Mapper) là gì? Lợi ích và nhược điểm?",
    a: "ORM là layer ánh xạ database tables thành objects trong code, cho phép thao tác database bằng programming language thay vì SQL. Hai patterns chính: Active Record (model tự chứa DB logic, ví dụ Rails, Sequelize) và Data Mapper (tách domain object khỏi DB logic, ví dụ TypeORM DataMapper mode, Prisma). Lợi ích: type-safety, migration management (version-controlled schema changes), database abstraction (switch DB dễ hơn), boilerplate reduction. Nhược điểm: abstraction leaky — ORM generate SQL không optimal, ví dụ Prisma đôi khi tạo subquery thay vì JOIN hiệu quả hơn; N+1 query problem nếu không cẩn thận; khó debug khi cần hiểu SQL được generate; overhead runtime. Raw query escape hatch quan trọng: Prisma có $queryRaw, Sequelize có sequelize.query() — nên dùng cho queries phức tạp cần kiểm soát. Migration management: Prisma Migrate, Flyway, Liquibase giúp track và apply schema changes theo đúng thứ tự.",
    q_en: "What is Prisma and what are its main advantages over other ORMs?",
    a_en: "Prisma is a next-generation ORM for Node.js and TypeScript with three main components: Prisma Client (type-safe auto-generated query builder), Prisma Migrate (schema migration management), and Prisma Studio (GUI for browsing data). Advantages over traditional ORMs: auto-generated TypeScript types from schema (no runtime errors), intuitive query API (not SQL strings), built-in migration system, supports multiple databases (PostgreSQL, MySQL, SQLite, MongoDB, SQL Server). Compared to TypeORM: less boilerplate, better type safety. Compared to Sequelize: TypeScript-first design. Main limitation: the generated Prisma Client can be slow with complex queries due to abstraction layers.",
  },
  {
    id: 730,
    category: "Database",
    subcategory: "ORM",
    level: "intermediate",
    q: "Prisma ORM là gì? Tại sao phổ biến trong Node.js/TypeScript ecosystem?",
    a: "Prisma là ORM thế hệ mới cho Node.js/TypeScript với schema-first approach. Điểm mạnh: Prisma Client auto-generated từ schema.prisma — mỗi lần chạy `prisma generate` tạo ra TypeScript types hoàn toàn accurate với database schema; Prisma Migrate cho migrations với SQL migration files có thể review; Prisma Studio là GUI để browse data. Internals quan trọng: Prisma dùng query engine viết bằng Rust (binary được download khi install) — giúp performance tốt và type-safe query builder. Preview features như Prisma Accelerate (connection pooling + query caching ở edge) và Prisma Pulse (real-time database events). So với Sequelize/TypeORM: Prisma có DX tốt hơn, type safety mạnh hơn (không cần decorators), nhưng không hỗ trợ tốt complex inheritance patterns. Khi schema thay đổi, phải chạy `prisma generate` để sync TypeScript types — bước quan trọng trong CI/CD pipeline.",
    q_en: "What is Sequelize and how does it compare to Prisma?",
    a_en: "Sequelize is a mature, promise-based ORM for Node.js supporting PostgreSQL, MySQL, MariaDB, SQLite, and SQL Server. It uses the Model definition pattern (define class → map to table) and supports associations, transactions, hooks, and migrations. Compared to Prisma: Sequelize is more battle-tested and has a larger ecosystem; Prisma has better TypeScript support and a more modern API. Sequelize is suitable when: the project already uses Sequelize and migration is costly, or the team prefers an ActiveRecord-style pattern. Prisma is recommended for new TypeScript projects due to its developer experience and type safety.",
  },
  {
    id: 731,
    category: "Database",
    subcategory: "ORM",
    level: "intermediate",
    q: "Prisma schema file hoạt động như thế nào?",
    a: "schema.prisma là single source of truth với ba sections: datasource (kết nối DB), generator (config Prisma Client), và models. Relations syntax: @relation('PostToUser', fields: [authorId], references: [id]) cho foreign key; implicit M2M relations dùng junction table tự động. Enums được định nghĩa trực tiếp và map đến DB enums hoặc strings. @map/@db directives quan trọng: @map('user_name') đổi field name trong DB trong khi giữ camelCase trong code; @db.VarChar(255) chỉ định DB-specific type. Multi-schema support (preview): datasource db { schemas: ['public', 'auth'] } cho multi-tenant apps. prisma format tự động format và sort fields. `prisma migrate dev` tạo SQL migration files có thể review và edit trước khi apply — quan trọng khi cần migration phức tạp như data migration.",
    q_en: "What is Mongoose and what is its role in Node.js applications?",
    a_en: "Mongoose is an ODM (Object Document Mapper) for MongoDB in Node.js. It provides: Schema (defines the structure and types of documents), Model (interface for querying the collection), Validation (built-in and custom), Middleware/Hooks (pre/post save, update), and Population (similar to JOIN but in application layer). Advantages: structure and validation for otherwise schema-less MongoDB; TypeScript support via @types/mongoose or the newer mongoose TypeScript API. Disadvantages: adds overhead; complex queries can be awkward compared to native MongoDB driver. Mongoose is suitable for applications that want both MongoDB flexibility and some level of schema enforcement.",
  },
  {
    id: 732,
    category: "Database",
    subcategory: "ORM",
    level: "intermediate",
    q: "N+1 query problem trong ORM là gì? Cách giải quyết trong Prisma?",
    a: "N+1 problem: query 1 lần lấy list N items (1 query), rồi loop qua từng item để fetch related data (N queries) — tổng N+1 queries. Ví dụ: lấy 100 users rồi lấy posts của từng user = 101 queries thay vì 1. Cách phát hiện: enable query logging trong Prisma với `log: ['query']` trong PrismaClient constructor — đếm số queries per request. Giải quyết trong Prisma: dùng `include` để eager load (prisma.user.findMany({ include: { posts: true } })) — Prisma chạy 2 queries và merge; dùng `select` với nested select để lấy chỉ fields cần thiết; Prisma fluent API: user.posts() chạy lazy load — tránh trong loops. DataLoader pattern cho GraphQL: batch nhiều individual loads thành một query duy nhất, ví dụ DataLoader của Facebook group tất cả userId requests trong một tick rồi batch fetch. Prisma chưa có built-in DataLoader nhưng có thể dùng @paljs/plugins hoặc tự implement.",
    q_en: "What is Redis and what are its common use cases?",
    a_en: "Redis (Remote Dictionary Server) is an in-memory data structure store supporting strings, lists, sets, sorted sets, hashes, streams, and more. Common use cases: Caching — store database query results or API responses (set with TTL); Session management — store user sessions faster than database; Rate limiting — use INCR + EXPIRE to count requests; Pub/Sub messaging — real-time event broadcast; Job queues — with libraries like Bull or BullMQ; Leaderboards — sorted sets for ranking. Redis is extremely fast (sub-millisecond latency) because all data is stored in memory. For persistence, Redis supports RDB snapshots and AOF (Append Only File) logging.",
  },
  {
    id: 733,
    category: "Database",
    subcategory: "ORM",
    level: "intermediate",
    q: "Sequelize là gì? So sánh với Prisma?",
    a: "Sequelize là ORM lâu đời cho Node.js, hỗ trợ MySQL, PostgreSQL, SQLite, MSSQL, MariaDB. Migration system: sequelize-cli tạo migration files với up/down functions — mạnh nhưng verbose. Hooks (lifecycle events): beforeCreate, afterCreate, beforeUpdate, beforeDestroy — dùng cho audit logging, password hashing. Scopes: defaultScope (applied to all queries), named scopes như User.scope('active') — tái sử dụng query conditions. Transaction support: sequelize.transaction(async (t) => { await Model.create({...}, { transaction: t }) }). Tại sao teams migrate away: TypeScript types không accurate (cần sequelize-typescript), verbose model definitions với decorators, runtime errors khó debug, DX kém so với Prisma. Nên dùng Sequelize khi maintain legacy codebase hoặc cần tính năng như polymorphic associations mà Prisma chưa hỗ trợ.",
    q_en: "What is the N+1 query problem and how do you solve it?",
    a_en: "The N+1 query problem occurs when fetching a list of N items causes N additional queries for related data — for example, fetching 100 posts then running 100 separate queries to get each post's author. Solutions: Eager loading with JOIN — load related data in a single query; Dataloader (batching + caching) — popular in GraphQL, batches multiple individual queries into one; ORM include/with options — Prisma's include, Sequelize's include, or Mongoose's populate; Database-level optimization — materialized views or denormalization. The N+1 problem is one of the most common performance issues in ORM-based applications.",
  },
  {
    id: 734,
    category: "Database",
    subcategory: "ORM",
    level: "intermediate",
    q: "Mongoose là gì? Schema trong Mongoose hoạt động như thế nào?",
    a: "Mongoose là ODM (Object Document Mapper) cho MongoDB trong Node.js, giúp thêm structure vào schemaless MongoDB. Middleware (hooks) có 4 loại: document middleware (pre/post save, validate, remove — `this` là document), query middleware (pre/post find, findOne, update — `this` là query), aggregate middleware (pre/post aggregate), model middleware. Virtuals: computed properties không lưu trong DB — ví dụ fullName computed từ firstName + lastName, hay URL computed từ slug; có thể include trong JSON với toJSON({ virtuals: true }). Populate: thay thế ObjectId references bằng actual documents — populate('author') fetch User document; nhưng cẩn thận N+1, dùng populate với select để limit fields. lean(): trả về plain JavaScript objects thay vì Mongoose documents — nhanh hơn 2-3x vì bỏ overhead của Mongoose document methods; dùng khi chỉ cần read-only data. Pitfall: Mongoose buffering commands trước khi kết nối — cần handle connection errors properly.",
    q_en: "What is Drizzle ORM and what distinguishes it from Prisma?",
    a_en: "Drizzle ORM is a lightweight, type-safe TypeScript ORM with SQL-like syntax. Key differences from Prisma: Schema is defined in TypeScript code (not a .prisma file), queries are SQL-like (closer to raw SQL), significantly smaller bundle size (important for Edge/Serverless), no code generation step (Prisma generates a client), and better performance in edge environments. Drizzle is suitable for: Cloudflare Workers / Vercel Edge, projects where bundle size matters, teams who prefer SQL-like syntax. Prisma is more mature, has better tooling (Studio, migrations), and is the safer choice for complex enterprise projects.",
  },
  {
    id: 735,
    category: "Database",
    subcategory: "NoSQL",
    level: "intermediate",
    q: "Redis là gì? Use cases phổ biến của Redis?",
    a: "Redis là in-memory data store với rich data structures và persistence tùy chọn. Data structure use cases cụ thể: Sorted Set (ZADD, ZRANGE) cho leaderboard — score tự động sorted, query top-N O(log N); Pub/Sub (SUBSCRIBE, PUBLISH) cho real-time notifications và chat; List (LPUSH/RPOP) cho job queue hoặc activity feed; HyperLogLog cho approximate unique count với fixed memory; Streams (XADD, XREAD) cho event sourcing, message queue có consumer groups. Persistence: RDB (snapshot tại intervals — nhanh hơn cho restart, có thể mất data nếu crash giữa snapshots); AOF (Append-Only File — log mọi write operation, recovery tốt hơn nhưng file lớn hơn); RDB+AOF kết hợp cho best of both worlds. Redis Cluster: horizontal sharding tự động với 16384 hash slots, minimum 3 master nodes — đảm bảo availability khi node fail. Redis Sentinel: high availability cho single-master setup với automatic failover.",
    q_en: "What are the strategies for embedding and referencing documents in MongoDB?",
    a_en: "Two main strategies in MongoDB: Embedding (denormalization) — store related data as nested documents within a single document. Suitable when: data is always queried together, has a 1-to-1 or 1-to-few relationship, and the embedded data does not exceed 16MB (MongoDB document limit). Referencing (normalization) — store ObjectId referencing another collection. Suitable when: data has many-to-many relationships, the related data is large or changes frequently, or the embedded approach would cause document size issues. In practice, MongoDB applications mix both: embed frequently accessed data (e.g., product details in order items) and reference data that is independently managed (e.g., userId referencing the users collection).",
  },
  {
    id: 736,
    category: "Database",
    subcategory: "NoSQL",
    level: "intermediate",
    q: "Khi nào nên dùng Redis làm cache?",
    a: "Dùng Redis cache khi: data đọc nhiều hơn ghi, computation expensive (complex DB queries, API calls), cần reduce database load, data có thể stale trong thời gian ngắn. Chiến lược: Cache-aside/Lazy loading (check cache → miss → fetch DB → write cache); Write-through (write cache + DB đồng thời — consistent nhưng write latency cao); Write-behind/Write-back (write cache → async write DB — nhanh nhưng risk data loss). Cache stampede problem: nhiều requests hit cache cùng lúc khi cache expire, all go to DB — giải quyết bằng mutex lock (SET key value NX EX 30 để lock), hoặc probabilistic early expiration (expire sớm với xác suất nhỏ trước TTL thật). Memory management: maxmemory-policy quan trọng — allkeys-lru (evict least-recently-used) cho cache, volatile-lru (chỉ evict keys có TTL), noeviction (trả error khi full — dùng cho session storage). Monitoring: redis-cli INFO stats để xem hit rate, connected clients, memory usage; target cache hit rate >80% là healthy.",
    q_en: "What is TypeORM and what are its main features?",
    a_en: "TypeORM is an ORM for TypeScript and JavaScript supporting Active Record and Data Mapper patterns, compatible with many databases (PostgreSQL, MySQL, MariaDB, SQLite, MongoDB, SQL Server). Main features: Entity decorators (@Entity, @Column, @PrimaryGeneratedColumn), Relationships (@OneToMany, @ManyToMany), QueryBuilder for complex queries, Migrations for schema management, supports both TypeScript decorators and plain classes. TypeORM is popular in NestJS projects because of its deep integration with the NestJS module system. Drawbacks: less intuitive than Prisma, TypeScript support is less consistent, and some edge cases in the query builder can be confusing.",
  },
  {
    id: 737,
    category: "Database",
    subcategory: "NoSQL",
    level: "advanced",
    q: "MongoDB aggregation pipeline là gì?",
    a: "Aggregation pipeline là series of stages xử lý documents tuần tự, output của stage này là input của stage tiếp theo. Stages phổ biến: $match (filter — đặt đầu pipeline để tận dụng index), $group (group và aggregate với $sum, $avg, $push), $project (reshape, add computed fields), $sort/$limit/$skip, $lookup (LEFT JOIN với collection khác). Pipeline optimization: đặt $match và $project sớm nhất có thể để giảm số documents xử lý ở stages sau; $match sau $unwind là anti-pattern. $facet: chạy nhiều pipeline stages song song trên cùng input, trả về multiple result sets trong một query — phù hợp faceted search (count by category + count by price range simultaneously). $graphLookup: recursive lookup cho graph/tree data, ví dụ organizational hierarchy hay category tree với maxDepth control. Atlas Aggregation Builder: GUI tool trong MongoDB Atlas cho phép build pipeline visually và export code. Explain plan: db.collection.aggregate([...]).explain('executionStats') để kiểm tra query plan và indexes used.",
    q_en: "What is database indexing and what types of indexes are commonly used?",
    a_en: "An index is a data structure that improves query speed at the cost of additional storage and slower write operations. Common types: B-Tree index (default) — suitable for range queries, ORDER BY, and equality checks; Hash index — suitable only for equality checks, faster than B-Tree for this case; Composite index — index on multiple columns, column order matters (leading column rule); Partial index — indexes only rows matching a condition; Full-text index — for text search; Unique index — enforces uniqueness. When to add an index: columns in WHERE, JOIN ON, ORDER BY, GROUP BY clauses, and columns with high cardinality (many distinct values). Avoid over-indexing — each index adds overhead to INSERT/UPDATE/DELETE operations.",
  },
  {
    id: 738,
    category: "Database",
    subcategory: "ORM",
    level: "advanced",
    q: "Database transactions trong Prisma: cách sử dụng?",
    a: "Prisma hỗ trợ hai loại transactions: Sequential transactions với prisma.$transaction([op1, op2]) — chạy tuần tự, rollback toàn bộ nếu bất kỳ operation nào fail, không có conditional logic. Interactive transactions với prisma.$transaction(async (tx) => { const user = await tx.user.create({...}); if (user.role === 'admin') { await tx.adminProfile.create({...}); } }) — cho phép conditional logic, dùng tx thay cho prisma để operations nằm trong cùng transaction. Isolation levels: Prisma hỗ trợ chỉ định isolation level prisma.$transaction([...], { isolationLevel: Prisma.TransactionIsolationLevel.Serializable }). Nested writes như implicit transactions: prisma.user.create({ data: { profile: { create: {...} } } }) tự động wrap trong transaction. Timeout configuration: prisma.$transaction([...], { timeout: 5000 }) mặc định 5s — tăng khi cần. Distributed transaction limitations: Prisma không hỗ trợ transactions spanning multiple databases — cần 2-phase commit protocol hoặc saga pattern cho microservices.",
    q_en: "What is database replication and what types are there?",
    a_en: "Database replication is the process of copying data from one database server (primary) to one or more other servers (replicas/secondaries). Types: Master-Slave (Primary-Replica) — one writable primary, reads distributed to replicas, replicas are read-only; Master-Master — multiple writable nodes, requires conflict resolution; Synchronous replication — primary waits for replica confirmation before returning success (strong consistency, lower performance); Asynchronous replication — primary responds immediately, replica updates lag behind (higher performance, potential data loss). Common uses: read scaling (distribute read traffic), high availability (automatic failover when primary fails), geographic distribution (replicas in different regions for lower latency).",
  },
  {
    id: 739,
    category: "Database",
    subcategory: "ORM",
    level: "intermediate",
    q: "Drizzle ORM là gì? So sánh với Prisma?",
    a: "Drizzle là TypeScript ORM nhẹ với SQL-like query builder — viết queries gần với SQL nhưng type-safe: db.select().from(users).where(eq(users.age, 25)).orderBy(desc(users.createdAt)). Schema định nghĩa bằng TypeScript với type inference tự động: const users = pgTable('users', { id: serial('id').primaryKey(), name: text('name').notNull() }). Prepared statements: db.select().from(users).where(eq(users.id, sql.placeholder('id'))).prepare('getUserById') — compile một lần, execute nhiều lần với different params, nhanh hơn. Migration generation: drizzle-kit generate tạo SQL migration từ schema changes, drizzle-kit push để apply. Edge runtime compatibility: không có native binary như Prisma engine — bundle size nhỏ hơn nhiều (~100KB vs Prisma ~40MB), chạy tốt trên Cloudflare Workers, Vercel Edge. Performance: benchmark cho thấy Drizzle nhanh hơn Prisma 2-5x cho simple queries do ít abstraction layers. Hạn chế: DX kém hơn Prisma (verbose hơn), community nhỏ hơn, một số advanced features còn thiếu.",
    q_en: "What is database sharding and when should it be used?",
    a_en: "Sharding is a horizontal scaling technique that splits data across multiple database instances (shards) based on a shard key. Each shard holds a subset of the data. Types of sharding strategies: Range-based — split by value range (e.g., user IDs 1-1M on shard 1, 1M-2M on shard 2); Hash-based — apply hash function to shard key for uniform distribution; Directory-based — use a lookup table to determine which shard holds which data. Use sharding when: a single database cannot handle the data volume or write throughput. Challenges: cross-shard queries are complex and slow, resharding is difficult, transactions across shards require distributed transaction protocols. Sharding should be considered only after vertical scaling and read replicas have been exhausted.",
  },
  {
    id: 740,
    category: "Database",
    subcategory: "NoSQL",
    level: "intermediate",
    q: "Document embedding vs referencing trong MongoDB: khi nào dùng cái nào?",
    a: "Embedding (nested document): dùng khi data được truy cập cùng nhau thường xuyên (one query, no joins), quan hệ 1:1 hoặc 1:few, data ít thay đổi, subdocuments không cần exist độc lập. Giới hạn quan trọng: MongoDB document size limit là 16MB — embedding quá nhiều dẫn đến hit limit. Referencing (separate documents với ObjectId): dùng khi data lớn, quan hệ many-to-many, data được truy cập/update độc lập — nhưng cần $lookup (LEFT JOIN) hoặc application-side join, tốn thêm round trip. Data duplication trade-off: embedding duplicate data (ví dụ user info trong mỗi order document) — read nhanh hơn nhưng update tốn công (phải update nhiều documents). Schema design patterns: Bucket pattern — nhóm time-series data vào buckets (ví dụ nhóm sensor readings theo giờ thay vì một document per reading) để giảm document count; Outlier pattern — handle edge cases riêng khi 90% documents nhỏ nhưng 10% có thể rất lớn (ví dụ celebrity posts có millions of likes). $lookup performance: thêm index trên lookup field, tránh $lookup trong large collections không có index.",
    q_en: "What is the CAP theorem and how does it apply to database selection?",
    a_en: "The CAP theorem states that a distributed system can only guarantee two of the three following properties simultaneously: Consistency (C) — all nodes see the same data at the same time; Availability (A) — every request receives a response (not necessarily the latest data); Partition Tolerance (P) — the system continues operating even when network partitions occur. Since network partitions are unavoidable in distributed systems, the real choice is between CP and AP. CP systems (MongoDB, HBase, Zookeeper) — sacrifice availability to ensure consistency. AP systems (Cassandra, CouchDB, DynamoDB) — sacrifice strict consistency for availability. Traditional SQL databases (PostgreSQL, MySQL) prioritize consistency but are not designed for distributed partitions. Understanding CAP helps choose the right database for each use case.",
  },

  // === DATABASE: Advanced (741-750) ===
  {
    id: 741,
    category: "Database",
    subcategory: "Advanced",
    level: "advanced",
    q: "ACID properties trong database là gì?",
    a: "ACID đảm bảo database transactions đáng tin cậy. Atomicity: transaction thành công toàn bộ hoặc rollback toàn bộ — ví dụ bank transfer trừ $100 từ account A và cộng $100 vào account B phải là một đơn vị; nếu bước 2 fail, bước 1 phải rollback. Consistency: data luôn ở trạng thái hợp lệ theo constraints — ví dụ foreign key constraint đảm bảo không thể tạo order với non-existent userId. Isolation: concurrent transactions không thấy intermediate state của nhau — mức độ isolation khác nhau (READ COMMITTED, SERIALIZABLE). Durability: committed data không mất dù server crash — PostgreSQL dùng WAL (Write-Ahead Log) để ghi log trước khi apply changes, có thể replay để recover. Implementation khác nhau: PostgreSQL full ACID; MySQL InnoDB hỗ trợ ACID, MyISAM không; MongoDB 4.0+ ACID multi-document transactions nhưng performance cao hơn single-document operations. BASE (Basically Available, Soft state, Eventual consistency) là alternative cho NoSQL systems như Cassandra — ưu tiên availability và performance over strict consistency, phù hợp cho hệ thống cần scale cực lớn.",
    q_en: "What are ACID properties and why are they important in databases?",
    a_en: "ACID stands for four properties that guarantee database transaction reliability: Atomicity — a transaction is all-or-nothing; if any step fails, all changes are rolled back; Consistency — the database moves from one valid state to another, enforcing all constraints; Isolation — concurrent transactions do not interfere with each other; Durability — once committed, data survives even system crashes. ACID is critical for: financial transactions (money transfers), order processing, any operation where partial failure would leave data in an inconsistent state. SQL databases (PostgreSQL, MySQL) are strongly ACID-compliant. Some NoSQL databases sacrifice some ACID properties for performance and scalability.",
  },
  {
    id: 742,
    category: "Database",
    subcategory: "Advanced",
    level: "advanced",
    q: "Database transactions là gì? Cách sử dụng trong SQL?",
    a: "Transaction là nhóm operations thực hiện như một đơn vị atomic. Cú pháp: BEGIN; UPDATE accounts SET balance = balance - 100 WHERE id = 1; UPDATE accounts SET balance = balance + 100 WHERE id = 2; COMMIT; — hoặc ROLLBACK nếu có lỗi. Savepoints cho partial rollback: SAVEPOINT sp1; ... nếu lỗi ROLLBACK TO SAVEPOINT sp1; — có thể rollback về điểm giữa trong transaction mà không rollback toàn bộ, hữu ích trong stored procedures phức tạp. Transaction log (WAL — Write-Ahead Log): PostgreSQL ghi changes vào WAL trước khi apply vào actual data files — đảm bảo durability và cho phép Point-in-Time Recovery. Long-running transaction risks: giữ locks lâu dẫn đến lock escalation (row locks upgrade lên table lock), blocking other transactions, tăng replication lag, vacuum bị block trong PostgreSQL gây table bloat. Deadlock detection: database tự detect deadlock (circular wait), kill một transaction và trả error 40P01 trong PostgreSQL — application phải retry. Best practice: keep transactions ngắn, không làm I/O operations (HTTP calls, file reads) bên trong transaction.",
    q_en: "What is a database transaction and how do you manage it in Node.js?",
    a_en: "A database transaction is a group of operations that must all succeed or all fail together. In Node.js with Prisma: prisma.$transaction([op1, op2, op3]) — sequential execution; prisma.$transaction(async (tx) => { ... }) — interactive transaction with full control. With Sequelize: sequelize.transaction(async (t) => { ... }) — pass t as the transaction object to each query. Key principles: always rollback on error (try/catch), keep transactions short to minimize lock time, avoid long-running operations within a transaction. Nested transactions use SAVEPOINTs. Distributed transactions (across multiple databases) require 2-Phase Commit (2PC) protocol — complex and best avoided through careful system design.",
  },
  {
    id: 743,
    category: "Database",
    subcategory: "Advanced",
    level: "advanced",
    q: "Transaction isolation levels là gì? Các levels là gì?",
    a: "Isolation levels kiểm soát visibility giữa concurrent transactions và loại anomaly có thể xảy ra. Dirty Read (READ UNCOMMITTED): transaction A đọc data chưa committed của transaction B — B sau đó rollback, A đã đọc data sai; không có DB production nào recommend dùng. Non-repeatable Read (READ COMMITTED — default PostgreSQL): transaction A đọc row, transaction B update và commit, A đọc lại cùng row thấy giá trị khác. Phantom Read (REPEATABLE READ — default MySQL InnoDB): transaction A query với WHERE condition, B insert row mới thỏa condition và commit, A query lại thấy row mới (phantom). SERIALIZABLE: không có anomaly nào — transactions chạy như tuần tự, highest isolation, lowest concurrency. PostgreSQL implementation dùng MVCC (Multi-Version Concurrency Control): mỗi transaction thấy snapshot của database tại thời điểm bắt đầu — readers không block writers và ngược lại; PostgreSQL MVCC giúp READ COMMITTED và REPEATABLE READ không cần shared read locks. Serializable Snapshot Isolation (SSI) trong PostgreSQL: detect serialization conflicts mà không cần exclusive locks — ít deadlocks hơn traditional 2PL. Practical advice: dùng READ COMMITTED cho hầu hết cases, REPEATABLE READ khi cần consistent reads trong transaction, SERIALIZABLE chỉ khi thực sự cần.",
    q_en: "What are transaction isolation levels?",
    a_en: "Isolation levels control how much concurrent transactions can see each other's uncommitted changes: Read Uncommitted — can read dirty data (uncommitted changes), rarely used; Read Committed — only reads committed data, prevents dirty reads but allows non-repeatable reads; Repeatable Read — same query returns same results within a transaction, prevents dirty reads and non-repeatable reads; Serializable — highest isolation, transactions appear to execute sequentially, prevents all anomalies but is slowest. PostgreSQL defaults to Read Committed. MySQL InnoDB defaults to Repeatable Read. Higher isolation = fewer concurrency issues but more locking and reduced throughput. Choose the appropriate level based on the specific use case.",
  },
  {
    id: 744,
    category: "Database",
    subcategory: "Advanced",
    level: "advanced",
    q: "Database indexing strategies: B-tree index, Hash index, GIN index khác nhau như thế nào?",
    a: "B-tree (default): balanced tree structure, phù hợp equality (=), range queries (<, >, BETWEEN), ORDER BY, prefix LIKE 'abc%'. Là lựa chọn đúng cho 95% trường hợp. Hash: chỉ equality checks — lý thuyết nhanh hơn B-tree cho equality nhưng không thể dùng cho range, không được replicated trong PostgreSQL cũ (fixed 10+). GIN (Generalized Inverted Index): array containment, full-text search (tsvector), JSONB operators (@>, ?) — mỗi phần tử trong array/document được index riêng, build chậm hơn nhưng queries trên multi-value columns cực nhanh. GiST: geometric data (PostGIS), range types, full-text search — linh hoạt hơn GIN nhưng chậm hơn cho tìm kiếm đơn giản. BRIN (Block Range Index): rất nhỏ, cho time-series data có natural correlation với physical storage (ví dụ timestamp columns trong append-only tables) — index size chỉ vài KB thay vì GB. Expression indexes: CREATE INDEX ON orders (LOWER(email)) — index tính toán expression, hữu ích khi query luôn dùng LOWER(). Partial indexes với WHERE clause: CREATE INDEX ON orders (user_id) WHERE status = 'pending' — index nhỏ hơn, update nhanh hơn, rất hiệu quả khi query luôn filter theo condition cố định. Index on expressions và partial indexes thường bị bỏ qua nhưng có thể improve performance 10-100x cho specific query patterns.",
    q_en: "What is connection pooling and why is it important?",
    a_en: "Connection pooling maintains a pool of reusable database connections instead of creating a new connection for each request. Creating a new DB connection takes time (10-100ms) and consumes resources on both the application and database sides. A connection pool creates N connections upfront and reuses them. In Node.js: pg (node-postgres) has built-in pooling; Prisma uses connection pooling by default; Sequelize has its own pool. Configuration parameters: min (minimum connections), max (maximum connections), idle timeout (close idle connections after N ms). In serverless environments, each function instance creates its own pool — use external poolers like PgBouncer or Prisma Data Proxy to prevent exceeding database connection limits.",
  },
  {
    id: 745,
    category: "Database",
    subcategory: "Advanced",
    level: "advanced",
    q: "Database replication là gì? Primary-replica architecture hoạt động như thế nào?",
    a: "Replication copy data từ primary sang một hoặc nhiều replicas để scale read throughput và high availability. Streaming replication (PostgreSQL): primary liên tục stream WAL records đến replicas real-time — replica lag thường dưới 1 giây trong normal conditions; đo lag bằng pg_stat_replication view. Logical replication: replicate specific tables hoặc row changes (không toàn bộ WAL) — cho phép selective replication, version upgrades zero-downtime, replicate sang different schema. Synchronous vs asynchronous: sync replication (synchronous_standby_names) đảm bảo replica nhận data trước khi commit confirm — zero data loss nhưng write latency tăng (cần ít nhất một replica online); async replication confirm ngay, replica có thể lag vài giây — risk mất data nếu primary crash. Replication lag measurement: SELECT now() - pg_last_xact_replay_timestamp() trên replica. Failover automation: Patroni là HA solution phổ biến cho PostgreSQL — dùng etcd/ZooKeeper/Consul để distributed consensus, tự động promote replica thành primary khi primary fail, update DNS/HAProxy để redirect traffic. Read-after-write consistency issue: sau khi write đến primary, immediate read từ replica có thể không thấy data mới do replication lag — cần route reads sau writes đến primary hoặc implement lag-aware routing.",
    q_en: "What are the types of indexes and how do you choose the right one?",
    a_en: "B-Tree (default) — supports range queries (<, >, BETWEEN), ORDER BY, and equality checks. Suitable for most cases. Hash — only supports equality checks (=), faster than B-Tree for this case but not supported in all databases. Composite index (multi-column) — index on multiple columns; column order matters — the leftmost column rule: (a, b, c) index can be used for queries on a, (a, b), or (a, b, c) but not just b or c alone. Partial index — indexes only rows matching a specific condition (e.g., WHERE deleted_at IS NULL), reduces index size. Expression index — index on the result of a function (e.g., LOWER(email)). Full-text index — for text search queries. GiST/GIN (PostgreSQL) — for complex types like arrays, JSON, geographic data.",
  },
  {
    id: 746,
    category: "Database",
    subcategory: "Advanced",
    level: "advanced",
    q: "Database sharding là gì? Khi nào cần sharding?",
    a: "Sharding là chia data ngang (horizontal partitioning) ra nhiều database servers, mỗi shard chứa một tập con data — scale beyond single server capacity. Shard key selection là quyết định quan trọng nhất: chọn sai gây hot spots (một shard nhận phần lớn traffic). Ví dụ: sharding by user_id với hash — distribute evenly; sharding by country — có thể hot spot nếu US users chiếm 80% traffic. Hot spots: timestamp làm shard key cho time-series data — mọi writes vào shard mới nhất (sequential hotspot); giải quyết bằng add random suffix hoặc dùng hash. Range-based sharding: user 1-1M trên shard 1, 1M-2M trên shard 2 — range queries efficient nhưng dễ hot spot. Hash-based: hash(user_id) % num_shards — distribute evenly nhưng range queries không efficient. Resharding challenges: khi cần thêm shard phải redistribute data — tốn thời gian và có downtime risk; consistent hashing giảm số keys cần redistribute khi thêm/bỏ node. Cross-shard queries: JOIN across shards không native — phải application-level join (scatter-gather), chậm và phức tạp. Alternatives trước khi sharding: vertical scaling (bigger server), read replicas cho read-heavy workloads, caching layer (Redis), table partitioning (same server), archiving old data. Sharding chỉ cần thiết khi dataset thực sự quá lớn (thường >1-2TB hoặc write throughput vượt quá single master).",
    q_en: "What is full-text search and how is it different from LIKE queries?",
    a_en: "LIKE '%keyword%' cannot use indexes (except prefix patterns like 'keyword%'), scans the entire table, and does not understand language semantics. Full-text search uses inverted indexes to find documents containing specific words. Features: stemming (searching 'run' also finds 'running', 'ran'), ranking results by relevance, stop words (ignoring common words like 'the', 'and'), and phrase search. In PostgreSQL: use tsvector + tsquery, or the built-in full_text_search configuration. Dedicated search engines like Elasticsearch or Meilisearch offer more advanced features: fuzzy matching, typo tolerance, faceted search, and better multilingual support. Use full-text search when LIKE is too slow or insufficient for user search requirements.",
  },
  {
    id: 747,
    category: "Database",
    subcategory: "Advanced",
    level: "advanced",
    q: "CAP theorem là gì? Ý nghĩa trong database selection?",
    a: "CAP theorem: trong distributed database, chỉ có thể đảm bảo tối đa 2 trong 3 properties: Consistency (mọi node thấy data giống nhau), Availability (mọi request nhận response), Partition Tolerance (hoạt động khi network partition). Thực tế P luôn cần, nên chọn CP hoặc AP. MongoDB tùy cấu hình: default gần AP, với writeConcern/readConcern majority → CP. Cassandra thiên AP.",
    q_en: "What are optimistic and pessimistic locking?",
    a_en: "Pessimistic locking assumes conflicts will happen and locks the resource before reading — SELECT ... FOR UPDATE. Other transactions must wait until the lock is released. Suitable when conflicts are frequent and the cost of rollback is high. Optimistic locking assumes conflicts are rare, does not lock on read, and instead checks at write time whether data has changed (usually via a version column). If data has changed, the transaction is rolled back and retried. Suitable when conflicts are rare and reads are far more frequent than writes. In Prisma/Sequelize, optimistic locking is implemented by adding a version field and checking it in the WHERE clause of the UPDATE. Optimistic locking is generally preferred in web applications due to its better performance and scalability.",
  },
  {
    id: 748,
    category: "Database",
    subcategory: "Advanced",
    level: "advanced",
    q: "Connection pooling trong database là gì? Tại sao quan trọng?",
    a: "Connection pooling duy trì pool of reusable database connections thay vì tạo connection mới cho mỗi request. Tạo connection tốn 20-100ms (TCP handshake, SSL negotiation, auth, session setup) — với 1000 requests/s việc tạo new connection mỗi lần là catastrophic. PgBouncer: connection pooler phổ biến nhất cho PostgreSQL, đứng giữa app và DB, hỗ trợ 3 modes: Session (connection held for entire client session), Transaction (connection returned to pool after each transaction — recommended), Statement (per-statement, most aggressive nhưng không support multi-statement transactions). Pool sizing formula: connections = (core_count * 2) + effective_spindle_count — ví dụ server 4 cores với SSD: 4*2+1 = 9 connections; lớn hơn không phải lúc nào cũng tốt hơn vì context switching overhead. Prisma connection pool: DATABASE_URL='postgresql://...?connection_limit=10&pool_timeout=20' — connection_limit per Prisma instance. Serverless challenges: mỗi Lambda/Edge function có pool riêng — với 1000 concurrent invocations = 1000 * pool_size connections đến DB, vượt quá PostgreSQL max_connections (default 100). Giải quyết: PgBouncer ở transaction mode, Prisma Accelerate (managed connection pooling), Neon serverless driver với HTTP-based connections không cần persistent TCP.",
    q_en: "What is database partitioning and how does it differ from sharding?",
    a_en: "Partitioning splits a large table into smaller physical pieces on the same database server. Types: Range partitioning — split by value range (e.g., partition orders by year); List partitioning — split by specific values (e.g., partition by region); Hash partitioning — distribute evenly using a hash function. Benefits: better query performance (only relevant partitions are scanned), easier maintenance (archive old partitions). Sharding splits data across multiple database servers. Key differences: partitioning is within a single database server, sharding spans multiple servers; partitioning is transparent to the application, sharding requires application-level handling. Partitioning is simpler and should be considered before sharding when the problem is query performance rather than storage or write capacity.",
  },
  {
    id: 749,
    category: "Database",
    subcategory: "Advanced",
    level: "advanced",
    q: "Full-text search trong database là gì? PostgreSQL vs Elasticsearch?",
    a: "Full-text search tìm kiếm trong text content với stemming (running → run), stop words removal, và relevance ranking. PostgreSQL built-in: tsvector là processed document representation (to_tsvector('english', 'The quick brown fox') → 'brown':3 'fox':4 'quick':2); tsquery là search query (to_tsquery('quick & fox')). GIN index trên tsvector column cho fast lookups: CREATE INDEX ON articles USING GIN(to_tsvector('english', content)). Ranking với ts_rank(tsvector, tsquery) trả về float score. Đủ cho moderate needs (< vài triệu documents, single language, không cần fuzzy matching). Elasticsearch: inverted index — mỗi term map đến list of documents chứa term đó, cực nhanh cho text search. Distributed, horizontal scaling, advanced relevance scoring (BM25 algorithm), rich query DSL (fuzzy, phrase, wildcard, geospatial). Nhưng operational complexity cao: cần maintain Elasticsearch cluster, sync data từ primary DB. Typesense và Meilisearch: alternatives đơn giản hơn Elasticsearch — dễ setup, typo-tolerant search built-in, REST API friendly, tốt cho product search, documentation search. Quyết định: dùng PostgreSQL FTS nếu search là secondary feature và dataset nhỏ; Meilisearch/Typesense cho product search với good DX; Elasticsearch khi cần distributed, advanced analytics, log aggregation (ELK stack).",
    q_en: "What are the strategies for database backup and recovery?",
    a_en: "Backup types: Full backup — backup entire database, slowest but simplest to restore; Incremental backup — backup only changes since the last backup, faster but complex to restore; Differential backup — backup changes since the last full backup, a compromise between the two. Backup strategies: Point-in-Time Recovery (PITR) — use WAL (PostgreSQL) or binlog (MySQL) to restore to any point in time; Regular automated backups (daily full + continuous incremental); Test restore regularly — backup is useless if restoration is never tested; Off-site storage — backup to a different geographic location; Retention policy — determine how long to retain backups. RPO (Recovery Point Objective) defines acceptable data loss; RTO (Recovery Time Objective) defines acceptable downtime — these drive backup strategy decisions.",
  },
  {
    id: 750,
    category: "Database",
    subcategory: "Advanced",
    level: "advanced",
    q: "Optimistic locking vs Pessimistic locking trong database là gì?",
    a: "Pessimistic locking: lock record khi đọc với SELECT FOR UPDATE — ngăn other transactions modify cho đến khi transaction hiện tại commit/rollback. An toàn nhưng giảm concurrency, risk deadlock nếu không cẩn thận về lock order. Row-level vs table-level: PostgreSQL default là row-level locks (SELECT FOR UPDATE); table-level lock với LOCK TABLE — rất restrictive, tránh dùng. SELECT FOR UPDATE SKIP LOCKED: bỏ qua rows đã locked — pattern quan trọng cho job queue, tránh nhiều workers pick cùng job. Advisory locks: application-level locks không gắn với specific rows — pg_try_advisory_lock(key) trả về boolean, hữu ích cho distributed coordination (ví dụ chỉ một cron job chạy tại một thời điểm). Optimistic locking: không lock, thêm version column (INTEGER) hoặc updated_at timestamp; khi update: UPDATE ... WHERE id = ? AND version = ? — nếu 0 rows affected thì concurrent modification đã xảy ra, retry. Phù hợp read-heavy, low contention. Lock timeout configuration: SET lock_timeout = '5s' tránh query chờ lock vô tận. Monitoring locks trong PostgreSQL: SELECT * FROM pg_locks JOIN pg_stat_activity USING (pid) WHERE NOT granted — thấy queries đang chờ lock; pg_blocking_pids(pid) trả về PIDs đang block query.",
    q_en: "What is query optimization and what techniques are used?",
    a_en: "Query optimization involves identifying and improving slow database queries. Techniques: EXPLAIN/EXPLAIN ANALYZE — analyze the execution plan to find bottlenecks (seq scan vs index scan); Add missing indexes — on columns in WHERE, JOIN ON, ORDER BY; Rewrite queries — avoid SELECT *, use specific columns; reduce subqueries by using JOINs; Avoid N+1 queries — use eager loading; Optimize JOINs — join on indexed columns, filter data before joining; Pagination — use LIMIT/OFFSET or cursor-based pagination; Caching — cache frequent expensive queries with Redis; Denormalization — reduce complex JOINs by storing computed data; Partitioning — divide large tables for faster scanning. Profiling tools: pg_stat_statements (PostgreSQL), slow query log (MySQL), Prisma query events.",
  },

  // === BUILD TOOLS (751-765) ===
  {
    id: 751,
    category: "Build Tools",
    subcategory: "Webpack",
    level: "intermediate",
    q: "Webpack là gì? Vai trò trong modern web development?",
    a: "Webpack là module bundler tổng hợp tất cả assets (JS, CSS, images) thành bundle files tối ưu. Core concepts: Entry — điểm bắt đầu của dependency graph (thường src/index.js); Output — nơi emit bundles (dist/bundle.js); Loaders — transform file types thành modules (babel-loader chuyển JSX, css-loader parse CSS imports); Plugins — can thiệp vào build lifecycle (HtmlWebpackPlugin tạo HTML, MiniCssExtractPlugin tách CSS ra file riêng, DefinePlugin inject constants); Mode (development/production) — tự động apply optimizations. Dev server: webpack-dev-server serve bundles in-memory, HMR thay module mà không reload page. Code splitting với dynamic import(): Webpack tự tạo chunk files, load on demand. Tại sao đang bị thay thế: config quá phức tạp (webpack.config.js dài hàng trăm dòng), dev server chậm vì bundle toàn bộ app khi start và khi HMR — Vite/Turbopack giải quyết vấn đề này với native ESM. Vẫn phổ biến: nhiều enterprise projects, Create React App (eject), custom build pipelines cần flexibility cao.",
    q_en: "What is Webpack and what role does it play in frontend development?",
    a_en: "Webpack is a static module bundler for JavaScript applications. It builds a dependency graph starting from entry points, then bundles all modules (JS, CSS, images, fonts) into output files for the browser. Core concepts: Entry — the starting point; Output — where to write the bundled files; Loaders — transform non-JS files (TypeScript via ts-loader, CSS via css-loader); Plugins — more complex tasks (HtmlWebpackPlugin, MiniCssExtractPlugin); Mode (development/production) — enables different optimizations. In production mode, Webpack performs minification, tree shaking, and code splitting. Although Vite has become more popular for new projects, Webpack remains dominant in many existing enterprise applications due to its maturity and rich plugin ecosystem.",
  },
  {
    id: 752,
    category: "Build Tools",
    subcategory: "Webpack",
    level: "intermediate",
    q: "Loaders và Plugins trong Webpack khác nhau như thế nào?",
    a: "Loaders transform file types thành modules — hoạt động ở file level trong module resolution pipeline. Execution order: right-to-left (bottom-to-top trong config array) — ví dụ ['style-loader', 'css-loader', 'sass-loader'] chạy sass-loader trước. Loaders phổ biến: babel-loader (ES6+/JSX→ES5), css-loader (parse @import và url()), style-loader (inject CSS vào DOM), sass-loader (SCSS→CSS), file-loader/asset modules (handle images/fonts), ts-loader/swc-loader (TypeScript). Custom loader là function nhận source string, transform, return new string — đơn giản implement. Plugins can thiệp vào Webpack compiler lifecycle qua hooks (beforeRun, emit, done). Plugins phổ biến: HtmlWebpackPlugin (generate HTML với injected script tags), MiniCssExtractPlugin (tách CSS thành files riêng thay vì inject vào JS), TerserPlugin (minify JS), DefinePlugin (thay constants lúc build — NODE_ENV), BundleAnalyzerPlugin (visualize bundle). Custom plugin phức tạp hơn loader: phải implement apply(compiler) method và tap vào compiler hooks. Rule of thumb: cần transform file type → loader; cần can thiệp vào build process rộng hơn → plugin.",
    q_en: "What is Vite and why is it faster than Webpack in development?",
    a_en: "Vite is a modern frontend build tool that takes advantage of native ES Modules in browsers. In development mode, Vite does not bundle — it serves source files directly and only transforms them when requested. This makes startup nearly instantaneous regardless of project size (Webpack must bundle everything first). Hot Module Replacement (HMR) is also much faster because only the changed module is updated. In production mode, Vite uses Rollup to bundle for optimal output. Vite natively supports TypeScript, JSX, CSS Modules, and PostCSS without additional configuration. Vite is the current recommendation for new React, Vue, and Svelte projects. Create React App (CRA) which was based on Webpack is now deprecated in favor of Vite-based alternatives.",
  },
  {
    id: 753,
    category: "Build Tools",
    subcategory: "Vite",
    level: "beginner",
    q: "Vite là gì? Tại sao nhanh hơn Webpack trong development?",
    a: "Vite là build tool thế hệ mới của Evan You. Dev server không bundle — browser request file nào thì Vite transform file đó on-demand qua native ESM, cold start gần như instant bất kể project size. esbuild pre-bundling: lần đầu start, Vite dùng esbuild (Rust-based, 10-100x nhanh hơn JS bundlers) để bundle node_modules thành single module — tránh browser phải request hàng nghìn files nhỏ; result được cache trong node_modules/.vite. Native ESM serving: source files served dưới dạng ES modules trực tiếp, browser handle dependency graph — chỉ transform file cần thiết cho request hiện tại. Production: dùng Rollup để bundle vì Rollup có tree shaking tốt hơn và output nhỏ hơn esbuild cho production; cũng dùng esbuild để minify (nhanh hơn Terser). Plugin ecosystem: Vite plugins compatible với Rollup plugin API — vite-plugin-react (React Fast Refresh), @vitejs/plugin-vue, vite-plugin-pwa. Framework integrations: Vite là foundation cho SvelteKit, Qwik, Astro, và Remix (một phần). HMR: chỉ invalidate module thay đổi và direct importers — không re-process toàn bộ bundle graph.",
    q_en: "What is Babel and what is its role in modern JavaScript development?",
    a_en: "Babel is a JavaScript compiler (transpiler) that converts modern JavaScript (ES2015+) and JSX into backwards-compatible code that runs in older browsers. Core functionality: transform new syntax (arrow functions, optional chaining, async/await) to ES5 equivalents; transform JSX to React.createElement() calls; support TypeScript (strips types, no type checking); apply polyfills for new APIs (Promise, Array.from) via @babel/polyfill or core-js. Babel uses a plugin system — each transform is a separate plugin, and presets group common plugins (@babel/preset-env, @babel/preset-react, @babel/preset-typescript). With the rise of SWC and esbuild (faster Rust/Go-based alternatives), Babel is gradually being replaced in new projects, but remains widely used in existing codebases.",
  },
  {
    id: 754,
    category: "Build Tools",
    subcategory: "Vite",
    level: "intermediate",
    q: "Sự khác biệt giữa Vite dev server và Webpack dev server?",
    a: "Cold start comparison: Webpack dev server bundle toàn bộ module graph trước khi server ready — project 1000 modules có thể mất 10-30 giây; Vite start trong <1 giây bất kể project size vì không pre-bundle source. Dependency pre-bundling: Vite dùng esbuild pre-bundle node_modules một lần (cached), browser chỉ cần load vài files thay vì hàng nghìn; Webpack bundle tất cả cùng nhau. HMR mechanism differences: Webpack HMR phải re-compile affected module và dependencies, có thể mất 1-10 giây với large modules; Vite HMR chỉ invalidate changed module và propagate lên import chain, thường <100ms. Vite precision: Vite biết chính xác file nào thay đổi và chỉ update đúng phần đó; React Fast Refresh với Vite preserve component state khi update. Trade-off: Vite dev (ESM) và production (Rollup bundled) có thể có behavior khác nhau — potential inconsistencies với circular imports hay module loading order. Large projects: Vite advantage càng rõ khi project lớn hơn — Webpack cold start tăng linear với module count, Vite gần như constant. Turbopack (Next.js 13+): Vercel's Webpack successor viết bằng Rust, cạnh tranh trực tiếp với Vite về speed trong dev mode.",
    q_en: "What is SWC and how does it compare to Babel?",
    a_en: "SWC (Speedy Web Compiler) is a Rust-based JavaScript/TypeScript compiler designed to replace Babel. SWC is 20-70x faster than Babel due to being written in Rust and using parallel processing. SWC supports: TypeScript and JSX transformation, modern JavaScript syntax transpilation, and minification. Next.js uses SWC as its default compiler (replaces Babel since Next.js 12). Vite uses esbuild (also written in Go) for fast pre-bundling. Compared to Babel: SWC has a smaller plugin ecosystem, but for most standard use cases (TypeScript, JSX, modern JS), SWC is sufficient and much faster. The main tradeoff is that Babel has more community plugins for specific custom transformations.",
  },
  {
    id: 755,
    category: "Build Tools",
    subcategory: "Babel",
    level: "intermediate",
    q: "Babel là gì? Vai trò trong JavaScript toolchain?",
    a: "Babel là JavaScript compiler với 3-phase pipeline: Parse (source code → AST via @babel/parser), Transform (plugins traverse và modify AST — ví dụ arrow function plugin chuyển () => {} thành function() {}), Generate (AST → output code via @babel/generator). Polyfills vs transforms: transforms là syntax changes (const → var, class → prototype); polyfills là runtime additions cho missing APIs (Promise, Array.from, fetch) — Babel transforms syntax nhưng không tự polyfill APIs. core-js là polyfill library chuẩn — @babel/preset-env với useBuiltIns: 'usage' tự động import chỉ polyfills cần thiết dựa trên browserslist config. browserslist config (trong package.json hoặc .browserslistrc): target browsers list như '> 0.5%, last 2 versions, not dead' — preset-env compile chỉ những features không được target browsers hỗ trợ natively, giảm output size. Plugin order: plugins chạy trước presets, plugins first-to-last, presets last-to-first (legacy behavior). Babel vs SWC vs esbuild: Babel chậm nhất (JS) nhưng most customizable plugin ecosystem; SWC nhanh hơn 20-70x (Rust), ít plugins hơn; esbuild nhanh nhất nhưng không hỗ trợ custom transforms.",
    q_en: "What is pnpm and how does it differ from npm and yarn?",
    a_en: "pnpm (performant npm) is a fast, disk-efficient package manager. Key differences: Disk space — pnpm uses a content-addressable storage (CAS) system with hard links, so packages are stored only once globally across all projects; Speed — installation is faster due to fewer disk writes; Strict mode — by default, packages cannot access dependencies they did not explicitly declare (avoids phantom dependencies); Workspace support — excellent monorepo support with pnpm workspaces. Compared to npm: pnpm is faster and uses less disk space. Compared to yarn: similar features but pnpm has stricter dependency isolation. pnpm is widely adopted in large projects and monorepos (Turborepo recommends pnpm). The main drawback is that some poorly configured packages may break due to strict mode.",
  },
  {
    id: 756,
    category: "Build Tools",
    subcategory: "SWC",
    level: "intermediate",
    q: "SWC là gì? So sánh với Babel?",
    a: "SWC (Speedy Web Compiler) là Rust-based JS/TS compiler — nhanh hơn Babel 20-70x single-thread, 70x với multi-core parallelization. Next.js adoption: từ Next.js 12, SWC thay thế Babel cho transpilation và Terser cho minification — cold build 5x nhanh hơn, HMR 3x nhanh hơn; tự động detect và migrate từ Babel config khi có .babelrc. Compatibility: SWC hỗ trợ hầu hết Babel transforms — preset-env equivalents, TypeScript, JSX, decorators. Plugin system limitations: SWC plugins viết bằng WebAssembly (WASM) — khó implement hơn Babel plugins (JS), ecosystem nhỏ hơn nhiều; một số advanced Babel transforms (custom macros, babel-plugin-macros) chưa có SWC equivalent. @swc/core dùng trực tiếp trong Node.js scripts, swc-loader cho Webpack, @vitejs/plugin-react có thể dùng SWC. Khi không thể dùng SWC: project phụ thuộc vào Babel plugins đặc biệt (babel-plugin-styled-components transformation, babel-plugin-import for tree shaking), hoặc cần custom transforms. Deno sử dụng SWC internally cho TypeScript transpilation.",
    q_en: "What is CI/CD and how is it applied in frontend projects?",
    a_en: "CI/CD (Continuous Integration/Continuous Deployment) is a set of practices for automating the software development pipeline. CI (Continuous Integration): developers push code frequently, the system automatically runs tests, type checks, and linting; catches errors early before they reach production. CD (Continuous Deployment/Delivery): automatically deploys code to staging/production after all checks pass. In frontend projects: GitHub Actions, GitLab CI, or CircleCI run build → test → lint on each pull request; successful merges to main automatically deploy to Vercel/Netlify; preview deployments for each PR enable review before merging. Key benefits: faster feedback, fewer integration issues, consistent deployments. Configuration is stored in .github/workflows/*.yml or similar files.",
  },
  {
    id: 757,
    category: "Build Tools",
    subcategory: "Package Managers",
    level: "beginner",
    q: "pnpm khác npm và yarn như thế nào?",
    a: "pnpm dùng content-addressable store: mỗi package version lưu một lần trong global store (~/.pnpm-store), tất cả projects symlink đến store — tiết kiệm disk đáng kể (100 projects dùng React chỉ lưu 1 copy). node_modules structure: pnpm tạo non-flat node_modules — mỗi package chỉ thấy dependencies được khai báo trong package.json của nó (strict mode). Phantom dependencies: npm/yarn hoist packages lên root node_modules, code có thể import packages không khai báo trong dependencies — chạy được locally nhưng fail ở nơi khác; pnpm strict mode ngăn phantom deps bằng cách không hoist. Workspace protocol: pnpm-workspace.yaml khai báo workspace packages; 'my-lib': 'workspace:*' trong dependencies để ref local packages — pnpm tự resolve khi install, thay bằng version thật khi publish. Performance: pnpm nhanh hơn npm vì hardlinks, parallel installs; tương đương hoặc nhanh hơn Yarn. Strict mode có thể break legacy code dùng phantom deps — dùng shamefully-hoist: true hoặc .npmrc để workaround. pnpm ngày càng phổ biến cho monorepos (Turborepo + pnpm là stack phổ biến).",
    q_en: "What is Docker and what are the benefits of containerization?",
    a_en: "Docker is a containerization platform that packages an application and all its dependencies into a standardized unit (container). Benefits: Consistency — the container runs the same way in development, staging, and production; Isolation — no conflicts between different applications with different dependencies; Portability — runs on any machine with Docker installed; Scalability — easily scale by running multiple containers; Resource efficiency — lighter than VMs, containers share the host OS kernel. Key concepts: Image — blueprint for creating containers; Container — running instance of an image; Dockerfile — instructions for building an image; Docker Compose — defines and runs multi-container applications. In frontend/Node.js projects, Docker is used to containerize API servers and run services like PostgreSQL and Redis locally during development.",
  },
  {
    id: 758,
    category: "Build Tools",
    subcategory: "CI/CD",
    level: "intermediate",
    q: "CI/CD là gì? Sự khác biệt giữa CI và CD?",
    a: "CI (Continuous Integration): tự động build và test code mỗi khi push hoặc PR, phát hiện lỗi sớm khi còn dễ fix. CD (Continuous Delivery): tự động deploy đến staging sau CI thành công — production deploy cần manual approval; Continuous Deployment: tự động deploy đến production không cần intervention. Pipeline stages điển hình: Lint → Type check → Unit tests → Build → Integration tests → Deploy staging → Smoke tests → Deploy production. Environment promotion: code di chuyển qua dev → staging → production — mỗi stage có environment variables riêng, không share secrets giữa environments. Rollback strategies: blue-green (switch traffic back), feature flags (disable feature không cần redeploy), database migration rollback (down migrations — tại sao mỗi migration cần down script). Feature flags: deploy code mới nhưng disable feature — gradually enable cho users, instant rollback không cần redeploy, A/B testing. Tools: GitHub Actions (tích hợp tốt với GitHub, marketplace), GitLab CI (built-in, tốt cho self-hosted), Jenkins (flexible nhưng phức tạp maintain), CircleCI, Buildkite (fast self-hosted runners). Secrets management: không commit secrets vào code — dùng GitHub Secrets, HashiCorp Vault, AWS Secrets Manager; rotate secrets định kỳ.",
    q_en: "What is a monorepo and what tools are used to manage it?",
    a_en: "A monorepo is a single repository containing multiple related projects (packages/apps). Benefits: code sharing between packages, atomic commits across projects, consistent tooling and configurations, easier dependency management. Tools: Turborepo — optimized task runner with intelligent caching for JavaScript/TypeScript monorepos; Nx — more feature-rich with code generation, dependency graph analysis; pnpm workspaces — package manager-level workspace support; Lerna (less popular now). A typical monorepo structure: apps/ (Next.js apps, APIs), packages/ (shared UI components, utilities, types). Key challenges: build time (solved by Turborepo caching), IDE performance with many packages, and managing cross-package dependencies correctly.",
  },
  {
    id: 759,
    category: "Build Tools",
    subcategory: "CI/CD",
    level: "intermediate",
    q: "GitHub Actions là gì? Cấu trúc của một workflow file?",
    a: "GitHub Actions là CI/CD platform tích hợp trong GitHub, trigger bởi events (push, PR, schedule, workflow_dispatch). Workflow YAML trong .github/workflows/ với cấu trúc: on (triggers), jobs (chạy parallel mặc định, dùng needs: để sequential), steps (actions hoặc run commands). Matrix strategy: chạy job với nhiều combinations — matrix: { node: [18, 20], os: [ubuntu-latest, windows-latest] } tạo 4 parallel jobs tự động; dùng để test cross-platform/cross-version. Caching: actions/cache để cache node_modules, build artifacts — key dựa trên hash của package-lock.json để invalidate khi dependencies thay đổi; significant speedup cho install steps. Secrets: lưu trong GitHub repo/org Settings → Secrets, access bằng ${{ secrets.MY_SECRET }} — không bao giờ log secrets. Reusable workflows: workflow A có thể call workflow B với uses: ./.github/workflows/shared.yml — tránh duplicate CI logic across repos. Self-hosted runners: chạy Actions trên máy chủ riêng — tiết kiệm cost cho large workloads, access internal resources, custom hardware (GPU, ARM). OIDC tokens: GitHub Actions có thể auth với AWS/GCP/Azure mà không cần long-lived credentials — best practice cho cloud deployments. Environments với required reviewers: production deployment cần approval từ reviewer trước khi proceed.",
    q_en: "What is tree shaking and how does it work?",
    a_en: "Tree shaking is a dead code elimination technique used by bundlers (Webpack, Rollup, Vite) to remove unused exports from the final bundle. It works based on ES Module static imports (import/export) which allow the bundler to statically analyze the dependency graph and determine which exports are never used. Example: if you import { Button } from 'ui-library' and the library has 100 components, tree shaking ensures only Button ends up in the bundle. Requirements for effective tree shaking: use ES Modules (not CommonJS require()); libraries must set sideEffects: false in package.json (or list files with side effects); avoid importing entire libraries (import _ from 'lodash' vs import { debounce } from 'lodash/debounce'). Tree shaking significantly reduces bundle size and improves application loading performance.",
  },
  {
    id: 760,
    category: "Build Tools",
    subcategory: "Docker",
    level: "intermediate",
    q: "Docker là gì? Tại sao hữu ích trong development và deployment?",
    a: "Docker tạo isolated containers chứa app và dependencies. Containers vs VMs: VMs virtualize hardware (heavyweight, có OS riêng, boot minutes); containers share host OS kernel (lightweight, start milliseconds, MB vs GB). Layered filesystem (Union FS): Docker image là stack of read-only layers, container thêm writable layer trên cùng — layers được cache và shared giữa images, kéo image nhanh khi layers đã có. Copy-on-write: khi container modify file từ image layer, Docker copy file lên writable layer trước khi modify. Networking: Bridge network (default, containers communicate qua virtual network); Host network (container dùng host network stack trực tiếp, performance tốt hơn); Overlay network (multi-host, dùng trong Docker Swarm/K8s). Volumes: bind mounts (link host directory vào container cho dev mode); named volumes (Docker-managed storage, persist data cho database); tmpfs mounts (in-memory, không persist). docker-compose: define multi-container apps với YAML — services, networks, volumes; docker-compose up để start toàn bộ stack; essential cho local development với database + app + cache. Production: Docker + Kubernetes (K8s) cho orchestration, auto-scaling, self-healing; Docker + ECS/Cloud Run cho managed container services.",
    q_en: "What is blue-green deployment and what are its advantages?",
    a_en: "Blue-green deployment is a release strategy that maintains two identical production environments: Blue (current live version) and Green (new version). The process: deploy the new version to Green; run tests on Green; switch traffic from Blue to Green; if issues arise, switch back to Blue (rollback in seconds). Advantages: zero downtime deployments — users are not disrupted during deployment; instant rollback — just switch traffic back; complete production testing before going live; eliminates deployment risk. Compared to rolling deployment (gradually replacing instances), blue-green requires double the infrastructure cost but provides cleaner rollbacks. Many CI/CD platforms (Vercel, AWS, Heroku) support blue-green deployments. Database migrations are the most challenging part — they must be backward-compatible with both versions.",
  },
  {
    id: 761,
    category: "Build Tools",
    subcategory: "Docker",
    level: "intermediate",
    q: "Dockerfile là gì? Các instructions cơ bản?",
    a: "Dockerfile là script định nghĩa cách build Docker image. Layer caching optimization: Docker cache mỗi instruction layer — nếu layer không thay đổi, dùng cache từ previous build. Đặt instructions ít thay đổi trước: FROM → COPY package*.json → RUN npm install → COPY . . — nếu source code thay đổi nhưng package.json không, Docker reuse cached npm install layer (tiết kiệm 1-2 phút). .dockerignore: exclude node_modules, .git, .env, build artifacts — tránh copy không cần thiết vào build context, giảm build time. Security — non-root user: mặc định container chạy root (risk nếu có vulnerability); thêm RUN addgroup -S appgroup && adduser -S appuser -G appgroup; USER appuser trước CMD — principle of least privilege. Health checks: HEALTHCHECK --interval=30s --timeout=3s CMD wget -q -O /dev/null http://localhost:3000/health || exit 1 — Docker/K8s biết container có healthy không để route traffic. CMD vs ENTRYPOINT: ENTRYPOINT định nghĩa executable không thể override (docker run image args — args được pass vào ENTRYPOINT); CMD là default args có thể override; dùng kết hợp ENTRYPOINT ['node'] CMD ['server.js'] — có thể đổi file chạy nhưng không đổi runtime. Multi-line RUN với && và cleanup: RUN apt-get update && apt-get install -y ... && rm -rf /var/lib/apt/lists/* — clean trong cùng layer tránh layer to.",
    q_en: "What is Jest and what are its main features?",
    a_en: "Jest is a JavaScript testing framework developed by Facebook, widely used for testing React applications and Node.js backends. Main features: Zero configuration — works out of the box with Create React App, Next.js, and most Node.js setups; Snapshot testing — captures component output and detects unexpected changes; Mocking system — mock modules, functions, and timers; Code coverage — built-in coverage reports; Watch mode — runs only changed tests. Jest uses jsdom to simulate a browser environment for component testing. @testing-library/react is commonly used alongside Jest for testing React components in a more user-centric way. Key APIs: describe(), it()/test(), expect() with matchers, beforeEach/afterEach hooks.",
  },
  {
    id: 762,
    category: "Build Tools",
    subcategory: "Docker",
    level: "advanced",
    q: "Multi-stage builds trong Docker là gì? Lợi ích?",
    a: "Multi-stage builds dùng nhiều FROM statements để tách build environment khỏi runtime image — production image không chứa build tools, source code, dev dependencies. Node.js example: stage 1 (builder): FROM node:20 AS builder; COPY package*.json ./; RUN npm ci; COPY . .; RUN npm run build — compile TypeScript, bundle assets. Stage 2 (runtime): FROM node:20-alpine AS runtime; COPY --from=builder /app/dist ./dist; COPY --from=builder /app/node_modules ./node_modules; USER node; CMD ['node', 'dist/server.js'] — chỉ copy artifacts cần thiết, alpine image (~5MB vs ~900MB full node). Image size reduction: từ 1GB (node:20 với dev deps + source + build tools) xuống 150MB (node:20-alpine với chỉ production deps + built artifacts). Security benefits: attack surface giảm vì không có build tools (TypeScript compiler, webpack, etc.) trong production image — ít potential vulnerabilities hơn. Build arguments: ARG NODE_ENV=production cho phép customize behavior per stage — ARG có thể truyền từ docker build --build-arg. Named stages với AS: có thể copy từ specific stage bằng tên — COPY --from=builder; có thể build đến specific stage: docker build --target builder để debug build issues.",
    q_en: "What is Vitest and what advantages does it have over Jest?",
    a_en: "Vitest is a modern testing framework built on Vite, designed to be fast and compatible with Jest APIs. Advantages over Jest: Speed — uses Vite for transformation (SWC/esbuild), much faster than Jest with Babel; Native ESM support — no need for complex configuration; Vite config reuse — uses the same vite.config.ts, no need for separate Jest config; TypeScript support without additional setup; Hot Module Replacement for tests in watch mode. The API is almost identical to Jest (describe, it, expect, vi instead of jest), making migration straightforward. Vitest is recommended for new Vite-based projects. For Next.js projects, Jest with SWC compiler is still a solid choice, although Vitest is increasingly supported.",
  },
  {
    id: 763,
    category: "Build Tools",
    subcategory: "Monorepo",
    level: "advanced",
    q: "Monorepo là gì? Công cụ phổ biến để quản lý monorepo?",
    a: "Monorepo là single repository chứa nhiều packages/apps — ngược với polyrepo (mỗi service có repo riêng). Lợi ích: code sharing dễ dàng (shared UI library, utils), atomic commits across packages (thay đổi API và consumer trong một commit), unified tooling và CI, dễ refactor cross-package. Task orchestration: Turborepo và Nx chạy tasks (build, test, lint) song song và theo dependency order — nếu package B depends on package A, A phải build trước B. Affected commands: Nx có nx affected --target=test chỉ chạy tests cho packages bị ảnh hưởng bởi changes — critical cho CI performance trong large monorepos. Caching: Turborepo cache build outputs locally và remotely (Vercel Remote Cache) — nếu inputs không thay đổi, reuse cache thay vì rebuild; có thể chia sẻ cache giữa developers và CI. Internal packages: packages/ui, packages/utils không publish lên npm, chỉ dùng trong repo — workspace:* protocol trong pnpm để reference. Versioning: Changesets tool quản lý versioning và changelogs cho publishable packages. Trade-offs monorepo: repo size lớn, CI phức tạp hơn, cần tooling investment; polyrepo dễ hơn cho independent teams và deployments. Turborepo vs Nx: Turborepo đơn giản hơn, Nx có nhiều features hơn (generators, devtools, cloud).",
    q_en: "What is Cypress and what type of testing does it support?",
    a_en: "Cypress is an end-to-end (E2E) testing framework that runs tests directly in the browser. Key features: Real browser testing — tests run in Chrome/Firefox/Edge, not a simulated environment; Time travel — screenshots at each step for debugging; Automatic waiting — no need to write explicit waits, Cypress automatically retries assertions; Network interception — cy.intercept() to mock API calls; Component testing — test individual components in isolation (since Cypress 10). Cypress is suitable for: testing user flows from end to end (login → checkout → confirmation), API testing, visual regression testing. Limitations: only supports JavaScript/TypeScript, can be slow for large test suites, parallel execution requires Cypress Cloud (paid). Playwright is increasingly preferred as an alternative for cross-browser testing.",
  },
  {
    id: 764,
    category: "Build Tools",
    subcategory: "Webpack",
    level: "advanced",
    q: "Tree shaking là gì? Điều kiện để tree shaking hoạt động?",
    a: "Tree shaking là dead code elimination — loại bỏ unused exports khỏi bundle dựa trên static analysis của import/export statements. Mechanism: bundler build dependency graph, mark exports nào được imported ở đâu, eliminate exports không được referenced — chỉ possible với ES modules (static structure) vì CommonJS require() là dynamic (runtime). Điều kiện để tree shaking hoạt động: phải dùng ES modules (import/export không phải require/module.exports); modules phải side-effect free — library phải khai báo sideEffects: false trong package.json (nếu không Webpack giữ lại toàn bộ module vì không chắc chắn); không có dynamic imports của cùng module. sideEffects field: sideEffects: false nói với bundler 'không có file nào trong package này có side effects khi import' — safe to tree shake; sideEffects: ['*.css', './src/setup.js'] exclude specific files. /*#__PURE__*/ annotation: đánh dấu function call không có side effects — bundler có thể eliminate nếu result không được used: const result = /*#__PURE__*/ createComponent() — phổ biến trong React compiled output. Pitfall: nhiều utility libraries vẫn dùng CommonJS (lodash) — không tree-shakeable; dùng lodash-es (ESM version) hoặc import từ specific path: import debounce from 'lodash/debounce'. Named exports tree-shake tốt hơn default exports vì bundler biết chính xác tên được dùng.",
    q_en: "What is Playwright and how does it compare to Cypress?",
    a_en: "Playwright is an E2E testing framework from Microsoft supporting Chromium, Firefox, and WebKit (Safari). Advantages over Cypress: True cross-browser testing — including WebKit (Safari), which Cypress does not support; Multi-tab/multi-origin — can test flows spanning multiple domains; Better performance — runs tests in parallel across browsers by default; Python, Java, .NET APIs — not limited to JavaScript; Auto-waiting — smart element waiting similar to Cypress. Playwright is preferred when: Safari testing is required, tests span multiple domains, or performance at scale is critical. Cypress is preferred when: the team is already familiar with it, debugging with time travel is a priority, or component testing is a key use case. Both are mature, production-ready tools.",
  },
  {
    id: 765,
    category: "Build Tools",
    subcategory: "CI/CD",
    level: "advanced",
    q: "Blue-Green deployment và Canary deployment là gì?",
    a: "Blue-Green deployment: duy trì hai production environments (blue=current live, green=new version). Deploy lên green, chạy integration tests, rồi switch traffic tại load balancer (nginx upstream, AWS ALB target groups, Kubernetes ingress) — switch instant, không có downtime. Rollback instant: nếu issue phát hiện sau deploy, switch traffic back về blue — green vẫn còn đó. Implementation với load balancer: Nginx upstream block với weight; AWS ALB forward rules; Kubernetes service selector switch. Cost: phải maintain 2 environments song song — expensive cho large infrastructure. Database migrations: challenge lớn nhất — migration phải backward-compatible với cả blue và green code (không thể drop column trong green nếu blue vẫn dùng). Canary deployment: deploy phiên bản mới cho một phần nhỏ users (1-5%), monitor metrics (error rate, latency, business metrics), rồi dần tăng percentage nếu ổn. Metrics cần monitor: error rate tăng đột biến, P99 latency tăng, conversion rate giảm, custom business metrics (orders/minute). Rollback trigger: tự động rollback nếu error rate vượt threshold. Implementation: Kubernetes với Flagger hoặc Argo Rollouts tự động canary với metric-based promotion; feature flags như LaunchDarkly cho canary ở application level. A/B testing vs Canary: A/B test new feature/UI cho subset users để measure business impact; Canary là risk reduction strategy cho infrastructure changes — có thể kết hợp cả hai.",
    q_en: "What are TDD and BDD and what is the difference between them?",
    a_en: "TDD (Test-Driven Development): write a failing test first, then write just enough code to make it pass, then refactor. Cycle: Red → Green → Refactor. Focus is on implementation units (functions, classes). BDD (Behavior-Driven Development): extends TDD by writing tests in natural language describing user behavior. Uses Given-When-Then format: Given (initial state) → When (action) → Then (expected outcome). Tools: Cucumber, Jest with describe/it syntax, Cypress with natural language test names. BDD improves communication between developers, QA, and business stakeholders because tests describe user-visible behavior rather than internal implementation. In practice, many teams use TDD discipline (write tests first) with BDD-style descriptions (natural language test names) for the best of both approaches.",
  },

  // === TESTING (766-780) ===
  {
    id: 766,
    category: "Testing",
    subcategory: "Jest",
    level: "beginner",
    q: "Jest là gì? Các tính năng chính của Jest?",
    a: "Jest là JavaScript testing framework từ Meta, phổ biến nhất trong ecosystem. Zero-config với Create React App và Next.js; TypeScript support qua ts-jest hoặc babel-jest. Test isolation: mỗi test file chạy trong separate Node.js environment (jsdom hoặc node), global state không leak giữa files. Parallel execution: Jest chạy test files song song qua worker pool, isolate failures tốt. Snapshot testing: serialize output và so sánh với .snap files — detect unexpected UI changes. Mocking system: jest.fn(), jest.mock(), jest.spyOn() — mock modules, timers (jest.useFakeTimers), và globals. Code coverage: --coverage flag generate Istanbul reports (statements, branches, functions, lines). Watch mode: --watchAll hoặc --watch chỉ re-run tests affected by changes. Matchers: toBe (===), toEqual (deep), toMatchObject (partial match), toThrow, toHaveBeenCalledWith, resolves/rejects cho async. Setup files: jest.config.js với setupFilesAfterEach để configure Testing Library, mock global APIs. Tích hợp với React Testing Library: render, screen, fireEvent, userEvent — test behavior không implementation details.",
    q_en: "What is the test pyramid and what does it mean in practice?",
    a_en: 'The test pyramid is a model for structuring automated tests with three layers: Unit tests (base) — fast, isolated, many tests, cover individual functions/components; Integration tests (middle) — test interaction between components/services, moderate quantity; E2E tests (apex) — test full user flows in real browsers, fewest in number but highest confidence. The pyramid shape indicates: write many unit tests (cheap, fast), fewer integration tests, and very few E2E tests (expensive, slow). In practice, the test pyramid prevents over-relying on slow E2E tests (the "ice cream cone" anti-pattern). A healthy project: 70% unit tests (Jest/Vitest), 20% integration tests, 10% E2E tests (Playwright/Cypress). Each layer serves a different purpose — unit tests verify logic, integration tests verify contracts, E2E tests verify user workflows.',
  },
  {
    id: 767,
    category: "Testing",
    subcategory: "Jest",
    level: "beginner",
    q: "describe, it/test, expect trong Jest được dùng như thế nào?",
    a: "describe nhóm related tests thành suite (có thể nest nhiều cấp). it/test định nghĩa test case — tên test nên đọc như documentation: it('should return 404 when user not found'). expect tạo assertion với matchers: toBe (strict ===, dùng cho primitives), toEqual (deep equality, dùng cho objects/arrays), toMatchObject (partial object match — chỉ check specified keys), toContain (array/string contains), toHaveLength, toBeNull/toBeUndefined/toBeTruthy/toBeFalsy, toThrow (function throw error), toHaveBeenCalledWith (mock assertion). Async matchers: await expect(promise).resolves.toBe(value) hoặc await expect(promise).rejects.toThrow(). beforeEach/afterEach chạy trước/sau mỗi test trong describe block — dùng cho setup/teardown (tạo test data, cleanup database, reset mocks). beforeAll/afterAll chạy một lần cho cả describe block — dùng cho expensive setup (start test server, database connection). test.only/describe.only: chỉ chạy selected tests — hữu ích khi debug nhưng đừng commit. test.skip: bỏ qua test tạm thời với reason. test.each: parameterized tests với multiple input/output combinations.",
    q_en: "What is snapshot testing and when should you use it?",
    a_en: "Snapshot testing captures the rendered output of a component (or any serializable value) and stores it as a .snap file. On subsequent test runs, Jest compares the current output to the stored snapshot and fails if there is any difference. Use cases: detect unexpected UI changes in components, test serialization logic, test complex object structures. Advantages: easy to write, automatically detects regressions. Disadvantages: developers tend to blindly update snapshots (jest --updateSnapshot) without reviewing changes, making snapshots meaningless; large snapshots are hard to review; they break with every intentional UI change. Best practice: use snapshots for stable, rarely changing components; for dynamic or complex UIs, prefer explicit assertions with @testing-library/react queries (getByRole, getByText) which are more meaningful and maintainable.",
  },
  {
    id: 768,
    category: "Testing",
    subcategory: "Jest",
    level: "intermediate",
    q: "Mocking trong Jest: jest.fn(), jest.mock(), jest.spyOn() dùng như thế nào?",
    a: "jest.fn() tạo mock function tracking calls, arguments, return values — mockReturnValue(val) cho sync, mockResolvedValue(val) cho Promise, mockRejectedValue(err) cho rejected Promise, mockImplementation(fn) cho custom logic. jest.mock('module-path') auto-mock toàn bộ module (tất cả exports thành jest.fn()); với factory function jest.mock('./api', () => ({ fetchUser: jest.fn() })) cho control toàn diện; hoisting: jest.mock() được hoisted lên đầu file tự động. jest.spyOn(obj, 'method') wrap existing method — vẫn gọi real implementation mặc định, dùng .mockImplementation() để override; quan trọng: phải restore sau test bằng jest.restoreAllMocks() hoặc mockRestore(). Module mock patterns: mock fetch API toàn cục với jest.spyOn(global, 'fetch'); mock environment variables với process.env.MY_VAR = 'test'. Timer mocks: jest.useFakeTimers() replace setTimeout/setInterval/Date; jest.runAllTimers() trigger tất cả pending timers; jest.advanceTimersByTime(ms). Partial mocks: jest.requireActual('module') để mock một phần module và giữ lại real implementation cho phần còn lại. clearMocks vs resetMocks vs restoreMocks: clear (reset calls/instances), reset (clear + remove implementations), restore (reset + restore spies to originals).",
    q_en: "What is code coverage and how should it be interpreted?",
    a_en: "Code coverage measures the percentage of source code executed by tests. Types: Statement coverage — percentage of statements executed; Branch coverage — percentage of if/else, switch branches executed; Function coverage — percentage of functions called; Line coverage — similar to statement coverage. Coverage is calculated with tools like Istanbul (built into Jest) and displayed in reports. Interpretation: 100% coverage does not mean no bugs — it only means all code was executed, not that all behaviors were tested correctly; low coverage (< 50%) indicates high-risk areas with insufficient testing; target 70-80% overall is a common industry benchmark; critical business logic should aim for near 100%. Avoid the anti-pattern of writing meaningless tests just to increase coverage numbers. Coverage is a signal, not a goal in itself.",
  },
  {
    id: 769,
    category: "Testing",
    subcategory: "Vitest",
    level: "intermediate",
    q: "Vitest là gì? Tại sao là lựa chọn tốt cho Vite projects?",
    a: "Vitest là test framework xây dựng trên Vite với Jest-compatible API — migrate từ Jest thường chỉ cần đổi import và config. Lợi ích: reuse Vite config và transforms (cùng aliases, plugins, TypeScript setup) — không cần config riêng cho tests; nhanh hơn Jest nhờ esbuild transpilation và native ESM. Watch mode thông minh: chỉ re-run tests liên quan đến changed files, smart dependency tracking qua Vite HMR graph. In-source testing: viết tests trực tiếp trong source file trong if (import.meta.vitest) block — tests bị tree-shaken khỏi production build, hữu ích cho utility functions. Browser mode: chạy tests trong real browser (Chromium/Firefox) thay vì jsdom — accurate DOM behavior, dùng cho component tests. Concurrent tests: test.concurrent() chạy tests trong describe block song song, tăng speed. Snapshot: tương thích với Jest snapshots, có inline snapshots. Coverage: V8 provider (nhanh hơn) hoặc Istanbul. @vitest/ui: browser-based test runner UI để visualize và debug tests. Khác biệt với Jest: Vitest dùng ES modules natively (không transform), config trong vitest.config.ts thay vì jest.config.js, một số globals như __dirname cần config thêm.",
    q_en: "What is integration testing and how does it differ from unit testing?",
    a_en: "Unit testing tests a single unit (function, component) in complete isolation — all dependencies are mocked. Integration testing tests how multiple units work together with real or near-real dependencies. Examples: testing an Express route handler with a real (test) database; testing a React component that makes real API calls to a mock server (msw); testing that a service correctly interacts with a repository layer. Integration tests are slower than unit tests but catch a different class of bugs — issues at the boundaries between components. Tools: Supertest (HTTP integration testing for Express/Koa), MSW (Mock Service Worker — intercepts network requests), Testcontainers (spin up real Docker containers for databases). A healthy test suite has both: unit tests for business logic, integration tests for data access and API layers.",
  },
  {
    id: 770,
    category: "Testing",
    subcategory: "Cypress",
    level: "intermediate",
    q: "Cypress là gì? Khác gì với Selenium?",
    a: "Cypress là E2E testing framework chạy trong browser (không qua WebDriver protocol như Selenium). Architecture khác biệt: Cypress chạy trong cùng event loop với app — có thể access application state, intercept network requests, control timers trực tiếp. Time-travel debugging: mỗi command được snapshot, hover qua command list để xem DOM state tại từng bước — exceptional debugging experience. Automatic waiting: Cypress tự retry assertions cho đến khi pass hoặc timeout — không cần manual sleep/waitForElement như Selenium. cy.intercept() cho network stubbing: mock API responses, simulate network errors, wait for specific requests to complete. Component testing: Cypress Component Testing mount individual components trong isolation — alternative cho Jest + Testing Library với real browser. cy.session() để cache và reuse authentication state giữa tests — giảm login overhead. Cypress Cloud: parallel test execution, flaky test detection, video recording. Nhược điểm: chỉ support Chromium-based browsers và Firefox (không có Safari/WebKit); không support multiple tabs trong cùng test; test code phải bất đồng bộ theo Cypress command queue — không dùng async/await thông thường. Thích hợp: rich interactive app testing, khi cần time-travel debugging, single-browser test suites.",
    q_en: "What are test doubles (mocks, stubs, spies) and when to use each?",
    a_en: "Test doubles are objects that replace real dependencies in tests: Stub — returns a pre-defined response (jest.fn().mockReturnValue(data)), used to control what a dependency returns; Mock — like a stub but also verifies how it was called (expect(mockFn).toHaveBeenCalledWith(args)), used to assert behavior; Spy — wraps the real implementation and records calls (jest.spyOn(obj, 'method')), used when you want the real logic but also want to verify it was called; Fake — a working but simplified implementation (in-memory database instead of real database). Rules of thumb: mock external services (HTTP APIs, payment gateways); use fakes for fast in-memory implementations (repositories); avoid over-mocking — if too many mocks are needed, it may indicate a design issue. In Jest: jest.fn(), jest.spyOn(), jest.mock() are the primary tools.",
  },
  {
    id: 771,
    category: "Testing",
    subcategory: "Playwright",
    level: "intermediate",
    q: "Playwright là gì? So sánh với Cypress?",
    a: "Playwright là E2E testing framework từ Microsoft, hỗ trợ Chromium, Firefox, WebKit (Safari) — cross-browser coverage thực sự. Parallel test execution built-in: test files chạy song song theo default, có thể config workers; Cypress Cloud tính thêm phí cho parallelization. Multiple tabs/contexts/pages: test multi-tab flows như OAuth popup, new window; browser contexts là isolated sessions (khác profiles). Page Object Model tự nhiên: Playwright khuyến khích tổ chức code theo POM pattern với class-based pages. await/async API: test code dùng standard async/await — dễ đọc và debug hơn Cypress command queue. Network interception: page.route('**/api/**', route => route.fulfill({ json: mockData })) — mạnh và flexible; page.waitForResponse() để wait cho specific API call. Locators: page.getByRole('button', { name: 'Submit' }), getByText(), getByTestId() — semantic locators như Testing Library, auto-retry built-in. Visual regression testing: expect(page).toHaveScreenshot() so sánh screenshots. Tracing: tạo trace.zip với screenshots, network logs, DOM snapshots — mở trong Playwright Trace Viewer để debug CI failures. Playwright Inspector: step-through test execution, pick locators từ browser. So với Cypress: Playwright có better multi-browser support và parallel execution; Cypress có better DX và debugging cho single-browser.",
    q_en: "What is contract testing and why is it important in microservices?",
    a_en: "Contract testing verifies that two services (consumer and provider) adhere to a shared API contract. Unlike E2E tests that require running all services simultaneously, contract tests run independently. Process: Consumer writes a contract (expected requests and responses); Provider verifies it can fulfill the contract; Contracts are stored in a shared broker (Pact Broker). Tools: Pact (most popular for consumer-driven contracts), Spring Cloud Contract (Java). Why it matters in microservices: services are developed and deployed independently; breaking changes in one service can silently break dependent services; contract tests catch these issues early without requiring integration environments. Contract testing fills the gap between unit tests (too isolated) and E2E tests (too coupled and slow) in microservices architectures.",
  },
  {
    id: 772,
    category: "Testing",
    subcategory: "TDD/BDD",
    level: "intermediate",
    q: "TDD (Test-Driven Development) là gì? Quy trình Red-Green-Refactor?",
    a: "TDD viết test trước code — quy trình Red-Green-Refactor: Red (viết test cho behavior mong muốn, test fail vì code chưa có), Green (viết minimum code đủ để test pass — không over-engineer), Refactor (cải thiện code structure, remove duplication trong khi tests vẫn pass). Lợi ích thực tế: buộc suy nghĩ về API/interface trước implementation — kết quả là better-designed, more testable code; coverage cao tự nhiên vì code chỉ được viết khi có test; dễ refactor vì test suite là safety net. Ví dụ: test 'calculateTax(100, 0.1) should return 10' → implement calculateTax → refactor nếu cần. Micro-TDD cycles: mỗi cycle 2-10 phút, test một behavior nhỏ. TDD không phải 'test first' một cách cứng nhắc — nhiều developers làm 'test alongside' (viết test song song với code). Khi TDD khó: legacy code không testable (cần refactor trước), UI-heavy code (E2E tests khó TDD), exploratory code khi chưa biết design. Detroit vs London schools: Detroit (classic TDD, bottom-up, focus on state verification, minimal mocking); London (mockist TDD, top-down với mocks cho collaborators, verify interactions). Practical advice: TDD đặc biệt valuable cho business logic, algorithms, utilities; ít valuable cho simple CRUD và UI layout.",
    q_en: "What are Core Web Vitals and why do they matter?",
    a_en: "Core Web Vitals are Google's metrics for measuring real-world user experience and are direct ranking signals in Google Search: LCP (Largest Contentful Paint) — loading performance, time until the largest visible element renders; target < 2.5s; CLS (Cumulative Layout Shift) — visual stability, measures unexpected layout shifts; target < 0.1; INP (Interaction to Next Paint, replaced FID) — responsiveness, delay from user interaction to visual response; target < 200ms. Why they matter: Google uses them as ranking factors; poor scores directly impact SEO and organic traffic; they reflect actual user experience. Tools to measure: Lighthouse (lab data), Chrome DevTools, Google Search Console (field data from real users), PageSpeed Insights, WebPageTest. Next.js @vercel/speed-insights collects real user Core Web Vitals automatically.",
  },
  {
    id: 773,
    category: "Testing",
    subcategory: "TDD/BDD",
    level: "intermediate",
    q: "BDD (Behavior-Driven Development) là gì? Cú pháp Given-When-Then?",
    a: "BDD là extension của TDD, focus vào behavior từ user/business perspective thay vì technical implementation. Given-When-Then syntax: Given (context/preconditions — 'Given a user with $100 in account'), When (action — 'When they transfer $50'), Then (expected outcome — 'Then the balance should be $50 and recipient has $50'). Gherkin language: human-readable scenarios viết trong .feature files — Given/When/Then/And/But; được Cucumber parse và map đến step definitions trong code. Lợi ích: common language giữa developers, QA, và business stakeholders; scenarios serve as living documentation; non-technical people có thể review và write scenarios. Trong Jest/Vitest: BDD style với describe/it — it('should deduct amount from sender balance when transfer succeeds') đọc như specification. Cucumber.js: feature files → step definitions (JS functions) → actual test code; overkill cho most projects trừ khi business stakeholders thực sự write scenarios. Practical BDD: dùng Given-When-Then naming convention trong unit tests mà không cần Cucumber — captures intent, readable as docs. Pitfall: BDD không phải chỉ về syntax — phải thực sự involve business trong defining behaviors, không chỉ wrap existing tests với Given/When/Then.",
    q_en: "How do you optimize LCP (Largest Contentful Paint)?",
    a_en: "LCP measures the time until the largest visible element (usually hero image or heading) renders. Optimization strategies: Optimize the LCP element itself — compress images (WebP/AVIF), correct sizing (avoid serving 4K images for 400px slots); Preload critical resources — <link rel='preload'> for LCP images; Server-side rendering (SSR/SSG) — LCP element already present in HTML; CDN — serve static assets from edge locations near users; Eliminate render-blocking resources — defer non-critical CSS/JS; Font optimization — font-display: optional or swap, next/font for automatic optimization; Server response time — improve TTFB with caching and database optimization; Resource hints — preconnect to third-party domains; Remove lazy loading from LCP image — never add loading='lazy' to the above-the-fold image.",
  },
  {
    id: 774,
    category: "Testing",
    subcategory: "Test Pyramid",
    level: "intermediate",
    q: "Test pyramid là gì? Ba layers của test pyramid?",
    a: "Test pyramid của Mike Cohn: base là nhiều unit tests (nhanh <1ms, rẻ, isolated, dễ maintain), giữa là integration tests (test interaction giữa components/services với real dependencies), đỉnh là ít E2E tests (chậm, đắt, brittle — dễ fail vì network, timing, UI changes). Lý do balance 70/20/10: unit tests cho fast feedback và precise error location; integration tests catch boundary bugs unit tests miss; E2E tests verify user journeys nhưng cost cao. Ice cream cone anti-pattern (inverted pyramid): nhiều E2E, ít unit — CI chạy chậm, flaky tests, khó debug khi fail. Testing Trophy (Kent C. Dodds): variation với nhiều integration tests hơn pyramid — 'integration tests give the most confidence per dollar of test cost'. Service layer testing: test business logic through service/use-case layer với real database (integration) thay vì mock everything — catches more real bugs. Practical: không nên có ratio cứng nhắc, phụ thuộc vào project. Smoke tests: minimal E2E tests chạy sau mỗi deployment để verify critical paths hoạt động. Contract tests thay E2E trong microservices. Key insight: fast tests chạy thường xuyên → fast feedback → catch bugs sớm; slow E2E tests chạy ít hơn → feedback chậm.",
    q_en: "How do you optimize CLS (Cumulative Layout Shift)?",
    a_en: "CLS measures unexpected layout shifts — elements jumping around after initial render. Common causes and fixes: Images without dimensions — always set width and height on <img> or use aspect-ratio CSS; Ads and embeds — reserve space with min-height before loading; Fonts causing FOIT/FOUT — use font-display: optional or next/font which prevents layout shift; Dynamic content injected above existing content — insert new content below the fold or use reserved space; Animations using top/left/margin — use transform instead (does not trigger layout); Late-loading third-party scripts — defer them and reserve space. Tools to diagnose: Chrome DevTools Performance tab shows layout shift events; Lighthouse CLS report identifies which elements are shifting.",
  },
  {
    id: 775,
    category: "Testing",
    subcategory: "Jest",
    level: "intermediate",
    q: "Snapshot testing trong Jest là gì? Khi nào nên dùng?",
    a: "Snapshot testing serialize output (component render, JSON object, string) và save vào .snap file — subsequent runs compare với saved snapshot, fail nếu thay đổi. Khi thay đổi là intentional: jest --updateSnapshot (hoặc -u) update tất cả snapshots; jest --updateSnapshot --testNamePattern để update specific. Inline snapshots: toMatchInlineSnapshot() lưu snapshot trong test file thay vì separate .snap file — dễ review hơn trong PRs nhưng clutters test files. Khi nên dùng: stable UI components ít thay đổi (Button, Card, Input), serializable data structures, API response shapes. Khi không nên dùng: frequently changing components (mỗi PR update snapshot không meaningful), dynamic content (timestamps, random IDs — phải mock), complex business logic (unit tests với explicit assertions tốt hơn). Pitfall phổ biến: commit outdated snapshots khi developers chạy update mà không review changes — snapshot tests trở thành rubber stamp. Tốt nhất: kết hợp snapshot tests với explicit assertions cho critical properties. Enzyme vs React Testing Library: Enzyme snapshots test implementation details (component structure); RTL khuyến khích test user-visible output. Custom serializers: jest.addSnapshotSerializer() để format snapshot output đẹp hơn cho custom objects.",
    q_en: "What is lazy loading and what strategies are there for implementing it?",
    a_en: "Lazy loading defers loading resources until they are needed, reducing initial page load time. Strategies: Image lazy loading — <img loading='lazy'> (native browser support) or Intersection Observer API for custom behavior; JavaScript code splitting — dynamic import() loads modules on demand; Route-based code splitting — load page components only when the route is visited (built into Next.js App Router); Component-based lazy loading — React.lazy() + Suspense for client components; Video lazy loading — load video poster image, load video source only on user interaction. In Next.js: next/image handles lazy loading automatically; next/dynamic wraps React.lazy() with SSR support. Lazy loading is most effective for below-the-fold content — never lazy load LCP elements.",
  },
  {
    id: 776,
    category: "Testing",
    subcategory: "Jest",
    level: "intermediate",
    q: "Code coverage là gì? Các loại coverage metrics?",
    a: "Code coverage đo phần trăm code được chạy bởi tests — generated bởi Istanbul (Jest), V8 (Vitest V8 provider). Metrics: Statement coverage (% statements executed — mỗi expression/assignment là statement); Branch coverage (% branches covered — mỗi if/else/ternary/&&/|| tạo 2 branches, phải test cả true và false case); Function coverage (% functions called ít nhất một lần); Line coverage (% lines executed — thường cao nhất, ít meaningful nhất). Branch coverage là metric quan trọng nhất — 100% statement nhưng 70% branch nghĩa là nhiều edge cases chưa được test. 100% coverage không đảm bảo bug-free: test có thể chạy code nhưng không assert correctly; test với wrong inputs không catch bugs với right inputs. Low coverage là red flag: dưới 60-70% thường là warning sign. Coverage thresholds trong jest.config.js: coverageThreshold: { global: { branches: 80, functions: 80 } } — fail CI nếu không đạt. Practical targets: 80-90% cho business logic; 70%+ overall; không chase 100% cho UI components và configuration files. Excluded from coverage: vendor code, generated files, test utilities — dùng coveragePathIgnorePatterns. Coverage reports: text (CI), html (visual inspection), lcov (SonarQube, Codecov). Mutation testing (Stryker): modify code và check nếu tests fail — đo test quality, không chỉ coverage quantity.",
    q_en: "What is code splitting and how does it improve performance?",
    a_en: "Code splitting divides the JavaScript bundle into smaller chunks that are loaded on demand instead of all at once. Benefits: faster initial page load (less JS to parse and execute), only load code actually used. Types: Route-based splitting — each route has its own bundle (automatic in Next.js App Router); Component-based splitting — React.lazy() + dynamic import() for heavy components (charts, rich text editors, maps); Vendor splitting — separate third-party libraries from application code for better caching (library code changes less often); Library granular imports — import { Button } from 'ui-lib' vs import entire library. Tools: Webpack Bundle Analyzer, Rollup Visualizer (Vite) — visualize bundle composition and identify large modules. Next.js performs route-level code splitting automatically; all other splitting is done manually.",
  },
  {
    id: 777,
    category: "Testing",
    subcategory: "Testing",
    level: "intermediate",
    q: "Integration testing khác unit testing như thế nào?",
    a: "Unit testing: test một function/class isolated, mock tất cả external dependencies — nhanh (<1ms), stable, dễ debug khi fail (chính xác biết chỗ nào sai), không cần external services. Tốt cho: algorithms, business logic, pure functions, utilities. Integration testing: test interaction giữa multiple components/services với real hoặc minimal-mock dependencies — ví dụ test API handler với real database (test container), test service với real HTTP client. Chậm hơn (100ms-1s), phát hiện bugs ở boundary (schema mismatch, query lỗi, API contract violation) mà unit tests không thể catch. Supertest pattern (Node.js): const app = express(); const response = await request(app).get('/api/users').expect(200) — test Express routes với real middleware stack, mock chỉ external services. Test containers: spin up real PostgreSQL/Redis trong Docker cho tests — deterministic, isolated, cleanup sau tests. Contract tests thay thế integration tests trong microservices khi services owned bởi different teams. Testing Library philosophy: 'The more your tests resemble the way your software is used, the more confidence they can give you' — integration tests thường cho more confidence per test than unit tests. Mock database vs real database: mock nhanh hơn nhưng có thể miss SQL syntax errors, constraint violations, query performance issues — real DB catches more real-world bugs.",
    q_en: "What is HTTP caching and how does it work?",
    a_en: "HTTP caching stores responses in the browser (or intermediate proxies/CDNs) to avoid re-fetching unchanged resources. Key headers: Cache-Control: max-age=31536000, immutable — tell browser to cache for 1 year without revalidation (for hashed static assets); Cache-Control: no-cache — store in cache but always revalidate with server; Cache-Control: no-store — do not cache at all; ETag — server fingerprint of the resource; browser sends If-None-Match, server returns 304 Not Modified if unchanged; Last-Modified / If-Modified-Since — similar mechanism using timestamps. Strategies: Static assets (JS, CSS) — use content hashing in filenames (app.abc123.js) with immutable caching; HTML — no-cache or short TTL; API responses — vary based on how dynamic data is; CDN caching — edge servers cache responses globally. Proper HTTP caching dramatically reduces bandwidth and improves repeat visit performance.",
  },
  {
    id: 778,
    category: "Testing",
    subcategory: "Playwright",
    level: "advanced",
    q: "Cách xử lý authentication trong Playwright E2E tests?",
    a: "Playwright hỗ trợ storageState để save và reuse authentication state giữa tests. Pattern: globalSetup.ts chạy một lần trước tất cả tests — login qua UI hoặc API, save state: await context.storageState({ path: 'auth/user.json' }). Reuse trong playwright.config.ts: use: { storageState: 'auth/user.json' } — mỗi test bắt đầu đã authenticated, không phải login lại. Multiple roles: save different storageStates cho admin, regular user, guest — dùng projects trong config để chạy same tests với different roles. API-based auth nhanh hơn UI login: const response = await request.post('/api/login', { data: { email, password } }); await page.context().addCookies([...]) — avoid UI flakiness. Page Object Model (POM): class LoginPage { constructor(page) {...}; async login(email, pw) { await this.page.fill('#email', email); ... } } — encapsulate page interactions, reusable across tests, easy maintenance khi UI thay đổi. Fixtures trong Playwright: custom fixtures extend base test với pre-setup state — const test = base.extend({ loggedInPage: async ({page}, use) => { await loginUser(page); await use(page); } }) — elegant và composable. Avoid test interdependency: mỗi test phải independent, không phụ thuộc vào state từ test trước — dùng beforeEach để reset state.",
    q_en: "What are the image optimization techniques in web development?",
    a_en: "Image optimization is often the highest-impact performance improvement: Format selection — WebP is 25-35% smaller than JPEG/PNG with similar quality; AVIF is even smaller but has less browser support; SVG for icons and simple graphics; Compression — lossy (reduce quality slightly) or lossless; tools: Squoosh, ImageOptim, Sharp (Node.js library); Correct sizing — serve images at the size they will be displayed, not 4K images for 400px slots; Responsive images — srcset and sizes attributes for different screen sizes and resolutions; Lazy loading — loading='lazy' for below-the-fold images; Modern CDN services — Cloudinary, Imgix provide on-the-fly transforms; next/image — automatically handles WebP conversion, responsive sizes, lazy loading, and CLS prevention (reserves space). Image optimization alone can reduce page size by 40-70%.",
  },
  {
    id: 779,
    category: "Testing",
    subcategory: "Testing",
    level: "advanced",
    q: "Contract testing là gì? Tại sao quan trọng trong microservices?",
    a: "Contract testing verify API contract (format, structure, behavior) giữa service provider và consumer mà không cần cả hai chạy đồng thời — critical trong microservices. Consumer-driven contract testing với Pact: Consumer viết tests định nghĩa expectations về provider responses (pact file); Provider chạy pact file để verify nó thỏa mãn consumer expectations. Pact Broker: central repository cho pact files, track which consumer versions are compatible với which provider versions. Can-I-Deploy tool: query Pact Broker để check nếu service version có thể deploy safely — 'can consumer v2 deploy if provider is v3?'. Tại sao quan trọng hơn E2E tests trong microservices: E2E tests cần tất cả services chạy đồng thời (expensive, slow, flaky); contract tests chạy independently per service — fast, reliable, pinpoint exactly which service broke contract. Breaking change detection: nếu provider thay đổi response schema, pact verification fail ngay trong CI trước khi deploy — không phải phát hiện trong production. Schema registry (Avro, Protobuf) cho event-driven architecture: schema evolution rules, backward/forward compatibility checks — contract testing cho async messaging. OpenAPI contract testing: Dredd, Spectral verify API implementation against OpenAPI spec — simpler than Pact nhưng ít powerful.",
    q_en: "What are Service Workers and what can they do?",
    a_en: "Service Workers are JavaScript files that run in a background thread, separate from the main browser thread. They act as a network proxy, intercepting requests between the browser and network. Key capabilities: Offline support — cache responses, serve cached content when offline (Progressive Web Apps); Background sync — sync data when connectivity is restored; Push notifications — receive and display push notifications even when the app is closed; Precaching — cache critical assets on install for instant loading; Performance optimization — serve cached responses, fetch updates in the background (stale-while-revalidate). Service Workers are HTTPS-only for security. In Next.js: next-pwa package simplifies Service Worker setup for PWAs. Workbox (from Google) is the most popular library for managing Service Worker caching strategies.",
  },
  {
    id: 780,
    category: "Testing",
    subcategory: "Testing",
    level: "advanced",
    q: "Test doubles: mock, stub, spy, fake khác nhau như thế nào?",
    a: "Test doubles là các loại replacements cho real dependencies trong tests — Martin Fowler phân loại trong 'Mocks Aren't Stubs'. Dummy: placeholder object được pass vào nhưng không bao giờ thực sự được dùng — ví dụ pass null hoặc empty object để fulfill parameter requirement. Stub: trả về pre-configured responses cho calls — không verify interactions, chỉ provide canned answers; ví dụ stub database trả về fixed user object. Mock: pre-programmed với expectations về calls mà nó sẽ nhận — verify những interactions này xảy ra; jest.fn() là mock vì có thể assert toHaveBeenCalledWith(); test fail nếu expected calls không xảy ra. Spy: wrap real implementation, record calls — jest.spyOn() by default calls real method, ghi lại arguments; có thể override với mockImplementation khi cần. Fake: có working implementation đơn giản hơn real — fake in-memory database (Map<id, entity>), fake email service (lưu emails vào array thay vì gửi thật), fake message queue. Practical guidance: dùng Stub khi cần control return values; Mock khi muốn verify behavior (dùng đúng dependencies đúng cách); Spy khi muốn observe real implementation; Fake khi cần realistic behavior nhưng không thể/không muốn dùng real service. Overcrowded mocks là code smell: nếu test cần mock quá nhiều dependencies, code đang vi phạm Single Responsibility Principle — refactor code, không thêm mocks.",
    q_en: "What is virtual scrolling and when should it be used?",
    a_en: "Virtual scrolling (also called windowing) renders only the DOM elements currently visible in the viewport, even if the underlying list has thousands of items. Without it, rendering 10,000 list items creates 10,000 DOM nodes, causing slow initial render and janky scrolling. Virtual scrolling: calculates which items are in view based on scroll position; renders only those items plus a small buffer above and below; replaces off-screen items with empty spacers to maintain correct scroll height. Use when: lists have > 100-200 items and performance is noticeably degraded; tables or grids with many rows; infinite scroll feeds. Libraries: react-window and react-virtual (TanStack Virtual) for React. Tradeoffs: more complex implementation, accessibility issues (screen readers cannot read off-screen content), search-in-page (Ctrl+F) does not work for off-screen items.",
  },

  // === PERFORMANCE (781-790) ===
  {
    id: 781,
    category: "Performance",
    subcategory: "Core Web Vitals",
    level: "intermediate",
    q: "Core Web Vitals là gì? Ba metrics chính là gì?",
    a: "Core Web Vitals là bộ metrics của Google đo trải nghiệm người dùng thực tế, ảnh hưởng đến SEO ranking từ 2021. Ba metrics: LCP (Largest Contentful Paint — loading performance, target <2.5s: good, >4s: poor; đo thời gian render phần tử lớn nhất trong viewport); INP (Interaction to Next Paint — thay thế FID từ tháng 3/2024, target <200ms: good, >500ms: poor; đo total delay từ user interaction đến next paint cho tất cả interactions trong session, không chỉ first); CLS (Cumulative Layout Shift — visual stability, target <0.1: good, >0.25: poor; đo mức độ layout shift unexpected). Đo lường: PageSpeed Insights (field data từ Chrome UX Report + lab data từ Lighthouse), Search Console Core Web Vitals report, web-vitals JavaScript library (import { getLCP, getINP, getCLS } from 'web-vitals'). Quan trọng: field data (real users) khác lab data (controlled) — optimize cho field data. TTFB (Time to First Byte) không phải Core Web Vital nhưng ảnh hưởng LCP mạnh; FCP (First Contentful Paint) là diagnostic metric.",
    q_en: "What is XSS (Cross-Site Scripting) and how do you prevent it?",
    a_en: "XSS is an attack where malicious scripts are injected into web pages viewed by other users. Types: Stored XSS — malicious script is saved to the database and served to all users; Reflected XSS — script is embedded in a URL and executed when the link is opened; DOM-based XSS — script is injected via client-side JavaScript. Prevention: Output encoding — escape HTML special characters (<, >, &, \", ') before rendering user input; Use safe APIs — textContent instead of innerHTML; use framework-built-in escaping (React auto-escapes JSX by default — dangerouslySetInnerHTML should be avoided); Content Security Policy (CSP) — HTTP header restricting which scripts can run; Input validation — reject or sanitize dangerous input on the server; Sanitization libraries — DOMPurify for safely rendering user-provided HTML.",
  },
  {
    id: 782,
    category: "Performance",
    subcategory: "Core Web Vitals",
    level: "intermediate",
    q: "LCP (Largest Contentful Paint) là gì? Cách cải thiện LCP?",
    a: "LCP đo thời gian render phần tử lớn nhất trong viewport — thường là hero image, large text block, video poster. Candidates: img, image trong CSS background, video (poster), block-level text. Cải thiện TTFB trước (ảnh hưởng LCP nhất): server response time, CDN, edge caching. Image optimization: serve WebP/AVIF (25-50% nhỏ hơn JPEG), đúng kích thước (không serve 2000px cho 400px container), dùng responsive images với srcset. Preload LCP image: <link rel='preload' as='image' href='hero.webp'> trong <head> — critical vì browser discover image late trong parsing. Không lazy load LCP image: loading='lazy' trên hero image làm tăng LCP đáng kể — chỉ lazy load below-fold. Eliminate render-blocking resources: defer/async non-critical JS, inline critical CSS, preconnect đến third-party domains. fetchpriority='high' attribute trên LCP image hint browser prioritize fetch. Next.js next/image tự động: format conversion, responsive sizes, lazy load by default (priority prop để disable cho LCP image). Thường gặp: LCP image discovered late vì được set bằng CSS background-image — browser không discover cho đến khi parse CSS; dùng <img> thay vì CSS background cho LCP element.",
    q_en: "What is CSRF (Cross-Site Request Forgery) and how do you prevent it?",
    a_en: "CSRF is an attack where a malicious website tricks an authenticated user's browser into making unintended requests to another site (e.g., transferring money). The attack works because browsers automatically send cookies with requests. Prevention: CSRF tokens — include a secret token in forms that the server validates; SameSite cookies — Set-Cookie: SameSite=Strict or Lax prevents cookies from being sent in cross-site requests; Double submit cookie pattern — include a random value in both a cookie and a request parameter, verify they match; Origin/Referer header validation — check that the request comes from the expected domain. Modern frameworks (Next.js, Rails, Django) include built-in CSRF protection. REST APIs using Authorization headers (Bearer tokens) are naturally immune to CSRF since browsers do not automatically add Authorization headers.",
  },
  {
    id: 783,
    category: "Performance",
    subcategory: "Core Web Vitals",
    level: "intermediate",
    q: "CLS (Cumulative Layout Shift) là gì? Nguyên nhân và cách fix?",
    a: "CLS đo mức độ layout shift unexpected trong quá trình loading — score là sum của (impact fraction * distance fraction) cho mỗi shift. Target <0.1; >0.25 là poor. Nguyên nhân phổ biến và fix: Images/videos không có dimensions — luôn set width/height attributes (aspect-ratio CSS tự tính) hoặc dùng aspect-ratio: 16/9 container; browser reserve space trước khi image load. Fonts gây FOUT (Flash of Unstyled Text) khi web font load — dùng font-display: swap với size-adjust để match fallback font metrics; font-display: optional skip web font nếu không cached (zero CLS nhưng có thể không dùng web font); preload critical fonts. Dynamically injected content (banners, cookie notices, ads) shift content — reserve space trước bằng min-height container; không inject content above existing content sau page load. Dynamic ads: Google Ads có thể gây CLS — fix bằng reserve fixed space cho ad slot. Infinite scroll: thêm items dưới thay vì trên viewport. transform: translateY() thay vì top/margin cho animations — transform không trigger layout. CLS sau interaction (300ms window) không tính vào score — UX vẫn tệ nhưng không penalty. Debugging: Chrome DevTools Performance panel, Layout Shift Regions (Rendering tab).",
    q_en: "What is Content Security Policy (CSP) and how does it work?",
    a_en: "CSP is an HTTP response header (Content-Security-Policy) that tells the browser which resources are allowed to load. It is the most effective defense against XSS attacks. Directives: default-src — fallback for all resource types; script-src — allowed JavaScript sources; style-src — allowed CSS sources; img-src — allowed image sources; connect-src — allowed API endpoints (fetch, XHR); frame-ancestors — controls iframe embedding (replaces X-Frame-Options). Example: Content-Security-Policy: default-src 'self'; script-src 'self' 'nonce-{random}'; CSP can be set via HTTP headers or <meta http-equiv> tag. In Next.js: set via headers in next.config.ts or middleware. Strict CSP (nonce-based) is more effective than allowlist-based CSP. Start with report-only mode (Content-Security-Policy-Report-Only) to see violations before enforcing.",
  },
  {
    id: 784,
    category: "Performance",
    subcategory: "Lazy Loading",
    level: "intermediate",
    q: "Lazy loading là gì? Cách implement trong React?",
    a: "Lazy loading trì hoãn loading resources cho đến khi thực sự cần — giảm initial load time và Time to Interactive. Images: <img loading='lazy'> attribute là native browser lazy loading, browser load image khi cần thiết khi scroll đến; không dùng cho LCP image (dùng loading='eager' hoặc priority). Components trong React: const HeavyChart = React.lazy(() => import('./HeavyChart')) kết hợp <Suspense fallback={<Spinner/>}> — chunk được split tự động và load on demand; dùng cho large components, chart libraries, editors. next/dynamic trong Next.js: const DynamicMap = dynamic(() => import('./Map'), { loading: () => <p>Loading...</p>, ssr: false }) — thêm ssr: false để skip server-side rendering cho browser-only components. Route-based lazy loading: React Router <Suspense> + React.lazy() mỗi route là chunk riêng — standard approach. Preloading: sau lazy load, có thể preload chunk trước khi user navigate: import('./HeavyPage') trong onMouseEnter — reduce perceived latency. Intersection Observer API: detect khi element vào viewport để trigger lazy load — basis của nhiều lazy loading libraries. Trade-off: lazy loading adds waterfall (request phải chờ parent component render) — cẩn thận với critical path; chỉ lazy load truly non-critical components.",
    q_en: "What is OAuth 2.0 and how does the Authorization Code flow work?",
    a_en: "OAuth 2.0 is an authorization framework that allows third-party applications to access user resources without exposing user credentials. Authorization Code flow (most secure, for web apps): User clicks 'Login with Google'; App redirects to Google authorization server with client_id, redirect_uri, scope, and state (CSRF protection); User grants permission; Google redirects back with an authorization code; App exchanges the code for access token + refresh token (server-to-server, never exposed to browser); App uses access token to call protected APIs. PKCE (Proof Key for Code Exchange) is required for SPAs and mobile apps that cannot securely store client_secret. Scopes limit what the app can access (e.g., email, profile, read:calendar). Refresh tokens allow obtaining new access tokens without re-authentication.",
  },
  {
    id: 785,
    category: "Performance",
    subcategory: "Code Splitting",
    level: "intermediate",
    q: "Code splitting là gì? Các chiến lược code splitting trong web apps?",
    a: "Code splitting chia bundle thành chunks nhỏ hơn — load initial chunk nhỏ hơn, load thêm chunks on demand. Dynamic import() là foundation: import('./module') trả về Promise, bundler tự tạo separate chunk. Chiến lược: Route-based splitting — mỗi route là chunk riêng, user chỉ download code cho route đang visit; pattern chuẩn với React Router và React.lazy(). Component-based splitting — lazy load heavy components (rich text editor, PDF viewer, 3D rendering) chỉ khi cần. Vendor splitting: tách node_modules thành vendor chunk — ít thay đổi hơn app code, cache lâu hơn; Vite tự động split vendor; Webpack SplitChunksPlugin cấu hình. Shared chunks: code dùng ở nhiều routes tách thành shared chunk — tránh duplicate code trong nhiều chunks. Prefetching và preloading: <link rel='prefetch'> cho next likely navigation (browser idle), <link rel='preload'> cho current route critical resources; Webpack magic comments: import(/* webpackPrefetch: true */ './NextPage'). Analysis: Webpack Bundle Analyzer, vite-bundle-visualizer, source-map-explorer để xem chunk composition và identify optimization opportunities. Pitfall: quá nhiều chunks nhỏ tệ hơn một chunk lớn do HTTP request overhead — HTTP/2 multiplexing giảm vấn đề này nhưng có limits.",
    q_en: "What is HTTPS/TLS and why is it required for modern web applications?",
    a_en: "HTTPS (HTTP Secure) uses TLS (Transport Layer Security) to encrypt communication between browser and server. TLS provides: Encryption — data is unreadable to eavesdroppers; Authentication — certificates verify server identity (prevents man-in-the-middle attacks); Integrity — data cannot be tampered with in transit. TLS handshake: client sends ClientHello (supported cipher suites); server responds with certificate; client verifies certificate against CA (Certificate Authority); both parties derive session keys; encrypted communication begins. Why mandatory: Google penalizes HTTP sites in search rankings; browsers show 'Not Secure' warnings; required for Service Workers, Web Push, WebRTC, and other modern APIs; HTTP/2 (and HTTP/3) require HTTPS; cookie security attributes (Secure flag) require HTTPS. Free certificates via Let's Encrypt have made HTTPS accessible to everyone.",
  },
  {
    id: 786,
    category: "Performance",
    subcategory: "Caching",
    level: "intermediate",
    q: "HTTP caching hoạt động như thế nào? Cache-Control headers?",
    a: "HTTP caching giảm bandwidth và latency bằng cách lưu responses tại browser hoặc CDN. Cache-Control directives: max-age=3600 (cache 1 giờ trước khi stale); no-cache (phải revalidate với server trước khi dùng — không phải 'không cache'); no-store (không cache bao giờ, sensitive data); public (CDN có thể cache); private (chỉ browser cache, không CDN); immutable (content không bao giờ thay đổi, skip revalidation kể cả khi max-age expired — dùng với content-hashed assets). Static assets strategy: Cache-Control: public, max-age=31536000, immutable cho files có content hash trong tên (bundle.abc123.js) — cache 1 năm, không bao giờ revalidate. HTML: Cache-Control: no-cache — revalidate mỗi lần nhưng serve từ cache nếu ETag match. Conditional requests: ETag (hash của content) và Last-Modified (timestamp); client gửi If-None-Match/If-Modified-Since trong request; nếu không thay đổi, server trả 304 Not Modified (no body) — tiết kiệm bandwidth. Stale-While-Revalidate: Cache-Control: max-age=60, stale-while-revalidate=300 — serve stale content trong 5 phút trong khi revalidate background; zero latency impact. CDN caching: Cloudflare, Vercel Edge Network cache responses — đặt s-maxage khác max-age để CDN cache lâu hơn browser. Cache busting: thay đổi URL khi content thay đổi (content hash, query string ?v=2).",
    q_en: "What is CORS (Cross-Origin Resource Sharing) and how does it work?",
    a_en: "CORS is a browser security mechanism that controls how web pages make requests to different origins (domain + port + protocol). Same-origin policy by default blocks cross-origin requests. CORS allows servers to explicitly grant cross-origin access via headers: Access-Control-Allow-Origin: * (all origins) or https://example.com (specific origin); Access-Control-Allow-Methods: GET, POST, PUT, DELETE; Access-Control-Allow-Headers: Content-Type, Authorization; Access-Control-Allow-Credentials: true (required to send cookies). Preflight requests: for non-simple requests (PUT, DELETE, custom headers), the browser sends an OPTIONS preflight request first. In Express: use the cors() middleware; in Next.js: set headers in Route Handlers or next.config.ts. CORS is a browser-enforced mechanism — server-to-server requests are not affected.",
  },
  {
    id: 787,
    category: "Performance",
    subcategory: "Performance",
    level: "intermediate",
    q: "Image optimization trong web: các kỹ thuật phổ biến?",
    a: "Image optimization là một trong các cách hiệu quả nhất để giảm page weight và cải thiện LCP. Modern formats: WebP nhỏ hơn JPEG 25-34%, AVIF nhỏ hơn 50% nhưng encode chậm hơn; dùng <picture> element với fallback cho browser không support. Responsive images: srcset='image-400.webp 400w, image-800.webp 800w' với sizes='(max-width: 600px) 400px, 800px' — browser chọn đúng size cho device; next/image tự động generate. Correct sizing: không serve 2000px image cho 400px container — lãng phí bandwidth 25x; dùng Image CDN (Cloudinary, Imgix) để resize on demand theo URL params. Compression: lossy (JPEG quality 80 thường indistinguishable từ 100), lossless (PNG, WebP lossless); tools: Squoosh, Sharp (Node.js), ImageMagick. Lazy loading: loading='lazy' cho below-fold images; không lazy load LCP image. Preload LCP: <link rel='preload' as='image' href='hero.webp' imagesrcset='...' imagesizes='...'> cho critical images. LQIP (Low Quality Image Placeholder): show tiny blurred placeholder trong khi load — next/image placeholder='blur' prop. CDN và edge: serve images từ locations gần users; modern Image CDNs auto-convert format, resize, compress based on device. Sprites: combine nhiều icons thành một file — giảm HTTP requests; ngày nay SVG sprites hoặc icon fonts phổ biến hơn.",
    q_en: "What are security headers and which ones are most important?",
    a_en: "Security headers are HTTP response headers that instruct browsers on security policies: Strict-Transport-Security (HSTS) — forces HTTPS for a specified duration; prevents protocol downgrade attacks; X-Content-Type-Options: nosniff — prevents MIME-type sniffing (browser executes content as declared type only); X-Frame-Options: DENY or SAMEORIGIN — prevents clickjacking by controlling iframe embedding (superseded by CSP frame-ancestors); Referrer-Policy — controls how much referrer info is sent; Permissions-Policy — controls browser feature access (camera, microphone, geolocation); Content-Security-Policy — see CSP section. Tools to check: securityheaders.com, Mozilla Observatory. In Next.js: configure all headers in next.config.ts headers() function. Start with SecurityHeaders.com recommendations and adjust based on application needs.",
  },
  {
    id: 788,
    category: "Performance",
    subcategory: "Performance",
    level: "advanced",
    q: "Bundle analysis: cách phát hiện và giảm bundle size?",
    a: "Bundle analysis là bước đầu tiên khi optimize bundle size — không đoán mò, phải đo. Tools: webpack-bundle-analyzer (treemap visualization), vite-bundle-visualizer (rollup-plugin-visualizer), source-map-explorer (analyze actual bundle không phải estimates), bundlephobia.com (check package size trước khi install). Những gì cần tìm: Large dependencies có alternatives nhỏ hơn — moment.js (330KB) → date-fns/dayjs (10KB); lodash (71KB full) → lodash-es + tree shaking hoặc chỉ import specific: import debounce from 'lodash/debounce'; chart.js (200KB) → recharts (160KB) → victory (100KB). Duplicate modules: cùng dependency nhiều versions (webpack deduplication), cùng code ở nhiều chunks. Unexpected large modules: ví dụ icon library import toàn bộ thay vì specific icons — @mui/icons-material 30MB vs import MenuIcon from '@mui/icons-material/Menu' (một file). Giải pháp: Tree shaking (sideEffects: false trong package.json), code splitting + dynamic imports, externals (load React từ CDN thay vì bundle), Module Federation (share dependencies giữa micro-frontends). Compression context: gzip/Brotli reduce bundle size 60-80% — báo cáo compressed size thực tế, không raw. Benchmark: track bundle size trong CI với size-limit package — fail build nếu bundle vượt threshold.",
    q_en: "What is SQL injection and how do you prevent it?",
    a_en: "SQL injection is an attack where malicious SQL code is inserted into user input fields, allowing attackers to manipulate database queries. Example: if a query is built as 'SELECT * FROM users WHERE email = \'' + userInput + '\'', an attacker can input ' OR '1'='1 to return all users, or '; DROP TABLE users; -- to destroy data. Prevention: Parameterized queries / Prepared statements — use ? or $1 placeholders, the database driver handles escaping; ORMs (Prisma, Sequelize, TypeORM) use parameterized queries by default; Never build queries with string concatenation of user input; Input validation — validate and sanitize input types and formats; Principle of least privilege — database user should only have necessary permissions; WAF (Web Application Firewall) — additional layer but not a substitute for proper coding practices.",
  },
  {
    id: 789,
    category: "Performance",
    subcategory: "Performance",
    level: "advanced",
    q: "Service Worker và caching strategy trong PWA là gì?",
    a: "Service Worker là JavaScript running trong background thread, intercept network requests giữa app và network — foundation của PWA. Lifecycle: Install (cache precache assets) → Activate (cleanup old caches) → Fetch (intercept requests). Caching strategies: Cache First (check cache trước, fallback network — offline-first, dùng cho static assets, icon fonts); Network First (fetch từ network, fallback cache nếu offline — dùng cho API calls cần fresh data); Stale-While-Revalidate (serve từ cache immediately, fetch từ network để update cache background — balance speed và freshness, tốt cho non-critical data); Cache Only (chỉ cache, không network — precached resources); Network Only (không cache — real-time data). Background Sync: queue failed requests khi offline, retry khi network available — ví dụ submit form offline. Push Notifications: receive push messages từ server kể cả khi app không open — cần permission từ user. Workbox từ Google: abstraction layer cho Service Worker strategies — workbox-strategies, workbox-routing, workbox-precaching; tránh viết Service Worker boilerplate thủ công. vite-plugin-pwa tích hợp Workbox với Vite, tự động generate service worker. Pitfall: Service Worker cache bugs khó debug — phải explicitly update cache version khi deploy; chrome://serviceworker-internals để debug. HTTPS required (localhost exception).",
    q_en: "What are the security best practices for cookies?",
    a_en: "Cookie security attributes: HttpOnly — prevents JavaScript access to the cookie (document.cookie), mitigates XSS; Secure — cookie only sent over HTTPS; SameSite=Strict — cookie not sent in cross-site requests (prevents CSRF); SameSite=Lax — allows GET cross-site requests but blocks POST (good default balance); SameSite=None; Secure — for third-party cookies (requires Secure); Path and Domain — limit cookie scope; Max-Age / Expires — set expiration. Session management best practices: rotate session IDs after login (session fixation prevention); use short-lived access tokens with refresh tokens; store session data server-side (not client-side JWT for sensitive data); implement token revocation for logout. For JWT: store in httpOnly cookies (not localStorage) to prevent XSS access; verify signature on every request; include expiration (exp claim).",
  },
  {
    id: 790,
    category: "Performance",
    subcategory: "Performance",
    level: "advanced",
    q: "Virtual scrolling (windowing) là gì? Khi nào cần dùng?",
    a: "Virtual scrolling (windowing) chỉ render DOM nodes cho items visible trong viewport — list 10,000 items chỉ render 20-50 items trong DOM tại bất kỳ thời điểm nào, maintain scroll position qua spacer elements. Mechanism: total height của list được set bằng container (tạo scrollbar đúng size), khi scroll, calculate which items should be visible, render chỉ those items với absolute positioning. Libraries: @tanstack/react-virtual (headless, flexible, không opinionated về UI — phổ biến nhất hiện tại); react-window (Brian Vaughn, smaller, simpler API cho fixed-size items); react-virtuoso (dynamic item heights, auto-measure). Khi cần dùng: list >100-200 items với complex rendering (images, many DOM nodes per item); performance profiling (React DevTools Profiler hoặc Chrome Performance tab) cho thấy list rendering là bottleneck — đừng premature optimize. Pitfall: virtual scrolling phức tạp hơn regular list — khó implement features như 'scroll to item', dynamic item heights, sticky headers; @tanstack/react-virtual có examples cho tất cả cases này. Variable height items: cần measure items sau render để tính scroll position — @tanstack/react-virtual và react-virtuoso handle dynamic heights tốt hơn react-window. Alternative đơn giản hơn khi list không quá dài: pagination hoặc 'load more' button — ít phức tạp, không cần virtual scrolling.",
    q_en: "What is dependency security and how do you manage it?",
    a_en: "Dependency security involves managing security vulnerabilities in third-party packages: Vulnerability scanning: npm audit — built-in npm tool to find known vulnerabilities; Snyk, Dependabot, OWASP Dependency-Check — automated scanning; GitHub Dependabot — automatically creates PRs to update vulnerable dependencies; Supply chain attacks — attackers publish malicious packages with similar names (typosquatting) or compromise legitimate packages; Practices: run npm audit in CI/CD pipeline; use lockfiles (package-lock.json, yarn.lock) for reproducible installs; review dependencies before adding them (check download count, maintenance status, GitHub stars); prefer well-maintained packages; pin exact versions for critical security-sensitive packages; use npm publish --access restricted for private packages; monitor CVE databases for known vulnerabilities. OWASP Top 10 includes A06:2021 — Vulnerable and Outdated Components.",
  },

  // === SECURITY (791-800) ===
  {
    id: 791,
    category: "Security",
    subcategory: "XSS & CSRF",
    level: "intermediate",
    q: "XSS (Cross-Site Scripting) là gì? Các loại XSS và cách phòng chống?",
    a: "XSS (Cross-Site Scripting) cho phép attacker inject và execute malicious scripts trong browser của victim, đánh cắp sessions, redirect users, modify page content. Ba loại: Stored XSS — script được lưu trong DB (comment, profile), mỗi user xem page bị attack; Reflected XSS — script trong URL parameter, server reflect lại trong response (phishing links); DOM-based XSS — client-side code đọc từ URL/storage và inject vào DOM mà không qua server (document.location, window.location.hash → innerHTML). Phòng chống: Context-appropriate encoding — HTML encode khi insert vào HTML, JS encode khi insert vào JavaScript, URL encode khi insert vào URL; React JSX tự động escape — dangerouslySetInnerHTML vẫn nguy hiểm. Không dùng innerHTML với user content — dùng textContent hoặc createElement. DOMPurify: sanitize HTML khi cần render user-provided HTML (rich text editor output) — DOMPurify.sanitize(userHtml). Content Security Policy: block inline scripts và unauthorized external scripts — defense in depth. HttpOnly cookies: JavaScript không thể đọc session cookie dù có XSS. Subresource Integrity (SRI): verify CDN scripts không bị tamper — integrity attribute trên <script>. Template literals và DOM APIs: const div = document.createElement('div'); div.textContent = userInput — safe; div.innerHTML = userInput — unsafe.",
    q_en: "What is the bundle analyzer and how do you use it to optimize bundle size?",
    a_en: "A bundle analyzer generates a visual treemap of the JavaScript bundle, showing the size of each module and dependency. Tools: @next/bundle-analyzer — wraps Next.js build with bundle visualization; webpack-bundle-analyzer — for Webpack-based projects; rollup-plugin-visualizer — for Vite/Rollup projects. Common findings and fixes: Large libraries — replace moment.js (67KB) with date-fns (tree-shakeable); replace lodash with lodash-es or individual imports; Large polyfills — check if @babel/polyfill targets too many old browsers; Duplicate packages — multiple versions of the same library; Unused code — large imports that are not fully used; Third-party analytics/SDKs — consider lazy loading. Process: run ANALYZE=true next build; identify the largest chunks; check if they are necessary or can be replaced/lazily loaded; measure the improvement.",
  },
  {
    id: 792,
    category: "Security",
    subcategory: "XSS & CSRF",
    level: "intermediate",
    q: "CSRF (Cross-Site Request Forgery) là gì? Cách phòng chống?",
    a: "CSRF (Cross-Site Request Forgery) exploit việc browser tự động gửi cookies khi request đến domain — attacker tạo form/image trên evil.com gửi request đến bank.com, browser tự động attach session cookie của user. Ví dụ: <img src='https://bank.com/transfer?to=attacker&amount=1000'> — nếu user đang logged in và bank không có CSRF protection, transfer xảy ra. Phòng chống: SameSite cookie attribute là defense chính hiện nay — SameSite=Strict: cookie không gửi trong bất kỳ cross-site request nào (bao gồm link navigation); SameSite=Lax (default Chrome): cookie gửi trong top-level GET navigation nhưng không trong POST/iframe/img — đủ cho hầu hết cases; SameSite=None: phải có Secure, gửi trong tất cả cross-site requests. CSRF tokens: random token lưu trong session, include trong form hidden field và verify server-side — attacker không thể đọc token vì same-origin policy. Double Submit Cookie: gửi token trong cả cookie và request body/header — server verify chúng match; không cần server-side state. Custom request headers (AJAX): XMLHttpRequest và fetch không thể set custom headers cross-origin mà không qua CORS preflight — thêm X-Requested-With: XMLHttpRequest hoặc Content-Type: application/json tự nhiên ngăn CSRF vì attacker không thể tạo cross-origin request với custom headers. Origin header checking: verify Origin/Referer header match expected domain — không đủ một mình vì có thể bị spoof trong một số cases.",
    q_en: "What are the main differences between authentication and authorization?",
    a_en: "Authentication (AuthN) verifies who you are — confirming identity. Examples: login with username/password, OAuth, biometrics, magic link. The result is knowing the user's identity. Authorization (AuthZ) verifies what you are allowed to do — checking permissions after identity is confirmed. Examples: user can read posts but not delete them; admin can access /admin routes; a team member can view their team's data only. Common patterns: RBAC (Role-Based Access Control) — assign roles (admin, user, moderator) with defined permissions; ABAC (Attribute-Based Access Control) — permissions based on attributes (user's department, resource's owner, time of day); ACL (Access Control List) — explicit list of who can access what resource. In Next.js: handle authentication via middleware/proxy, authorization in Server Components or API routes by checking user role after verifying the session.",
  },
  {
    id: 793,
    category: "Security",
    subcategory: "CSP",
    level: "advanced",
    q: "Content Security Policy (CSP) là gì? Cách cấu hình?",
    a: "CSP (Content Security Policy) là HTTP header whitelist trusted sources cho scripts, styles, images, fonts — defense in depth chống XSS. default-src: fallback cho directives không specify; script-src: chỉ load scripts từ approved sources; style-src, img-src, font-src, connect-src (fetch/XHR), frame-src, form-action. Directives: 'self' (same origin), 'none' (block all), specific domains, 'unsafe-inline' (allow inline — weakens CSP), 'unsafe-eval' (allow eval — dangerous). Strict CSP với nonces: script-src 'nonce-{random}' — mỗi request tạo random nonce, inline scripts chỉ chạy nếu có matching nonce attribute; phòng chống injection tốt hơn domain whitelist. Hash-based: sha256-{base64hash} thay vì nonce cho static inline scripts. Report-Only mode: Content-Security-Policy-Report-Only header — observe violations mà không block, collect reports để debug trước khi enforce. report-uri/report-to: endpoint nhận violation reports dạng JSON — essential để monitor và catch issues sau deploy (ví dụ third-party script bị block). Trusted Types API: ngăn DOM XSS bằng cách enforce typed DOM operations — require-trusted-types-for 'script'. Pitfall: CSP với 'unsafe-inline' và 'unsafe-eval' cho toàn bộ site thực tế vô dụng — common mistake khi copy từ examples mà không hiểu. Bắt đầu với Report-Only, collect violations, fix, rồi enforce.",
    q_en: "What is rate limiting and what strategies exist for implementing it?",
    a_en: "Rate limiting restricts how many requests a client can make in a given time window, protecting against abuse, DoS attacks, and API overuse. Strategies: Fixed window — count requests per time window (e.g., 100 requests per minute); resets at window boundary; Token bucket — users accumulate tokens at a fixed rate; each request costs a token; allows short bursts; Sliding window — tracks request timestamps over a rolling time period; smoother than fixed window; Leaky bucket — requests are processed at a fixed rate; excess requests are queued or dropped. Implementation: In-memory (not recommended for distributed systems — not shared between instances); Redis-based — use INCR + EXPIRE commands; Upstash Rate Limit library (@upstash/ratelimit) — serverless-friendly with Redis backend; API Gateway rate limiting (Vercel, AWS API Gateway, Nginx). Apply rate limiting at the edge (middleware/Vercel Firewall) for better performance and DDoS protection.",
  },
  {
    id: 794,
    category: "Security",
    subcategory: "Authentication",
    level: "intermediate",
    q: "OAuth 2.0 là gì? Luồng Authorization Code flow hoạt động thế nào?",
    a: "OAuth 2.0 là authorization framework — delegated authorization (không phải authentication). Phân biệt: OAuth cho authorization (app có quyền gì), OpenID Connect (OIDC) là layer trên OAuth cho authentication (ai đang login, trả về ID token). Authorization Code flow chi tiết: 1) App redirect user đến Authorization Server với client_id, redirect_uri, scope, state (CSRF protection), code_challenge (PKCE); 2) User authenticate và consent; 3) Auth Server redirect về app với authorization code; 4) App exchange code cho access token qua back-channel request (POST, client_secret không expose trong URL); 5) App dùng access token để call protected APIs; 6) Refresh token để get new access token khi expire. PKCE (Proof Key for Code Exchange): app tạo code_verifier (random string), gửi code_challenge = hash(code_verifier) trong step 1, gửi code_verifier trong step 4 — server verify hash match — ngăn authorization code interception attack, bắt buộc cho public clients (SPA, mobile). Implicit flow: deprecated — trả access token trực tiếp trong URL, exposure risk. Client credentials flow: machine-to-machine, không có user — backend service gọi API. Scopes: granular permissions (read:email, write:calendar) — request chỉ scopes cần thiết. Token storage: access token trong memory (SPA), refresh token trong HttpOnly cookie — không dùng localStorage cho sensitive tokens.",
    q_en: "What is input validation and sanitization?",
    a_en: "Input validation checks whether input meets expected format, type, and constraints. Input sanitization cleans or transforms input to remove potentially dangerous content. Validation: type checking (is it a number?); format validation (is it a valid email?); range validation (is the age between 0 and 120?); required field checking. Libraries: Zod (TypeScript-first schema validation), Joi, Yup, class-validator (for NestJS). Sanitization: trim whitespace; normalize unicode; escape HTML for display; DOMPurify for user-provided HTML (e.g., rich text editor output). Best practices: validate on both client AND server — client validation is UX, server validation is security; never trust client-side validation alone; use allowlists (define what is allowed) rather than blocklists (define what to block); validate early and fail fast; use structured types (Zod schemas) that generate TypeScript types automatically.",
  },
  {
    id: 795,
    category: "Security",
    subcategory: "Authentication",
    level: "intermediate",
    q: "HTTPS và TLS là gì? Tại sao bắt buộc phải dùng?",
    a: "HTTPS là HTTP over TLS (Transport Layer Security) — encrypt toàn bộ communication, verify server identity, đảm bảo data integrity. TLS handshake: Client Hello (supported cipher suites, TLS version) → Server Hello (chosen cipher, certificate) → Client verify certificate với CA (Certificate Authority) → Key exchange (ECDH — không share private key) → Session keys derived → Encrypted communication. Certificate chain: leaf certificate → intermediate CA → root CA (trusted bởi browser/OS); Let's Encrypt cung cấp free certificates tự động renew. HSTS (HTTP Strict Transport Security): Strict-Transport-Security: max-age=31536000; includeSubDomains — browser chỉ kết nối HTTPS trong 1 năm, ngăn SSL stripping attack; HSTS Preload list (chromium preload) — hardcoded trong browsers. TLS 1.3 improvements: 1-RTT handshake (vs TLS 1.2 2-RTT), 0-RTT resumption cho subsequent connections, removed insecure cipher suites. Certificate Transparency: public log của mọi issued certificate — detect unauthorized certificate issuance cho domain của bạn. Mixed content: HTTPS page load HTTP resources — browsers block active mixed content (scripts, XHR), warn về passive (images). Bắt buộc vì: MITM attack prevention, required cho HTTP/2 (browsers only support HTTP/2 over TLS), Service Workers, PWA, getUserMedia, required cho modern browser features; SEO ranking factor; browser 'Not Secure' warning tăng bounce rate.",
    q_en: "What is environment variable security and what are the best practices?",
    a_en: "Environment variables store sensitive configuration (API keys, database passwords, secrets) outside of source code. Best practices: Never commit .env files — add .env, .env.local, .env.*.local to .gitignore; Use .env.example as a template (no real values); Prefix client-side variables — in Next.js, NEXT_PUBLIC_ prefix exposes variables to the browser; all others are server-only; Least privilege — each service should have its own API keys with minimal required permissions; Rotate secrets regularly — especially after team member departures; Use secret management services — AWS Secrets Manager, HashiCorp Vault, Vercel Environment Variables (encrypted at rest); Separate environments — different keys for development/staging/production; Audit access — know who has access to production secrets; Scan for leaked secrets — GitHub secret scanning, GitGuardian detect accidental commits.",
  },
  {
    id: 796,
    category: "Security",
    subcategory: "CORS",
    level: "intermediate",
    q: "CORS là gì? Tại sao browser enforce same-origin policy?",
    a: "Same-origin policy ngăn JavaScript từ một origin (scheme+host+port) đọc response từ origin khác — bảo vệ khỏi CSRF và data theft. CORS cho phép server opt-in cho cross-origin requests qua response headers. Simple requests (GET/POST với basic Content-Type) không cần preflight — browser gửi request và check response headers. Preflight (OPTIONS request): cho non-simple requests (custom headers, PUT/DELETE, Content-Type: application/json) — browser gửi OPTIONS trước, server phải respond với allowed methods/headers trước khi browser gửi actual request. Response headers: Access-Control-Allow-Origin: https://trusted.com (hoặc * cho public APIs nhưng không thể dùng với credentials); Access-Control-Allow-Methods: GET, POST, PUT, DELETE; Access-Control-Allow-Headers: Content-Type, Authorization; Access-Control-Max-Age: 86400 (cache preflight 24h, giảm OPTIONS requests); Access-Control-Allow-Credentials: true (để gửi cookies/auth headers — phải specify exact origin, không dùng *). Common mistakes: add CORS headers ở nginx nhưng app cũng trả CORS headers → duplicate headers → browser reject; thêm * nhưng cần credentials → không work; không handle OPTIONS method trong route → 405 error. CORS chỉ restrict browser requests — Postman, curl, server-to-server không bị ảnh hưởng. Credentials mode: fetch(url, { credentials: 'include' }) gửi cookies; cần server set Access-Control-Allow-Credentials: true và exact origin.",
    q_en: "What are the main performance monitoring tools for web applications?",
    a_en: "Performance monitoring tools fall into two categories: Lab (synthetic) tools — test under controlled conditions; Real User Monitoring (RUM) — collect data from actual users. Lab tools: Lighthouse — in-browser or CI, scores 5 categories (Performance, Accessibility, Best Practices, SEO, PWA); WebPageTest — detailed waterfall, filmstrip, multi-step tests; Chrome DevTools Performance tab — record and analyze runtime performance. RUM tools: Vercel Speed Insights — automatically collects Core Web Vitals from real users; Google Analytics + Web Vitals library; Datadog, New Relic, Sentry Performance — APM with real-user data. Key metrics to track: Core Web Vitals (LCP, CLS, INP), TTFB (Time to First Byte), FCP (First Contentful Paint), TTI (Time to Interactive), bundle sizes, API response times. Set up alerts for metric regressions in production.",
  },
  {
    id: 797,
    category: "Security",
    subcategory: "Headers",
    level: "intermediate",
    q: "Security headers quan trọng trong web application là gì?",
    a: "Security headers là server response headers kiểm soát browser behavior để prevent attacks. Quan trọng nhất: Strict-Transport-Security: max-age=31536000; includeSubDomains (HSTS — force HTTPS, prevent SSL stripping, 1 năm duration); X-Content-Type-Options: nosniff (prevent MIME type sniffing — browser không tự đoán content type khác Content-Type header, ngăn polyglot file attacks); X-Frame-Options: DENY hoặc SAMEORIGIN (prevent clickjacking — page không thể được embed trong iframe của domain khác); CSP frame-ancestors thay thế X-Frame-Options và flexible hơn. Referrer-Policy: no-referrer-when-downgrade (default) hoặc strict-origin-when-cross-origin — control bao nhiêu URL info gửi trong Referer header; no-referrer cho sensitive pages. Permissions-Policy (thay thế Feature-Policy): kiểm soát browser APIs — Permissions-Policy: geolocation=(), camera=(), microphone=() — disable features không dùng đến. X-XSS-Protection: deprecated, browsers modern tự handle — không cần set. Cross-Origin-Opener-Policy: same-origin — isolate browsing context, enable SharedArrayBuffer, required cho high-resolution timers. Cross-Origin-Embedder-Policy: require-corp — cùng với COOP enable process isolation. Helmet.js trong Express: app.use(helmet()) tự động set tất cả headers với sensible defaults, có thể customize per-header. securityheaders.com để test và grade headers của site.",
    q_en: "What is a CDN (Content Delivery Network) and how does it work?",
    a_en: "A CDN is a geographically distributed network of edge servers that cache and serve content from locations close to users. How it works: user requests a resource; DNS routes the request to the nearest edge server (PoP — Point of Presence); if the edge server has the resource cached, it serves it immediately (cache hit); if not, the edge server fetches from the origin server, caches it, and serves it (cache miss); subsequent requests are served from cache. Benefits: lower latency (physical proximity to users), reduced origin server load, better availability (multiple PoPs), DDoS protection (absorbs attack traffic across many servers). CDN providers: Cloudflare, AWS CloudFront, Fastly, Vercel Edge Network (~300ms propagation globally). Best for: static assets (JS, CSS, images, fonts), API responses with appropriate Cache-Control headers, video streaming.",
  },
  {
    id: 798,
    category: "Security",
    subcategory: "Authentication",
    level: "advanced",
    q: "SQL Injection là gì? Cách phòng chống trong Node.js?",
    a: "SQL Injection là attack inject SQL code qua user input để manipulate queries, bypass authentication, extract/delete data, hoặc execute OS commands (xp_cmdshell trong MSSQL). Classic example: username input là ' OR '1'='1' -- → query trở thành SELECT * FROM users WHERE username = '' OR '1'='1' -- ' AND password = '...' — luôn true, bypass login. Second-order injection: input được store vào DB, sau đó dùng trong query khác mà không sanitize — khó phát hiện. Phòng chống chính: parameterized queries/prepared statements — placeholder thay vì string concatenation: db.query('SELECT * FROM users WHERE id = $1', [userId]) — user input không bao giờ được interpret là SQL code. ORM như Prisma, Sequelize, TypeORM tự động parameterize — nhưng raw queries vẫn nguy hiểm nếu dùng template literals: prisma.$queryRaw`SELECT * FROM users WHERE id = ${userId}` (safe với tagged template) vs prisma.$queryRawUnsafe('SELECT * FROM users WHERE id = ' + userId) (unsafe). Stored procedures có thể vẫn bị nếu dùng dynamic SQL bên trong. Input validation là defense in depth nhưng không thể thay thế parameterized queries — validation có thể bị bypass. Principle of least privilege: DB user chỉ có quyền cần thiết — SELECT/INSERT/UPDATE nhưng không DROP TABLE, không xp_cmdshell. WAF (Web Application Firewall) thêm layer bảo vệ nhưng không đủ một mình.",
    q_en: "What is the difference between SSR, SSG, and ISR in Next.js?",
    a_en: "SSR (Server-Side Rendering): page is rendered on the server on every request. Data is always fresh. Suitable for: user-specific content, real-time data, pages requiring authentication. Slower because server computation happens per request. SSG (Static Site Generation): page is pre-rendered at build time. Extremely fast — served directly from CDN. Suitable for: marketing pages, blog posts, documentation — content that rarely changes. ISR (Incremental Static Regeneration): hybrid of SSG and SSR. Page is pre-rendered at build time and cached. After the revalidate period expires, the next request triggers background regeneration. Suitable for: product pages, news articles — relatively static but needing periodic updates. In Next.js App Router: default is SSR (Server Components); add cache: 'force-cache' or export const revalidate = N for ISR/SSG behavior; use 'use cache' directive with Cache Components for fine-grained control.",
  },
  {
    id: 799,
    category: "Security",
    subcategory: "Authentication",
    level: "advanced",
    q: "Cookie security: HttpOnly, Secure, SameSite attributes là gì?",
    a: "Cookie security attributes kiểm soát cookie exposure và transmission. HttpOnly: cookie không accessible qua document.cookie hay JavaScript — ngăn XSS đánh cắp session token; vẫn tự động gửi trong HTTP requests. Secure: cookie chỉ gửi qua HTTPS — ngăn network sniffing, bắt buộc trong production. SameSite: Strict (không gửi cookie trong bất kỳ cross-site request nào — kể cả click link từ email đến site của bạn; rất secure nhưng friction cao cho federated services); Lax (gửi trong top-level navigation GET nhưng không trong img/iframe/fetch cross-site — good balance, default Chrome từ 2020); None (gửi tất cả cross-site — phải kết hợp với Secure, dùng cho third-party cookies như OAuth flows, embedded widgets). Best practice cho session cookies: Set-Cookie: sessionId=abc; HttpOnly; Secure; SameSite=Lax; Path=/; Max-Age=3600. Cookie prefixes tăng security: __Secure- prefix (phải có Secure flag, HTTPS only); __Host- prefix (phải có Secure, không có Domain, Path=/ — strongest, pinned to host). Max-Age vs Expires: Max-Age (seconds relative) ưu tiên hơn Expires (absolute datetime) vì không phụ thuộc client clock. Session vs Persistent cookies: session cookie (không có Max-Age/Expires) bị xóa khi browser close; persistent có expiry. Third-party cookies: đang bị phase out bởi browsers — Privacy Sandbox, Storage Access API là alternatives.",
    q_en: "What is Web Accessibility (a11y) and what are the key standards?",
    a_en: "Web Accessibility ensures websites are usable by people with disabilities. WCAG (Web Content Accessibility Guidelines) from W3C defines standards at three levels: A (minimum), AA (standard target), AAA (enhanced). Key principles — POUR: Perceivable — content is available to all senses (alt text for images, captions for video); Operable — all functionality accessible via keyboard; Navigable with skip links and clear headings; Understandable — clear language, consistent navigation, helpful error messages; Robust — compatible with assistive technologies (screen readers). Key practices: Semantic HTML — use <button> not <div onclick>, use <nav>, <main>, <h1>-<h6> correctly; ARIA attributes — aria-label, aria-describedby, role for complex widgets; Color contrast — minimum 4.5:1 ratio for normal text; Focus management — visible focus indicators, logical tab order; Testing tools: axe DevTools, Lighthouse accessibility audit, NVDA/VoiceOver screen readers.",
  },
  {
    id: 800,
    category: "Security",
    subcategory: "Security",
    level: "advanced",
    q: "Dependency security: cách quản lý vulnerabilities trong npm packages?",
    a: "Dependency security quản lý supply chain risks — dependencies là attack vector phổ biến (log4shell, event-stream incident, ua-parser-js hijack). npm audit: scan known vulnerabilities trong CVE databases, npm audit --json để parse programmatically; npm audit fix tự fix patch versions, --force cho major versions (cẩn thận breaking changes). Mức severity: critical/high cần fix ngay; moderate/low assess risk trước. Dependabot (GitHub built-in): tự động tạo PRs để update dependencies với changelogs — configure trong .github/dependabot.yml với schedule và allowed update types. Renovate (Mend): alternative mạnh hơn Dependabot, grouping updates, auto-merge với passing CI, monorepo support. Snyk: deeper scanning (không chỉ CVEs), license compliance, container scanning, IaC scanning, IDE plugins. Socket.dev: phân tích behavior của packages (không chỉ known CVEs) — detect typosquatting, suspicious code patterns, permission escalation. Nguyên tắc: lock file (package-lock.json, pnpm-lock.yaml) phải commit — reproducible installs; không run npm install --legacy-peer-deps trong production bừa bãi; review package trước install (bundlephobia size, npm downloads, GitHub activity, last publish); prefer packages ít dependencies (dependency tree nhỏ = attack surface nhỏ). Software Bill of Materials (SBOM): generate list of all dependencies và versions — compliance requirement cho enterprise và government. npm publish với provenance — verify package genuinely published từ specific CI run.",
    q_en: "What are Web Workers and when should you use them?",
    a_en: "Web Workers allow running JavaScript in background threads, separate from the main thread. This prevents heavy computations from blocking the UI. Types: Dedicated Worker — used by a single script; Shared Worker — shared across multiple tabs/scripts; Service Worker — special type for offline/caching (see separate topic). Use cases: CPU-intensive computations — image processing, data compression, complex algorithms, parsing large datasets; Real-time data processing — processing WebSocket streams; Background tasks — indexing search data, pre-fetching and pre-processing data. Communication via postMessage() and event listeners — structured cloning for data transfer. Limitations: no DOM access from workers; communication overhead (serialization/deserialization of messages). In React: workers are typically initialized in useEffect and cleaned up in the return function. Libraries: comlink (Google) simplifies Worker communication with an RPC-like API.",
  },
];
